# Clean REDUCTION zero-builder fixture

This control-notation score deliberately yields zero beam-origin builders in
its first system while preserving Java-exact raw HEADS (58). Java treats the
empty SIDES and STUMPS loops as successful terminals, so native STEMS and
REDUCTION must continue into the second system rather than reject the page.

The tracked raster was rendered with Verovio 6.2.1, Bravura, and librsvg. Run
`./regenerate.sh` to reproduce this SHA-256 digest:

```text
184f781759b4cd637bcfa710daef873c4b64c10c71b4b02b611b839f6415e790  coverage-2x.png
```
