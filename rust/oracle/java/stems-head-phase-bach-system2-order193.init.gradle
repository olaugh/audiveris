// SPDX-License-Identifier: AGPL-3.0-or-later

gradle.projectsEvaluated {
    def app = gradle.rootProject.findProject(':app')
    if (app == null) return

    def outputDir = app.layout.buildDirectory.dir('rust-head-phase-bach-system2-order193/classes')
    app.tasks.register('compileStemsHeadPhaseBachSystem2Order193Probe', JavaCompile) {
        source = file(providers.gradleProperty('order193Probe').get())
        classpath = app.sourceSets.test.runtimeClasspath
        destinationDirectory = outputDir
        options.release.set(25)
    }

    app.tasks.register('stemsHeadPhaseBachSystem2Order193Probe', JavaExec) {
        dependsOn 'compileStemsHeadPhaseBachSystem2Order193Probe'
        classpath = app.files(outputDir) + app.sourceSets.test.runtimeClasspath
        mainClass = 'org.audiveris.omr.rustport.StemsHeadPhaseOneBachSystem2Order193Probe'
        workingDir = app.projectDir
        maxHeapSize = '16g'
        jvmArgs '-XX:+UseG1GC'
        systemProperty 'logback.configurationFile',
                "${gradle.rootProject.projectDir}/rust/oracle/java/logback-quiet.xml"
        args = [providers.gradleProperty('phaseOneOrder193Page').get()]
    }
}
