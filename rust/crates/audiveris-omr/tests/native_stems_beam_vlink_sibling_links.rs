//! Independent gate scaffold for `BeamLinker.VLinker.linkSiblings`.
//!
//! Boundary 16 begins immediately after the selected B-linker's shared
//! `linked` cell was assigned by boundary 15.  It executes the complete
//! sibling-beam loop, including graph callbacks and sibling-linker flag
//! writes, and stops before the first head-relation-loop read.  The model in
//! this file is deliberately independent from production so that the frozen
//! Java rows and the native transaction have two separately reviewable
//! projections.

use std::{
    cmp::Ordering,
    collections::{BTreeMap, BTreeSet},
    path::PathBuf,
};

use audiveris_image::{beam_structure::Segment, section::Bounds};
use audiveris_omr::{
    head_scanner_slices::JavaRectangle,
    native_headers::recognize_native_headers,
    native_heads::recognize_native_heads,
    native_ledgers::recognize_native_ledgers,
    native_sig::{
        NativeSigBounds, NativeSigEdge, NativeSigHeadStemPayload, NativeSigInterKind,
        NativeSigRelationKind, NativeSigRelationOrigin, NativeSigSupport, NativeSigVertex,
        NativeSigVertexId, assemble_native_sig,
    },
    native_stem_seeds::recognize_native_stem_seeds,
    native_stems_beam_builders::{
        NativeStemsBeamBuilder, NativeStemsBeamBuilderItemKind, NativeStemsBeamBuilderTargetRef,
        NativeStemsModeledCanonicalGlyph,
    },
    native_stems_beam_link_plans::NativeStemsBeamLinkPlanAttempt,
    native_stems_beam_scheduler::{
        NativeStemsBeamAwaitingVLinkTransaction, NativeStemsBeamCompletedVLinkEvidence,
        NativeStemsBeamPlanRef, NativeStemsBeamSchedulerEvent, NativeStemsBeamSchedulerPass,
        NativeStemsBeamSchedulerResumeStatus, NativeStemsBeamSchedulerStatus,
        NativeStemsBeamSchedulerStumpsStatus, NativeStemsBeamWorklistSnapshot,
        resume_native_stems_beam_scheduler_after_transaction,
    },
    native_stems_beam_sides::{
        NativeStemsBeamSidesCarrier, NativeStemsBeamSidesContext,
        advance_native_stems_beam_sides_transaction_from_first_stems_bridge,
        advance_native_stems_beam_stumps_transaction_from_first_stems_bridge,
        advance_native_stems_head_continuation_c_link,
        advance_native_stems_head_continuation_c_link_order18,
        advance_native_stems_head_continuation_c_link_order20,
        advance_native_stems_head_continuation_c_link_order27,
        advance_native_stems_head_continuation_c_link_order34,
        advance_native_stems_head_continuation_c_link_order36,
        advance_native_stems_head_existing_stem_c_link_order57,
        advance_native_stems_head_existing_stem_retry_order21,
        advance_native_stems_head_existing_stem_retry_order22,
        advance_native_stems_head_existing_stem_retry_order37,
        advance_native_stems_head_existing_stem_retry_order38,
        advance_native_stems_head_existing_stem_retry_order39,
        advance_native_stems_head_existing_stem_retry_order40,
        advance_native_stems_head_existing_stem_retry_order41,
        advance_native_stems_head_existing_stem_retry_order42,
        advance_native_stems_head_existing_stem_retry_order43,
        advance_native_stems_head_existing_stem_retry_order44,
        advance_native_stems_head_existing_stem_retry_order45,
        advance_native_stems_head_existing_stem_retry_order46,
        advance_native_stems_head_existing_stem_retry_order47,
        advance_native_stems_head_existing_stem_retry_order48,
        advance_native_stems_head_existing_stem_retry_order49,
        advance_native_stems_head_existing_stem_retry_order51,
        advance_native_stems_head_existing_stem_retry_order52,
        advance_native_stems_head_existing_stem_retry_order53,
        advance_native_stems_head_existing_stem_retry_order54,
        advance_native_stems_head_existing_stem_retry_order55,
        advance_native_stems_head_existing_stem_retry_order56,
        advance_native_stems_head_existing_stem_retry_order58,
        advance_native_stems_head_existing_stem_retry_order59,
        advance_native_stems_head_existing_stem_retry_order62,
        advance_native_stems_head_existing_stem_retry_order63,
        advance_native_stems_head_existing_stem_retry_order64,
        advance_native_stems_head_existing_stem_retry_order65,
        advance_native_stems_head_existing_stem_retry_order66,
        advance_native_stems_head_existing_stem_retry_order69,
        advance_native_stems_head_existing_stem_retry_order71,
        advance_native_stems_head_existing_stem_retry_order74,
        advance_native_stems_head_existing_stem_retry_order76,
        advance_native_stems_head_existing_stem_retry_order77,
        advance_native_stems_head_existing_stem_retry_order78,
        advance_native_stems_head_existing_stem_retry_order79,
        advance_native_stems_head_existing_stem_retry_order80,
        advance_native_stems_head_existing_stem_retry_order81,
        advance_native_stems_head_existing_stem_retry_order82,
        advance_native_stems_head_existing_stem_retry_order83,
        advance_native_stems_head_existing_stem_retry_order84,
        advance_native_stems_head_existing_stem_retry_order85,
        advance_native_stems_head_existing_stem_retry_order86,
        advance_native_stems_head_existing_stem_retry_order87,
        advance_native_stems_head_existing_stem_retry_order88,
        advance_native_stems_head_existing_stem_retry_order89,
        advance_native_stems_head_existing_stem_retry_order90,
        advance_native_stems_head_existing_stem_retry_order91,
        advance_native_stems_head_existing_stem_retry_order92,
        advance_native_stems_head_existing_stem_retry_order94,
        advance_native_stems_head_existing_stem_retry_order95,
        advance_native_stems_head_existing_stem_retry_order96,
        advance_native_stems_head_multi_head_reuse_c_link_order67,
        advance_native_stems_head_multi_head_reuse_c_link_order70,
        advance_native_stems_head_multi_head_reuse_c_link_order73,
        advance_native_stems_head_open_frontier_order50,
        advance_native_stems_head_open_frontier_order60,
        advance_native_stems_head_open_frontier_order61,
        advance_native_stems_head_open_frontier_order68,
        advance_native_stems_head_open_frontier_order75,
        advance_native_stems_head_right_side_reuse_c_link_order93,
        advance_native_stems_head_single_head_reuse_c_link_order72,
        advance_native_stems_head_single_item_c_link, begin_native_stems_head_linking_phase1,
        continue_native_stems_beam_sides_carrier_into_stumps,
        continue_native_stems_head_linking_phase1,
        drive_native_stems_beam_stumps_from_first_stems_bridge,
        remove_native_stems_beam_competing_hook_and_resume,
    },
    native_stems_beam_stumps::NativeStemsBeamSource,
    native_stems_beam_vlink_b_linker_flag::{
        NativeStemsBeamVLinkBLinkerFlagState,
        apply_native_stems_beam_vlink_b_linker_flag_transaction,
    },
    native_stems_beam_vlink_base_apply::{
        NativeStemsBeamBeamIncidentRead, NativeStemsBeamBeamIncidentRule,
        NativeStemsBeamBeamInterIndexBootstrapEntry, NativeStemsBeamGroupRuntimeState,
        NativeStemsBeamIncidentDirection, NativeStemsBeamIncidentOpposite,
        NativeStemsBeamQueryRelationKind, NativeStemsBeamSheetEditState,
        NativeStemsBeamSigListenerTopology, NativeStemsBeamSigRelationKind,
        NativeStemsBeamVLinkBaseRolloverAuthority, NativeStemsBeamVLinkBeamRuntimeState,
        apply_native_stems_beam_vlink_base_transaction_to_native_sig,
        roll_native_stems_beam_vlink_base_apply_state,
    },
    native_stems_beam_vlink_head_links::{
        NativeStemsBeamHeadLinkBranch, NativeStemsBeamHeadSLinkerRef,
        apply_native_stems_beam_vlink_head_transaction_to_native_sig,
        initialize_native_stems_beam_s_linker_cells,
    },
    native_stems_beam_vlink_outer_b_linker::apply_native_stems_beam_outer_and_resume_transaction,
    native_stems_beam_vlink_reuse_check::{
        NativeStemsBeamHeadStemLookupEvidence, NativeStemsBeamReuseDisposition,
        NativeStemsBeamReuseEntryObservation, NativeStemsBeamVLinkReuseLiveEvaluation,
        evaluate_native_stems_beam_vlink_reuse_check,
        project_native_stems_beam_vlink_reuse_live_state,
    },
    native_stems_beam_vlink_sibling_links::{
        NativeStemsBeamSiblingAppendedRelation, NativeStemsBeamSiblingBLinkerCell,
        NativeStemsBeamSiblingBeamAbnormalTrace, NativeStemsBeamSiblingBeamIncidentRelation,
        NativeStemsBeamSiblingBeamIncidentScan, NativeStemsBeamSiblingBranch,
        NativeStemsBeamSiblingBuilderAction, NativeStemsBeamSiblingBuilderItemRead,
        NativeStemsBeamSiblingBuilderLinkerIdentity, NativeStemsBeamSiblingBuilderLinkerRead,
        NativeStemsBeamSiblingBuilderLookupRow, NativeStemsBeamSiblingBuilderLookupScan,
        NativeStemsBeamSiblingBuilderLookupState, NativeStemsBeamSiblingBuilderLookupTiming,
        NativeStemsBeamSiblingBuilderSourceRead, NativeStemsBeamSiblingGeometryTrace,
        NativeStemsBeamSiblingGlyphIdentity, NativeStemsBeamSiblingGroupMemberTrace,
        NativeStemsBeamSiblingGroupRelation, NativeStemsBeamSiblingGroupRuntimeState,
        NativeStemsBeamSiblingGroupScan, NativeStemsBeamSiblingGroupTarget,
        NativeStemsBeamSiblingGroupTargetEvidence, NativeStemsBeamSiblingLiveBeam,
        NativeStemsBeamSiblingPairClassRead, NativeStemsBeamSiblingPairRelation,
        NativeStemsBeamSiblingPairScan, NativeStemsBeamSiblingQueryProvenance,
        NativeStemsBeamSiblingRelationObjectIdentity, NativeStemsBeamSiblingSourceOutgoingRelation,
        NativeStemsBeamSiblingStemIncidentRelation, NativeStemsBeamSiblingStemIncidentScan,
        NativeStemsBeamSiblingStepCertificate, NativeStemsBeamVLinkSiblingLinksCertificate,
        NativeStemsBeamVLinkSiblingLinksOperation, NativeStemsBeamVLinkSiblingLinksOutcome,
        NativeStemsBeamVLinkSiblingLinksState, NativeStemsBeamVLinkSiblingLinksTransaction,
        apply_native_stems_beam_vlink_sibling_links_transaction,
        apply_native_stems_beam_vlink_sibling_transaction_to_native_sig,
        initialize_native_stems_beam_b_linker_cells,
    },
    native_stems_beam_vlink_transaction::{
        NativeStemsBeamCreateStemDisposition, NativeStemsBeamCreatedStemGeometry,
        NativeStemsBeamFixedGlyphContent, NativeStemsBeamGlyphRegistrationAction,
        NativeStemsBeamGlyphRegistryBootstrapEntry, NativeStemsBeamKnownSystemStem,
        NativeStemsBeamPersistentIdState, NativeStemsBeamRegistryAuthority,
        NativeStemsBeamStemGrade, NativeStemsBeamSystemStemAuthorityProof,
        NativeStemsBeamSystemStemTransactionState, NativeStemsFirstGlyphFingerprint,
        NativeStemsFirstGlyphIndexBridge, NativeStemsFirstGlyphIndexSnapshot,
        NativeStemsFirstGlyphSnapshotEntry, apply_native_stems_beam_vlink_create_stem_transaction,
        materialize_native_stems_beam_frontier_candidate,
        prepare_native_stems_beam_vlink_frontier_state,
        prepare_native_stems_beam_vlink_frontier_state_from_first_stems_bridge,
    },
    native_stems_beam_vlinkers::{NativeStemsBeamBLinkerRef, NativeStemsBeamVLinkerRef},
    recognize::{recognize_grid_lines, recognize_native_beams_with_stem_seeds},
    stems_step::{
        NativeBeamPortion, NativeStemHeadSide, NativeStemLine, NativeStemPoint,
        NativeStemVerticalSide,
    },
};

#[path = "common/b15_hydration.rs"]
mod b15_hydration;

const FIXTURE_SCHEMA: &str = "# schema: stems-beam-vlink-sibling-links-v1";
const FIXTURE_OVERRIDE_ENV: &str = "AUDIVERIS_B16_SIBLING_LINKS_FIXTURE";
const CORPUS_PAGES: [(&str, &str); 8] = [
    ("chula", "chula.png"),
    ("allegretto", "allegretto.png"),
    ("batuque", "batuque.png"),
    ("carmen", "carmen.png"),
    ("cucaracha", "cucaracha.png"),
    ("hove", "hove.png"),
    ("zizi", "zizi.png"),
    ("BachInvention5", "BachInvention5.jpg"),
];
const BOUNDARY_FIFTEEN_MANIFEST_PATH: &str =
    "rust/oracle/stems-beam-vlink-b-linker-flag-manifest.txt";
const BOUNDARY_FIFTEEN_MANIFEST_SHA256: &str =
    "c7032ac4871188ef0cf48ac63d99996e78a0e163bf1470d3be84c5e9b10d1d92";
const FIRST_STEMS_GLYPH_REGISTRY_SHA256: &str =
    "7311235c38b0667b249749a0e7e6ade278ce92a05ca4200529eb67d73bf1de1c";
const FIRST_STEMS_GLYPH_REGISTRY_LINES: usize = 1_658;
const FIRST_STEMS_GLYPH_REGISTRY_BYTES: usize = 226_926;
const FIRST_STEMS_GLYPH_ACTIVE_SHA256: &str =
    "dae5de3eabc2fb8d19613abcc8b24f4d865bcd55ca1ec6533faae30792692642";
const FIRST_STEMS_GLYPH_ORIGINALS_SHA256: &str =
    "38f4861501e8099dedcb36b0ff9cf615f156ec5b929dffe31c51906a15362af0";
const BEAM_INTER_INDEX_SHA256: &str =
    "fde4daebadc5c7158fa8e83dcbd4ac0ca6381c614876b6fe48408ec2e245064e";
const BEAM_INTER_INDEX_LINES: usize = 52;
const BEAM_INTER_INDEX_BYTES: usize = 6_259;
const EXECUTED_BASE_BEAM_SIG_ORDINALS: [usize; 16] = [
    12, 15, 16, 19, 20, 21, 22, 28, 29, 30, 31, 32, 33, 34, 35, 36,
];

fn first_stems_glyph_bridge(
    registry_text: &str,
    modeled_registry: &[NativeStemsModeledCanonicalGlyph],
    visible_modeled_count: usize,
) -> NativeStemsFirstGlyphIndexBridge {
    assert_eq!(registry_text.len(), FIRST_STEMS_GLYPH_REGISTRY_BYTES);
    assert_eq!(
        registry_text.lines().count(),
        FIRST_STEMS_GLYPH_REGISTRY_LINES
    );
    assert_eq!(
        sha256_hex(registry_text.as_bytes()),
        FIRST_STEMS_GLYPH_REGISTRY_SHA256
    );
    assert!(
        registry_text
            .lines()
            .any(|line| line == "# schema: stems-beam-glyph-registry-v1")
    );
    assert_eq!(visible_modeled_count, 1_058);
    assert!(modeled_registry.len() > visible_modeled_count);

    let page_rows = registry_text
        .lines()
        .filter(|line| line.starts_with("stemsbeamglyphregistrypage "))
        .collect::<Vec<_>>();
    let [page] = page_rows.as_slice() else {
        panic!(
            "first-STEMS registry page-row cardinality: {}",
            page_rows.len()
        );
    };
    let page_tokens = page.split_ascii_whitespace().collect::<Vec<_>>();
    assert_eq!(
        page_tokens,
        [
            "stemsbeamglyphregistrypage",
            "active",
            "1650",
            "originals",
            "1650",
            "entries",
            "1650",
            "activeHash",
            FIRST_STEMS_GLYPH_ACTIVE_SHA256,
            "originalsHash",
            FIRST_STEMS_GLYPH_ORIGINALS_SHA256,
        ]
    );

    let mut visible_by_content = modeled_registry[..visible_modeled_count]
        .iter()
        .map(|glyph| {
            assert!(glyph.modeled_canonical_ordinal < visible_modeled_count);
            (
                format!(
                    "g:{}:{}:{}:{}:{}",
                    glyph.bounds.x,
                    glyph.bounds.y,
                    glyph.bounds.width,
                    glyph.bounds.height,
                    b15_hydration::run_table_digest(&glyph.run_table)
                ),
                glyph.modeled_canonical_ordinal,
            )
        })
        .collect::<BTreeMap<_, _>>();
    assert_eq!(visible_by_content.len(), visible_modeled_count);

    let mut entries = Vec::new();
    let mut ids = BTreeSet::new();
    let mut fingerprints = BTreeSet::new();
    for line in registry_text
        .lines()
        .filter(|line| line.starts_with("stemsbeamglyphregistryentry "))
    {
        let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
        let [
            "stemsbeamglyphregistryentry",
            "id",
            glyph_id,
            "active",
            "true",
            "content",
            content,
        ] = tokens.as_slice()
        else {
            panic!("malformed first-STEMS registry entry: {line}");
        };
        let glyph_id = glyph_id.parse::<i32>().expect("positive registry glyph ID");
        assert!((1..=2_339).contains(&glyph_id));
        assert!(
            ids.insert(glyph_id),
            "duplicate registry glyph ID {glyph_id}"
        );
        let parts = content.split(':').collect::<Vec<_>>();
        let ["g", x, y, width, height, run_table_sha256] = parts.as_slice() else {
            panic!("malformed first-STEMS glyph fingerprint: {content}");
        };
        let fingerprint = NativeStemsFirstGlyphFingerprint {
            bounds: Bounds {
                x: x.parse().expect("registry glyph x"),
                y: y.parse().expect("registry glyph y"),
                width: width.parse().expect("registry glyph width"),
                height: height.parse().expect("registry glyph height"),
            },
            run_table_sha256: (*run_table_sha256).to_owned(),
        };
        assert!(
            fingerprints.insert((*content).to_owned()),
            "duplicate registry glyph content {content}"
        );
        entries.push(NativeStemsFirstGlyphSnapshotEntry {
            glyph_id,
            active_in_index: true,
            // The page row says all 1,650 entries are live originals, and all
            // 1,650 individual entries are active at this exact baseline.
            live_original: true,
            fingerprint,
            modeled_canonical_ordinal: visible_by_content.remove(*content),
        });
    }
    assert_eq!(entries.len(), 1_650);
    assert!(
        visible_by_content.is_empty(),
        "visible native glyph lacks persistent binding"
    );
    assert_eq!(
        entries
            .iter()
            .filter(|entry| entry.modeled_canonical_ordinal.is_some())
            .count(),
        visible_modeled_count
    );
    assert_eq!(
        entries
            .iter()
            .filter(|entry| entry.modeled_canonical_ordinal.is_none())
            .count(),
        592
    );

    NativeStemsFirstGlyphIndexBridge::from_snapshot(
        NativeStemsFirstGlyphIndexSnapshot {
            system_id: 1,
            persistent_ids: NativeStemsBeamPersistentIdState {
                sheet_last_id: 2_339,
                glyph_index_last_id: 2_339,
                inter_index_last_id: 2_339,
            },
            union_size: 1_650,
            active_count: 1_650,
            live_original_count: 1_650,
            active_sha256: FIRST_STEMS_GLYPH_ACTIVE_SHA256.to_owned(),
            live_original_sha256: FIRST_STEMS_GLYPH_ORIGINALS_SHA256.to_owned(),
            visible_modeled_count,
            entries,
        },
        modeled_registry,
    )
    .expect("validated one-time first-STEMS GlyphIndex bridge")
}

fn glyph_bootstrap_for_attempt(
    attempt: &NativeStemsBeamLinkPlanAttempt,
    registry_text: &str,
) -> Vec<NativeStemsBeamGlyphRegistryBootstrapEntry> {
    attempt
        .glyphs
        .iter()
        .map(|glyph| {
            let content_key = format!(
                "g:{}:{}:{}:{}:{}",
                glyph.bounds.x,
                glyph.bounds.y,
                glyph.bounds.width,
                glyph.bounds.height,
                b15_hydration::run_table_digest(&glyph.structural_key.run_table)
            );
            let matches = registry_text
                .lines()
                .filter(|line| line.starts_with("stemsbeamglyphregistryentry "))
                .filter_map(|line| {
                    let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
                    let value = |name: &str| {
                        tokens
                            .iter()
                            .position(|token| *token == name)
                            .and_then(|index| tokens.get(index + 1))
                            .copied()
                    };
                    (value("content") == Some(content_key.as_str())).then(|| {
                        (
                            value("id").expect("registry id").parse::<i32>().unwrap(),
                            value("active") == Some("true"),
                        )
                    })
                })
                .collect::<Vec<_>>();
            let [(glyph_id, active_in_index)] = matches.as_slice() else {
                panic!("selected glyph bootstrap cardinality: {}", matches.len());
            };
            NativeStemsBeamGlyphRegistryBootstrapEntry {
                canonical_alias: usize::try_from(*glyph_id).expect("positive glyph ID"),
                glyph_id: *glyph_id,
                content: NativeStemsBeamFixedGlyphContent {
                    bounds: glyph.bounds,
                    weight: glyph.weight,
                    run_table: glyph.structural_key.run_table.clone(),
                },
                active_in_index: *active_in_index,
                strongly_retained: *active_in_index,
            }
        })
        .collect()
}

const BOUNDARY_FIFTEEN_GATE_PATH: &str =
    "rust/crates/audiveris-omr/tests/native_stems_beam_vlink_b_linker_flag.rs";
const BOUNDARY_FIFTEEN_GATE_SHA256: &str =
    "9085f461c143615f47659d3b5f0a760ad9bdfc0098084ef6e376d8a32afe61b6";
const BOUNDARY_FIFTEEN_FIXTURE_PATH: &str = "rust/oracle/stems-beam-vlink-b-linker-flag-chula.txt";
const BOUNDARY_FIFTEEN_FIXTURE_SHA256: &str =
    "85681437af5e7a5b3c5fc220fe7ced7299516b9de8c4d95a6c651dd5ebf926d6";
const PROBE_SOURCE_PATH: &str = "rust/oracle/java/StemsBeamVLinkSiblingLinksProbe.java";
const RUNNER_SOURCE_PATH: &str = "rust/oracle/java/run-stems-beam-vlink-sibling-links.sh";
const MANIFEST_SCHEMA: &str = "# schema: stems-beam-vlink-sibling-links-manifest-v1";
const MANIFEST_PATH: &str = "rust/oracle/stems-beam-vlink-sibling-links-manifest.txt";
const MANIFEST_OVERRIDE_ENV: &str = "AUDIVERIS_B16_SIBLING_LINKS_MANIFEST";
const MANIFEST_ENTRY_LABEL: &str = "stemsbeamvlinksiblinglinksmanifestentry";
const MANIFEST_SUMMARY_LABEL: &str = "stemsbeamvlinksiblinglinksmanifestsummary";
const MANIFEST_SHA256: &str = "6dcca78c13facf7fa9ee29506eab2961d1410babf396930724dce16f5474e29d";
const MANIFEST_LINES: usize = 10;
const MANIFEST_BYTES: usize = 31_471;
const MANIFEST_BODY_SHA256: &str =
    "c5d44bf655814aac1a297d4ad67fe401291449e231d581d11c812e197ef0fba0";
const MANIFEST_BODY_LINES: usize = 9;
const MANIFEST_BODY_BYTES: usize = 23_218;
const NORMALIZED_CORPUS_SHA256: &str =
    "c6a62f9b98ce55eda2bd142b083a2ff6b14d08dab6b1a2ce3c1a0d643d5efd66";
const NORMALIZED_CORPUS_LINES: usize = 717;
const NORMALIZED_CORPUS_BYTES: usize = 580_329;
const SPLIT_FIXTURE_LINES: usize = 789;
const SPLIT_FIXTURE_BYTES: usize = 654_858;

const FIXTURE_HEADER: &[&str] = &[
    "# Java Audiveris 5.11 (Temurin JDK 25.0.3+9 LTS) beam VLink sibling-links oracle.",
    FIXTURE_SCHEMA,
    "# Frozen scheduler/expand/createStem/reuse-check/base-apply/B-linker-flag predecessors are replayed and joined.",
    "# Group members follow outgoing Containment insertion order then Java stable top-down Double.compare sorting.",
    "# Siblings execute serially: glyph skip, directed duplicate scan, lazy shorter geometry, edge callback, item lookup, flag write.",
    "# Fresh BeamStem callbacks require exhaustive zero ChordStem matches in compact v1.",
    "# Java exception cases are envelope-only evidence and are not production-equivalent transactions.",
    "# Stop is after linkSiblings returns and immediately before the head-relation entry-set loop.",
];

const COMMON_FIELDS: &[&str] = &["system", "plan", "scope", "case"];
const PAGE_FIELDS: &[&str] = &[
    "systems",
    "schedulerFixtureSha256",
    "expandFixtureSha256",
    "createStemFixtureSha256",
    "reuseCheckFixtureSha256",
    "baseApplyFixtureSha256",
    "bLinkerFlagFixtureSha256",
    "executionMode",
    "groupOrder",
    "incidentOrder",
    "headless",
    "methodDispatch",
    "stop",
];
const PREDECESSOR_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "join",
    "b15TransactionRows",
    "b15TransactionEvidenceSha256",
    "b15ResultRowSha256",
    "b15GuardRowSha256",
    "b15SummaryRowSha256",
    "predecessorTerminal",
    "applyReturn",
    "supportGrade",
    "stemAlias",
    "stemInterId",
    "baseBeamAlias",
    "targetBAlias",
    "triggeringVAlias",
    "targetBLinked",
];
const BASELINE_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "baseBeamAlias",
    "baseBeamClass",
    "baseBeamInterId",
    "baseBeamVertexOrdinal",
    "baseBeamGlyphIdentity",
    "baseBeamGlyph",
    "baseBeamMedian",
    "baseBeamHeight",
    "baseBeamAbnormal",
    "stemAlias",
    "stemInterId",
    "stemMedian",
    "stemAbnormal",
    "cachedMedianSameIdentity",
    "refPt",
    "yDir",
    "skewedVertical",
    "groupAlias",
    "groupClass",
    "groupInterId",
    "groupVertexOrdinal",
    "groupRemoved",
    "groupVip",
    "groupAbnormal",
    "groupStateHashBefore",
    "groupObjectStateHash",
    "groupOutgoingScanned",
    "groupQueryProvenanceSha256",
    "groupMembers",
    "selectedBeforeBaseRemoval",
    "baseRemoved",
    "siblings",
    "maxBeamSideDx",
    "maxShorterRatio",
    "interline",
    "xInGapMaximum0",
    "maxDxRint",
    "baseCross",
    "baseLength",
    "supportGradeSource",
    "supportGradeRead",
    "supportGrade",
    "graphVertices",
    "graphEdgesBefore",
    "graphVertexHashBefore",
    "graphEdgeHashBefore",
    "listenerTopologyHash",
    "soleSigListener",
    "stemIncidentState",
    "stemIncidentRows",
    "chordStemMatches",
    "stemIncidentHash",
    "builderItems",
    "builderItemsHash",
    "arenaStateHashBefore",
    "relationInputHashBefore",
];
const GROUP_MEMBER_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "groupOutgoingOrdinal",
    "graphRelationIdentity",
    "relationObjectIdentity",
    "relationClass",
    "containmentMatch",
    "targetAlias",
    "targetRuntimeClass",
    "targetReadByGetMembers",
    "targetEvidence",
    "targetInterId",
    "targetVertexOrdinal",
    "memberOrdinal",
    "beamRuntimeClass",
    "beamInterId",
    "interIndexOrdinal",
    "interIndexObjectMatches",
    "interIndexIdMatches",
    "sigMembership",
    "sigSystemId",
    "beamRemoved",
    "beamVip",
    "beamAbnormal",
    "beamGroupVertexOrdinal",
    "beamGroupStateHash",
    "beamGroupObjectStateHash",
    "median",
    "height",
    "glyphIdentity",
    "glyph",
    "verticalCross",
    "leftLimit",
    "rightLimit",
    "inclusiveLeft",
    "inclusiveRight",
    "selected",
    "sortedOrdinal",
    "baseIdentity",
    "removeAction",
];
const SIBLING_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "sortedOrdinal",
    "beamAlias",
    "beamClass",
    "beamInterId",
    "beamVertexOrdinal",
    "glyphIdentity",
    "glyph",
    "baseGlyphSameIdentity",
    "pairState",
    "sourceOutgoingScanned",
    "sourceOutgoingProvenanceSha256",
    "pairRows",
    "pairProvenanceSha256",
    "firstBeamStemIdentity",
    "branch",
];
const SOURCE_OUTGOING_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "sourceOutgoingOrdinal",
    "graphRelationIdentity",
    "relationObjectIdentity",
    "runtimeClass",
];
const PAIR_RELATION_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "pairOrdinal",
    "sourceOutgoingOrdinal",
    "graphRelationIdentity",
    "relationObjectIdentity",
    "runtimeClass",
    "classRead",
    "matches",
    "action",
];
const GEOMETRY_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "stemMedian",
    "baseMedian",
    "siblingMedian",
    "baseCross",
    "siblingCross",
    "baseLength",
    "siblingLength",
    "ratio",
    "maxShorterRatio",
    "shorterInclusive",
    "dyRead",
    "dy",
    "yDir",
    "product",
    "wrongSideStrict",
    "extension",
    "beamHalfHeight",
    "xInGapMaximum0",
    "maxDxRint",
    "leftThreshold",
    "rightThreshold",
    "strictLeft",
    "strictRight",
    "portion",
    "gradeSource",
    "gradeRead",
    "grade",
    "branch",
];
const EDGE_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "eventOrdinal",
    "freshRelationIdentity",
    "relationClass",
    "sourceAlias",
    "sourceInterId",
    "targetAlias",
    "targetInterId",
    "insertionReturn",
    "returnObservedByCaller",
    "graphRelationIdentity",
    "graphInsertionOrdinal",
    "sourceOutgoingOrdinal",
    "targetIncomingOrdinal",
    "extension",
    "portion",
    "grade",
];
const STEM_INCIDENT_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "eventOrdinal",
    "incidentOrdinal",
    "direction",
    "directionOrdinal",
    "graphRelationIdentity",
    "relationObjectIdentity",
    "runtimeClass",
    "classRead",
    "oppositeReadByGetChords",
    "oppositeEvidence",
    "oppositeAlias",
    "oppositeInterId",
    "oppositeVertexOrdinal",
    "chordStemMatch",
];
const BEAM_INCIDENT_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "eventOrdinal",
    "incidentOrdinal",
    "direction",
    "directionOrdinal",
    "graphRelationIdentity",
    "relationObjectIdentity",
    "runtimeClass",
    "classRead",
    "oppositeReadByCheckAbnormal",
    "oppositeEvidence",
    "oppositeAlias",
    "oppositeInterId",
    "oppositeVertexOrdinal",
    "readState",
    "relevant",
    "portion",
    "contribution",
];
const CALLBACK_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "eventOrdinal",
    "relationAdded",
    "extensionPrepopulated",
    "portionPrepopulated",
    "defaultExtensionBranchRead",
    "defaultPortionBranchRead",
    "stemIncidentState",
    "stemIncidentRows",
    "chordStemMatches",
    "stemIncidentHash",
    "beamIncidentState",
    "beamRule",
    "beamIncidentRows",
    "beamIncidentHash",
    "requestedAbnormal",
    "beamAbnormalBefore",
    "beamAbnormalAfter",
    "abnormalChanged",
    "dirtyBefore",
    "dirtyAfter",
];
const LINKER_LOOKUP_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "itemOrdinal",
    "runtimeClass",
    "linkerRead",
    "sourceRead",
    "linkerAlias",
    "linkerRuntimeClass",
    "sourceAlias",
    "sourceInterId",
    "identityMatch",
    "action",
];
const LINKER_FLAG_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "eventOrdinal",
    "lookupState",
    "selectedAlias",
    "selectedRuntimeClass",
    "selectedSourceAlias",
    "sharedCell",
    "observerAliases",
    "linkedBefore",
    "linkedAfter",
    "closedBefore",
    "closedAfter",
    "requested",
    "writeCount",
    "valueChangeCount",
    "transition",
];
const SIBLING_RESULT_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "siblingOrdinal",
    "branch",
    "edgeCommitted",
    "linkerLookupRead",
    "linkerLookupState",
    "linkerLookupTiming",
    "linkerLookupRows",
    "linkerLookupHash",
    "linkerSelectedAlias",
    "edgePrefixCount",
    "flagPrefixCount",
    "terminal",
];
const RESULT_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "terminal",
    "applyReturn",
    "supportGrade",
    "stemAlias",
    "stemInterId",
    "siblings",
    "committedEdges",
    "committedEdgeAliases",
    "committedFlags",
    "committedBCells",
    "eventCount",
    "groupStateHashAfter",
    "headRelationLoopRead",
];
const DELTA_GUARD_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "allocatorBefore",
    "allocatorAfter",
    "glyphActiveHashBefore",
    "glyphActiveHashAfter",
    "glyphOriginalsHashBefore",
    "glyphOriginalsHashAfter",
    "interIndexCountBefore",
    "interIndexCountAfter",
    "sigVertexCountBefore",
    "sigVertexCountAfter",
    "sigEdgeCountBefore",
    "sigEdgeCountAfter",
    "systemStemsHashBefore",
    "systemStemsHashAfter",
    "lineHashBefore",
    "lineHashAfter",
    "arenaTopologyHashBefore",
    "arenaTopologyHashAfter",
    "builderItemsHashBefore",
    "builderItemsHashAfter",
    "relationInputHashBefore",
    "relationInputHashAfter",
    "groupStateHashBefore",
    "groupStateHashAfter",
    "allowedMutations",
    "zeroChordStem",
    "baseBeamStemUnchanged",
    "unrelatedRelationsUnchanged",
    "unrelatedLinkerFlagsUnchanged",
    "headRelationLoopRead",
    "stopBeforeHeadRelationLoop",
];
const SUMMARY_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "groupRows",
    "siblings",
    "sameGlyph",
    "existingBeamStem",
    "shorterWrongSide",
    "linked",
    "edgesAdded",
    "linkerWrites",
    "events",
    "chordStemMatches",
    "terminal",
];
const PAGE_SUMMARY_FIELDS: &[&str] = &[
    "systems",
    "realTransactions",
    "supportedSyntheticCases",
    "envelopeCases",
    "totalTransactions",
    "groupRows",
    "siblingCandidates",
    "sameGlyph",
    "existingBeamStem",
    "shorterWrongSide",
    "linked",
    "edgesAdded",
    "linkerWrites",
    "events",
    "chordStemMatches",
    "stopBeforeHeadRelationLoop",
];
const SYNTHETIC_CASE_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "join",
    "sourceRealB15EvidenceSha256",
    "construction",
    "baseRuntimeClass",
    "siblingRuntimeClass",
    "stemAttachedBefore",
    "baseMedian",
    "siblingMedian",
    "stemMedian",
    "yDir",
    "sameGlyphIdentity",
    "pairState",
    "baseCross",
    "siblingCross",
    "baseLength",
    "siblingLength",
    "ratioRead",
    "ratio",
    "maxShorterRatio",
    "shorterInclusive",
    "dyRead",
    "dy",
    "product",
    "wrongSideStrict",
    "extension",
    "portion",
    "supportGrade",
    "branch",
    "builderItems",
    "lookupState",
    "siblingBLinkedBefore",
    "siblingBLinkedAfter",
    "writeCount",
    "valueChangeCount",
    "graphEdgesBefore",
    "graphEdgesAfter",
    "freshRelationIdentity",
    "callbackCompleted",
    "stemIncidentState",
    "stemIncidentRows",
    "chordStemMatches",
    "beamIncidentState",
    "beamRule",
    "beamAbnormalBefore",
    "beamAbnormalAfter",
    "dirtyBefore",
    "dirtyAfter",
    "throwClass",
    "throwStage",
    "eventCount",
    "terminal",
];
const SYNTHETIC_EVENT_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "eventOrdinal",
    "kind",
    "relationIdentity",
];
const SYNTHETIC_GUARD_FIELDS: &[&str] = &[
    "system",
    "plan",
    "scope",
    "case",
    "graphDelta",
    "allowedMutations",
    "baseBeamUnchanged",
    "stemGeometryUnchanged",
    "groupObjectUnchanged",
    "zeroChordStem",
    "isolatedOnly",
    "productionEquivalent",
    "enclosingRealSheetUnchanged",
    "headRelationLoopRead",
    "terminal",
];
const CORPUS_SUMMARY_FIELDS: &[&str] = &[
    "schema",
    "mode",
    "pages",
    "pageRefs",
    "rowCounts",
    "pageInputSha256",
    "probeSourceSha256",
    "runnerSourceSha256",
    "effectiveClasspathSha256",
    "jdkReleaseSha256",
    "javaExecutableSha256",
    "javaJpegLibrarySha256",
    "javaModulesSha256",
    "javaVmLibrarySha256",
    "javaAwtLibrarySha256",
    "javaAwtLwawtLibrarySha256",
    "javaArchitecture",
    "javaRuntimeVersion",
    "javaVmVariant",
    "javaImageType",
    "beamLinkerClassSha256",
    "bLinkerClassSha256",
    "vLinkerClassSha256",
    "stemLinkerClassSha256",
    "beamLinkerSourceSha256",
    "stemLinkerSourceSha256",
    "linkSourceSha256",
    "sigraphSourceSha256",
    "sigListenerSourceSha256",
    "systemInfoSourceSha256",
    "sheetSourceSha256",
    "basicIndexSourceSha256",
    "entityIndexSourceSha256",
    "glyphIndexSourceSha256",
    "interIndexSourceSha256",
    "abstractEntitySourceSha256",
    "abstractInterSourceSha256",
    "interSourceSha256",
    "stemInterSourceSha256",
    "abstractChordInterSourceSha256",
    "headChordInterSourceSha256",
    "relationSourceSha256",
    "supportSourceSha256",
    "beamStemRelationSourceSha256",
    "beamRestRelationSourceSha256",
    "chordStemRelationSourceSha256",
    "abstractBeamInterSourceSha256",
    "beamInterSourceSha256",
    "beamHookInterSourceSha256",
    "smallBeamInterSourceSha256",
    "beamBeamRelationSourceSha256",
    "sheetStubSourceSha256",
    "bookSourceSha256",
    "horizontalSideSourceSha256",
    "verticalSideSourceSha256",
    "stemBuilderSourceSha256",
    "stemItemSourceSha256",
    "stemsRetrieverSourceSha256",
    "beamGroupInterSourceSha256",
    "ensembleHelperSourceSha256",
    "containmentSourceSha256",
    "abstractStemConnectionSourceSha256",
    "beamPortionSourceSha256",
    "scaleSourceSha256",
    "skewSourceSha256",
    "staffSourceSha256",
    "staffLineSourceSha256",
    "lineInfoSourceSha256",
    "lineUtilSourceSha256",
    "gradleSourceSha256",
    "jgraphtCoreVersion",
    "jgraphtCoreJarSha256",
    "schedulerFixtureSha256",
    "expandFixtureSha256",
    "createStemFixtureSha256",
    "reuseCheckFixtureSha256",
    "baseApplyFixtureSha256",
    "baseApplyManifestSha256",
    "bLinkerFlagFixtureSha256",
    "bLinkerFlagManifestSha256",
    "predecessorReplay",
    "querySerialization",
    "emittedBodySha256",
    "emittedBodyLines",
    "emittedBodyBytes",
    "freshRunsPerPage",
    "freshRunsByteIdentical",
    "rawPassSha256",
    "freshJvmPerSystem",
    "compilerJavaProcesses",
    "runtimeJavaProcessesPerPass",
    "runtimeJavaProcesses",
    "totalJavaProcesses",
    "maximumConcurrentJavaProcesses",
    "concurrencyScope",
    "compilerJavaProcessReaped",
    "runtimeJavaProcessesReaped",
    "foregroundJavaProcessesOnly",
    "backgroundJavaProcessesStarted",
    "realTransactions",
    "supportedSyntheticCases",
    "envelopeCases",
    "totalTransactions",
    "siblingCandidates",
    "sameGlyph",
    "existingBeamStem",
    "shorterWrongSide",
    "linked",
    "edgesAdded",
    "linkerWrites",
    "chordStemMatches",
    "system1SyntheticBlock",
    "addEdgeReturnedFalseEvidence",
    "envelopeEvidenceScope",
    "stopBeforeHeadRelationLoop",
];
const MANIFEST_ENTRY_FIELDS: &[&str] = &[
    "ordinal",
    "page",
    "fixture",
    "rowCounts",
    "systems",
    "realTransactions",
    "supportedSyntheticCases",
    "envelopeCases",
    "totalTransactions",
    "groupRows",
    "groupGlyphRows",
    "nullGroupGlyphs",
    "siblingCandidates",
    "sameGlyph",
    "existingBeamStem",
    "shorterWrongSide",
    "linked",
    "edgesAdded",
    "linkerWrites",
    "events",
    "chordStemMatches",
    "pageInputSha256",
    "schedulerFixtureSha256",
    "expandFixtureSha256",
    "createStemFixtureSha256",
    "reuseCheckFixtureSha256",
    "baseApplyFixtureSha256",
    "baseApplyManifestSha256",
    "bLinkerFlagFixtureSha256",
    "bLinkerFlagManifestSha256",
    "emittedBodySha256",
    "emittedBodyLines",
    "emittedBodyBytes",
    "rawPassSha256",
    "fixtureSha256",
    "fixtureLines",
    "fixtureBytes",
    "freshRunsPerPage",
    "freshRunsByteIdentical",
    "compilerJavaProcesses",
    "runtimeJavaProcessesPerPass",
    "runtimeJavaProcesses",
    "totalJavaProcesses",
    "maximumConcurrentJavaProcesses",
    "concurrencyScope",
    "freshJvmPerSystem",
    "compilerJavaProcessReaped",
    "runtimeJavaProcessesReaped",
    "foregroundJavaProcessesOnly",
    "backgroundJavaProcessesStarted",
    "system1SyntheticBlock",
    "addEdgeReturnedFalseEvidence",
    "envelopeEvidenceScope",
    "stopBeforeHeadRelationLoop",
];
const MANIFEST_SUMMARY_FIELDS: &[&str] = &[
    "schema",
    "entries",
    "probeSourceSha256",
    "runnerSourceSha256",
    "effectiveClasspathSha256",
    "jdkReleaseSha256",
    "javaExecutableSha256",
    "javaJpegLibrarySha256",
    "javaModulesSha256",
    "javaVmLibrarySha256",
    "javaAwtLibrarySha256",
    "javaAwtLwawtLibrarySha256",
    "javaArchitecture",
    "javaRuntimeVersion",
    "javaVmVariant",
    "javaImageType",
    "beamLinkerClassSha256",
    "bLinkerClassSha256",
    "vLinkerClassSha256",
    "stemLinkerClassSha256",
    "beamLinkerSourceSha256",
    "stemLinkerSourceSha256",
    "linkSourceSha256",
    "sigraphSourceSha256",
    "sigListenerSourceSha256",
    "systemInfoSourceSha256",
    "sheetSourceSha256",
    "basicIndexSourceSha256",
    "entityIndexSourceSha256",
    "glyphIndexSourceSha256",
    "interIndexSourceSha256",
    "abstractEntitySourceSha256",
    "abstractInterSourceSha256",
    "interSourceSha256",
    "stemInterSourceSha256",
    "abstractChordInterSourceSha256",
    "headChordInterSourceSha256",
    "relationSourceSha256",
    "supportSourceSha256",
    "beamStemRelationSourceSha256",
    "beamRestRelationSourceSha256",
    "chordStemRelationSourceSha256",
    "abstractBeamInterSourceSha256",
    "beamInterSourceSha256",
    "beamHookInterSourceSha256",
    "smallBeamInterSourceSha256",
    "beamBeamRelationSourceSha256",
    "sheetStubSourceSha256",
    "bookSourceSha256",
    "horizontalSideSourceSha256",
    "verticalSideSourceSha256",
    "stemBuilderSourceSha256",
    "stemItemSourceSha256",
    "stemsRetrieverSourceSha256",
    "beamGroupInterSourceSha256",
    "ensembleHelperSourceSha256",
    "containmentSourceSha256",
    "abstractStemConnectionSourceSha256",
    "beamPortionSourceSha256",
    "scaleSourceSha256",
    "skewSourceSha256",
    "staffSourceSha256",
    "staffLineSourceSha256",
    "lineInfoSourceSha256",
    "lineUtilSourceSha256",
    "gradleSourceSha256",
    "jgraphtCoreVersion",
    "jgraphtCoreJarSha256",
    "baseApplyManifestSha256",
    "bLinkerFlagManifestSha256",
    "predecessorReplay",
    "querySerialization",
    "sharedHeaderSha256",
    "sharedHeaderLines",
    "sharedHeaderBytes",
    "corpusBodySha256",
    "corpusBodyLines",
    "corpusBodyBytes",
    "corpusRowCounts",
    "corpusReconstruction",
    "semanticRows",
    "splitFixtureLines",
    "splitFixtureBytes",
    "realSystems",
    "realTransactions",
    "groupRows",
    "groupGlyphRows",
    "nullGroupGlyphs",
    "realSiblingCandidates",
    "realSameGlyph",
    "realExistingBeamStem",
    "realShorterWrongSide",
    "realLinked",
    "realEdgesAdded",
    "realLinkerWrites",
    "realEvents",
    "chordStemMatches",
    "syntheticBlocks",
    "supportedSyntheticCases",
    "envelopeCases",
    "isolatedCases",
    "totalTransactions",
    "sameGlyph",
    "existingBeamStem",
    "shorterWrongSide",
    "linked",
    "edgesAdded",
    "linkerWrites",
    "events",
    "compilerJavaProcesses",
    "runtimeJavaProcesses",
    "totalJavaProcesses",
    "maximumConcurrentJavaProcesses",
    "concurrencyScope",
    "freshRunsPerPage",
    "freshRunsByteIdentical",
    "freshJvmPerSystem",
    "compilerJavaProcessesReaped",
    "runtimeJavaProcessesReaped",
    "foregroundJavaProcessesOnly",
    "backgroundJavaProcessesStarted",
    "system1SyntheticBlock",
    "addEdgeReturnedFalseEvidence",
    "supplementalEvidenceScope",
    "stopBeforeHeadRelationLoop",
    "manifestBodySha256",
    "manifestBodyLines",
    "manifestBodyBytes",
];
const CORPUS_SOURCE_PINS: &[(&str, &str)] = &[
    (
        "beamLinkerSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/stem/BeamLinker.java",
    ),
    (
        "stemLinkerSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/stem/StemLinker.java",
    ),
    (
        "linkSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/Link.java",
    ),
    (
        "sigraphSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/SIGraph.java",
    ),
    (
        "sigListenerSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/SigListener.java",
    ),
    (
        "systemInfoSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/SystemInfo.java",
    ),
    (
        "sheetSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/Sheet.java",
    ),
    (
        "basicIndexSourceSha256",
        "app/src/main/java/org/audiveris/omr/util/BasicIndex.java",
    ),
    (
        "entityIndexSourceSha256",
        "app/src/main/java/org/audiveris/omr/util/EntityIndex.java",
    ),
    (
        "glyphIndexSourceSha256",
        "app/src/main/java/org/audiveris/omr/glyph/GlyphIndex.java",
    ),
    (
        "interIndexSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/InterIndex.java",
    ),
    (
        "abstractEntitySourceSha256",
        "app/src/main/java/org/audiveris/omr/util/AbstractEntity.java",
    ),
    (
        "abstractInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/AbstractInter.java",
    ),
    (
        "interSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/Inter.java",
    ),
    (
        "stemInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/StemInter.java",
    ),
    (
        "abstractChordInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/AbstractChordInter.java",
    ),
    (
        "headChordInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/HeadChordInter.java",
    ),
    (
        "relationSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/Relation.java",
    ),
    (
        "supportSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/Support.java",
    ),
    (
        "beamStemRelationSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/BeamStemRelation.java",
    ),
    (
        "beamRestRelationSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/BeamRestRelation.java",
    ),
    (
        "chordStemRelationSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/ChordStemRelation.java",
    ),
    (
        "abstractBeamInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/AbstractBeamInter.java",
    ),
    (
        "beamInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/BeamInter.java",
    ),
    (
        "beamHookInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/BeamHookInter.java",
    ),
    (
        "smallBeamInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/SmallBeamInter.java",
    ),
    (
        "beamBeamRelationSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/BeamBeamRelation.java",
    ),
    (
        "sheetStubSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/SheetStub.java",
    ),
    (
        "bookSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/Book.java",
    ),
    (
        "horizontalSideSourceSha256",
        "app/src/main/java/org/audiveris/omr/util/HorizontalSide.java",
    ),
    (
        "verticalSideSourceSha256",
        "app/src/main/java/org/audiveris/omr/util/VerticalSide.java",
    ),
    (
        "stemBuilderSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/stem/StemBuilder.java",
    ),
    (
        "stemItemSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/stem/StemItem.java",
    ),
    (
        "stemsRetrieverSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/stem/StemsRetriever.java",
    ),
    (
        "beamGroupInterSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/BeamGroupInter.java",
    ),
    (
        "ensembleHelperSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/inter/EnsembleHelper.java",
    ),
    (
        "containmentSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/Containment.java",
    ),
    (
        "abstractStemConnectionSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/AbstractStemConnection.java",
    ),
    (
        "beamPortionSourceSha256",
        "app/src/main/java/org/audiveris/omr/sig/relation/BeamPortion.java",
    ),
    (
        "scaleSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/Scale.java",
    ),
    (
        "skewSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/Skew.java",
    ),
    (
        "staffSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/Staff.java",
    ),
    (
        "staffLineSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/StaffLine.java",
    ),
    (
        "lineInfoSourceSha256",
        "app/src/main/java/org/audiveris/omr/sheet/grid/LineInfo.java",
    ),
    (
        "lineUtilSourceSha256",
        "app/src/main/java/org/audiveris/omr/math/LineUtil.java",
    ),
    ("gradleSourceSha256", "app/build.gradle"),
];
const CORPUS_GLOBAL_FIXTURE_PINS: &[(&str, &str)] = &[
    (
        "baseApplyManifestSha256",
        "rust/oracle/stems-beam-vlink-base-apply-manifest.txt",
    ),
    ("bLinkerFlagManifestSha256", BOUNDARY_FIFTEEN_MANIFEST_PATH),
];

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum RowKind {
    Page,
    Predecessor,
    Baseline,
    GroupMember,
    Sibling,
    SourceOutgoing,
    PairRelation,
    Geometry,
    Edge,
    StemIncident,
    BeamIncident,
    Callback,
    LinkerLookup,
    LinkerFlag,
    SiblingResult,
    Result,
    DeltaGuard,
    Summary,
    SyntheticCase,
    SyntheticEvent,
    SyntheticGuard,
    PageSummary,
    CorpusSummary,
}

impl RowKind {
    const fn label(self) -> &'static str {
        match self {
            Self::Page => "stemsbeamvlinksiblinglinkspage",
            Self::Predecessor => "stemsbeamvlinksiblinglinkspredecessor",
            Self::Baseline => "stemsbeamvlinksiblinglinksbaseline",
            Self::GroupMember => "stemsbeamvlinksiblinglinksgroupmember",
            Self::Sibling => "stemsbeamvlinksiblinglinkssibling",
            Self::SourceOutgoing => "stemsbeamvlinksiblinglinkssourceoutgoing",
            Self::PairRelation => "stemsbeamvlinksiblinglinkspairrelation",
            Self::Geometry => "stemsbeamvlinksiblinglinksgeometry",
            Self::Edge => "stemsbeamvlinksiblinglinksedge",
            Self::StemIncident => "stemsbeamvlinksiblinglinksstemincident",
            Self::BeamIncident => "stemsbeamvlinksiblinglinksbeamincident",
            Self::Callback => "stemsbeamvlinksiblinglinkscallback",
            Self::LinkerLookup => "stemsbeamvlinksiblinglinkslinkerlookup",
            Self::LinkerFlag => "stemsbeamvlinksiblinglinkslinkerflag",
            Self::SiblingResult => "stemsbeamvlinksiblinglinkssiblingresult",
            Self::Result => "stemsbeamvlinksiblinglinksresult",
            Self::DeltaGuard => "stemsbeamvlinksiblinglinksdeltaguard",
            Self::Summary => "stemsbeamvlinksiblinglinkssummary",
            Self::SyntheticCase => "stemsbeamvlinksiblinglinkssyntheticcase",
            Self::SyntheticEvent => "stemsbeamvlinksiblinglinkssyntheticevent",
            Self::SyntheticGuard => "stemsbeamvlinksiblinglinkssyntheticguard",
            Self::PageSummary => "stemsbeamvlinksiblinglinkspagesummary",
            Self::CorpusSummary => "stemsbeamvlinksiblinglinkscorpussummary",
        }
    }

    fn parse(label: &str) -> Option<Self> {
        let suffix = label.strip_prefix("stemsbeamvlinksiblinglinks")?;
        Some(match suffix {
            "page" => Self::Page,
            "predecessor" => Self::Predecessor,
            "baseline" => Self::Baseline,
            "groupmember" => Self::GroupMember,
            "sibling" => Self::Sibling,
            "sourceoutgoing" => Self::SourceOutgoing,
            "pairrelation" => Self::PairRelation,
            "geometry" => Self::Geometry,
            "edge" => Self::Edge,
            "stemincident" => Self::StemIncident,
            "beamincident" => Self::BeamIncident,
            "callback" => Self::Callback,
            "linkerlookup" => Self::LinkerLookup,
            "linkerflag" => Self::LinkerFlag,
            "siblingresult" => Self::SiblingResult,
            "result" => Self::Result,
            "deltaguard" => Self::DeltaGuard,
            "summary" => Self::Summary,
            "syntheticcase" => Self::SyntheticCase,
            "syntheticevent" => Self::SyntheticEvent,
            "syntheticguard" => Self::SyntheticGuard,
            "pagesummary" => Self::PageSummary,
            "corpussummary" => Self::CorpusSummary,
            _ => return None,
        })
    }

    const fn expected_fields(self) -> &'static [&'static str] {
        match self {
            Self::Page => PAGE_FIELDS,
            Self::Predecessor => PREDECESSOR_FIELDS,
            Self::Baseline => BASELINE_FIELDS,
            Self::GroupMember => GROUP_MEMBER_FIELDS,
            Self::Sibling => SIBLING_FIELDS,
            Self::SourceOutgoing => SOURCE_OUTGOING_FIELDS,
            Self::PairRelation => PAIR_RELATION_FIELDS,
            Self::Geometry => GEOMETRY_FIELDS,
            Self::Edge => EDGE_FIELDS,
            Self::StemIncident => STEM_INCIDENT_FIELDS,
            Self::BeamIncident => BEAM_INCIDENT_FIELDS,
            Self::Callback => CALLBACK_FIELDS,
            Self::LinkerLookup => LINKER_LOOKUP_FIELDS,
            Self::LinkerFlag => LINKER_FLAG_FIELDS,
            Self::SiblingResult => SIBLING_RESULT_FIELDS,
            Self::Result => RESULT_FIELDS,
            Self::DeltaGuard => DELTA_GUARD_FIELDS,
            Self::Summary => SUMMARY_FIELDS,
            Self::SyntheticCase => SYNTHETIC_CASE_FIELDS,
            Self::SyntheticEvent => SYNTHETIC_EVENT_FIELDS,
            Self::SyntheticGuard => SYNTHETIC_GUARD_FIELDS,
            Self::PageSummary => PAGE_SUMMARY_FIELDS,
            Self::CorpusSummary => CORPUS_SUMMARY_FIELDS,
        }
    }
}

const CORE_ROW_KINDS: [RowKind; 21] = [
    RowKind::Page,
    RowKind::Predecessor,
    RowKind::Baseline,
    RowKind::GroupMember,
    RowKind::Sibling,
    RowKind::SourceOutgoing,
    RowKind::PairRelation,
    RowKind::Geometry,
    RowKind::Edge,
    RowKind::StemIncident,
    RowKind::BeamIncident,
    RowKind::Callback,
    RowKind::LinkerLookup,
    RowKind::LinkerFlag,
    RowKind::SiblingResult,
    RowKind::Result,
    RowKind::DeltaGuard,
    RowKind::Summary,
    RowKind::SyntheticCase,
    RowKind::SyntheticEvent,
    RowKind::SyntheticGuard,
];

fn core_row_counts(rows: &[StrictRow]) -> String {
    CORE_ROW_KINDS
        .iter()
        .map(|kind| {
            format!(
                "{}:{}",
                kind.label(),
                rows.iter().filter(|row| row.kind == *kind).count()
            )
        })
        .collect::<Vec<_>>()
        .join(",")
}

#[derive(Clone, Debug, Eq, PartialEq)]
struct StrictRow {
    kind: RowKind,
    page: String,
    fields: Vec<(String, String)>,
}

impl StrictRow {
    fn parse(line: &str) -> Result<Self, String> {
        let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
        let label = tokens.first().ok_or_else(|| "row is empty".to_owned())?;
        let kind = RowKind::parse(label).ok_or_else(|| format!("unknown row label {label}"))?;
        let (page, field_tokens) = if kind == RowKind::CorpusSummary {
            ("", &tokens[1..])
        } else {
            (
                *tokens
                    .get(1)
                    .ok_or_else(|| "row lacks its implicit page token".to_owned())?,
                &tokens[2..],
            )
        };
        if field_tokens.len() % 2 != 0 {
            return Err("row does not contain ordered key/value pairs".to_owned());
        }
        let mut names = BTreeSet::new();
        let mut fields = Vec::with_capacity(field_tokens.len() / 2);
        for pair in field_tokens.chunks_exact(2) {
            if !names.insert(pair[0]) {
                return Err(format!("duplicate field {}", pair[0]));
            }
            fields.push((pair[0].to_owned(), pair[1].to_owned()));
        }
        let actual = fields
            .iter()
            .map(|(name, _)| name.as_str())
            .collect::<Vec<_>>();
        if actual != kind.expected_fields() {
            return Err(format!(
                "wrong field order for {kind:?}: expected {:?}, got {actual:?}",
                kind.expected_fields()
            ));
        }
        Ok(Self {
            kind,
            page: page.to_owned(),
            fields,
        })
    }

    fn value(&self, name: &str) -> Result<&str, String> {
        self.fields
            .iter()
            .find_map(|(field, value)| (field == name).then_some(value.as_str()))
            .ok_or_else(|| format!("missing {name} in {:?}", self.kind))
    }

    fn usize(&self, name: &str) -> Result<usize, String> {
        self.value(name)?
            .parse()
            .map_err(|error| format!("invalid {name} in {:?}: {error}", self.kind))
    }

    fn i64(&self, name: &str) -> Result<i64, String> {
        self.value(name)?
            .parse()
            .map_err(|error| format!("invalid {name} in {:?}: {error}", self.kind))
    }

    fn bool(&self, name: &str) -> Result<bool, String> {
        match self.value(name)? {
            "true" => Ok(true),
            "false" => Ok(false),
            value => Err(format!("invalid Boolean {name}={value} in {:?}", self.kind)),
        }
    }

    fn key(&self) -> Result<TransactionKey, String> {
        if matches!(
            self.kind,
            RowKind::Page | RowKind::PageSummary | RowKind::CorpusSummary
        ) {
            return Err("page row has no transaction key".to_owned());
        }
        let actual = self
            .fields
            .iter()
            .take(COMMON_FIELDS.len())
            .map(|(name, _)| name.as_str())
            .collect::<Vec<_>>();
        if actual != COMMON_FIELDS {
            return Err(format!("transaction prefix differs in {:?}", self.kind));
        }
        Ok(TransactionKey {
            page: self.page.clone(),
            system: self.usize("system")?,
            plan: self.usize("plan")?,
            scope: self.value("scope")?.to_owned(),
            case_name: self.value("case")?.to_owned(),
        })
    }
}

#[derive(Clone, Debug, Eq, PartialEq)]
struct ManifestRow {
    label: String,
    fields: Vec<(String, String)>,
}

impl ManifestRow {
    fn parse(line: &str, expected_label: &str, expected_fields: &[&str]) -> Result<Self, String> {
        let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
        let label = tokens
            .first()
            .ok_or_else(|| "manifest row is empty".to_owned())?;
        if *label != expected_label {
            return Err(format!(
                "manifest row label differs: expected {expected_label}, got {label}"
            ));
        }
        let field_tokens = &tokens[1..];
        if field_tokens.len() % 2 != 0 {
            return Err("manifest row does not contain ordered key/value pairs".to_owned());
        }
        let fields = field_tokens
            .chunks_exact(2)
            .map(|pair| (pair[0].to_owned(), pair[1].to_owned()))
            .collect::<Vec<_>>();
        let actual = fields
            .iter()
            .map(|(name, _)| name.as_str())
            .collect::<Vec<_>>();
        if actual != expected_fields {
            return Err(format!(
                "wrong field order for {expected_label}: expected {expected_fields:?}, got {actual:?}"
            ));
        }
        let unique = actual.iter().copied().collect::<BTreeSet<_>>();
        if unique.len() != actual.len() {
            return Err(format!("duplicate field in {expected_label}"));
        }
        Ok(Self {
            label: (*label).to_owned(),
            fields,
        })
    }

    fn value(&self, name: &str) -> Result<&str, String> {
        self.fields
            .iter()
            .find_map(|(field, value)| (field == name).then_some(value.as_str()))
            .ok_or_else(|| format!("missing {name} in {}", self.label))
    }

    fn usize(&self, name: &str) -> Result<usize, String> {
        self.value(name)?
            .parse()
            .map_err(|error| format!("invalid {name} in {}: {error}", self.label))
    }

    fn bool(&self, name: &str) -> Result<bool, String> {
        match self.value(name)? {
            "true" => Ok(true),
            "false" => Ok(false),
            value => Err(format!("invalid Boolean {name}={value} in {}", self.label)),
        }
    }
}

#[derive(Clone, Debug, Eq, PartialEq)]
struct TransactionKey {
    page: String,
    system: usize,
    plan: usize,
    scope: String,
    case_name: String,
}

fn parse_scaffold_fixture(text: &str) -> Result<Vec<StrictRow>, String> {
    if !text.as_bytes().ends_with(b"\n") {
        return Err("fixture must end with one newline".to_owned());
    }
    let lines = text.lines().collect::<Vec<_>>();
    if lines.len() <= FIXTURE_HEADER.len() || &lines[..FIXTURE_HEADER.len()] != FIXTURE_HEADER {
        return Err("fixture header differs".to_owned());
    }
    lines[FIXTURE_HEADER.len()..]
        .iter()
        .enumerate()
        .map(|(index, line)| {
            if line.is_empty() || line.starts_with('#') {
                return Err(format!(
                    "unexpected blank/comment at semantic line {}",
                    index + FIXTURE_HEADER.len() + 1
                ));
            }
            StrictRow::parse(line)
                .map_err(|error| format!("line {}: {error}", index + FIXTURE_HEADER.len() + 1))
        })
        .collect()
}

fn java_printf_fields(source: &str, kind: RowKind) -> Result<Vec<String>, String> {
    if kind == RowKind::PageSummary {
        return Err("page summary is emitted by the runner, not Java".to_owned());
    }
    let marker = format!("\"{} ", kind.label());
    let start = source
        .find(&marker)
        .ok_or_else(|| format!("Java probe lacks {:?} printf", kind))?;
    let mut format_text = String::new();
    for line in source[start..].lines() {
        let Some(first_quote) = line.find('"') else {
            continue;
        };
        let last_quote = line
            .rfind('"')
            .ok_or_else(|| format!("unterminated Java format literal for {kind:?}"))?;
        if last_quote == first_quote {
            return Err(format!("empty Java format literal for {kind:?}"));
        }
        format_text.push_str(&line[first_quote + 1..last_quote]);
        if line[first_quote + 1..last_quote].contains("%n") {
            break;
        }
    }
    let format_text = format_text.replace("%n", "");
    let tokens = format_text.split_ascii_whitespace().collect::<Vec<_>>();
    if tokens.first().copied() != Some(kind.label()) || tokens.get(1).copied() != Some("%s") {
        return Err(format!("Java {:?} format lacks label/page prefix", kind));
    }
    if (tokens.len() - 2) % 2 != 0 {
        return Err(format!("Java {:?} format is not key/value shaped", kind));
    }
    let mut fields = if kind == RowKind::Page {
        Vec::new()
    } else {
        COMMON_FIELDS
            .iter()
            .map(|field| (*field).to_owned())
            .collect()
    };
    fields.extend(tokens[2..].chunks_exact(2).map(|pair| pair[0].to_owned()));
    Ok(fields)
}

fn runner_corpus_printf_fields(source: &str) -> Result<Vec<String>, String> {
    let prefix = format!("printf '{} ", RowKind::CorpusSummary.label());
    let line = source
        .lines()
        .find(|line| line.starts_with(&prefix))
        .ok_or_else(|| "runner lacks corpus-summary printf".to_owned())?;
    let first_quote = line
        .find('\'')
        .ok_or_else(|| "runner corpus printf lacks opening quote".to_owned())?;
    let last_quote = line
        .rfind('\'')
        .filter(|last| *last > first_quote)
        .ok_or_else(|| "runner corpus printf lacks closing quote".to_owned())?;
    let tokens = line[first_quote + 1..last_quote]
        .split_ascii_whitespace()
        .collect::<Vec<_>>();
    if tokens.first().copied() != Some(RowKind::CorpusSummary.label())
        || (tokens.len() - 1) % 2 != 0
    {
        return Err("runner corpus printf is not key/value shaped".to_owned());
    }
    let mut fields = tokens[1..]
        .chunks_exact(2)
        .map(|pair| pair[0].to_owned())
        .collect::<Vec<_>>();
    let insertion = fields
        .iter()
        .position(|field| field == "stemLinkerClassSha256")
        .ok_or_else(|| "runner corpus printf lacks class/source boundary".to_owned())?
        + 1;
    fields.splice(
        insertion..insertion,
        CORPUS_SOURCE_PINS
            .iter()
            .map(|(field, _)| (*field).to_owned()),
    );
    Ok(fields)
}

struct RowCursor<'a> {
    rows: &'a [StrictRow],
    index: usize,
}

impl<'a> RowCursor<'a> {
    fn new(rows: &'a [StrictRow], index: usize) -> Self {
        Self { rows, index }
    }

    fn peek_kind(&self) -> Option<RowKind> {
        self.rows.get(self.index).map(|row| row.kind)
    }

    fn take(&mut self, kind: RowKind, key: &TransactionKey) -> Result<&'a StrictRow, String> {
        let row = self
            .rows
            .get(self.index)
            .ok_or_else(|| format!("expected {kind:?}, reached end of fixture"))?;
        if row.kind != kind {
            return Err(format!(
                "expected {kind:?} at semantic row {}, found {:?}",
                self.index, row.kind
            ));
        }
        if &row.key()? != key {
            return Err(format!(
                "transaction key drift at semantic row {}",
                self.index
            ));
        }
        self.index += 1;
        Ok(row)
    }
}

#[derive(Clone, Debug)]
struct ParsedTransaction {
    key: TransactionKey,
    predecessor: StrictRow,
    rows: Vec<StrictRow>,
}

fn is_lower_sha256(value: &str) -> bool {
    value.len() == 64
        && value
            .bytes()
            .all(|byte| byte.is_ascii_digit() || (b'a'..=b'f').contains(&byte))
}

fn sha256_rows(rows: impl IntoIterator<Item = String>) -> String {
    let mut bytes = Vec::new();
    for row in rows {
        bytes.extend_from_slice(row.as_bytes());
        bytes.push(b'\n');
    }
    sha256_hex(&bytes)
}

fn joined_row_token(row: &StrictRow, fields: &[&str]) -> Result<String, String> {
    fields
        .iter()
        .map(|field| row.value(field))
        .collect::<Result<Vec<_>, _>>()
        .map(|values| values.join(":"))
}

fn require_ordinal(row: &StrictRow, field: &str, expected: usize) -> Result<(), String> {
    if row.usize(field)? != expected {
        return Err(format!(
            "{field} chronology differs in {:?}: expected {expected}",
            row.kind
        ));
    }
    Ok(())
}

fn validate_page_row(row: &StrictRow) -> Result<(), String> {
    if row.kind != RowKind::Page
        || row.usize("systems")? == 0
        || row.value("executionMode")? != "foregroundJvmPerSystem"
        || row.value("groupOrder")? != "OutgoingContainmentThenStableTopDown"
        || row.value("incidentOrder")? != "IncomingThenOutgoing"
        || !row.bool("headless")?
        || row.value("methodDispatch")? != "ExactPrivateBeamLinkerVLinkerLinkSiblings"
        || row.value("stop")? != "ReadyBeforeHeadRelationLoop"
    {
        return Err("page execution envelope differs".to_owned());
    }
    for field in [
        "schedulerFixtureSha256",
        "expandFixtureSha256",
        "createStemFixtureSha256",
        "reuseCheckFixtureSha256",
        "baseApplyFixtureSha256",
        "bLinkerFlagFixtureSha256",
    ] {
        if !is_lower_sha256(row.value(field)?) {
            return Err(format!("page {field} is not lowercase SHA-256"));
        }
    }
    Ok(())
}

fn validate_predecessor_row(row: &StrictRow) -> Result<(), String> {
    if row.value("join")? != "FullBoundary15Replay"
        || row.usize("b15TransactionRows")? == 0
        || !is_lower_sha256(row.value("b15TransactionEvidenceSha256")?)
        || !is_lower_sha256(row.value("b15ResultRowSha256")?)
        || !is_lower_sha256(row.value("b15GuardRowSha256")?)
        || !is_lower_sha256(row.value("b15SummaryRowSha256")?)
        || row.value("predecessorTerminal")? != "ReadyBeforeSiblingBeamLinks"
        || !row.bool("targetBLinked")?
        || row.value("stemAlias")?.is_empty()
        || row.usize("stemInterId")? == 0
        || row.value("baseBeamAlias")?.is_empty()
        || row.value("targetBAlias")?.is_empty()
        || row.value("triggeringVAlias")?.is_empty()
    {
        return Err("Boundary-15 predecessor row differs".to_owned());
    }
    parse_hex_bits(row.value("supportGrade")?)?;
    Ok(())
}

fn parse_hex_bits(value: &str) -> Result<u64, String> {
    let (java_hex, raw) = value
        .split_once('/')
        .ok_or_else(|| format!("missing raw bits in {value}"))?;
    if java_hex.is_empty() || raw.len() != 16 {
        return Err(format!("invalid Java hex/raw token {value}"));
    }
    let bits = u64::from_str_radix(raw, 16)
        .map_err(|error| format!("invalid raw f64 bits in {value}: {error}"))?;
    if !f64::from_bits(bits).is_finite() {
        return Err(format!("non-finite compact f64 token {value}"));
    }
    Ok(bits)
}

fn parse_f64(value: &str) -> Result<f64, String> {
    Ok(f64::from_bits(parse_hex_bits(value)?))
}

fn parse_point(value: &str) -> Result<NativeStemPoint, String> {
    let values = value.split(':').collect::<Vec<_>>();
    let [x, y] = values.as_slice() else {
        return Err(format!("invalid point token {value}"));
    };
    Ok(NativeStemPoint {
        x: parse_f64(x)?,
        y: parse_f64(y)?,
    })
}

fn parse_segment(value: &str) -> Result<Segment, String> {
    let values = value.split(':').collect::<Vec<_>>();
    let [x1, y1, x2, y2] = values.as_slice() else {
        return Err(format!("invalid segment token {value}"));
    };
    Ok(Segment {
        x1: parse_f64(x1)?,
        y1: parse_f64(y1)?,
        x2: parse_f64(x2)?,
        y2: parse_f64(y2)?,
    })
}

fn parse_i32_field(row: &StrictRow, field: &str) -> Result<i32, String> {
    row.value(field)?
        .parse()
        .map_err(|error| format!("invalid {field} in {:?}: {error}", row.kind))
}

fn parse_optional_usize(value: &str) -> Result<Option<usize>, String> {
    if value == "-" {
        Ok(None)
    } else {
        value
            .parse()
            .map(Some)
            .map_err(|error| format!("invalid optional usize {value}: {error}"))
    }
}

fn parse_optional_i32(value: &str) -> Result<Option<i32>, String> {
    if value == "-" {
        Ok(None)
    } else {
        value
            .parse()
            .map(Some)
            .map_err(|error| format!("invalid optional i32 {value}: {error}"))
    }
}

fn parse_optional_bool(value: &str) -> Result<Option<bool>, String> {
    match value {
        "-" => Ok(None),
        "true" => Ok(Some(true)),
        "false" => Ok(Some(false)),
        _ => Err(format!("invalid optional Boolean {value}")),
    }
}

include!("common/b16_hydration.rs");

fn validate_baseline_row(row: &StrictRow, predecessor: &StrictRow) -> Result<(), String> {
    let y_dir = row
        .value("yDir")?
        .parse::<i32>()
        .map_err(|error| format!("invalid yDir: {error}"))?;
    if row.value("baseBeamAlias")? != predecessor.value("baseBeamAlias")?
        || row.value("stemAlias")? != predecessor.value("stemAlias")?
        || row.value("stemInterId")? != predecessor.value("stemInterId")?
        || row.value("supportGrade")? != predecessor.value("supportGrade")?
        || !matches!(
            row.value("baseBeamClass")?,
            "org.audiveris.omr.sig.inter.BeamInter"
                | "org.audiveris.omr.sig.inter.BeamHookInter"
                | "org.audiveris.omr.sig.inter.SmallBeamInter"
        )
        || row.value("groupClass")? != "org.audiveris.omr.sig.inter.BeamGroupInter"
        || !row.bool("cachedMedianSameIdentity")?
        || !row.bool("baseRemoved")?
        || row.value("supportGradeSource")? != "FreshBaseBeamStemDraft"
        || !row.bool("supportGradeRead")?
        || !row.bool("soleSigListener")?
        || row.usize("chordStemMatches")? != 0
        || !matches!(y_dir, -1 | 1)
        || row.usize("groupMembers")? < row.usize("siblings")? + 1
        || row.usize("selectedBeforeBaseRemoval")? != row.usize("siblings")? + 1
        || row.usize("groupOutgoingScanned")? < row.usize("groupMembers")?
        || row.usize("stemIncidentRows")? == 0
        || row.usize("builderItems")? == 0
    {
        return Err("baseline join/domain differs".to_owned());
    }
    for field in [
        "groupStateHashBefore",
        "groupObjectStateHash",
        "groupQueryProvenanceSha256",
        "graphVertexHashBefore",
        "graphEdgeHashBefore",
        "listenerTopologyHash",
        "stemIncidentHash",
        "builderItemsHash",
        "arenaStateHashBefore",
        "relationInputHashBefore",
    ] {
        if !is_lower_sha256(row.value(field)?) {
            return Err(format!("baseline {field} is not lowercase SHA-256"));
        }
    }
    for field in [
        "baseBeamHeight",
        "maxShorterRatio",
        "xInGapMaximum0",
        "baseLength",
        "supportGrade",
    ] {
        parse_hex_bits(row.value(field)?)?;
    }
    Ok(())
}

fn validate_group_rows(baseline: &StrictRow, rows: &[&StrictRow]) -> Result<(), String> {
    if rows.len() != baseline.usize("groupOutgoingScanned")? {
        return Err("group outgoing row count differs".to_owned());
    }
    let mut member_ordinal = 0;
    let mut selected = 0;
    let mut base = 0;
    for (ordinal, row) in rows.iter().enumerate() {
        require_ordinal(row, "groupOutgoingOrdinal", ordinal)?;
        let containment = row.bool("containmentMatch")?;
        if containment {
            require_ordinal(row, "memberOrdinal", member_ordinal)?;
            member_ordinal += 1;
            if row.value("targetReadByGetMembers")? != "true"
                || row.value("targetEvidence")? != "GetMembersRead"
                || row.value("beamRuntimeClass")? != row.value("targetRuntimeClass")?
                || row.value("beamInterId")? != row.value("targetInterId")?
                || row.value("sigMembership")? != "true"
                || row.value("beamRemoved")? != "false"
            {
                return Err("containment/member live projection differs".to_owned());
            }
            for field in ["beamGroupStateHash", "beamGroupObjectStateHash"] {
                if !is_lower_sha256(row.value(field)?) {
                    return Err(format!("group member {field} is not lowercase SHA-256"));
                }
            }
            selected += usize::from(row.bool("selected")?);
            if row.bool("baseIdentity")? {
                base += 1;
                if row.value("removeAction")? != "RemoveFirstBase" || !row.bool("selected")? {
                    return Err("base group member removal differs".to_owned());
                }
            } else if row.value("removeAction")? != "Retain" {
                return Err("non-base group member action differs".to_owned());
            }
        } else if row.value("memberOrdinal")? != "-"
            || row.value("targetReadByGetMembers")? != "false"
            || row.value("targetEvidence")? != "GraphReconstruction"
            || row.value("beamRuntimeClass")? != "-"
            || row.value("selected")? != "-"
        {
            return Err("non-containment group reconstruction differs".to_owned());
        }
    }
    if member_ordinal != baseline.usize("groupMembers")?
        || selected != baseline.usize("selectedBeforeBaseRemoval")?
        || base != 1
    {
        return Err("group member/select/base census differs".to_owned());
    }
    let hash = sha256_rows(rows.iter().map(|row| {
        joined_row_token(
            row,
            &[
                "groupOutgoingOrdinal",
                "graphRelationIdentity",
                "relationObjectIdentity",
                "relationClass",
                "targetAlias",
                "targetRuntimeClass",
                "targetInterId",
                "targetVertexOrdinal",
                "containmentMatch",
                "memberOrdinal",
            ],
        )
        .expect("validated group row fields")
    }));
    if hash != baseline.value("groupQueryProvenanceSha256")? {
        return Err("group query provenance hash differs".to_owned());
    }
    Ok(())
}

fn validate_source_rows(sibling: &StrictRow, rows: &[&StrictRow]) -> Result<(), String> {
    if rows.len() != sibling.usize("sourceOutgoingScanned")? {
        return Err("source-outgoing row count differs".to_owned());
    }
    for (ordinal, row) in rows.iter().enumerate() {
        require_ordinal(row, "sourceOutgoingOrdinal", ordinal)?;
    }
    let expected = sibling.value("sourceOutgoingProvenanceSha256")?;
    if rows.is_empty() && sibling.value("pairState")? == "NotRead" {
        if expected != "NotRead" {
            return Err("non-read source-outgoing provenance is not literal NotRead".to_owned());
        }
    } else {
        let hash = sha256_rows(rows.iter().map(|row| {
            joined_row_token(
                row,
                &[
                    "sourceOutgoingOrdinal",
                    "graphRelationIdentity",
                    "relationObjectIdentity",
                    "runtimeClass",
                ],
            )
            .expect("validated source-outgoing fields")
        }));
        if hash != expected {
            return Err("source-outgoing provenance hash differs".to_owned());
        }
    }
    Ok(())
}

fn validate_pair_rows(sibling: &StrictRow, rows: &[&StrictRow]) -> Result<(), String> {
    if rows.len() != sibling.usize("pairRows")? {
        return Err("directed-pair row count differs".to_owned());
    }
    let mut first_match = None;
    for (ordinal, row) in rows.iter().enumerate() {
        require_ordinal(row, "pairOrdinal", ordinal)?;
        let matches = row.bool("matches")?;
        let class_read = row.bool("classRead")?;
        match (first_match, class_read, matches, row.value("action")?) {
            (None, true, false, "Continue") => {}
            (None, true, true, "SelectBreak") => first_match = Some(ordinal),
            (Some(_), false, false, "UnreadAfterBreak") => {}
            _ => return Err("directed-pair lazy class-read trace differs".to_owned()),
        }
    }
    let expected = sibling.value("pairProvenanceSha256")?;
    if rows.is_empty() && sibling.value("pairState")? == "NotRead" {
        if expected != "NotRead" {
            return Err("non-read pair provenance is not literal NotRead".to_owned());
        }
    } else {
        let hash = sha256_rows(rows.iter().map(|row| {
            joined_row_token(
                row,
                &[
                    "pairOrdinal",
                    "sourceOutgoingOrdinal",
                    "graphRelationIdentity",
                    "relationObjectIdentity",
                    "runtimeClass",
                ],
            )
            .expect("validated pair fields")
        }));
        if hash != expected {
            return Err("directed-pair provenance hash differs".to_owned());
        }
    }
    let first_identity = first_match.map_or("-", |ordinal| {
        rows[ordinal]
            .value("graphRelationIdentity")
            .expect("validated graph identity")
    });
    if sibling.value("firstBeamStemIdentity")? != first_identity {
        return Err("first directed BeamStem identity differs".to_owned());
    }
    Ok(())
}

fn validate_geometry_row(
    geometry: &StrictRow,
    sibling: &StrictRow,
    baseline: &StrictRow,
) -> Result<(), String> {
    let branch = sibling.value("branch")?;
    if geometry.value("branch")? != branch
        || geometry.value("stemMedian")? != baseline.value("stemMedian")?
        || geometry.value("baseMedian")? != baseline.value("baseBeamMedian")?
        || geometry.value("baseCross")? != baseline.value("baseCross")?
        || geometry.value("baseLength")? != baseline.value("baseLength")?
        || geometry.value("maxShorterRatio")? != baseline.value("maxShorterRatio")?
        || geometry.value("yDir")? != baseline.value("yDir")?
        || geometry.value("xInGapMaximum0")? != baseline.value("xInGapMaximum0")?
        || geometry.value("maxDxRint")? != baseline.value("maxDxRint")?
    {
        return Err("sibling geometry/baseline join differs".to_owned());
    }
    for field in ["baseLength", "siblingLength", "ratio", "maxShorterRatio"] {
        parse_hex_bits(geometry.value(field)?)?;
    }
    let shorter = geometry.bool("shorterInclusive")?;
    if geometry.bool("dyRead")? != shorter
        || (shorter
            && (geometry.value("dy")? == "-"
                || geometry.value("product")? == "-"
                || geometry.value("wrongSideStrict")? == "-"))
        || (!shorter
            && (geometry.value("dy")? != "-"
                || geometry.value("product")? != "-"
                || geometry.value("wrongSideStrict")? != "-"))
    {
        return Err("lazy shorter-geometry read trace differs".to_owned());
    }
    if shorter {
        parse_hex_bits(geometry.value("dy")?)?;
        parse_hex_bits(geometry.value("product")?)?;
    }
    match branch {
        "ShorterWrongSide"
            if shorter
                && geometry.bool("wrongSideStrict")?
                && geometry.value("extension")? == "-"
                && geometry.value("beamHalfHeight")? == "-"
                && geometry.value("leftThreshold")? == "-"
                && geometry.value("rightThreshold")? == "-"
                && geometry.value("strictLeft")? == "-"
                && geometry.value("strictRight")? == "-"
                && geometry.value("portion")? == "-"
                && geometry.value("gradeSource")? == "NotRead"
                && geometry.value("gradeRead")? == "false"
                && geometry.value("grade")? == "-" => {}
        "Linked"
            if (!shorter || !geometry.bool("wrongSideStrict")?)
                && geometry.value("extension")? != "-"
                && geometry.value("beamHalfHeight")? != "-"
                && geometry.value("leftThreshold")? != "-"
                && geometry.value("rightThreshold")? != "-"
                && matches!(geometry.value("portion")?, "LEFT" | "CENTER" | "RIGHT")
                && geometry.value("gradeSource")? == "FreshBaseBeamStemDraft"
                && geometry.bool("gradeRead")?
                && geometry.value("grade")? == baseline.value("supportGrade")? =>
        {
            for field in ["beamHalfHeight", "leftThreshold", "rightThreshold", "grade"] {
                parse_hex_bits(geometry.value(field)?)?;
            }
            geometry.bool("strictLeft")?;
            geometry.bool("strictRight")?;
        }
        _ => return Err("branch-specific geometry laziness differs".to_owned()),
    }
    Ok(())
}

fn validate_edge_row(
    edge: &StrictRow,
    sibling: &StrictRow,
    predecessor: &StrictRow,
    geometry: &StrictRow,
) -> Result<(), String> {
    let expected_draft = format!(
        "sibling-draft:{}:{}",
        predecessor.value("plan")?,
        sibling.value("siblingOrdinal")?
    );
    if edge.value("freshRelationIdentity")? != expected_draft
        || edge.value("relationClass")? != "org.audiveris.omr.sig.relation.BeamStemRelation"
        || edge.value("sourceAlias")? != sibling.value("beamAlias")?
        || edge.value("sourceInterId")? != sibling.value("beamInterId")?
        || edge.value("targetAlias")? != predecessor.value("stemAlias")?
        || edge.value("targetInterId")? != predecessor.value("stemInterId")?
        || !edge.bool("insertionReturn")?
        || edge.bool("returnObservedByCaller")?
        || edge.value("graphRelationIdentity")?
            != format!("sig-edge:{}", edge.value("graphInsertionOrdinal")?)
        || edge.value("extension")? != geometry.value("extension")?
        || edge.value("portion")? != geometry.value("portion")?
        || edge.value("grade")? != predecessor.value("supportGrade")?
    {
        return Err("fresh sibling edge payload differs".to_owned());
    }
    parse_hex_bits(edge.value("grade")?)?;
    Ok(())
}

fn validate_incident_chronology(rows: &[&StrictRow]) -> Result<(), String> {
    let mut incoming = 0;
    let mut outgoing = 0;
    let mut saw_outgoing = false;
    for (incident, row) in rows.iter().enumerate() {
        require_ordinal(row, "incidentOrdinal", incident)?;
        match row.value("direction")? {
            "Incoming" if !saw_outgoing => {
                require_ordinal(row, "directionOrdinal", incoming)?;
                incoming += 1;
            }
            "Outgoing" => {
                saw_outgoing = true;
                require_ordinal(row, "directionOrdinal", outgoing)?;
                outgoing += 1;
            }
            _ => return Err("incident incoming/outgoing chronology differs".to_owned()),
        }
    }
    Ok(())
}

fn validate_callback_rows(
    callback: &StrictRow,
    edge: &StrictRow,
    stem_rows: &[&StrictRow],
    beam_rows: &[&StrictRow],
) -> Result<(), String> {
    validate_incident_chronology(stem_rows)?;
    validate_incident_chronology(beam_rows)?;
    let callback_event = callback.usize("eventOrdinal")?;
    if callback_event != edge.usize("eventOrdinal")? + 1
        || stem_rows
            .iter()
            .chain(beam_rows)
            .any(|row| row.usize("eventOrdinal") != Ok(callback_event))
        || !callback.bool("relationAdded")?
        || !callback.bool("extensionPrepopulated")?
        || !callback.bool("portionPrepopulated")?
        || callback.bool("defaultExtensionBranchRead")?
        || callback.bool("defaultPortionBranchRead")?
        || callback.usize("stemIncidentRows")? != stem_rows.len()
        || callback.usize("beamIncidentRows")? != beam_rows.len()
        || callback.usize("chordStemMatches")? != 0
    {
        return Err("callback envelope/count/event chronology differs".to_owned());
    }
    for row in stem_rows {
        let chord = row.bool("chordStemMatch")?;
        if !row.bool("classRead")?
            || row.bool("oppositeReadByGetChords")? != chord
            || row.value("oppositeEvidence")?
                != if chord {
                    "GetChordsRead"
                } else {
                    "GraphReconstruction"
                }
        {
            return Err("stem callback read/evidence trace differs".to_owned());
        }
    }
    for row in beam_rows {
        let class_read = row.value("readState")? != "UnreadAfterBreak";
        if row.bool("classRead")? != class_read
            || row.bool("oppositeReadByCheckAbnormal")?
            || row.value("oppositeEvidence")? != "GraphReconstruction"
        {
            return Err("beam callback read/evidence trace differs".to_owned());
        }
    }
    let stem_hash = sha256_rows(stem_rows.iter().map(|row| {
        joined_row_token(
            row,
            &[
                "incidentOrdinal",
                "direction",
                "directionOrdinal",
                "graphRelationIdentity",
                "relationObjectIdentity",
                "runtimeClass",
                "oppositeAlias",
                "oppositeInterId",
                "oppositeVertexOrdinal",
                "chordStemMatch",
            ],
        )
        .expect("validated stem incident fields")
    }));
    let beam_hash = sha256_rows(beam_rows.iter().map(|row| {
        joined_row_token(
            row,
            &[
                "incidentOrdinal",
                "direction",
                "directionOrdinal",
                "graphRelationIdentity",
                "relationObjectIdentity",
                "runtimeClass",
                "oppositeAlias",
                "oppositeInterId",
                "oppositeVertexOrdinal",
                "readState",
                "relevant",
                "portion",
                "contribution",
            ],
        )
        .expect("validated beam incident fields")
    }));
    if callback.value("stemIncidentHash")? != stem_hash
        || callback.value("beamIncidentHash")? != beam_hash
    {
        return Err("callback incident provenance hash differs".to_owned());
    }
    Ok(())
}

fn validate_lookup_rows(result: &StrictRow, rows: &[&StrictRow]) -> Result<(), String> {
    if result.usize("linkerLookupRows")? != rows.len() {
        return Err("builder lookup row count differs".to_owned());
    }
    let mut selected = false;
    for (ordinal, row) in rows.iter().enumerate() {
        require_ordinal(row, "itemOrdinal", ordinal)?;
        match (selected, row.value("action")?) {
            (false, "Continue") => {}
            (false, "SelectBreak") => selected = true,
            (true, "UnreadAfterBreak") => {}
            _ => return Err("builder first-source-identity scan differs".to_owned()),
        }
    }
    let hash = sha256_rows(rows.iter().map(|row| {
        joined_row_token(
            row,
            &[
                "itemOrdinal",
                "runtimeClass",
                "linkerRead",
                "sourceRead",
                "linkerAlias",
                "linkerRuntimeClass",
                "sourceAlias",
                "sourceInterId",
                "identityMatch",
                "action",
            ],
        )
        .expect("validated builder lookup fields")
    }));
    if result.value("linkerLookupHash")? != hash {
        return Err("builder lookup provenance hash differs".to_owned());
    }
    Ok(())
}

fn parse_list(value: &str) -> Result<Vec<&str>, String> {
    if value == "-" {
        return Ok(Vec::new());
    }
    let body = value
        .strip_prefix('[')
        .and_then(|value| value.strip_suffix(']'))
        .ok_or_else(|| format!("invalid list token {value}"))?;
    if body.is_empty() {
        return Err("empty list must use '-'".to_owned());
    }
    Ok(body.split(',').collect())
}

fn validate_linker_flag_row(flag: &StrictRow, sibling: &StrictRow) -> Result<(), String> {
    match flag.value("lookupState")? {
        "FirstSourceIdentityMatch" => {
            let selected = flag.value("selectedAlias")?;
            let observers = parse_list(flag.value("observerAliases")?)?;
            if flag.value("selectedSourceAlias")? != sibling.value("beamAlias")?
                || flag.value("sharedCell")? != format!("bcell:{selected}")
                || observers.first().copied() != Some(selected)
                || !flag.bool("linkedAfter")?
                || flag.value("closedBefore")? != flag.value("closedAfter")?
                || !flag.bool("requested")?
                || flag.usize("writeCount")? != 1
                || flag.usize("valueChangeCount")? != usize::from(!flag.bool("linkedBefore")?)
            {
                return Err("selected sibling B-linker shared-cell trace differs".to_owned());
            }
        }
        "ExhaustiveNoMatch"
            if flag.value("eventOrdinal")? == "-"
                && flag.value("selectedAlias")? == "-"
                && flag.value("observerAliases")? == "-"
                && flag.value("requested")? == "NotRead"
                && flag.usize("writeCount")? == 0
                && flag.usize("valueChangeCount")? == 0
                && flag.value("transition")? == "NoLinkerNoWrite" => {}
        _ => return Err("sibling linker-flag outcome differs".to_owned()),
    }
    Ok(())
}

fn validate_delta_guard(
    guard: &StrictRow,
    baseline: &StrictRow,
    result: &StrictRow,
    edges_added: usize,
    beam_abnormal_changes: usize,
) -> Result<(), String> {
    let unchanged_pairs = [
        ("allocatorBefore", "allocatorAfter"),
        ("glyphActiveHashBefore", "glyphActiveHashAfter"),
        ("glyphOriginalsHashBefore", "glyphOriginalsHashAfter"),
        ("interIndexCountBefore", "interIndexCountAfter"),
        ("sigVertexCountBefore", "sigVertexCountAfter"),
        ("systemStemsHashBefore", "systemStemsHashAfter"),
        ("lineHashBefore", "lineHashAfter"),
        ("arenaTopologyHashBefore", "arenaTopologyHashAfter"),
        ("builderItemsHashBefore", "builderItemsHashAfter"),
        ("relationInputHashBefore", "relationInputHashAfter"),
    ];
    if unchanged_pairs
        .iter()
        .any(|(before, after)| guard.value(before) != guard.value(after))
        || guard.usize("sigEdgeCountBefore")? != baseline.usize("graphEdgesBefore")?
        || guard.usize("sigEdgeCountAfter")? != guard.usize("sigEdgeCountBefore")? + edges_added
        || guard.value("groupStateHashBefore")? != baseline.value("groupStateHashBefore")?
        || guard.value("groupStateHashAfter")? != result.value("groupStateHashAfter")?
        || !is_lower_sha256(guard.value("groupStateHashBefore")?)
        || !is_lower_sha256(guard.value("groupStateHashAfter")?)
        || ((beam_abnormal_changes == 0)
            != (guard.value("groupStateHashBefore")? == guard.value("groupStateHashAfter")?))
        || guard.value("allowedMutations")?
            != "FreshSiblingBeamStemEdgesBeamAbnormalDirtySelectedBCells"
        || !guard.bool("zeroChordStem")?
        || !guard.bool("baseBeamStemUnchanged")?
        || !guard.bool("unrelatedRelationsUnchanged")?
        || !guard.bool("unrelatedLinkerFlagsUnchanged")?
        || guard.bool("headRelationLoopRead")?
        || !guard.bool("stopBeforeHeadRelationLoop")?
    {
        return Err("transaction delta guard differs".to_owned());
    }
    Ok(())
}

#[derive(Clone, Copy, Debug, Default)]
struct SupplementalCensus {
    supported: usize,
    envelope: usize,
    siblings: usize,
    same_glyph: usize,
    existing_beam_stem: usize,
    shorter_wrong_side: usize,
    linked: usize,
    edges: usize,
    flags: usize,
    events: usize,
}

#[derive(Clone, Copy)]
struct SupplementalCaseSpec {
    name: &'static str,
    scope: &'static str,
    sibling_class: &'static str,
    branch: &'static str,
    pair_state: &'static str,
    ratio_read: bool,
    shorter: Option<bool>,
    fresh_edge: bool,
    graph_delta: i64,
    include_linker: bool,
    write: bool,
    linked_before: bool,
    throw_class: &'static str,
    throw_stage: &'static str,
}

const SUPPLEMENTAL_CASES: &[SupplementalCaseSpec] = &[
    SupplementalCaseSpec {
        name: "SameGlyph",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "SameGlyph",
        pair_state: "NotReadSameGlyph",
        ratio_read: false,
        shorter: None,
        fresh_edge: false,
        graph_delta: 0,
        include_linker: true,
        write: false,
        linked_before: false,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "ExistingBeamStem",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "ExistingBeamStem",
        pair_state: "ExistingBeamStem",
        ratio_read: false,
        shorter: None,
        fresh_edge: false,
        graph_delta: 0,
        include_linker: true,
        write: false,
        linked_before: false,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "ShorterWrongSide",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "ShorterWrongSide",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(true),
        fresh_edge: false,
        graph_delta: 0,
        include_linker: true,
        write: false,
        linked_before: false,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "LinkedBeam",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "Linked",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(false),
        fresh_edge: true,
        graph_delta: 1,
        include_linker: true,
        write: true,
        linked_before: false,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "LinkedSmallBeam",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.SmallBeamInter",
        branch: "Linked",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(false),
        fresh_edge: true,
        graph_delta: 1,
        include_linker: true,
        write: true,
        linked_before: false,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "LinkedHook",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.BeamHookInter",
        branch: "Linked",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(false),
        fresh_edge: true,
        graph_delta: 1,
        include_linker: true,
        write: true,
        linked_before: false,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "LinkedNoBLinker",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "Linked",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(false),
        fresh_edge: true,
        graph_delta: 1,
        include_linker: false,
        write: false,
        linked_before: false,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "LinkedIdempotentBCell",
        scope: "synthetic",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "Linked",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(false),
        fresh_edge: true,
        graph_delta: 1,
        include_linker: true,
        write: true,
        linked_before: true,
        throw_class: "-",
        throw_stage: "-",
    },
    SupplementalCaseSpec {
        name: "ThrowBeforeInsertion",
        scope: "envelope",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "Linked",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(false),
        fresh_edge: false,
        graph_delta: 0,
        include_linker: true,
        write: false,
        linked_before: false,
        throw_class: "java.lang.IllegalArgumentException",
        throw_stage: "AddEdgeBeforeInsertion",
    },
    SupplementalCaseSpec {
        name: "ThrowDuringCallback",
        scope: "envelope",
        sibling_class: "org.audiveris.omr.sig.inter.BeamInter",
        branch: "Linked",
        pair_state: "Absent",
        ratio_read: true,
        shorter: Some(false),
        fresh_edge: true,
        graph_delta: 1,
        include_linker: true,
        write: false,
        linked_before: false,
        throw_class: "org.audiveris.omr.rustport.StemsBeamVLinkSiblingLinksProbe$SyntheticListenerException",
        throw_stage: "LaterListenerAfterRelationCallback",
    },
];

fn validate_supplemental_case(
    row: &StrictRow,
    spec: SupplementalCaseSpec,
    real_predecessor: &StrictRow,
) -> Result<(), String> {
    let key = row.key()?;
    let expected_terminal = if spec.scope == "envelope" {
        format!("Threw:{}", spec.throw_stage)
    } else {
        "ReadyBeforeHeadRelationLoop".to_owned()
    };
    let expected_lookup = match spec.name {
        "LinkedBeam" | "LinkedSmallBeam" | "LinkedHook" | "LinkedIdempotentBCell" => {
            "FirstSourceIdentityMatch"
        }
        "LinkedNoBLinker" => "ExhaustiveNoMatch",
        _ => "NotRead",
    };
    let expected_events = usize::from(spec.fresh_edge) * 2
        + usize::from(spec.write)
        + usize::from(spec.scope == "envelope");
    if key.system != 1
        || key.plan != real_predecessor.usize("plan")?
        || key.scope != spec.scope
        || key.case_name != spec.name
        || row.value("join")? != "IsolatedBoundary15Replay"
        || row.value("sourceRealB15EvidenceSha256")?
            != real_predecessor.value("b15TransactionEvidenceSha256")?
        || row.value("construction")? != "RealBookSheetSystemSIG"
        || row.value("baseRuntimeClass")? != "org.audiveris.omr.sig.inter.BeamInter"
        || row.value("siblingRuntimeClass")? != spec.sibling_class
        || row.bool("stemAttachedBefore")? != (spec.name != "ThrowBeforeInsertion")
        || row.value("yDir")? != "1"
        || row.bool("sameGlyphIdentity")? != (spec.branch == "SameGlyph")
        || row.value("pairState")? != spec.pair_state
        || row.bool("ratioRead")? != spec.ratio_read
        || row.value("branch")? != spec.branch
        || row.usize("builderItems")? != usize::from(spec.include_linker)
        || row.value("lookupState")? != expected_lookup
        || row.bool("siblingBLinkedBefore")? != spec.linked_before
        || row.bool("siblingBLinkedAfter")? != (spec.linked_before || spec.write)
        || row.usize("writeCount")? != usize::from(spec.write)
        || row.usize("valueChangeCount")? != usize::from(spec.write && !spec.linked_before)
        || row.i64("graphEdgesAfter")? - row.i64("graphEdgesBefore")? != spec.graph_delta
        || (row.value("freshRelationIdentity")? != "-") != spec.fresh_edge
        || row.bool("callbackCompleted")? != spec.fresh_edge
        || row.usize("chordStemMatches")? != 0
        || row.value("throwClass")? != spec.throw_class
        || row.value("throwStage")? != spec.throw_stage
        || row.usize("eventCount")? != expected_events
        || row.value("terminal")? != expected_terminal
        || row.value("supportGrade")? != real_predecessor.value("supportGrade")?
    {
        return Err(format!("isolated case {} topology differs", spec.name));
    }
    for field in [
        "baseLength",
        "siblingLength",
        "maxShorterRatio",
        "supportGrade",
    ] {
        parse_hex_bits(row.value(field)?)?;
    }
    if spec.ratio_read {
        parse_hex_bits(row.value("ratio")?)?;
    } else if row.value("ratio")? != "-" {
        return Err(format!("isolated case {} eagerly read ratio", spec.name));
    }
    match spec.shorter {
        None if row.value("shorterInclusive")? == "-"
            && !row.bool("dyRead")?
            && row.value("dy")? == "-"
            && row.value("product")? == "-"
            && row.value("wrongSideStrict")? == "-" => {}
        Some(true)
            if row.bool("shorterInclusive")?
                && row.bool("dyRead")?
                && row.bool("wrongSideStrict")? =>
        {
            parse_hex_bits(row.value("dy")?)?;
            parse_hex_bits(row.value("product")?)?;
        }
        Some(false)
            if !row.bool("shorterInclusive")?
                && !row.bool("dyRead")?
                && row.value("dy")? == "-"
                && row.value("product")? == "-"
                && row.value("wrongSideStrict")? == "-" => {}
        _ => {
            return Err(format!(
                "isolated case {} shorter laziness differs",
                spec.name
            ));
        }
    }
    if spec.branch == "Linked" {
        if row.value("extension")? == "-"
            || !matches!(row.value("portion")?, "LEFT" | "CENTER" | "RIGHT")
        {
            return Err(format!("isolated case {} link geometry differs", spec.name));
        }
    } else if row.value("extension")? != "-" || row.value("portion")? != "-" {
        return Err(format!(
            "isolated case {} eagerly read link geometry",
            spec.name
        ));
    }
    if spec.fresh_edge {
        if row.value("stemIncidentState")? != "ExhaustiveIncomingThenOutgoing"
            || row.usize("stemIncidentRows")? == 0
            || row.value("beamIncidentState")? == "NotRead"
            || row.value("beamRule")? == "NotRead"
        {
            return Err(format!("isolated case {} callback scan differs", spec.name));
        }
    } else if row.value("stemIncidentState")? != "NotRead"
        || row.usize("stemIncidentRows")? != 0
        || row.value("beamIncidentState")? != "NotRead"
        || row.value("beamRule")? != "NotRead"
    {
        return Err(format!("isolated case {} eagerly read callback", spec.name));
    }
    row.bool("beamAbnormalBefore")?;
    row.bool("beamAbnormalAfter")?;
    if row.value("dirtyBefore")?.is_empty() || row.value("dirtyAfter")?.is_empty() {
        return Err(format!(
            "isolated case {} lacks dirty-state evidence",
            spec.name
        ));
    }
    Ok(())
}

fn validate_supplemental_rows(
    cursor: &mut RowCursor<'_>,
    page: &StrictRow,
    parsed: &[ParsedTransaction],
) -> Result<SupplementalCensus, String> {
    let real_predecessor = parsed
        .iter()
        .find(|transaction| transaction.key.scope == "real" && transaction.key.system == 1)
        .map(|transaction| &transaction.predecessor)
        .ok_or_else(|| "supplemental cases lack the real system-1 predecessor".to_owned())?;
    let mut census = SupplementalCensus::default();
    for spec in SUPPLEMENTAL_CASES {
        let case = cursor
            .rows
            .get(cursor.index)
            .ok_or_else(|| format!("missing isolated case {}", spec.name))?;
        if case.kind != RowKind::SyntheticCase || case.page != page.page {
            return Err(format!("isolated case order differs before {}", spec.name));
        }
        let key = case.key()?;
        validate_supplemental_case(case, *spec, real_predecessor)?;
        cursor.index += 1;

        let mut event_ordinal = 0;
        let mut event_kinds = Vec::new();
        while cursor.peek_kind() == Some(RowKind::SyntheticEvent) {
            let event = cursor.take(RowKind::SyntheticEvent, &key)?;
            require_ordinal(event, "eventOrdinal", event_ordinal)?;
            event_ordinal += 1;
            let kind = event.value("kind")?;
            if !matches!(
                kind,
                "SigEdgeInserted" | "RelationCallbackCompleted" | "BLinkerLinkedAssigned" | "Throw"
            ) {
                return Err(format!("unknown isolated event kind {kind}"));
            }
            event_kinds.push((kind, event.value("relationIdentity")?));
        }
        if event_ordinal != case.usize("eventCount")? {
            return Err(format!("isolated case {} event count differs", spec.name));
        }
        let fresh = case.value("freshRelationIdentity")?;
        let mut expected_events = Vec::new();
        if spec.fresh_edge {
            expected_events.push(("SigEdgeInserted", fresh));
            expected_events.push(("RelationCallbackCompleted", fresh));
        }
        if spec.write {
            expected_events.push(("BLinkerLinkedAssigned", "-"));
        }
        if spec.scope == "envelope" {
            expected_events.push(("Throw", fresh));
        }
        if event_kinds != expected_events {
            return Err(format!("isolated case {} event prefix differs", spec.name));
        }

        let guard = cursor.take(RowKind::SyntheticGuard, &key)?;
        if guard.i64("graphDelta")? != spec.graph_delta
            || guard.value("allowedMutations")?
                != "FreshSiblingBeamStemBeamAbnormalDirtySelectedBCell"
            || !guard.bool("baseBeamUnchanged")?
            || !guard.bool("stemGeometryUnchanged")?
            || !guard.bool("groupObjectUnchanged")?
            || !guard.bool("zeroChordStem")?
            || !guard.bool("isolatedOnly")?
            || guard.bool("productionEquivalent")?
            || !guard.bool("enclosingRealSheetUnchanged")?
            || guard.bool("headRelationLoopRead")?
            || guard.value("terminal")? != case.value("terminal")?
        {
            return Err(format!("isolated case {} guard differs", spec.name));
        }

        census.supported += usize::from(spec.scope == "synthetic");
        census.envelope += usize::from(spec.scope == "envelope");
        census.siblings += 1;
        census.same_glyph += usize::from(spec.branch == "SameGlyph");
        census.existing_beam_stem += usize::from(spec.branch == "ExistingBeamStem");
        census.shorter_wrong_side += usize::from(spec.branch == "ShorterWrongSide");
        census.linked += usize::from(spec.branch == "Linked");
        census.edges += usize::from(spec.fresh_edge);
        census.flags += usize::from(spec.write);
        census.events += event_ordinal;
    }
    Ok(census)
}

fn validate_core_rows(rows: &[StrictRow]) -> Result<Vec<ParsedTransaction>, String> {
    let page = rows
        .first()
        .ok_or_else(|| "fixture has no semantic rows".to_owned())?;
    validate_page_row(page)?;
    if rows.iter().skip(1).any(|row| row.kind == RowKind::Page) {
        return Err("fixture contains more than one page row".to_owned());
    }
    let mut cursor = RowCursor::new(rows, 1);
    let mut parsed = Vec::new();
    let mut real_systems = BTreeSet::new();
    let mut real_transactions = 0;
    let mut supported_synthetic_cases = 0;
    let mut envelope_cases = 0;
    let mut total_group_rows = 0;
    let mut total_siblings = 0;
    let mut total_branches = BTreeMap::from([
        ("SameGlyph", 0usize),
        ("ExistingBeamStem", 0),
        ("ShorterWrongSide", 0),
        ("Linked", 0),
    ]);
    let mut total_edges = 0;
    let mut total_flags = 0;
    let mut total_events = 0;
    let mut supplemental = None;
    while cursor.peek_kind() == Some(RowKind::Predecessor) {
        let transaction_start = cursor.index;
        let first = &rows[cursor.index];
        if first.kind != RowKind::Predecessor {
            return Err(format!(
                "transaction does not start with predecessor at semantic row {}",
                cursor.index
            ));
        }
        let key = first.key()?;
        if key.page != page.page {
            return Err("transaction page differs from page row".to_owned());
        }
        let predecessor = cursor.take(RowKind::Predecessor, &key)?;
        validate_predecessor_row(predecessor)?;
        let baseline = cursor.take(RowKind::Baseline, &key)?;
        validate_baseline_row(baseline, predecessor)?;

        let mut group_rows = Vec::new();
        for _ in 0..baseline.usize("groupOutgoingScanned")? {
            group_rows.push(cursor.take(RowKind::GroupMember, &key)?);
        }
        validate_group_rows(baseline, &group_rows)?;

        let mut branch_counts = BTreeMap::from([
            ("SameGlyph", 0usize),
            ("ExistingBeamStem", 0),
            ("ShorterWrongSide", 0),
            ("Linked", 0),
        ]);
        let mut committed_edges = 0;
        let mut committed_flags = 0;
        let mut beam_abnormal_changes = 0;
        let mut event_count = 0;
        for sibling_ordinal in 0..baseline.usize("siblings")? {
            let sibling = cursor.take(RowKind::Sibling, &key)?;
            require_ordinal(sibling, "siblingOrdinal", sibling_ordinal)?;
            let branch = sibling.value("branch")?;
            let count = branch_counts
                .get_mut(branch)
                .ok_or_else(|| format!("unknown sibling branch {branch}"))?;
            *count += 1;

            let mut source_rows = Vec::new();
            for _ in 0..sibling.usize("sourceOutgoingScanned")? {
                let row = cursor.take(RowKind::SourceOutgoing, &key)?;
                require_ordinal(row, "siblingOrdinal", sibling_ordinal)?;
                source_rows.push(row);
            }
            validate_source_rows(sibling, &source_rows)?;

            let mut pair_rows = Vec::new();
            for _ in 0..sibling.usize("pairRows")? {
                let row = cursor.take(RowKind::PairRelation, &key)?;
                require_ordinal(row, "siblingOrdinal", sibling_ordinal)?;
                pair_rows.push(row);
            }
            validate_pair_rows(sibling, &pair_rows)?;

            let geometry = match branch {
                "SameGlyph" | "ExistingBeamStem" => None,
                "ShorterWrongSide" | "Linked" => {
                    let row = cursor.take(RowKind::Geometry, &key)?;
                    require_ordinal(row, "siblingOrdinal", sibling_ordinal)?;
                    validate_geometry_row(row, sibling, baseline)?;
                    Some(row)
                }
                _ => unreachable!("branch domain checked above"),
            };

            let mut lookup_rows = Vec::new();
            let mut linker_flag = None;
            if branch == "Linked" {
                let edge = cursor.take(RowKind::Edge, &key)?;
                require_ordinal(edge, "siblingOrdinal", sibling_ordinal)?;
                if edge.usize("eventOrdinal")? != event_count {
                    return Err("edge global event ordinal differs".to_owned());
                }
                validate_edge_row(
                    edge,
                    sibling,
                    predecessor,
                    geometry.expect("Linked branch has geometry"),
                )?;
                committed_edges += 1;
                event_count += 1;

                let mut stem_rows = Vec::new();
                while cursor.peek_kind() == Some(RowKind::StemIncident) {
                    let row = cursor.take(RowKind::StemIncident, &key)?;
                    require_ordinal(row, "siblingOrdinal", sibling_ordinal)?;
                    stem_rows.push(row);
                }
                let mut beam_rows = Vec::new();
                while cursor.peek_kind() == Some(RowKind::BeamIncident) {
                    let row = cursor.take(RowKind::BeamIncident, &key)?;
                    require_ordinal(row, "siblingOrdinal", sibling_ordinal)?;
                    beam_rows.push(row);
                }
                let callback = cursor.take(RowKind::Callback, &key)?;
                require_ordinal(callback, "siblingOrdinal", sibling_ordinal)?;
                validate_callback_rows(callback, edge, &stem_rows, &beam_rows)?;
                beam_abnormal_changes += usize::from(callback.bool("abnormalChanged")?);
                event_count += 1;

                while cursor.peek_kind() == Some(RowKind::LinkerLookup) {
                    let row = cursor.take(RowKind::LinkerLookup, &key)?;
                    require_ordinal(row, "siblingOrdinal", sibling_ordinal)?;
                    lookup_rows.push(row);
                }
                let flag = cursor.take(RowKind::LinkerFlag, &key)?;
                require_ordinal(flag, "siblingOrdinal", sibling_ordinal)?;
                validate_linker_flag_row(flag, sibling)?;
                if flag.value("lookupState")? == "FirstSourceIdentityMatch" {
                    if flag.usize("eventOrdinal")? != event_count {
                        return Err("linker-flag global event ordinal differs".to_owned());
                    }
                    event_count += 1;
                    committed_flags += 1;
                }
                linker_flag = Some(flag);
            }

            let sibling_result = cursor.take(RowKind::SiblingResult, &key)?;
            require_ordinal(sibling_result, "siblingOrdinal", sibling_ordinal)?;
            if sibling_result.value("branch")? != branch
                || sibling_result.value("terminal")? != "Continue"
                || sibling_result.bool("edgeCommitted")? != (branch == "Linked")
                || sibling_result.bool("linkerLookupRead")? != (branch == "Linked")
                || sibling_result.usize("edgePrefixCount")? != committed_edges
                || sibling_result.usize("flagPrefixCount")? != committed_flags
            {
                return Err("sibling terminal/prefix census differs".to_owned());
            }
            if branch == "Linked" {
                validate_lookup_rows(sibling_result, &lookup_rows)?;
                let flag = linker_flag.expect("Linked branch has linker-flag row");
                if sibling_result.value("linkerLookupState")? != flag.value("lookupState")?
                    || sibling_result.value("linkerSelectedAlias")?
                        != flag.value("selectedAlias")?
                {
                    return Err("sibling lookup/result join differs".to_owned());
                }
            } else if sibling_result.value("linkerLookupState")? != "NotRead"
                || sibling_result.value("linkerLookupTiming")? != "NotRead"
                || sibling_result.usize("linkerLookupRows")? != 0
                || sibling_result.value("linkerLookupHash")? != "NotRead"
                || sibling_result.value("linkerSelectedAlias")? != "-"
            {
                return Err("non-linked sibling lookup was not lazy".to_owned());
            }
        }

        let result = cursor.take(RowKind::Result, &key)?;
        let guard = cursor.take(RowKind::DeltaGuard, &key)?;
        let summary = cursor.take(RowKind::Summary, &key)?;
        if result.value("terminal")? != "ReadyBeforeHeadRelationLoop"
            || result.value("supportGrade")? != predecessor.value("supportGrade")?
            || result.value("stemAlias")? != predecessor.value("stemAlias")?
            || result.value("stemInterId")? != predecessor.value("stemInterId")?
            || result.usize("siblings")? != baseline.usize("siblings")?
            || result.usize("committedEdges")? != committed_edges
            || result.usize("committedFlags")? != committed_flags
            || result.usize("eventCount")? != event_count
            || result.bool("headRelationLoopRead")?
            || summary.usize("groupRows")? != group_rows.len()
            || summary.usize("siblings")? != baseline.usize("siblings")?
            || summary.usize("sameGlyph")? != branch_counts["SameGlyph"]
            || summary.usize("existingBeamStem")? != branch_counts["ExistingBeamStem"]
            || summary.usize("shorterWrongSide")? != branch_counts["ShorterWrongSide"]
            || summary.usize("linked")? != branch_counts["Linked"]
            || summary.usize("edgesAdded")? != committed_edges
            || summary.usize("linkerWrites")? != committed_flags
            || summary.usize("events")? != event_count
            || summary.usize("chordStemMatches")? != 0
            || summary.value("terminal")? != "ReadyBeforeHeadRelationLoop"
        {
            return Err("transaction result/summary census differs".to_owned());
        }
        validate_delta_guard(
            guard,
            baseline,
            result,
            committed_edges,
            beam_abnormal_changes,
        )?;
        if key.scope == "real" && key.case_name == "-" {
            real_transactions += 1;
            real_systems.insert(key.system);
        } else {
            return Err(format!(
                "core transaction is not a real whole-page transaction: {:?}",
                key
            ));
        }
        total_group_rows += group_rows.len();
        total_siblings += baseline.usize("siblings")?;
        for branch in [
            "SameGlyph",
            "ExistingBeamStem",
            "ShorterWrongSide",
            "Linked",
        ] {
            total_branches.insert(branch, total_branches[branch] + branch_counts[branch]);
        }
        total_edges += committed_edges;
        total_flags += committed_flags;
        total_events += event_count;
        parsed.push(ParsedTransaction {
            key,
            predecessor: predecessor.clone(),
            rows: rows[transaction_start..cursor.index].to_vec(),
        });
        if cursor.peek_kind() == Some(RowKind::SyntheticCase) {
            if supplemental.is_some() {
                return Err("fixture contains more than one isolated system-1 block".to_owned());
            }
            supplemental = Some(validate_supplemental_rows(&mut cursor, page, &parsed)?);
        }
    }
    if parsed.is_empty() {
        return Err("fixture has no transactions".to_owned());
    }
    let supplemental = supplemental
        .ok_or_else(|| "fixture lacks its isolated system-1 supplemental block".to_owned())?;
    supported_synthetic_cases += supplemental.supported;
    envelope_cases += supplemental.envelope;
    total_siblings += supplemental.siblings;
    total_branches.insert(
        "SameGlyph",
        total_branches["SameGlyph"] + supplemental.same_glyph,
    );
    total_branches.insert(
        "ExistingBeamStem",
        total_branches["ExistingBeamStem"] + supplemental.existing_beam_stem,
    );
    total_branches.insert(
        "ShorterWrongSide",
        total_branches["ShorterWrongSide"] + supplemental.shorter_wrong_side,
    );
    total_branches.insert("Linked", total_branches["Linked"] + supplemental.linked);
    total_edges += supplemental.edges;
    total_flags += supplemental.flags;
    total_events += supplemental.events;
    if cursor.peek_kind() == Some(RowKind::PageSummary) {
        let summary = &rows[cursor.index];
        cursor.index += 1;
        if summary.page != page.page
            || summary.usize("systems")? != page.usize("systems")?
            || summary.usize("systems")? != real_systems.len()
            || summary.usize("realTransactions")? != real_transactions
            || summary.usize("supportedSyntheticCases")? != supported_synthetic_cases
            || summary.usize("envelopeCases")? != envelope_cases
            || summary.usize("totalTransactions")?
                != parsed.len() + supported_synthetic_cases + envelope_cases
            || summary.usize("groupRows")? != total_group_rows
            || summary.usize("siblingCandidates")? != total_siblings
            || summary.usize("sameGlyph")? != total_branches["SameGlyph"]
            || summary.usize("existingBeamStem")? != total_branches["ExistingBeamStem"]
            || summary.usize("shorterWrongSide")? != total_branches["ShorterWrongSide"]
            || summary.usize("linked")? != total_branches["Linked"]
            || summary.usize("edgesAdded")? != total_edges
            || summary.usize("linkerWrites")? != total_flags
            || summary.usize("events")? != total_events
            || summary.usize("chordStemMatches")? != 0
            || !summary.bool("stopBeforeHeadRelationLoop")?
        {
            return Err("page-summary census differs".to_owned());
        }
    } else {
        return Err("fixture lacks its page-summary trailer".to_owned());
    }
    if cursor.peek_kind() == Some(RowKind::CorpusSummary) {
        cursor.index += 1;
    } else {
        return Err("fixture lacks its corpus-summary trailer".to_owned());
    }
    if cursor.index != rows.len() {
        return Err(format!(
            "unexpected semantic row after core/page summary: {:?}",
            rows[cursor.index].kind
        ));
    }
    Ok(parsed)
}

fn raw_fields(line: &str) -> Result<(String, BTreeMap<String, String>), String> {
    let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
    let page = tokens
        .get(1)
        .ok_or_else(|| "predecessor row lacks page token".to_owned())?;
    if (tokens.len() - 2) % 2 != 0 {
        return Err("predecessor row lacks key/value pairs".to_owned());
    }
    let mut fields = BTreeMap::new();
    for pair in tokens[2..].chunks_exact(2) {
        if fields
            .insert(pair[0].to_owned(), pair[1].to_owned())
            .is_some()
        {
            return Err(format!("duplicate predecessor field {}", pair[0]));
        }
    }
    Ok(((*page).to_owned(), fields))
}

fn required_map<'a>(fields: &'a BTreeMap<String, String>, name: &str) -> Result<&'a str, String> {
    fields
        .get(name)
        .map(String::as_str)
        .ok_or_else(|| format!("missing predecessor field {name}"))
}

fn validate_manifest_fixture_entry(key: &str, expected_sha256: &str) -> Result<(), String> {
    let manifest = std::fs::read_to_string(repo_root().join(BOUNDARY_FIFTEEN_MANIFEST_PATH))
        .map_err(|error| format!("cannot read Boundary-15 manifest: {error}"))?;
    let matches = manifest
        .lines()
        .filter(|line| line.starts_with("stemsbeamvlinkblinkerflagmanifestentry "))
        .filter_map(|line| {
            let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
            ((tokens.len() - 1) % 2 == 0).then(|| {
                tokens[1..]
                    .chunks_exact(2)
                    .map(|pair| (pair[0].to_owned(), pair[1].to_owned()))
                    .collect::<BTreeMap<_, _>>()
            })
        })
        .filter(|fields| fields.get("page").is_some_and(|page| page == key))
        .collect::<Vec<_>>();
    let [entry] = matches.as_slice() else {
        return Err(format!("Boundary-15 manifest lacks one {key} entry"));
    };
    if required_map(entry, "fixture")? != format!("stems-beam-vlink-b-linker-flag-{key}.txt")
        || required_map(entry, "fixtureSha256")? != expected_sha256
    {
        return Err(format!("Boundary-15 {key} manifest fixture pin differs"));
    }
    Ok(())
}

fn validate_manifest_fixture_pin() -> Result<(), String> {
    validate_manifest_fixture_entry("chula", BOUNDARY_FIFTEEN_FIXTURE_SHA256)
}

fn validate_boundary_fifteen_bundle(
    transaction: &ParsedTransaction,
    fixture: &str,
) -> Result<(), String> {
    let predecessor = &transaction.predecessor;
    let mut ordered = Vec::new();
    let mut result_row = None;
    let mut guard_row = None;
    let mut summary_row = None;
    let mut b15_predecessor = None;
    for line in fixture.lines() {
        if !line.starts_with("stemsbeamvlinkblinkerflag")
            || line.starts_with("stemsbeamvlinkblinkerflagpage ")
            || line.starts_with("stemsbeamvlinkblinkerflagpagesummary ")
            || line.starts_with("stemsbeamvlinkblinkerflagcorpussummary ")
        {
            continue;
        }
        let (page, fields) = raw_fields(line)?;
        if page != transaction.key.page
            || required_map(&fields, "scope")? != "real"
            || required_map(&fields, "system")? != transaction.key.system.to_string()
        {
            continue;
        }
        if required_map(&fields, "plan")? != transaction.key.plan.to_string()
            || required_map(&fields, "case")? != "-"
        {
            return Err("Boundary-15 real system row key differs".to_owned());
        }
        ordered.push(line.to_owned());
        if line.starts_with("stemsbeamvlinkblinkerflagpredecessor ") {
            if b15_predecessor.replace(fields).is_some() {
                return Err("duplicate Boundary-15 predecessor row".to_owned());
            }
        } else if line.starts_with("stemsbeamvlinkblinkerflagresult ") {
            if result_row.replace((line, fields)).is_some() {
                return Err("duplicate Boundary-15 result row".to_owned());
            }
        } else if line.starts_with("stemsbeamvlinkblinkerflagdeltaguard ") {
            if guard_row.replace(line).is_some() {
                return Err("duplicate Boundary-15 guard row".to_owned());
            }
        } else if line.starts_with("stemsbeamvlinkblinkerflagsummary ")
            && summary_row.replace(line).is_some()
        {
            return Err("duplicate Boundary-15 summary row".to_owned());
        }
    }
    let b15_predecessor =
        b15_predecessor.ok_or_else(|| "missing Boundary-15 predecessor row".to_owned())?;
    let (result_raw, result) =
        result_row.ok_or_else(|| "missing Boundary-15 result row".to_owned())?;
    let guard_raw = guard_row.ok_or_else(|| "missing Boundary-15 guard row".to_owned())?;
    let summary_raw = summary_row.ok_or_else(|| "missing Boundary-15 summary row".to_owned())?;
    let base_beam_alias = required_map(&result, "bAlias")?
        .split_once(":b:")
        .map(|(beam, _)| beam)
        .ok_or_else(|| "Boundary-15 B alias lacks beam prefix".to_owned())?;
    if predecessor.usize("b15TransactionRows")? != ordered.len()
        || predecessor.value("b15TransactionEvidenceSha256")?
            != sha256_rows(ordered.iter().cloned())
        || predecessor.value("b15ResultRowSha256")? != sha256_rows([result_raw.to_owned()])
        || predecessor.value("b15GuardRowSha256")? != sha256_rows([guard_raw.to_owned()])
        || predecessor.value("b15SummaryRowSha256")? != sha256_rows([summary_raw.to_owned()])
        || predecessor.value("predecessorTerminal")? != required_map(&result, "terminal")?
        || predecessor.value("applyReturn")? != required_map(&result, "applyReturn")?
        || predecessor.value("supportGrade")? != required_map(&result, "supportGrade")?
        || predecessor.value("stemAlias")? != required_map(&result, "stemAlias")?
        || predecessor.value("stemInterId")? != required_map(&result, "stemInterId")?
        || predecessor.value("baseBeamAlias")? != base_beam_alias
        || predecessor.value("targetBAlias")? != required_map(&result, "bAlias")?
        || predecessor.value("triggeringVAlias")? != required_map(&b15_predecessor, "vAlias")?
        || predecessor.value("targetBLinked")? != required_map(&result, "linked")?
        || predecessor.value("targetBLinked")? != "true"
    {
        return Err("Boundary-15 canonical all-row bundle/full terminal join differs".to_owned());
    }
    Ok(())
}

fn boundary_fifteen_linked_before(
    fixture: &str,
    transaction: &ParsedTransaction,
) -> Result<bool, String> {
    let matches = fixture
        .lines()
        .filter(|line| line.starts_with("stemsbeamvlinkblinkerflagtarget "))
        .filter_map(|line| raw_fields(line).ok())
        .filter(|(page, fields)| {
            page == &transaction.key.page
                && fields
                    .get("system")
                    .is_some_and(|value| value == &transaction.key.system.to_string())
                && fields
                    .get("plan")
                    .is_some_and(|value| value == &transaction.key.plan.to_string())
                && fields.get("scope").is_some_and(|value| value == "real")
                && fields.get("case").is_some_and(|value| value == "-")
        })
        .collect::<Vec<_>>();
    let [(_, fields)] = matches.as_slice() else {
        return Err(format!(
            "Boundary-15 fixture has {} target rows for {:?}",
            matches.len(),
            transaction.key
        ));
    };
    match required_map(fields, "linkedBefore")? {
        "true" => Ok(true),
        "false" => Ok(false),
        value => Err(format!("invalid Boundary-15 linkedBefore {value}")),
    }
}

fn validate_boundary_fifteen_predecessors(
    page_row: &StrictRow,
    transactions: &[ParsedTransaction],
) -> Result<(), String> {
    let (page_key, _) = corpus_page_for_token(&page_row.page)?;
    let fixture_path = boundary_fifteen_fixture_path(page_key);
    let fixture_sha256 = read_sha256(&fixture_path)?;
    if read_sha256(BOUNDARY_FIFTEEN_MANIFEST_PATH)? != BOUNDARY_FIFTEEN_MANIFEST_SHA256
        || read_sha256(BOUNDARY_FIFTEEN_GATE_PATH)? != BOUNDARY_FIFTEEN_GATE_SHA256
        || page_row.value("bLinkerFlagFixtureSha256")? != fixture_sha256
    {
        return Err("Boundary-15 manifest/gate/fixture source pin differs".to_owned());
    }
    validate_manifest_fixture_entry(page_key, &fixture_sha256)?;
    let fixture = std::fs::read_to_string(repo_root().join(&fixture_path))
        .map_err(|error| format!("cannot read Boundary-15 {page_key} fixture: {error}"))?;
    let b15_page = fixture
        .lines()
        .find(|line| line.starts_with("stemsbeamvlinkblinkerflagpage "))
        .ok_or_else(|| "Boundary-15 fixture lacks page row".to_owned())?;
    let (b15_page_name, b15_page_fields) = raw_fields(b15_page)?;
    if b15_page_name != page_row.page {
        return Err("Boundary-15/B16 page identity differs".to_owned());
    }
    for field in [
        "schedulerFixtureSha256",
        "expandFixtureSha256",
        "createStemFixtureSha256",
        "reuseCheckFixtureSha256",
        "baseApplyFixtureSha256",
    ] {
        if page_row.value(field)? != required_map(&b15_page_fields, field)? {
            return Err(format!("Boundary-15/B16 {field} predecessor pin differs"));
        }
    }
    for transaction in transactions {
        validate_boundary_fifteen_bundle(transaction, &fixture)?;
    }
    Ok(())
}

#[derive(Clone, Copy, Debug, Eq, Ord, PartialEq, PartialOrd)]
struct BeamId(usize);

#[derive(Clone, Copy, Debug, Eq, Ord, PartialEq, PartialOrd)]
struct StemId(usize);

#[derive(Clone, Copy, Debug, Eq, Ord, PartialEq, PartialOrd)]
struct LinkerCellId(usize);

#[derive(Clone, Copy, Debug, Eq, Ord, PartialEq, PartialOrd)]
struct LinkerId(usize);

#[derive(Clone, Copy, Debug, Eq, Ord, PartialEq, PartialOrd)]
enum Vertex {
    Beam(BeamId),
    Stem(StemId),
}

#[derive(Clone, Copy, Debug, PartialEq)]
struct Point {
    x: f64,
    y: f64,
}

#[derive(Clone, Copy, Debug, PartialEq)]
struct Line {
    x1: f64,
    y1: f64,
    x2: f64,
    y2: f64,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum BeamRuntimeClass {
    Beam,
    Hook,
    SmallBeam,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum BeamPortion {
    Left,
    Center,
    Right,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum RelationPayload {
    BeamStem {
        portion: BeamPortion,
        grade_bits: u64,
    },
    BeamRest {
        portion: BeamPortion,
    },
    ChordStem,
    Other,
}

#[derive(Clone, Debug, Eq, PartialEq)]
struct GraphEdge {
    relation_identity: usize,
    source: Vertex,
    target: Vertex,
    payload: RelationPayload,
}

#[derive(Clone, Debug, PartialEq)]
struct BeamState {
    runtime_class: BeamRuntimeClass,
    /// Java object identity. `None == None` deliberately models two null
    /// glyphs satisfying `b.getGlyph() == beam.getGlyph()`.
    glyph_identity: Option<usize>,
    median: Line,
    height: f64,
    abnormal: bool,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum BuilderItem {
    NonLinker,
    Linker {
        source: BeamId,
        linker: LinkerId,
        linked_cell: LinkerCellId,
    },
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum AddEdgeBehavior {
    Added,
    ReturnedFalse,
    ThrowBeforeInsertion,
    ThrowDuringCallback,
}

#[derive(Clone, Debug, PartialEq)]
struct IndependentState {
    vertices: BTreeSet<Vertex>,
    /// JGraphT global insertion order. Incident and directed-pair scans are
    /// stable filters over this order in the independent model.
    edges: Vec<GraphEdge>,
    beams: BTreeMap<BeamId, BeamState>,
    builder_items: Vec<BuilderItem>,
    linked_cells: BTreeMap<LinkerCellId, bool>,
    /// Exact sibling B-linker first, then its V children in TOP/BOTTOM order.
    /// The selected ordinary `LinkerItem` owns this shared cell directly.
    linked_cell_observers: BTreeMap<LinkerCellId, Vec<LinkerId>>,
    stub_modified: bool,
    book_modified: bool,
    book_dirty: bool,
    next_relation_identity: usize,
}

#[derive(Clone, Debug, PartialEq)]
struct IndependentInput {
    base_beam: BeamId,
    stem: StemId,
    ref_point: Point,
    skewed_vertical: Line,
    stem_median: Line,
    group_members: Vec<BeamId>,
    max_beam_side_dx: f64,
    max_shorter_ratio: f64,
    portion_max_dx: i32,
    y_dir: i32,
    continuation_support_grade_bits: u64,
    add_edge_behavior: BTreeMap<BeamId, AddEdgeBehavior>,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum SiblingBranch {
    SameGlyph,
    ExistingBeamStem,
    ShorterWrongSide,
    Linked,
}

const fn native_branch(branch: SiblingBranch) -> NativeStemsBeamSiblingBranch {
    match branch {
        SiblingBranch::SameGlyph => NativeStemsBeamSiblingBranch::SameGlyph,
        SiblingBranch::ExistingBeamStem => NativeStemsBeamSiblingBranch::ExistingBeamStem,
        SiblingBranch::ShorterWrongSide => NativeStemsBeamSiblingBranch::ShorterWrongSide,
        SiblingBranch::Linked => NativeStemsBeamSiblingBranch::Linked,
    }
}

fn assert_public_baseline_projection(transaction: &NativeStemsBeamVLinkSiblingLinksTransaction) {
    let _ = (
        &transaction.cached_base_median,
        transaction.cached_base_median_same_identity,
        &transaction.group_runtime,
        transaction.base_cross,
        transaction.base_length,
    );
    for sibling in &transaction.siblings {
        let _ = (
            &sibling.builder_lookup,
            sibling.closed_before,
            sibling.closed_after,
        );
    }
    for operation in &transaction.operations {
        if let NativeStemsBeamVLinkSiblingLinksOperation::BLinkerLinkedAssigned {
            ordered_observer_v_linkers,
            closed_before,
            closed_after,
            ..
        } = operation
        {
            let _ = (ordered_observer_v_linkers, closed_before, closed_after);
        }
    }
}

fn assert_public_state_projection(
    state: &NativeStemsBeamVLinkSiblingLinksState,
    live: &NativeStemsBeamSiblingLiveBeam,
) {
    let _ = (
        &state.b_linker_flag_state_after,
        live.inter_index_ordinal,
        live.inter_index_object_matches,
        live.inter_index_id_matches,
    );
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum ThrowStage {
    AddEdgeBeforeInsertion,
    RelationCallbackAfterInsertion,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum Terminal {
    ReadyBeforeHeadRelationLoop,
    Threw(ThrowStage),
}

#[derive(Clone, Debug, PartialEq)]
struct DiscoveryTrace {
    group_ordinal: usize,
    beam: BeamId,
    cross: Point,
    within_margin: bool,
    sorted_ordinal: Option<usize>,
}

#[derive(Clone, Debug, PartialEq)]
struct PairRelationTrace {
    pair_ordinal: usize,
    relation_identity: usize,
    payload: RelationPayload,
    class_read: bool,
    matched_beam_stem: bool,
}

#[derive(Clone, Debug, PartialEq)]
struct GeometryTrace {
    base_cross: Point,
    sibling_cross: Point,
    base_length: f64,
    sibling_length: f64,
    ratio: f64,
    shorter_branch_read: bool,
    dy: Option<f64>,
    dy_times_y_dir: Option<f64>,
    extension: Option<Point>,
    left_threshold: Option<f64>,
    right_threshold: Option<f64>,
    portion: Option<BeamPortion>,
    grade_bits: Option<u64>,
}

#[derive(Clone, Debug, PartialEq)]
struct CallbackTrace {
    stem_incident_relation_identities: Vec<usize>,
    beam_incident_relation_identities: Vec<usize>,
    extension_was_populated: bool,
    portion_was_populated: bool,
    chord_stem_matches: usize,
    abnormal_before: bool,
    abnormal_after: bool,
    stub_modified_before: bool,
    stub_modified_after: bool,
    book_modified_before: bool,
    book_modified_after: bool,
    book_dirty_before: bool,
    book_dirty_after: bool,
}

#[derive(Clone, Debug, Eq, PartialEq)]
struct LinkerLookupTrace {
    examined_item_ordinals: Vec<usize>,
    matched_item_ordinal: Option<usize>,
    unread_suffix: usize,
    selected_linker: Option<LinkerId>,
    linked_cell: Option<LinkerCellId>,
    linked_before: Option<bool>,
    linked_after: Option<bool>,
    write_count: usize,
    value_change_count: usize,
}

#[derive(Clone, Debug, PartialEq)]
struct SiblingTrace {
    sibling_ordinal: usize,
    beam: BeamId,
    pair_relations: Vec<PairRelationTrace>,
    geometry: Option<GeometryTrace>,
    callback: Option<CallbackTrace>,
    linker_lookup: Option<LinkerLookupTrace>,
    branch: SiblingBranch,
    first_event_ordinal: usize,
    event_count: usize,
}

#[derive(Clone, Debug, Eq, PartialEq)]
enum SerialEvent {
    EdgeInserted {
        sibling_ordinal: usize,
        relation_identity: usize,
    },
    CallbackCompleted {
        sibling_ordinal: usize,
        relation_identity: usize,
    },
    LinkerFlagAssigned {
        sibling_ordinal: usize,
        selected_linker: LinkerId,
        cell: LinkerCellId,
        ordered_observers: Vec<LinkerId>,
        before: bool,
        after: bool,
    },
}

#[derive(Clone, Debug, PartialEq)]
struct IndependentTransaction {
    ref_point: Point,
    discoveries: Vec<DiscoveryTrace>,
    sorted_siblings_before_base_removal: Vec<BeamId>,
    base_removal_index: Option<usize>,
    sibling_traces: Vec<SiblingTrace>,
    events: Vec<SerialEvent>,
    terminal: Terminal,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum IndependentError {
    InvalidDirection,
    MissingBaseBeam,
    MissingStem,
    MissingGroupBeam,
    InvalidRelationAllocator,
    ChordBearingStem,
    MissingLinkerCell,
}

fn java_double_bits(value: f64) -> u64 {
    if value.is_nan() {
        0x7ff8_0000_0000_0000
    } else {
        value.to_bits()
    }
}

fn java_double_compare(left: f64, right: f64) -> Ordering {
    if left < right {
        Ordering::Less
    } else if left > right {
        Ordering::Greater
    } else {
        (java_double_bits(left) as i64).cmp(&(java_double_bits(right) as i64))
    }
}

fn intersection(a: Line, b: Line) -> Point {
    // Preserve the operation order in Java LineUtil.intersection. Keeping the
    // intermediates named also makes exact-bit divergences diagnosable.
    let denominator = ((a.x1 - a.x2) * (b.y1 - b.y2)) - ((a.y1 - a.y2) * (b.x1 - b.x2));
    let v12 = (a.x1 * a.y2) - (a.y1 * a.x2);
    let v34 = (b.x1 * b.y2) - (b.y1 * b.x2);
    let x = ((v12 * (b.x1 - b.x2)) - ((a.x1 - a.x2) * v34)) / denominator;
    let y = ((v12 * (b.y1 - b.y2)) - ((a.y1 - a.y2) * v34)) / denominator;
    Point { x, y }
}

fn beam_portion(median: Line, x: f64, max_dx: i32) -> BeamPortion {
    let max_dx = f64::from(max_dx);
    if x < median.x1 + max_dx {
        BeamPortion::Left
    } else if x > median.x2 - max_dx {
        BeamPortion::Right
    } else {
        BeamPortion::Center
    }
}

fn preflight(input: &IndependentInput, state: &IndependentState) -> Result<(), IndependentError> {
    if !matches!(input.y_dir, -1 | 1) {
        return Err(IndependentError::InvalidDirection);
    }
    if !state.beams.contains_key(&input.base_beam) {
        return Err(IndependentError::MissingBaseBeam);
    }
    if !state.vertices.contains(&Vertex::Stem(input.stem)) {
        return Err(IndependentError::MissingStem);
    }
    if input
        .group_members
        .iter()
        .any(|beam| !state.beams.contains_key(beam))
    {
        return Err(IndependentError::MissingGroupBeam);
    }
    if state.next_relation_identity == 0
        || state
            .edges
            .iter()
            .any(|edge| edge.relation_identity >= state.next_relation_identity)
    {
        return Err(IndependentError::InvalidRelationAllocator);
    }
    for item in &state.builder_items {
        let BuilderItem::Linker {
            linker,
            linked_cell,
            ..
        } = item
        else {
            continue;
        };
        let Some(observers) = state.linked_cell_observers.get(linked_cell) else {
            return Err(IndependentError::MissingLinkerCell);
        };
        if !state.linked_cells.contains_key(linked_cell)
            || observers.first() != Some(linker)
            || observers.iter().collect::<BTreeSet<_>>().len() != observers.len()
        {
            return Err(IndependentError::MissingLinkerCell);
        }
    }
    // Compact v1 intentionally excludes the substantial chord cache mutation
    // branch. This checks live state, not a predecessor aggregate.
    if state.edges.iter().any(|edge| {
        edge.payload == RelationPayload::ChordStem
            && (edge.source == Vertex::Stem(input.stem) || edge.target == Vertex::Stem(input.stem))
    }) {
        return Err(IndependentError::ChordBearingStem);
    }
    Ok(())
}

fn directed_pair_trace(
    state: &IndependentState,
    sibling: BeamId,
    stem: StemId,
) -> Vec<PairRelationTrace> {
    let mut traces = Vec::new();
    let mut class_read = true;
    for edge in state
        .edges
        .iter()
        .filter(|edge| edge.source == Vertex::Beam(sibling) && edge.target == Vertex::Stem(stem))
    {
        let matched_beam_stem =
            class_read && matches!(edge.payload, RelationPayload::BeamStem { .. });
        traces.push(PairRelationTrace {
            pair_ordinal: traces.len(),
            relation_identity: edge.relation_identity,
            payload: edge.payload,
            class_read,
            matched_beam_stem,
        });
        if matched_beam_stem {
            class_read = false;
        }
    }
    traces
}

fn complete_relation_callback(
    sibling: BeamId,
    stem: StemId,
    state: &mut IndependentState,
) -> CallbackTrace {
    let beam_before = state.beams[&sibling].clone();
    let stub_before = state.stub_modified;
    let book_modified_before = state.book_modified;
    let book_dirty_before = state.book_dirty;
    let stem_incident = state
        .edges
        .iter()
        .filter(|edge| edge.source == Vertex::Stem(stem) || edge.target == Vertex::Stem(stem))
        .map(|edge| edge.relation_identity)
        .collect::<Vec<_>>();
    let chord_stem_matches = state
        .edges
        .iter()
        .filter(|edge| {
            edge.payload == RelationPayload::ChordStem
                && (edge.source == Vertex::Stem(stem) || edge.target == Vertex::Stem(stem))
        })
        .count();
    let beam_incident_edges = state
        .edges
        .iter()
        .filter(|edge| edge.source == Vertex::Beam(sibling) || edge.target == Vertex::Beam(sibling))
        .collect::<Vec<_>>();
    let beam_incident = beam_incident_edges
        .iter()
        .map(|edge| edge.relation_identity)
        .collect::<Vec<_>>();
    let abnormal_after = match beam_before.runtime_class {
        BeamRuntimeClass::Hook => !beam_incident_edges
            .iter()
            .any(|edge| matches!(edge.payload, RelationPayload::BeamStem { .. })),
        BeamRuntimeClass::Beam | BeamRuntimeClass::SmallBeam => {
            let mut left = false;
            let mut right = false;
            for edge in &beam_incident_edges {
                let portion = match edge.payload {
                    RelationPayload::BeamStem { portion, .. }
                    | RelationPayload::BeamRest { portion } => portion,
                    RelationPayload::ChordStem | RelationPayload::Other => continue,
                };
                left |= portion == BeamPortion::Left;
                right |= portion == BeamPortion::Right;
            }
            !left || !right
        }
    };
    if abnormal_after != beam_before.abnormal {
        state
            .beams
            .get_mut(&sibling)
            .expect("validated beam")
            .abnormal = abnormal_after;
        state.stub_modified = true;
        state.book_modified = true;
        state.book_dirty = true;
    }
    CallbackTrace {
        stem_incident_relation_identities: stem_incident,
        beam_incident_relation_identities: beam_incident,
        extension_was_populated: true,
        portion_was_populated: true,
        chord_stem_matches,
        abnormal_before: beam_before.abnormal,
        abnormal_after,
        stub_modified_before: stub_before,
        stub_modified_after: state.stub_modified,
        book_modified_before,
        book_modified_after: state.book_modified,
        book_dirty_before,
        book_dirty_after: state.book_dirty,
    }
}

fn lookup_and_assign_linker(
    sibling: BeamId,
    state: &mut IndependentState,
) -> Result<LinkerLookupTrace, IndependentError> {
    let mut examined = Vec::new();
    let mut matched = None;
    for (ordinal, item) in state.builder_items.iter().enumerate() {
        examined.push(ordinal);
        if matches!(item, BuilderItem::Linker { source, .. } if *source == sibling) {
            matched = Some((ordinal, *item));
            break;
        }
    }
    let Some((
        ordinal,
        BuilderItem::Linker {
            linker,
            linked_cell,
            ..
        },
    )) = matched
    else {
        return Ok(LinkerLookupTrace {
            examined_item_ordinals: examined,
            matched_item_ordinal: None,
            unread_suffix: 0,
            selected_linker: None,
            linked_cell: None,
            linked_before: None,
            linked_after: None,
            write_count: 0,
            value_change_count: 0,
        });
    };
    let Some(linked) = state.linked_cells.get_mut(&linked_cell) else {
        return Err(IndependentError::MissingLinkerCell);
    };
    let before = *linked;
    *linked = true;
    Ok(LinkerLookupTrace {
        examined_item_ordinals: examined,
        matched_item_ordinal: Some(ordinal),
        unread_suffix: state.builder_items.len() - ordinal - 1,
        selected_linker: Some(linker),
        linked_cell: Some(linked_cell),
        linked_before: Some(before),
        linked_after: Some(true),
        write_count: 1,
        value_change_count: usize::from(!before),
    })
}

fn apply_independent(
    input: &IndependentInput,
    state: &mut IndependentState,
) -> Result<IndependentTransaction, IndependentError> {
    preflight(input, state)?;
    let base = state.beams[&input.base_beam].clone();
    let base_cross = intersection(input.stem_median, base.median);
    let base_length = base.median.x2 - base.median.x1;
    let mut discoveries = Vec::with_capacity(input.group_members.len());
    let mut accepted = Vec::<(usize, BeamId, Point)>::new();
    for (group_ordinal, beam_id) in input.group_members.iter().copied().enumerate() {
        let beam = &state.beams[&beam_id];
        let cross = intersection(input.skewed_vertical, beam.median);
        let within = beam.median.x1 - input.max_beam_side_dx <= cross.x
            && cross.x <= beam.median.x2 + input.max_beam_side_dx;
        if within {
            accepted.push((group_ordinal, beam_id, cross));
        }
        discoveries.push(DiscoveryTrace {
            group_ordinal,
            beam: beam_id,
            cross,
            within_margin: within,
            sorted_ordinal: None,
        });
    }
    accepted.sort_by(|left, right| java_double_compare(left.2.y, right.2.y));
    for (sorted_ordinal, (group_ordinal, _, _)) in accepted.iter().enumerate() {
        discoveries[*group_ordinal].sorted_ordinal = Some(sorted_ordinal);
    }
    let sorted_siblings_before_base_removal = accepted
        .iter()
        .map(|(_, beam, _)| *beam)
        .collect::<Vec<_>>();
    let base_removal_index = accepted
        .iter()
        .position(|(_, beam, _)| *beam == input.base_beam);
    if let Some(index) = base_removal_index {
        accepted.remove(index);
    }

    let mut sibling_traces = Vec::with_capacity(accepted.len());
    let mut events = Vec::new();
    let mut terminal = Terminal::ReadyBeforeHeadRelationLoop;
    for (sibling_ordinal, (_, sibling, _)) in accepted.into_iter().enumerate() {
        let first_event_ordinal = events.len();
        let sibling_state = state.beams[&sibling].clone();
        if sibling_state.glyph_identity == base.glyph_identity {
            sibling_traces.push(SiblingTrace {
                sibling_ordinal,
                beam: sibling,
                pair_relations: Vec::new(),
                geometry: None,
                callback: None,
                linker_lookup: None,
                branch: SiblingBranch::SameGlyph,
                first_event_ordinal,
                event_count: 0,
            });
            continue;
        }
        let pair_relations = directed_pair_trace(state, sibling, input.stem);
        if pair_relations
            .iter()
            .any(|relation| relation.matched_beam_stem)
        {
            sibling_traces.push(SiblingTrace {
                sibling_ordinal,
                beam: sibling,
                pair_relations,
                geometry: None,
                callback: None,
                linker_lookup: None,
                branch: SiblingBranch::ExistingBeamStem,
                first_event_ordinal,
                event_count: 0,
            });
            continue;
        }

        let sibling_cross = intersection(input.stem_median, sibling_state.median);
        let sibling_length = sibling_state.median.x2 - sibling_state.median.x1;
        let ratio = sibling_length / base_length;
        let shorter_branch_read = ratio <= input.max_shorter_ratio;
        let (dy, dy_times_y_dir) = if shorter_branch_read {
            let dy = sibling_cross.y - base_cross.y;
            (Some(dy), Some(dy * f64::from(input.y_dir)))
        } else {
            (None, None)
        };
        let mut geometry = GeometryTrace {
            base_cross,
            sibling_cross,
            base_length,
            sibling_length,
            ratio,
            shorter_branch_read,
            dy,
            dy_times_y_dir,
            extension: None,
            left_threshold: None,
            right_threshold: None,
            portion: None,
            grade_bits: None,
        };
        if matches!(dy_times_y_dir, Some(product) if product < 0.0) {
            sibling_traces.push(SiblingTrace {
                sibling_ordinal,
                beam: sibling,
                pair_relations,
                geometry: Some(geometry),
                callback: None,
                linker_lookup: None,
                branch: SiblingBranch::ShorterWrongSide,
                first_event_ordinal,
                event_count: 0,
            });
            continue;
        }

        let extension = Point {
            x: sibling_cross.x,
            y: sibling_cross.y - f64::from(input.y_dir) * (sibling_state.height / 2.0),
        };
        let portion = beam_portion(sibling_state.median, sibling_cross.x, input.portion_max_dx);
        geometry.extension = Some(extension);
        geometry.left_threshold = Some(sibling_state.median.x1 + f64::from(input.portion_max_dx));
        geometry.right_threshold = Some(sibling_state.median.x2 - f64::from(input.portion_max_dx));
        geometry.portion = Some(portion);
        geometry.grade_bits = Some(input.continuation_support_grade_bits);

        let relation_identity = state.next_relation_identity;
        state.next_relation_identity += 1;
        let behavior = input
            .add_edge_behavior
            .get(&sibling)
            .copied()
            .unwrap_or(AddEdgeBehavior::Added);
        if behavior == AddEdgeBehavior::ThrowBeforeInsertion {
            terminal = Terminal::Threw(ThrowStage::AddEdgeBeforeInsertion);
            sibling_traces.push(SiblingTrace {
                sibling_ordinal,
                beam: sibling,
                pair_relations,
                geometry: Some(geometry),
                callback: None,
                linker_lookup: None,
                branch: SiblingBranch::Linked,
                first_event_ordinal,
                event_count: 0,
            });
            break;
        }
        if behavior == AddEdgeBehavior::ReturnedFalse {
            let linker_lookup = lookup_and_assign_linker(sibling, state)?;
            if let (Some(selected_linker), Some(cell), Some(before), Some(after)) = (
                linker_lookup.selected_linker,
                linker_lookup.linked_cell,
                linker_lookup.linked_before,
                linker_lookup.linked_after,
            ) {
                events.push(SerialEvent::LinkerFlagAssigned {
                    sibling_ordinal,
                    selected_linker,
                    cell,
                    ordered_observers: state.linked_cell_observers[&cell].clone(),
                    before,
                    after,
                });
            }
            sibling_traces.push(SiblingTrace {
                sibling_ordinal,
                beam: sibling,
                pair_relations,
                geometry: Some(geometry),
                callback: None,
                linker_lookup: Some(linker_lookup),
                branch: SiblingBranch::Linked,
                first_event_ordinal,
                event_count: events.len() - first_event_ordinal,
            });
            continue;
        }
        state.edges.push(GraphEdge {
            relation_identity,
            source: Vertex::Beam(sibling),
            target: Vertex::Stem(input.stem),
            payload: RelationPayload::BeamStem {
                portion,
                grade_bits: input.continuation_support_grade_bits,
            },
        });
        events.push(SerialEvent::EdgeInserted {
            sibling_ordinal,
            relation_identity,
        });
        if behavior == AddEdgeBehavior::ThrowDuringCallback {
            terminal = Terminal::Threw(ThrowStage::RelationCallbackAfterInsertion);
            sibling_traces.push(SiblingTrace {
                sibling_ordinal,
                beam: sibling,
                pair_relations,
                geometry: Some(geometry),
                callback: None,
                linker_lookup: None,
                branch: SiblingBranch::Linked,
                first_event_ordinal,
                event_count: 1,
            });
            break;
        }
        let callback = complete_relation_callback(sibling, input.stem, state);
        events.push(SerialEvent::CallbackCompleted {
            sibling_ordinal,
            relation_identity,
        });
        let linker_lookup = lookup_and_assign_linker(sibling, state)?;
        if let (Some(selected_linker), Some(cell), Some(before), Some(after)) = (
            linker_lookup.selected_linker,
            linker_lookup.linked_cell,
            linker_lookup.linked_before,
            linker_lookup.linked_after,
        ) {
            events.push(SerialEvent::LinkerFlagAssigned {
                sibling_ordinal,
                selected_linker,
                cell,
                ordered_observers: state.linked_cell_observers[&cell].clone(),
                before,
                after,
            });
        }
        sibling_traces.push(SiblingTrace {
            sibling_ordinal,
            beam: sibling,
            pair_relations,
            geometry: Some(geometry),
            callback: Some(callback),
            linker_lookup: Some(linker_lookup),
            branch: SiblingBranch::Linked,
            first_event_ordinal,
            event_count: events.len() - first_event_ordinal,
        });
    }
    Ok(IndependentTransaction {
        ref_point: input.ref_point,
        discoveries,
        sorted_siblings_before_base_removal,
        base_removal_index,
        sibling_traces,
        events,
        terminal,
    })
}

fn repo_root() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("../../..")
}

fn corpus_page_for_token(page: &str) -> Result<(&'static str, &'static str), String> {
    CORPUS_PAGES
        .iter()
        .copied()
        .find(|(_, image)| page == format!("{image}#1"))
        .ok_or_else(|| format!("unknown Boundary-16 page token {page}"))
}

fn boundary_sixteen_fixture_path(key: &str) -> String {
    format!("rust/oracle/stems-beam-vlink-sibling-links-{key}.txt")
}

fn boundary_fifteen_fixture_path(key: &str) -> String {
    format!("rust/oracle/stems-beam-vlink-b-linker-flag-{key}.txt")
}

fn predecessor_fixture_path(family: &str, key: &str) -> String {
    format!("rust/oracle/{family}-{key}.txt")
}

fn sha256_hex(bytes: &[u8]) -> String {
    const K: [u32; 64] = [
        0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5, 0x3956c25b, 0x59f111f1, 0x923f82a4,
        0xab1c5ed5, 0xd807aa98, 0x12835b01, 0x243185be, 0x550c7dc3, 0x72be5d74, 0x80deb1fe,
        0x9bdc06a7, 0xc19bf174, 0xe49b69c1, 0xefbe4786, 0x0fc19dc6, 0x240ca1cc, 0x2de92c6f,
        0x4a7484aa, 0x5cb0a9dc, 0x76f988da, 0x983e5152, 0xa831c66d, 0xb00327c8, 0xbf597fc7,
        0xc6e00bf3, 0xd5a79147, 0x06ca6351, 0x14292967, 0x27b70a85, 0x2e1b2138, 0x4d2c6dfc,
        0x53380d13, 0x650a7354, 0x766a0abb, 0x81c2c92e, 0x92722c85, 0xa2bfe8a1, 0xa81a664b,
        0xc24b8b70, 0xc76c51a3, 0xd192e819, 0xd6990624, 0xf40e3585, 0x106aa070, 0x19a4c116,
        0x1e376c08, 0x2748774c, 0x34b0bcb5, 0x391c0cb3, 0x4ed8aa4a, 0x5b9cca4f, 0x682e6ff3,
        0x748f82ee, 0x78a5636f, 0x84c87814, 0x8cc70208, 0x90befffa, 0xa4506ceb, 0xbef9a3f7,
        0xc67178f2,
    ];
    let mut padded = bytes.to_vec();
    let bit_len = u64::try_from(bytes.len()).expect("fixture length fits u64") * 8;
    padded.push(0x80);
    while padded.len() % 64 != 56 {
        padded.push(0);
    }
    padded.extend_from_slice(&bit_len.to_be_bytes());
    let mut state = [
        0x6a09e667_u32,
        0xbb67ae85,
        0x3c6ef372,
        0xa54ff53a,
        0x510e527f,
        0x9b05688c,
        0x1f83d9ab,
        0x5be0cd19,
    ];
    for chunk in padded.chunks_exact(64) {
        let mut words = [0_u32; 64];
        for (index, word) in words[..16].iter_mut().enumerate() {
            let offset = index * 4;
            *word = u32::from_be_bytes([
                chunk[offset],
                chunk[offset + 1],
                chunk[offset + 2],
                chunk[offset + 3],
            ]);
        }
        for index in 16..64 {
            let s0 = words[index - 15].rotate_right(7)
                ^ words[index - 15].rotate_right(18)
                ^ (words[index - 15] >> 3);
            let s1 = words[index - 2].rotate_right(17)
                ^ words[index - 2].rotate_right(19)
                ^ (words[index - 2] >> 10);
            words[index] = words[index - 16]
                .wrapping_add(s0)
                .wrapping_add(words[index - 7])
                .wrapping_add(s1);
        }
        let [mut a, mut b, mut c, mut d, mut e, mut f, mut g, mut h] = state;
        for index in 0..64 {
            let sigma1 = e.rotate_right(6) ^ e.rotate_right(11) ^ e.rotate_right(25);
            let choice = (e & f) ^ ((!e) & g);
            let temporary1 = h
                .wrapping_add(sigma1)
                .wrapping_add(choice)
                .wrapping_add(K[index])
                .wrapping_add(words[index]);
            let sigma0 = a.rotate_right(2) ^ a.rotate_right(13) ^ a.rotate_right(22);
            let majority = (a & b) ^ (a & c) ^ (b & c);
            let temporary2 = sigma0.wrapping_add(majority);
            h = g;
            g = f;
            f = e;
            e = d.wrapping_add(temporary1);
            d = c;
            c = b;
            b = a;
            a = temporary1.wrapping_add(temporary2);
        }
        for (slot, value) in state.iter_mut().zip([a, b, c, d, e, f, g, h]) {
            *slot = slot.wrapping_add(value);
        }
    }
    state.iter().map(|word| format!("{word:08x}")).collect()
}

fn read_sha256(relative: &str) -> Result<String, String> {
    let path = repo_root().join(relative);
    let bytes = std::fs::read(&path)
        .map_err(|error| format!("cannot read provenance path {}: {error}", path.display()))?;
    Ok(sha256_hex(&bytes))
}

fn validate_corpus_summary(rows: &[StrictRow], text: &str) -> Result<(), String> {
    let page = rows
        .first()
        .filter(|row| row.kind == RowKind::Page)
        .ok_or_else(|| "corpus validation lacks its page row".to_owned())?;
    let page_summaries = rows
        .iter()
        .filter(|row| row.kind == RowKind::PageSummary)
        .collect::<Vec<_>>();
    let corpus_summaries = rows
        .iter()
        .filter(|row| row.kind == RowKind::CorpusSummary)
        .collect::<Vec<_>>();
    let [page_summary] = page_summaries.as_slice() else {
        return Err("fixture does not contain exactly one page summary".to_owned());
    };
    let [corpus] = corpus_summaries.as_slice() else {
        return Err("fixture does not contain exactly one corpus summary".to_owned());
    };
    let (page_key, image) = corpus_page_for_token(&page.page)?;
    if rows.last() != Some(*corpus)
        || corpus.value("schema")? != "stems-beam-vlink-sibling-links-v1"
        || corpus.value("mode")? != page_key
        || corpus.usize("pages")? != 1
        || corpus.value("pageRefs")? != page.page
        || page.page != format!("{image}#1")
    {
        return Err("corpus page/schema envelope differs".to_owned());
    }

    let expected_row_counts = core_row_counts(rows);
    if corpus.value("rowCounts")? != expected_row_counts {
        return Err("corpus ordered core-family row counts differ".to_owned());
    }

    if corpus.value("pageInputSha256")? != read_sha256(&format!("data/examples/{image}"))?
        || corpus.value("probeSourceSha256")? != read_sha256(PROBE_SOURCE_PATH)?
        || corpus.value("runnerSourceSha256")? != read_sha256(RUNNER_SOURCE_PATH)?
    {
        return Err("corpus page/probe/runner source pin differs".to_owned());
    }
    for (field, path) in CORPUS_SOURCE_PINS {
        if corpus.value(field)? != read_sha256(path)? {
            return Err(format!("corpus active source pin differs for {field}"));
        }
    }
    for (field, path) in CORPUS_GLOBAL_FIXTURE_PINS {
        if corpus.value(field)? != read_sha256(path)? {
            return Err(format!("corpus predecessor pin differs for {field}"));
        }
    }
    for (field, family) in [
        ("schedulerFixtureSha256", "stems-beam-scheduler"),
        ("expandFixtureSha256", "stems-beam-expand"),
        ("createStemFixtureSha256", "stems-beam-create-stem"),
        ("reuseCheckFixtureSha256", "stems-beam-vlink-reuse-check"),
        ("baseApplyFixtureSha256", "stems-beam-vlink-base-apply"),
    ] {
        let path = predecessor_fixture_path(family, page_key);
        if corpus.value(field)? != read_sha256(&path)? {
            return Err(format!("corpus predecessor pin differs for {field}"));
        }
    }
    let b15_path = boundary_fifteen_fixture_path(page_key);
    if corpus.value("bLinkerFlagFixtureSha256")? != read_sha256(&b15_path)? {
        return Err("corpus Boundary-15 fixture pin differs".to_owned());
    }
    for field in [
        "schedulerFixtureSha256",
        "expandFixtureSha256",
        "createStemFixtureSha256",
        "reuseCheckFixtureSha256",
        "baseApplyFixtureSha256",
        "bLinkerFlagFixtureSha256",
    ] {
        if corpus.value(field)? != page.value(field)? {
            return Err(format!("page/corpus predecessor join differs for {field}"));
        }
    }
    for field in [
        "effectiveClasspathSha256",
        "jdkReleaseSha256",
        "javaExecutableSha256",
        "javaJpegLibrarySha256",
        "javaModulesSha256",
        "javaVmLibrarySha256",
        "javaAwtLibrarySha256",
        "javaAwtLwawtLibrarySha256",
        "beamLinkerClassSha256",
        "bLinkerClassSha256",
        "vLinkerClassSha256",
        "stemLinkerClassSha256",
        "jgraphtCoreJarSha256",
    ] {
        if !is_lower_sha256(corpus.value(field)?) {
            return Err(format!("corpus runtime {field} is not lowercase SHA-256"));
        }
    }
    if corpus.value("javaArchitecture")? != "aarch64"
        || corpus.value("javaRuntimeVersion")? != "25.0.3+9-LTS"
        || corpus.value("javaVmVariant")? != "Hotspot"
        || corpus.value("javaImageType")? != "JDK"
        || corpus.value("jgraphtCoreVersion")? != "1.5.2"
        || corpus.value("jgraphtCoreJarSha256")?
            != "dfa596e9f0d0838f1b5e81dd0cd60e3a76c2c290ac25a0a029ffde58cf5e4c14"
        || corpus.value("predecessorReplay")? != "FullBoundary15TypedReplayAndExactJavaRowJoin"
        || corpus.value("querySerialization")? != "UTF8ColonTokensLF-LazyNotReadLiteral"
    {
        return Err("corpus runtime/predecessor constants differ".to_owned());
    }

    let page_summary_marker = format!("{} ", RowKind::PageSummary.label());
    let body_end = text
        .match_indices(&page_summary_marker)
        .find_map(|(index, _)| {
            (index == 0 || text.as_bytes().get(index.wrapping_sub(1)) == Some(&b'\n'))
                .then_some(index)
        })
        .ok_or_else(|| "fixture lacks a line-aligned page summary".to_owned())?;
    let emitted_body = &text.as_bytes()[..body_end];
    let emitted_body_sha = sha256_hex(emitted_body);
    let emitted_body_lines = emitted_body.iter().filter(|byte| **byte == b'\n').count();
    if corpus.value("emittedBodySha256")? != emitted_body_sha
        || corpus.value("rawPassSha256")? != emitted_body_sha
        || corpus.usize("emittedBodyLines")? != emitted_body_lines
        || corpus.usize("emittedBodyBytes")? != emitted_body.len()
    {
        return Err("corpus two-pass emitted-body evidence differs".to_owned());
    }

    let systems = page.usize("systems")?;
    if corpus.usize("freshRunsPerPage")? != 2
        || !corpus.bool("freshRunsByteIdentical")?
        || !corpus.bool("freshJvmPerSystem")?
        || corpus.usize("compilerJavaProcesses")? != 1
        || corpus.usize("runtimeJavaProcessesPerPass")? != systems
        || corpus.usize("runtimeJavaProcesses")? != 2 * systems
        || corpus.usize("totalJavaProcesses")? != 2 * systems + 1
        || corpus.usize("maximumConcurrentJavaProcesses")? != 1
        || corpus.value("concurrencyScope")? != "Boundary16RunnerLockedInvocation"
        || !corpus.bool("compilerJavaProcessReaped")?
        || !corpus.bool("runtimeJavaProcessesReaped")?
        || !corpus.bool("foregroundJavaProcessesOnly")?
        || corpus.usize("backgroundJavaProcessesStarted")? != 0
    {
        return Err("corpus fresh-process/two-pass census differs".to_owned());
    }

    for field in [
        "realTransactions",
        "supportedSyntheticCases",
        "envelopeCases",
        "totalTransactions",
        "siblingCandidates",
        "sameGlyph",
        "existingBeamStem",
        "shorterWrongSide",
        "linked",
        "edgesAdded",
        "linkerWrites",
        "chordStemMatches",
    ] {
        if corpus.value(field)? != page_summary.value(field)? {
            return Err(format!("page/corpus census join differs for {field}"));
        }
    }
    if !corpus.bool("system1SyntheticBlock")?
        || corpus.value("addEdgeReturnedFalseEvidence")? != "IndependentModelOnlyNoStockJavaFixture"
        || corpus.value("envelopeEvidenceScope")? != "JavaOnlyNotProductionEquivalent"
        || !corpus.bool("stopBeforeHeadRelationLoop")?
    {
        return Err("corpus synthetic/envelope/terminal evidence differs".to_owned());
    }
    Ok(())
}

fn validate_real_public_transactions(
    page: &StrictRow,
    transactions: &[ParsedTransaction],
) -> Result<(), String> {
    for transaction in transactions {
        let _ = hydrate_real_boundary_sixteen(page, transaction)?;
    }
    Ok(())
}

fn exactly_one_row(rows: &[StrictRow], kind: RowKind) -> Result<&StrictRow, String> {
    let matches = rows
        .iter()
        .filter(|row| row.kind == kind)
        .collect::<Vec<_>>();
    let [row] = matches.as_slice() else {
        return Err(format!(
            "fixture contains {} {kind:?} rows, expected one",
            matches.len()
        ));
    };
    Ok(row)
}

fn compare_manifest_and_strict_fields(
    manifest: &ManifestRow,
    strict: &StrictRow,
    fields: &[&str],
) -> Result<(), String> {
    for field in fields {
        if manifest.value(field)? != strict.value(field)? {
            return Err(format!(
                "manifest/fixture {} join differs for {field}",
                manifest.value("page").unwrap_or("summary")
            ));
        }
    }
    Ok(())
}

fn validate_boundary_sixteen_manifest(path: &std::path::Path) -> Result<(), String> {
    let manifest_bytes =
        std::fs::read(path).map_err(|error| format!("cannot read {}: {error}", path.display()))?;
    let manifest = std::str::from_utf8(&manifest_bytes)
        .map_err(|error| format!("{} is not UTF-8: {error}", path.display()))?;
    if !manifest_bytes.ends_with(b"\n") {
        return Err("Boundary-16 manifest must end with one newline".to_owned());
    }
    let lines = manifest.lines().collect::<Vec<_>>();
    if sha256_hex(&manifest_bytes) != MANIFEST_SHA256
        || lines.len() != MANIFEST_LINES
        || manifest_bytes.len() != MANIFEST_BYTES
        || MANIFEST_ENTRY_FIELDS.len() != 54
        || MANIFEST_SUMMARY_FIELDS.len() != 128
        || lines.len() != CORPUS_PAGES.len() + 2
        || lines.first().copied() != Some(MANIFEST_SCHEMA)
    {
        return Err("Boundary-16 manifest schema/line envelope differs".to_owned());
    }
    let entries = lines[1..=CORPUS_PAGES.len()]
        .iter()
        .map(|line| ManifestRow::parse(line, MANIFEST_ENTRY_LABEL, MANIFEST_ENTRY_FIELDS))
        .collect::<Result<Vec<_>, _>>()?;
    let summary = ManifestRow::parse(
        lines
            .last()
            .ok_or_else(|| "Boundary-16 manifest lacks summary".to_owned())?,
        MANIFEST_SUMMARY_LABEL,
        MANIFEST_SUMMARY_FIELDS,
    )?;

    let expected_header = format!("{}\n", FIXTURE_HEADER.join("\n"));
    let mut normalized_corpus = expected_header.as_bytes().to_vec();
    let mut all_rows = Vec::new();
    let mut split_fixture_lines = 0;
    let mut split_fixture_bytes = 0;
    let mut semantic_rows = 0;
    let mut first_corpus = None;
    let mut real_systems = 0;
    let mut real_transactions = 0;
    let mut group_rows_total = 0;
    let mut group_glyph_rows_total = 0;
    let mut null_group_glyphs_total = 0;
    let mut real_sibling_candidates = 0;
    let mut real_same_glyph = 0;
    let mut real_existing_beam_stem = 0;
    let mut real_shorter_wrong_side = 0;
    let mut real_linked = 0;
    let mut real_edges_added = 0;
    let mut real_linker_writes = 0;
    let mut real_events = 0;
    let mut chord_stem_matches = 0;
    let mut supported_synthetic_cases = 0;
    let mut envelope_cases = 0;
    let mut total_transactions = 0;
    let mut same_glyph = 0;
    let mut existing_beam_stem = 0;
    let mut shorter_wrong_side = 0;
    let mut linked = 0;
    let mut edges_added = 0;
    let mut linker_writes = 0;
    let mut events = 0;
    let mut compiler_java_processes = 0;
    let mut runtime_java_processes = 0;
    let mut total_java_processes = 0;
    let mut maximum_concurrent_java_processes = 0;

    for (ordinal, ((page_key, _), entry)) in CORPUS_PAGES.iter().zip(&entries).enumerate() {
        let expected_fixture = format!("stems-beam-vlink-sibling-links-{page_key}.txt");
        if entry.usize("ordinal")? != ordinal
            || entry.value("page")? != *page_key
            || entry.value("fixture")? != expected_fixture
        {
            return Err(format!("manifest entry {ordinal} identity/order differs"));
        }
        let fixture_path = repo_root().join("rust/oracle").join(&expected_fixture);
        let fixture_bytes = std::fs::read(&fixture_path)
            .map_err(|error| format!("cannot read {}: {error}", fixture_path.display()))?;
        let fixture = std::str::from_utf8(&fixture_bytes)
            .map_err(|error| format!("{} is not UTF-8: {error}", fixture_path.display()))?;
        let rows = parse_scaffold_fixture(fixture)?;
        let transactions = validate_core_rows(&rows)?;
        validate_corpus_summary(&rows, fixture)?;
        validate_boundary_fifteen_predecessors(&rows[0], &transactions)?;
        let page = exactly_one_row(&rows, RowKind::Page)?;
        let page_summary = exactly_one_row(&rows, RowKind::PageSummary)?;
        let corpus = exactly_one_row(&rows, RowKind::CorpusSummary)?;
        first_corpus.get_or_insert_with(|| corpus.clone());

        if entry.value("rowCounts")? != core_row_counts(&rows)
            || entry.usize("systems")? != page.usize("systems")?
        {
            return Err(format!("manifest {page_key} row/system census differs"));
        }
        compare_manifest_and_strict_fields(
            entry,
            page_summary,
            &[
                "realTransactions",
                "supportedSyntheticCases",
                "envelopeCases",
                "totalTransactions",
                "groupRows",
                "siblingCandidates",
                "sameGlyph",
                "existingBeamStem",
                "shorterWrongSide",
                "linked",
                "edgesAdded",
                "linkerWrites",
                "events",
                "chordStemMatches",
            ],
        )?;
        compare_manifest_and_strict_fields(
            entry,
            corpus,
            &[
                "pageInputSha256",
                "schedulerFixtureSha256",
                "expandFixtureSha256",
                "createStemFixtureSha256",
                "reuseCheckFixtureSha256",
                "baseApplyFixtureSha256",
                "baseApplyManifestSha256",
                "bLinkerFlagFixtureSha256",
                "bLinkerFlagManifestSha256",
                "emittedBodySha256",
                "emittedBodyLines",
                "emittedBodyBytes",
                "rawPassSha256",
                "freshRunsPerPage",
                "freshRunsByteIdentical",
                "compilerJavaProcesses",
                "runtimeJavaProcessesPerPass",
                "runtimeJavaProcesses",
                "totalJavaProcesses",
                "maximumConcurrentJavaProcesses",
                "concurrencyScope",
                "freshJvmPerSystem",
                "compilerJavaProcessReaped",
                "runtimeJavaProcessesReaped",
                "foregroundJavaProcessesOnly",
                "backgroundJavaProcessesStarted",
                "system1SyntheticBlock",
                "addEdgeReturnedFalseEvidence",
                "envelopeEvidenceScope",
                "stopBeforeHeadRelationLoop",
            ],
        )?;

        let group_rows = rows
            .iter()
            .filter(|row| row.kind == RowKind::GroupMember)
            .collect::<Vec<_>>();
        let group_glyph_rows = group_rows
            .iter()
            .filter(|row| {
                row.value("containmentMatch") == Ok("true") && row.value("glyph") != Ok("-")
            })
            .count();
        let null_group_glyphs = group_rows
            .iter()
            .filter(|row| row.value("glyph") == Ok("null"))
            .count();
        if entry.usize("groupRows")? != group_rows.len()
            || entry.usize("groupGlyphRows")? != group_glyph_rows
            || entry.usize("nullGroupGlyphs")? != null_group_glyphs
        {
            return Err(format!("manifest {page_key} group/glyph census differs"));
        }

        let fixture_lines = fixture_bytes.iter().filter(|byte| **byte == b'\n').count();
        if entry.value("fixtureSha256")? != sha256_hex(&fixture_bytes)
            || entry.usize("fixtureLines")? != fixture_lines
            || entry.usize("fixtureBytes")? != fixture_bytes.len()
        {
            return Err(format!("manifest {page_key} fixture identity differs"));
        }
        if !fixture.starts_with(&expected_header) {
            return Err(format!("manifest {page_key} shared header differs"));
        }
        for line in fixture.lines().skip(FIXTURE_HEADER.len()) {
            if line.starts_with(RowKind::PageSummary.label())
                || line.starts_with(RowKind::CorpusSummary.label())
            {
                continue;
            }
            normalized_corpus.extend_from_slice(line.as_bytes());
            normalized_corpus.push(b'\n');
            semantic_rows += 1;
        }
        split_fixture_lines += fixture_lines;
        split_fixture_bytes += fixture_bytes.len();
        all_rows.extend(rows.iter().cloned());

        real_systems += entry.usize("systems")?;
        real_transactions += entry.usize("realTransactions")?;
        group_rows_total += group_rows.len();
        group_glyph_rows_total += group_glyph_rows;
        null_group_glyphs_total += null_group_glyphs;
        supported_synthetic_cases += entry.usize("supportedSyntheticCases")?;
        envelope_cases += entry.usize("envelopeCases")?;
        total_transactions += entry.usize("totalTransactions")?;
        same_glyph += entry.usize("sameGlyph")?;
        existing_beam_stem += entry.usize("existingBeamStem")?;
        shorter_wrong_side += entry.usize("shorterWrongSide")?;
        linked += entry.usize("linked")?;
        edges_added += entry.usize("edgesAdded")?;
        linker_writes += entry.usize("linkerWrites")?;
        events += entry.usize("events")?;
        chord_stem_matches += entry.usize("chordStemMatches")?;
        compiler_java_processes += entry.usize("compilerJavaProcesses")?;
        runtime_java_processes += entry.usize("runtimeJavaProcesses")?;
        total_java_processes += entry.usize("totalJavaProcesses")?;
        maximum_concurrent_java_processes =
            maximum_concurrent_java_processes.max(entry.usize("maximumConcurrentJavaProcesses")?);
        for row in rows.iter().filter(|row| row.kind == RowKind::Summary) {
            real_sibling_candidates += row.usize("siblings")?;
            real_same_glyph += row.usize("sameGlyph")?;
            real_existing_beam_stem += row.usize("existingBeamStem")?;
            real_shorter_wrong_side += row.usize("shorterWrongSide")?;
            real_linked += row.usize("linked")?;
            real_edges_added += row.usize("edgesAdded")?;
            real_linker_writes += row.usize("linkerWrites")?;
            real_events += row.usize("events")?;
        }
    }

    if summary.value("schema")? != "stems-beam-vlink-sibling-links-manifest-v1"
        || summary.usize("entries")? != entries.len()
    {
        return Err("manifest summary schema/entry count differs".to_owned());
    }
    let first_corpus = first_corpus.ok_or_else(|| "manifest has no corpus rows".to_owned())?;
    for field in MANIFEST_SUMMARY_FIELDS
        .iter()
        .skip(2)
        .take_while(|field| **field != "sharedHeaderSha256")
    {
        let expected = first_corpus.value(field)?;
        if summary.value(field)? != expected {
            return Err(format!("manifest shared provenance differs for {field}"));
        }
        for (page_key, _) in CORPUS_PAGES {
            let path = repo_root().join(boundary_sixteen_fixture_path(page_key));
            let fixture = std::fs::read_to_string(&path)
                .map_err(|error| format!("cannot reread {}: {error}", path.display()))?;
            let rows = parse_scaffold_fixture(&fixture)?;
            if exactly_one_row(&rows, RowKind::CorpusSummary)?.value(field)? != expected {
                return Err(format!("manifest per-page provenance differs for {field}"));
            }
        }
    }

    if summary.value("sharedHeaderSha256")? != sha256_hex(expected_header.as_bytes())
        || summary.usize("sharedHeaderLines")? != FIXTURE_HEADER.len()
        || summary.usize("sharedHeaderBytes")? != expected_header.len()
        || summary.value("corpusBodySha256")? != sha256_hex(&normalized_corpus)
        || summary.usize("corpusBodyLines")?
            != normalized_corpus
                .iter()
                .filter(|byte| **byte == b'\n')
                .count()
        || summary.usize("corpusBodyBytes")? != normalized_corpus.len()
        || summary.value("corpusRowCounts")? != core_row_counts(&all_rows)
        || summary.value("corpusReconstruction")? != "SharedHeaderOnceThenPageSemanticRows"
        || summary.usize("semanticRows")? != semantic_rows
        || summary.usize("splitFixtureLines")? != split_fixture_lines
        || summary.usize("splitFixtureBytes")? != split_fixture_bytes
        || sha256_hex(&normalized_corpus) != NORMALIZED_CORPUS_SHA256
        || normalized_corpus
            .iter()
            .filter(|byte| **byte == b'\n')
            .count()
            != NORMALIZED_CORPUS_LINES
        || normalized_corpus.len() != NORMALIZED_CORPUS_BYTES
        || split_fixture_lines != SPLIT_FIXTURE_LINES
        || split_fixture_bytes != SPLIT_FIXTURE_BYTES
    {
        return Err("manifest corpus reconstruction differs".to_owned());
    }

    for (field, actual) in [
        ("realSystems", real_systems),
        ("realTransactions", real_transactions),
        ("groupRows", group_rows_total),
        ("groupGlyphRows", group_glyph_rows_total),
        ("nullGroupGlyphs", null_group_glyphs_total),
        ("realSiblingCandidates", real_sibling_candidates),
        ("realSameGlyph", real_same_glyph),
        ("realExistingBeamStem", real_existing_beam_stem),
        ("realShorterWrongSide", real_shorter_wrong_side),
        ("realLinked", real_linked),
        ("realEdgesAdded", real_edges_added),
        ("realLinkerWrites", real_linker_writes),
        ("realEvents", real_events),
        ("chordStemMatches", chord_stem_matches),
        ("syntheticBlocks", entries.len()),
        ("supportedSyntheticCases", supported_synthetic_cases),
        ("envelopeCases", envelope_cases),
        ("isolatedCases", supported_synthetic_cases + envelope_cases),
        ("totalTransactions", total_transactions),
        ("sameGlyph", same_glyph),
        ("existingBeamStem", existing_beam_stem),
        ("shorterWrongSide", shorter_wrong_side),
        ("linked", linked),
        ("edgesAdded", edges_added),
        ("linkerWrites", linker_writes),
        ("events", events),
        ("compilerJavaProcesses", compiler_java_processes),
        ("runtimeJavaProcesses", runtime_java_processes),
        ("totalJavaProcesses", total_java_processes),
        (
            "maximumConcurrentJavaProcesses",
            maximum_concurrent_java_processes,
        ),
    ] {
        if summary.usize(field)? != actual {
            return Err(format!("manifest aggregate {field} differs"));
        }
    }
    if summary.value("concurrencyScope")? != "Boundary16RunnerLockedInvocation"
        || summary.usize("freshRunsPerPage")? != 2
        || !summary.bool("freshRunsByteIdentical")?
        || !summary.bool("freshJvmPerSystem")?
        || !summary.bool("compilerJavaProcessesReaped")?
        || !summary.bool("runtimeJavaProcessesReaped")?
        || !summary.bool("foregroundJavaProcessesOnly")?
        || summary.usize("backgroundJavaProcessesStarted")? != 0
        || !summary.bool("system1SyntheticBlock")?
        || summary.value("addEdgeReturnedFalseEvidence")?
            != "IndependentModelOnlyNoStockJavaFixture"
        || summary.value("supplementalEvidenceScope")?
            != "IsolatedJavaGateOnlyNotProductionEquivalent"
        || !summary.bool("stopBeforeHeadRelationLoop")?
    {
        return Err("manifest execution/supplemental constants differ".to_owned());
    }

    let summary_marker = format!("{MANIFEST_SUMMARY_LABEL} ");
    let body_end = manifest
        .match_indices(&summary_marker)
        .find_map(|(index, _)| {
            (index == 0 || manifest_bytes.get(index.wrapping_sub(1)) == Some(&b'\n'))
                .then_some(index)
        })
        .ok_or_else(|| "manifest lacks a line-aligned summary".to_owned())?;
    let body = &manifest_bytes[..body_end];
    if summary.value("manifestBodySha256")? != sha256_hex(body)
        || summary.usize("manifestBodyLines")? != body.iter().filter(|byte| **byte == b'\n').count()
        || summary.usize("manifestBodyBytes")? != body.len()
        || sha256_hex(body) != MANIFEST_BODY_SHA256
        || body.iter().filter(|byte| **byte == b'\n').count() != MANIFEST_BODY_LINES
        || body.len() != MANIFEST_BODY_BYTES
    {
        return Err("manifest self-pinned body differs".to_owned());
    }
    Ok(())
}

fn validate_installed_boundary_sixteen_fixture(path: &std::path::Path) -> Result<(), String> {
    let bytes =
        std::fs::read(path).map_err(|error| format!("cannot read {}: {error}", path.display()))?;
    let text = std::str::from_utf8(&bytes)
        .map_err(|error| format!("{} is not UTF-8: {error}", path.display()))?;
    let rows = parse_scaffold_fixture(text)?;
    let transactions = validate_core_rows(&rows)?;
    validate_corpus_summary(&rows, text)?;
    validate_boundary_fifteen_predecessors(&rows[0], &transactions)?;
    validate_real_public_transactions(&rows[0], &transactions)?;
    Ok(())
}

#[test]
fn installed_boundary_sixteen_prefix_is_strictly_replayed() {
    if let Some(path) = std::env::var_os(FIXTURE_OVERRIDE_ENV).map(PathBuf::from) {
        validate_installed_boundary_sixteen_fixture(&path)
            .unwrap_or_else(|error| panic!("{}: {error}", path.display()));
        return;
    }
    let mut installed = 0;
    let mut saw_missing = false;
    for (key, _) in CORPUS_PAGES {
        let path = repo_root().join(boundary_sixteen_fixture_path(key));
        match std::fs::metadata(&path) {
            Ok(metadata) if metadata.is_file() => {
                assert!(
                    !saw_missing,
                    "installed Boundary-16 corpus has a gap before {key}"
                );
                validate_installed_boundary_sixteen_fixture(&path)
                    .unwrap_or_else(|error| panic!("{key} exact replay: {error}"));
                installed += 1;
            }
            Err(error) if error.kind() == std::io::ErrorKind::NotFound => saw_missing = true,
            Ok(_) => panic!("{} is not a file", path.display()),
            Err(error) => panic!("cannot inspect {}: {error}", path.display()),
        }
    }
    assert!(installed > 0, "Boundary-16 installed prefix is empty");
}

#[test]
fn installed_boundary_sixteen_manifest_is_exactly_reconstructed_and_pinned() {
    let path = std::env::var_os(MANIFEST_OVERRIDE_ENV)
        .map(PathBuf::from)
        .unwrap_or_else(|| repo_root().join(MANIFEST_PATH));
    validate_boundary_sixteen_manifest(&path)
        .unwrap_or_else(|error| panic!("{}: {error}", path.display()));
}

#[test]
fn frozen_java_printf_field_arrays_match_the_strict_parser() {
    let source = std::fs::read_to_string(repo_root().join(PROBE_SOURCE_PATH))
        .expect("Boundary-16 Java probe source");
    for kind in [
        RowKind::Page,
        RowKind::Predecessor,
        RowKind::Baseline,
        RowKind::GroupMember,
        RowKind::Sibling,
        RowKind::SourceOutgoing,
        RowKind::PairRelation,
        RowKind::Geometry,
        RowKind::Edge,
        RowKind::StemIncident,
        RowKind::BeamIncident,
        RowKind::Callback,
        RowKind::LinkerLookup,
        RowKind::LinkerFlag,
        RowKind::SiblingResult,
        RowKind::Result,
        RowKind::DeltaGuard,
        RowKind::Summary,
        RowKind::SyntheticCase,
        RowKind::SyntheticEvent,
        RowKind::SyntheticGuard,
    ] {
        assert_eq!(
            java_printf_fields(&source, kind).expect("Java printf field extraction"),
            kind.expected_fields(),
            "{kind:?} parser/source field order differs"
        );
    }
    let runner = std::fs::read_to_string(repo_root().join(RUNNER_SOURCE_PATH))
        .expect("Boundary-16 oracle runner source");
    assert_eq!(
        runner_corpus_printf_fields(&runner).expect("runner corpus printf field extraction"),
        CORPUS_SUMMARY_FIELDS,
        "CorpusSummary parser/runner field order differs"
    );
}

#[test]
fn boundary_fifteen_fixture_manifest_and_typed_gate_are_exactly_pinned() {
    assert_eq!(
        read_sha256(BOUNDARY_FIFTEEN_MANIFEST_PATH).expect("Boundary-15 manifest"),
        BOUNDARY_FIFTEEN_MANIFEST_SHA256
    );
    assert_eq!(
        read_sha256(BOUNDARY_FIFTEEN_FIXTURE_PATH).expect("Boundary-15 Chula fixture"),
        BOUNDARY_FIFTEEN_FIXTURE_SHA256
    );
    assert_eq!(
        read_sha256(BOUNDARY_FIFTEEN_GATE_PATH).expect("Boundary-15 typed replay gate"),
        BOUNDARY_FIFTEEN_GATE_SHA256
    );
    validate_manifest_fixture_pin().expect("Boundary-15 manifest split pin");
}

fn horizontal(x1: f64, x2: f64, y: f64) -> Line {
    Line {
        x1,
        y1: y,
        x2,
        y2: y,
    }
}

fn serial_fixture() -> (IndependentInput, IndependentState) {
    let base = BeamId(10);
    let same_glyph = BeamId(11);
    let existing = BeamId(12);
    let shorter_wrong_side = BeamId(13);
    let full_link = BeamId(14);
    let hook_link = BeamId(15);
    let stem = StemId(20);
    let beams = BTreeMap::from([
        (
            base,
            BeamState {
                runtime_class: BeamRuntimeClass::Beam,
                glyph_identity: Some(100),
                median: horizontal(0.0, 10.0, 10.0),
                height: 4.0,
                abnormal: true,
            },
        ),
        (
            same_glyph,
            BeamState {
                runtime_class: BeamRuntimeClass::Beam,
                glyph_identity: Some(100),
                median: horizontal(0.0, 10.0, 0.0),
                height: 4.0,
                abnormal: true,
            },
        ),
        (
            existing,
            BeamState {
                runtime_class: BeamRuntimeClass::Beam,
                glyph_identity: Some(102),
                median: horizontal(0.0, 10.0, 25.0),
                height: 4.0,
                abnormal: true,
            },
        ),
        (
            shorter_wrong_side,
            BeamState {
                runtime_class: BeamRuntimeClass::Beam,
                glyph_identity: Some(103),
                median: horizontal(1.0, 9.0, 5.0),
                height: 4.0,
                abnormal: true,
            },
        ),
        (
            full_link,
            BeamState {
                runtime_class: BeamRuntimeClass::SmallBeam,
                glyph_identity: Some(104),
                median: horizontal(0.0, 10.0, 15.0),
                height: 4.0,
                abnormal: true,
            },
        ),
        (
            hook_link,
            BeamState {
                runtime_class: BeamRuntimeClass::Hook,
                glyph_identity: Some(105),
                median: horizontal(0.0, 10.0, 20.0),
                height: 2.0,
                abnormal: true,
            },
        ),
    ]);
    let vertices = beams
        .keys()
        .copied()
        .map(Vertex::Beam)
        .chain([Vertex::Stem(stem)])
        .collect();
    let grade_bits = 0.75_f64.to_bits();
    let state = IndependentState {
        vertices,
        edges: vec![
            GraphEdge {
                relation_identity: 1,
                source: Vertex::Beam(existing),
                target: Vertex::Stem(stem),
                payload: RelationPayload::Other,
            },
            GraphEdge {
                relation_identity: 2,
                source: Vertex::Beam(existing),
                target: Vertex::Stem(stem),
                payload: RelationPayload::BeamStem {
                    portion: BeamPortion::Left,
                    grade_bits,
                },
            },
            GraphEdge {
                relation_identity: 3,
                source: Vertex::Beam(existing),
                target: Vertex::Stem(stem),
                payload: RelationPayload::Other,
            },
            GraphEdge {
                relation_identity: 4,
                source: Vertex::Beam(full_link),
                target: Vertex::Beam(base),
                payload: RelationPayload::BeamRest {
                    portion: BeamPortion::Left,
                },
            },
        ],
        beams,
        builder_items: vec![
            BuilderItem::NonLinker,
            BuilderItem::Linker {
                source: full_link,
                linker: LinkerId(10),
                linked_cell: LinkerCellId(1),
            },
            BuilderItem::Linker {
                source: full_link,
                linker: LinkerId(20),
                linked_cell: LinkerCellId(2),
            },
            BuilderItem::Linker {
                source: hook_link,
                linker: LinkerId(30),
                linked_cell: LinkerCellId(3),
            },
        ],
        linked_cells: BTreeMap::from([
            (LinkerCellId(1), false),
            (LinkerCellId(2), false),
            (LinkerCellId(3), true),
        ]),
        linked_cell_observers: BTreeMap::from([
            (
                LinkerCellId(1),
                vec![LinkerId(10), LinkerId(11), LinkerId(13)],
            ),
            (LinkerCellId(2), vec![LinkerId(20), LinkerId(21)]),
            (
                LinkerCellId(3),
                vec![LinkerId(30), LinkerId(31), LinkerId(32)],
            ),
        ]),
        stub_modified: false,
        book_modified: false,
        book_dirty: false,
        next_relation_identity: 5,
    };
    let input = IndependentInput {
        base_beam: base,
        stem,
        ref_point: Point { x: 5.0, y: 10.0 },
        skewed_vertical: Line {
            x1: 5.0,
            y1: -100.0,
            x2: 5.0,
            y2: 100.0,
        },
        stem_median: Line {
            x1: 5.0,
            y1: -100.0,
            x2: 5.0,
            y2: 100.0,
        },
        // Deliberately not top-to-bottom. Discovery must sort stably by
        // intersection ordinate before removing only the first base identity.
        group_members: vec![
            existing,
            hook_link,
            base,
            shorter_wrong_side,
            same_glyph,
            full_link,
        ],
        max_beam_side_dx: 2.0,
        max_shorter_ratio: 0.8,
        portion_max_dx: 2,
        y_dir: 1,
        continuation_support_grade_bits: grade_bits,
        add_edge_behavior: BTreeMap::new(),
    };
    (input, state)
}

#[test]
fn public_boundary_shape_pins_four_branches_and_head_loop_terminal() {
    assert_eq!(
        [
            SiblingBranch::SameGlyph,
            SiblingBranch::ExistingBeamStem,
            SiblingBranch::ShorterWrongSide,
            SiblingBranch::Linked,
        ]
        .map(native_branch),
        [
            NativeStemsBeamSiblingBranch::SameGlyph,
            NativeStemsBeamSiblingBranch::ExistingBeamStem,
            NativeStemsBeamSiblingBranch::ShorterWrongSide,
            NativeStemsBeamSiblingBranch::Linked,
        ]
    );
    let outcome = NativeStemsBeamVLinkSiblingLinksOutcome::ReadyBeforeHeadRelationLoop {
        stem_identity: 17,
        continuation_support_grade: 0.75,
    };
    let NativeStemsBeamVLinkSiblingLinksOutcome::ReadyBeforeHeadRelationLoop {
        stem_identity,
        continuation_support_grade,
    } = outcome;
    assert_eq!(stem_identity, 17);
    assert_eq!(continuation_support_grade.to_bits(), 0.75_f64.to_bits());

    let _entry_point = apply_native_stems_beam_vlink_sibling_links_transaction;
    let _certificate_binding: Option<NativeStemsBeamVLinkSiblingLinksCertificate> = None;
    let _state_binding: Option<NativeStemsBeamVLinkSiblingLinksState> = None;
    let _transaction_binding: Option<NativeStemsBeamVLinkSiblingLinksTransaction> = None;
    let _baseline_projection: fn(&NativeStemsBeamVLinkSiblingLinksTransaction) =
        assert_public_baseline_projection;
    let _state_projection: fn(
        &NativeStemsBeamVLinkSiblingLinksState,
        &NativeStemsBeamSiblingLiveBeam,
    ) = assert_public_state_projection;
}

#[test]
fn supplemental_case_matrix_is_eight_supported_then_two_envelopes() {
    assert_eq!(
        SUPPLEMENTAL_CASES
            .iter()
            .map(|spec| spec.name)
            .collect::<Vec<_>>(),
        [
            "SameGlyph",
            "ExistingBeamStem",
            "ShorterWrongSide",
            "LinkedBeam",
            "LinkedSmallBeam",
            "LinkedHook",
            "LinkedNoBLinker",
            "LinkedIdempotentBCell",
            "ThrowBeforeInsertion",
            "ThrowDuringCallback",
        ]
    );
    assert_eq!(
        SUPPLEMENTAL_CASES
            .iter()
            .filter(|spec| spec.scope == "synthetic")
            .count(),
        8
    );
    assert_eq!(
        SUPPLEMENTAL_CASES
            .iter()
            .filter(|spec| spec.scope == "envelope")
            .count(),
        2
    );
    assert_eq!(
        SUPPLEMENTAL_CASES
            .iter()
            .filter(|spec| spec.sibling_class.ends_with("SmallBeamInter"))
            .map(|spec| spec.name)
            .collect::<Vec<_>>(),
        ["LinkedSmallBeam"]
    );
}

#[test]
fn independent_model_preserves_branches_callback_and_flag_interleaving() {
    let (input, mut state) = serial_fixture();
    let transaction = apply_independent(&input, &mut state).expect("independent sibling loop");
    assert_eq!(transaction.ref_point, input.ref_point);
    assert_eq!(transaction.terminal, Terminal::ReadyBeforeHeadRelationLoop);
    assert_eq!(
        transaction.sorted_siblings_before_base_removal,
        vec![
            BeamId(11),
            BeamId(13),
            BeamId(10),
            BeamId(14),
            BeamId(15),
            BeamId(12),
        ]
    );
    assert_eq!(transaction.base_removal_index, Some(2));
    assert_eq!(
        transaction
            .sibling_traces
            .iter()
            .map(|trace| trace.branch)
            .collect::<Vec<_>>(),
        vec![
            SiblingBranch::SameGlyph,
            SiblingBranch::ShorterWrongSide,
            SiblingBranch::Linked,
            SiblingBranch::Linked,
            SiblingBranch::ExistingBeamStem,
        ]
    );
    let shorter = &transaction.sibling_traces[1];
    let shorter_geometry = shorter.geometry.as_ref().expect("shorter geometry");
    assert_eq!(shorter_geometry.ratio.to_bits(), 0.8_f64.to_bits());
    assert!(shorter_geometry.shorter_branch_read);
    assert_eq!(shorter_geometry.dy, Some(-5.0));
    assert_eq!(shorter_geometry.dy_times_y_dir, Some(-5.0));
    assert_eq!(shorter_geometry.extension, None);
    assert_eq!(shorter_geometry.portion, None);
    assert_eq!(shorter_geometry.grade_bits, None);

    let full = &transaction.sibling_traces[2];
    assert_eq!(full.first_event_ordinal, 0);
    assert_eq!(full.event_count, 3);
    assert_eq!(
        full.geometry.as_ref().and_then(|geometry| geometry.portion),
        Some(BeamPortion::Center)
    );
    let full_callback = full.callback.as_ref().expect("full callback");
    assert_eq!(full_callback.beam_incident_relation_identities, vec![4, 5]);
    assert_eq!(
        (full_callback.abnormal_before, full_callback.abnormal_after),
        (true, true)
    );
    let full_lookup = full.linker_lookup.as_ref().expect("full lookup");
    assert_eq!(full_lookup.examined_item_ordinals, vec![0, 1]);
    assert_eq!(full_lookup.matched_item_ordinal, Some(1));
    assert_eq!(full_lookup.unread_suffix, 2);
    assert_eq!(
        (full_lookup.write_count, full_lookup.value_change_count),
        (1, 1)
    );

    let hook = &transaction.sibling_traces[3];
    assert_eq!(hook.first_event_ordinal, 3);
    assert_eq!(hook.event_count, 3);
    let hook_callback = hook.callback.as_ref().expect("hook callback");
    assert_eq!(
        (hook_callback.abnormal_before, hook_callback.abnormal_after),
        (true, false)
    );
    assert!(!hook_callback.stub_modified_before);
    assert!(hook_callback.stub_modified_after);
    assert!(hook_callback.book_modified_after);
    assert!(hook_callback.book_dirty_after);
    let hook_lookup = hook.linker_lookup.as_ref().expect("hook lookup");
    assert_eq!(hook_lookup.examined_item_ordinals, vec![0, 1, 2, 3]);
    assert_eq!(hook_lookup.matched_item_ordinal, Some(3));
    assert_eq!(
        (hook_lookup.write_count, hook_lookup.value_change_count),
        (1, 0)
    );

    let existing = &transaction.sibling_traces[4];
    assert_eq!(existing.pair_relations.len(), 3);
    assert!(existing.pair_relations[0].class_read);
    assert!(!existing.pair_relations[0].matched_beam_stem);
    assert!(existing.pair_relations[1].class_read);
    assert!(existing.pair_relations[1].matched_beam_stem);
    assert!(!existing.pair_relations[2].class_read);
    assert!(!existing.pair_relations[2].matched_beam_stem);
    assert_eq!(
        transaction.events,
        vec![
            SerialEvent::EdgeInserted {
                sibling_ordinal: 2,
                relation_identity: 5,
            },
            SerialEvent::CallbackCompleted {
                sibling_ordinal: 2,
                relation_identity: 5,
            },
            SerialEvent::LinkerFlagAssigned {
                sibling_ordinal: 2,
                selected_linker: LinkerId(10),
                cell: LinkerCellId(1),
                ordered_observers: vec![LinkerId(10), LinkerId(11), LinkerId(13)],
                before: false,
                after: true,
            },
            SerialEvent::EdgeInserted {
                sibling_ordinal: 3,
                relation_identity: 6,
            },
            SerialEvent::CallbackCompleted {
                sibling_ordinal: 3,
                relation_identity: 6,
            },
            SerialEvent::LinkerFlagAssigned {
                sibling_ordinal: 3,
                selected_linker: LinkerId(30),
                cell: LinkerCellId(3),
                ordered_observers: vec![LinkerId(30), LinkerId(31), LinkerId(32)],
                before: true,
                after: true,
            },
        ]
    );
    assert_eq!(state.edges.len(), 6);
    assert!(state.linked_cells[&LinkerCellId(1)]);
    assert!(!state.linked_cells[&LinkerCellId(2)]);
    assert!(state.linked_cells[&LinkerCellId(3)]);
}

#[test]
fn java_double_order_is_stable_and_canonicalizes_nan() {
    assert_eq!(java_double_compare(-0.0, 0.0), Ordering::Less);
    assert_eq!(java_double_compare(0.0, -0.0), Ordering::Greater);
    assert_eq!(java_double_compare(f64::NEG_INFINITY, -0.0), Ordering::Less);
    let negative_nan = f64::from_bits(0xfff0_0000_0000_0001);
    let payload_nan = f64::from_bits(0x7ff0_0000_0000_0042);
    assert_eq!(
        java_double_compare(negative_nan, payload_nan),
        Ordering::Equal
    );
    let mut stable = vec![(0, payload_nan), (1, negative_nan), (2, payload_nan)];
    stable.sort_by(|left, right| java_double_compare(left.1, right.1));
    assert_eq!(
        stable.into_iter().map(|(id, _)| id).collect::<Vec<_>>(),
        vec![0, 1, 2]
    );
}

fn one_sibling_fixture() -> (IndependentInput, IndependentState) {
    let base = BeamId(1);
    let sibling = BeamId(2);
    let stem = StemId(1);
    let beams = BTreeMap::from([
        (
            base,
            BeamState {
                runtime_class: BeamRuntimeClass::Beam,
                glyph_identity: None,
                median: horizontal(0.0, 10.0, 0.0),
                height: 4.0,
                abnormal: true,
            },
        ),
        (
            sibling,
            BeamState {
                runtime_class: BeamRuntimeClass::Hook,
                glyph_identity: Some(2),
                median: horizontal(0.0, 10.0, 10.0),
                height: 2.0,
                abnormal: true,
            },
        ),
    ]);
    let state = IndependentState {
        vertices: BTreeSet::from([
            Vertex::Beam(base),
            Vertex::Beam(sibling),
            Vertex::Stem(stem),
        ]),
        edges: Vec::new(),
        beams,
        builder_items: vec![BuilderItem::Linker {
            source: sibling,
            linker: LinkerId(1),
            linked_cell: LinkerCellId(1),
        }],
        linked_cells: BTreeMap::from([(LinkerCellId(1), false)]),
        linked_cell_observers: BTreeMap::from([(LinkerCellId(1), vec![LinkerId(1), LinkerId(2)])]),
        stub_modified: false,
        book_modified: false,
        book_dirty: false,
        next_relation_identity: 1,
    };
    let input = IndependentInput {
        base_beam: base,
        stem,
        ref_point: Point { x: 5.0, y: 0.0 },
        skewed_vertical: Line {
            x1: 5.0,
            y1: -100.0,
            x2: 5.0,
            y2: 100.0,
        },
        stem_median: Line {
            x1: 5.0,
            y1: -100.0,
            x2: 5.0,
            y2: 100.0,
        },
        group_members: vec![base, sibling],
        max_beam_side_dx: 2.0,
        max_shorter_ratio: 0.8,
        portion_max_dx: 2,
        y_dir: 1,
        continuation_support_grade_bits: 1.0_f64.to_bits(),
        add_edge_behavior: BTreeMap::new(),
    };
    (input, state)
}

#[test]
fn add_edge_return_and_throw_prefixes_are_not_atomic() {
    let (mut input, initial) = one_sibling_fixture();
    input
        .add_edge_behavior
        .insert(BeamId(2), AddEdgeBehavior::ReturnedFalse);
    let mut returned_false = initial.clone();
    let transaction =
        apply_independent(&input, &mut returned_false).expect("ignored false addEdge result");
    assert_eq!(transaction.terminal, Terminal::ReadyBeforeHeadRelationLoop);
    assert!(returned_false.edges.is_empty());
    assert_eq!(
        transaction.events,
        vec![SerialEvent::LinkerFlagAssigned {
            sibling_ordinal: 0,
            selected_linker: LinkerId(1),
            cell: LinkerCellId(1),
            ordered_observers: vec![LinkerId(1), LinkerId(2)],
            before: false,
            after: true,
        }]
    );

    input
        .add_edge_behavior
        .insert(BeamId(2), AddEdgeBehavior::ThrowBeforeInsertion);
    let mut before_insert = initial.clone();
    let transaction = apply_independent(&input, &mut before_insert).expect("throw envelope");
    assert_eq!(
        transaction.terminal,
        Terminal::Threw(ThrowStage::AddEdgeBeforeInsertion)
    );
    assert!(transaction.events.is_empty());
    assert!(before_insert.edges.is_empty());
    assert!(!before_insert.linked_cells[&LinkerCellId(1)]);

    input
        .add_edge_behavior
        .insert(BeamId(2), AddEdgeBehavior::ThrowDuringCallback);
    let mut callback_throw = initial;
    let transaction = apply_independent(&input, &mut callback_throw).expect("callback envelope");
    assert_eq!(
        transaction.terminal,
        Terminal::Threw(ThrowStage::RelationCallbackAfterInsertion)
    );
    assert_eq!(callback_throw.edges.len(), 1);
    assert_eq!(
        transaction.events,
        vec![SerialEvent::EdgeInserted {
            sibling_ordinal: 0,
            relation_identity: 1,
        }]
    );
    assert!(!callback_throw.linked_cells[&LinkerCellId(1)]);
}

#[test]
fn compact_model_fails_closed_on_live_chords_and_malformed_state() {
    let (input, initial) = one_sibling_fixture();
    let mut chord_state = initial.clone();
    chord_state.edges.push(GraphEdge {
        relation_identity: 1,
        source: Vertex::Beam(BeamId(1)),
        target: Vertex::Stem(StemId(1)),
        payload: RelationPayload::ChordStem,
    });
    chord_state.next_relation_identity = 2;
    let before = chord_state.clone();
    assert_eq!(
        apply_independent(&input, &mut chord_state),
        Err(IndependentError::ChordBearingStem)
    );
    assert_eq!(chord_state, before);

    let mut invalid_direction = input.clone();
    invalid_direction.y_dir = 0;
    let mut state = initial.clone();
    assert_eq!(
        apply_independent(&invalid_direction, &mut state),
        Err(IndependentError::InvalidDirection)
    );
    assert_eq!(state, initial);

    let mut missing_base = input.clone();
    missing_base.base_beam = BeamId(99);
    let mut state = initial.clone();
    assert_eq!(
        apply_independent(&missing_base, &mut state),
        Err(IndependentError::MissingBaseBeam)
    );

    let mut missing_stem_state = initial.clone();
    missing_stem_state
        .vertices
        .remove(&Vertex::Stem(input.stem));
    let missing_stem_before = missing_stem_state.clone();
    assert_eq!(
        apply_independent(&input, &mut missing_stem_state),
        Err(IndependentError::MissingStem)
    );
    assert_eq!(missing_stem_state, missing_stem_before);

    let mut missing_group = input.clone();
    missing_group.group_members.push(BeamId(99));
    let mut state = initial.clone();
    assert_eq!(
        apply_independent(&missing_group, &mut state),
        Err(IndependentError::MissingGroupBeam)
    );

    let mut invalid_allocator = initial.clone();
    invalid_allocator.next_relation_identity = 0;
    let invalid_allocator_before = invalid_allocator.clone();
    assert_eq!(
        apply_independent(&input, &mut invalid_allocator),
        Err(IndependentError::InvalidRelationAllocator)
    );
    assert_eq!(invalid_allocator, invalid_allocator_before);

    let mut missing_cell = initial.clone();
    missing_cell.linked_cells.clear();
    let missing_cell_before = missing_cell.clone();
    assert_eq!(
        apply_independent(&input, &mut missing_cell),
        Err(IndependentError::MissingLinkerCell)
    );
    assert_eq!(missing_cell, missing_cell_before);
}

/// Boundary 16 computes the sibling writes the SIDES pass recorded.
///
/// The resume chain needs, per transaction, which *other* B linkers `linkSiblings` left
/// linked -- that is what retires the 21 sides Java skips, and feeding it from the oracle
/// makes the chain stop exactly where Java stops. This checks the port can produce those
/// writes itself, by running the production Boundary-16 apply and comparing its
/// `sibling_b_linker_cells` against the aliases the full-pass probe recorded for the same
/// transaction.
///
/// Replay-on-frozen, per rust/PORTING.md: the derivation is proven on the transaction that
/// already has frozen evidence before it is trusted on the 31 that do not.
#[test]
fn boundary_sixteen_derives_the_sibling_writes_the_pass_recorded() {
    let text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-vlink-sibling-links-chula.txt"),
    )
    .expect("frozen sibling-links fixture");
    let rows = parse_scaffold_fixture(&text).expect("fixture parses");
    let transactions = validate_core_rows(&rows).expect("core rows");
    let transaction = transactions
        .iter()
        .find(|candidate| candidate.key.system == 1)
        .expect("chula system 1 transaction");
    let hydrated =
        hydrate_real_boundary_sixteen(&rows[0], transaction).expect("boundary 16 applies");

    let sig_of: std::collections::BTreeMap<_, _> = hydrated
        .predecessor
        .stumps
        .beams_by_abscissa
        .iter()
        .map(|beam| (beam.source, beam.sig_ordinal))
        .collect();
    let alias =
        |reference: &audiveris_omr::native_stems_beam_vlinkers::NativeStemsBeamBLinkerRef| {
            format!(
                "beam:{}:b:{}",
                sig_of.get(&reference.beam).expect("sig ordinal"),
                reference.id - 1
            )
        };

    let mut derived: Vec<String> = hydrated
        .state_after
        .sibling_b_linker_cells
        .iter()
        .filter(|cell| cell.linked)
        .map(|cell| alias(&cell.reference))
        .collect();
    derived.sort();
    derived.dedup();

    // What Java's full pass recorded for the same transaction.
    let pass = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-sides-pass-chula-system1.txt"),
    )
    .expect("frozen SIDES pass");
    let first = pass
        .lines()
        .find(|line| {
            line.starts_with("stemsbeamsidesloopsibling ") && line.contains(" transaction 1 ")
        })
        .expect("the pass records a first transaction");
    let mut recorded: Vec<String> = first
        .split(" aliases ")
        .nth(1)
        .map(|list| list.trim().split(',').map(str::to_owned).collect())
        .unwrap_or_default();
    recorded.sort();

    assert!(
        !recorded.is_empty(),
        "the first transaction should write sibling cells; if it stopped doing so the \
         comparison below would prove nothing"
    );
    assert_eq!(
        derived, recorded,
        "Boundary 16's sibling cells differ from what Java's pass recorded for the same \
         transaction"
    );
    println!(
        "boundary 16 derived {} sibling writes, matching Java's pass: {derived:?}",
        derived.len()
    );
}

/// The first measured linked-S branch uses native Allegretto plan topology and
/// the owned SIG/S-cell read path.  The compact Java result is not opened until
/// that read has selected the persistent stem and proved the suffix unread.
#[test]
fn allegretto_transaction_28_linked_s_is_graph_derived_before_oracle_read() {
    let page = b15_hydration::native_predecessor_page("allegretto.png");
    let plans = &page.plans.systems[0];
    let mut plan_ordinal = 0_usize;
    let mut selected = None;
    for builder in &plans.builders {
        for attempt in &builder.attempts {
            if plan_ordinal == 25 {
                selected = Some((builder, attempt));
            }
            plan_ordinal += 1;
        }
    }
    let (builder, attempt) = selected.expect("native Allegretto plan 25");
    assert_eq!(attempt.relations.len(), 2);
    assert_eq!(attempt.stem_profile, 1);
    assert_eq!(builder.start.side, NativeStemVerticalSide::Bottom);
    assert_eq!(
        attempt
            .relations
            .iter()
            .map(|relation| (
                relation.corner.x_ordinal,
                relation.corner.horizontal,
                relation.corner.vertical,
            ))
            .collect::<Vec<_>>(),
        vec![
            (2, NativeStemHeadSide::Right, NativeStemVerticalSide::Top,),
            (3, NativeStemHeadSide::Right, NativeStemVerticalSide::Top,),
        ]
    );

    let mut scheduler = page.scheduler.systems[0].clone();
    let beam = builder.start.b_linker.beam;
    let beam_sig = page.beam_stumps.systems[0]
        .beams_by_abscissa
        .iter()
        .find(|candidate| candidate.source == beam)
        .expect("plan-25 beam in native stump catalogue")
        .sig_ordinal;
    assert_eq!(beam_sig, 25);
    scheduler.status = NativeStemsBeamSchedulerStatus::AwaitingVLinkTransaction(Box::new(
        NativeStemsBeamAwaitingVLinkTransaction {
            invocation_ordinal: 28,
            snapshot: NativeStemsBeamWorklistSnapshot {
                pass: NativeStemsBeamSchedulerPass::Sides,
                current_index: 0,
                sources: vec![beam],
                current: beam,
                remaining: Vec::new(),
            },
            beam,
            horizontal_side: Some(NativeStemHeadSide::Right),
            b_linker: builder.start.b_linker,
            v_linker: builder.start,
            vertical_side: builder.start.side,
            plan: NativeStemsBeamPlanRef {
                system_id: 1,
                plan_ordinal: 25,
                builder_ordinal: builder.builder_ordinal,
                stem_profile: attempt.stem_profile,
            },
            outcome: attempt.outcome,
            linked_sides_before: Vec::new(),
            retained_beams_before: Vec::new(),
            would_apply_stored_line_delta: None,
        },
    ));

    let assembled = page.sig.as_ref().expect("native Allegretto SIG");
    let mut sig = assembled.systems[0].clone();
    let mut bindings = assembled.bindings[0].clone();
    let stem_vertex = NativeSigVertexId(sig.vertices.len());
    sig.append_vertex(NativeSigVertex {
        ordinal: stem_vertex.0,
        active: true,
        removed: false,
        kind: NativeSigInterKind::Stem,
        shape: Some("STEM".to_owned()),
        grade: 0.5,
        bounds: NativeSigBounds {
            x: 602,
            y: 10,
            width: 3,
            height: 90,
        },
        abnormal: false,
        beam_geometry: None,
    })
    .expect("modeled persistent stem vertex");
    bindings
        .bind_stem(0, stem_vertex)
        .expect("modeled persistent stem binding");

    // Java's selected relation has global identity 229. Preserve every native
    // baseline edge, then represent unrelated predecessor insertions as
    // tombstones so the one live measured HeadStem relation occupies that
    // identity without entering this bounded gate's semantic scan.
    assert!(sig.edges.len() <= 229);
    let tombstone = sig.edges[0];
    while sig.edges.len() < 229 {
        let mut edge = tombstone;
        edge.ordinal = sig.edges.len();
        edge.active = false;
        sig.edges.push(edge);
    }
    let first_corner = attempt.relations[0].corner;
    let head_vertex = bindings.head_vertices[&first_corner.head];
    sig.append_edge(NativeSigEdge {
        ordinal: 229,
        active: true,
        source: head_vertex.0,
        target: stem_vertex.0,
        kind: NativeSigRelationKind::HeadStem,
        origin: NativeSigRelationOrigin::BeamVHeadDraft {
            plan_ordinal: 15,
            map_ordinal: 0,
        },
        support: Some(NativeSigSupport {
            grade: 0.5,
            bar_connection_impacts: None,
        }),
        beam_portion: None,
        stem_extension: None,
        head_stem: Some(NativeSigHeadStemPayload {
            dx: 0.0,
            dy: 0.0,
            head_side: NativeStemHeadSide::Right,
            extension_point: NativeStemPoint { x: 603.0, y: 10.0 },
            consistency: 1.0,
            manual: false,
        }),
    })
    .expect("measured predecessor HeadStem relation");

    let glyph = &attempt.glyphs[0];
    let system_stems = NativeStemsBeamSystemStemTransactionState {
        system_id: 1,
        next_stem_identity: 1,
        known_stems: vec![NativeStemsBeamKnownSystemStem {
            stem_identity: 0,
            glyph_id: 266,
            glyph_content: NativeStemsBeamFixedGlyphContent {
                bounds: glyph.bounds,
                weight: glyph.weight,
                run_table: glyph.structural_key.run_table.clone(),
            },
            inter_id: Some(2227),
            grade: NativeStemsBeamStemGrade::Artificial(0.5),
            geometry: NativeStemsBeamCreatedStemGeometry {
                median: NativeStemLine {
                    start: NativeStemPoint { x: 603.0, y: 10.0 },
                    stop: NativeStemPoint { x: 603.0, y: 100.0 },
                },
                mean_thickness: 3.0,
                ribbon_bounds: JavaRectangle {
                    x: 602,
                    y: 10,
                    width: 3,
                    height: 90,
                },
            },
            sig_attached: true,
            abnormal: false,
        }],
        authority: NativeStemsBeamRegistryAuthority::CompleteSinceEmptyBaseline,
        exhaustive_lookup: None,
    };
    let mut s_cells = initialize_native_stems_beam_s_linker_cells(&page.head_corners.systems[0])
        .expect("native Allegretto S-cell topology");
    let selected_cell = s_cells
        .iter_mut()
        .find(|cell| {
            cell.reference.head.reference == first_corner.head
                && cell.reference.horizontal == first_corner.horizontal
        })
        .expect("plan-25 shared S cell");
    selected_cell.linked = true;

    let sig_before = sig.clone();
    let bindings_before = bindings.clone();
    let cells_before = s_cells.clone();
    let stems_before = system_stems.clone();
    let actual = project_native_stems_beam_vlink_reuse_live_state(
        &sig,
        &bindings,
        &scheduler,
        plans,
        &s_cells,
        &system_stems,
    )
    .expect("graph-derived Allegretto linked-S B13 read");
    assert_eq!(sig, sig_before);
    assert_eq!(bindings, bindings_before);
    assert_eq!(s_cells, cells_before);
    assert_eq!(system_stems, stems_before);
    let NativeStemsBeamVLinkReuseLiveEvaluation::Entries(entries) = &actual.evaluation else {
        panic!("accepted plan must inspect its relations");
    };
    let [first, second] = entries.as_slice() else {
        panic!("plan 25 must retain two relation positions");
    };
    let NativeStemsBeamReuseEntryObservation::Examined {
        s_linker_linked: true,
        head_stem_lookup: NativeStemsBeamHeadStemLookupEvidence::Exhaustive(scan),
    } = &first.observation
    else {
        panic!("first relation must exhaustively scan the linked S cell");
    };
    assert_eq!((scan.scanned_relation_count, scan.edges.len()), (1, 1));
    assert!(matches!(
        second.observation,
        NativeStemsBeamReuseEntryObservation::UnreadAfterSelection
    ));
    let snapshot_hash = scan.provenance_sha256.clone();
    let projection_hash = scan
        .java_projection_sha256(NativeStemHeadSide::Right, &actual.live_sig_stems)
        .expect("Java projection hash");

    // Expected-only rows and their source pins are opened after the native
    // graph read and its read-only/termination properties are established.
    let fixture_path = repo_root().join("rust/oracle/stems-beam-linked-s-allegretto-system1.txt");
    let fixture = std::fs::read_to_string(&fixture_path).expect("bounded linked-S fixture");
    assert_eq!(
        sha256_hex(fixture.as_bytes()),
        "287175a58717874882bc6487f7d59ea86a22e44cadcac003ee99a36606e5ab34"
    );
    let data = fixture
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(data.len(), 6);
    let entry_rows = data
        .iter()
        .filter(|line| line.starts_with("stemsbeamlinkedsentry "))
        .copied()
        .collect::<Vec<_>>();
    assert_eq!(entry_rows.len(), 2);
    let field = |line: &str, name: &str| {
        let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
        let position = tokens
            .iter()
            .position(|token| *token == name)
            .unwrap_or_else(|| panic!("missing {name} in {line}"));
        tokens[position + 1].to_owned()
    };
    assert_eq!(field(entry_rows[0], "mapOrdinal"), "0");
    assert_eq!(field(entry_rows[0], "cAlias"), "h:2:RIGHT:TOP");
    assert_eq!(field(entry_rows[0], "sLinked"), "true");
    assert_eq!(field(entry_rows[0], "incidentEdges"), "1");
    assert_eq!(field(entry_rows[0], "matchingEdges"), "1");
    assert_eq!(field(entry_rows[0], "distinctSideStems"), "1");
    assert_eq!(field(entry_rows[0], "headSnapshotHash"), snapshot_hash);
    assert_eq!(field(entry_rows[0], "projectionHash"), projection_hash);
    assert_eq!(field(entry_rows[0], "action"), "SelectBreak");
    assert_eq!(field(entry_rows[1], "mapOrdinal"), "1");
    assert_eq!(field(entry_rows[1], "conditionRead"), "false");
    assert_eq!(field(entry_rows[1], "action"), "UnreadAfterBreak");
    let result = data
        .iter()
        .find(|line| line.starts_with("stemsbeamlinkedsresult "))
        .expect("linked-S result row");
    assert_eq!(field(result, "outcome"), "Selected");
    assert_eq!(field(result, "selectedMapOrdinal"), "0");
    assert_eq!(field(result, "entriesRead"), "1");
    assert_eq!(field(result, "unreadFrom"), "1");
    assert_eq!(field(result, "finalStemInterId"), "2227");
    let summary = data.last().expect("linked-S summary");
    assert_eq!(
        field(summary, "probeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("probe source")
        )
    );
    assert_eq!(
        field(summary, "runnerSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/run-stems-beam-linked-s.sh"))
                .expect("runner source")
        )
    );
    assert_eq!(field(summary, "freshRuns"), "2");
    assert_eq!(field(summary, "freshRunsByteIdentical"), "true");
    assert_eq!(field(summary, "stopBeforeSigAddVertex"), "true");
}

/// Direct atomic gate at the first real Java competing-hook checkpoint.
///
/// This deliberately reconstructs the measured transaction-28 boundary; it
/// does not claim that native code executed Allegretto transactions 1..28.
/// Java's per-transaction sibling writes are input authority for reaching the
/// typed scheduler frontier. The mutation/result fixture stays unopened until
/// the native graph mutation and continuation have returned.
#[test]
fn allegretto_hook_removal_checkpoint_is_atomic_and_reaches_sides_exhaustion() {
    let predecessor_path =
        repo_root().join("rust/oracle/stems-beam-hook-removal-predecessor-allegretto-system1.txt");
    let predecessor_text =
        std::fs::read_to_string(&predecessor_path).expect("hook scheduler predecessor");
    assert_eq!(
        sha256_hex(predecessor_text.as_bytes()),
        "d173f1c475245980cad02bbf4624987d787fb293e5419d21444729f18bf7c8f8"
    );
    let predecessor_rows = predecessor_text
        .lines()
        .filter(|line| line.starts_with("stemsbeamsidesloopsibling "))
        .collect::<Vec<_>>();
    assert_eq!(predecessor_rows.len(), 28);

    let base_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-vlink-base-apply-allegretto.txt"),
    )
    .expect("Allegretto B14 predecessor");
    let create_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-create-stem-allegretto.txt"),
    )
    .expect("Allegretto B12 predecessor");
    let reuse_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-vlink-reuse-check-allegretto.txt"),
    )
    .expect("Allegretto B13 predecessor");
    let hydrated = b15_hydration::run_real(
        "allegretto.png",
        1,
        &base_text,
        &create_text,
        &reuse_text,
        false,
    )
    .expect("native Allegretto checkpoint products");
    let sig_of = hydrated
        .stumps
        .beams_by_abscissa
        .iter()
        .map(|beam| (beam.source, beam.sig_ordinal))
        .collect::<BTreeMap<_, _>>();
    let source_of = sig_of
        .iter()
        .map(|(source, ordinal)| (*ordinal, *source))
        .collect::<BTreeMap<_, _>>();
    let alias_of = |reference: NativeStemsBeamBLinkerRef| {
        format!("beam:{}:b:{}", sig_of[&reference.beam], reference.id - 1)
    };
    let reference_of = |alias: &str| {
        let fields = alias.split(':').collect::<Vec<_>>();
        assert_eq!(fields.len(), 4);
        assert_eq!(fields[0], "beam");
        assert_eq!(fields[2], "b");
        NativeStemsBeamBLinkerRef {
            beam: source_of[&fields[1].parse::<usize>().expect("beam ordinal")],
            id: fields[3].parse::<usize>().expect("B ordinal") + 1,
        }
    };

    // Reproduce only the scheduler input checkpoint from Java's recorded
    // transaction results. No mutation-result row has been opened yet.
    let mut scheduler = hydrated.scheduler.clone();
    for (index, row) in predecessor_rows.iter().enumerate() {
        let NativeStemsBeamSchedulerStatus::AwaitingVLinkTransaction(frontier) = &scheduler.status
        else {
            panic!(
                "scheduler stopped before predecessor transaction {}",
                index + 1
            );
        };
        let tokens = row.split_ascii_whitespace().collect::<Vec<_>>();
        let field = |name: &str| {
            let position = tokens
                .iter()
                .position(|token| *token == name)
                .unwrap_or_else(|| panic!("missing {name}: {row}"));
            tokens[position + 1]
        };
        assert_eq!(field("transaction").parse::<usize>().unwrap(), index + 1);
        assert_eq!(field("bAlias"), alias_of(frontier.b_linker));
        assert_eq!(field("ownLinked"), "true");
        let sibling_count = field("siblings").parse::<usize>().unwrap();
        let siblings = row
            .split(" aliases ")
            .nth(1)
            .map(|aliases| aliases.split(',').map(reference_of).collect::<Vec<_>>())
            .unwrap_or_default();
        assert_eq!(siblings.len(), sibling_count);
        let completed = NativeStemsBeamCompletedVLinkEvidence {
            plan: frontier.plan,
            b_linker: frontier.b_linker,
            v_linker: frontier.v_linker,
            outer_b_linked_after: true,
            sibling_linked_b_linkers: siblings,
        };
        scheduler = *resume_native_stems_beam_scheduler_after_transaction(
            &scheduler,
            &hydrated.vlinkers,
            &hydrated.builder,
            &hydrated.plans,
            &completed,
        )
        .expect("real scheduler predecessor write")
        .advanced_system;
    }
    let awaiting = match &scheduler.status {
        NativeStemsBeamSchedulerStatus::AwaitingHookRemovalTransaction(awaiting) => {
            awaiting.as_ref().clone()
        }
        _ => panic!("28 real Java transaction results did not reach hook removal"),
    };
    assert_eq!(sig_of[&awaiting.beam], 25);
    assert_eq!(sig_of[&awaiting.competing_hook], 24);
    assert_eq!(awaiting.snapshot.current_index, 19);
    // Native keeps more internal prefix events than Java's probe-visible
    // resume counter; both domains are pinned below at their boundary.
    assert_eq!(scheduler.prefix_events.len(), 89);

    // Reconstruct the checkpoint's live hook neighborhood from native initial
    // topology plus the two measured BeamStem incidences created earlier in
    // the carried pass. Unrelated intermediate insertions remain out of scope.
    let assembled = b15_hydration::native_predecessor_page("allegretto.png")
        .sig
        .expect("native Allegretto SIG");
    let mut sig = assembled.systems[0].clone();
    let mut bindings = assembled.bindings[0].clone();
    let hook_vertex = bindings.beam_vertices[&awaiting.competing_hook];
    let initial_hook_incidents = sig
        .incident_edges(hook_vertex.0)
        .expect("initial hook incidents");
    assert_eq!(initial_hook_incidents.len(), 3);
    for kind in [
        NativeSigRelationKind::Containment,
        NativeSigRelationKind::BeamBeam,
        NativeSigRelationKind::Exclusion,
    ] {
        assert_eq!(
            initial_hook_incidents
                .iter()
                .filter(|edge| edge.kind == kind)
                .count(),
            1
        );
    }
    for stem_identity in 0..2 {
        let stem_vertex = NativeSigVertexId(sig.vertices.len());
        sig.append_vertex(NativeSigVertex {
            ordinal: stem_vertex.0,
            active: true,
            removed: false,
            kind: NativeSigInterKind::Stem,
            shape: Some("STEM".to_owned()),
            grade: 0.5,
            bounds: NativeSigBounds {
                x: 603,
                y: 653,
                width: 3,
                height: 96,
            },
            abnormal: false,
            beam_geometry: None,
        })
        .expect("checkpoint stem vertex");
        bindings
            .bind_stem(stem_identity, stem_vertex)
            .expect("checkpoint stem binding");
        sig.append_edge(NativeSigEdge {
            ordinal: sig.edges.len(),
            active: true,
            source: hook_vertex.0,
            target: stem_vertex.0,
            kind: NativeSigRelationKind::BeamStem,
            origin: NativeSigRelationOrigin::BeamVSiblingDraft {
                plan_ordinal: 13 + (2 * stem_identity),
                sibling_ordinal: 0,
            },
            support: Some(NativeSigSupport {
                grade: 0.5,
                bar_connection_impacts: None,
            }),
            beam_portion: Some(if stem_identity == 0 {
                NativeBeamPortion::Right
            } else {
                NativeBeamPortion::Left
            }),
            stem_extension: Some(NativeStemPoint { x: 603.0, y: 653.0 }),
            head_stem: None,
        })
        .expect("checkpoint BeamStem incidence");
    }
    assert_eq!(sig.incident_edges(hook_vertex.0).unwrap().len(), 5);

    let mut b_cells = initialize_native_stems_beam_b_linker_cells(&hydrated.reachability)
        .expect("Allegretto B-cell arena");
    for cell in &mut b_cells {
        cell.linked = scheduler.linked_b_linkers.contains(&cell.reference);
    }
    let s_cells = initialize_native_stems_beam_s_linker_cells(&hydrated.head_corners)
        .expect("Allegretto S-cell arena");
    let context = NativeStemsBeamSidesContext {
        plans: &hydrated.plans,
        builders: &hydrated.builder,
        stumps: &hydrated.stumps,
        vlinkers: &hydrated.vlinkers,
        reachability: &hydrated.reachability,
        head_corners: &hydrated.head_corners,
        checker: &b15_hydration::checker_context_for_page(&b15_hydration::native_predecessor_page(
            "allegretto.png",
        )),
        relation_parameters: hydrated.relation_parameters,
    };
    let mut carrier = NativeStemsBeamSidesCarrier {
        scheduler,
        latest_base_apply: hydrated.state_before.base_apply_state_before.clone(),
        sig,
        bindings,
        b_cells,
        s_cells,
        beam_inter_index: Vec::new(),
        configured_inter_vip_ids: Vec::new(),
    };
    let carrier_before = carrier.clone();
    let before_snapshot = awaiting.snapshot.clone();

    let mut missing_exclusion = carrier.clone();
    let exclusion = missing_exclusion
        .sig
        .incident_edges(hook_vertex.0)
        .unwrap()
        .into_iter()
        .find(|edge| edge.kind == NativeSigRelationKind::Exclusion)
        .expect("full/hook exclusion")
        .ordinal;
    missing_exclusion
        .sig
        .remove_edge(audiveris_omr::native_sig::NativeSigEdgeId(exclusion))
        .unwrap();
    let corrupt_before = missing_exclusion.clone();
    let error = remove_native_stems_beam_competing_hook_and_resume(&mut missing_exclusion, context)
        .expect_err("missing exclusion must reject atomically");
    assert_eq!(error.stage, "hook-removal-incident");
    assert_eq!(missing_exclusion, corrupt_before);

    let actual = remove_native_stems_beam_competing_hook_and_resume(&mut carrier, context)
        .expect("atomic native hook removal and continuation");
    assert_eq!(actual.removed_edges.len(), 5);
    assert_eq!(
        actual.active_vertex_count_before - actual.active_vertex_count_after,
        1
    );
    assert_eq!(
        actual.active_edge_count_before - actual.active_edge_count_after,
        5
    );
    assert_eq!(actual.group_members_before.len(), 3);
    assert_eq!(actual.group_members_after.len(), 2);
    assert_eq!(carrier.latest_base_apply, carrier_before.latest_base_apply);
    assert_eq!(carrier.b_cells, carrier_before.b_cells);
    assert_eq!(carrier.s_cells, carrier_before.s_cells);
    assert_eq!(carrier.beam_inter_index, carrier_before.beam_inter_index);
    assert_eq!(
        carrier.bindings.beam_vertices.len() + 1,
        carrier_before.bindings.beam_vertices.len()
    );
    assert!(
        !carrier
            .bindings
            .beam_vertices
            .contains_key(&awaiting.competing_hook)
    );
    let NativeStemsBeamSchedulerStatus::SidesExhausted {
        final_local_worklist,
        ..
    } = &carrier.scheduler.status
    else {
        panic!("hook removal did not exhaust the remaining SIDES worklist");
    };
    assert_eq!(
        final_local_worklist,
        &before_snapshot.sources[..=before_snapshot.current_index]
    );
    assert_eq!(actual.resume.resume_events.len(), 54);
    assert_eq!(carrier.scheduler.prefix_events.len(), 143);
    assert!(matches!(
        actual.resume.resume_events.first(),
        Some(NativeStemsBeamSchedulerEvent::CompetingHookRemoved {
            event_ordinal: 89,
            ..
        })
    ));

    // Only now read mutation expectations. The scheduler predecessor above is
    // input authority; these rows cannot influence the native return.
    let expected_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-hook-removal-allegretto-system1.txt"),
    )
    .expect("expected-only hook removal fixture");
    assert_eq!(
        sha256_hex(expected_text.as_bytes()),
        "d4c5decf03eaab893c79b2cb7ebd0378f13ac019acc007a38718105c75eacc71"
    );
    let expected = expected_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(expected.len(), 5);
    let field = |line: &str, name: &str| {
        let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
        let position = tokens
            .iter()
            .position(|token| *token == name)
            .unwrap_or_else(|| panic!("missing {name}: {line}"));
        tokens[position + 1].to_owned()
    };
    let frontier = expected[0];
    let result = expected[1];
    assert_eq!(
        field(frontier, "beamSig").parse::<usize>().unwrap(),
        sig_of[&actual.beam]
    );
    assert_eq!(
        field(frontier, "hookSig").parse::<usize>().unwrap(),
        sig_of[&actual.competing_hook]
    );
    assert_eq!(field(frontier, "workIndex"), "19");
    assert_eq!(field(frontier, "sigVertices"), "202");
    assert_eq!(field(frontier, "sigEdges"), "232");
    assert_eq!(field(result, "sigVertices"), "201");
    assert_eq!(field(result, "sigEdges"), "227");
    assert_eq!(field(result, "hookVertexMatches"), "0");
    assert_eq!(field(result, "hookIncidentAfter"), "0");
    assert_eq!(field(result, "workUnchanged"), "true");
    assert_eq!(field(result, "linkedBUnchanged"), "true");
    assert_eq!(field(result, "terminal"), "RemovedCompetingHook");
    assert_eq!(field(expected[2], "event"), "110");
    assert_eq!(
        field(expected[2], "terminal"),
        "SidesWorklistExhaustedBeforeSecondFrontier"
    );
    assert_eq!(field(expected[3], "sidesExhausted"), "true");
    assert_eq!(
        field(expected[4], "schedulerPredecessorFixtureSha256"),
        sha256_hex(predecessor_text.as_bytes())
    );
    assert_eq!(field(expected[4], "freshRunsByteIdentical"), "true");
}

/// The first measured transaction now derives B16 from the owned SIG and
/// typed products.  Java rows are opened only after the complete native
/// graph/cell result exists.
#[test]
fn native_carrier_drives_full_sides_pass_before_oracle_read() {
    let base_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-vlink-base-apply-chula.txt"),
    )
    .expect("frozen B14 predecessor");
    let create_text =
        std::fs::read_to_string(repo_root().join("rust/oracle/stems-beam-create-stem-chula.txt"))
            .expect("frozen B12 predecessor");
    let reuse_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-vlink-reuse-check-chula.txt"),
    )
    .expect("frozen B13 predecessor");
    let hydrated =
        b15_hydration::run_real("chula.png", 1, &base_text, &create_text, &reuse_text, false)
            .expect("native predecessors through B15");
    let grid = recognize_grid_lines(repo_root().join("data/examples/chula.png"))
        .expect("GRID recognition");
    let headers = recognize_native_headers(&grid).expect("HEADERS recognition");
    let stem_seeds = recognize_native_stem_seeds(&grid, &headers).expect("STEM_SEEDS recognition");
    let beams = recognize_native_beams_with_stem_seeds(&grid, headers.beam_erases(), &stem_seeds)
        .expect("BEAMS recognition");
    let ledgers = recognize_native_ledgers(&grid, &beams).expect("LEDGERS recognition");
    let heads = recognize_native_heads(&grid, &headers, &stem_seeds, &beams, &ledgers)
        .expect("HEADS recognition");
    let assembled = assemble_native_sig(&grid, &headers, &beams, &ledgers, &heads)
        .expect("native SIG assembly");
    let mut sig = assembled
        .systems
        .iter()
        .find(|system| system.system_id == 1)
        .expect("system 1 SIG")
        .clone();
    let mut bindings = assembled
        .bindings
        .iter()
        .find(|bindings| bindings.system_id == 1)
        .expect("system 1 bindings")
        .clone();

    // Freeze the complete native catalogue before opening the disclosed
    // persistent-ID rows. Fixture topology can validate this catalogue, but
    // cannot select a source, vertex, or runtime class for it.
    let native_beam_catalogue = hydrated
        .stumps
        .beams_by_abscissa
        .iter()
        .map(|beam| {
            let vertex_id = bindings
                .beam_vertices
                .get(&beam.source)
                .copied()
                .expect("native beam binding");
            let vertex = sig.vertex(vertex_id.0).expect("live native beam vertex");
            assert!(matches!(
                vertex.kind,
                NativeSigInterKind::Beam
                    | NativeSigInterKind::BeamHook
                    | NativeSigInterKind::SmallBeam
            ));
            (
                beam.sig_ordinal,
                (beam.source, vertex_id.0, vertex.kind.java_class()),
            )
        })
        .collect::<BTreeMap<_, _>>();
    assert_eq!(native_beam_catalogue.len(), 48);
    assert_eq!(
        native_beam_catalogue.keys().copied().collect::<Vec<_>>(),
        (0..48).collect::<Vec<_>>()
    );
    assert_eq!(
        native_beam_catalogue
            .values()
            .filter(|(_, _, class)| *class == "BeamInter")
            .count(),
        31
    );
    assert_eq!(
        native_beam_catalogue
            .values()
            .filter(|(_, _, class)| *class == "BeamHookInter")
            .count(),
        17
    );

    let beam_index_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-inter-index-chula-system1.txt"),
    )
    .expect("disclosed sparse beam InterIndex authority");
    assert_eq!(beam_index_text.len(), BEAM_INTER_INDEX_BYTES);
    assert_eq!(beam_index_text.lines().count(), BEAM_INTER_INDEX_LINES);
    assert_eq!(
        sha256_hex(beam_index_text.as_bytes()),
        BEAM_INTER_INDEX_SHA256
    );
    assert!(
        beam_index_text
            .lines()
            .any(|line| line == "# schema: stems-beam-inter-index-v1")
    );
    let mut seen_fixture_ordinals = BTreeSet::new();
    let mut seen_inter_ids = BTreeSet::new();
    let mut seen_index_ordinals = BTreeSet::new();
    let mut beam_bootstrap = Vec::new();
    for line in beam_index_text
        .lines()
        .filter(|line| line.starts_with("stemsbeaminterindexentry "))
    {
        let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
        let [
            "stemsbeaminterindexentry",
            "chula.png#1",
            "system",
            "1",
            "beamSig",
            beam_sig,
            "vertex",
            fixture_vertex,
            "interId",
            inter_id,
            "indexOrdinal",
            index_ordinal,
            "vip",
            "false",
            "class",
            fixture_class,
        ] = tokens.as_slice()
        else {
            panic!("malformed beam InterIndex authority row: {line}");
        };
        let beam_sig = beam_sig.parse::<usize>().expect("beam SIG ordinal");
        let fixture_vertex = fixture_vertex.parse::<usize>().expect("beam vertex");
        let inter_id = inter_id.parse::<i32>().expect("beam Inter ID");
        let index_ordinal = index_ordinal.parse::<usize>().expect("beam index ordinal");
        assert!(seen_fixture_ordinals.insert(beam_sig));
        assert!(seen_inter_ids.insert(inter_id));
        assert!(seen_index_ordinals.insert(index_ordinal));
        let &(source, native_vertex, native_class) = native_beam_catalogue
            .get(&beam_sig)
            .expect("fixture beam exists in prior native catalogue");
        assert_eq!(fixture_vertex, native_vertex);
        assert_eq!(*fixture_class, native_class);
        assert_eq!(index_ordinal, 113 + beam_sig);
        if EXECUTED_BASE_BEAM_SIG_ORDINALS.contains(&beam_sig) {
            beam_bootstrap.push(NativeStemsBeamBeamInterIndexBootstrapEntry {
                source,
                inter_id,
                index_ordinal,
                vip: false,
            });
        }
    }
    assert_eq!(
        seen_fixture_ordinals.into_iter().collect::<Vec<_>>(),
        (0..48).collect::<Vec<_>>()
    );
    assert_eq!(seen_inter_ids.len(), 48);
    assert_eq!(seen_inter_ids.first(), Some(&905));
    assert_eq!(seen_inter_ids.last(), Some(&989));
    assert_eq!(
        seen_index_ordinals.into_iter().collect::<Vec<_>>(),
        (113..161).collect::<Vec<_>>()
    );
    assert_eq!(beam_bootstrap.len(), EXECUTED_BASE_BEAM_SIG_ORDINALS.len());
    assert_eq!(
        beam_bootstrap
            .iter()
            .map(|entry| {
                native_beam_catalogue
                    .iter()
                    .find_map(|(ordinal, (source, _, _))| {
                        (*source == entry.source).then_some(*ordinal)
                    })
                    .expect("sparse beam source in native catalogue")
            })
            .collect::<Vec<_>>(),
        EXECUTED_BASE_BEAM_SIG_ORDINALS
    );
    assert!(beam_index_text.lines().any(|line| {
        line == "stemsbeaminterindexsummary chula.png#1 system 1 interIndexEntries 639 beams 48 rowsSha256 1550d098eadde838a3b66ec0a3e12dfdbe121189829a1718a78e95295622038b"
    }));
    drop(beam_index_text);

    let mut base_state = hydrated.state_before.base_apply_state_before.clone();
    let native_base = apply_native_stems_beam_vlink_base_transaction_to_native_sig(
        &hydrated.scheduler,
        &hydrated.plans,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &hydrated.create_transaction,
        &hydrated.reuse_live_state,
        hydrated.relation_parameters,
        &hydrated.reuse_check,
        &mut base_state,
        &mut sig,
        &mut bindings,
    )
    .expect("native B14 commit");
    // The native graph deliberately uses one-based native vertex identities
    // where the legacy replay certificate retains Java EntityIndex IDs.  Join
    // the complete mutation result in the shared semantic domain instead of
    // pretending those identity domains are interchangeable.
    assert_eq!(native_base.key, hydrated.base_apply.key);
    assert_eq!(native_base.stem_before, hydrated.base_apply.stem_before);
    assert_eq!(native_base.stem_after, hydrated.base_apply.stem_after);
    assert_eq!(
        native_base.fresh_relation,
        hydrated.base_apply.fresh_relation
    );
    assert_eq!(
        native_base.graph_relation_identity,
        hydrated.base_apply.graph_relation_identity
    );
    assert_eq!(
        native_base.apply_disposition,
        hydrated.base_apply.apply_disposition
    );
    assert_eq!(native_base.callback, hydrated.base_apply.callback);
    assert_eq!(native_base.operations, hydrated.base_apply.operations);
    assert_eq!(native_base.outcome, hydrated.base_apply.outcome);
    assert_eq!((sig.vertices.len(), sig.edges.len()), (222, 203));

    let mut cells = initialize_native_stems_beam_b_linker_cells(&hydrated.reachability)
        .expect("complete native B-cell arena");
    let post_b14_sig = sig.clone();
    let pre_b15_cells = cells.clone();
    let actual = apply_native_stems_beam_vlink_sibling_transaction_to_native_sig(
        &mut sig,
        &bindings,
        &hydrated.scheduler,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &hydrated.reachability,
        &hydrated.builder,
        &native_base,
        &hydrated.transaction,
        &mut cells,
    )
    .expect("native B15+B16 carrier commit");

    assert_eq!((sig.vertices.len(), sig.edges.len()), (222, 205));
    assert!(!actual.base_linked_before);
    assert!(actual.base_linked_after);
    assert_eq!(
        actual.graph.appended_edges,
        vec![
            audiveris_omr::native_sig::NativeSigEdgeId(203),
            audiveris_omr::native_sig::NativeSigEdgeId(204)
        ]
    );
    assert_eq!(actual.siblings.len(), 2);
    assert!(
        actual
            .siblings
            .iter()
            .all(|step| step.branch == NativeStemsBeamSiblingBranch::Linked)
    );
    assert_eq!(actual.b_linker_write_count, 3);
    assert_eq!(actual.b_linker_value_change_count, 3);
    assert_eq!(
        sig.incident_edges(221)
            .expect("native stem incidents")
            .iter()
            .map(|edge| edge.ordinal)
            .collect::<Vec<_>>(),
        vec![202, 203, 204]
    );

    // Carry the same owned SIG and B-cell authority through B17 before any
    // Boundary-16 or Boundary-17 oracle row is opened.
    let mut s_cells = initialize_native_stems_beam_s_linker_cells(&hydrated.head_corners)
        .expect("complete native S-cell arena");
    let post_b16_sig = sig.clone();
    let pre_b17_s_cells = s_cells.clone();
    let head_actual = apply_native_stems_beam_vlink_head_transaction_to_native_sig(
        &mut sig,
        &bindings,
        &hydrated.scheduler,
        &hydrated.plans,
        &hydrated.builder,
        &hydrated.head_corners,
        &hydrated.reachability,
        &hydrated.transaction,
        &actual,
        &cells,
        &mut s_cells,
    )
    .expect("native B17 graph/S-cell carrier commit");
    assert_eq!((sig.vertices.len(), sig.edges.len()), (222, 207));
    assert_eq!(head_actual.plan_ordinal, 143);
    assert_eq!(head_actual.steps.len(), 2);
    assert!(
        head_actual
            .steps
            .iter()
            .all(|step| step.branch == NativeStemsBeamHeadLinkBranch::Linked)
    );
    assert_eq!(
        head_actual
            .steps
            .iter()
            .map(|step| (step.head_vertex.0, step.stem_vertex.0))
            .collect::<Vec<_>>(),
        vec![(119, 221), (120, 221)]
    );
    assert_eq!(
        head_actual
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![205, 206]
    );
    assert_eq!(head_actual.s_linker_write_count, 2);
    assert_eq!(head_actual.s_linker_value_change_count, 2);
    assert_eq!(head_actual.head_abnormal_value_change_count, 2);
    assert_eq!(head_actual.stem_abnormal_value_change_count, 1);
    assert_eq!(head_actual.dirty_cascade_assignment_count, 3);
    assert_eq!((head_actual.last_index, head_actual.max_index), (5, 5));
    assert!(!head_actual.remainder_less_than);
    assert!(head_actual.returned_true);
    assert_eq!(
        head_actual
            .steps
            .iter()
            .map(|step| step.consistency.expect("linked consistency").to_bits())
            .collect::<Vec<_>>(),
        vec![0x3ffc_9249_2492_4925; 2]
    );
    assert_eq!(
        head_actual.steps[0]
            .stem_incident_after
            .as_ref()
            .expect("first stem callback")
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![202, 203, 204, 205]
    );
    assert_eq!(
        head_actual.steps[1]
            .stem_incident_after
            .as_ref()
            .expect("second stem callback")
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![202, 203, 204, 205, 206]
    );
    assert!(!sig.vertices[119].abnormal);
    assert!(!sig.vertices[120].abnormal);
    assert!(!sig.vertices[221].abnormal);

    let outer_resume = apply_native_stems_beam_outer_and_resume_transaction(
        &hydrated.scheduler,
        &hydrated.vlinkers,
        &hydrated.builder,
        &hydrated.plans,
        &hydrated.reachability,
        &hydrated.transaction,
        &actual,
        &head_actual,
        &mut cells,
    )
    .expect("native B18/B19 carrier resume");
    assert!(outer_resume.outer.linked_before);
    assert!(outer_resume.outer.linked_after);
    assert_eq!(outer_resume.outer.linked_value_change_count, 0);
    let NativeStemsBeamSchedulerResumeStatus::AwaitingVLinkTransaction(second) =
        &outer_resume.resume.status
    else {
        panic!("native B19 did not reach the second frontier");
    };
    assert_eq!(second.plan.plan_ordinal, 152);
    assert_eq!(
        second.horizontal_side,
        Some(audiveris_omr::stems_step::NativeStemHeadSide::Right)
    );
    assert!(
        outer_resume
            .resume
            .advanced_system
            .linked_b_linkers
            .contains(&actual.base_b_linker)
    );
    assert!(actual.assigned_b_linkers.iter().all(|reference| {
        outer_resume
            .resume
            .advanced_system
            .linked_b_linkers
            .contains(reference)
    }));

    // Carry transaction 2 through a graph-derived B14 rollover before any
    // transaction-2 family fixture is opened. The page GlyphIndex bootstrap
    // is the one disclosed authority still outside native recognition.
    let second_scheduler = &outer_resume.resume.advanced_system;
    let NativeStemsBeamSchedulerResumeStatus::AwaitingVLinkTransaction(second) =
        &outer_resume.resume.status
    else {
        unreachable!()
    };
    let second_attempt = hydrated
        .plans
        .builders
        .iter()
        .flat_map(|builder| &builder.attempts)
        .nth(second.plan.plan_ordinal)
        .expect("second plan attempt");
    let registry_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-glyph-registry-chula.txt"),
    )
    .expect("disclosed page GlyphIndex bootstrap");
    let bootstrap = glyph_bootstrap_for_attempt(second_attempt, &registry_text);
    let mut second_transaction_state = native_base.state_after.transaction_state.clone();
    let authority = NativeStemsBeamSystemStemAuthorityProof::from_empty_stems_entry(
        &second_transaction_state.system_stems,
        0,
    )
    .expect("dense carried systemStems");
    prepare_native_stems_beam_vlink_frontier_state(
        second_scheduler,
        &hydrated.plans,
        &mut second_transaction_state,
        &bootstrap,
        authority,
    )
    .expect("native transaction-2 preparation");
    let second_create = apply_native_stems_beam_vlink_create_stem_transaction(
        second_scheduler,
        &hydrated.builder,
        &hydrated.plans,
        &mut second_transaction_state,
        &b15_hydration::checker_context_for_page(&b15_hydration::native_predecessor_page(
            "chula.png",
        )),
    )
    .expect("native transaction-2 B12");
    let second_live = project_native_stems_beam_vlink_reuse_live_state(
        &sig,
        &bindings,
        second_scheduler,
        &hydrated.plans,
        &s_cells,
        &second_transaction_state.system_stems,
    )
    .expect("native transaction-2 B13 live state");
    let second_reuse = evaluate_native_stems_beam_vlink_reuse_check(
        second_scheduler,
        &hydrated.plans,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &second_create,
        &second_transaction_state,
        &second_live,
        hydrated.relation_parameters,
    )
    .expect("native transaction-2 B13");
    let mut second_sig = sig.clone();
    let mut second_bindings = bindings.clone();
    let mut second_base_state = roll_native_stems_beam_vlink_base_apply_state(
        &native_base.state_after,
        &second_transaction_state,
        &second_reuse,
        &second_sig,
        &second_bindings,
        NativeStemsBeamVLinkBaseRolloverAuthority {
            stump_system: &hydrated.stumps,
            beam_inter_index: &beam_bootstrap,
            configured_inter_vip_ids: &[],
        },
    )
    .expect("native transaction-2 B14 rollover");
    assert_eq!(
        (
            second_base_state.inter_index.baseline_entry_count,
            second_base_state.sig.baseline_vertex_count,
            second_base_state.sig.baseline_relation_count,
        ),
        (640, 222, 207)
    );
    let second_base = apply_native_stems_beam_vlink_base_transaction_to_native_sig(
        second_scheduler,
        &hydrated.plans,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &second_create,
        &second_live,
        hydrated.relation_parameters,
        &second_reuse,
        &mut second_base_state,
        &mut second_sig,
        &mut second_bindings,
    )
    .expect("native transaction-2 B14 commit");
    assert_eq!(second_base.key.plan.plan_ordinal, 152);
    assert_eq!(
        (second_sig.vertices.len(), second_sig.edges.len()),
        (223, 208)
    );
    assert_eq!(second_bindings.stem_vertices[&1].0, 222);
    assert_eq!(second_base.graph_relation_identity, Some(207));
    assert_eq!(
        second_base.fresh_relation.grade.to_bits(),
        0x3fee_e2b2_e530_80f0
    );
    assert_eq!(
        second_base.fresh_relation.extension_point.x.to_bits(),
        0x4088_5975_1290_d133
    );
    assert_eq!(
        second_base.fresh_relation.extension_point.y.to_bits(),
        0x407c_b1ed_e751_1cca
    );

    let second_base_state_before = roll_native_stems_beam_vlink_base_apply_state(
        &native_base.state_after,
        &second_transaction_state,
        &second_reuse,
        &sig,
        &bindings,
        NativeStemsBeamVLinkBaseRolloverAuthority {
            stump_system: &hydrated.stumps,
            beam_inter_index: &beam_bootstrap,
            configured_inter_vip_ids: &[],
        },
    )
    .expect("repeatable transaction-2 B14 rollover input");
    let second_target = second.b_linker;
    let second_target_linked = cells
        .iter()
        .find(|cell| cell.reference == second_target)
        .expect("transaction-2 target B cell")
        .linked;
    assert!(!second_target_linked);
    let mut second_flag_state = NativeStemsBeamVLinkBLinkerFlagState {
        system_id: 1,
        base_apply_state_before: second_base_state_before,
        target_b_linker: second_target,
        linked: second_target_linked,
        committed: None,
    };
    let second_flag = apply_native_stems_beam_vlink_b_linker_flag_transaction(
        second_scheduler,
        &hydrated.plans,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &second_create,
        &second_live,
        hydrated.relation_parameters,
        &second_reuse,
        &second_base,
        &mut second_flag_state,
    )
    .expect("native transaction-2 B15");
    let mut second_cells = cells.clone();
    let second_siblings = apply_native_stems_beam_vlink_sibling_transaction_to_native_sig(
        &mut second_sig,
        &second_bindings,
        second_scheduler,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &hydrated.reachability,
        &hydrated.builder,
        &second_base,
        &second_flag,
        &mut second_cells,
    )
    .expect("native transaction-2 B16");
    assert_eq!(
        second_siblings
            .graph
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![208, 209]
    );
    let mut second_s_cells = s_cells.clone();
    let second_heads = apply_native_stems_beam_vlink_head_transaction_to_native_sig(
        &mut second_sig,
        &second_bindings,
        second_scheduler,
        &hydrated.plans,
        &hydrated.builder,
        &hydrated.head_corners,
        &hydrated.reachability,
        &second_flag,
        &second_siblings,
        &second_cells,
        &mut second_s_cells,
    )
    .expect("native transaction-2 B17");
    assert_eq!(
        (second_sig.vertices.len(), second_sig.edges.len()),
        (223, 212)
    );
    assert_eq!(
        second_heads
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![210, 211]
    );
    assert_eq!(
        second_heads
            .steps
            .iter()
            .map(|step| (step.head_vertex.0, step.stem_vertex.0))
            .collect::<Vec<_>>(),
        vec![(130, 222), (131, 222)]
    );
    assert_eq!(second_heads.s_linker_write_count, 2);
    assert_eq!(second_heads.s_linker_value_change_count, 2);
    assert_eq!((second_heads.last_index, second_heads.max_index), (4, 4));
    assert!(second_heads.returned_true);
    let second_sig_ordinals = hydrated
        .stumps
        .beams_by_abscissa
        .iter()
        .map(|beam| (beam.source, beam.sig_ordinal))
        .collect::<BTreeMap<_, _>>();
    let second_b_alias = |reference: NativeStemsBeamBLinkerRef| {
        format!(
            "beam:{}:b:{}",
            second_sig_ordinals[&reference.beam],
            reference.id - 1
        )
    };
    assert_eq!(second_b_alias(second_siblings.base_b_linker), "beam:12:b:2");
    assert_eq!(
        second_siblings
            .assigned_b_linkers
            .iter()
            .copied()
            .map(second_b_alias)
            .collect::<Vec<_>>(),
        vec!["beam:2:b:0", "beam:3:b:0"]
    );
    let second_outer_resume = apply_native_stems_beam_outer_and_resume_transaction(
        second_scheduler,
        &hydrated.vlinkers,
        &hydrated.builder,
        &hydrated.plans,
        &hydrated.reachability,
        &second_flag,
        &second_siblings,
        &second_heads,
        &mut second_cells,
    )
    .expect("native transaction-2 B18/B19");
    let NativeStemsBeamSchedulerResumeStatus::AwaitingVLinkTransaction(third) =
        &second_outer_resume.resume.status
    else {
        panic!("transaction 2 did not reach a third frontier");
    };
    assert_eq!(third.plan.plan_ordinal, 618);
    let third_alias = second_b_alias(third.b_linker);
    assert_eq!(third_alias, "beam:22:b:0");
    assert_eq!(third.vertical_side, NativeStemVerticalSide::Top);

    // Transaction 3 is the first changed-base-beam and compound-glyph case.
    // Consume the disclosed first-STEMS page snapshot once, join its persistent
    // identities to the system-1-visible native registry prefix, and drop the
    // raw rows before any carried transaction runs.
    let third_scheduler = &second_outer_resume.resume.advanced_system;
    let checker_page = b15_hydration::native_predecessor_page("chula.png");
    let visible_modeled_count = checker_page.first_system_visible_modeled_count;
    let bridge = first_stems_glyph_bridge(
        &registry_text,
        &checker_page.modeled_canonical_glyphs,
        visible_modeled_count,
    );
    assert!(hydrated.plans.builders.iter().all(|builder| {
        builder
            .attempts
            .iter()
            .flat_map(|attempt| &attempt.glyphs)
            .all(|glyph| glyph.modeled_canonical_ordinal < visible_modeled_count)
    }));
    drop(registry_text);

    // Retain an independently prepared transaction-3 state for the beam-index
    // authority negative below. It uses the same bridge, not a frontier row.
    let mut third_transaction_state = second_base.state_after.transaction_state.clone();
    let third_authority = NativeStemsBeamSystemStemAuthorityProof::from_empty_stems_entry(
        &third_transaction_state.system_stems,
        0,
    )
    .expect("transaction-3 dense carried systemStems");
    prepare_native_stems_beam_vlink_frontier_state_from_first_stems_bridge(
        third_scheduler,
        &hydrated.plans,
        &mut third_transaction_state,
        &bridge,
        third_authority,
    )
    .expect("native transaction-3 bridge preparation");
    let third_candidate = materialize_native_stems_beam_frontier_candidate(
        third_scheduler,
        &hydrated.plans,
        &third_transaction_state,
    )
    .expect("native transaction-3 compound candidate");
    assert!(
        third_transaction_state
            .selected_glyph_bindings
            .iter()
            .any(|selected| {
                selected.content == third_candidate
                    && selected.glyph_id == 298
                    && selected.canonical_alias == 298
            })
    );
    third_transaction_state.glyph_index.exhaustive_lookup = None;

    // The production carrier owns the same transaction as one clone-and-swap
    // operation. All plan-specific glyph selection now comes from the bridge.
    let checker = b15_hydration::checker_context_for_page(&checker_page);
    let carrier_before = NativeStemsBeamSidesCarrier {
        scheduler: (**third_scheduler).clone(),
        latest_base_apply: (*second_base.state_after).clone(),
        sig: second_sig.clone(),
        bindings: second_bindings.clone(),
        b_cells: second_cells.clone(),
        s_cells: second_s_cells.clone(),
        beam_inter_index: beam_bootstrap.clone(),
        configured_inter_vip_ids: Vec::new(),
    };
    let context = NativeStemsBeamSidesContext {
        plans: &hydrated.plans,
        builders: &hydrated.builder,
        stumps: &hydrated.stumps,
        vlinkers: &hydrated.vlinkers,
        reachability: &hydrated.reachability,
        head_corners: &hydrated.head_corners,
        checker: &checker,
        relation_parameters: hydrated.relation_parameters,
    };
    let mut corrupt_bridge_carrier = carrier_before.clone();
    corrupt_bridge_carrier
        .latest_base_apply
        .transaction_state
        .glyph_index
        .union_size -= 1;
    let corrupt_bridge_before = corrupt_bridge_carrier.clone();
    let bridge_before = bridge.clone();
    advance_native_stems_beam_sides_transaction_from_first_stems_bridge(
        &mut corrupt_bridge_carrier,
        context,
        &bridge,
    )
    .expect_err("carried GlyphIndex union mismatch must reject the bridge transaction");
    assert_eq!(corrupt_bridge_carrier, corrupt_bridge_before);
    assert_eq!(bridge, bridge_before);

    // Beam 19 is not selected until transaction 31. A sparse authority can
    // therefore carry the first 30 transactions, but the first missing-ID
    // lookup must reject atomically rather than borrowing another beam's ID.
    let late_source = native_beam_catalogue[&19].0;
    let mut missing_late_carrier = carrier_before.clone();
    missing_late_carrier
        .beam_inter_index
        .retain(|entry| entry.source != late_source);
    assert_eq!(missing_late_carrier.beam_inter_index.len(), 15);
    for transaction_ordinal in 3..31 {
        advance_native_stems_beam_sides_transaction_from_first_stems_bridge(
            &mut missing_late_carrier,
            context,
            &bridge,
        )
        .unwrap_or_else(|error| {
            panic!("sparse authority stopped before transaction {transaction_ordinal}: {error}")
        });
    }
    let audiveris_omr::native_stems_beam_scheduler::NativeStemsBeamSchedulerStatus::AwaitingVLinkTransaction(
        late_frontier,
    ) = &missing_late_carrier.scheduler.status
    else {
        panic!("sparse authority did not reach the late missing beam");
    };
    assert_eq!(late_frontier.b_linker.beam, late_source);
    let missing_late_before = missing_late_carrier.clone();
    let bridge_before = bridge.clone();
    advance_native_stems_beam_sides_transaction_from_first_stems_bridge(
        &mut missing_late_carrier,
        context,
        &bridge,
    )
    .expect_err("late selected beam without persistent identity must reject");
    assert_eq!(missing_late_carrier, missing_late_before);
    assert_eq!(bridge, bridge_before);

    let mut carrier = carrier_before.clone();
    let carried_third = advance_native_stems_beam_sides_transaction_from_first_stems_bridge(
        &mut carrier,
        context,
        &bridge,
    )
    .expect("atomic native transaction-3 bridge carrier");
    assert_eq!(
        (carrier.sig.vertices.len(), carrier.sig.edges.len()),
        (224, 216)
    );
    assert_eq!(carrier.bindings.stem_vertices[&2].0, 223);
    assert_eq!(carried_third.base.graph_relation_identity, Some(212));
    assert_eq!(
        carried_third
            .siblings
            .graph
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![213]
    );
    assert_eq!(
        carried_third
            .heads
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![214, 215]
    );
    assert_eq!(
        carrier
            .latest_base_apply
            .transaction_state
            .system_stems
            .known_stems
            .len(),
        3
    );
    assert_eq!(carried_third.create.registration.glyph_id, 298);
    assert_eq!(
        carried_third.create.registration.action,
        NativeStemsBeamGlyphRegistrationAction::Reused {
            reinserted_into_active_index: false
        }
    );
    assert_eq!(
        carried_third.create.disposition,
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity: 2 }
    );
    assert_eq!(
        carrier
            .latest_base_apply
            .transaction_state
            .glyph_index
            .union_size,
        1650
    );
    assert_eq!(
        carrier.latest_base_apply.inter_index.baseline_entry_count,
        641
    );
    assert_eq!(
        carrier.latest_base_apply.inter_index.appended_entries.len(),
        1
    );
    assert_eq!(carrier.b_cells.iter().filter(|cell| cell.linked).count(), 8);
    assert_eq!(carrier.s_cells.iter().filter(|cell| cell.linked).count(), 6);
    assert!(
        carrier
            .latest_base_apply
            .transaction_state
            .system_stems
            .known_stems
            .iter()
            .all(|stem| stem.sig_attached
                && stem.abnormal
                    == carrier.sig.vertices[carrier.bindings.stem_vertices[&stem.stem_identity].0]
                        .abnormal)
    );
    let NativeStemsBeamSchedulerResumeStatus::AwaitingVLinkTransaction(carried_fourth) =
        &carried_third.outer_resume.resume.status
    else {
        panic!("atomic transaction 3 did not reach the fourth frontier");
    };
    assert_eq!(carried_fourth.plan.plan_ordinal, 627);

    // Invoke the same production bridge carrier again from committed state.
    let carried_fourth = advance_native_stems_beam_sides_transaction_from_first_stems_bridge(
        &mut carrier,
        context,
        &bridge,
    )
    .expect("second atomic bridge carrier invocation for transaction 4");
    assert_eq!(carried_fourth.flag.key.plan.plan_ordinal, 627);
    assert_eq!(
        (carrier.sig.vertices.len(), carrier.sig.edges.len()),
        (225, 221)
    );
    assert_eq!(carrier.bindings.stem_vertices[&3].0, 224);
    assert_eq!(carried_fourth.base.graph_relation_identity, Some(216));
    assert_eq!(
        carried_fourth
            .siblings
            .graph
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![217, 218]
    );
    assert_eq!(
        carried_fourth
            .heads
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![219, 220]
    );
    assert_eq!(
        carrier.b_cells.iter().filter(|cell| cell.linked).count(),
        11
    );
    assert_eq!(carrier.s_cells.iter().filter(|cell| cell.linked).count(), 8);
    let NativeStemsBeamSchedulerResumeStatus::AwaitingVLinkTransaction(carried_fifth) =
        &carried_fourth.outer_resume.resume.status
    else {
        panic!("second atomic carrier invocation did not reach transaction 5");
    };
    assert_eq!(carried_fifth.plan.plan_ordinal, 400);
    assert_eq!(second_b_alias(carried_fifth.b_linker), "beam:16:b:0");
    assert_eq!(carried_fifth.vertical_side, NativeStemVerticalSide::Top);

    let mut repeated = vec![carried_third.clone(), carried_fourth.clone()];
    for transaction_ordinal in 5..=32 {
        let frontier = match &carrier.scheduler.status {
            audiveris_omr::native_stems_beam_scheduler::NativeStemsBeamSchedulerStatus::AwaitingVLinkTransaction(
                frontier,
            ) => frontier.clone(),
            status => panic!(
                "carrier stopped before transaction {transaction_ordinal}: {status:?}"
            ),
        };
        let transaction = advance_native_stems_beam_sides_transaction_from_first_stems_bridge(
            &mut carrier,
            context,
            &bridge,
        )
        .unwrap_or_else(|error| {
            panic!(
                "transaction {transaction_ordinal} plan {} failed: {error}",
                frontier.plan.plan_ordinal
            )
        });
        repeated.push(transaction);
    }
    assert_eq!(repeated.len(), 30);
    let audiveris_omr::native_stems_beam_scheduler::NativeStemsBeamSchedulerStatus::SidesExhausted {
        retained_for_stumps,
        final_local_worklist,
    } = &carrier.scheduler.status
    else {
        panic!("32 native carrier transactions did not exhaust SIDES");
    };
    assert_eq!(retained_for_stumps, final_local_worklist);
    assert_eq!(
        (carrier.sig.vertices.len(), carrier.sig.edges.len()),
        (253, 331)
    );
    assert_eq!(carrier.bindings.stem_vertices.len(), 32);
    assert_eq!(
        carrier.b_cells.iter().filter(|cell| cell.linked).count(),
        61
    );
    assert_eq!(
        carrier.s_cells.iter().filter(|cell| cell.linked).count(),
        68
    );
    assert!(carrier.b_cells.iter().all(|cell| !cell.closed));
    assert!(carrier.s_cells.iter().all(|cell| !cell.closed));

    // Leave the authenticated SIDES terminal through the production STUMPS
    // continuation before opening the expected prefix. The scheduler's linked
    // set must be a bijective view of the 61 persistent true B cells.
    let sides_terminal_carrier = carrier.clone();
    let mut corrupt_linked_b = sides_terminal_carrier.clone();
    corrupt_linked_b
        .b_cells
        .iter_mut()
        .find(|cell| cell.linked)
        .expect("linked B cell at SIDES terminal")
        .linked = false;
    let corrupt_before = corrupt_linked_b.clone();
    continue_native_stems_beam_sides_carrier_into_stumps(&mut corrupt_linked_b, context)
        .expect_err("linked-B scheduler/cell disagreement must reject atomically");
    assert_eq!(corrupt_linked_b, corrupt_before);

    let stumps_actual = continue_native_stems_beam_sides_carrier_into_stumps(&mut carrier, context)
        .expect("native post-SIDES STUMPS prefix");
    assert_eq!(stumps_actual.stump_events.len(), 2);
    let NativeStemsBeamSchedulerEvent::BeamPassStart {
        snapshot,
        selected_stem_profile,
        ..
    } = &stumps_actual.stump_events[0]
    else {
        panic!("STUMPS prefix must begin with the retained beam");
    };
    assert_eq!(snapshot.pass, NativeStemsBeamSchedulerPass::Stumps);
    assert_eq!(snapshot.current_index, 0);
    assert_eq!(*selected_stem_profile, 3);
    let NativeStemsBeamSchedulerEvent::StumpSkippedStructuralSideGlyph {
        beam,
        b_linker,
        v_linker,
        ..
    } = stumps_actual.stump_events[1]
    else {
        panic!("first stump must take structural-side precedence");
    };
    assert_eq!(beam, snapshot.current);
    assert_eq!(second_b_alias(b_linker), "beam:12:b:0");
    assert_eq!(v_linker.side, NativeStemVerticalSide::Top);
    assert!(
        sides_terminal_carrier
            .scheduler
            .linked_b_linkers
            .contains(&b_linker),
        "the structural-side branch must win while the same B cell is linked"
    );
    assert!(
        sides_terminal_carrier
            .b_cells
            .iter()
            .any(|cell| cell.reference == b_linker && cell.linked),
        "the persistent B-cell authority must agree that the structural stump is linked"
    );
    let NativeStemsBeamSchedulerStumpsStatus::AwaitingVLinkTransaction(stump_frontier) =
        &stumps_actual.status
    else {
        panic!("measured STUMPS prefix must stop at a V-link transaction");
    };
    assert_eq!(
        stump_frontier.snapshot.pass,
        NativeStemsBeamSchedulerPass::Stumps
    );
    assert_eq!(stump_frontier.snapshot.current_index, 0);
    assert_eq!(stump_frontier.beam, snapshot.current);
    assert_eq!(second_b_alias(stump_frontier.b_linker), "beam:12:b:1");
    assert_eq!(stump_frontier.horizontal_side, None);
    assert_eq!(stump_frontier.vertical_side, NativeStemVerticalSide::Top);
    assert_eq!(stump_frontier.plan.plan_ordinal, 147);
    assert_eq!(stump_frontier.plan.stem_profile, 3);
    let stump_attempt = hydrated
        .plans
        .builders
        .iter()
        .flat_map(|builder| builder.attempts.iter())
        .nth(stump_frontier.plan.plan_ordinal)
        .expect("native STUMPS plan 147");
    assert_eq!(stump_attempt.stem_profile, 3);
    assert_eq!(stump_attempt.link_profile, 1);
    assert_eq!(stump_attempt.head_target_count, 2);
    assert_eq!(stump_attempt.expand_last_index, Some(2));
    assert_eq!(stump_attempt.relations.len(), 2);
    assert_eq!(stump_attempt.glyphs.len(), 1);
    assert!(!stump_attempt.stored_theoretical_line_would_mutate);
    assert_eq!(carrier.scheduler, *stumps_actual.advanced_system);
    assert_eq!(
        carrier.latest_base_apply,
        sides_terminal_carrier.latest_base_apply
    );
    assert_eq!(carrier.sig, sides_terminal_carrier.sig);
    assert_eq!(carrier.bindings, sides_terminal_carrier.bindings);
    assert_eq!(carrier.b_cells, sides_terminal_carrier.b_cells);
    assert_eq!(carrier.s_cells, sides_terminal_carrier.s_cells);
    assert_eq!(
        carrier.beam_inter_index,
        sides_terminal_carrier.beam_inter_index
    );

    // Execute the first STUMPS frontier and resume the same retained worklist
    // before either the prefix or transaction expected rows are opened.
    let awaiting_stump = carrier.clone();
    let mut missing_stump_b = awaiting_stump.clone();
    missing_stump_b
        .b_cells
        .retain(|cell| cell.reference != stump_frontier.b_linker);
    let missing_stump_b_before = missing_stump_b.clone();
    let bridge_before = bridge.clone();
    advance_native_stems_beam_stumps_transaction_from_first_stems_bridge(
        &mut missing_stump_b,
        context,
        &bridge,
    )
    .expect_err("missing first-STUMPS B cell must reject atomically");
    assert_eq!(missing_stump_b, missing_stump_b_before);
    assert_eq!(bridge, bridge_before);

    let first_stump = advance_native_stems_beam_stumps_transaction_from_first_stems_bridge(
        &mut carrier,
        context,
        &bridge,
    )
    .expect("native first STUMPS transaction and resume");
    assert_eq!(first_stump.flag.key.plan.plan_ordinal, 147);
    assert_eq!(first_stump.create.registration.glyph_id, 310);
    assert_eq!(
        first_stump.create.registration.action,
        NativeStemsBeamGlyphRegistrationAction::Reused {
            reinserted_into_active_index: false
        }
    );
    assert_eq!(
        first_stump.create.disposition,
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity: 32 }
    );
    assert_eq!(
        first_stump.reuse.reuse_disposition,
        NativeStemsBeamReuseDisposition::AllUnlinked
    );
    assert_eq!(first_stump.reuse.reuse_trace.len(), 2);
    assert_eq!(first_stump.base.graph_relation_identity, Some(331));
    assert!(first_stump.base.apply_returned);
    assert!(first_stump.flag.linked_after);
    assert!(first_stump.siblings.assigned_b_linkers.is_empty());
    assert_eq!(first_stump.heads.appended_edges.len(), 2);
    assert_eq!(
        (carrier.sig.vertices.len(), carrier.sig.edges.len()),
        (254, 334)
    );
    assert_eq!(carrier.bindings.stem_vertices.len(), 33);
    assert_eq!(
        carrier.b_cells.iter().filter(|cell| cell.linked).count(),
        62
    );
    assert_eq!(
        carrier.s_cells.iter().filter(|cell| cell.linked).count(),
        70
    );
    let NativeStemsBeamSchedulerStumpsStatus::AwaitingVLinkTransaction(next_stump) =
        &first_stump.resume.status
    else {
        panic!("first STUMPS transaction must resume at a second frontier");
    };
    assert_eq!(
        next_stump.snapshot.pass,
        NativeStemsBeamSchedulerPass::Stumps
    );
    assert_eq!(next_stump.snapshot.current_index, 1);
    assert_eq!(second_b_alias(next_stump.b_linker), "beam:22:b:1");
    assert_eq!(next_stump.vertical_side, NativeStemVerticalSide::Top);
    assert_eq!(next_stump.plan.plan_ordinal, 622);
    assert_eq!(carrier.scheduler, *first_stump.resume.advanced_system);
    assert_eq!(bridge, bridge_before);

    let mut missing_second_stump_b = carrier.clone();
    missing_second_stump_b
        .b_cells
        .retain(|cell| cell.reference != next_stump.b_linker);
    let missing_second_stump_b_before = missing_second_stump_b.clone();
    advance_native_stems_beam_stumps_transaction_from_first_stems_bridge(
        &mut missing_second_stump_b,
        context,
        &bridge,
    )
    .expect_err("missing second-STUMPS B cell must reject atomically");
    assert_eq!(missing_second_stump_b, missing_second_stump_b_before);
    assert_eq!(bridge, bridge_before);

    let second_stump = advance_native_stems_beam_stumps_transaction_from_first_stems_bridge(
        &mut carrier,
        context,
        &bridge,
    )
    .expect("native second STUMPS transaction and resume");
    assert_eq!(second_stump.flag.key.plan.plan_ordinal, 622);
    assert_eq!(second_stump.create.registration.glyph_id, 321);
    assert_eq!(
        second_stump.create.registration.action,
        NativeStemsBeamGlyphRegistrationAction::Reused {
            reinserted_into_active_index: false
        }
    );
    assert_eq!(
        second_stump.create.disposition,
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity: 33 }
    );
    assert_eq!(
        second_stump.reuse.reuse_disposition,
        NativeStemsBeamReuseDisposition::AllUnlinked
    );
    assert_eq!(second_stump.reuse.reuse_trace.len(), 2);
    assert_eq!(second_stump.base.graph_relation_identity, Some(334));
    assert!(second_stump.base.apply_returned);
    assert!(second_stump.flag.linked_after);
    assert!(second_stump.siblings.assigned_b_linkers.is_empty());
    assert_eq!(second_stump.heads.appended_edges.len(), 2);
    assert_eq!(
        (carrier.sig.vertices.len(), carrier.sig.edges.len()),
        (255, 337)
    );
    assert_eq!(carrier.bindings.stem_vertices.len(), 34);
    assert_eq!(
        carrier.b_cells.iter().filter(|cell| cell.linked).count(),
        63
    );
    assert_eq!(
        carrier.s_cells.iter().filter(|cell| cell.linked).count(),
        72
    );
    let NativeStemsBeamSchedulerStumpsStatus::AwaitingVLinkTransaction(third_stump) =
        &second_stump.resume.status
    else {
        panic!("second STUMPS transaction must resume at a third frontier");
    };
    assert_eq!(
        third_stump.snapshot.pass,
        NativeStemsBeamSchedulerPass::Stumps
    );
    assert_eq!(third_stump.snapshot.current_index, 2);
    assert_eq!(second_b_alias(third_stump.b_linker), "beam:16:b:1");
    assert_eq!(third_stump.horizontal_side, None);
    assert_eq!(third_stump.vertical_side, NativeStemVerticalSide::Top);
    assert_eq!(third_stump.plan.plan_ordinal, 404);
    assert_eq!(third_stump.plan.stem_profile, 3);
    let third_stump_attempt = hydrated
        .plans
        .builders
        .iter()
        .flat_map(|builder| builder.attempts.iter())
        .nth(third_stump.plan.plan_ordinal)
        .expect("native STUMPS plan 404");
    assert_eq!(third_stump_attempt.glyphs.len(), 2);
    assert_eq!(carrier.scheduler, *second_stump.resume.advanced_system);
    assert_eq!(bridge, bridge_before);

    let mut missing_third_stump_b = carrier.clone();
    missing_third_stump_b
        .b_cells
        .retain(|cell| cell.reference != third_stump.b_linker);
    let missing_third_stump_b_before = missing_third_stump_b.clone();
    advance_native_stems_beam_stumps_transaction_from_first_stems_bridge(
        &mut missing_third_stump_b,
        context,
        &bridge,
    )
    .expect_err("missing third-STUMPS B cell must reject atomically");
    assert_eq!(missing_third_stump_b, missing_third_stump_b_before);
    assert_eq!(bridge, bridge_before);

    let third_stump_transaction =
        advance_native_stems_beam_stumps_transaction_from_first_stems_bridge(
            &mut carrier,
            context,
            &bridge,
        )
        .expect("native third STUMPS transaction and resume");
    assert_eq!(third_stump_transaction.flag.key.plan.plan_ordinal, 404);
    assert_eq!(third_stump_transaction.create.registration.glyph_id, 303);
    assert_eq!(
        third_stump_transaction.create.registration.action,
        NativeStemsBeamGlyphRegistrationAction::Reused {
            reinserted_into_active_index: false
        }
    );
    assert_eq!(
        third_stump_transaction.create.disposition,
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity: 34 }
    );
    assert_eq!(
        third_stump_transaction.reuse.reuse_disposition,
        NativeStemsBeamReuseDisposition::AllUnlinked
    );
    assert_eq!(third_stump_transaction.reuse.reuse_trace.len(), 2);
    assert_eq!(
        third_stump_transaction.base.graph_relation_identity,
        Some(337)
    );
    assert!(third_stump_transaction.base.apply_returned);
    assert!(third_stump_transaction.flag.linked_after);
    assert!(
        third_stump_transaction
            .siblings
            .assigned_b_linkers
            .is_empty()
    );
    assert_eq!(third_stump_transaction.heads.appended_edges.len(), 2);
    assert_eq!(
        (carrier.sig.vertices.len(), carrier.sig.edges.len()),
        (256, 340)
    );
    assert_eq!(carrier.bindings.stem_vertices.len(), 35);
    assert_eq!(
        carrier.b_cells.iter().filter(|cell| cell.linked).count(),
        64
    );
    assert_eq!(
        carrier.s_cells.iter().filter(|cell| cell.linked).count(),
        74
    );
    let NativeStemsBeamSchedulerStumpsStatus::AwaitingVLinkTransaction(fourth_stump) =
        &third_stump_transaction.resume.status
    else {
        panic!("third STUMPS transaction must resume at a fourth frontier");
    };
    assert_eq!(
        fourth_stump.snapshot.pass,
        NativeStemsBeamSchedulerPass::Stumps
    );
    assert_eq!(fourth_stump.snapshot.current_index, 3);
    assert_eq!(second_b_alias(fourth_stump.b_linker), "beam:28:b:1");
    assert_eq!(fourth_stump.horizontal_side, None);
    assert_eq!(fourth_stump.vertical_side, NativeStemVerticalSide::Top);
    assert_eq!(fourth_stump.plan.plan_ordinal, 508);
    assert_eq!(fourth_stump.plan.stem_profile, 3);
    assert_eq!(
        carrier.scheduler,
        *third_stump_transaction.resume.advanced_system
    );
    assert_eq!(bridge, bridge_before);

    let mut zero_limit_carrier = carrier.clone();
    let zero_limit_before = zero_limit_carrier.clone();
    drive_native_stems_beam_stumps_from_first_stems_bridge(
        &mut zero_limit_carrier,
        context,
        &bridge,
        0,
    )
    .expect_err("zero STUMPS drive limit must reject atomically");
    assert_eq!(zero_limit_carrier, zero_limit_before);

    let mut late_missing_b_carrier = carrier.clone();
    late_missing_b_carrier
        .b_cells
        .retain(|cell| second_b_alias(cell.reference) != "beam:32:b:1");
    let late_missing_b_before = late_missing_b_carrier.clone();
    drive_native_stems_beam_stumps_from_first_stems_bridge(
        &mut late_missing_b_carrier,
        context,
        &bridge,
        100,
    )
    .expect_err("late missing STUMPS B cell must roll back earlier shadow transactions");
    assert_eq!(late_missing_b_carrier, late_missing_b_before);

    let mut bounded_stumps_carrier = carrier.clone();
    let bounded_stumps = drive_native_stems_beam_stumps_from_first_stems_bridge(
        &mut bounded_stumps_carrier,
        context,
        &bridge,
        1,
    )
    .expect("one-transaction STUMPS drive");
    assert_eq!(bounded_stumps.transactions.len(), 1);
    assert_eq!(
        bounded_stumps.transactions[0].flag.key.plan.plan_ordinal,
        508
    );
    let NativeStemsBeamSchedulerStumpsStatus::AwaitingVLinkTransaction(bounded_next) =
        &bounded_stumps.status
    else {
        panic!("bounded STUMPS drive must stop at its next frontier");
    };
    assert_eq!(bounded_next.plan.plan_ordinal, 28);
    assert_eq!(bounded_next.snapshot.current_index, 4);
    assert_eq!(second_b_alias(bounded_next.b_linker), "beam:29:b:1");
    assert_eq!(
        (
            bounded_stumps_carrier.sig.vertices.len(),
            bounded_stumps_carrier.sig.edges.len()
        ),
        (257, 343)
    );

    let complete_stumps =
        drive_native_stems_beam_stumps_from_first_stems_bridge(&mut carrier, context, &bridge, 100)
            .expect("native STUMPS suffix through completion");
    assert_eq!(complete_stumps.transactions.len(), 4);
    assert_eq!(
        complete_stumps
            .transactions
            .iter()
            .map(|transaction| transaction.flag.key.plan.plan_ordinal)
            .collect::<Vec<_>>(),
        [508, 28, 330, 251]
    );
    assert_eq!(
        complete_stumps
            .transactions
            .iter()
            .map(|transaction| transaction.create.registration.glyph_id)
            .collect::<Vec<_>>(),
        [308, 305, 302, 300]
    );
    assert!(complete_stumps.transactions.iter().all(|transaction| {
        transaction.create.registration.action
            == NativeStemsBeamGlyphRegistrationAction::Reused {
                reinserted_into_active_index: false,
            }
    }));
    assert_eq!(
        complete_stumps
            .transactions
            .iter()
            .map(|transaction| transaction.create.disposition)
            .collect::<Vec<_>>(),
        (35..=38)
            .map(
                |stem_identity| NativeStemsBeamCreateStemDisposition::CreatedChecked {
                    stem_identity,
                }
            )
            .collect::<Vec<_>>()
    );
    assert!(complete_stumps.transactions.iter().all(|transaction| {
        transaction.reuse.reuse_disposition == NativeStemsBeamReuseDisposition::AllUnlinked
            && transaction.flag.linked_after
            && transaction.siblings.assigned_b_linkers.is_empty()
    }));
    assert_eq!(
        complete_stumps
            .transactions
            .iter()
            .map(|transaction| transaction.base.graph_relation_identity)
            .collect::<Vec<_>>(),
        [Some(340), Some(343), Some(346), Some(350)]
    );
    assert_eq!(
        complete_stumps
            .transactions
            .iter()
            .map(|transaction| transaction.heads.appended_edges.len())
            .collect::<Vec<_>>(),
        [2, 2, 3, 2]
    );
    assert!(matches!(
        complete_stumps.status,
        NativeStemsBeamSchedulerStumpsStatus::Completed { .. }
    ));
    assert_eq!(
        (carrier.sig.vertices.len(), carrier.sig.edges.len()),
        (260, 353)
    );
    assert_eq!(carrier.bindings.stem_vertices.len(), 39);
    assert_eq!(
        carrier.b_cells.iter().filter(|cell| cell.linked).count(),
        68
    );
    assert_eq!(
        carrier.s_cells.iter().filter(|cell| cell.linked).count(),
        83
    );
    assert_eq!(bridge, bridge_before);

    // Transfer the complete native-owned post-STUMPS state into the first
    // head-phase decision before opening any head-phase expected row.
    let post_stumps_before = carrier.clone();
    let mut head_phase = begin_native_stems_head_linking_phase1(
        &carrier,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native post-STUMPS head-phase frontier");
    assert_eq!(carrier, post_stumps_before);
    assert_eq!(head_phase.beam_state, carrier);
    assert_eq!(head_phase.heads.len(), 102);
    assert_eq!(head_phase.current_index, 0);
    assert!(head_phase.unlinked_heads.is_empty());
    assert!(head_phase.undefined_sides.is_empty());
    assert_eq!(head_phase.frontier.head, head_phase.heads[0].reference);
    assert_eq!(head_phase.frontier.stem_profile, 0);
    assert_eq!(head_phase.frontier.link_profile, 1);
    assert!(!head_phase.frontier.append);
    assert_eq!(head_phase.frontier.side_decisions.len(), 2);
    assert_eq!(
        head_phase
            .frontier
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.linked_before,
                    decision.closed_before,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![
            (
                NativeStemHeadSide::Left,
                false,
                false,
                Some(false),
                Some(false)
            ),
            (
                NativeStemHeadSide::Right,
                false,
                false,
                Some(true),
                Some(false)
            ),
        ]
    );

    // Execute the selected C linker atomically before opening any head-phase
    // expected row. A late glyph-authority corruption must reject without
    // leaking any graph, registry, allocator, or shared-cell mutation.
    let head_phase_entry = head_phase.clone();
    let head_transaction = advance_native_stems_head_single_item_c_link(
        &mut head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native first-head C-link transaction");
    assert_eq!(head_transaction.corner.x_ordinal, 38);
    assert_eq!(head_transaction.corner.sig_ordinal, 45);
    assert_eq!(
        head_transaction.corner.horizontal,
        NativeStemHeadSide::Right
    );
    assert_eq!(
        head_transaction.corner.vertical,
        NativeStemVerticalSide::Top
    );
    assert_eq!(
        (head_transaction.last_index, head_transaction.max_index),
        (0, 0)
    );
    assert!(head_transaction.relation.accepted);
    assert_eq!(
        head_transaction.relation.derived_horizontal,
        NativeStemHeadSide::Right
    );
    assert_eq!(
        head_transaction.relation.grade.to_bits(),
        0x3fee_3eb4_ae84_ca9d
    );
    assert_eq!(
        head_transaction.relation.dx.to_bits(),
        0xbfa5_d942_375d_430c
    );
    assert_eq!(head_transaction.relation.dy.to_bits(), 0);
    assert_eq!(
        head_transaction
            .relation
            .extension_point
            .expect("accepted head relation extension")
            .x
            .to_bits(),
        0x4091_d5d6_e666_8034
    );
    assert_eq!(head_transaction.create.registration.glyph_id, 307);
    assert_eq!(
        head_transaction.create.registration.action,
        NativeStemsBeamGlyphRegistrationAction::Reused {
            reinserted_into_active_index: false
        }
    );
    let stem_identity = match head_transaction.create.disposition {
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity } => stem_identity,
        ref other => panic!("first head created unexpected stem: {other:?}"),
    };
    let created = head_transaction
        .create
        .stem
        .as_ref()
        .expect("checked first-head stem");
    let NativeStemsBeamStemGrade::Checked(created_check) = &created.grade else {
        panic!("first head stem is not classifier-checked");
    };
    assert_eq!(created_check.grade.to_bits(), 0x3fe9_3554_3bd3_1399);
    assert_eq!(
        created.geometry.ribbon_bounds,
        JavaRectangle {
            x: 1140,
            y: 319,
            width: 4,
            height: 92,
        }
    );
    assert!(!created.abnormal);
    assert_eq!(head_transaction.stem_vertex.0, 260);
    assert_eq!(head_transaction.head_stem_edge.0, 353);
    assert_eq!(head_transaction.s_linker.head.x_ordinal, 38);
    assert_eq!(
        head_transaction.s_linker.horizontal,
        NativeStemHeadSide::Right
    );
    assert!(!head_transaction.s_linked_before);
    assert!(head_transaction.s_linked_after);
    assert_eq!(head_transaction.closed_cell_changes, 0);
    assert_eq!(head_phase.current_index, 1);
    assert!(head_phase.frontier_consumed);
    assert_eq!(
        (
            head_phase.beam_state.sig.vertices.len(),
            head_phase.beam_state.sig.edges.len()
        ),
        (261, 354)
    );
    assert_eq!(head_phase.beam_state.bindings.stem_vertices.len(), 40);
    assert_eq!(
        head_phase
            .beam_state
            .s_cells
            .iter()
            .filter(|cell| cell.linked)
            .count(),
        84
    );
    let queued_right = head_phase.heads[0]
        .sides
        .iter()
        .find(|cell| cell.reference.horizontal == NativeStemHeadSide::Right)
        .expect("queued first-head RIGHT S cell");
    assert!(queued_right.linked);
    assert!(!queued_right.closed);
    let final_stem = head_phase
        .beam_state
        .latest_base_apply
        .transaction_state
        .system_stems
        .known_stems
        .iter()
        .find(|stem| stem.stem_identity == stem_identity)
        .expect("attached first-head stem in persistent systemStems");
    assert_eq!(final_stem.inter_id, Some(2379));
    assert!(final_stem.sig_attached);

    // Boundary 29: carry the successful first-head mutation through Java's next
    // two prelinked-success queue entries and their shared-stem closure writes.
    let head_after_first = head_phase.clone();
    let second_continuation = continue_native_stems_head_linking_phase1(
        &head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native second-head phase-1 continuation");
    assert_eq!(head_phase, head_after_first);
    assert_eq!(second_continuation.processed_head.sig_ordinal, 23);
    assert_eq!(second_continuation.returned_linked, Some(true));
    assert_eq!(second_continuation.closed_value_changes, 2);
    assert_eq!(
        second_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (89, NativeStemHeadSide::Left),
            (89, NativeStemHeadSide::Right),
        ]
    );
    let second_head_phase = (*second_continuation.state_after).clone();
    assert_eq!(second_head_phase.current_index, 2);
    assert!(second_head_phase.frontier_consumed);
    assert!(second_head_phase.unlinked_heads.is_empty());
    assert_eq!(
        second_head_phase.heads[1]
            .sides
            .iter()
            .map(|cell| { (cell.reference.horizontal, cell.linked, cell.closed,) })
            .collect::<Vec<_>>(),
        vec![
            (NativeStemHeadSide::Left, true, false),
            (NativeStemHeadSide::Right, false, false),
        ]
    );
    let closed_x89 = second_head_phase
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 89)
        .expect("head sharing second head's prelinked stem");
    assert!(closed_x89.sides.iter().all(|cell| cell.closed));

    let third_continuation = continue_native_stems_head_linking_phase1(
        &second_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native third-head phase-1 continuation");
    assert_eq!(third_continuation.processed_head.sig_ordinal, 33);
    assert_eq!(third_continuation.returned_linked, Some(true));
    assert_eq!(third_continuation.closed_value_changes, 4);
    assert_eq!(
        third_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (79, NativeStemHeadSide::Left),
            (79, NativeStemHeadSide::Right),
            (80, NativeStemHeadSide::Left),
            (80, NativeStemHeadSide::Right),
        ]
    );
    let third_head_phase = (*third_continuation.state_after).clone();
    assert_eq!(third_head_phase.current_index, 3);
    assert!(third_head_phase.frontier_consumed);
    assert!(third_head_phase.unlinked_heads.is_empty());
    assert_eq!(third_head_phase.heads[3].reference.x_ordinal, 20);
    assert_eq!(third_head_phase.heads[3].reference.sig_ordinal, 65);

    let fourth_continuation = continue_native_stems_head_linking_phase1(
        &third_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native fourth-head phase-1 continuation");
    assert_eq!(fourth_continuation.processed_head.sig_ordinal, 65);
    assert_eq!(fourth_continuation.returned_linked, Some(true));
    assert_eq!(fourth_continuation.closed_value_changes, 2);
    assert_eq!(
        fourth_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (19, NativeStemHeadSide::Left),
            (19, NativeStemHeadSide::Right)
        ]
    );
    let fourth_head_phase = (*fourth_continuation.state_after).clone();
    assert_eq!(fourth_head_phase.current_index, 4);
    assert!(fourth_head_phase.frontier_consumed);
    assert!(fourth_head_phase.unlinked_heads.is_empty());
    assert_eq!(fourth_head_phase.heads[4].reference.x_ordinal, 36);
    assert_eq!(fourth_head_phase.heads[4].reference.sig_ordinal, 69);

    // Boundary 31: carry the next prelinked-success queue entry.  This is
    // intentionally still the unchanged phase-1 continuation path: head x36
    // closes both S cells of its shared Stem 2369 and stops before head x99.
    let fifth_continuation = continue_native_stems_head_linking_phase1(
        &fourth_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native fifth-head phase-1 continuation");
    assert_eq!(fifth_continuation.processed_head.sig_ordinal, 69);
    assert_eq!(fifth_continuation.returned_linked, Some(true));
    assert_eq!(fifth_continuation.closed_value_changes, 2);
    assert_eq!(
        fifth_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (35, NativeStemHeadSide::Left),
            (35, NativeStemHeadSide::Right)
        ]
    );
    let fifth_head_phase = (*fifth_continuation.state_after).clone();
    assert_eq!(fifth_head_phase.current_index, 5);
    assert!(fifth_head_phase.frontier_consumed);
    assert!(fifth_head_phase.unlinked_heads.is_empty());
    assert_eq!(fifth_head_phase.heads[5].reference.x_ordinal, 99);
    assert_eq!(fifth_head_phase.heads[5].reference.sig_ordinal, 61);

    // Boundary 32: carry the next prelinked-success queue entry. Head x99
    // closes both S cells of shared Stem 2365 and stops before head x22.
    let sixth_continuation = continue_native_stems_head_linking_phase1(
        &fifth_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native sixth-head phase-1 continuation");
    assert_eq!(sixth_continuation.processed_head.sig_ordinal, 61);
    assert_eq!(sixth_continuation.returned_linked, Some(true));
    assert_eq!(sixth_continuation.closed_value_changes, 2);
    assert_eq!(
        sixth_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (98, NativeStemHeadSide::Left),
            (98, NativeStemHeadSide::Right)
        ]
    );
    let sixth_head_phase = (*sixth_continuation.state_after).clone();
    assert_eq!(sixth_head_phase.current_index, 6);
    assert!(sixth_head_phase.frontier_consumed);
    assert!(sixth_head_phase.unlinked_heads.is_empty());
    assert_eq!(sixth_head_phase.heads[6].reference.x_ordinal, 22);
    assert_eq!(sixth_head_phase.heads[6].reference.sig_ordinal, 12);

    // Boundary 33: consume the first later C-link frontier. Java selects the
    // unlinked x76/SIG97 LEFT/BOTTOM BottomOnly corner and creates Stem 2380
    // from active glyph 319 before the queue advances to index 8.
    let seventh_continuation = continue_native_stems_head_linking_phase1(
        &sixth_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order7 C-link frontier");
    assert_eq!(seventh_continuation.processed_head.sig_ordinal, 12);
    assert_eq!(seventh_continuation.returned_linked, Some(true));
    let order7_frontier = continue_native_stems_head_linking_phase1(
        &seventh_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order7 C-link frontier");
    assert_eq!(order7_frontier.processed_head.sig_ordinal, 97);
    assert_eq!(order7_frontier.returned_linked, None);
    let seventh_frontier_before = (*order7_frontier.state_after).clone();
    let mut seventh_head_phase = seventh_frontier_before.clone();
    let seventh_transaction = advance_native_stems_head_continuation_c_link(
        &mut seventh_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order7 continuation C-link transaction");
    assert_eq!(seventh_transaction.corner.x_ordinal, 76);
    assert_eq!(seventh_transaction.corner.sig_ordinal, 97);
    assert_eq!(
        seventh_transaction.corner.horizontal,
        NativeStemHeadSide::Left
    );
    assert_eq!(
        seventh_transaction.corner.vertical,
        NativeStemVerticalSide::Bottom
    );
    assert_eq!(
        (
            seventh_transaction.last_index,
            seventh_transaction.max_index
        ),
        (0, 0)
    );
    assert_eq!(seventh_transaction.selected_glyph_id, 319);
    assert_eq!(
        seventh_transaction.relation.grade.to_bits(),
        0x3fef_c384_9946_38a5
    );
    assert_eq!(
        seventh_transaction.relation.dx.to_bits(),
        0xbf78_1a7f_5e17_cf3d
    );
    assert_eq!(seventh_transaction.relation.dy.to_bits(), 0);
    assert_eq!(
        seventh_transaction
            .relation
            .extension_point
            .expect("order7 relation extension")
            .x
            .to_bits(),
        0x409b_0511_ffd9_187c
    );
    let seventh_stem_identity = match seventh_transaction.create.disposition {
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity } => stem_identity,
        ref other => panic!("order7 created unexpected stem: {other:?}"),
    };
    assert_eq!(seventh_stem_identity, 40);
    assert_eq!(seventh_transaction.create.registration.glyph_id, 319);
    assert_eq!(
        seventh_transaction.create.registration.action,
        NativeStemsBeamGlyphRegistrationAction::Reused {
            reinserted_into_active_index: false
        }
    );
    let seventh_stem = seventh_head_phase
        .beam_state
        .latest_base_apply
        .transaction_state
        .system_stems
        .known_stems
        .iter()
        .find(|stem| stem.stem_identity == seventh_stem_identity)
        .expect("order7 created system stem");
    assert_eq!(seventh_stem.inter_id, Some(2380));
    assert_eq!(seventh_transaction.stem_vertex.0, 261);
    assert_eq!(seventh_transaction.head_stem_edge.0, 354);
    assert_eq!(seventh_transaction.s_linker.head.x_ordinal, 76);
    assert_eq!(
        seventh_transaction.s_linker.horizontal,
        NativeStemHeadSide::Left
    );
    assert!(!seventh_transaction.s_linked_before);
    assert!(seventh_transaction.s_linked_after);
    assert_eq!(seventh_transaction.closed_cell_changes, 0);
    assert_eq!(seventh_head_phase.current_index, 8);
    assert!(seventh_head_phase.frontier_consumed);
    assert!(seventh_head_phase.unlinked_heads.is_empty());
    assert_eq!(seventh_head_phase.heads[7].reference.x_ordinal, 76);
    assert!(
        seventh_head_phase.heads[7]
            .sides
            .iter()
            .any(|cell| { cell.reference.horizontal == NativeStemHeadSide::Left && cell.linked })
    );
    assert_eq!(
        seventh_head_phase.beam_state.sig.vertices.len(),
        seventh_frontier_before.beam_state.sig.vertices.len() + 1
    );
    assert_eq!(
        seventh_head_phase.beam_state.sig.edges.len(),
        seventh_frontier_before.beam_state.sig.edges.len() + 1
    );

    // Boundary 34: order 8 is an unchanged prelinked-success continuation.
    // Java follows Stem 2364 through x95/SIG100 and closes the two preceding
    // heads (x91 and x94) on both sides before exposing x42/SIG93.
    let order8_before = seventh_head_phase.clone();
    let order8_continuation = continue_native_stems_head_linking_phase1(
        &seventh_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order8 prelinked continuation");
    assert_eq!(seventh_head_phase, order8_before);
    assert_eq!(order8_continuation.processed_head.x_ordinal, 95);
    assert_eq!(order8_continuation.processed_head.sig_ordinal, 100);
    assert_eq!(order8_continuation.returned_linked, Some(true));
    assert_eq!(order8_continuation.closed_value_changes, 4);
    assert_eq!(
        order8_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (91, NativeStemHeadSide::Left),
            (91, NativeStemHeadSide::Right),
            (94, NativeStemHeadSide::Left),
            (94, NativeStemHeadSide::Right),
        ]
    );
    assert_eq!(order8_continuation.state_after.current_index, 9);
    assert!(order8_continuation.state_after.frontier_consumed);
    assert_eq!(
        order8_continuation.state_after.heads[9].reference.x_ordinal,
        42
    );
    assert_eq!(
        order8_continuation.state_after.heads[9]
            .reference
            .sig_ordinal,
        93
    );
    for x_ordinal in [91, 94] {
        let closed = order8_continuation
            .state_after
            .heads
            .iter()
            .find(|head| head.reference.x_ordinal == x_ordinal)
            .expect("order8 closure target");
        assert!(closed.sides.iter().all(|cell| cell.closed));
    }

    // Boundary 35: order 9 is another unchanged prelinked-success
    // continuation. Stem 2352 closes x41 on both sides before x65/SIG95.
    let order9_before = (*order8_continuation.state_after).clone();
    let order9_continuation = continue_native_stems_head_linking_phase1(
        &order8_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order9 prelinked continuation");
    assert_eq!(*order8_continuation.state_after, order9_before);
    assert_eq!(order9_continuation.processed_head.x_ordinal, 42);
    assert_eq!(order9_continuation.processed_head.sig_ordinal, 93);
    assert_eq!(order9_continuation.returned_linked, Some(true));
    assert_eq!(order9_continuation.closed_value_changes, 2);
    assert_eq!(
        order9_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (41, NativeStemHeadSide::Left),
            (41, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order9_continuation.state_after.current_index, 10);
    assert!(order9_continuation.state_after.frontier_consumed);
    assert_eq!(
        order9_continuation.state_after.heads[10]
            .reference
            .x_ordinal,
        65
    );
    assert_eq!(
        order9_continuation.state_after.heads[10]
            .reference
            .sig_ordinal,
        95
    );
    let closed_x41 = order9_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 41)
        .expect("order9 closure target");
    assert!(closed_x41.sides.iter().all(|cell| cell.closed));

    // Boundary 36: order 10 remains prelinked-success. Stem 2346 closes x64
    // on both sides before x46/SIG57.
    let order10_before = (*order9_continuation.state_after).clone();
    let order10_continuation = continue_native_stems_head_linking_phase1(
        &order9_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order10 prelinked continuation");
    assert_eq!(*order9_continuation.state_after, order10_before);
    assert_eq!(order10_continuation.processed_head.x_ordinal, 65);
    assert_eq!(order10_continuation.processed_head.sig_ordinal, 95);
    assert_eq!(order10_continuation.returned_linked, Some(true));
    assert_eq!(order10_continuation.closed_value_changes, 2);
    assert_eq!(
        order10_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (64, NativeStemHeadSide::Left),
            (64, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order10_continuation.state_after.current_index, 11);
    assert!(order10_continuation.state_after.frontier_consumed);
    assert_eq!(
        order10_continuation.state_after.heads[11]
            .reference
            .x_ordinal,
        46
    );
    assert_eq!(
        order10_continuation.state_after.heads[11]
            .reference
            .sig_ordinal,
        57
    );
    let closed_x64 = order10_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 64)
        .expect("order10 closure target");
    assert!(closed_x64.sides.iter().all(|cell| cell.closed));

    // Boundary 37: order 11 remains prelinked-success. Stem 2377 closes x44
    // and x45 on both sides before x55/SIG79.
    let order11_before = (*order10_continuation.state_after).clone();
    let order11_continuation = continue_native_stems_head_linking_phase1(
        &order10_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order11 prelinked continuation");
    assert_eq!(*order10_continuation.state_after, order11_before);
    assert_eq!(order11_continuation.processed_head.x_ordinal, 46);
    assert_eq!(order11_continuation.processed_head.sig_ordinal, 57);
    assert_eq!(order11_continuation.returned_linked, Some(true));
    assert_eq!(order11_continuation.closed_value_changes, 4);
    assert_eq!(
        order11_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (44, NativeStemHeadSide::Left),
            (44, NativeStemHeadSide::Right),
            (45, NativeStemHeadSide::Left),
            (45, NativeStemHeadSide::Right),
        ]
    );
    assert_eq!(order11_continuation.state_after.current_index, 12);
    assert!(order11_continuation.state_after.frontier_consumed);
    assert_eq!(
        order11_continuation.state_after.heads[12]
            .reference
            .x_ordinal,
        55
    );
    assert_eq!(
        order11_continuation.state_after.heads[12]
            .reference
            .sig_ordinal,
        79
    );
    for x_ordinal in [44, 45] {
        let closed = order11_continuation
            .state_after
            .heads
            .iter()
            .find(|head| head.reference.x_ordinal == x_ordinal)
            .expect("order11 closure target");
        assert!(closed.sides.iter().all(|cell| cell.closed));
    }

    // Boundary 38: order 12 is prelinked-success under the bounded oracle
    // replay. Stem 2362 closes x51 and x54 on both sides before x53/SIG3.
    let order12_before = (*order11_continuation.state_after).clone();
    let order12_continuation = continue_native_stems_head_linking_phase1(
        &order11_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order12 prelinked continuation");
    assert_eq!(*order11_continuation.state_after, order12_before);
    assert_eq!(order12_continuation.processed_head.x_ordinal, 55);
    assert_eq!(order12_continuation.processed_head.sig_ordinal, 79);
    assert_eq!(order12_continuation.returned_linked, Some(true));
    assert_eq!(order12_continuation.closed_value_changes, 4);
    assert_eq!(
        order12_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (51, NativeStemHeadSide::Left),
            (51, NativeStemHeadSide::Right),
            (54, NativeStemHeadSide::Left),
            (54, NativeStemHeadSide::Right),
        ]
    );
    assert_eq!(order12_continuation.state_after.current_index, 13);
    assert!(order12_continuation.state_after.frontier_consumed);
    assert_eq!(
        order12_continuation.state_after.heads[13]
            .reference
            .x_ordinal,
        53
    );
    assert_eq!(
        order12_continuation.state_after.heads[13]
            .reference
            .sig_ordinal,
        3
    );
    for x_ordinal in [51, 54] {
        let closed = order12_continuation
            .state_after
            .heads
            .iter()
            .find(|head| head.reference.x_ordinal == x_ordinal)
            .expect("order12 closure target");
        assert!(closed.sides.iter().all(|cell| cell.closed));
    }

    // Boundary 39: order 13 is prelinked-success under the bounded replay.
    // Stem 2344 closes x52 on both sides before x12/SIG63.
    let order13_before = (*order12_continuation.state_after).clone();
    let order13_continuation = continue_native_stems_head_linking_phase1(
        &order12_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order13 prelinked continuation");
    assert_eq!(*order12_continuation.state_after, order13_before);
    assert_eq!(order13_continuation.processed_head.x_ordinal, 53);
    assert_eq!(order13_continuation.processed_head.sig_ordinal, 3);
    assert_eq!(order13_continuation.returned_linked, Some(true));
    assert_eq!(order13_continuation.closed_value_changes, 2);
    assert_eq!(
        order13_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (52, NativeStemHeadSide::Left),
            (52, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order13_continuation.state_after.current_index, 14);
    assert!(order13_continuation.state_after.frontier_consumed);
    assert_eq!(
        order13_continuation.state_after.heads[14]
            .reference
            .x_ordinal,
        12
    );
    assert_eq!(
        order13_continuation.state_after.heads[14]
            .reference
            .sig_ordinal,
        63
    );
    let closed_x52 = order13_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 52)
        .expect("order13 closure target");
    assert!(closed_x52.sides.iter().all(|cell| cell.closed));

    // Boundary 40: order 14 is another prelinked-success continuation.
    // Stem 2349 closes x11 on both sides before x67/SIG59.
    let order14_before = (*order13_continuation.state_after).clone();
    let order14_continuation = continue_native_stems_head_linking_phase1(
        &order13_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order14 prelinked continuation");
    assert_eq!(*order13_continuation.state_after, order14_before);
    assert_eq!(order14_continuation.processed_head.x_ordinal, 12);
    assert_eq!(order14_continuation.processed_head.sig_ordinal, 63);
    assert_eq!(order14_continuation.returned_linked, Some(true));
    assert_eq!(order14_continuation.closed_value_changes, 2);
    assert_eq!(
        order14_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (11, NativeStemHeadSide::Left),
            (11, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order14_continuation.state_after.current_index, 15);
    assert!(order14_continuation.state_after.frontier_consumed);
    assert_eq!(
        order14_continuation.state_after.heads[15]
            .reference
            .x_ordinal,
        67
    );
    assert_eq!(
        order14_continuation.state_after.heads[15]
            .reference
            .sig_ordinal,
        59
    );
    let closed_x11 = order14_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 11)
        .expect("order14 closure target");
    assert!(closed_x11.sides.iter().all(|cell| cell.closed));

    // Boundary 41: order 15 remains prelinked-success.
    // Stem 2375 closes x66 on both sides before x8/SIG53.
    let order15_before = (*order14_continuation.state_after).clone();
    let order15_continuation = continue_native_stems_head_linking_phase1(
        &order14_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order15 prelinked continuation");
    assert_eq!(*order14_continuation.state_after, order15_before);
    assert_eq!(order15_continuation.processed_head.x_ordinal, 67);
    assert_eq!(order15_continuation.processed_head.sig_ordinal, 59);
    assert_eq!(order15_continuation.returned_linked, Some(true));
    assert_eq!(order15_continuation.closed_value_changes, 2);
    assert_eq!(
        order15_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (66, NativeStemHeadSide::Left),
            (66, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order15_continuation.state_after.current_index, 16);
    assert!(order15_continuation.state_after.frontier_consumed);
    assert_eq!(
        order15_continuation.state_after.heads[16]
            .reference
            .x_ordinal,
        8
    );
    assert_eq!(
        order15_continuation.state_after.heads[16]
            .reference
            .sig_ordinal,
        53
    );
    let closed_x66 = order15_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 66)
        .expect("order15 closure target");
    assert!(closed_x66.sides.iter().all(|cell| cell.closed));

    // Boundary 42: order 16 is prelinked-success.
    // Stem 2376 closes x7 on both sides before x48/SIG29.
    let order16_before = (*order15_continuation.state_after).clone();
    let order16_continuation = continue_native_stems_head_linking_phase1(
        &order15_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order16 prelinked continuation");
    assert_eq!(*order15_continuation.state_after, order16_before);
    assert_eq!(order16_continuation.processed_head.x_ordinal, 8);
    assert_eq!(order16_continuation.processed_head.sig_ordinal, 53);
    assert_eq!(order16_continuation.returned_linked, Some(true));
    assert_eq!(order16_continuation.closed_value_changes, 2);
    assert_eq!(
        order16_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (7, NativeStemHeadSide::Left),
            (7, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order16_continuation.state_after.current_index, 17);
    assert!(order16_continuation.state_after.frontier_consumed);
    assert_eq!(
        order16_continuation.state_after.heads[17]
            .reference
            .x_ordinal,
        48
    );
    assert_eq!(
        order16_continuation.state_after.heads[17]
            .reference
            .sig_ordinal,
        29
    );
    let closed_x7 = order16_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 7)
        .expect("order16 closure target");
    assert!(closed_x7.sides.iter().all(|cell| cell.closed));

    // Boundary 43: order 17 remains prelinked-success.
    // Stem 2351 closes x47 on both sides before x63/SIG17.
    let order17_before = (*order16_continuation.state_after).clone();
    let order17_continuation = continue_native_stems_head_linking_phase1(
        &order16_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order17 prelinked continuation");
    assert_eq!(*order16_continuation.state_after, order17_before);
    assert_eq!(order17_continuation.processed_head.x_ordinal, 48);
    assert_eq!(order17_continuation.processed_head.sig_ordinal, 29);
    assert_eq!(order17_continuation.returned_linked, Some(true));
    assert_eq!(order17_continuation.closed_value_changes, 2);
    assert_eq!(
        order17_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (47, NativeStemHeadSide::Left),
            (47, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order17_continuation.state_after.current_index, 18);
    assert!(order17_continuation.state_after.frontier_consumed);
    assert_eq!(
        order17_continuation.state_after.heads[18]
            .reference
            .x_ordinal,
        63
    );
    assert_eq!(
        order17_continuation.state_after.heads[18]
            .reference
            .sig_ordinal,
        17
    );
    let closed_x47 = order17_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 47)
        .expect("order17 closure target");
    assert!(closed_x47.sides.iter().all(|cell| cell.closed));

    // Boundary 44: consume the first open/unlinked continuation C-link.
    // Java selects x63/SIG17 LEFT/BOTTOM, reuses active glyph 328, creates
    // Stem 2381, and advances to order 19 (last/max index are both 1).
    let order18_frontier = continue_native_stems_head_linking_phase1(
        &order17_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order18 C-link frontier");
    assert_eq!(order18_frontier.processed_head.x_ordinal, 63);
    assert_eq!(order18_frontier.processed_head.sig_ordinal, 17);
    assert_eq!(order18_frontier.returned_linked, None);
    let order18_frontier_before = (*order18_frontier.state_after).clone();
    let mut order18_head_phase = order18_frontier_before.clone();
    let order18_transaction = advance_native_stems_head_continuation_c_link_order18(
        &mut order18_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order18 continuation C-link transaction");
    assert_eq!(order18_transaction.corner.x_ordinal, 63);
    assert_eq!(order18_transaction.corner.sig_ordinal, 17);
    assert_eq!(
        order18_transaction.corner.horizontal,
        NativeStemHeadSide::Left
    );
    assert_eq!(
        order18_transaction.corner.vertical,
        NativeStemVerticalSide::Bottom
    );
    assert_eq!(
        (
            order18_transaction.last_index,
            order18_transaction.max_index
        ),
        (1, 1)
    );
    assert_eq!(order18_transaction.selected_glyph_id, 328);
    assert_eq!(
        order18_transaction.relation.grade.to_bits(),
        0x3fef_839e_420c_5c49
    );
    assert_eq!(
        order18_transaction.relation.dx.to_bits(),
        0xbf88_b000_860f_8618
    );
    assert_eq!(order18_transaction.relation.dy.to_bits(), 0);
    assert_eq!(
        order18_transaction
            .relation
            .extension_point
            .expect("order18 relation extension")
            .x
            .to_bits(),
        0x4098_9d96_acc1_ea22
    );
    let order18_stem_identity = match &order18_transaction.create.disposition {
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity } => *stem_identity,
        other => panic!("order18 created unexpected stem: {other:?}"),
    };
    assert_eq!(order18_stem_identity, 41);
    assert_eq!(order18_transaction.create.registration.glyph_id, 328);
    assert_eq!(
        order18_transaction.create.registration.action,
        NativeStemsBeamGlyphRegistrationAction::Reused {
            reinserted_into_active_index: false
        }
    );
    let order18_stem = order18_head_phase
        .beam_state
        .latest_base_apply
        .transaction_state
        .system_stems
        .known_stems
        .iter()
        .find(|stem| stem.stem_identity == order18_stem_identity)
        .expect("order18 created system stem");
    assert_eq!(order18_stem.inter_id, Some(2381));
    assert_eq!(order18_head_phase.current_index, 19);
    assert!(order18_head_phase.frontier_consumed);
    assert_eq!(order18_head_phase.heads[18].reference.x_ordinal, 63);
    assert!(
        order18_head_phase.heads[18]
            .sides
            .iter()
            .any(|cell| { cell.reference.horizontal == NativeStemHeadSide::Left && cell.linked })
    );
    assert_eq!(
        order18_head_phase.beam_state.sig.vertices.len(),
        order18_frontier_before.beam_state.sig.vertices.len() + 1
    );
    assert_eq!(
        order18_head_phase.beam_state.sig.edges.len(),
        order18_frontier_before.beam_state.sig.edges.len() + 1
    );

    // Boundary 45: order 19 is prelinked-success. Stem 2347 closes x68 on
    // both sides before the queue advances to x74/SIG19.
    let order19_before = order18_head_phase.clone();
    let order19_continuation = continue_native_stems_head_linking_phase1(
        &order18_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order19 prelinked continuation");
    assert_eq!(order18_head_phase, order19_before);
    assert_eq!(order19_continuation.processed_head.x_ordinal, 69);
    assert_eq!(order19_continuation.processed_head.sig_ordinal, 76);
    assert_eq!(order19_continuation.returned_linked, Some(true));
    assert_eq!(order19_continuation.closed_value_changes, 2);
    assert_eq!(
        order19_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (68, NativeStemHeadSide::Left),
            (68, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order19_continuation.state_after.current_index, 20);
    assert!(order19_continuation.state_after.frontier_consumed);
    assert_eq!(
        order19_continuation.state_after.heads[20]
            .reference
            .x_ordinal,
        74
    );
    assert_eq!(
        order19_continuation.state_after.heads[20]
            .reference
            .sig_ordinal,
        19
    );
    let closed_x68 = order19_continuation
        .state_after
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 68)
        .expect("order19 closure target");
    assert!(closed_x68.sides.iter().all(|cell| cell.closed));

    // Boundary 46: consume the second open C-link at x74/SIG19.
    let order20_frontier = continue_native_stems_head_linking_phase1(
        &order19_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order20 C-link frontier");
    assert_eq!(order20_frontier.processed_head.x_ordinal, 74);
    assert_eq!(order20_frontier.processed_head.sig_ordinal, 19);
    assert_eq!(order20_frontier.returned_linked, None);
    let order20_frontier_before = (*order20_frontier.state_after).clone();
    let mut order20_head_phase = order20_frontier_before.clone();
    let order20_transaction = advance_native_stems_head_continuation_c_link_order20(
        &mut order20_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order20 continuation C-link transaction");
    assert_eq!(order20_transaction.corner.x_ordinal, 74);
    assert_eq!(order20_transaction.corner.sig_ordinal, 19);
    assert_eq!(
        order20_transaction.corner.horizontal,
        NativeStemHeadSide::Left
    );
    assert_eq!(
        order20_transaction.corner.vertical,
        NativeStemVerticalSide::Bottom
    );
    assert_eq!(
        (
            order20_transaction.last_index,
            order20_transaction.max_index
        ),
        (1, 1)
    );
    assert_eq!(order20_transaction.selected_glyph_id, 332);
    assert_eq!(
        order20_transaction.relation.grade.to_bits(),
        0x3fef_20e4_839d_326e
    );
    assert_eq!(
        order20_transaction.relation.dx.to_bits(),
        0xbf96_01c5_ae04_b6db
    );
    assert_eq!(order20_transaction.relation.dy.to_bits(), 0);
    assert_eq!(
        order20_transaction
            .relation
            .extension_point
            .expect("order20 relation extension")
            .x
            .to_bits(),
        0x409b_0261_99f3_b0e2
    );
    let order20_stem_identity = match &order20_transaction.create.disposition {
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity } => *stem_identity,
        other => panic!("order20 created unexpected stem: {other:?}"),
    };
    assert_eq!(order20_stem_identity, 42);
    assert_eq!(order20_transaction.create.registration.glyph_id, 332);
    assert_eq!(order20_head_phase.current_index, 21);
    assert!(order20_head_phase.frontier_consumed);
    assert_eq!(order20_head_phase.heads[20].reference.x_ordinal, 74);
    assert!(
        order20_head_phase.heads[20]
            .sides
            .iter()
            .any(|cell| { cell.reference.horizontal == NativeStemHeadSide::Left && cell.linked })
    );
    let order20_stem = order20_head_phase
        .beam_state
        .latest_base_apply
        .transaction_state
        .system_stems
        .known_stems
        .iter()
        .find(|stem| stem.stem_identity == order20_stem_identity)
        .expect("order20 created system stem");
    assert_eq!(order20_stem.inter_id, Some(2382));
    assert_eq!(
        order20_head_phase.beam_state.sig.vertices.len(),
        order20_frontier_before.beam_state.sig.vertices.len() + 1
    );
    assert_eq!(
        order20_head_phase.beam_state.sig.edges.len(),
        order20_frontier_before.beam_state.sig.edges.len() + 1
    );

    let mut invalid_order20 = order20_frontier_before.clone();
    invalid_order20.current_index = 19;
    let invalid_order20_before = invalid_order20.clone();
    assert!(
        advance_native_stems_head_continuation_c_link_order20(
            &mut invalid_order20,
            &checker_page.head_corners.systems[0],
            &checker_page.head_reachability.systems[0],
            &checker_page.stem_seeds.systems[0],
            &checker_page.head_builders.systems[0],
            &hydrated.plans,
            &checker,
            &bridge,
        )
        .is_err()
    );
    assert_eq!(invalid_order20, invalid_order20_before);

    // Boundary 47: existing-stem retry/reconciliation at x28/SIG55.
    let order21_before = order20_head_phase.clone();
    let order21_continuation = advance_native_stems_head_existing_stem_retry_order21(
        &order20_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order21 existing-stem retry");
    assert_eq!(order20_head_phase, order21_before);
    assert_eq!(order21_continuation.processed_head.x_ordinal, 28);
    assert_eq!(order21_continuation.processed_head.sig_ordinal, 55);
    assert_eq!(order21_continuation.returned_linked, Some(true));
    assert_eq!(order21_continuation.closed_value_changes, 2);
    assert_eq!(
        order21_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (27, NativeStemHeadSide::Left),
            (27, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(order21_continuation.state_after.current_index, 22);
    assert!(order21_continuation.state_after.frontier_consumed);
    assert_eq!(
        order21_continuation.state_after.heads[22]
            .reference
            .x_ordinal,
        4
    );
    assert_eq!(
        order21_continuation.state_after.heads[22]
            .reference
            .sig_ordinal,
        7
    );

    let mut invalid_order21 = order20_head_phase.clone();
    invalid_order21.current_index = 20;
    let invalid_order21_before = invalid_order21.clone();
    assert!(
        advance_native_stems_head_existing_stem_retry_order21(
            &invalid_order21,
            &checker_page.head_corners.systems[0],
            &checker_page.head_builders.systems[0],
            &hydrated.plans,
        )
        .is_err()
    );
    assert_eq!(invalid_order21, invalid_order21_before);

    // Boundary 48: existing-stem retry/reconciliation at x4/SIG7.
    let order22_before = order21_continuation.state_after.clone();
    let order22_continuation = advance_native_stems_head_existing_stem_retry_order22(
        &order21_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order22 existing-stem retry");
    assert_eq!(*order21_continuation.state_after, *order22_before);
    assert_eq!(order22_continuation.processed_head.x_ordinal, 4);
    assert_eq!(order22_continuation.processed_head.sig_ordinal, 7);
    assert_eq!(order22_continuation.returned_linked, Some(true));
    assert_eq!(order22_continuation.closed_value_changes, 2);
    assert_eq!(order22_continuation.state_after.current_index, 23);
    assert!(order22_continuation.state_after.frontier_consumed);
    assert_eq!(
        order22_continuation.state_after.heads[23]
            .reference
            .x_ordinal,
        78
    );
    assert_eq!(
        order22_continuation.state_after.heads[23]
            .reference
            .sig_ordinal,
        39
    );
    assert_eq!(
        order22_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (3, NativeStemHeadSide::Left),
            (3, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 49: order 23 is a generic prelinked closure.
    let order23_before = order22_continuation.state_after.clone();
    let order23_continuation = continue_native_stems_head_linking_phase1(
        &order22_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order23 prelinked closure");
    assert_eq!(*order22_continuation.state_after, *order23_before);
    assert_eq!(order23_continuation.processed_head.x_ordinal, 78);
    assert_eq!(order23_continuation.processed_head.sig_ordinal, 39);
    assert_eq!(order23_continuation.returned_linked, Some(true));
    assert_eq!(order23_continuation.closed_value_changes, 2);
    assert_eq!(order23_continuation.state_after.current_index, 24);
    assert!(order23_continuation.state_after.frontier_consumed);
    assert_eq!(
        order23_continuation.state_after.heads[24]
            .reference
            .x_ordinal,
        93
    );
    assert_eq!(
        order23_continuation.state_after.heads[24]
            .reference
            .sig_ordinal,
        25
    );
    assert_eq!(
        order23_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (77, NativeStemHeadSide::Left),
            (77, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 50: order 24 is another generic prelinked closure.
    let order24_before = order23_continuation.state_after.clone();
    let order24_continuation = continue_native_stems_head_linking_phase1(
        &order23_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order24 prelinked closure");
    assert_eq!(*order23_continuation.state_after, *order24_before);
    assert_eq!(order24_continuation.processed_head.x_ordinal, 93);
    assert_eq!(order24_continuation.processed_head.sig_ordinal, 25);
    assert_eq!(order24_continuation.returned_linked, Some(true));
    assert_eq!(order24_continuation.closed_value_changes, 2);
    assert_eq!(order24_continuation.state_after.current_index, 25);
    assert!(order24_continuation.state_after.frontier_consumed);
    assert_eq!(
        order24_continuation.state_after.heads[25]
            .reference
            .x_ordinal,
        59
    );
    assert_eq!(
        order24_continuation.state_after.heads[25]
            .reference
            .sig_ordinal,
        74
    );
    assert_eq!(
        order24_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (92, NativeStemHeadSide::Left),
            (92, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 51: order 25 is another generic prelinked closure.
    let order25_before = order24_continuation.state_after.clone();
    let order25_continuation = continue_native_stems_head_linking_phase1(
        &order24_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order25 prelinked closure");
    assert_eq!(*order24_continuation.state_after, *order25_before);
    assert_eq!(order25_continuation.processed_head.x_ordinal, 59);
    assert_eq!(order25_continuation.processed_head.sig_ordinal, 74);
    assert_eq!(order25_continuation.returned_linked, Some(true));
    assert_eq!(order25_continuation.closed_value_changes, 2);
    assert_eq!(order25_continuation.state_after.current_index, 26);
    assert!(order25_continuation.state_after.frontier_consumed);
    assert_eq!(
        order25_continuation.state_after.heads[26]
            .reference
            .x_ordinal,
        61
    );
    assert_eq!(
        order25_continuation.state_after.heads[26]
            .reference
            .sig_ordinal,
        31
    );
    assert_eq!(
        order25_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (58, NativeStemHeadSide::Left),
            (58, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 52: order 26 is another generic prelinked closure.
    let order26_before = order25_continuation.state_after.clone();
    let order26_continuation = continue_native_stems_head_linking_phase1(
        &order25_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order26 prelinked closure");
    assert_eq!(*order25_continuation.state_after, *order26_before);
    assert_eq!(order26_continuation.processed_head.x_ordinal, 61);
    assert_eq!(order26_continuation.processed_head.sig_ordinal, 31);
    assert_eq!(order26_continuation.returned_linked, Some(true));
    assert_eq!(order26_continuation.closed_value_changes, 2);
    assert_eq!(order26_continuation.state_after.current_index, 27);
    assert!(order26_continuation.state_after.frontier_consumed);
    assert_eq!(
        order26_continuation.state_after.heads[27]
            .reference
            .x_ordinal,
        33
    );
    assert_eq!(
        order26_continuation.state_after.heads[27]
            .reference
            .sig_ordinal,
        26
    );
    assert_eq!(
        order26_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (60, NativeStemHeadSide::Left),
            (60, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 53: order 27 is the next authenticated C-link transaction.
    let order27_frontier = continue_native_stems_head_linking_phase1(
        &order26_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order27 C-link frontier");
    assert_eq!(order27_frontier.processed_head.x_ordinal, 33);
    assert_eq!(order27_frontier.processed_head.sig_ordinal, 26);
    assert_eq!(order27_frontier.returned_linked, None);
    let order27_frontier_before = (*order27_frontier.state_after).clone();
    let mut order27_head_phase = order27_frontier_before.clone();
    let order27_transaction = advance_native_stems_head_continuation_c_link_order27(
        &mut order27_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order27 continuation C-link transaction");
    assert_eq!(order27_transaction.corner.x_ordinal, 33);
    assert_eq!(order27_transaction.corner.sig_ordinal, 26);
    assert_eq!(
        order27_transaction.corner.horizontal,
        NativeStemHeadSide::Left
    );
    assert_eq!(
        order27_transaction.corner.vertical,
        NativeStemVerticalSide::Bottom
    );
    assert_eq!(
        (
            order27_transaction.last_index,
            order27_transaction.max_index
        ),
        (1, 1)
    );
    assert_eq!(order27_transaction.selected_glyph_id, 314);
    assert_eq!(
        order27_transaction.relation.grade.to_bits(),
        0x3fef_60c6_52f5_e4c7
    );
    assert_eq!(
        order27_transaction.relation.dx.to_bits(),
        0xbf8f_891b_a6de_30c3
    );
    assert_eq!(order27_transaction.relation.dy.to_bits(), 0);
    assert_eq!(
        order27_transaction
            .relation
            .extension_point
            .expect("order27 relation extension")
            .x
            .to_bits(),
        0x408f_e3bd_28bd_8538
    );
    let order27_stem_identity = match &order27_transaction.create.disposition {
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity } => *stem_identity,
        other => panic!("order27 created unexpected stem: {other:?}"),
    };
    assert_eq!(order27_stem_identity, 43);
    assert_eq!(order27_transaction.create.registration.glyph_id, 314);
    assert_eq!(order27_head_phase.current_index, 28);
    assert!(order27_head_phase.frontier_consumed);
    assert_eq!(order27_head_phase.heads[27].reference.x_ordinal, 33);
    assert_eq!(order27_head_phase.heads[27].reference.sig_ordinal, 26);
    assert_eq!(
        order27_head_phase.beam_state.sig.vertices.len(),
        order27_frontier_before.beam_state.sig.vertices.len() + 1
    );
    assert_eq!(
        order27_head_phase.beam_state.sig.edges.len(),
        order27_frontier_before.beam_state.sig.edges.len() + 1
    );

    // Boundary 54: order 28 is a generic prelinked closure.
    let order28_continuation = continue_native_stems_head_linking_phase1(
        &order27_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order28 prelinked closure");
    assert_eq!(order28_continuation.processed_head.x_ordinal, 85);
    assert_eq!(order28_continuation.processed_head.sig_ordinal, 87);
    assert_eq!(order28_continuation.returned_linked, Some(true));
    assert_eq!(order28_continuation.closed_value_changes, 4);
    assert_eq!(order28_continuation.state_after.current_index, 29);
    assert!(order28_continuation.state_after.frontier_consumed);
    assert_eq!(
        order28_continuation.state_after.heads[29]
            .reference
            .x_ordinal,
        10
    );
    assert_eq!(
        order28_continuation.state_after.heads[29]
            .reference
            .sig_ordinal,
        9
    );
    assert_eq!(
        order28_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (84, NativeStemHeadSide::Left),
            (84, NativeStemHeadSide::Right),
            (86, NativeStemHeadSide::Left),
            (86, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 55: order 29 is another generic prelinked closure.
    let order29_continuation = continue_native_stems_head_linking_phase1(
        &order28_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order29 prelinked closure");
    assert_eq!(order29_continuation.processed_head.x_ordinal, 10);
    assert_eq!(order29_continuation.processed_head.sig_ordinal, 9);
    assert_eq!(order29_continuation.returned_linked, Some(true));
    assert_eq!(order29_continuation.closed_value_changes, 2);
    assert_eq!(order29_continuation.state_after.current_index, 30);
    assert!(order29_continuation.state_after.frontier_consumed);
    assert_eq!(
        order29_continuation.state_after.heads[30]
            .reference
            .x_ordinal,
        101
    );
    assert_eq!(
        order29_continuation.state_after.heads[30]
            .reference
            .sig_ordinal,
        43
    );
    assert_eq!(
        order29_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (9, NativeStemHeadSide::Left),
            (9, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 56: order 30 is a generic prelinked closure.
    let order30_continuation = continue_native_stems_head_linking_phase1(
        &order29_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order30 prelinked closure");
    assert_eq!(order30_continuation.processed_head.x_ordinal, 101);
    assert_eq!(order30_continuation.processed_head.sig_ordinal, 43);
    assert_eq!(order30_continuation.returned_linked, Some(true));
    assert_eq!(order30_continuation.closed_value_changes, 2);
    assert_eq!(order30_continuation.state_after.current_index, 31);
    assert!(order30_continuation.state_after.frontier_consumed);
    assert_eq!(
        order30_continuation.state_after.heads[31]
            .reference
            .x_ordinal,
        16
    );
    assert_eq!(
        order30_continuation.state_after.heads[31]
            .reference
            .sig_ordinal,
        81
    );
    assert_eq!(
        order30_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (100, NativeStemHeadSide::Left),
            (100, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 57: order 31 is a generic prelinked closure.
    let order31_continuation = continue_native_stems_head_linking_phase1(
        &order30_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order31 prelinked closure");
    assert_eq!(order31_continuation.processed_head.x_ordinal, 16);
    assert_eq!(order31_continuation.processed_head.sig_ordinal, 81);
    assert_eq!(order31_continuation.returned_linked, Some(true));
    assert_eq!(order31_continuation.closed_value_changes, 2);
    assert_eq!(order31_continuation.state_after.current_index, 32);
    assert!(order31_continuation.state_after.frontier_consumed);
    assert_eq!(
        order31_continuation.state_after.heads[32]
            .reference
            .x_ordinal,
        34
    );
    assert_eq!(
        order31_continuation.state_after.heads[32]
            .reference
            .sig_ordinal,
        77
    );
    assert_eq!(
        order31_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (15, NativeStemHeadSide::Left),
            (15, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 58: order 32 is a generic prelinked no-op closure.
    let order32_continuation = continue_native_stems_head_linking_phase1(
        &order31_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order32 prelinked no-op closure");
    assert_eq!(order32_continuation.processed_head.x_ordinal, 34);
    assert_eq!(order32_continuation.processed_head.sig_ordinal, 77);
    assert_eq!(order32_continuation.returned_linked, Some(true));
    assert_eq!(order32_continuation.closed_value_changes, 0);
    assert_eq!(order32_continuation.state_after.current_index, 33);
    assert!(order32_continuation.state_after.frontier_consumed);
    assert_eq!(
        order32_continuation.state_after.heads[33]
            .reference
            .x_ordinal,
        88
    );
    assert_eq!(
        order32_continuation.state_after.heads[33]
            .reference
            .sig_ordinal,
        84
    );
    assert!(order32_continuation.closed_s_linkers.is_empty());

    // Boundary 59: order 33 is a generic prelinked closure.
    let order33_continuation = continue_native_stems_head_linking_phase1(
        &order32_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order33 prelinked closure");
    assert_eq!(order33_continuation.processed_head.x_ordinal, 88);
    assert_eq!(order33_continuation.processed_head.sig_ordinal, 84);
    assert_eq!(order33_continuation.returned_linked, Some(true));
    assert_eq!(order33_continuation.closed_value_changes, 2);
    assert_eq!(order33_continuation.state_after.current_index, 34);
    assert!(order33_continuation.state_after.frontier_consumed);
    assert_eq!(
        order33_continuation.state_after.heads[34]
            .reference
            .x_ordinal,
        2
    );
    assert_eq!(
        order33_continuation.state_after.heads[34]
            .reference
            .sig_ordinal,
        36
    );
    assert_eq!(
        order33_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (87, NativeStemHeadSide::Left),
            (87, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 60: order 34 is an authenticated both-open C-link.
    let order34_frontier = continue_native_stems_head_linking_phase1(
        &order33_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order34 C-link frontier");
    assert_eq!(order34_frontier.returned_linked, None);
    assert_eq!(order34_frontier.state_after.current_index, 34);
    let mut order34_head_phase = order34_frontier.state_after.clone();
    let order34_transaction = advance_native_stems_head_continuation_c_link_order34(
        &mut order34_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order34 continuation C-link transaction");
    assert_eq!(order34_transaction.corner.x_ordinal, 2);
    assert_eq!(order34_transaction.corner.sig_ordinal, 36);
    assert_eq!(
        order34_transaction.corner.horizontal,
        NativeStemHeadSide::Left
    );
    assert_eq!(
        order34_transaction.corner.vertical,
        NativeStemVerticalSide::Bottom
    );
    assert_eq!(
        (
            order34_transaction.last_index,
            order34_transaction.max_index
        ),
        (1, 1)
    );
    assert_eq!(order34_transaction.selected_glyph_id, 322);
    assert_eq!(
        order34_transaction.relation.grade.to_bits(),
        0x3fef_77d5_00fa_d911
    );
    assert_eq!(
        order34_transaction.relation.dx.to_bits(),
        0xbf8b_01db_3c16_1249
    );
    assert_eq!(order34_transaction.relation.dy.to_bits(), 0);
    assert_eq!(
        order34_transaction
            .relation
            .extension_point
            .expect("order34 relation extension")
            .x
            .to_bits(),
        0x4078_a6bc_20e9_859b
    );
    let order34_stem_identity = match &order34_transaction.create.disposition {
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity } => *stem_identity,
        other => panic!("order34 created unexpected stem: {other:?}"),
    };
    assert_eq!(order34_stem_identity, 44);
    assert_eq!(order34_transaction.create.registration.glyph_id, 322);
    assert_eq!(order34_head_phase.current_index, 35);
    assert!(order34_head_phase.frontier_consumed);
    assert_eq!(
        order34_head_phase.beam_state.sig.vertices.len(),
        order34_frontier.state_after.beam_state.sig.vertices.len() + 1
    );
    assert_eq!(
        order34_head_phase.beam_state.sig.edges.len(),
        order34_frontier.state_after.beam_state.sig.edges.len() + 1
    );

    // Boundary 61: order 35 is a generic prelinked closure after the C-link.
    let order35_continuation = continue_native_stems_head_linking_phase1(
        &order34_head_phase,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order35 prelinked closure");
    assert_eq!(order35_continuation.processed_head.x_ordinal, 50);
    assert_eq!(order35_continuation.processed_head.sig_ordinal, 72);
    assert_eq!(order35_continuation.returned_linked, Some(true));
    assert_eq!(order35_continuation.closed_value_changes, 2);
    assert_eq!(order35_continuation.state_after.current_index, 36);
    assert!(order35_continuation.state_after.frontier_consumed);
    assert_eq!(
        order35_continuation.state_after.heads[36]
            .reference
            .x_ordinal,
        23
    );
    assert_eq!(
        order35_continuation.state_after.heads[36]
            .reference
            .sig_ordinal,
        14
    );
    assert_eq!(
        order35_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (49, NativeStemHeadSide::Left),
            (49, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 62: order 36 is an authenticated both-open C-link.
    let order36_frontier = continue_native_stems_head_linking_phase1(
        &order35_continuation.state_after,
        &checker_page.head_corners.systems[0],
        None,
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order36 C-link frontier");
    assert_eq!(order36_frontier.returned_linked, None);
    assert_eq!(order36_frontier.state_after.current_index, 36);
    let mut order36_head_phase = order36_frontier.state_after.clone();
    let order36_transaction = advance_native_stems_head_continuation_c_link_order36(
        &mut order36_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order36 continuation C-link transaction");
    assert_eq!(order36_transaction.corner.x_ordinal, 23);
    assert_eq!(order36_transaction.corner.sig_ordinal, 14);
    assert_eq!(
        order36_transaction.corner.horizontal,
        NativeStemHeadSide::Left
    );
    assert_eq!(
        order36_transaction.corner.vertical,
        NativeStemVerticalSide::Bottom
    );
    assert_eq!(
        (
            order36_transaction.last_index,
            order36_transaction.max_index
        ),
        (0, 0)
    );
    assert_eq!(order36_transaction.selected_glyph_id, 324);
    assert_eq!(
        order36_transaction.relation.grade.to_bits(),
        0x3fef_53d1_5e98_5435
    );
    assert_eq!(
        order36_transaction.relation.dx.to_bits(),
        0xbf91_098a_436a_1862
    );
    assert_eq!(order36_transaction.relation.dy.to_bits(), 0);
    let order36_stem_identity = match &order36_transaction.create.disposition {
        NativeStemsBeamCreateStemDisposition::CreatedChecked { stem_identity } => *stem_identity,
        other => panic!("order36 created unexpected stem: {other:?}"),
    };
    assert_eq!(order36_stem_identity, 45);
    assert_eq!(order36_transaction.create.registration.glyph_id, 324);
    assert_eq!(order36_head_phase.current_index, 37);
    assert!(order36_head_phase.frontier_consumed);
    assert_eq!(
        order36_head_phase.beam_state.sig.vertices.len(),
        order36_frontier.state_after.beam_state.sig.vertices.len() + 1
    );
    assert_eq!(
        order36_head_phase.beam_state.sig.edges.len(),
        order36_frontier.state_after.beam_state.sig.edges.len() + 1
    );

    // Boundary 63: order 37 reconciles an existing stem and closes x13.
    let order37_continuation = advance_native_stems_head_existing_stem_retry_order37(
        &order36_head_phase,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order37 existing-stem reconciliation");
    assert_eq!(order37_continuation.processed_head.x_ordinal, 14);
    assert_eq!(order37_continuation.processed_head.sig_ordinal, 1);
    assert_eq!(order37_continuation.returned_linked, Some(true));
    assert_eq!(order37_continuation.closed_value_changes, 2);
    assert_eq!(order37_continuation.state_after.current_index, 38);
    assert!(order37_continuation.state_after.frontier_consumed);
    assert_eq!(
        order37_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (13, NativeStemHeadSide::Left),
            (13, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 64: order 38 reconciles an existing stem and closes x17.
    let order38_continuation = advance_native_stems_head_existing_stem_retry_order38(
        &order37_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order38 existing-stem reconciliation");
    assert_eq!(order38_continuation.processed_head.x_ordinal, 18);
    assert_eq!(order38_continuation.processed_head.sig_ordinal, 4);
    assert_eq!(order38_continuation.returned_linked, Some(true));
    assert_eq!(order38_continuation.closed_value_changes, 2);
    assert_eq!(order38_continuation.state_after.current_index, 39);
    assert!(order38_continuation.state_after.frontier_consumed);
    assert_eq!(
        order38_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (17, NativeStemHeadSide::Left),
            (17, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 65: order 39 reconciles an existing stem and closes x96.
    let order39_continuation = advance_native_stems_head_existing_stem_retry_order39(
        &order38_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order39 existing-stem reconciliation");
    assert_eq!(order39_continuation.processed_head.x_ordinal, 97);
    assert_eq!(order39_continuation.processed_head.sig_ordinal, 34);
    assert_eq!(order39_continuation.returned_linked, Some(true));
    assert_eq!(order39_continuation.closed_value_changes, 2);
    assert_eq!(order39_continuation.state_after.current_index, 40);
    assert!(order39_continuation.state_after.frontier_consumed);
    assert_eq!(
        order39_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (96, NativeStemHeadSide::Left),
            (96, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 66: order 40 reconciles an existing stem and closes x5.
    let order40_continuation = advance_native_stems_head_existing_stem_retry_order40(
        &order39_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order40 existing-stem reconciliation");
    assert_eq!(order40_continuation.processed_head.x_ordinal, 6);
    assert_eq!(order40_continuation.processed_head.sig_ordinal, 89);
    assert_eq!(order40_continuation.returned_linked, Some(true));
    assert_eq!(order40_continuation.closed_value_changes, 2);
    assert_eq!(order40_continuation.state_after.current_index, 41);
    assert!(order40_continuation.state_after.frontier_consumed);
    assert_eq!(
        order40_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (5, NativeStemHeadSide::Left),
            (5, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 67: order 41 reconciles an existing stem and closes x29.
    let order41_continuation = advance_native_stems_head_existing_stem_retry_order41(
        &order40_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order41 existing-stem reconciliation");
    assert_eq!(order41_continuation.processed_head.x_ordinal, 30);
    assert_eq!(order41_continuation.processed_head.sig_ordinal, 67);
    assert_eq!(order41_continuation.returned_linked, Some(true));
    assert_eq!(order41_continuation.closed_value_changes, 2);
    assert_eq!(order41_continuation.state_after.current_index, 42);
    assert!(order41_continuation.state_after.frontier_consumed);
    assert_eq!(
        order41_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (29, NativeStemHeadSide::Left),
            (29, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 68: order 42 reconciles an existing stem and closes x39/x40.
    let order42_continuation = advance_native_stems_head_existing_stem_retry_order42(
        &order41_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order42 existing-stem reconciliation");
    assert_eq!(order42_continuation.processed_head.x_ordinal, 43);
    assert_eq!(order42_continuation.processed_head.sig_ordinal, 48);
    assert_eq!(order42_continuation.returned_linked, Some(true));
    assert_eq!(order42_continuation.closed_value_changes, 4);
    assert_eq!(order42_continuation.state_after.current_index, 43);
    assert!(order42_continuation.state_after.frontier_consumed);
    assert_eq!(
        order42_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (39, NativeStemHeadSide::Left),
            (39, NativeStemHeadSide::Right),
            (40, NativeStemHeadSide::Left),
            (40, NativeStemHeadSide::Right),
        ]
    );

    // Boundary 69: order 43 reconciles an existing stem and closes x24.
    let order43_continuation = advance_native_stems_head_existing_stem_retry_order43(
        &order42_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order43 existing-stem reconciliation");
    assert_eq!(order43_continuation.processed_head.x_ordinal, 25);
    assert_eq!(order43_continuation.processed_head.sig_ordinal, 91);
    assert_eq!(order43_continuation.returned_linked, Some(true));
    assert_eq!(order43_continuation.closed_value_changes, 2);
    assert_eq!(order43_continuation.state_after.current_index, 44);
    assert!(order43_continuation.state_after.frontier_consumed);
    assert_eq!(
        order43_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (24, NativeStemHeadSide::Left),
            (24, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 70: order 44 reconciles an existing stem and closes x82.
    let order44_continuation = advance_native_stems_head_existing_stem_retry_order44(
        &order43_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order44 existing-stem reconciliation");
    assert_eq!(order44_continuation.processed_head.x_ordinal, 83);
    assert_eq!(order44_continuation.processed_head.sig_ordinal, 21);
    assert_eq!(order44_continuation.returned_linked, Some(true));
    assert_eq!(order44_continuation.closed_value_changes, 2);
    assert_eq!(order44_continuation.state_after.current_index, 45);
    assert!(order44_continuation.state_after.frontier_consumed);
    assert_eq!(
        order44_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (82, NativeStemHeadSide::Left),
            (82, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 71: order 45 reconciles an existing stem and closes x56.
    let order45_continuation = advance_native_stems_head_existing_stem_retry_order45(
        &order44_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order45 existing-stem reconciliation");
    assert_eq!(order45_continuation.processed_head.x_ordinal, 57);
    assert_eq!(order45_continuation.processed_head.sig_ordinal, 5);
    assert_eq!(order45_continuation.returned_linked, Some(true));
    assert_eq!(order45_continuation.closed_value_changes, 2);
    assert_eq!(order45_continuation.state_after.current_index, 46);
    assert!(order45_continuation.state_after.frontier_consumed);
    assert_eq!(
        order45_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (56, NativeStemHeadSide::Left),
            (56, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 72: order 46 reconciles an existing stem and closes x43.
    let order46_continuation = advance_native_stems_head_existing_stem_retry_order46(
        &order45_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order46 existing-stem reconciliation");
    assert_eq!(order46_continuation.processed_head.x_ordinal, 40);
    assert_eq!(order46_continuation.processed_head.sig_ordinal, 27);
    assert_eq!(order46_continuation.returned_linked, Some(true));
    assert_eq!(order46_continuation.closed_value_changes, 2);
    assert_eq!(order46_continuation.state_after.current_index, 47);
    assert!(order46_continuation.state_after.frontier_consumed);
    assert_eq!(
        order46_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (39, NativeStemHeadSide::Left),
            (39, NativeStemHeadSide::Right),
            (43, NativeStemHeadSide::Left),
            (43, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 73: order 47 reconciles an existing stem and closes x90.
    let order47_continuation = advance_native_stems_head_existing_stem_retry_order47(
        &order46_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order47 existing-stem reconciliation");
    assert_eq!(order47_continuation.processed_head.x_ordinal, 89);
    assert_eq!(order47_continuation.processed_head.sig_ordinal, 22);
    assert_eq!(order47_continuation.returned_linked, Some(true));
    assert_eq!(order47_continuation.closed_value_changes, 2);
    assert_eq!(order47_continuation.state_after.current_index, 48);
    assert!(order47_continuation.state_after.frontier_consumed);
    assert_eq!(
        order47_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (90, NativeStemHeadSide::Left),
            (90, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 74: order 48 reconciles an existing stem and closes x53.
    let order48_continuation = advance_native_stems_head_existing_stem_retry_order48(
        &order47_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order48 existing-stem reconciliation");
    assert_eq!(order48_continuation.processed_head.x_ordinal, 52);
    assert_eq!(order48_continuation.processed_head.sig_ordinal, 2);
    assert_eq!(order48_continuation.returned_linked, Some(true));
    assert_eq!(order48_continuation.closed_value_changes, 2);
    assert_eq!(order48_continuation.state_after.current_index, 49);
    assert!(order48_continuation.state_after.frontier_consumed);
    assert_eq!(
        order48_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (53, NativeStemHeadSide::Left),
            (53, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 75: order 49 reconciles an existing stem and closes x36.
    let order49_continuation = advance_native_stems_head_existing_stem_retry_order49(
        &order48_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order49 existing-stem reconciliation");
    assert_eq!(order49_continuation.processed_head.x_ordinal, 35);
    assert_eq!(order49_continuation.processed_head.sig_ordinal, 68);
    assert_eq!(order49_continuation.returned_linked, Some(true));
    assert_eq!(order49_continuation.closed_value_changes, 2);
    assert_eq!(order49_continuation.state_after.current_index, 50);
    assert!(order49_continuation.state_after.frontier_consumed);
    assert_eq!(
        order49_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (36, NativeStemHeadSide::Left),
            (36, NativeStemHeadSide::Right)
        ]
    );

    // Boundary 76: order 50 is an authenticated open/undefined frontier.
    // The existing StemInter is reused without any graph or linker write;
    // Java records LEFT as undefined and advances with a false return.
    let order50_continuation = advance_native_stems_head_open_frontier_order50(
        &order49_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order50 open frontier");
    assert_eq!(order50_continuation.processed_head.x_ordinal, 32);
    assert_eq!(order50_continuation.processed_head.sig_ordinal, 50);
    assert_eq!(order50_continuation.returned_linked, Some(false));
    assert_eq!(order50_continuation.closed_value_changes, 0);
    assert!(order50_continuation.closed_s_linkers.is_empty());
    assert_eq!(order50_continuation.state_after.current_index, 51);
    assert!(order50_continuation.state_after.frontier_consumed);
    assert_eq!(
        order50_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );
    assert_eq!(
        order50_continuation.state_after.unlinked_heads,
        vec![order50_continuation.processed_head]
    );
    assert_eq!(
        order50_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![
            (NativeStemHeadSide::Left, Some(true), Some(true),),
            (NativeStemHeadSide::Right, Some(true), Some(false),),
        ]
    );

    // Boundary 77: order 51 reconciles an existing stem and closes x20
    // while the undefined LEFT side carried from order 50 stays unchanged.
    let order51_continuation = advance_native_stems_head_existing_stem_retry_order51(
        &order50_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order51 existing-stem reconciliation");
    assert_eq!(order51_continuation.processed_head.x_ordinal, 19);
    assert_eq!(order51_continuation.processed_head.sig_ordinal, 64);
    assert_eq!(order51_continuation.returned_linked, Some(true));
    assert_eq!(order51_continuation.closed_value_changes, 2);
    assert_eq!(order51_continuation.state_after.current_index, 52);
    assert!(order51_continuation.state_after.frontier_consumed);
    assert_eq!(
        order51_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (20, NativeStemHeadSide::Left),
            (20, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order51_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 78: order 52 reconciles an existing stem and closes x16
    // while the undefined LEFT side carried from order 50 stays unchanged.
    let order52_continuation = advance_native_stems_head_existing_stem_retry_order52(
        &order51_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order52 existing-stem reconciliation");
    assert_eq!(order52_continuation.processed_head.x_ordinal, 15);
    assert_eq!(order52_continuation.processed_head.sig_ordinal, 80);
    assert_eq!(order52_continuation.returned_linked, Some(true));
    assert_eq!(order52_continuation.closed_value_changes, 2);
    assert_eq!(order52_continuation.state_after.current_index, 53);
    assert!(order52_continuation.state_after.frontier_consumed);
    assert_eq!(
        order52_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (16, NativeStemHeadSide::Left),
            (16, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order52_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 79: order 53 reconciles a three-head shared stem, closing
    // x85's open cells and re-writing x86's already-closed cells with no
    // value change, while the carried undefined LEFT side stays unchanged.
    let order53_continuation = advance_native_stems_head_existing_stem_retry_order53(
        &order52_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order53 existing-stem reconciliation");
    assert_eq!(order53_continuation.processed_head.x_ordinal, 84);
    assert_eq!(order53_continuation.processed_head.sig_ordinal, 86);
    assert_eq!(order53_continuation.returned_linked, Some(true));
    assert_eq!(order53_continuation.closed_value_changes, 2);
    assert_eq!(order53_continuation.state_after.current_index, 54);
    assert!(order53_continuation.state_after.frontier_consumed);
    assert_eq!(
        order53_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (85, NativeStemHeadSide::Left),
            (85, NativeStemHeadSide::Right),
            (86, NativeStemHeadSide::Left),
            (86, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order53_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 80: order 54 reconciles an existing stem and closes x12
    // while the undefined LEFT side carried from order 50 stays unchanged.
    let order54_continuation = advance_native_stems_head_existing_stem_retry_order54(
        &order53_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order54 existing-stem reconciliation");
    assert_eq!(order54_continuation.processed_head.x_ordinal, 11);
    assert_eq!(order54_continuation.processed_head.sig_ordinal, 62);
    assert_eq!(order54_continuation.returned_linked, Some(true));
    assert_eq!(order54_continuation.closed_value_changes, 2);
    assert_eq!(order54_continuation.state_after.current_index, 55);
    assert!(order54_continuation.state_after.frontier_consumed);
    assert_eq!(
        order54_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (12, NativeStemHeadSide::Left),
            (12, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order54_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 81: order 55 reconciles an existing stem and closes x69
    // while the undefined LEFT side carried from order 50 stays unchanged.
    let order55_continuation = advance_native_stems_head_existing_stem_retry_order55(
        &order54_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order55 existing-stem reconciliation");
    assert_eq!(order55_continuation.processed_head.x_ordinal, 68);
    assert_eq!(order55_continuation.processed_head.sig_ordinal, 75);
    assert_eq!(order55_continuation.returned_linked, Some(true));
    assert_eq!(order55_continuation.closed_value_changes, 2);
    assert_eq!(order55_continuation.state_after.current_index, 56);
    assert!(order55_continuation.state_after.frontier_consumed);
    assert_eq!(
        order55_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (69, NativeStemHeadSide::Left),
            (69, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order55_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 82: order 56 reconciles an existing stem and closes x22
    // while the undefined LEFT side carried from order 50 stays unchanged.
    let order56_continuation = advance_native_stems_head_existing_stem_retry_order56(
        &order55_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order56 existing-stem reconciliation");
    assert_eq!(order56_continuation.processed_head.x_ordinal, 21);
    assert_eq!(order56_continuation.processed_head.sig_ordinal, 11);
    assert_eq!(order56_continuation.returned_linked, Some(true));
    assert_eq!(order56_continuation.closed_value_changes, 2);
    assert_eq!(order56_continuation.state_after.current_index, 57);
    assert!(order56_continuation.state_after.frontier_consumed);
    assert_eq!(
        order56_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (22, NativeStemHeadSide::Left),
            (22, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order56_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 83: order 57's both-open LEFT BottomOnly frontier reuses
    // existing StemInter 2381 through one appended HeadStem relation, links
    // x62's LEFT cell, and closes sibling x63, without vertex, allocator,
    // ID, or system-stem mutation.
    let order57_continuation = advance_native_stems_head_existing_stem_c_link_order57(
        &order56_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order57 existing-stem C-link");
    assert_eq!(order57_continuation.processed_head.x_ordinal, 62);
    assert_eq!(order57_continuation.processed_head.sig_ordinal, 16);
    assert_eq!(order57_continuation.returned_linked, Some(true));
    assert_eq!(order57_continuation.closed_value_changes, 2);
    assert_eq!(order57_continuation.state_after.current_index, 58);
    assert!(order57_continuation.state_after.frontier_consumed);
    assert_eq!(
        order57_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![(NativeStemHeadSide::Left, Some(false), Some(true))]
    );
    assert_eq!(
        order57_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (63, NativeStemHeadSide::Left),
            (63, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order57_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );
    assert_eq!(
        order57_continuation.state_after.beam_state.sig.edges.len(),
        order56_continuation.state_after.beam_state.sig.edges.len() + 1
    );
    assert_eq!(
        order57_continuation
            .state_after
            .beam_state
            .sig
            .vertices
            .len(),
        order56_continuation
            .state_after
            .beam_state
            .sig
            .vertices
            .len()
    );
    let order57_edge = order57_continuation
        .state_after
        .beam_state
        .sig
        .edges
        .last()
        .expect("order57 appended HeadStem edge");
    let order57_support = order57_edge
        .support
        .as_ref()
        .expect("order57 HeadStem support");
    assert_eq!(order57_support.grade.to_bits(), 0x3fe919ad999159ae);
    let order57_payload = order57_edge
        .head_stem
        .as_ref()
        .expect("order57 HeadStem payload");
    assert_eq!(order57_payload.dx.to_bits(), 0x3fb38a22434ecf3d);
    assert_eq!(order57_payload.dy.to_bits(), 0x0000000000000000);
    assert_eq!(order57_payload.consistency.to_bits(), 0x3ff695c4ca037ba6);
    assert_eq!(
        order57_payload.extension_point.x.to_bits(),
        0x40989d96acc1ea24
    );
    assert_eq!(
        order57_payload.extension_point.y.to_bits(),
        0x4077200000000000
    );
    assert_eq!(order57_payload.head_side, NativeStemHeadSide::Left);

    // Boundary 84: order 58 reconciles an existing stem and closes x93
    // while the undefined LEFT side carried from order 50 stays unchanged.
    let order58_continuation = advance_native_stems_head_existing_stem_retry_order58(
        &order57_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order58 existing-stem reconciliation");
    assert_eq!(order58_continuation.processed_head.x_ordinal, 92);
    assert_eq!(order58_continuation.processed_head.sig_ordinal, 24);
    assert_eq!(order58_continuation.returned_linked, Some(true));
    assert_eq!(order58_continuation.closed_value_changes, 2);
    assert_eq!(order58_continuation.state_after.current_index, 59);
    assert!(order58_continuation.state_after.frontier_consumed);
    assert_eq!(
        order58_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (93, NativeStemHeadSide::Left),
            (93, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order58_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 85: order 59 reconciles an existing stem and closes x101
    // while the undefined LEFT side carried from order 50 stays unchanged.
    let order59_continuation = advance_native_stems_head_existing_stem_retry_order59(
        &order58_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order59 existing-stem reconciliation");
    assert_eq!(order59_continuation.processed_head.x_ordinal, 100);
    assert_eq!(order59_continuation.processed_head.sig_ordinal, 42);
    assert_eq!(order59_continuation.returned_linked, Some(true));
    assert_eq!(order59_continuation.closed_value_changes, 2);
    assert_eq!(order59_continuation.state_after.current_index, 60);
    assert!(order59_continuation.state_after.frontier_consumed);
    assert_eq!(
        order59_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (101, NativeStemHeadSide::Left),
            (101, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order59_continuation.state_after.undefined_sides,
        vec![NativeStemsBeamHeadSLinkerRef {
            head: order50_continuation.processed_head,
            horizontal: NativeStemHeadSide::Left,
        }]
    );

    // Boundary 86: order 60 is a second authenticated open/undefined
    // frontier.  The existing StemInter 2382 is reused without any graph or
    // linker write; Java records a second undefined LEFT side and advances
    // with a false return.
    let order60_continuation = advance_native_stems_head_open_frontier_order60(
        &order59_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order60 open frontier");
    assert_eq!(order60_continuation.processed_head.x_ordinal, 71);
    assert_eq!(order60_continuation.processed_head.sig_ordinal, 49);
    assert_eq!(order60_continuation.returned_linked, Some(false));
    assert_eq!(order60_continuation.closed_value_changes, 0);
    assert!(order60_continuation.closed_s_linkers.is_empty());
    assert_eq!(order60_continuation.state_after.current_index, 61);
    assert!(order60_continuation.state_after.frontier_consumed);
    assert_eq!(
        order60_continuation.state_after.unlinked_heads,
        vec![
            order50_continuation.processed_head,
            order60_continuation.processed_head
        ]
    );
    assert_eq!(
        order60_continuation.state_after.undefined_sides,
        vec![
            NativeStemsBeamHeadSLinkerRef {
                head: order50_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order60_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            }
        ]
    );
    assert_eq!(
        order60_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![
            (NativeStemHeadSide::Left, Some(true), Some(true),),
            (NativeStemHeadSide::Right, Some(true), Some(false),),
        ]
    );

    // Boundary 87: order 61 is a third authenticated open/undefined
    // frontier against the same existing StemInter 2382; Java records a
    // third undefined LEFT side and advances with a false return.
    let order61_continuation = advance_native_stems_head_open_frontier_order61(
        &order60_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order61 open frontier");
    assert_eq!(order61_continuation.processed_head.x_ordinal, 70);
    assert_eq!(order61_continuation.processed_head.sig_ordinal, 46);
    assert_eq!(order61_continuation.returned_linked, Some(false));
    assert_eq!(order61_continuation.closed_value_changes, 0);
    assert!(order61_continuation.closed_s_linkers.is_empty());
    assert_eq!(order61_continuation.state_after.current_index, 62);
    assert!(order61_continuation.state_after.frontier_consumed);
    assert_eq!(
        order61_continuation.state_after.unlinked_heads,
        vec![
            order50_continuation.processed_head,
            order60_continuation.processed_head,
            order61_continuation.processed_head
        ]
    );
    assert_eq!(
        order61_continuation.state_after.undefined_sides,
        vec![
            NativeStemsBeamHeadSLinkerRef {
                head: order50_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order60_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order61_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            }
        ]
    );
    assert_eq!(
        order61_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![
            (NativeStemHeadSide::Left, Some(true), Some(true),),
            (NativeStemHeadSide::Right, Some(true), Some(false),),
        ]
    );

    // Boundary 88: order 62 reconciles an existing stem and closes x10
    // while the three undefined LEFT sides carried from orders 50, 60, and
    // 61 stay unchanged.
    let order62_continuation = advance_native_stems_head_existing_stem_retry_order62(
        &order61_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order62 existing-stem reconciliation");
    assert_eq!(order62_continuation.processed_head.x_ordinal, 9);
    assert_eq!(order62_continuation.processed_head.sig_ordinal, 8);
    assert_eq!(order62_continuation.returned_linked, Some(true));
    assert_eq!(order62_continuation.closed_value_changes, 2);
    assert_eq!(order62_continuation.state_after.current_index, 63);
    assert!(order62_continuation.state_after.frontier_consumed);
    assert_eq!(
        order62_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (10, NativeStemHeadSide::Left),
            (10, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order62_continuation.state_after.undefined_sides,
        vec![
            NativeStemsBeamHeadSLinkerRef {
                head: order50_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order60_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order61_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            }
        ]
    );

    // Boundary 89: order 63 reconciles an existing stem and closes x42
    // while the three carried undefined LEFT sides stay unchanged.
    let order63_continuation = advance_native_stems_head_existing_stem_retry_order63(
        &order62_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order63 existing-stem reconciliation");
    assert_eq!(order63_continuation.processed_head.x_ordinal, 41);
    assert_eq!(order63_continuation.processed_head.sig_ordinal, 92);
    assert_eq!(order63_continuation.returned_linked, Some(true));
    assert_eq!(order63_continuation.closed_value_changes, 2);
    assert_eq!(order63_continuation.state_after.current_index, 64);
    assert!(order63_continuation.state_after.frontier_consumed);
    assert_eq!(
        order63_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (42, NativeStemHeadSide::Left),
            (42, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order63_continuation.state_after.undefined_sides,
        order62_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order63_continuation.state_after.unlinked_heads,
        vec![
            order50_continuation.processed_head,
            order60_continuation.processed_head,
            order61_continuation.processed_head
        ]
    );

    // Boundary 90: order 64 reconciles an existing stem and closes x4
    // while the three carried undefined LEFT sides and the phase-2 queue
    // stay unchanged.
    let order64_continuation = advance_native_stems_head_existing_stem_retry_order64(
        &order63_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order64 existing-stem reconciliation");
    assert_eq!(order64_continuation.processed_head.x_ordinal, 3);
    assert_eq!(order64_continuation.processed_head.sig_ordinal, 6);
    assert_eq!(order64_continuation.returned_linked, Some(true));
    assert_eq!(order64_continuation.closed_value_changes, 2);
    assert_eq!(order64_continuation.state_after.current_index, 65);
    assert!(order64_continuation.state_after.frontier_consumed);
    assert_eq!(
        order64_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (4, NativeStemHeadSide::Left),
            (4, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order64_continuation.state_after.undefined_sides,
        order63_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order64_continuation.state_after.unlinked_heads,
        order63_continuation.state_after.unlinked_heads
    );

    // Boundary 91: order 65 reconciles an existing stem and closes x59
    // while the three carried undefined LEFT sides and the phase-2 queue
    // stay unchanged.
    let order65_continuation = advance_native_stems_head_existing_stem_retry_order65(
        &order64_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order65 existing-stem reconciliation");
    assert_eq!(order65_continuation.processed_head.x_ordinal, 58);
    assert_eq!(order65_continuation.processed_head.sig_ordinal, 73);
    assert_eq!(order65_continuation.returned_linked, Some(true));
    assert_eq!(order65_continuation.closed_value_changes, 2);
    assert_eq!(order65_continuation.state_after.current_index, 66);
    assert!(order65_continuation.state_after.frontier_consumed);
    assert_eq!(
        order65_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (59, NativeStemHeadSide::Left),
            (59, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order65_continuation.state_after.undefined_sides,
        order64_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order65_continuation.state_after.unlinked_heads,
        order64_continuation.state_after.unlinked_heads
    );

    // Boundary 92: order 66 reconciles an existing stem and closes x14
    // while the three carried undefined LEFT sides and the phase-2 queue
    // stay unchanged.
    let order66_continuation = advance_native_stems_head_existing_stem_retry_order66(
        &order65_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order66 existing-stem reconciliation");
    assert_eq!(order66_continuation.processed_head.x_ordinal, 13);
    assert_eq!(order66_continuation.processed_head.sig_ordinal, 0);
    assert_eq!(order66_continuation.returned_linked, Some(true));
    assert_eq!(order66_continuation.closed_value_changes, 2);
    assert_eq!(order66_continuation.state_after.current_index, 67);
    assert!(order66_continuation.state_after.frontier_consumed);
    assert_eq!(
        order66_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (14, NativeStemHeadSide::Left),
            (14, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order66_continuation.state_after.undefined_sides,
        order65_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order66_continuation.state_after.unlinked_heads,
        order65_continuation.state_after.unlinked_heads
    );

    // Boundary 93: order 67's both-open LEFT BottomOnly frontier expands
    // through the carried undef heads x70 and x71, reuses existing Stem
    // 2382 through three appended HeadStem relations, links all three LEFT
    // cells, and closes stem-sharing x70/x71/x74, without vertex,
    // allocator, ID, registry, or system-stem mutation.
    let order67_continuation = advance_native_stems_head_multi_head_reuse_c_link_order67(
        &order66_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order67 multi-head reuse C-link");
    assert_eq!(order67_continuation.processed_head.x_ordinal, 73);
    assert_eq!(order67_continuation.processed_head.sig_ordinal, 18);
    assert_eq!(order67_continuation.returned_linked, Some(true));
    assert_eq!(order67_continuation.closed_value_changes, 6);
    assert_eq!(order67_continuation.state_after.current_index, 68);
    assert!(order67_continuation.state_after.frontier_consumed);
    assert_eq!(
        order67_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![(NativeStemHeadSide::Left, Some(false), Some(true))]
    );
    assert_eq!(
        order67_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (74, NativeStemHeadSide::Left),
            (74, NativeStemHeadSide::Right),
            (70, NativeStemHeadSide::Left),
            (70, NativeStemHeadSide::Right),
            (71, NativeStemHeadSide::Left),
            (71, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order67_continuation.state_after.undefined_sides,
        order66_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order67_continuation.state_after.unlinked_heads,
        order66_continuation.state_after.unlinked_heads
    );
    assert_eq!(
        order67_continuation.state_after.beam_state.sig.edges.len(),
        order66_continuation.state_after.beam_state.sig.edges.len() + 3
    );
    assert_eq!(
        order67_continuation
            .state_after
            .beam_state
            .sig
            .vertices
            .len(),
        order66_continuation
            .state_after
            .beam_state
            .sig
            .vertices
            .len()
    );
    let order67_edges = &order67_continuation.state_after.beam_state.sig.edges;
    let order67_new = &order67_edges[order67_edges.len() - 3..];
    let order67_expected = [
        (
            18usize,
            0x3fea0061ea4a6cae_u64,
            0x3fb11fb0e88f9862_u64,
            0x409b026199f3b0e2_u64,
            0x4077300000000000_u64,
        ),
        (
            46,
            0x3fec8e766ba61044,
            0x3fa418455632b6db,
            0x409b00b4049fdbae,
            0x407a800000000000,
        ),
        (
            49,
            0x3fec8e766ba61044,
            0x3fa418455632b6db,
            0x409b00b4049fdbae,
            0x407a800000000000,
        ),
    ];
    for (edge, (sig_ordinal, grade, dx, ext_x, ext_y)) in order67_new.iter().zip(order67_expected) {
        let support = edge.support.as_ref().expect("order67 HeadStem support");
        assert_eq!(support.grade.to_bits(), grade);
        let payload = edge.head_stem.as_ref().expect("order67 HeadStem payload");
        assert_eq!(payload.dx.to_bits(), dx);
        assert_eq!(payload.dy.to_bits(), 0);
        assert_eq!(payload.consistency.to_bits(), 0x3ff60a72f0539783);
        assert_eq!(payload.extension_point.x.to_bits(), ext_x);
        assert_eq!(payload.extension_point.y.to_bits(), ext_y);
        assert_eq!(payload.head_side, NativeStemHeadSide::Left);
        assert!(matches!(
            edge.origin,
            NativeSigRelationOrigin::HeadCLinkDraft {
                head_sig_ordinal, ..
            } if head_sig_ordinal == sig_ordinal
        ));
    }

    // Boundary 94: order 68 is a fourth authenticated open/undefined
    // frontier.  The existing StemInter 2384 is reused without any graph or
    // linker write; Java records a fourth undefined LEFT side and advances
    // with a false return.
    let order68_continuation = advance_native_stems_head_open_frontier_order68(
        &order67_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order68 open frontier");
    assert_eq!(order68_continuation.processed_head.x_ordinal, 0);
    assert_eq!(order68_continuation.processed_head.sig_ordinal, 51);
    assert_eq!(order68_continuation.returned_linked, Some(false));
    assert_eq!(order68_continuation.closed_value_changes, 0);
    assert!(order68_continuation.closed_s_linkers.is_empty());
    assert_eq!(order68_continuation.state_after.current_index, 69);
    assert!(order68_continuation.state_after.frontier_consumed);
    assert_eq!(
        order68_continuation.state_after.undefined_sides,
        vec![
            NativeStemsBeamHeadSLinkerRef {
                head: order50_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order60_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order61_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order68_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            }
        ]
    );
    assert_eq!(
        order68_continuation.state_after.unlinked_heads,
        vec![
            order50_continuation.processed_head,
            order60_continuation.processed_head,
            order61_continuation.processed_head,
            order68_continuation.processed_head
        ]
    );
    assert_eq!(
        order68_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![
            (NativeStemHeadSide::Left, Some(true), Some(true),),
            (NativeStemHeadSide::Right, Some(false), Some(false),),
        ]
    );

    // Boundary 95: order 69 reconciles an existing stem and closes x88
    // while the four carried undefined LEFT sides and the phase-2 queue
    // stay unchanged.
    let order69_continuation = advance_native_stems_head_existing_stem_retry_order69(
        &order68_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order69 existing-stem reconciliation");
    assert_eq!(order69_continuation.processed_head.x_ordinal, 87);
    assert_eq!(order69_continuation.processed_head.sig_ordinal, 83);
    assert_eq!(order69_continuation.returned_linked, Some(true));
    assert_eq!(order69_continuation.closed_value_changes, 2);
    assert_eq!(order69_continuation.state_after.current_index, 70);
    assert!(order69_continuation.state_after.frontier_consumed);
    assert_eq!(
        order69_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (88, NativeStemHeadSide::Left),
            (88, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order69_continuation.state_after.undefined_sides,
        order68_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order69_continuation.state_after.unlinked_heads,
        order68_continuation.state_after.unlinked_heads
    );

    // Boundary 96: order 70's both-open LEFT BottomOnly frontier walks the
    // frontier chunk plus the carried undef head x0, reuses existing Stem
    // 2384 through two appended HeadStem relations, links both LEFT cells,
    // and closes stem-sharing x0 and x2.  The carried undef list and the
    // phase-2 queue stay unchanged even though x0 is now linked: Java never
    // retracts an undef entry.
    let order70_continuation = advance_native_stems_head_multi_head_reuse_c_link_order70(
        &order69_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order70 multi-head reuse C-link");
    assert_eq!(order70_continuation.processed_head.x_ordinal, 1);
    assert_eq!(order70_continuation.processed_head.sig_ordinal, 35);
    assert_eq!(order70_continuation.returned_linked, Some(true));
    assert_eq!(order70_continuation.closed_value_changes, 4);
    assert_eq!(order70_continuation.state_after.current_index, 71);
    assert!(order70_continuation.state_after.frontier_consumed);
    assert_eq!(
        order70_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![(NativeStemHeadSide::Left, Some(false), Some(true))]
    );
    assert_eq!(
        order70_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (2, NativeStemHeadSide::Left),
            (2, NativeStemHeadSide::Right),
            (0, NativeStemHeadSide::Left),
            (0, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order70_continuation.state_after.undefined_sides,
        order69_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order70_continuation.state_after.unlinked_heads,
        order69_continuation.state_after.unlinked_heads
    );

    // Boundary 97: order 71 reconciles an existing stem and closes x78
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order71_continuation = advance_native_stems_head_existing_stem_retry_order71(
        &order70_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order71 existing-stem reconciliation");
    assert_eq!(order71_continuation.processed_head.x_ordinal, 77);
    assert_eq!(order71_continuation.processed_head.sig_ordinal, 38);
    assert_eq!(order71_continuation.returned_linked, Some(true));
    assert_eq!(order71_continuation.closed_value_changes, 2);
    assert_eq!(order71_continuation.state_after.current_index, 72);
    assert!(order71_continuation.state_after.frontier_consumed);
    assert_eq!(
        order71_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (78, NativeStemHeadSide::Left),
            (78, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order71_continuation.state_after.undefined_sides,
        order70_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order71_continuation.state_after.unlinked_heads,
        order70_continuation.state_after.unlinked_heads
    );

    // Boundary 98: order 72's both-open LEFT BottomOnly frontier resolves
    // its seed directly to existing Stem 2385 with no chunk and no crossed
    // head, appends one HeadStem relation, links x26's LEFT cell, and
    // closes stem-sharing x23.
    let order72_continuation = advance_native_stems_head_single_head_reuse_c_link_order72(
        &order71_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order72 single-head reuse C-link");
    assert_eq!(order72_continuation.processed_head.x_ordinal, 26);
    assert_eq!(order72_continuation.processed_head.sig_ordinal, 13);
    assert_eq!(order72_continuation.returned_linked, Some(true));
    assert_eq!(order72_continuation.closed_value_changes, 2);
    assert_eq!(order72_continuation.state_after.current_index, 73);
    assert!(order72_continuation.state_after.frontier_consumed);
    assert_eq!(
        order72_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (23, NativeStemHeadSide::Left),
            (23, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order72_continuation.state_after.undefined_sides,
        order71_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order72_continuation.state_after.unlinked_heads,
        order71_continuation.state_after.unlinked_heads
    );
    assert_eq!(
        order72_continuation.state_after.beam_state.sig.edges.len(),
        order71_continuation.state_after.beam_state.sig.edges.len() + 1
    );
    let order72_edge = order72_continuation
        .state_after
        .beam_state
        .sig
        .edges
        .last()
        .expect("order72 appended HeadStem edge");
    assert_eq!(
        order72_edge
            .support
            .as_ref()
            .expect("order72 HeadStem support")
            .grade
            .to_bits(),
        0x3fe4c2ab27b5b4f9
    );
    let order72_payload = order72_edge
        .head_stem
        .as_ref()
        .expect("order72 HeadStem payload");
    assert_eq!(order72_payload.dx.to_bits(), 0x3fbe8e82cf670186);
    assert_eq!(order72_payload.dy.to_bits(), 0);
    assert_eq!(order72_payload.consistency.to_bits(), 0x3ff766bf908b51da);
    assert_eq!(
        order72_payload.extension_point.x.to_bits(),
        0x408b2bf27a27e467
    );
    assert_eq!(
        order72_payload.extension_point.y.to_bits(),
        0x4076e00000000000
    );
    assert_eq!(order72_payload.head_side, NativeStemHeadSide::Left);

    // Boundary 99: order 73's both-open LEFT BottomOnly frontier crosses
    // x72, whose stump is the same already-registered glyph 319, so Java's
    // glyph set stays a single entry.  Stem 2380 is reused through two
    // appended HeadStem relations; the transaction closes x76 and x72.
    let order73_continuation = advance_native_stems_head_multi_head_reuse_c_link_order73(
        &order72_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order73 multi-head reuse C-link");
    assert_eq!(order73_continuation.processed_head.x_ordinal, 75);
    assert_eq!(order73_continuation.processed_head.sig_ordinal, 96);
    assert_eq!(order73_continuation.returned_linked, Some(true));
    assert_eq!(order73_continuation.closed_value_changes, 4);
    assert_eq!(order73_continuation.state_after.current_index, 74);
    assert!(order73_continuation.state_after.frontier_consumed);
    assert_eq!(
        order73_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (76, NativeStemHeadSide::Left),
            (76, NativeStemHeadSide::Right),
            (72, NativeStemHeadSide::Left),
            (72, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order73_continuation.state_after.undefined_sides,
        order72_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order73_continuation.state_after.unlinked_heads,
        order72_continuation.state_after.unlinked_heads
    );
    assert_eq!(
        order73_continuation.state_after.beam_state.sig.edges.len(),
        order72_continuation.state_after.beam_state.sig.edges.len() + 2
    );

    // Boundary 100: order 74 reconciles an existing stem and closes x50
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order74_continuation = advance_native_stems_head_existing_stem_retry_order74(
        &order73_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order74 existing-stem reconciliation");
    assert_eq!(order74_continuation.processed_head.x_ordinal, 49);
    assert_eq!(order74_continuation.processed_head.sig_ordinal, 71);
    assert_eq!(order74_continuation.returned_linked, Some(true));
    assert_eq!(order74_continuation.closed_value_changes, 2);
    assert_eq!(order74_continuation.state_after.current_index, 75);
    assert!(order74_continuation.state_after.frontier_consumed);
    assert_eq!(
        order74_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (50, NativeStemHeadSide::Left),
            (50, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order74_continuation.state_after.undefined_sides,
        order73_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order74_continuation.state_after.unlinked_heads,
        order73_continuation.state_after.unlinked_heads
    );
    let order73_edges = &order73_continuation.state_after.beam_state.sig.edges;
    let order73_new = &order73_edges[order73_edges.len() - 2..];
    let order73_expected = [
        (
            96usize,
            0x3fe880946d87171e_u64,
            0x3fb51e7a5e2f4925_u64,
            0x409b0511ffd9187c_u64,
            0x4081_8800_0000_0000_u64,
        ),
        (
            101,
            0x3fef0dceb81e3185,
            0x3f868971b5a90c31,
            0x409b03135cd60c91,
            0x4083800000000000,
        ),
    ];
    for (edge, (sig_ordinal, grade, dx, ext_x, ext_y)) in order73_new.iter().zip(order73_expected) {
        let support = edge.support.as_ref().expect("order73 HeadStem support");
        assert_eq!(support.grade.to_bits(), grade);
        let payload = edge.head_stem.as_ref().expect("order73 HeadStem payload");
        assert_eq!(payload.dx.to_bits(), dx);
        assert_eq!(payload.dy.to_bits(), 0);
        assert_eq!(payload.consistency.to_bits(), 0x3ff837ba5713280e);
        assert_eq!(payload.extension_point.x.to_bits(), ext_x);
        assert_eq!(payload.extension_point.y.to_bits(), ext_y);
        assert_eq!(payload.head_side, NativeStemHeadSide::Left);
        assert!(matches!(
            edge.origin,
            NativeSigRelationOrigin::HeadCLinkDraft {
                head_sig_ordinal, ..
            } if head_sig_ordinal == sig_ordinal
        ));
    }
    assert_eq!(
        order70_continuation.state_after.beam_state.sig.edges.len(),
        order69_continuation.state_after.beam_state.sig.edges.len() + 2
    );
    assert_eq!(
        order70_continuation
            .state_after
            .beam_state
            .sig
            .vertices
            .len(),
        order69_continuation
            .state_after
            .beam_state
            .sig
            .vertices
            .len()
    );
    let order70_edges = &order70_continuation.state_after.beam_state.sig.edges;
    let order70_new = &order70_edges[order70_edges.len() - 2..];
    let order70_expected = [
        (
            35usize,
            0x3fe935961f369da6_u64,
            0x3fb33fe6ec8e0492_u64,
            0x4078a6bc20e9859a_u64,
            0x4078000000000000_u64,
        ),
        (
            51,
            0x3febcd262ddce1b1,
            0x3fa858b15e2f90c3,
            0x4078a005cb9a30c9,
            0x407b500000000000,
        ),
    ];
    for (edge, (sig_ordinal, grade, dx, ext_x, ext_y)) in order70_new.iter().zip(order70_expected) {
        let support = edge.support.as_ref().expect("order70 HeadStem support");
        assert_eq!(support.grade.to_bits(), grade);
        let payload = edge.head_stem.as_ref().expect("order70 HeadStem payload");
        assert_eq!(payload.dx.to_bits(), dx);
        assert_eq!(payload.dy.to_bits(), 0);
        assert_eq!(payload.consistency.to_bits(), 0x3ff766bf908b51da);
        assert_eq!(payload.extension_point.x.to_bits(), ext_x);
        assert_eq!(payload.extension_point.y.to_bits(), ext_y);
        assert_eq!(payload.head_side, NativeStemHeadSide::Left);
        assert!(matches!(
            edge.origin,
            NativeSigRelationOrigin::HeadCLinkDraft {
                head_sig_ordinal, ..
            } if head_sig_ordinal == sig_ordinal
        ));
    }

    // Boundary 101: order 75 is a fifth authenticated open/undefined
    // frontier.  The existing StemInter 2383 is reused without any graph or
    // linker write; Java reports LEFT Both and RIGHT TopOnly, records a fifth
    // undefined LEFT side, and advances with a false return.
    let order75_continuation = advance_native_stems_head_open_frontier_order75(
        &order74_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order75 open frontier");
    assert_eq!(order75_continuation.processed_head.x_ordinal, 31);
    assert_eq!(order75_continuation.processed_head.sig_ordinal, 47);
    assert_eq!(order75_continuation.returned_linked, Some(false));
    assert_eq!(order75_continuation.closed_value_changes, 0);
    assert!(order75_continuation.closed_s_linkers.is_empty());
    assert_eq!(order75_continuation.state_after.current_index, 76);
    assert!(order75_continuation.state_after.frontier_consumed);
    assert_eq!(
        order75_continuation.state_after.undefined_sides,
        vec![
            NativeStemsBeamHeadSLinkerRef {
                head: order50_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order60_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order61_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order68_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            },
            NativeStemsBeamHeadSLinkerRef {
                head: order75_continuation.processed_head,
                horizontal: NativeStemHeadSide::Left,
            }
        ]
    );
    assert_eq!(
        order75_continuation.state_after.unlinked_heads,
        vec![
            order50_continuation.processed_head,
            order60_continuation.processed_head,
            order61_continuation.processed_head,
            order68_continuation.processed_head,
            order75_continuation.processed_head
        ]
    );
    assert_eq!(
        order75_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![
            (NativeStemHeadSide::Left, Some(true), Some(true),),
            (NativeStemHeadSide::Right, Some(true), Some(false),),
        ]
    );

    // Boundary 102: order 76 reconciles an existing stem and closes x67
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order76_continuation = advance_native_stems_head_existing_stem_retry_order76(
        &order75_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order76 existing-stem reconciliation");
    assert_eq!(order76_continuation.processed_head.x_ordinal, 66);
    assert_eq!(order76_continuation.processed_head.sig_ordinal, 58);
    assert_eq!(order76_continuation.returned_linked, Some(true));
    assert_eq!(order76_continuation.closed_value_changes, 2);
    assert_eq!(order76_continuation.state_after.current_index, 77);
    assert!(order76_continuation.state_after.frontier_consumed);
    assert_eq!(
        order76_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (67, NativeStemHeadSide::Left),
            (67, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order76_continuation.state_after.undefined_sides,
        order75_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order76_continuation.state_after.unlinked_heads,
        order75_continuation.state_after.unlinked_heads
    );

    // Boundary 103: order 77 reconciles an existing stem and closes x65
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order77_continuation = advance_native_stems_head_existing_stem_retry_order77(
        &order76_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order77 existing-stem reconciliation");
    assert_eq!(order77_continuation.processed_head.x_ordinal, 64);
    assert_eq!(order77_continuation.processed_head.sig_ordinal, 94);
    assert_eq!(order77_continuation.returned_linked, Some(true));
    assert_eq!(order77_continuation.closed_value_changes, 2);
    assert_eq!(order77_continuation.state_after.current_index, 78);
    assert!(order77_continuation.state_after.frontier_consumed);
    assert_eq!(
        order77_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (65, NativeStemHeadSide::Left),
            (65, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order77_continuation.state_after.undefined_sides,
        order76_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order77_continuation.state_after.unlinked_heads,
        order76_continuation.state_after.unlinked_heads
    );

    // Boundary 104: order 78 reconciles an existing stem and closes x83
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order78_continuation = advance_native_stems_head_existing_stem_retry_order78(
        &order77_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order78 existing-stem reconciliation");
    assert_eq!(order78_continuation.processed_head.x_ordinal, 82);
    assert_eq!(order78_continuation.processed_head.sig_ordinal, 20);
    assert_eq!(order78_continuation.returned_linked, Some(true));
    assert_eq!(order78_continuation.closed_value_changes, 2);
    assert_eq!(order78_continuation.state_after.current_index, 79);
    assert!(order78_continuation.state_after.frontier_consumed);
    assert_eq!(
        order78_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (83, NativeStemHeadSide::Left),
            (83, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order78_continuation.state_after.undefined_sides,
        order77_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order78_continuation.state_after.unlinked_heads,
        order77_continuation.state_after.unlinked_heads
    );

    // Boundary 105: order 79 reconciles an existing stem and closes x18
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order79_continuation = advance_native_stems_head_existing_stem_retry_order79(
        &order78_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order79 existing-stem reconciliation");
    assert_eq!(order79_continuation.processed_head.x_ordinal, 17);
    assert_eq!(order79_continuation.processed_head.sig_ordinal, 10);
    assert_eq!(order79_continuation.returned_linked, Some(true));
    assert_eq!(order79_continuation.closed_value_changes, 2);
    assert_eq!(order79_continuation.state_after.current_index, 80);
    assert!(order79_continuation.state_after.frontier_consumed);
    assert_eq!(
        order79_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (18, NativeStemHeadSide::Left),
            (18, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order79_continuation.state_after.undefined_sides,
        order78_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order79_continuation.state_after.unlinked_heads,
        order78_continuation.state_after.unlinked_heads
    );

    // Boundary 106: order 80 reconciles an existing stem and closes x30
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order80_continuation = advance_native_stems_head_existing_stem_retry_order80(
        &order79_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order80 existing-stem reconciliation");
    assert_eq!(order80_continuation.processed_head.x_ordinal, 29);
    assert_eq!(order80_continuation.processed_head.sig_ordinal, 66);
    assert_eq!(order80_continuation.returned_linked, Some(true));
    assert_eq!(order80_continuation.closed_value_changes, 2);
    assert_eq!(order80_continuation.state_after.current_index, 81);
    assert!(order80_continuation.state_after.frontier_consumed);
    assert_eq!(
        order80_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (30, NativeStemHeadSide::Left),
            (30, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order80_continuation.state_after.undefined_sides,
        order79_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order80_continuation.state_after.unlinked_heads,
        order79_continuation.state_after.unlinked_heads
    );

    // Boundary 107: order 81 reconciles an existing stem and closes x99
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order81_continuation = advance_native_stems_head_existing_stem_retry_order81(
        &order80_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order81 existing-stem reconciliation");
    assert_eq!(order81_continuation.processed_head.x_ordinal, 98);
    assert_eq!(order81_continuation.processed_head.sig_ordinal, 60);
    assert_eq!(order81_continuation.returned_linked, Some(true));
    assert_eq!(order81_continuation.closed_value_changes, 2);
    assert_eq!(order81_continuation.state_after.current_index, 82);
    assert!(order81_continuation.state_after.frontier_consumed);
    assert_eq!(
        order81_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (99, NativeStemHeadSide::Left),
            (99, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order81_continuation.state_after.undefined_sides,
        order80_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order81_continuation.state_after.unlinked_heads,
        order80_continuation.state_after.unlinked_heads
    );

    // Boundary 108: order 82 reconciles the three-head existing Stem 2371,
    // re-writing x79's already-closed cells without a value change and closing
    // x81
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order82_continuation = advance_native_stems_head_existing_stem_retry_order82(
        &order81_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order82 existing-stem reconciliation");
    assert_eq!(order82_continuation.processed_head.x_ordinal, 80);
    assert_eq!(order82_continuation.processed_head.sig_ordinal, 32);
    assert_eq!(order82_continuation.returned_linked, Some(true));
    assert_eq!(order82_continuation.closed_value_changes, 2);
    assert_eq!(order82_continuation.state_after.current_index, 83);
    assert!(order82_continuation.state_after.frontier_consumed);
    assert_eq!(
        order82_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (79, NativeStemHeadSide::Left),
            (79, NativeStemHeadSide::Right),
            (81, NativeStemHeadSide::Left),
            (81, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order82_continuation.state_after.undefined_sides,
        order81_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order82_continuation.state_after.unlinked_heads,
        order81_continuation.state_after.unlinked_heads
    );

    // Boundary 109: order 83 reconciles an existing stem and closes x25
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order83_continuation = advance_native_stems_head_existing_stem_retry_order83(
        &order82_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order83 existing-stem reconciliation");
    assert_eq!(order83_continuation.processed_head.x_ordinal, 24);
    assert_eq!(order83_continuation.processed_head.sig_ordinal, 90);
    assert_eq!(order83_continuation.returned_linked, Some(true));
    assert_eq!(order83_continuation.closed_value_changes, 2);
    assert_eq!(order83_continuation.state_after.current_index, 84);
    assert!(order83_continuation.state_after.frontier_consumed);
    assert_eq!(
        order83_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (25, NativeStemHeadSide::Left),
            (25, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order83_continuation.state_after.undefined_sides,
        order82_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order83_continuation.state_after.unlinked_heads,
        order82_continuation.state_after.unlinked_heads
    );

    // Boundary 110: order 84 reconciles the three-head existing Stem 2364,
    // re-writing x91's already-closed cells without a value change and closing
    // x95
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order84_continuation = advance_native_stems_head_existing_stem_retry_order84(
        &order83_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order84 existing-stem reconciliation");
    assert_eq!(order84_continuation.processed_head.x_ordinal, 94);
    assert_eq!(order84_continuation.processed_head.sig_ordinal, 99);
    assert_eq!(order84_continuation.returned_linked, Some(true));
    assert_eq!(order84_continuation.closed_value_changes, 2);
    assert_eq!(order84_continuation.state_after.current_index, 85);
    assert!(order84_continuation.state_after.frontier_consumed);
    assert_eq!(
        order84_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (91, NativeStemHeadSide::Left),
            (91, NativeStemHeadSide::Right),
            (95, NativeStemHeadSide::Left),
            (95, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order84_continuation.state_after.undefined_sides,
        order83_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order84_continuation.state_after.unlinked_heads,
        order83_continuation.state_after.unlinked_heads
    );

    // Boundary 111: order 85 reconciles three-head Stem 2371 whose siblings
    // x80 and x81 were already closed at Boundary 108, so every write is a
    // no-op
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order85_continuation = advance_native_stems_head_existing_stem_retry_order85(
        &order84_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order85 existing-stem reconciliation");
    assert_eq!(order85_continuation.processed_head.x_ordinal, 79);
    assert_eq!(order85_continuation.processed_head.sig_ordinal, 40);
    assert_eq!(order85_continuation.returned_linked, Some(true));
    assert_eq!(order85_continuation.closed_value_changes, 0);
    assert_eq!(order85_continuation.state_after.current_index, 86);
    assert!(order85_continuation.state_after.frontier_consumed);
    assert_eq!(
        order85_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (80, NativeStemHeadSide::Left),
            (80, NativeStemHeadSide::Right),
            (81, NativeStemHeadSide::Left),
            (81, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order85_continuation.state_after.undefined_sides,
        order84_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order85_continuation.state_after.unlinked_heads,
        order84_continuation.state_after.unlinked_heads
    );

    // Boundary 112: order 86 reconciles the three-head existing Stem 2362,
    // re-writing x54's already-closed cells without a value change and closing
    // x55
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order86_continuation = advance_native_stems_head_existing_stem_retry_order86(
        &order85_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order86 existing-stem reconciliation");
    assert_eq!(order86_continuation.processed_head.x_ordinal, 51);
    assert_eq!(order86_continuation.processed_head.sig_ordinal, 82);
    assert_eq!(order86_continuation.returned_linked, Some(true));
    assert_eq!(order86_continuation.closed_value_changes, 2);
    assert_eq!(order86_continuation.state_after.current_index, 87);
    assert!(order86_continuation.state_after.frontier_consumed);
    assert_eq!(
        order86_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (54, NativeStemHeadSide::Left),
            (54, NativeStemHeadSide::Right),
            (55, NativeStemHeadSide::Left),
            (55, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order86_continuation.state_after.undefined_sides,
        order85_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order86_continuation.state_after.unlinked_heads,
        order85_continuation.state_after.unlinked_heads
    );

    // Boundary 113: order 87 reconciles the three-head existing Stem 2377,
    // re-writing x44's already-closed cells without a value change and closing
    // x46
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order87_continuation = advance_native_stems_head_existing_stem_retry_order87(
        &order86_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order87 existing-stem reconciliation");
    assert_eq!(order87_continuation.processed_head.x_ordinal, 45);
    assert_eq!(order87_continuation.processed_head.sig_ordinal, 56);
    assert_eq!(order87_continuation.returned_linked, Some(true));
    assert_eq!(order87_continuation.closed_value_changes, 2);
    assert_eq!(order87_continuation.state_after.current_index, 88);
    assert!(order87_continuation.state_after.frontier_consumed);
    assert_eq!(
        order87_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (44, NativeStemHeadSide::Left),
            (44, NativeStemHeadSide::Right),
            (46, NativeStemHeadSide::Left),
            (46, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order87_continuation.state_after.undefined_sides,
        order86_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order87_continuation.state_after.unlinked_heads,
        order86_continuation.state_after.unlinked_heads
    );

    // Boundary 114: order 88 reconciles Boundary 99's three-head Stem 2380,
    // re-writing x76's already-closed cells without a value change and closing
    // x75
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order88_continuation = advance_native_stems_head_existing_stem_retry_order88(
        &order87_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order88 existing-stem reconciliation");
    assert_eq!(order88_continuation.processed_head.x_ordinal, 72);
    assert_eq!(order88_continuation.processed_head.sig_ordinal, 101);
    assert_eq!(order88_continuation.returned_linked, Some(true));
    assert_eq!(order88_continuation.closed_value_changes, 2);
    assert_eq!(order88_continuation.state_after.current_index, 89);
    assert!(order88_continuation.state_after.frontier_consumed);
    assert_eq!(
        order88_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (76, NativeStemHeadSide::Left),
            (76, NativeStemHeadSide::Right),
            (75, NativeStemHeadSide::Left),
            (75, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order88_continuation.state_after.undefined_sides,
        order87_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order88_continuation.state_after.unlinked_heads,
        order87_continuation.state_after.unlinked_heads
    );

    // Boundary 115: order 89 reconciles an existing stem and closes x48
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order89_continuation = advance_native_stems_head_existing_stem_retry_order89(
        &order88_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order89 existing-stem reconciliation");
    assert_eq!(order89_continuation.processed_head.x_ordinal, 47);
    assert_eq!(order89_continuation.processed_head.sig_ordinal, 28);
    assert_eq!(order89_continuation.returned_linked, Some(true));
    assert_eq!(order89_continuation.closed_value_changes, 2);
    assert_eq!(order89_continuation.state_after.current_index, 90);
    assert!(order89_continuation.state_after.frontier_consumed);
    assert_eq!(
        order89_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (48, NativeStemHeadSide::Left),
            (48, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order89_continuation.state_after.undefined_sides,
        order88_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order89_continuation.state_after.unlinked_heads,
        order88_continuation.state_after.unlinked_heads
    );

    // Boundary 116: order 90 reconciles an existing stem and closes x28
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order90_continuation = advance_native_stems_head_existing_stem_retry_order90(
        &order89_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order90 existing-stem reconciliation");
    assert_eq!(order90_continuation.processed_head.x_ordinal, 27);
    assert_eq!(order90_continuation.processed_head.sig_ordinal, 54);
    assert_eq!(order90_continuation.returned_linked, Some(true));
    assert_eq!(order90_continuation.closed_value_changes, 2);
    assert_eq!(order90_continuation.state_after.current_index, 91);
    assert!(order90_continuation.state_after.frontier_consumed);
    assert_eq!(
        order90_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (28, NativeStemHeadSide::Left),
            (28, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order90_continuation.state_after.undefined_sides,
        order89_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order90_continuation.state_after.unlinked_heads,
        order89_continuation.state_after.unlinked_heads
    );

    // Boundary 117: order 91 reconciles three-head Stem 2364 whose siblings
    // x94 and x95 were already closed at Boundary 110, so every write is a
    // no-op
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order91_continuation = advance_native_stems_head_existing_stem_retry_order91(
        &order90_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order91 existing-stem reconciliation");
    assert_eq!(order91_continuation.processed_head.x_ordinal, 91);
    assert_eq!(order91_continuation.processed_head.sig_ordinal, 98);
    assert_eq!(order91_continuation.returned_linked, Some(true));
    assert_eq!(order91_continuation.closed_value_changes, 0);
    assert_eq!(order91_continuation.state_after.current_index, 92);
    assert!(order91_continuation.state_after.frontier_consumed);
    assert_eq!(
        order91_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (94, NativeStemHeadSide::Left),
            (94, NativeStemHeadSide::Right),
            (95, NativeStemHeadSide::Left),
            (95, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order91_continuation.state_after.undefined_sides,
        order90_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order91_continuation.state_after.unlinked_heads,
        order90_continuation.state_after.unlinked_heads
    );

    // Boundary 118: order 92 reconciles three-head Stem 2362 whose siblings
    // x51 and x55 were already closed at Boundary 112, so every write is a
    // no-op
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order92_continuation = advance_native_stems_head_existing_stem_retry_order92(
        &order91_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order92 existing-stem reconciliation");
    assert_eq!(order92_continuation.processed_head.x_ordinal, 54);
    assert_eq!(order92_continuation.processed_head.sig_ordinal, 78);
    assert_eq!(order92_continuation.returned_linked, Some(true));
    assert_eq!(order92_continuation.closed_value_changes, 0);
    assert_eq!(order92_continuation.state_after.current_index, 93);
    assert!(order92_continuation.state_after.frontier_consumed);
    assert_eq!(
        order92_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (51, NativeStemHeadSide::Left),
            (51, NativeStemHeadSide::Right),
            (55, NativeStemHeadSide::Left),
            (55, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order92_continuation.state_after.undefined_sides,
        order91_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order92_continuation.state_after.unlinked_heads,
        order91_continuation.state_after.unlinked_heads
    );

    // Boundary 119: order 93's both-open frontier links on the RIGHT side.
    // Java reports LEFT Neither and RIGHT TopOnly, reuses existing Stem 2379
    // through one appended RIGHT-side HeadStem relation, and closes x38.
    let order93_continuation = advance_native_stems_head_right_side_reuse_c_link_order93(
        &order92_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_reachability.systems[0],
        &checker_page.stem_seeds.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
        &checker,
        &bridge,
    )
    .expect("native order93 right-side reuse C-link");
    assert_eq!(order93_continuation.processed_head.x_ordinal, 37);
    assert_eq!(order93_continuation.processed_head.sig_ordinal, 44);
    assert_eq!(order93_continuation.returned_linked, Some(true));
    assert_eq!(order93_continuation.closed_value_changes, 2);
    assert_eq!(order93_continuation.state_after.current_index, 94);
    assert!(order93_continuation.state_after.frontier_consumed);
    assert_eq!(
        order93_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (38, NativeStemHeadSide::Left),
            (38, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order93_continuation
            .side_decisions
            .iter()
            .map(|decision| {
                (
                    decision.side,
                    decision.top_can_link,
                    decision.bottom_can_link,
                )
            })
            .collect::<Vec<_>>(),
        vec![
            (NativeStemHeadSide::Left, Some(false), Some(false),),
            (NativeStemHeadSide::Right, Some(true), Some(false),),
        ]
    );
    assert_eq!(
        order93_continuation.state_after.undefined_sides,
        order92_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order93_continuation.state_after.unlinked_heads,
        order92_continuation.state_after.unlinked_heads
    );
    assert_eq!(
        order93_continuation.state_after.beam_state.sig.edges.len(),
        order92_continuation.state_after.beam_state.sig.edges.len() + 1
    );
    let order93_edge = order93_continuation
        .state_after
        .beam_state
        .sig
        .edges
        .last()
        .expect("order93 appended edge");
    assert_eq!(
        order93_edge
            .support
            .as_ref()
            .expect("order93 HeadStem support")
            .grade
            .to_bits(),
        0x3fe4fedcec6ab042
    );
    let order93_payload = order93_edge
        .head_stem
        .as_ref()
        .expect("order93 HeadStem payload");
    assert_eq!(order93_payload.dx.to_bits(), 0x3fbdfca5caab4925);
    assert_eq!(order93_payload.dy.to_bits(), 0);
    assert_eq!(order93_payload.consistency.to_bits(), 0x3ff908b51d9afe43);
    assert_eq!(
        order93_payload.extension_point.x.to_bits(),
        0x4091d5d6e6668034
    );
    assert_eq!(
        order93_payload.extension_point.y.to_bits(),
        0x407a300000000000
    );
    assert_eq!(order93_payload.head_side, NativeStemHeadSide::Right);

    // Boundary 120: order 94 reconciles an existing stem and closes x97
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order94_continuation = advance_native_stems_head_existing_stem_retry_order94(
        &order93_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order94 existing-stem reconciliation");
    assert_eq!(order94_continuation.processed_head.x_ordinal, 96);
    assert_eq!(order94_continuation.processed_head.sig_ordinal, 41);
    assert_eq!(order94_continuation.returned_linked, Some(true));
    assert_eq!(order94_continuation.closed_value_changes, 2);
    assert_eq!(order94_continuation.state_after.current_index, 95);
    assert!(order94_continuation.state_after.frontier_consumed);
    assert_eq!(
        order94_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (97, NativeStemHeadSide::Left),
            (97, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order94_continuation.state_after.undefined_sides,
        order93_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order94_continuation.state_after.unlinked_heads,
        order93_continuation.state_after.unlinked_heads
    );

    // Boundary 121: order 95 reconciles an existing stem and closes x8
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order95_continuation = advance_native_stems_head_existing_stem_retry_order95(
        &order94_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order95 existing-stem reconciliation");
    assert_eq!(order95_continuation.processed_head.x_ordinal, 7);
    assert_eq!(order95_continuation.processed_head.sig_ordinal, 52);
    assert_eq!(order95_continuation.returned_linked, Some(true));
    assert_eq!(order95_continuation.closed_value_changes, 2);
    assert_eq!(order95_continuation.state_after.current_index, 96);
    assert!(order95_continuation.state_after.frontier_consumed);
    assert_eq!(
        order95_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (8, NativeStemHeadSide::Left),
            (8, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order95_continuation.state_after.undefined_sides,
        order94_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order95_continuation.state_after.unlinked_heads,
        order94_continuation.state_after.unlinked_heads
    );

    // Boundary 122: order 96 reconciles an existing stem and closes x61
    // while the carried undefined LEFT sides and the phase-2 queue stay
    // unchanged.
    let order96_continuation = advance_native_stems_head_existing_stem_retry_order96(
        &order95_continuation.state_after,
        &checker_page.head_corners.systems[0],
        &checker_page.head_builders.systems[0],
        &hydrated.plans,
    )
    .expect("native order96 existing-stem reconciliation");
    assert_eq!(order96_continuation.processed_head.x_ordinal, 60);
    assert_eq!(order96_continuation.processed_head.sig_ordinal, 30);
    assert_eq!(order96_continuation.returned_linked, Some(true));
    assert_eq!(order96_continuation.closed_value_changes, 2);
    assert_eq!(order96_continuation.state_after.current_index, 97);
    assert!(order96_continuation.state_after.frontier_consumed);
    assert_eq!(
        order96_continuation
            .closed_s_linkers
            .iter()
            .map(|cell| (cell.head.x_ordinal, cell.horizontal))
            .collect::<Vec<_>>(),
        vec![
            (61, NativeStemHeadSide::Left),
            (61, NativeStemHeadSide::Right)
        ]
    );
    assert_eq!(
        order96_continuation.state_after.undefined_sides,
        order95_continuation.state_after.undefined_sides
    );
    assert_eq!(
        order96_continuation.state_after.unlinked_heads,
        order95_continuation.state_after.unlinked_heads
    );

    // The authenticated order18 wrapper must fail closed without mutating
    // the carrier when its queue index is tampered with.
    let mut invalid_order18 = order18_frontier_before.clone();
    invalid_order18.current_index = 17;
    let invalid_order18_before = invalid_order18.clone();
    assert!(
        advance_native_stems_head_continuation_c_link_order18(
            &mut invalid_order18,
            &checker_page.head_corners.systems[0],
            &checker_page.head_reachability.systems[0],
            &checker_page.stem_seeds.systems[0],
            &checker_page.head_builders.systems[0],
            &hydrated.plans,
            &checker,
            &bridge,
        )
        .is_err()
    );
    assert_eq!(invalid_order18, invalid_order18_before);

    let mut invalid_order7 = seventh_frontier_before.clone();
    invalid_order7.current_index = 6;
    let invalid_order7_before = invalid_order7.clone();
    assert!(
        advance_native_stems_head_continuation_c_link(
            &mut invalid_order7,
            &checker_page.head_corners.systems[0],
            &checker_page.head_reachability.systems[0],
            &checker_page.stem_seeds.systems[0],
            &checker_page.head_builders.systems[0],
            &hydrated.plans,
            &checker,
            &bridge,
        )
        .is_err()
    );
    assert_eq!(invalid_order7, invalid_order7_before);

    let mut corrupt_closure = head_phase.clone();
    let x89_ref = corrupt_closure
        .heads
        .iter()
        .find(|head| head.reference.x_ordinal == 89)
        .expect("closure target x89")
        .reference
        .reference;
    corrupt_closure
        .beam_state
        .bindings
        .head_vertices
        .remove(&x89_ref);
    let corrupt_closure_before = corrupt_closure.clone();
    assert!(
        continue_native_stems_head_linking_phase1(
            &corrupt_closure,
            &checker_page.head_corners.systems[0],
            None,
            &checker_page.head_builders.systems[0],
            &hydrated.plans,
        )
        .is_err()
    );
    assert_eq!(corrupt_closure, corrupt_closure_before);
    let mut invalid_second_head = head_phase.clone();
    invalid_second_head.current_index = 0;
    let invalid_second_head_before = invalid_second_head.clone();
    assert!(
        continue_native_stems_head_linking_phase1(
            &invalid_second_head,
            &checker_page.head_corners.systems[0],
            None,
            &checker_page.head_builders.systems[0],
            &hydrated.plans,
        )
        .is_err()
    );
    assert_eq!(invalid_second_head, invalid_second_head_before);

    let mut corrupted = head_phase_entry.clone();
    let mut corrupt_canonical = head_phase
        .beam_state
        .latest_base_apply
        .transaction_state
        .glyph_index
        .known_canonical_glyphs
        .iter()
        .find(|glyph| glyph.glyph_id == head_transaction.selected_glyph_id)
        .expect("selected head seed was promoted into carried canonical state")
        .clone();
    corrupt_canonical.glyph_id += 1;
    corrupted
        .beam_state
        .latest_base_apply
        .transaction_state
        .glyph_index
        .known_canonical_glyphs
        .push(corrupt_canonical);
    let corrupted_before = corrupted.clone();
    assert!(
        advance_native_stems_head_single_item_c_link(
            &mut corrupted,
            &checker_page.head_corners.systems[0],
            &checker_page.head_reachability.systems[0],
            &checker_page.stem_seeds.systems[0],
            &checker_page.head_builders.systems[0],
            &hydrated.plans,
            &checker,
            &bridge,
        )
        .is_err()
    );
    assert_eq!(corrupted, corrupted_before);

    // Expected-only STUMPS rows are opened after the production continuation
    // and its atomic/coherence guards have returned.
    let stumps_prefix_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-stumps-prefix-chula-system1.txt"),
    )
    .expect("expected-only STUMPS prefix");
    assert_eq!(
        sha256_hex(stumps_prefix_text.as_bytes()),
        "ef8f180110a409f85167ee1cc0f641c210144d6e5b5c737d5d8eb69e82d47bcb"
    );
    let stumps_rows = stumps_prefix_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(stumps_rows.len(), 6);
    assert!(stumps_rows[0].starts_with(
        "stemsbeamstumpsprefixbaseline chula.png#1 system 1 retained 34 work [beam:12,"
    ));
    assert!(stumps_rows[0].contains(" linkedB 61 "));
    let between = |row: &str, start: &str, end: &str| {
        row.split_once(start)
            .and_then(|(_, suffix)| suffix.split_once(end).map(|(value, _)| value))
            .expect("strict STUMPS baseline field")
            .to_owned()
    };
    let expected_work_field = between(stumps_rows[0], " work [", "] linkedB ");
    let expected_work = expected_work_field
        .split(',')
        .map(str::to_owned)
        .collect::<Vec<_>>();
    let actual_work = snapshot
        .sources
        .iter()
        .map(|source| {
            let ordinal = hydrated
                .stumps
                .beams_by_abscissa
                .iter()
                .find(|beam| beam.source == *source)
                .expect("retained STUMPS beam in native stump catalogue")
                .sig_ordinal;
            format!("beam:{ordinal}")
        })
        .collect::<Vec<_>>();
    assert_eq!(actual_work, expected_work);
    let expected_linked_field = between(stumps_rows[0], " linkedAliases [", "] sigVertices ");
    let expected_linked = expected_linked_field
        .split(',')
        .map(str::to_owned)
        .collect::<Vec<_>>();
    let mut actual_linked = sides_terminal_carrier
        .scheduler
        .linked_b_linkers
        .iter()
        .copied()
        .map(second_b_alias)
        .collect::<Vec<_>>();
    actual_linked.sort();
    assert_eq!(actual_linked, expected_linked);
    assert_eq!(
        stumps_rows[1],
        "stemsbeamstumpsprefixbeam chula.png#1 system 1 event 0 beamOrdinal 0 beamSig 12 stumpLinkers 3 sideStumps 2"
    );
    assert!(
        stumps_rows[2].contains(
            "event 1 beamOrdinal 0 beamSig 12 stumpOrdinal 0 bAlias beam:12:b:0 vSide TOP"
        )
    );
    assert!(stumps_rows[2].ends_with("sideStump true linkedBefore true action SkipSideStump"));
    assert!(
        stumps_rows[3].contains(
            "event 2 beamOrdinal 0 beamSig 12 stumpOrdinal 1 bAlias beam:12:b:1 vSide TOP"
        )
    );
    assert!(stumps_rows[3].ends_with(
        "plan 147 stemProfile 3 linkProfile 1 headTargets 2 lastIndex 2 relations 2 glyphs 1 lineChanged false action AwaitingVLinkTransaction"
    ));
    assert_eq!(
        stumps_rows[4],
        "stemsbeamstumpsprefixterminal chula.png#1 system 1 events 3 beamOrdinal 0 beamSig 12 stumpOrdinal 1 bAlias beam:12:b:1 vSide TOP plan 147 terminal AwaitingVLinkTransaction stopBeforeCreateStem true"
    );
    assert!(
        stumps_rows[5]
            .contains("schema stems-beam-stumps-prefix-v1 page chula.png#1 system 1 rows 5")
    );
    assert!(stumps_rows[5].contains(" sidesRowsByteIdentical true "));
    assert!(
        stumps_rows[5]
            .ends_with("freshRuns 2 freshRunsByteIdentical true stopBeforeCreateStem true")
    );
    let summary_tokens = stumps_rows[5].split_ascii_whitespace().collect::<Vec<_>>();
    let summary_value = |name: &str| {
        summary_tokens
            .iter()
            .position(|token| *token == name)
            .and_then(|index| summary_tokens.get(index + 1))
            .copied()
            .expect("strict STUMPS summary field")
    };
    assert_eq!(
        summary_value("probeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active STUMPS probe source")
        )
    );
    assert_eq!(
        summary_value("runnerSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/run-stems-beam-stumps-prefix.sh"))
                .expect("active STUMPS runner source")
        )
    );
    assert_eq!(
        summary_value("sidesFixtureSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/stems-beam-sides-pass-chula-system1.txt"))
                .expect("frozen predecessor SIDES fixture")
        )
    );
    let emitted_body = format!("{}\n", stumps_rows[..5].join("\n"));
    assert_eq!(
        summary_value("emittedBodySha256"),
        sha256_hex(emitted_body.as_bytes())
    );

    let stumps_transaction_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-stumps-transaction-chula-system1.txt"),
    )
    .expect("expected-only first-STUMPS transaction");
    assert_eq!(
        sha256_hex(stumps_transaction_text.as_bytes()),
        "b1a312ddc690911b916971081ce21ea1c2211283df174a2175094ace7c144d5e"
    );
    let transaction_rows = stumps_transaction_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(transaction_rows.len(), 7);
    assert_eq!(
        transaction_rows[0],
        "stemsbeamstumpstxnresult chula.png#1 system 1 transaction 1 plan 147 beamSig 12 bAlias beam:12:b:1 vSide TOP registration ReuseActive disposition CreatedChecked registeredGlyphId 310 reuseOutcome AllUnlinked reuseEntriesRead 2 finalStemInterId 2372 baseNewVertex true baseGraphRelation sig-edge:331 bLinkedAfter true siblings 0 siblingAliases - heads 2 sigVertices 672 sigEdges 670 systemStems 33 outerAssignment false"
    );
    assert!(
        transaction_rows[1].contains(
            "event 3 beamOrdinal 0 beamSig 12 stumpOrdinal 2 bAlias beam:12:b:2 vSide TOP"
        )
    );
    assert!(transaction_rows[1].ends_with("sideStump true linkedBefore true action SkipSideStump"));
    assert_eq!(
        transaction_rows[2],
        "stemsbeamstumpstxnresumebeam chula.png#1 system 1 event 4 beamOrdinal 1 beamSig 22 stumpLinkers 3 sideStumps 2"
    );
    assert!(
        transaction_rows[3].contains(
            "event 5 beamOrdinal 1 beamSig 22 stumpOrdinal 0 bAlias beam:22:b:0 vSide TOP"
        )
    );
    assert!(transaction_rows[3].ends_with("sideStump true linkedBefore true action SkipSideStump"));
    assert!(
        transaction_rows[4].contains(
            "event 6 beamOrdinal 1 beamSig 22 stumpOrdinal 1 bAlias beam:22:b:1 vSide TOP"
        )
    );
    assert!(transaction_rows[4].ends_with(
        "plan 622 stemProfile 3 linkProfile 1 headTargets 2 lastIndex 2 relations 2 glyphs 1 lineChanged false action AwaitingVLinkTransaction"
    ));
    assert_eq!(
        transaction_rows[5],
        "stemsbeamstumpstxnresumeterminal chula.png#1 system 1 events 7 transactions 1 beamOrdinal 1 beamSig 22 stumpOrdinal 1 bAlias beam:22:b:1 vSide TOP plan 622 terminal AwaitingVLinkTransaction stopBeforeCreateStem true"
    );
    assert!(
        transaction_rows[6]
            .contains("schema stems-beam-stumps-transaction-v1 page chula.png#1 system 1 rows 6")
    );
    assert!(transaction_rows[6].contains(" sidesRowsByteIdentical true "));
    assert!(
        transaction_rows[6]
            .ends_with("freshRuns 2 freshRunsByteIdentical true stopBeforeSecondCreateStem true")
    );
    let transaction_summary_tokens = transaction_rows[6]
        .split_ascii_whitespace()
        .collect::<Vec<_>>();
    let transaction_summary_value = |name: &str| {
        transaction_summary_tokens
            .iter()
            .position(|token| *token == name)
            .and_then(|index| transaction_summary_tokens.get(index + 1))
            .copied()
            .expect("strict first-STUMPS transaction summary field")
    };
    assert_eq!(
        transaction_summary_value("probeSourceSha256"),
        summary_value("probeSourceSha256")
    );
    assert_eq!(
        transaction_summary_value("runnerSourceSha256"),
        summary_value("runnerSourceSha256")
    );
    assert_eq!(
        transaction_summary_value("prefixFixtureSha256"),
        sha256_hex(stumps_prefix_text.as_bytes())
    );
    assert_eq!(
        transaction_summary_value("sidesFixtureSha256"),
        summary_value("sidesFixtureSha256")
    );
    let transaction_body = format!("{}\n", transaction_rows[..6].join("\n"));
    assert_eq!(
        transaction_summary_value("emittedBodySha256"),
        sha256_hex(transaction_body.as_bytes())
    );

    let second_stumps_transaction_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-stumps-second-transaction-chula-system1.txt"),
    )
    .expect("expected-only second-STUMPS transaction");
    assert_eq!(
        sha256_hex(second_stumps_transaction_text.as_bytes()),
        "4e54cc848116597ad563fd9038e102a135ff606660775e09142c8c8564567173"
    );
    let second_transaction_rows = second_stumps_transaction_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(second_transaction_rows.len(), 7);
    assert_eq!(
        second_transaction_rows[0],
        "stemsbeamstumpstxnresult chula.png#1 system 1 transaction 2 plan 622 beamSig 22 bAlias beam:22:b:1 vSide TOP registration ReuseActive disposition CreatedChecked registeredGlyphId 321 reuseOutcome AllUnlinked reuseEntriesRead 2 finalStemInterId 2373 baseNewVertex true baseGraphRelation sig-edge:334 bLinkedAfter true siblings 0 siblingAliases - heads 2 sigVertices 673 sigEdges 673 systemStems 34 outerAssignment false"
    );
    assert!(
        second_transaction_rows[1].contains(
            "event 7 beamOrdinal 1 beamSig 22 stumpOrdinal 2 bAlias beam:22:b:2 vSide TOP"
        )
    );
    assert!(
        second_transaction_rows[1]
            .ends_with("sideStump true linkedBefore true action SkipSideStump")
    );
    assert_eq!(
        second_transaction_rows[2],
        "stemsbeamstumpstxnresumebeam chula.png#1 system 1 event 8 beamOrdinal 2 beamSig 16 stumpLinkers 3 sideStumps 2"
    );
    assert!(
        second_transaction_rows[3].contains(
            "event 9 beamOrdinal 2 beamSig 16 stumpOrdinal 0 bAlias beam:16:b:0 vSide TOP"
        )
    );
    assert!(
        second_transaction_rows[3]
            .ends_with("sideStump true linkedBefore true action SkipSideStump")
    );
    assert!(
        second_transaction_rows[4].contains(
            "event 10 beamOrdinal 2 beamSig 16 stumpOrdinal 1 bAlias beam:16:b:1 vSide TOP"
        )
    );
    assert!(second_transaction_rows[4].ends_with(
        "plan 404 stemProfile 3 linkProfile 1 headTargets 2 lastIndex 3 relations 2 glyphs 2 lineChanged false action AwaitingVLinkTransaction"
    ));
    assert_eq!(
        second_transaction_rows[5],
        "stemsbeamstumpstxnresumeterminal chula.png#1 system 1 events 11 transactions 2 beamOrdinal 2 beamSig 16 stumpOrdinal 1 bAlias beam:16:b:1 vSide TOP plan 404 terminal AwaitingVLinkTransaction stopBeforeCreateStem true"
    );
    assert!(second_transaction_rows[6].contains(
        "schema stems-beam-stumps-second-transaction-v1 page chula.png#1 system 1 rows 6"
    ));
    assert!(second_transaction_rows[6].contains(" sidesRowsByteIdentical true "));
    assert!(second_transaction_rows[6].contains(" firstTransactionPrefixByteIdentical true "));
    assert!(
        second_transaction_rows[6]
            .ends_with("freshRuns 2 freshRunsByteIdentical true stopBeforeThirdCreateStem true")
    );
    let second_summary_tokens = second_transaction_rows[6]
        .split_ascii_whitespace()
        .collect::<Vec<_>>();
    let second_summary_value = |name: &str| {
        second_summary_tokens
            .iter()
            .position(|token| *token == name)
            .and_then(|index| second_summary_tokens.get(index + 1))
            .copied()
            .expect("strict second-STUMPS transaction summary field")
    };
    assert_eq!(
        second_summary_value("probeSourceSha256"),
        summary_value("probeSourceSha256")
    );
    assert_eq!(
        second_summary_value("runnerSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/run-stems-beam-stumps-second.sh"))
                .expect("active second-STUMPS runner source")
        )
    );
    assert_eq!(
        second_summary_value("firstTransactionFixtureSha256"),
        sha256_hex(stumps_transaction_text.as_bytes())
    );
    assert_eq!(
        second_summary_value("sidesFixtureSha256"),
        summary_value("sidesFixtureSha256")
    );
    let second_transaction_body = format!("{}\n", second_transaction_rows[..6].join("\n"));
    assert_eq!(
        second_summary_value("emittedBodySha256"),
        sha256_hex(second_transaction_body.as_bytes())
    );

    let third_stumps_transaction_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-stumps-third-transaction-chula-system1.txt"),
    )
    .expect("expected-only third-STUMPS transaction");
    assert_eq!(
        sha256_hex(third_stumps_transaction_text.as_bytes()),
        "e7409462ec43f5cde89ffdeafb0c5bb59586c37fff1506086d9c5fa770b30490"
    );
    let third_transaction_rows = third_stumps_transaction_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(third_transaction_rows.len(), 7);
    assert_eq!(
        third_transaction_rows[0],
        "stemsbeamstumpstxnresult chula.png#1 system 1 transaction 3 plan 404 beamSig 16 bAlias beam:16:b:1 vSide TOP registration ReuseActive disposition CreatedChecked registeredGlyphId 303 reuseOutcome AllUnlinked reuseEntriesRead 2 finalStemInterId 2374 baseNewVertex true baseGraphRelation sig-edge:337 bLinkedAfter true siblings 0 siblingAliases - heads 2 sigVertices 674 sigEdges 676 systemStems 35 outerAssignment false"
    );
    assert!(
        third_transaction_rows[1].contains(
            "event 11 beamOrdinal 2 beamSig 16 stumpOrdinal 2 bAlias beam:16:b:2 vSide TOP"
        )
    );
    assert!(
        third_transaction_rows[1]
            .ends_with("sideStump true linkedBefore true action SkipSideStump")
    );
    assert_eq!(
        third_transaction_rows[2],
        "stemsbeamstumpstxnresumebeam chula.png#1 system 1 event 12 beamOrdinal 3 beamSig 28 stumpLinkers 3 sideStumps 2"
    );
    assert!(
        third_transaction_rows[3].contains(
            "event 13 beamOrdinal 3 beamSig 28 stumpOrdinal 0 bAlias beam:28:b:0 vSide TOP"
        )
    );
    assert!(
        third_transaction_rows[3]
            .ends_with("sideStump true linkedBefore true action SkipSideStump")
    );
    assert!(
        third_transaction_rows[4].contains(
            "event 14 beamOrdinal 3 beamSig 28 stumpOrdinal 1 bAlias beam:28:b:1 vSide TOP"
        )
    );
    assert!(third_transaction_rows[4].ends_with(
        "plan 508 stemProfile 3 linkProfile 1 headTargets 2 lastIndex 3 relations 2 glyphs 2 lineChanged false action AwaitingVLinkTransaction"
    ));
    assert_eq!(
        third_transaction_rows[5],
        "stemsbeamstumpstxnresumeterminal chula.png#1 system 1 events 15 transactions 3 beamOrdinal 3 beamSig 28 stumpOrdinal 1 bAlias beam:28:b:1 vSide TOP plan 508 terminal AwaitingVLinkTransaction stopBeforeCreateStem true"
    );
    assert!(third_transaction_rows[6].contains(
        "schema stems-beam-stumps-third-transaction-v1 page chula.png#1 system 1 rows 6"
    ));
    assert!(third_transaction_rows[6].contains(" sidesRowsByteIdentical true "));
    assert!(third_transaction_rows[6].contains(" secondTransactionPrefixByteIdentical true "));
    assert!(
        third_transaction_rows[6]
            .ends_with("freshRuns 2 freshRunsByteIdentical true stopBeforeFourthCreateStem true")
    );
    let third_summary_tokens = third_transaction_rows[6]
        .split_ascii_whitespace()
        .collect::<Vec<_>>();
    let third_summary_value = |name: &str| {
        third_summary_tokens
            .iter()
            .position(|token| *token == name)
            .and_then(|index| third_summary_tokens.get(index + 1))
            .copied()
            .expect("strict third-STUMPS transaction summary field")
    };
    assert_eq!(
        third_summary_value("probeSourceSha256"),
        summary_value("probeSourceSha256")
    );
    assert_eq!(
        third_summary_value("runnerSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/run-stems-beam-stumps-third.sh"))
                .expect("active third-STUMPS runner source")
        )
    );
    assert_eq!(
        third_summary_value("secondTransactionFixtureSha256"),
        sha256_hex(second_stumps_transaction_text.as_bytes())
    );
    assert_eq!(
        third_summary_value("sidesFixtureSha256"),
        summary_value("sidesFixtureSha256")
    );
    let third_transaction_body = format!("{}\n", third_transaction_rows[..6].join("\n"));
    assert_eq!(
        third_summary_value("emittedBodySha256"),
        sha256_hex(third_transaction_body.as_bytes())
    );

    let complete_stumps_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-stumps-complete-chula-system1.txt"),
    )
    .expect("expected-only complete STUMPS suffix");
    assert_eq!(
        sha256_hex(complete_stumps_text.as_bytes()),
        "81fecf842495ddc93792b0ed5acf5641231181f172acd4e5cbf3bc57565f0cd2"
    );
    let complete_rows = complete_stumps_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(complete_rows.len(), 83);
    let complete_results = complete_rows
        .iter()
        .copied()
        .filter(|line| line.starts_with("stemsbeamstumpstxnresult "))
        .collect::<Vec<_>>();
    assert_eq!(complete_results.len(), 4);
    assert_eq!(
        complete_results[0],
        "stemsbeamstumpstxnresult chula.png#1 system 1 transaction 4 plan 508 beamSig 28 bAlias beam:28:b:1 vSide TOP registration ReuseActive disposition CreatedChecked registeredGlyphId 308 reuseOutcome AllUnlinked reuseEntriesRead 2 finalStemInterId 2375 baseNewVertex true baseGraphRelation sig-edge:340 bLinkedAfter true siblings 0 siblingAliases - heads 2 sigVertices 675 sigEdges 679 systemStems 36 outerAssignment false"
    );
    assert_eq!(
        complete_results[1],
        "stemsbeamstumpstxnresult chula.png#1 system 1 transaction 5 plan 28 beamSig 29 bAlias beam:29:b:1 vSide TOP registration ReuseActive disposition CreatedChecked registeredGlyphId 305 reuseOutcome AllUnlinked reuseEntriesRead 2 finalStemInterId 2376 baseNewVertex true baseGraphRelation sig-edge:343 bLinkedAfter true siblings 0 siblingAliases - heads 2 sigVertices 676 sigEdges 682 systemStems 37 outerAssignment false"
    );
    assert_eq!(
        complete_results[2],
        "stemsbeamstumpstxnresult chula.png#1 system 1 transaction 6 plan 330 beamSig 32 bAlias beam:32:b:1 vSide TOP registration ReuseActive disposition CreatedChecked registeredGlyphId 302 reuseOutcome AllUnlinked reuseEntriesRead 3 finalStemInterId 2377 baseNewVertex true baseGraphRelation sig-edge:346 bLinkedAfter true siblings 0 siblingAliases - heads 3 sigVertices 677 sigEdges 686 systemStems 38 outerAssignment false"
    );
    assert_eq!(
        complete_results[3],
        "stemsbeamstumpstxnresult chula.png#1 system 1 transaction 7 plan 251 beamSig 31 bAlias beam:31:b:1 vSide TOP registration ReuseActive disposition CreatedChecked registeredGlyphId 300 reuseOutcome AllUnlinked reuseEntriesRead 2 finalStemInterId 2378 baseNewVertex true baseGraphRelation sig-edge:350 bLinkedAfter true siblings 0 siblingAliases - heads 2 sigVertices 678 sigEdges 689 systemStems 39 outerAssignment false"
    );
    assert_eq!(
        complete_rows
            .iter()
            .filter(|line| line.starts_with("stemsbeamstumpstxnresumebeam "))
            .count(),
        30
    );
    let complete_steps = complete_rows
        .iter()
        .copied()
        .filter(|line| line.starts_with("stemsbeamstumpstxnresumestep "))
        .collect::<Vec<_>>();
    assert_eq!(complete_steps.len(), 47);
    assert_eq!(
        complete_steps
            .iter()
            .filter(|line| line.ends_with("action SkipSideStump"))
            .count(),
        44
    );
    assert_eq!(
        complete_steps
            .iter()
            .filter(|line| line.ends_with("action AwaitingVLinkTransaction"))
            .count(),
        3
    );
    assert!(complete_steps.iter().all(|line| {
        line.ends_with("action SkipSideStump") || line.ends_with("action AwaitingVLinkTransaction")
    }));
    assert_eq!(
        complete_rows[81],
        "stemsbeamstumpstxnresumeterminal chula.png#1 system 1 events 92 transactions 7 terminal Completed stopBeforeCreateStem true"
    );
    assert!(complete_rows[82].contains(
        "schema stems-beam-stumps-complete-v1 page chula.png#1 system 1 rows 82 transactions 4"
    ));
    assert!(complete_rows[82].contains(" sidesRowsByteIdentical true "));
    assert!(complete_rows[82].contains(" thirdTransactionPrefixByteIdentical true "));
    assert!(
        complete_rows[82].ends_with("freshRuns 2 freshRunsByteIdentical true terminal Completed")
    );
    let complete_summary_tokens = complete_rows[82]
        .split_ascii_whitespace()
        .collect::<Vec<_>>();
    let complete_summary_value = |name: &str| {
        complete_summary_tokens
            .iter()
            .position(|token| *token == name)
            .and_then(|index| complete_summary_tokens.get(index + 1))
            .copied()
            .expect("strict complete-STUMPS summary field")
    };
    assert_eq!(
        complete_summary_value("probeSourceSha256"),
        summary_value("probeSourceSha256")
    );
    assert_eq!(
        complete_summary_value("runnerSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/run-stems-beam-stumps-complete.sh"))
                .expect("active complete-STUMPS runner source")
        )
    );
    assert_eq!(
        complete_summary_value("thirdTransactionFixtureSha256"),
        sha256_hex(third_stumps_transaction_text.as_bytes())
    );
    assert_eq!(
        complete_summary_value("sidesFixtureSha256"),
        summary_value("sidesFixtureSha256")
    );
    let complete_body = format!("{}\n", complete_rows[..82].join("\n"));
    assert_eq!(
        complete_summary_value("emittedBodySha256"),
        sha256_hex(complete_body.as_bytes())
    );

    let head_phase_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1.txt"),
    )
    .expect("expected-only post-STUMPS head-phase prefix");
    assert_eq!(
        sha256_hex(head_phase_text.as_bytes()),
        "91541fc08786b8d81b6f6c26d68d83214276a3e68bcdd488f5607a135438aff8"
    );
    let head_phase_rows = head_phase_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(head_phase_rows.len(), 12);
    let head_field = |line: &str, name: &str| {
        let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
        let index = tokens
            .iter()
            .position(|token| *token == name)
            .unwrap_or_else(|| panic!("missing head-phase field {name}: {line}"));
        tokens[index + 1].to_owned()
    };
    let second_java = head_phase_rows[6];
    assert_eq!(head_field(second_java, "headOrder"), "1");
    assert_eq!(
        head_field(second_java, "headSig").parse::<usize>().unwrap(),
        second_continuation.processed_head.sig_ordinal
    );
    assert_eq!(head_field(second_java, "headX"), "90");
    assert_eq!(head_field(second_java, "headInterId"), "1331");
    assert_eq!(
        head_field(second_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(second_java, "returned"), "true");
    assert_eq!(second_continuation.returned_linked, Some(true));
    assert_eq!(
        head_field(second_java, "closedValueChanges")
            .parse::<usize>()
            .unwrap(),
        second_continuation.closed_value_changes
    );
    assert_eq!(
        head_field(second_java, "closureWrites"),
        "[x89:sig22:LEFT:false->true,x89:sig22:RIGHT:false->true]"
    );
    assert_eq!(head_field(second_java, "unlinkedCount"), "0");
    assert_eq!(head_field(second_java, "nextHeadOrder"), "2");
    assert_eq!(head_field(second_java, "nextHeadX"), "81");
    assert_eq!(head_field(second_java, "nextHeadSig"), "33");
    assert_eq!(head_field(second_java, "nextHeadInterId"), "1351");

    let third_java = head_phase_rows[7];
    assert_eq!(head_field(third_java, "headOrder"), "2");
    assert_eq!(
        head_field(third_java, "headSig").parse::<usize>().unwrap(),
        third_continuation.processed_head.sig_ordinal
    );
    assert_eq!(head_field(third_java, "headX"), "81");
    assert_eq!(head_field(third_java, "headInterId"), "1351");
    assert_eq!(
        head_field(third_java, "grade"),
        "0x1.901efd26d99b1p-1/3fe901efd26d99b1"
    );
    assert_eq!(
        head_field(third_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(third_java, "returned"), "true");
    assert_eq!(third_continuation.returned_linked, Some(true));
    assert_eq!(
        head_field(third_java, "closedValueChanges")
            .parse::<usize>()
            .unwrap(),
        third_continuation.closed_value_changes
    );
    assert_eq!(
        head_field(third_java, "closureWrites"),
        "[x79:sig40:LEFT:false->true,x79:sig40:RIGHT:false->true,x80:sig32:LEFT:false->true,x80:sig32:RIGHT:false->true]"
    );
    assert_eq!(head_field(third_java, "unlinkedCount"), "0");
    assert_eq!(head_field(third_java, "nextHeadOrder"), "3");
    assert_eq!(head_field(third_java, "nextHeadX"), "20");
    assert_eq!(head_field(third_java, "nextHeadSig"), "65");
    assert_eq!(head_field(third_java, "nextHeadInterId"), "1419");
    assert_eq!(
        head_field(third_java, "nextGrade"),
        "0x1.8e97b8a9fa8cap-1/3fe8e97b8a9fa8ca"
    );

    let fourth_java = head_phase_rows[8];
    assert_eq!(head_field(fourth_java, "headOrder"), "3");
    assert_eq!(
        head_field(fourth_java, "headSig").parse::<usize>().unwrap(),
        fourth_continuation.processed_head.sig_ordinal
    );
    assert_eq!(head_field(fourth_java, "headX"), "20");
    assert_eq!(head_field(fourth_java, "headInterId"), "1419");
    assert_eq!(
        head_field(fourth_java, "grade"),
        "0x1.8e97b8a9fa8cap-1/3fe8e97b8a9fa8ca"
    );
    assert_eq!(
        head_field(fourth_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(fourth_java, "returned"), "true");
    assert_eq!(fourth_continuation.returned_linked, Some(true));
    assert_eq!(
        head_field(fourth_java, "closedValueChanges")
            .parse::<usize>()
            .unwrap(),
        fourth_continuation.closed_value_changes
    );
    assert_eq!(
        head_field(fourth_java, "closureWrites"),
        "[x19:sig64:LEFT:false->true,x19:sig64:RIGHT:false->true]"
    );
    assert_eq!(head_field(fourth_java, "unlinkedCount"), "0");
    assert_eq!(head_field(fourth_java, "nextHeadOrder"), "4");
    assert_eq!(head_field(fourth_java, "nextHeadX"), "36");
    assert_eq!(head_field(fourth_java, "nextHeadSig"), "69");
    assert_eq!(head_field(fourth_java, "nextHeadInterId"), "1427");

    let fifth_java = head_phase_rows[9];
    assert_eq!(head_field(fifth_java, "headOrder"), "4");
    assert_eq!(
        head_field(fifth_java, "headSig").parse::<usize>().unwrap(),
        fifth_continuation.processed_head.sig_ordinal
    );
    assert_eq!(head_field(fifth_java, "headX"), "36");
    assert_eq!(head_field(fifth_java, "headInterId"), "1427");
    assert_eq!(
        head_field(fifth_java, "grade"),
        "0x1.8e37718100f0cp-1/3fe8e37718100f0c"
    );
    assert_eq!(
        head_field(fifth_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(fifth_java, "returned"), "true");
    assert_eq!(fifth_continuation.returned_linked, Some(true));
    assert_eq!(
        head_field(fifth_java, "closedValueChanges")
            .parse::<usize>()
            .unwrap(),
        fifth_continuation.closed_value_changes
    );
    assert_eq!(
        head_field(fifth_java, "closureWrites"),
        "[x35:sig68:LEFT:false->true,x35:sig68:RIGHT:false->true]"
    );
    assert_eq!(head_field(fifth_java, "unlinkedCount"), "0");
    assert_eq!(head_field(fifth_java, "nextHeadOrder"), "5");
    assert_eq!(head_field(fifth_java, "nextHeadX"), "99");
    assert_eq!(head_field(fifth_java, "nextHeadSig"), "61");
    assert_eq!(head_field(fifth_java, "nextHeadInterId"), "1411");
    let native_fifth_next_sides = format!(
        "[{}]",
        fifth_head_phase.heads[5]
            .sides
            .iter()
            .map(|cell| {
                let side = match cell.reference.horizontal {
                    NativeStemHeadSide::Left => "LEFT",
                    NativeStemHeadSide::Right => "RIGHT",
                };
                format!("{}:{}:{}", side, cell.linked, cell.closed)
            })
            .collect::<Vec<_>>()
            .join(",")
    );
    assert_eq!(head_field(fifth_java, "nextSides"), native_fifth_next_sides);

    let sixth_java = head_phase_rows[10];
    assert_eq!(head_field(sixth_java, "headOrder"), "5");
    assert_eq!(
        head_field(sixth_java, "headSig").parse::<usize>().unwrap(),
        sixth_continuation.processed_head.sig_ordinal
    );
    assert_eq!(head_field(sixth_java, "headX"), "99");
    assert_eq!(head_field(sixth_java, "headInterId"), "1411");
    assert_eq!(
        head_field(sixth_java, "grade"),
        "0x1.8b9e1faa7607p-1/3fe8b9e1faa76070"
    );
    assert_eq!(
        head_field(sixth_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(sixth_java, "returned"), "true");
    assert_eq!(sixth_continuation.returned_linked, Some(true));
    assert_eq!(
        head_field(sixth_java, "closedValueChanges")
            .parse::<usize>()
            .unwrap(),
        sixth_continuation.closed_value_changes
    );
    assert_eq!(
        head_field(sixth_java, "closureWrites"),
        "[x98:sig60:LEFT:false->true,x98:sig60:RIGHT:false->true]"
    );
    assert_eq!(head_field(sixth_java, "unlinkedCount"), "0");
    assert_eq!(head_field(sixth_java, "nextHeadOrder"), "6");
    assert_eq!(head_field(sixth_java, "nextHeadX"), "22");
    assert_eq!(head_field(sixth_java, "nextHeadSig"), "12");
    assert_eq!(head_field(sixth_java, "nextHeadInterId"), "1309");
    assert_eq!(
        head_field(sixth_java, "nextGrade"),
        "0x1.890680173456fp-1/3fe890680173456f"
    );
    let native_sixth_next_sides = format!(
        "[{}]",
        sixth_head_phase.heads[6]
            .sides
            .iter()
            .map(|cell| {
                let side = match cell.reference.horizontal {
                    NativeStemHeadSide::Left => "LEFT",
                    NativeStemHeadSide::Right => "RIGHT",
                };
                format!("{}:{}:{}", side, cell.linked, cell.closed)
            })
            .collect::<Vec<_>>()
            .join(",")
    );
    assert_eq!(head_field(sixth_java, "nextSides"), native_sixth_next_sides);

    let frontier = head_phase_rows[1];
    assert_eq!(head_field(frontier, "headOrder"), "0");
    assert_eq!(
        head_field(frontier, "headSig").parse::<usize>().unwrap(),
        head_phase.frontier.head.sig_ordinal
    );
    assert_eq!(head_field(frontier, "headInterId"), "1375");
    assert_eq!(
        head_field(frontier, "stemProfile").parse::<i32>().unwrap(),
        head_phase.frontier.stem_profile
    );
    assert_eq!(
        head_field(frontier, "linkProfile").parse::<i32>().unwrap(),
        head_phase.frontier.link_profile
    );
    assert_eq!(head_field(frontier, "append"), "false");
    assert_eq!(
        head_field(frontier, "sides"),
        "[LEFT:false:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(frontier, "decisions"),
        "[LEFT:top=false:bottom=false:branch=Neither,RIGHT:top=true:bottom=false:branch=TopOnly]"
    );
    assert_eq!(head_field(frontier, "selectedC"), "h:38:RIGHT:TOP");
    assert_eq!(head_phase.frontier.next_corner.x_ordinal, 38);
    assert_eq!(head_phase.frontier.next_corner.sig_ordinal, 45);
    assert_eq!(
        head_phase.frontier.next_corner.horizontal,
        NativeStemHeadSide::Right
    );
    assert_eq!(
        head_phase.frontier.next_corner.vertical,
        NativeStemVerticalSide::Top
    );
    assert_eq!(head_phase.heads[0].grade.to_bits(), 0x3fe9_17c3_b820_7578);
    assert_eq!(
        head_field(frontier, "terminal"),
        "AwaitingHeadCLinkTransaction"
    );
    let expand = head_phase_rows[2];
    assert_eq!(head_field(expand, "cAlias"), "h:38:RIGHT:TOP");
    assert_eq!(
        head_field(expand, "lastIndex"),
        head_transaction.last_index.to_string()
    );
    assert_eq!(
        head_field(expand, "maxIndex"),
        head_transaction.max_index.to_string()
    );
    assert_eq!(head_field(expand, "relations"), "1");
    assert_eq!(head_field(expand, "glyphs"), "1");
    assert_eq!(head_field(expand, "candidateIdBefore"), "307");
    assert_eq!(head_field(expand, "existingGlyph"), "glyph:307");
    assert_eq!(head_field(expand, "existingActive"), "true");
    assert_eq!(head_field(expand, "existingStem"), "-");
    assert_eq!(head_field(expand, "terminal"), "ReadyForHeadCreateStem");
    let java_result = head_phase_rows[3];
    assert_eq!(head_field(java_result, "relationsBefore"), "0");
    assert_eq!(head_field(java_result, "relationsAfter"), "1");
    assert_eq!(head_field(java_result, "linked"), "true");
    assert_eq!(head_field(java_result, "sigVerticesBefore"), "678");
    assert_eq!(head_field(java_result, "sigVerticesAfter"), "679");
    assert_eq!(head_field(java_result, "sigEdgesBefore"), "689");
    assert_eq!(head_field(java_result, "sigEdgesAfter"), "690");
    assert_eq!(head_field(java_result, "systemStemsBefore"), "39");
    assert_eq!(head_field(java_result, "systemStemsAfter"), "40");
    assert_eq!(
        head_field(java_result, "terminal"),
        "ReturnedBeforeSecondHead"
    );
    assert_eq!(head_field(java_result, "dirtyBefore"), "true:true:true");
    assert_eq!(head_field(java_result, "dirtyAfter"), "true:true:true");
    assert_eq!(head_field(java_result, "nextHeadOrder"), "1");
    assert_eq!(head_field(java_result, "nextHeadSig"), "23");
    assert_eq!(head_field(java_result, "nextHeadInterId"), "1331");
    assert_eq!(
        head_field(java_result, "nextSides"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(java_result, "nextHeadSig")
            .parse::<usize>()
            .expect("numeric next head SIG ordinal"),
        second_continuation.processed_head.sig_ordinal
    );
    let native_next_sides = format!(
        "[{}]",
        second_head_phase.heads[1]
            .sides
            .iter()
            .map(|cell| {
                let side = match cell.reference.horizontal {
                    NativeStemHeadSide::Left => "LEFT",
                    NativeStemHeadSide::Right => "RIGHT",
                };
                format!("{}:{}:{}", side, cell.linked, cell.closed)
            })
            .collect::<Vec<_>>()
            .join(",")
    );
    assert_eq!(head_field(java_result, "nextSides"), native_next_sides);
    let create = head_phase_rows[4];
    assert_eq!(head_field(create, "registeredAlias"), "glyph:307");
    assert_eq!(head_field(create, "registeredId"), "307");
    assert_eq!(head_field(create, "registration"), "ReuseActive");
    assert_eq!(head_field(create, "disposition"), "CreatedChecked");
    assert_eq!(head_field(create, "stemId"), "2379");
    assert_eq!(head_field(create, "stemVertex"), "260");
    assert_eq!(head_field(create, "allocatorBefore"), "2378");
    assert_eq!(head_field(create, "allocatorAfter"), "2379");
    assert_eq!(head_field(create, "systemStemsBefore"), "39");
    assert_eq!(head_field(create, "systemStemsAfter"), "40");
    assert_eq!(head_field(create, "interIndexBefore"), "678");
    assert_eq!(head_field(create, "interIndexAfter"), "679");
    let apply = head_phase_rows[5];
    assert_eq!(head_field(apply, "linked"), "true");
    assert_eq!(head_field(apply, "addedVertices"), "1");
    assert_eq!(head_field(apply, "addedEdges"), "1");
    assert_eq!(
        head_field(apply, "terminal"),
        "ReturnedHeadCLinkTransaction"
    );
    let head_summary = head_phase_rows[11];
    assert_eq!(
        head_field(head_summary, "schema"),
        "stems-head-phase-prefix-v6"
    );
    assert_eq!(head_field(head_summary, "rows"), "11");
    assert_eq!(head_field(head_summary, "freshRuns"), "2");
    assert_eq!(head_field(head_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(head_summary, "probeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared head-phase probe")
        )
    );
    assert_eq!(
        head_field(head_summary, "runnerSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/run-stems-head-phase-prefix.sh"))
                .expect("active head-phase runner")
        )
    );
    assert_eq!(
        head_field(head_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );

    // Boundary 33 expected-only v7 envelope/result rows are opened only after
    // the authenticated order7 native C-link has committed.
    let v7_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v7.txt"),
    )
    .expect("expected-only order7 head C-link fixture");
    assert_eq!(
        sha256_hex(v7_text.as_bytes()),
        "8df7d36e780e90e569fcc37144bd48ff43e5b647f9cdc240d899ee10386b153d"
    );
    let v7_rows = v7_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v7_rows.len(), 16);
    let v7_body = format!("{}\n", v7_rows[..15].join("\n"));
    assert_eq!(
        sha256_hex(v7_body.as_bytes()),
        "06285da43ff0b5a1f3644c4468570a10f24c0c8f2b8173e9e7d1e268284704d6"
    );
    let v7_frontier = v7_rows[12];
    assert_eq!(head_field(v7_frontier, "headOrder"), "7");
    assert_eq!(head_field(v7_frontier, "headX"), "76");
    assert_eq!(head_field(v7_frontier, "headSig"), "97");
    assert_eq!(head_field(v7_frontier, "cAlias"), "h:76:LEFT:BOTTOM");
    assert_eq!(head_field(v7_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v7_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v7_frontier, "relations"), "1");
    assert_eq!(head_field(v7_frontier, "glyphs"), "1");
    assert_eq!(head_field(v7_frontier, "candidateIdBefore"), "319");
    assert_eq!(head_field(v7_frontier, "existingGlyph"), "glyph:319");
    assert_eq!(head_field(v7_frontier, "existingActive"), "true");
    assert_eq!(head_field(v7_frontier, "existingStem"), "-");
    assert_eq!(
        head_field(v7_frontier, "terminal"),
        "ReadyForHeadCreateStem"
    );
    assert_eq!(
        head_field(v7_frontier, "relationRows"),
        "[h:76:LEFT:BOTTOM:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.fc384994638a5p-1/3fefc384994638a5:impacts=[xInGap:0x1.f877b832988f4p-1/3fef877b832988f4:w=0x1.0p0/3ff0000000000000,yGap:0x1.0p0/3ff0000000000000:w=0x1.0p0/3ff0000000000000]:dx=-0x1.81a7f5e17cf3dp-8/bf781a7f5e17cf3d:dy=0x0.0p0/0000000000000000:extension=0x1.b0511ffd9187cp10/409b0511ffd9187c:0x1.188p9/4081880000000000:headSide=LEFT:consistency=0x1.0p0/3ff0000000000000]"
    );
    let v7_result = v7_rows[13];
    assert_eq!(head_field(v7_result, "headOrder"), "7");
    assert_eq!(head_field(v7_result, "allocatorBefore"), "2379");
    assert_eq!(head_field(v7_result, "allocatorAfter"), "2380");
    assert_eq!(head_field(v7_result, "registeredGlyphs"), "-");
    assert!(head_field(v7_result, "addedVertices").starts_with("[id2380:"));
    assert!(head_field(v7_result, "addedEdges").starts_with("[system1:sourceId1483:targetId2380:"));
    assert!(head_field(v7_result, "addedSystemStems").ends_with(":stemId2380]"));
    assert_eq!(
        head_field(v7_result, "linkerChanges"),
        "[h:76:LEFT:BOTTOM:false:false->true:false,h:76:LEFT:TOP:false:false->true:false,linker:SLinker:head:76:false:false->true:false]"
    );
    assert_eq!(
        head_field(v7_result, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v7_result, "linkerStateHashAfter"),
        "51b890b4d928b5d5615c324986ecdb4f0155f22f4be5e4381c393744fe3335e7"
    );
    assert_eq!(
        head_field(v7_result, "terminal"),
        "ReturnedHeadCLinkTransaction"
    );
    let v7_java = v7_rows[14];
    assert_eq!(head_field(v7_java, "headOrder"), "7");
    assert_eq!(head_field(v7_java, "headX"), "76");
    assert_eq!(head_field(v7_java, "headSig"), "97");
    assert_eq!(head_field(v7_java, "headInterId"), "1483");
    assert_eq!(
        head_field(v7_java, "grade"),
        "0x1.88a60dd67c8a7p-1/3fe88a60dd67c8a7"
    );
    assert_eq!(
        head_field(v7_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v7_java, "incident"), "-");
    assert_eq!(head_field(v7_java, "returned"), "true");
    assert_eq!(head_field(v7_java, "closureWrites"), "-");
    assert_eq!(head_field(v7_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v7_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v7_java, "sigVerticesBefore"), "679");
    assert_eq!(head_field(v7_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v7_java, "sigEdgesBefore"), "690");
    assert_eq!(head_field(v7_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v7_java, "systemStemsBefore"), "40");
    assert_eq!(head_field(v7_java, "systemStemsAfter"), "41");
    assert_eq!(head_field(v7_java, "nextHeadOrder"), "8");
    assert_eq!(head_field(v7_java, "nextHeadX"), "95");
    assert_eq!(head_field(v7_java, "nextHeadSig"), "100");
    assert_eq!(head_field(v7_java, "nextHeadInterId"), "1489");
    assert_eq!(head_field(v7_java, "terminal"), "ReturnedBeforeNextHead");
    let v7_summary = v7_rows[15];
    assert_eq!(
        head_field(v7_summary, "schema"),
        "stems-head-phase-prefix-v7"
    );
    assert_eq!(head_field(v7_summary, "rows"), "15");
    assert_eq!(
        head_field(v7_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v7_summary, "probeSourceSha256"),
        "93c6771d55b814cff4155d4065d94a322767df9a668033bc7f2e5ea1ea7f6edd"
    );
    assert_eq!(
        head_field(v7_summary, "runnerSourceSha256"),
        "87a12b97b6d9c79e6c0d346f8187b426505ab5e0e7785bd07a5984a03a18c197"
    );
    assert_eq!(
        head_field(v7_summary, "emittedBodySha256"),
        "06285da43ff0b5a1f3644c4468570a10f24c0c8f2b8173e9e7d1e268284704d6"
    );
    assert_eq!(
        head_field(v7_summary, "semanticPassSha256"),
        "68d581d84f21a79c41df3d4ebf6a856cc0dee266288512e4cd1e44bb3260fa0c"
    );
    assert_eq!(
        head_field(v7_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );

    // Boundary 34 expected-only v8 rows pin the unchanged order-8
    // prelinked continuation after native closure writes have committed.
    let v8_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v8.txt"),
    )
    .expect("expected-only order8 head continuation fixture");
    assert_eq!(
        sha256_hex(v8_text.as_bytes()),
        "82eca291e69ec27e49903d31b1da408f68962469780a1f706f3f979564e8aebb"
    );
    let v8_rows = v8_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v8_rows.len(), 17);
    let v8_body = format!("{}\n", v8_rows[..16].join("\n"));
    assert_eq!(
        sha256_hex(v8_body.as_bytes()),
        "a5460ce6a40756092d2e2dc91975ac5c2665c480370249084faa141d7b45eca8"
    );
    let v8_java = v8_rows[15];
    assert_eq!(head_field(v8_java, "headOrder"), "8");
    assert_eq!(head_field(v8_java, "headX"), "95");
    assert_eq!(head_field(v8_java, "headSig"), "100");
    assert_eq!(head_field(v8_java, "headInterId"), "1489");
    assert_eq!(
        head_field(v8_java, "grade"),
        "0x1.889757fe9c8dp-1/3fe889757fe9c8d0"
    );
    assert_eq!(
        head_field(v8_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v8_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v8_java, "incident"),
        "[stem2364:headSideLEFT:heads[x91:sig98:id1485:sideLEFT,x94:sig99:id1487:sideLEFT,x95:sig100:id1489:sideLEFT]]"
    );
    assert_eq!(head_field(v8_java, "returned"), "true");
    assert_eq!(
        head_field(v8_java, "sidesAfter"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(head_field(v8_java, "undefs"), "[]");
    assert_eq!(
        head_field(v8_java, "closureWrites"),
        "[x91:sig98:LEFT:false->true,x91:sig98:RIGHT:false->true,x94:sig99:LEFT:false->true,x94:sig99:RIGHT:false->true]"
    );
    assert_eq!(head_field(v8_java, "closedValueChanges"), "4");
    assert_eq!(head_field(v8_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v8_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v8_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v8_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v8_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v8_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v8_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v8_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v8_java, "linkerStateHashAfter"),
        "36cdae3651ed3cc4e70f48b600e0333d114661db6e9467b84e8000e39d9cc542"
    );
    assert_eq!(head_field(v8_java, "nextHeadOrder"), "9");
    assert_eq!(head_field(v8_java, "nextHeadX"), "42");
    assert_eq!(head_field(v8_java, "nextHeadSig"), "93");
    assert_eq!(head_field(v8_java, "nextHeadInterId"), "1475");
    assert_eq!(head_field(v8_java, "terminal"), "ReturnedBeforeNextHead");
    let v8_summary = v8_rows[16];
    assert_eq!(
        head_field(v8_summary, "schema"),
        "stems-head-phase-prefix-v8"
    );
    assert_eq!(head_field(v8_summary, "rows"), "16");
    assert_eq!(
        head_field(v8_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v8_summary, "baseV7RunnerSourceSha256"),
        "87a12b97b6d9c79e6c0d346f8187b426505ab5e0e7785bd07a5984a03a18c197"
    );
    assert_eq!(
        head_field(v8_summary, "baseV7FixtureSha256"),
        "8df7d36e780e90e569fcc37144bd48ff43e5b647f9cdc240d899ee10386b153d"
    );
    assert_eq!(
        head_field(v8_summary, "probeSourceSha256"),
        "fe2bd835c8359810099881288608bc0055336f1ebb77e6715aa2946570181867"
    );
    assert_eq!(
        head_field(v8_summary, "runnerSourceSha256"),
        "4d3be4619b7fbe5f5ca39e4065914fe7bb2a56dcbbfb6ae67c95cf444140edfc"
    );
    assert_eq!(
        head_field(v8_summary, "emittedBodySha256"),
        "a5460ce6a40756092d2e2dc91975ac5c2665c480370249084faa141d7b45eca8"
    );
    assert_eq!(
        head_field(v8_summary, "semanticPassSha256"),
        "062721eabd59d1d4b4bc5d4c18b3d6ee8e510c68d76473278e6cb60c5e2f7597"
    );
    assert_eq!(
        head_field(v8_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v8_summary, "freshRuns"), "2");
    assert_eq!(head_field(v8_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v8_summary, "nativeScope"),
        "ReturnedOrder8HeadContinuation"
    );
    assert_eq!(
        head_field(v8_summary, "javaEvidence"),
        "ReturnedBeforeTenthHead"
    );

    // Boundary 35 expected-only v9 rows pin the unchanged order-9
    // prelinked continuation and its two ordered closure writes.
    let v9_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v9.txt"),
    )
    .expect("expected-only order9 head continuation fixture");
    assert_eq!(
        sha256_hex(v9_text.as_bytes()),
        "b0d3c67f9b76a56a528d8a962f3f2bc54710616f2e86650ac8500e750534ff2c"
    );
    let v9_rows = v9_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v9_rows.len(), 18);
    let v9_body = format!("{}\n", v9_rows[..17].join("\n"));
    assert_eq!(
        sha256_hex(v9_body.as_bytes()),
        "dbe0a891add7c613c340dfaee983a75c97b20cba4744ca19619e10cd9f7a78f5"
    );
    let v9_java = v9_rows[16];
    assert_eq!(head_field(v9_java, "headOrder"), "9");
    assert_eq!(head_field(v9_java, "headX"), "42");
    assert_eq!(head_field(v9_java, "headSig"), "93");
    assert_eq!(head_field(v9_java, "headInterId"), "1475");
    assert_eq!(
        head_field(v9_java, "grade"),
        "0x1.862c1df373f58p-1/3fe862c1df373f58"
    );
    assert_eq!(
        head_field(v9_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v9_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v9_java, "incident"),
        "[stem2352:headSideLEFT:heads[x41:sig92:id1473:sideLEFT,x42:sig93:id1475:sideLEFT]]"
    );
    assert_eq!(head_field(v9_java, "returned"), "true");
    assert_eq!(
        head_field(v9_java, "closureWrites"),
        "[x41:sig92:LEFT:false->true,x41:sig92:RIGHT:false->true]"
    );
    assert_eq!(head_field(v9_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v9_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v9_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v9_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v9_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v9_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v9_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v9_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v9_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v9_java, "linkerStateHashAfter"),
        "d501b29cf833e02f92bb567548918bc28f3bac8586f193f560e8c43016405c5b"
    );
    assert_eq!(head_field(v9_java, "nextHeadOrder"), "10");
    assert_eq!(head_field(v9_java, "nextHeadX"), "65");
    assert_eq!(head_field(v9_java, "nextHeadSig"), "95");
    assert_eq!(head_field(v9_java, "nextHeadInterId"), "1479");
    assert_eq!(head_field(v9_java, "terminal"), "ReturnedBeforeNextHead");
    let v9_summary = v9_rows[17];
    assert_eq!(
        head_field(v9_summary, "schema"),
        "stems-head-phase-prefix-v9"
    );
    assert_eq!(head_field(v9_summary, "rows"), "17");
    assert_eq!(
        head_field(v9_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v9_summary, "baseV8RunnerSourceSha256"),
        "4d3be4619b7fbe5f5ca39e4065914fe7bb2a56dcbbfb6ae67c95cf444140edfc"
    );
    assert_eq!(
        head_field(v9_summary, "baseV8FixtureSha256"),
        "82eca291e69ec27e49903d31b1da408f68962469780a1f706f3f979564e8aebb"
    );
    assert_eq!(
        head_field(v9_summary, "probeSourceSha256"),
        "caf6a7f25cc36cbe7480c7cb798a8c900bbd526fa7e4071d625724045bb88af5"
    );
    assert_eq!(
        head_field(v9_summary, "runnerSourceSha256"),
        "368724efe73e194aff024d68204d758d089d81511e9bbfaa4dfb9ef9516f4c48"
    );
    assert_eq!(
        head_field(v9_summary, "emittedBodySha256"),
        "dbe0a891add7c613c340dfaee983a75c97b20cba4744ca19619e10cd9f7a78f5"
    );
    assert_eq!(
        head_field(v9_summary, "semanticPassSha256"),
        "8e271204197c0d84afe4948a94f6723f6a419cd9611495aa8ca74fb7731bbf95"
    );
    assert_eq!(
        head_field(v9_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v9_summary, "freshRuns"), "2");
    assert_eq!(head_field(v9_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v9_summary, "nativeScope"),
        "ReturnedOrder9HeadContinuation"
    );
    assert_eq!(
        head_field(v9_summary, "javaEvidence"),
        "ReturnedBeforeEleventhHead"
    );

    // Boundary 36 expected-only v10 rows pin order-10's unchanged
    // prelinked continuation and its two ordered closure writes.
    let v10_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v10.txt"),
    )
    .expect("expected-only order10 head continuation fixture");
    assert_eq!(
        sha256_hex(v10_text.as_bytes()),
        "7b0bf32fcf75cf792eb67c2c8a52ae9702de215078a54bea7edc7cde853869d0"
    );
    let v10_rows = v10_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v10_rows.len(), 19);
    let v10_body = format!("{}\n", v10_rows[..18].join("\n"));
    assert_eq!(
        sha256_hex(v10_body.as_bytes()),
        "cbab4a06edd591e068007152dbb623d206a29c450aa2f9a153c75010fa184658"
    );
    let v10_java = v10_rows[17];
    assert_eq!(head_field(v10_java, "headOrder"), "10");
    assert_eq!(head_field(v10_java, "headX"), "65");
    assert_eq!(head_field(v10_java, "headSig"), "95");
    assert_eq!(head_field(v10_java, "headInterId"), "1479");
    assert_eq!(
        head_field(v10_java, "grade"),
        "0x1.85b7d8115ac0dp-1/3fe85b7d8115ac0d"
    );
    assert_eq!(
        head_field(v10_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v10_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v10_java, "incident"),
        "[stem2346:headSideLEFT:heads[x64:sig94:id1477:sideLEFT,x65:sig95:id1479:sideLEFT]]"
    );
    assert_eq!(head_field(v10_java, "returned"), "true");
    assert_eq!(
        head_field(v10_java, "closureWrites"),
        "[x64:sig94:LEFT:false->true,x64:sig94:RIGHT:false->true]"
    );
    assert_eq!(head_field(v10_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v10_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v10_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v10_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v10_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v10_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v10_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v10_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v10_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v10_java, "linkerStateHashAfter"),
        "5f019c1fbcb69e64f83a6eeef4d78d474a3092b4c6201ecae7c17c597acdd4cf"
    );
    assert_eq!(head_field(v10_java, "nextHeadOrder"), "11");
    assert_eq!(head_field(v10_java, "nextHeadX"), "46");
    assert_eq!(head_field(v10_java, "nextHeadSig"), "57");
    assert_eq!(head_field(v10_java, "nextHeadInterId"), "1403");
    assert_eq!(
        head_field(v10_java, "nextGrade"),
        "0x1.85b4639064df8p-1/3fe85b4639064df8"
    );
    assert_eq!(head_field(v10_java, "terminal"), "ReturnedBeforeNextHead");
    let v10_summary = v10_rows[18];
    assert_eq!(
        head_field(v10_summary, "schema"),
        "stems-head-phase-prefix-v10"
    );
    assert_eq!(head_field(v10_summary, "rows"), "18");
    assert_eq!(
        head_field(v10_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v10_summary, "baseV9RunnerSourceSha256"),
        "368724efe73e194aff024d68204d758d089d81511e9bbfaa4dfb9ef9516f4c48"
    );
    assert_eq!(
        head_field(v10_summary, "baseV9FixtureSha256"),
        "b0d3c67f9b76a56a528d8a962f3f2bc54710616f2e86650ac8500e750534ff2c"
    );
    assert_eq!(
        head_field(v10_summary, "probeSourceSha256"),
        "e7cf9dd3ceed19c3e387eabffb587005acb01725434776fe39501605ce4cd4af"
    );
    assert_eq!(
        head_field(v10_summary, "runnerSourceSha256"),
        "ddf5b4c3f6d726c3e7d91de33d077930ff29254f1e7e84751ee391614978c464"
    );
    assert_eq!(
        head_field(v10_summary, "emittedBodySha256"),
        "cbab4a06edd591e068007152dbb623d206a29c450aa2f9a153c75010fa184658"
    );
    assert_eq!(
        head_field(v10_summary, "semanticPassSha256"),
        "d5cd5dbed69852e48add157efd936ba8501879c30a023e730e0825c38825b712"
    );
    assert_eq!(
        head_field(v10_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v10_summary, "freshRuns"), "2");
    assert_eq!(head_field(v10_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v10_summary, "nativeScope"),
        "ReturnedOrder10HeadContinuation"
    );
    assert_eq!(
        head_field(v10_summary, "javaEvidence"),
        "ReturnedBeforeTwelfthHead"
    );

    // Boundary 37 expected-only v11 rows pin order-11's prelinked
    // continuation and its four ordered closure writes.
    let v11_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v11.txt"),
    )
    .expect("expected-only order11 head continuation fixture");
    assert_eq!(
        sha256_hex(v11_text.as_bytes()),
        "cad1527e556481a073ead938094de9edce09954e366bf5608ebc57a30ef946a3"
    );
    let v11_rows = v11_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v11_rows.len(), 20);
    let v11_body = format!("{}\n", v11_rows[..19].join("\n"));
    assert_eq!(
        sha256_hex(v11_body.as_bytes()),
        "a0716a3379db5d268419624a193d6a6d1dc0105f78ff56fecd44fa70272165e4"
    );
    let v11_java = v11_rows[18];
    assert_eq!(head_field(v11_java, "headOrder"), "11");
    assert_eq!(head_field(v11_java, "headX"), "46");
    assert_eq!(head_field(v11_java, "headSig"), "57");
    assert_eq!(head_field(v11_java, "headInterId"), "1403");
    assert_eq!(
        head_field(v11_java, "grade"),
        "0x1.85b4639064df8p-1/3fe85b4639064df8"
    );
    assert_eq!(
        head_field(v11_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v11_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v11_java, "incident"),
        "[stem2377:headSideLEFT:heads[x44:sig70:id1429:sideLEFT,x45:sig56:id1401:sideLEFT,x46:sig57:id1403:sideLEFT]]"
    );
    assert_eq!(head_field(v11_java, "returned"), "true");
    assert_eq!(
        head_field(v11_java, "closureWrites"),
        "[x44:sig70:LEFT:false->true,x44:sig70:RIGHT:false->true,x45:sig56:LEFT:false->true,x45:sig56:RIGHT:false->true]"
    );
    assert_eq!(head_field(v11_java, "closedValueChanges"), "4");
    assert_eq!(head_field(v11_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v11_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v11_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v11_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v11_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v11_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v11_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v11_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v11_java, "linkerStateHashAfter"),
        "290d5afd502d8a35610f27e96cd78797bcfc01c1a66cce89693d4084c65e4a0d"
    );
    assert_eq!(head_field(v11_java, "nextHeadOrder"), "12");
    assert_eq!(head_field(v11_java, "nextHeadX"), "55");
    assert_eq!(head_field(v11_java, "nextHeadSig"), "79");
    assert_eq!(head_field(v11_java, "nextHeadInterId"), "1447");
    assert_eq!(
        head_field(v11_java, "nextGrade"),
        "0x1.847463fc14b09p-1/3fe847463fc14b09"
    );
    assert_eq!(head_field(v11_java, "terminal"), "ReturnedBeforeNextHead");
    let v11_summary = v11_rows[19];
    assert_eq!(
        head_field(v11_summary, "schema"),
        "stems-head-phase-prefix-v11"
    );
    assert_eq!(head_field(v11_summary, "rows"), "19");
    assert_eq!(
        head_field(v11_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v11_summary, "baseV10RunnerSourceSha256"),
        "ddf5b4c3f6d726c3e7d91de33d077930ff29254f1e7e84751ee391614978c464"
    );
    assert_eq!(
        head_field(v11_summary, "baseV10FixtureSha256"),
        "7b0bf32fcf75cf792eb67c2c8a52ae9702de215078a54bea7edc7cde853869d0"
    );
    assert_eq!(
        head_field(v11_summary, "probeSourceSha256"),
        "24f67a53e407909d07e1fc12bb2e180b15e6dfcf74983d52d1326cff906284ca"
    );
    assert_eq!(
        head_field(v11_summary, "runnerSourceSha256"),
        "f05ea06f61193785a84440b457b4e79b10e7d88e765b81bce51d6f996beb1702"
    );
    assert_eq!(
        head_field(v11_summary, "emittedBodySha256"),
        "a0716a3379db5d268419624a193d6a6d1dc0105f78ff56fecd44fa70272165e4"
    );
    assert_eq!(
        head_field(v11_summary, "semanticPassSha256"),
        "eefa750fd63fa91fec84c2fd9afc62b82d51081da606a0687496a111f5059602"
    );
    assert_eq!(
        head_field(v11_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v11_summary, "freshRuns"), "2");
    assert_eq!(head_field(v11_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v11_summary, "nativeScope"),
        "ReturnedOrder11HeadContinuation"
    );
    assert_eq!(
        head_field(v11_summary, "javaEvidence"),
        "ReturnedBeforeThirteenthHead"
    );

    // Boundary 38 expected-only v12 rows use the bounded snapshot-minimized
    // replay and pin the exact order-12 continuation.
    let v12_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v12.txt"),
    )
    .expect("expected-only order12 head continuation fixture");
    assert_eq!(
        sha256_hex(v12_text.as_bytes()),
        "e8b19156d29722a74b41e6d07d1591edd78b3077844f6be7268fa78754a1acd2"
    );
    let v12_rows = v12_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v12_rows.len(), 8);
    let v12_body = format!("{}\n", v12_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v12_body.as_bytes()),
        "ab41455ece56d8cce145f1105a417315be379f3c6d644efca539d008db1c099a"
    );
    let v12_java = v12_rows[6];
    assert_eq!(head_field(v12_java, "headOrder"), "12");
    assert_eq!(head_field(v12_java, "headX"), "55");
    assert_eq!(head_field(v12_java, "headSig"), "79");
    assert_eq!(head_field(v12_java, "headInterId"), "1447");
    assert_eq!(
        head_field(v12_java, "grade"),
        "0x1.847463fc14b09p-1/3fe847463fc14b09"
    );
    assert_eq!(
        head_field(v12_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v12_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v12_java, "incident"),
        "[stem2362:headSideLEFT:heads[x51:sig82:id1453:sideLEFT,x54:sig78:id1445:sideLEFT,x55:sig79:id1447:sideLEFT]]"
    );
    assert_eq!(head_field(v12_java, "returned"), "true");
    assert_eq!(
        head_field(v12_java, "closureWrites"),
        "[x51:sig82:LEFT:false->true,x51:sig82:RIGHT:false->true,x54:sig78:LEFT:false->true,x54:sig78:RIGHT:false->true]"
    );
    assert_eq!(head_field(v12_java, "closedValueChanges"), "4");
    assert_eq!(head_field(v12_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v12_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v12_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v12_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v12_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v12_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v12_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v12_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v12_java, "linkerStateHashAfter"),
        "961b49275376d451b946fddb7ac5da99d997e1804870505f559154598209a9db"
    );
    assert_eq!(head_field(v12_java, "nextHeadOrder"), "13");
    assert_eq!(head_field(v12_java, "nextHeadX"), "53");
    assert_eq!(head_field(v12_java, "nextHeadSig"), "3");
    assert_eq!(head_field(v12_java, "nextHeadInterId"), "1291");
    assert_eq!(
        head_field(v12_java, "nextGrade"),
        "0x1.83971fb8b04c3p-1/3fe83971fb8b04c3"
    );
    assert_eq!(head_field(v12_java, "terminal"), "ReturnedBeforeNextHead");
    let v12_summary = v12_rows[7];
    assert_eq!(
        head_field(v12_summary, "schema"),
        "stems-head-phase-prefix-v12"
    );
    assert_eq!(head_field(v12_summary, "rows"), "7");
    assert_eq!(
        head_field(v12_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v12_summary, "baseV11RunnerSourceSha256"),
        "f05ea06f61193785a84440b457b4e79b10e7d88e765b81bce51d6f996beb1702"
    );
    assert_eq!(
        head_field(v12_summary, "baseV11FixtureSha256"),
        "cad1527e556481a073ead938094de9edce09954e366bf5608ebc57a30ef946a3"
    );
    assert_eq!(
        head_field(v12_summary, "probeSourceSha256"),
        "7b8f232f56d92f83966311478de6b0255820d6d00c9aa4dbb3f0f9351c43abc6"
    );
    assert_eq!(
        head_field(v12_summary, "runnerSourceSha256"),
        "74b6ba4f84c046ae2ca08e270ce9726acee42a14f4b639282bfbccd3c8b654d1"
    );
    assert_eq!(
        head_field(v12_summary, "emittedBodySha256"),
        "ab41455ece56d8cce145f1105a417315be379f3c6d644efca539d008db1c099a"
    );
    assert_eq!(
        head_field(v12_summary, "semanticPassSha256"),
        "ad4dd95c5b9c12f101a8c2420cca76902e7cc7571b3277bfbd879a6ba4bcda67"
    );
    assert_eq!(
        head_field(v12_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v12_summary, "freshRuns"), "2");
    assert_eq!(head_field(v12_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v12_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder12"
    );
    assert_eq!(
        head_field(v12_summary, "javaEvidence"),
        "ReturnedBeforeThirteenthHead"
    );

    // Boundary 39 expected-only v13 rows use the bounded order-13 replay.
    let v13_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v13.txt"),
    )
    .expect("expected-only order13 head continuation fixture");
    assert_eq!(
        sha256_hex(v13_text.as_bytes()),
        "ff27fa03e80e44e554d46682c827097ecec1d463abf0c0e131a6ab1beccfbb5e"
    );
    let v13_rows = v13_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v13_rows.len(), 8);
    let v13_body = format!("{}\n", v13_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v13_body.as_bytes()),
        "84254e3f9dc1e4297b4efaabb30c36d07244ffe3d268cce5097ec14d365ab974"
    );
    let v13_java = v13_rows[6];
    assert_eq!(head_field(v13_java, "headOrder"), "13");
    assert_eq!(head_field(v13_java, "headX"), "53");
    assert_eq!(head_field(v13_java, "headSig"), "3");
    assert_eq!(head_field(v13_java, "headInterId"), "1291");
    assert_eq!(
        head_field(v13_java, "grade"),
        "0x1.83971fb8b04c3p-1/3fe83971fb8b04c3"
    );
    assert_eq!(
        head_field(v13_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v13_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v13_java, "incident"),
        "[stem2344:headSideLEFT:heads[x52:sig2:id1289:sideLEFT,x53:sig3:id1291:sideLEFT]]"
    );
    assert_eq!(head_field(v13_java, "returned"), "true");
    assert_eq!(
        head_field(v13_java, "closureWrites"),
        "[x52:sig2:LEFT:false->true,x52:sig2:RIGHT:false->true]"
    );
    assert_eq!(head_field(v13_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v13_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v13_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v13_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v13_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v13_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v13_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v13_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v13_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v13_java, "linkerStateHashAfter"),
        "e18f05589cf40c344ad16d92422f6d079d8f1bba992f22d18b1de87622680fa7"
    );
    assert_eq!(head_field(v13_java, "nextHeadOrder"), "14");
    assert_eq!(head_field(v13_java, "nextHeadX"), "12");
    assert_eq!(head_field(v13_java, "nextHeadSig"), "63");
    assert_eq!(head_field(v13_java, "nextHeadInterId"), "1415");
    assert_eq!(
        head_field(v13_java, "nextGrade"),
        "0x1.8187dd5fbfd0cp-1/3fe8187dd5fbfd0c"
    );
    assert_eq!(head_field(v13_java, "terminal"), "ReturnedBeforeNextHead");
    let v13_summary = v13_rows[7];
    assert_eq!(
        head_field(v13_summary, "schema"),
        "stems-head-phase-prefix-v13"
    );
    assert_eq!(head_field(v13_summary, "rows"), "7");
    assert_eq!(
        head_field(v13_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v13_summary, "baseV12RunnerSourceSha256"),
        "74b6ba4f84c046ae2ca08e270ce9726acee42a14f4b639282bfbccd3c8b654d1"
    );
    assert_eq!(
        head_field(v13_summary, "baseV12FixtureSha256"),
        "e8b19156d29722a74b41e6d07d1591edd78b3077844f6be7268fa78754a1acd2"
    );
    assert_eq!(
        head_field(v13_summary, "probeSourceSha256"),
        "915bc4a3563943b93fa806a614b835da8e7799732cf8c1c1c7aa9127fc39a61e"
    );
    assert_eq!(
        head_field(v13_summary, "runnerSourceSha256"),
        "675bce84bfa4e76ed78cc72592da9f8fe95571752d424da99bd4be93af7478f8"
    );
    assert_eq!(
        head_field(v13_summary, "emittedBodySha256"),
        "84254e3f9dc1e4297b4efaabb30c36d07244ffe3d268cce5097ec14d365ab974"
    );
    assert_eq!(
        head_field(v13_summary, "semanticPassSha256"),
        "f2b4a2e49aee6fd27d41470eb38a1bfe541d72688b03bb33d5b3ed3266514519"
    );
    assert_eq!(
        head_field(v13_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v13_summary, "freshRuns"), "2");
    assert_eq!(head_field(v13_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v13_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder13"
    );
    assert_eq!(
        head_field(v13_summary, "javaEvidence"),
        "ReturnedBeforeFourteenthHead"
    );

    // Boundary 40 expected-only v14 rows use the bounded order-14 replay.
    let v14_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v14.txt"),
    )
    .expect("expected-only order14 head continuation fixture");
    assert_eq!(
        sha256_hex(v14_text.as_bytes()),
        "f60e5dff377e5e51038ec061b1ebeec5a5868f4cd51af6b9618377bfa3a12e6a"
    );
    let v14_rows = v14_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v14_rows.len(), 8);
    let v14_body = format!("{}\n", v14_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v14_body.as_bytes()),
        "9ebf233711be059ddee5adf964b6bbbbe44770caef19f5903c8ce9a5a16d1889"
    );
    let v14_java = v14_rows[6];
    assert_eq!(head_field(v14_java, "headOrder"), "14");
    assert_eq!(head_field(v14_java, "headX"), "12");
    assert_eq!(head_field(v14_java, "headSig"), "63");
    assert_eq!(head_field(v14_java, "headInterId"), "1415");
    assert_eq!(
        head_field(v14_java, "grade"),
        "0x1.8187dd5fbfd0cp-1/3fe8187dd5fbfd0c"
    );
    assert_eq!(
        head_field(v14_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v14_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v14_java, "incident"),
        "[stem2349:headSideLEFT:heads[x11:sig62:id1413:sideLEFT,x12:sig63:id1415:sideLEFT]]"
    );
    assert_eq!(head_field(v14_java, "returned"), "true");
    assert_eq!(
        head_field(v14_java, "closureWrites"),
        "[x11:sig62:LEFT:false->true,x11:sig62:RIGHT:false->true]"
    );
    assert_eq!(head_field(v14_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v14_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v14_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v14_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v14_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v14_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v14_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v14_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v14_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v14_java, "linkerStateHashAfter"),
        "cc5568f753b9568b96fdd7d4eb4c1d69342a7df4a580893e99f8c9818c569587"
    );
    assert_eq!(head_field(v14_java, "nextHeadOrder"), "15");
    assert_eq!(head_field(v14_java, "nextHeadX"), "67");
    assert_eq!(head_field(v14_java, "nextHeadSig"), "59");
    assert_eq!(head_field(v14_java, "nextHeadInterId"), "1407");
    assert_eq!(
        head_field(v14_java, "nextGrade"),
        "0x1.814269b1247c7p-1/3fe814269b1247c7"
    );
    assert_eq!(head_field(v14_java, "terminal"), "ReturnedBeforeNextHead");
    let v14_summary = v14_rows[7];
    assert_eq!(
        head_field(v14_summary, "schema"),
        "stems-head-phase-prefix-v14"
    );
    assert_eq!(head_field(v14_summary, "rows"), "7");
    assert_eq!(
        head_field(v14_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v14_summary, "baseV13RunnerSourceSha256"),
        "675bce84bfa4e76ed78cc72592da9f8fe95571752d424da99bd4be93af7478f8"
    );
    assert_eq!(
        head_field(v14_summary, "baseV13FixtureSha256"),
        "ff27fa03e80e44e554d46682c827097ecec1d463abf0c0e131a6ab1beccfbb5e"
    );
    assert_eq!(
        head_field(v14_summary, "probeSourceSha256"),
        "eea0869093b1c1a262da5da0d7ad914f3dc7b6a8d771a32bc60849687291c834"
    );
    assert_eq!(
        head_field(v14_summary, "runnerSourceSha256"),
        "6b5e339f8b91db08d4e03edf7ed3b69ea8ab713b98ce95c62a95440a0652ccb9"
    );
    assert_eq!(
        head_field(v14_summary, "emittedBodySha256"),
        "9ebf233711be059ddee5adf964b6bbbbe44770caef19f5903c8ce9a5a16d1889"
    );
    assert_eq!(
        head_field(v14_summary, "semanticPassSha256"),
        "14d0e0c71dff0f40e5745858ad10d615c56463291cf6caa863edd2ebccde0590"
    );
    assert_eq!(
        head_field(v14_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v14_summary, "freshRuns"), "2");
    assert_eq!(head_field(v14_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v14_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder14"
    );
    assert_eq!(
        head_field(v14_summary, "javaEvidence"),
        "ReturnedBeforeFifteenthHead"
    );

    // Boundary 41 expected-only v15 rows use the bounded order-15 replay.
    let v15_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v15.txt"),
    )
    .expect("expected-only order15 head continuation fixture");
    assert_eq!(
        sha256_hex(v15_text.as_bytes()),
        "aae5116a32e0fd77bb9f4a26dc1a8c1cd53a3f3ff35ea01d350c97012a146ca8"
    );
    let v15_rows = v15_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v15_rows.len(), 8);
    let v15_body = format!("{}\n", v15_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v15_body.as_bytes()),
        "1e198195daf91b8d56ebcc2a88a5e97fc2752603f365d0d5cea3145f9a1f1ef2"
    );
    let v15_java = v15_rows[6];
    assert_eq!(head_field(v15_java, "headOrder"), "15");
    assert_eq!(head_field(v15_java, "headX"), "67");
    assert_eq!(head_field(v15_java, "headSig"), "59");
    assert_eq!(head_field(v15_java, "headInterId"), "1407");
    assert_eq!(
        head_field(v15_java, "grade"),
        "0x1.814269b1247c7p-1/3fe814269b1247c7"
    );
    assert_eq!(
        head_field(v15_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v15_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v15_java, "incident"),
        "[stem2375:headSideLEFT:heads[x66:sig58:id1405:sideLEFT,x67:sig59:id1407:sideLEFT]]"
    );
    assert_eq!(head_field(v15_java, "returned"), "true");
    assert_eq!(
        head_field(v15_java, "closureWrites"),
        "[x66:sig58:LEFT:false->true,x66:sig58:RIGHT:false->true]"
    );
    assert_eq!(head_field(v15_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v15_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v15_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v15_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v15_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v15_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v15_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v15_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v15_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v15_java, "linkerStateHashAfter"),
        "8a3405351d0fef0f51341cf08dfcbbf809499b31ff96cb46656498023515edc2"
    );
    assert_eq!(head_field(v15_java, "nextHeadOrder"), "16");
    assert_eq!(head_field(v15_java, "nextHeadX"), "8");
    assert_eq!(head_field(v15_java, "nextHeadSig"), "53");
    assert_eq!(head_field(v15_java, "nextHeadInterId"), "1395");
    assert_eq!(
        head_field(v15_java, "nextGrade"),
        "0x1.81161126880f9p-1/3fe81161126880f9"
    );
    assert_eq!(head_field(v15_java, "terminal"), "ReturnedBeforeNextHead");
    let v15_summary = v15_rows[7];
    assert_eq!(
        head_field(v15_summary, "schema"),
        "stems-head-phase-prefix-v15"
    );
    assert_eq!(head_field(v15_summary, "rows"), "7");
    assert_eq!(
        head_field(v15_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v15_summary, "baseV14RunnerSourceSha256"),
        "6b5e339f8b91db08d4e03edf7ed3b69ea8ab713b98ce95c62a95440a0652ccb9"
    );
    assert_eq!(
        head_field(v15_summary, "baseV14FixtureSha256"),
        "f60e5dff377e5e51038ec061b1ebeec5a5868f4cd51af6b9618377bfa3a12e6a"
    );
    assert_eq!(
        head_field(v15_summary, "probeSourceSha256"),
        "98ac227864e84c3693d5368a85adf970512648a9a99c74a2b612a01d4b45d065"
    );
    assert_eq!(
        head_field(v15_summary, "runnerSourceSha256"),
        "e595eefa74453ecfe9980cb294b80d37d0ff5ad1e2f3e01d88f8801d0f23ca18"
    );
    assert_eq!(
        head_field(v15_summary, "emittedBodySha256"),
        "1e198195daf91b8d56ebcc2a88a5e97fc2752603f365d0d5cea3145f9a1f1ef2"
    );
    assert_eq!(
        head_field(v15_summary, "semanticPassSha256"),
        "55323828f0e4c8e08d85373684f71b7ec9a6f2e75a49278006dae1b8ec673cd9"
    );
    assert_eq!(
        head_field(v15_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v15_summary, "freshRuns"), "2");
    assert_eq!(head_field(v15_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v15_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder15"
    );
    assert_eq!(
        head_field(v15_summary, "javaEvidence"),
        "ReturnedBeforeSixteenthHead"
    );

    // Boundary 42 expected-only v16 rows use the bounded order-16 replay.
    let v16_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v16.txt"),
    )
    .expect("expected-only order16 head continuation fixture");
    assert_eq!(
        sha256_hex(v16_text.as_bytes()),
        "04d35bb21c808dc38edd93c0631b3a01af9931efc8f500422646adf8f7123de4"
    );
    let v16_rows = v16_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v16_rows.len(), 8);
    let v16_body = format!("{}\n", v16_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v16_body.as_bytes()),
        "49b97d61e08769b58a449edf2931313f91a5855000fd89e27761330f30a81077"
    );
    let v16_java = v16_rows[6];
    assert_eq!(head_field(v16_java, "headOrder"), "16");
    assert_eq!(head_field(v16_java, "headX"), "8");
    assert_eq!(head_field(v16_java, "headSig"), "53");
    assert_eq!(head_field(v16_java, "headInterId"), "1395");
    assert_eq!(
        head_field(v16_java, "grade"),
        "0x1.81161126880f9p-1/3fe81161126880f9"
    );
    assert_eq!(
        head_field(v16_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v16_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v16_java, "incident"),
        "[stem2376:headSideLEFT:heads[x8:sig53:id1395:sideLEFT,x7:sig52:id1393:sideLEFT]]"
    );
    assert_eq!(head_field(v16_java, "returned"), "true");
    assert_eq!(
        head_field(v16_java, "closureWrites"),
        "[x7:sig52:LEFT:false->true,x7:sig52:RIGHT:false->true]"
    );
    assert_eq!(head_field(v16_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v16_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v16_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v16_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v16_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v16_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v16_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v16_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v16_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v16_java, "linkerStateHashAfter"),
        "a210aea1b2352f953c50ca0e29a559fdc8ff633b55e36e0333016e456bb2925d"
    );
    assert_eq!(head_field(v16_java, "nextHeadOrder"), "17");
    assert_eq!(head_field(v16_java, "nextHeadX"), "48");
    assert_eq!(head_field(v16_java, "nextHeadSig"), "29");
    assert_eq!(head_field(v16_java, "nextHeadInterId"), "1343");
    assert_eq!(
        head_field(v16_java, "nextGrade"),
        "0x1.80cc40bda9d4cp-1/3fe80cc40bda9d4c"
    );
    assert_eq!(head_field(v16_java, "terminal"), "ReturnedBeforeNextHead");
    let v16_summary = v16_rows[7];
    assert_eq!(
        head_field(v16_summary, "schema"),
        "stems-head-phase-prefix-v16"
    );
    assert_eq!(head_field(v16_summary, "rows"), "7");
    assert_eq!(
        head_field(v16_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v16_summary, "baseV15RunnerSourceSha256"),
        "e595eefa74453ecfe9980cb294b80d37d0ff5ad1e2f3e01d88f8801d0f23ca18"
    );
    assert_eq!(
        head_field(v16_summary, "baseV15FixtureSha256"),
        "aae5116a32e0fd77bb9f4a26dc1a8c1cd53a3f3ff35ea01d350c97012a146ca8"
    );
    assert_eq!(
        head_field(v16_summary, "probeSourceSha256"),
        "d4dcad17952d2de86de193bd87c3a96916ad7781d67f1ea469180e05e4e106fd"
    );
    assert_eq!(
        head_field(v16_summary, "runnerSourceSha256"),
        "d6edd52b746acd625c2e516f328c4b43253e23bbbe906ffcdae0b3674eae1dcf"
    );
    assert_eq!(
        head_field(v16_summary, "emittedBodySha256"),
        "49b97d61e08769b58a449edf2931313f91a5855000fd89e27761330f30a81077"
    );
    assert_eq!(
        head_field(v16_summary, "semanticPassSha256"),
        "88ea097c4a003e7493c5d28296cc6dd778486660bb6a1e3eb1bfb5aa71f40f7d"
    );
    assert_eq!(
        head_field(v16_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v16_summary, "freshRuns"), "2");
    assert_eq!(head_field(v16_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v16_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder16"
    );
    assert_eq!(
        head_field(v16_summary, "javaEvidence"),
        "ReturnedBeforeSeventeenthHead"
    );

    // Boundary 43 expected-only v17 rows use the bounded order-17 replay.
    let v17_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v17.txt"),
    )
    .expect("expected-only order17 head continuation fixture");
    assert_eq!(
        sha256_hex(v17_text.as_bytes()),
        "8e4909edc2196f2baff6f517693f9a9af50405cf85fc88bcf3e771711bae2b4b"
    );
    let v17_rows = v17_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v17_rows.len(), 8);
    let v17_body = format!("{}\n", v17_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v17_body.as_bytes()),
        "2362b903486db2d4ddbc14aeeeb54761205bdd06a206875ef0c131a7a22e5fdd"
    );
    let v17_java = v17_rows[6];
    assert_eq!(head_field(v17_java, "headOrder"), "17");
    assert_eq!(head_field(v17_java, "headX"), "48");
    assert_eq!(head_field(v17_java, "headSig"), "29");
    assert_eq!(head_field(v17_java, "headInterId"), "1343");
    assert_eq!(
        head_field(v17_java, "grade"),
        "0x1.80cc40bda9d4cp-1/3fe80cc40bda9d4c"
    );
    assert_eq!(
        head_field(v17_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v17_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v17_java, "incident"),
        "[stem2351:headSideLEFT:heads[x47:sig28:id1341:sideLEFT,x48:sig29:id1343:sideLEFT]]"
    );
    assert_eq!(head_field(v17_java, "returned"), "true");
    assert_eq!(
        head_field(v17_java, "closureWrites"),
        "[x47:sig28:LEFT:false->true,x47:sig28:RIGHT:false->true]"
    );
    assert_eq!(head_field(v17_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v17_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v17_java, "sigVerticesBefore"), "680");
    assert_eq!(head_field(v17_java, "sigVerticesAfter"), "680");
    assert_eq!(head_field(v17_java, "sigEdgesBefore"), "691");
    assert_eq!(head_field(v17_java, "sigEdgesAfter"), "691");
    assert_eq!(head_field(v17_java, "systemStemsBefore"), "41");
    assert_eq!(head_field(v17_java, "systemStemsAfter"), "41");
    assert_eq!(
        head_field(v17_java, "relationStateHashAfter"),
        "5418e578e4046d8d162f5f584d8969c2b4a7d447f73009b5bed39fd0ea739e44"
    );
    assert_eq!(
        head_field(v17_java, "linkerStateHashAfter"),
        "493f03a2d6459d24225ca1d9b03c576c4425f59f8e391e9cb13a9d347fd0b550"
    );
    assert_eq!(head_field(v17_java, "nextHeadOrder"), "18");
    assert_eq!(head_field(v17_java, "nextHeadX"), "63");
    assert_eq!(head_field(v17_java, "nextHeadSig"), "17");
    assert_eq!(head_field(v17_java, "nextHeadInterId"), "1319");
    assert_eq!(
        head_field(v17_java, "nextGrade"),
        "0x1.8009e50c15bf8p-1/3fe8009e50c15bf8"
    );
    assert_eq!(head_field(v17_java, "terminal"), "ReturnedBeforeNextHead");
    let v17_summary = v17_rows[7];
    assert_eq!(
        head_field(v17_summary, "schema"),
        "stems-head-phase-prefix-v17"
    );
    assert_eq!(head_field(v17_summary, "rows"), "7");
    assert_eq!(
        head_field(v17_summary, "baseProbeSourceSha256"),
        sha256_hex(
            &std::fs::read(repo_root().join("rust/oracle/java/StemsBeamSidesLoopProbe.java"))
                .expect("active shared base head-phase probe")
        )
    );
    assert_eq!(
        head_field(v17_summary, "baseV16RunnerSourceSha256"),
        "d6edd52b746acd625c2e516f328c4b43253e23bbbe906ffcdae0b3674eae1dcf"
    );
    assert_eq!(
        head_field(v17_summary, "baseV16FixtureSha256"),
        "04d35bb21c808dc38edd93c0631b3a01af9931efc8f500422646adf8f7123de4"
    );
    assert_eq!(
        head_field(v17_summary, "probeSourceSha256"),
        "b139149dd41b5581d96344617c2f52b49a85f085f011ff4b556b237f58765342"
    );
    assert_eq!(
        head_field(v17_summary, "runnerSourceSha256"),
        "84c176b45ec8adb7af8e0ab1014acabfe8c57c2e6b3cbbe5e8bbd0e971823196"
    );
    assert_eq!(
        head_field(v17_summary, "emittedBodySha256"),
        "2362b903486db2d4ddbc14aeeeb54761205bdd06a206875ef0c131a7a22e5fdd"
    );
    assert_eq!(
        head_field(v17_summary, "semanticPassSha256"),
        "c89f5a49456af435e2fb508e0ccbbd5a7b8fd9877616534cb7136ccd0ff84ecf"
    );
    assert_eq!(
        head_field(v17_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v17_summary, "freshRuns"), "2");
    assert_eq!(head_field(v17_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v17_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder17"
    );
    assert_eq!(
        head_field(v17_summary, "javaEvidence"),
        "ReturnedBeforeEighteenthHead"
    );

    // Boundary 44 expected-only v18 rows cover the first open C-link frontier.
    let v18_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v18.txt"),
    )
    .expect("expected-only order18 head continuation fixture");
    assert_eq!(
        sha256_hex(v18_text.as_bytes()),
        "4972836c5e2718f9441a007840cfc5100caa95a12dc349d7822c0695ad0f5b2b"
    );
    let v18_rows = v18_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v18_rows.len(), 10);
    let v18_body = format!("{}\n", v18_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v18_body.as_bytes()),
        "499b791dc34d2ca59666bbab20e4ca15a9dd335260d4714dbdd9042ed00456cd"
    );
    let v18_frontier = v18_rows[6];
    assert_eq!(head_field(v18_frontier, "headOrder"), "18");
    assert_eq!(head_field(v18_frontier, "headSig"), "17");
    assert_eq!(head_field(v18_frontier, "cAlias"), "h:63:LEFT:BOTTOM");
    assert_eq!(head_field(v18_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v18_frontier, "maxIndex"), "1");
    assert_eq!(
        head_field(v18_frontier, "relationRows"),
        "[h:63:LEFT:BOTTOM:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.f839e420c5cc5p-1/3fef839e420c5cc5:impacts=[xInGap:0x1.f091ffac365b7p-1/3fef091ffac365b7:w=0x1.0p0/3ff0000000000000,yGap:0x1.0p0/3ff0000000000000:w=0x1.0p0/3ff0000000000000]:dx=-0x1.8b000860f6db7p-7/bf88b000860f6db7:dy=0x0.0p0/0000000000000000:extension=0x1.89d96acc1ea21p10/40989d96acc1ea21:0x1.72p8/4077200000000000:headSide=LEFT:consistency=0x1.0p0/3ff0000000000000]"
    );
    assert_eq!(
        head_field(v18_frontier, "selected"),
        "[glyph:328:active:id=328:g:1574:379:3:83:621d7dbd890db99d5fd14bdf469bb58ec3cdab734a4ad9648355ce8d7303fa4b,glyph:2063:active:id=2063:g:1575:379:2:83:c691da8b9802b753b5471f5389c71030a8fb4a4aa4f65b9eebbfc4dd9f47147b]"
    );
    assert_eq!(head_field(v18_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v18_frontier, "existingGlyph"), "glyph:328");
    assert_eq!(
        head_field(v18_frontier, "terminal"),
        "ReadyForHeadCreateStem"
    );
    let v18_result = v18_rows[7];
    assert_eq!(head_field(v18_result, "headOrder"), "18");
    assert_eq!(head_field(v18_result, "allocatorBefore"), "2380");
    assert_eq!(head_field(v18_result, "allocatorAfter"), "2381");
    assert_eq!(
        head_field(v18_result, "addedVertices"),
        "[id2381:org.audiveris.omr.sig.inter.StemInter:shape=STEM:grade=0x1.9255a2b3803d4p-1/3fe9255a2b3803d4:bounds=1574:379:4:83:removed=false:abnormal=false:manual=false:implicit=false:profile=0:median=0x1.89f6b96b44704p10/40989f6b96b44704:0x1.7bp8/4077b00000000000:0x1.89d8e4596fe94p10/40989d8e4596fe94:0x1.cep8/407ce00000000000:width=0x1.5afcea68de128p1/4005afcea68de128]"
    );
    assert_eq!(
        head_field(v18_result, "addedEdges"),
        "[system1:sourceId1319:targetId2381:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.f839e420c5c49p-1/3fef839e420c5c49:impacts=[xInGap:0x1.f091ffac364c3p-1/3fef091ffac364c3:w=0x1.0p0/3ff0000000000000,yGap:0x1.0p0/3ff0000000000000:w=0x1.0p0/3ff0000000000000]:dx=-0x1.8b000860f8618p-7/bf88b000860f8618:dy=0x0.0p0/0000000000000000:extension=0x1.89d96acc1ea22p10/40989d96acc1ea22:0x1.72p8/4077200000000000:headSide=LEFT:consistency=0x1.695c4ca037ba6p0/3ff695c4ca037ba6]"
    );
    assert_eq!(
        head_field(v18_result, "addedSystemStems"),
        "[g:1574:379:3:83:621d7dbd890db99d5fd14bdf469bb58ec3cdab734a4ad9648355ce8d7303fa4b:stemId2381]"
    );
    assert_eq!(
        head_field(v18_result, "linkerChanges"),
        "[h:63:LEFT:BOTTOM:false:false->true:false,h:63:LEFT:TOP:false:false->true:false,linker:SLinker:head:63:false:false->true:false]"
    );
    assert_eq!(
        head_field(v18_result, "relationStateHashAfter"),
        "965613e993058fb480d53f56b02beb7e59371e981a12a3ea7938b44485e4228c"
    );
    assert_eq!(
        head_field(v18_result, "linkerStateHashAfter"),
        "eee7180ad4f6af075c2cc8d7eea19b170cd04294bda2951ba4efe82c99229824"
    );
    assert_eq!(
        head_field(v18_result, "terminal"),
        "ReturnedHeadCLinkTransaction"
    );
    let v18_continue = v18_rows[8];
    assert_eq!(head_field(v18_continue, "headOrder"), "18");
    assert_eq!(head_field(v18_continue, "headX"), "63");
    assert_eq!(head_field(v18_continue, "headSig"), "17");
    assert_eq!(
        head_field(v18_continue, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v18_continue, "returned"), "true");
    assert_eq!(head_field(v18_continue, "closureWrites"), "-");
    assert_eq!(head_field(v18_continue, "sigVerticesAfter"), "681");
    assert_eq!(head_field(v18_continue, "sigEdgesAfter"), "692");
    assert_eq!(head_field(v18_continue, "systemStemsAfter"), "42");
    assert_eq!(head_field(v18_continue, "nextHeadOrder"), "19");
    assert_eq!(head_field(v18_continue, "nextHeadX"), "69");
    assert_eq!(head_field(v18_continue, "nextHeadSig"), "76");
    assert_eq!(head_field(v18_continue, "nextHeadInterId"), "1441");
    assert_eq!(
        head_field(v18_continue, "terminal"),
        "ReturnedBeforeNextHead"
    );
    let v18_summary = v18_rows[9];
    assert_eq!(
        head_field(v18_summary, "schema"),
        "stems-head-phase-prefix-v18"
    );
    assert_eq!(head_field(v18_summary, "rows"), "9");
    assert_eq!(
        head_field(v18_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v18_summary, "baseV17RunnerSourceSha256"),
        "84c176b45ec8adb7af8e0ab1014acabfe8c57c2e6b3cbbe5e8bbd0e971823196"
    );
    assert_eq!(
        head_field(v18_summary, "baseV17FixtureSha256"),
        "8e4909edc2196f2baff6f517693f9a9af50405cf85fc88bcf3e771711bae2b4b"
    );
    assert_eq!(
        head_field(v18_summary, "fragmentSourceSha256"),
        "f56fdd58606c3d5101ebea1690162b38f9db6a18f89a4fe0e441cedff1bac36c"
    );
    assert_eq!(
        head_field(v18_summary, "probeSourceSha256"),
        "4e15aa27d982b6ea848b5a7349819e3db7300349dded652f859492abe2ea7460"
    );
    assert_eq!(
        head_field(v18_summary, "runnerSourceSha256"),
        "3bea814e71ba13374130351d0f5cc057779e5676e402e7b43b5c4ee4a263e332"
    );
    assert_eq!(
        head_field(v18_summary, "semanticPassSha256"),
        "7045d9060ea8e6d930b94d28e79e3e6d8d0cc0bb0b57bb20c64a3780b876bcb3"
    );
    assert_eq!(
        head_field(v18_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v18_summary, "freshRuns"), "2");
    assert_eq!(head_field(v18_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v18_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder18WithCLinkEnvelope"
    );
    assert_eq!(
        head_field(v18_summary, "javaEvidence"),
        "ReturnedBeforeNineteenthHead"
    );

    // Boundary 45 expected-only v19 rows extend the prelinked continuation.
    let v19_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v19.txt"),
    )
    .expect("expected-only order19 head continuation fixture");
    assert_eq!(
        sha256_hex(v19_text.as_bytes()),
        "6d415102995fd1fda8057fab27b0f2a3a6cb2367cbcce52269009f377bf672ae"
    );
    let v19_rows = v19_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v19_rows.len(), 11);
    let v19_body = format!("{}\n", v19_rows[..10].join("\n"));
    assert_eq!(
        sha256_hex(v19_body.as_bytes()),
        "3ae97b86466a49fafbe07f5c32d5641824099e677131fff14aee3797f61cc3a9"
    );
    let v19_java = v19_rows[9];
    assert_eq!(head_field(v19_java, "headOrder"), "19");
    assert_eq!(head_field(v19_java, "headX"), "69");
    assert_eq!(head_field(v19_java, "headSig"), "76");
    assert_eq!(head_field(v19_java, "headInterId"), "1441");
    assert_eq!(
        head_field(v19_java, "grade"),
        "0x1.7fe09c1461c49p-1/3fe7fe09c1461c49"
    );
    assert_eq!(
        head_field(v19_java, "sidesBefore"),
        "[LEFT:true:false,RIGHT:false:false]"
    );
    assert_eq!(
        head_field(v19_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v19_java, "incident"),
        "[stem2347:headSideLEFT:heads[x68:sig75:id1439:sideLEFT,x69:sig76:id1441:sideLEFT]]"
    );
    assert_eq!(head_field(v19_java, "returned"), "true");
    assert_eq!(
        head_field(v19_java, "closureWrites"),
        "[x68:sig75:LEFT:false->true,x68:sig75:RIGHT:false->true]"
    );
    assert_eq!(head_field(v19_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v19_java, "unlinkedCount"), "0");
    assert_eq!(head_field(v19_java, "sigVerticesBefore"), "681");
    assert_eq!(head_field(v19_java, "sigVerticesAfter"), "681");
    assert_eq!(head_field(v19_java, "sigEdgesBefore"), "692");
    assert_eq!(head_field(v19_java, "sigEdgesAfter"), "692");
    assert_eq!(head_field(v19_java, "systemStemsBefore"), "42");
    assert_eq!(head_field(v19_java, "systemStemsAfter"), "42");
    assert_eq!(
        head_field(v19_java, "relationStateHashAfter"),
        "965613e993058fb480d53f56b02beb7e59371e981a12a3ea7938b44485e4228c"
    );
    assert_eq!(
        head_field(v19_java, "linkerStateHashAfter"),
        "952be0ad3dfc4db06a0c4a986c22f3353f4b0e742d42a054f4377f6fd5a8b6fb"
    );
    assert_eq!(head_field(v19_java, "nextHeadOrder"), "20");
    assert_eq!(head_field(v19_java, "nextHeadX"), "74");
    assert_eq!(head_field(v19_java, "nextHeadSig"), "19");
    assert_eq!(head_field(v19_java, "nextHeadInterId"), "1323");
    assert_eq!(
        head_field(v19_java, "nextGrade"),
        "0x1.7f8f93b5cf2p-1/3fe7f8f93b5cf200"
    );
    assert_eq!(head_field(v19_java, "terminal"), "ReturnedBeforeNextHead");
    let v19_summary = v19_rows[10];
    assert_eq!(
        head_field(v19_summary, "schema"),
        "stems-head-phase-prefix-v19"
    );
    assert_eq!(head_field(v19_summary, "rows"), "10");
    assert_eq!(
        head_field(v19_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v19_summary, "baseV18RunnerSourceSha256"),
        "3bea814e71ba13374130351d0f5cc057779e5676e402e7b43b5c4ee4a263e332"
    );
    assert_eq!(
        head_field(v19_summary, "baseV18FixtureSha256"),
        "4972836c5e2718f9441a007840cfc5100caa95a12dc349d7822c0695ad0f5b2b"
    );
    assert_eq!(
        head_field(v19_summary, "fragmentSourceSha256"),
        "f56fdd58606c3d5101ebea1690162b38f9db6a18f89a4fe0e441cedff1bac36c"
    );
    assert_eq!(
        head_field(v19_summary, "probeSourceSha256"),
        "e94082b8faa8a8c26e70b00acd42bc091e7c9333317caa5299f6d18083cba781"
    );
    assert_eq!(
        head_field(v19_summary, "runnerSourceSha256"),
        "b79cb0c5cba1d3b1275dd943d7945722a5f025281686362d6b40a311d3ad5335"
    );
    assert_eq!(
        head_field(v19_summary, "emittedBodySha256"),
        "3ae97b86466a49fafbe07f5c32d5641824099e677131fff14aee3797f61cc3a9"
    );
    assert_eq!(
        head_field(v19_summary, "semanticPassSha256"),
        "9628fefbc7e1c88ab184aa711e329b9606e4d57252965428b9f3f33e96852a31"
    );
    assert_eq!(
        head_field(v19_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v19_summary, "freshRuns"), "2");
    assert_eq!(head_field(v19_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v19_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder19WithCLinkEnvelope"
    );
    assert_eq!(
        head_field(v19_summary, "javaEvidence"),
        "ReturnedBeforeTwentiethHead"
    );

    // Boundary 46 expected-only v20 rows cover the second open C-link.
    let v20_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v20.txt"),
    )
    .expect("expected-only order20 head continuation fixture");
    assert_eq!(
        sha256_hex(v20_text.as_bytes()),
        "be6a820b3740105e4fdddeb0e9ec475d1dd3ebc8611fd7be555cf55957dfe4a4"
    );
    let v20_rows = v20_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v20_rows.len(), 12);
    let v20_body = format!("{}\n", v20_rows[..11].join("\n"));
    assert_eq!(
        sha256_hex(v20_body.as_bytes()),
        "3b1f4c53462e4ff8241863e73c90043d813cf5709cd0f3c809858659d7261564"
    );
    let v20_frontier = v20_rows[8];
    assert_eq!(head_field(v20_frontier, "headOrder"), "20");
    assert_eq!(head_field(v20_frontier, "headX"), "74");
    assert_eq!(head_field(v20_frontier, "headSig"), "19");
    assert_eq!(head_field(v20_frontier, "headInterId"), "1323");
    assert_eq!(head_field(v20_frontier, "cAlias"), "h:74:LEFT:BOTTOM");
    assert_eq!(head_field(v20_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v20_frontier, "maxIndex"), "1");
    assert_eq!(
        head_field(v20_frontier, "selected"),
        "[glyph:332:active:id=332:g:1727:379:3:81:3e36225d69cfad591cc3c85eb38ad1c2ead6240f867708e97d0041e5e81e416a,glyph:2301:active:id=2301:g:1727:379:3:81:fd64bac32251045b154d3d40c7ff0337029cce46ddd3828860233849d95e9791]"
    );
    assert_eq!(head_field(v20_frontier, "candidateIdBefore"), "0");
    assert_eq!(
        head_field(v20_frontier, "terminal"),
        "ReadyForHeadCreateStem"
    );
    let v20_result = v20_rows[9];
    assert_eq!(head_field(v20_result, "headOrder"), "20");
    assert_eq!(head_field(v20_result, "allocatorBefore"), "2381");
    assert_eq!(head_field(v20_result, "allocatorAfter"), "2382");
    assert_eq!(
        head_field(v20_result, "addedVertices"),
        "[id2382:org.audiveris.omr.sig.inter.StemInter:shape=STEM:grade=0x1.9276faafa2386p-1/3fe9276faafa2386:bounds=1727:379:4:81:removed=false:abnormal=false:manual=false:implicit=false:profile=0:median=0x1.b03eab3fe1a4ap10/409b03eab3fe1a4a:0x1.7bp8/4077b00000000000:0x1.b02c3f5bff608p10/409b02c3f5bff608:0x1.ccp8/407cc00000000000:width=0x1.1f9add3c0ca46p1/4001f9add3c0ca46]"
    );
    assert_eq!(
        head_field(v20_result, "linkerChanges"),
        "[h:74:LEFT:BOTTOM:false:false->true:false,h:74:LEFT:TOP:false:false->true:false,linker:SLinker:head:74:false:false->true:false]"
    );
    assert_eq!(
        head_field(v20_result, "relationStateHashAfter"),
        "3c50fb8ad029b80132262f4607e856ffcfadd6aa777d03bd3795288018faac9d"
    );
    assert_eq!(
        head_field(v20_result, "linkerStateHashAfter"),
        "e77ba4ade2dd66f504a66f8ce9c7d80c6e2136fdd68666e5bdaffaa2414ed95c"
    );
    assert_eq!(
        head_field(v20_result, "terminal"),
        "ReturnedHeadCLinkTransaction"
    );
    let v20_continue = v20_rows[10];
    assert_eq!(head_field(v20_continue, "headOrder"), "20");
    assert_eq!(head_field(v20_continue, "headX"), "74");
    assert_eq!(head_field(v20_continue, "headSig"), "19");
    assert_eq!(
        head_field(v20_continue, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v20_continue, "returned"), "true");
    assert_eq!(head_field(v20_continue, "sigVerticesAfter"), "682");
    assert_eq!(head_field(v20_continue, "sigEdgesAfter"), "693");
    assert_eq!(head_field(v20_continue, "systemStemsAfter"), "43");
    assert_eq!(head_field(v20_continue, "nextHeadOrder"), "21");
    assert_eq!(head_field(v20_continue, "nextHeadX"), "28");
    assert_eq!(head_field(v20_continue, "nextHeadSig"), "55");
    assert_eq!(head_field(v20_continue, "nextHeadInterId"), "1399");
    assert_eq!(
        head_field(v20_continue, "terminal"),
        "ReturnedBeforeNextHead"
    );
    let v20_summary = v20_rows[11];
    assert_eq!(
        head_field(v20_summary, "schema"),
        "stems-head-phase-prefix-v20"
    );
    assert_eq!(head_field(v20_summary, "rows"), "11");
    assert_eq!(
        head_field(v20_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v20_summary, "baseV19RunnerSourceSha256"),
        "b79cb0c5cba1d3b1275dd943d7945722a5f025281686362d6b40a311d3ad5335"
    );
    assert_eq!(
        head_field(v20_summary, "baseV19FixtureSha256"),
        "6d415102995fd1fda8057fab27b0f2a3a6cb2367cbcce52269009f377bf672ae"
    );
    assert_eq!(
        head_field(v20_summary, "fragmentSourceSha256"),
        "5fa3ac22fe21091c313135909f13c793be575fd460f0af3349345ba8ede9ab3e"
    );
    assert_eq!(
        head_field(v20_summary, "probeSourceSha256"),
        "40ced3035bdb19298e925b499edce42365aca66586abe7f8756847f32a1abd82"
    );
    assert_eq!(
        head_field(v20_summary, "runnerSourceSha256"),
        "54468f53de6c0d1d931e391640642f55ce6c4733721df569ef6f10ef93704497"
    );
    assert_eq!(
        head_field(v20_summary, "emittedBodySha256"),
        "3b1f4c53462e4ff8241863e73c90043d813cf5709cd0f3c809858659d7261564"
    );
    assert_eq!(
        head_field(v20_summary, "semanticPassSha256"),
        "dbd1c398b3ab3565a75ab9ed6dfa276b3493c52a6dd22a9a54ad09dc5e89e4d5"
    );
    assert_eq!(
        head_field(v20_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v20_summary, "freshRuns"), "2");
    assert_eq!(head_field(v20_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v20_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder20WithCLinkEnvelope"
    );
    assert_eq!(
        head_field(v20_summary, "javaEvidence"),
        "ReturnedBeforeTwentyFirstHead"
    );

    // Boundary 47 expected-only v21 rows cover existing-stem retry/reconciliation.
    let v21_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v21.txt"),
    )
    .expect("expected-only order21 existing-stem retry fixture");
    assert_eq!(
        sha256_hex(v21_text.as_bytes()),
        "9505955ce7e3322cbfaea818d0d42b5873fa78b1f5e1941756bcc44efcb04f55"
    );
    let v21_rows = v21_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v21_rows.len(), 13);
    let v21_body = format!("{}\n", v21_rows[..12].join("\n"));
    assert_eq!(
        sha256_hex(v21_body.as_bytes()),
        "9ea8929d70f49d8a39636ffece251ad1e13b3a443cdce57b62138f6ef0075293"
    );
    let v21_frontier = v21_rows[9];
    assert_eq!(head_field(v21_frontier, "headOrder"), "21");
    assert_eq!(head_field(v21_frontier, "headX"), "28");
    assert_eq!(head_field(v21_frontier, "headSig"), "55");
    assert_eq!(head_field(v21_frontier, "headInterId"), "1399");
    assert_eq!(head_field(v21_frontier, "cAlias"), "h:28:LEFT:BOTTOM");
    assert_eq!(head_field(v21_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v21_frontier, "maxIndex"), "1");
    assert_eq!(
        head_field(v21_frontier, "selected"),
        "[glyph:300:active:id=300:g:911:587:3:98:94dcc1e5c51e4aaba2df797bfd06f21f5a391b15a929d41763dfc0d06bc21835]"
    );
    assert_eq!(head_field(v21_frontier, "candidateIdBefore"), "300");
    assert_eq!(head_field(v21_frontier, "existingGlyph"), "glyph:300");
    assert_eq!(head_field(v21_frontier, "existingStem"), "2378");
    assert_eq!(
        head_field(v21_frontier, "terminal"),
        "ReadyForHeadCreateStem"
    );
    let v21_result = v21_rows[10];
    assert_eq!(head_field(v21_result, "headOrder"), "21");
    assert_eq!(head_field(v21_result, "allocatorBefore"), "2382");
    assert_eq!(head_field(v21_result, "allocatorAfter"), "2382");
    assert_eq!(head_field(v21_result, "addedVertices"), "-");
    assert_eq!(head_field(v21_result, "addedEdges"), "-");
    assert_eq!(head_field(v21_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v21_result, "relationStateHashAfter"),
        "3c50fb8ad029b80132262f4607e856ffcfadd6aa777d03bd3795288018faac9d"
    );
    assert_eq!(
        head_field(v21_result, "linkerStateHashAfter"),
        "26f4f302e145dcea28926f896eb9675f3a686ad8459e267c7386fae0361ddb94"
    );
    assert_eq!(
        head_field(v21_result, "terminal"),
        "ReturnedHeadCLinkTransaction"
    );
    let v21_continue = v21_rows[11];
    assert_eq!(head_field(v21_continue, "headOrder"), "21");
    assert_eq!(head_field(v21_continue, "headX"), "28");
    assert_eq!(head_field(v21_continue, "headSig"), "55");
    assert_eq!(
        head_field(v21_continue, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v21_continue, "closureWrites"),
        "[x27:sig54:LEFT:false->true,x27:sig54:RIGHT:false->true]"
    );
    assert_eq!(head_field(v21_continue, "closedValueChanges"), "2");
    assert_eq!(head_field(v21_continue, "nextHeadOrder"), "22");
    assert_eq!(head_field(v21_continue, "nextHeadX"), "4");
    assert_eq!(head_field(v21_continue, "nextHeadSig"), "7");
    assert_eq!(head_field(v21_continue, "nextHeadInterId"), "1299");
    assert_eq!(
        head_field(v21_continue, "terminal"),
        "ReturnedBeforeNextHead"
    );
    let v21_summary = v21_rows[12];
    assert_eq!(
        head_field(v21_summary, "schema"),
        "stems-head-phase-prefix-v21"
    );
    assert_eq!(head_field(v21_summary, "rows"), "12");
    assert_eq!(
        head_field(v21_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v21_summary, "baseV20RunnerSourceSha256"),
        "54468f53de6c0d1d931e391640642f55ce6c4733721df569ef6f10ef93704497"
    );
    assert_eq!(
        head_field(v21_summary, "baseV20FixtureSha256"),
        "be6a820b3740105e4fdddeb0e9ec475d1dd3ebc8611fd7be555cf55957dfe4a4"
    );
    assert_eq!(
        head_field(v21_summary, "fragmentSourceSha256"),
        "f6a36215a86d9af177447069be271b0c4a84e4f8f56789d27769c161710c3629"
    );
    assert_eq!(
        head_field(v21_summary, "probeSourceSha256"),
        "186e9fb81f3b39d1591b23b5f94c565152bfc81dc1d0e4781d460b1126f3ac4a"
    );
    assert_eq!(
        head_field(v21_summary, "runnerSourceSha256"),
        "8cbd5d1de2e6e6b2b77d4ba94d99eb9f5813503a4afb960bb7511d0b92999ccd"
    );
    assert_eq!(
        head_field(v21_summary, "emittedBodySha256"),
        "9ea8929d70f49d8a39636ffece251ad1e13b3a443cdce57b62138f6ef0075293"
    );
    assert_eq!(
        head_field(v21_summary, "semanticPassSha256"),
        "a372eb0884f3679e62797343800beb70e8099c14267067f6d141f8c359216611"
    );
    assert_eq!(
        head_field(v21_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v21_summary, "freshRuns"), "2");
    assert_eq!(head_field(v21_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v21_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder21ExistingStemRetry"
    );
    assert_eq!(
        head_field(v21_summary, "javaEvidence"),
        "ReturnedBeforeTwentySecondHead"
    );

    // Boundary 48 expected-only v22 rows cover existing-stem retry/reconciliation.
    let v22_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v22.txt"),
    )
    .expect("expected-only order22 existing-stem retry fixture");
    assert_eq!(
        sha256_hex(v22_text.as_bytes()),
        "e7bd66417228bf8fed7fe0c04d904e81ade4026fb00b4c17270b73947f85a1a4"
    );
    let v22_rows = v22_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v22_rows.len(), 14);
    let v22_body = format!("{}\n", v22_rows[..13].join("\n"));
    assert_eq!(
        sha256_hex(v22_body.as_bytes()),
        "23d5da366efe5ce9d1bee9e7c5e3201677faef273075e23af68332a5e1f7e4bb"
    );
    let v22_frontier = v22_rows[10];
    assert_eq!(head_field(v22_frontier, "headOrder"), "22");
    assert_eq!(head_field(v22_frontier, "headX"), "4");
    assert_eq!(head_field(v22_frontier, "headSig"), "7");
    assert_eq!(head_field(v22_frontier, "headInterId"), "1299");
    assert_eq!(head_field(v22_frontier, "cAlias"), "h:4:LEFT:BOTTOM");
    assert_eq!(head_field(v22_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v22_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v22_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v22_frontier, "existingGlyph"), "glyph:315");
    assert_eq!(head_field(v22_frontier, "existingStem"), "2354");
    assert_eq!(
        head_field(v22_frontier, "terminal"),
        "ReadyForHeadCreateStem"
    );
    let v22_result = v22_rows[11];
    assert_eq!(head_field(v22_result, "headOrder"), "22");
    assert_eq!(head_field(v22_result, "allocatorBefore"), "2382");
    assert_eq!(head_field(v22_result, "allocatorAfter"), "2382");
    assert_eq!(head_field(v22_result, "addedVertices"), "-");
    assert_eq!(head_field(v22_result, "addedEdges"), "-");
    assert_eq!(head_field(v22_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v22_result, "linkerStateHashAfter"),
        "460cbb523fd626563afba13b2754d7fee08677dc2f3701f04a06e2df74833ff6"
    );
    assert_eq!(
        head_field(v22_result, "relationStateHashAfter"),
        "3c50fb8ad029b80132262f4607e856ffcfadd6aa777d03bd3795288018faac9d"
    );
    assert_eq!(
        head_field(v22_result, "terminal"),
        "ReturnedHeadCLinkTransaction"
    );
    let v22_continue = v22_rows[12];
    assert_eq!(head_field(v22_continue, "headOrder"), "22");
    assert_eq!(head_field(v22_continue, "headX"), "4");
    assert_eq!(head_field(v22_continue, "headSig"), "7");
    assert_eq!(
        head_field(v22_continue, "closureWrites"),
        "[x3:sig6:LEFT:false->true,x3:sig6:RIGHT:false->true]"
    );
    assert_eq!(head_field(v22_continue, "closedValueChanges"), "2");
    assert_eq!(head_field(v22_continue, "nextHeadOrder"), "23");
    assert_eq!(head_field(v22_continue, "nextHeadX"), "78");
    assert_eq!(head_field(v22_continue, "nextHeadSig"), "39");
    assert_eq!(head_field(v22_continue, "nextHeadInterId"), "1363");
    assert_eq!(
        head_field(v22_continue, "terminal"),
        "ReturnedBeforeNextHead"
    );
    let v22_summary = v22_rows[13];
    assert_eq!(
        head_field(v22_summary, "schema"),
        "stems-head-phase-prefix-v22"
    );
    assert_eq!(head_field(v22_summary, "rows"), "13");
    assert_eq!(
        head_field(v22_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v22_summary, "baseV21RunnerSourceSha256"),
        "8cbd5d1de2e6e6b2b77d4ba94d99eb9f5813503a4afb960bb7511d0b92999ccd"
    );
    assert_eq!(
        head_field(v22_summary, "baseV21FixtureSha256"),
        "9505955ce7e3322cbfaea818d0d42b5873fa78b1f5e1941756bcc44efcb04f55"
    );
    assert_eq!(
        head_field(v22_summary, "fragmentSourceSha256"),
        "576406fb3bd8bf9503ca883480bc55b217b3c6bc99ca440ef702774d3a2ca950"
    );
    assert_eq!(
        head_field(v22_summary, "probeSourceSha256"),
        "fc6ada7afdc64f1e42f9fbf0c1f9353138a02ec285d24697fc68a90d49c3dfc7"
    );
    assert_eq!(
        head_field(v22_summary, "runnerSourceSha256"),
        "be1091ab266ea190a507291351f50bec4842f50003c75fb048f6bb96537ceebc"
    );
    assert_eq!(
        head_field(v22_summary, "emittedBodySha256"),
        "23d5da366efe5ce9d1bee9e7c5e3201677faef273075e23af68332a5e1f7e4bb"
    );
    assert_eq!(
        head_field(v22_summary, "semanticPassSha256"),
        "62c5ac9c30ea6bf3666cdb567bfa52d6d0a857578a5146ac91927f08adfa8c6a"
    );
    assert_eq!(
        head_field(v22_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v22_summary, "freshRuns"), "2");
    assert_eq!(head_field(v22_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v22_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder22ExistingStemRetry"
    );
    assert_eq!(
        head_field(v22_summary, "javaEvidence"),
        "ReturnedBeforeTwentyThirdHead"
    );

    // Boundary 49 expected-only v23 rows cover the generic prelinked closure.
    let v23_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v23.txt"),
    )
    .expect("expected-only order23 continuation fixture");
    assert_eq!(
        sha256_hex(v23_text.as_bytes()),
        "20731b3ff52e2512407f17c00329e16f015aaedba7bf5c91ec1b0b9907c58e68"
    );
    let v23_rows = v23_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v23_rows.len(), 15);
    let v23_body = format!("{}\n", v23_rows[..14].join("\n"));
    assert_eq!(
        sha256_hex(v23_body.as_bytes()),
        "e8032bae1ffee2113b72b8a359d5c25cc219a84f1bd6a89485632138db42540f"
    );
    let v23_java = v23_rows[13];
    assert_eq!(head_field(v23_java, "headOrder"), "23");
    assert_eq!(head_field(v23_java, "headX"), "78");
    assert_eq!(head_field(v23_java, "headSig"), "39");
    assert_eq!(head_field(v23_java, "headInterId"), "1363");
    assert_eq!(
        head_field(v23_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v23_java, "incident"),
        "[stem2370:headSideLEFT:heads[x77:sig38:id1361:sideLEFT,x78:sig39:id1363:sideLEFT]]"
    );
    assert_eq!(
        head_field(v23_java, "closureWrites"),
        "[x77:sig38:LEFT:false->true,x77:sig38:RIGHT:false->true]"
    );
    assert_eq!(head_field(v23_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v23_java, "sigVerticesAfter"), "682");
    assert_eq!(head_field(v23_java, "sigEdgesAfter"), "693");
    assert_eq!(head_field(v23_java, "systemStemsAfter"), "43");
    assert_eq!(
        head_field(v23_java, "linkerStateHashAfter"),
        "ee6f3c58c11c375022e0470fefd2a7487e26cb9d5ac120933882dc84b3ccdc22"
    );
    assert_eq!(head_field(v23_java, "nextHeadOrder"), "24");
    assert_eq!(head_field(v23_java, "nextHeadX"), "93");
    assert_eq!(head_field(v23_java, "nextHeadSig"), "25");
    assert_eq!(head_field(v23_java, "nextHeadInterId"), "1335");
    assert_eq!(head_field(v23_java, "terminal"), "ReturnedBeforeNextHead");
    let v23_summary = v23_rows[14];
    assert_eq!(
        head_field(v23_summary, "schema"),
        "stems-head-phase-prefix-v23"
    );
    assert_eq!(head_field(v23_summary, "rows"), "14");
    assert_eq!(
        head_field(v23_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v23_summary, "baseV22RunnerSourceSha256"),
        "be1091ab266ea190a507291351f50bec4842f50003c75fb048f6bb96537ceebc"
    );
    assert_eq!(
        head_field(v23_summary, "baseV22FixtureSha256"),
        "e7bd66417228bf8fed7fe0c04d904e81ade4026fb00b4c17270b73947f85a1a4"
    );
    assert_eq!(
        head_field(v23_summary, "fragmentSourceSha256"),
        "576406fb3bd8bf9503ca883480bc55b217b3c6bc99ca440ef702774d3a2ca950"
    );
    assert_eq!(
        head_field(v23_summary, "probeSourceSha256"),
        "8c6694e8f0c9d293db056b515f51d5393b6a9a860d002e4033cafc8881f768af"
    );
    assert_eq!(
        head_field(v23_summary, "runnerSourceSha256"),
        "b945062e6c069c975f738ee066bc42107b4b6af599b5097abd0423bbb232aa25"
    );
    assert_eq!(
        head_field(v23_summary, "emittedBodySha256"),
        "e8032bae1ffee2113b72b8a359d5c25cc219a84f1bd6a89485632138db42540f"
    );
    assert_eq!(
        head_field(v23_summary, "semanticPassSha256"),
        "093645a4a4ffe760113cfd15776c7e0eb61381b405b66a2f5789999a29927f38"
    );
    assert_eq!(
        head_field(v23_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v23_summary, "freshRuns"), "2");
    assert_eq!(head_field(v23_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v23_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder23PrelinkedClosure"
    );
    assert_eq!(
        head_field(v23_summary, "javaEvidence"),
        "ReturnedBeforeTwentyFourthHead"
    );

    // Boundary 50 expected-only v24 rows cover the next prelinked closure.
    let v24_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v24.txt"),
    )
    .expect("expected-only order24 continuation fixture");
    assert_eq!(
        sha256_hex(v24_text.as_bytes()),
        "56684be47b32b49e3d6f3c1440a9f3062a6bdcdec28fa0554cc6f2be80242b6c"
    );
    let v24_rows = v24_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v24_rows.len(), 16);
    let v24_body = format!("{}\n", v24_rows[..15].join("\n"));
    assert_eq!(
        sha256_hex(v24_body.as_bytes()),
        "65d329d75ac1d9fff1fba2d13b9b418346645bbbcf3637061f95901039a0fac5"
    );
    let v24_java = v24_rows[14];
    assert_eq!(head_field(v24_java, "headOrder"), "24");
    assert_eq!(head_field(v24_java, "headX"), "93");
    assert_eq!(head_field(v24_java, "headSig"), "25");
    assert_eq!(head_field(v24_java, "headInterId"), "1335");
    assert_eq!(
        head_field(v24_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v24_java, "incident"),
        "[stem2342:headSideLEFT:heads[x92:sig24:id1333:sideLEFT,x93:sig25:id1335:sideLEFT]]"
    );
    assert_eq!(
        head_field(v24_java, "closureWrites"),
        "[x92:sig24:LEFT:false->true,x92:sig24:RIGHT:false->true]"
    );
    assert_eq!(head_field(v24_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v24_java, "sigVerticesAfter"), "682");
    assert_eq!(head_field(v24_java, "sigEdgesAfter"), "693");
    assert_eq!(head_field(v24_java, "systemStemsAfter"), "43");
    assert_eq!(
        head_field(v24_java, "linkerStateHashAfter"),
        "3862db75e7f6be2dd617cc0f7884d79de48354ae4b561963ab6437a1bf3d0483"
    );
    assert_eq!(head_field(v24_java, "nextHeadOrder"), "25");
    assert_eq!(head_field(v24_java, "nextHeadX"), "59");
    assert_eq!(head_field(v24_java, "nextHeadSig"), "74");
    assert_eq!(head_field(v24_java, "nextHeadInterId"), "1437");
    assert_eq!(head_field(v24_java, "terminal"), "ReturnedBeforeNextHead");
    let v24_summary = v24_rows[15];
    assert_eq!(
        head_field(v24_summary, "schema"),
        "stems-head-phase-prefix-v24"
    );
    assert_eq!(head_field(v24_summary, "rows"), "15");
    assert_eq!(
        head_field(v24_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v24_summary, "baseV23RunnerSourceSha256"),
        "b945062e6c069c975f738ee066bc42107b4b6af599b5097abd0423bbb232aa25"
    );
    assert_eq!(
        head_field(v24_summary, "baseV23FixtureSha256"),
        "20731b3ff52e2512407f17c00329e16f015aaedba7bf5c91ec1b0b9907c58e68"
    );
    assert_eq!(
        head_field(v24_summary, "fragmentSourceSha256"),
        "576406fb3bd8bf9503ca883480bc55b217b3c6bc99ca440ef702774d3a2ca950"
    );
    assert_eq!(
        head_field(v24_summary, "probeSourceSha256"),
        "24f9bab608b05b89f0a28198b19827cfc0d241a0fd558298564e24f868b30872"
    );
    assert_eq!(
        head_field(v24_summary, "runnerSourceSha256"),
        "2d2a7b2b58f674bdf3db3716a6e66eac1b9d56694df7c79d7ec91ff7cb629293"
    );
    assert_eq!(
        head_field(v24_summary, "emittedBodySha256"),
        "65d329d75ac1d9fff1fba2d13b9b418346645bbbcf3637061f95901039a0fac5"
    );
    assert_eq!(
        head_field(v24_summary, "semanticPassSha256"),
        "15cadab070e039fdb0753fcb57cc0e1aeb9012d0d19773eb701a47fc982d582e"
    );
    assert_eq!(
        head_field(v24_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v24_summary, "freshRuns"), "2");
    assert_eq!(head_field(v24_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v24_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder24PrelinkedClosure"
    );
    assert_eq!(
        head_field(v24_summary, "javaEvidence"),
        "ReturnedBeforeTwentyFifthHead"
    );

    // Boundary 51 expected-only v25 rows cover the next prelinked closure.
    let v25_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v25.txt"),
    )
    .expect("expected-only order25 continuation fixture");
    assert_eq!(
        sha256_hex(v25_text.as_bytes()),
        "39ccb74b6231aa2ce3f77a41adb59d18ae64c736598917523f4c4f8835722d2d"
    );
    let v25_rows = v25_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v25_rows.len(), 17);
    let v25_body = format!("{}\n", v25_rows[..16].join("\n"));
    assert_eq!(
        sha256_hex(v25_body.as_bytes()),
        "c361bb73ac81783c8b0862490582fb4a6384ca98b845a2f85ccbf42c77da02f2"
    );
    let v25_java = v25_rows[15];
    assert_eq!(head_field(v25_java, "headOrder"), "25");
    assert_eq!(head_field(v25_java, "headX"), "59");
    assert_eq!(head_field(v25_java, "headSig"), "74");
    assert_eq!(head_field(v25_java, "headInterId"), "1437");
    assert_eq!(
        head_field(v25_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v25_java, "incident"),
        "[stem2363:headSideLEFT:heads[x58:sig73:id1435:sideLEFT,x59:sig74:id1437:sideLEFT]]"
    );
    assert_eq!(
        head_field(v25_java, "closureWrites"),
        "[x58:sig73:LEFT:false->true,x58:sig73:RIGHT:false->true]"
    );
    assert_eq!(head_field(v25_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v25_java, "sigVerticesAfter"), "682");
    assert_eq!(head_field(v25_java, "sigEdgesAfter"), "693");
    assert_eq!(head_field(v25_java, "systemStemsAfter"), "43");
    assert_eq!(
        head_field(v25_java, "linkerStateHashAfter"),
        "f93b0ed28e3ec7ee648f10801652bd0a11da4bd16a681cf5ce6384d2fd2097a7"
    );
    assert_eq!(head_field(v25_java, "nextHeadOrder"), "26");
    assert_eq!(head_field(v25_java, "nextHeadX"), "61");
    assert_eq!(head_field(v25_java, "nextHeadSig"), "31");
    assert_eq!(head_field(v25_java, "nextHeadInterId"), "1347");
    assert_eq!(head_field(v25_java, "terminal"), "ReturnedBeforeNextHead");
    let v25_summary = v25_rows[16];
    assert_eq!(
        head_field(v25_summary, "schema"),
        "stems-head-phase-prefix-v25"
    );
    assert_eq!(head_field(v25_summary, "rows"), "16");
    assert_eq!(
        head_field(v25_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v25_summary, "baseV24RunnerSourceSha256"),
        "2d2a7b2b58f674bdf3db3716a6e66eac1b9d56694df7c79d7ec91ff7cb629293"
    );
    assert_eq!(
        head_field(v25_summary, "baseV24FixtureSha256"),
        "56684be47b32b49e3d6f3c1440a9f3062a6bdcdec28fa0554cc6f2be80242b6c"
    );
    assert_eq!(
        head_field(v25_summary, "fragmentSourceSha256"),
        "576406fb3bd8bf9503ca883480bc55b217b3c6bc99ca440ef702774d3a2ca950"
    );
    assert_eq!(
        head_field(v25_summary, "probeSourceSha256"),
        "d30b66790a5b3b9cfc3aa9da27908aa90a1018d6a7fedd0f7c7029e0f6cbb69d"
    );
    assert_eq!(
        head_field(v25_summary, "runnerSourceSha256"),
        "d9bb5989503627cf7486f6c3286ffe78754a1a089d1d18087fef1e6d15389c68"
    );
    assert_eq!(
        head_field(v25_summary, "emittedBodySha256"),
        "c361bb73ac81783c8b0862490582fb4a6384ca98b845a2f85ccbf42c77da02f2"
    );
    assert_eq!(
        head_field(v25_summary, "semanticPassSha256"),
        "34d99daf8ee4b8b670c52a4ea28cf1bfae406f2bbb9904e5595208f0b0188fc8"
    );
    assert_eq!(
        head_field(v25_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v25_summary, "freshRuns"), "2");
    assert_eq!(head_field(v25_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v25_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder25PrelinkedClosure"
    );
    assert_eq!(
        head_field(v25_summary, "javaEvidence"),
        "ReturnedBeforeTwentySixthHead"
    );

    // Boundary 52 expected-only v26 rows cover the next prelinked closure.
    let v26_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v26.txt"),
    )
    .expect("expected-only order26 continuation fixture");
    assert_eq!(
        sha256_hex(v26_text.as_bytes()),
        "a5e6a9cb07b49ecf1753fbe10ba709a63d274dce5393887acddc123e55342c36"
    );
    let v26_rows = v26_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v26_rows.len(), 18);
    let v26_body = format!("{}\n", v26_rows[..17].join("\n"));
    assert_eq!(
        sha256_hex(v26_body.as_bytes()),
        "8220b597632c878f90e6ebb8bf4f84ac4beda6a2458c07056663075520ff2f73"
    );
    let v26_java = v26_rows[16];
    assert_eq!(head_field(v26_java, "headOrder"), "26");
    assert_eq!(head_field(v26_java, "headX"), "61");
    assert_eq!(head_field(v26_java, "headSig"), "31");
    assert_eq!(head_field(v26_java, "headInterId"), "1347");
    assert_eq!(
        head_field(v26_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v26_java, "incident"),
        "[stem2345:headSideLEFT:heads[x60:sig30:id1345:sideLEFT,x61:sig31:id1347:sideLEFT]]"
    );
    assert_eq!(
        head_field(v26_java, "closureWrites"),
        "[x60:sig30:LEFT:false->true,x60:sig30:RIGHT:false->true]"
    );
    assert_eq!(head_field(v26_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v26_java, "sigVerticesAfter"), "682");
    assert_eq!(head_field(v26_java, "sigEdgesAfter"), "693");
    assert_eq!(head_field(v26_java, "systemStemsAfter"), "43");
    assert_eq!(
        head_field(v26_java, "linkerStateHashAfter"),
        "113bbf889b229fd88cf140034dd8bbabcdaab1ce3dae82089f3f00a657983b78"
    );
    assert_eq!(head_field(v26_java, "nextHeadOrder"), "27");
    assert_eq!(head_field(v26_java, "nextHeadX"), "33");
    assert_eq!(head_field(v26_java, "nextHeadSig"), "26");
    assert_eq!(head_field(v26_java, "nextHeadInterId"), "1337");
    assert_eq!(head_field(v26_java, "terminal"), "ReturnedBeforeNextHead");
    let v26_summary = v26_rows[17];
    assert_eq!(
        head_field(v26_summary, "schema"),
        "stems-head-phase-prefix-v26"
    );
    assert_eq!(head_field(v26_summary, "rows"), "17");
    assert_eq!(
        head_field(v26_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v26_summary, "baseV25RunnerSourceSha256"),
        "d9bb5989503627cf7486f6c3286ffe78754a1a089d1d18087fef1e6d15389c68"
    );
    assert_eq!(
        head_field(v26_summary, "baseV25FixtureSha256"),
        "39ccb74b6231aa2ce3f77a41adb59d18ae64c736598917523f4c4f8835722d2d"
    );
    assert_eq!(
        head_field(v26_summary, "fragmentSourceSha256"),
        "576406fb3bd8bf9503ca883480bc55b217b3c6bc99ca440ef702774d3a2ca950"
    );
    assert_eq!(
        head_field(v26_summary, "probeSourceSha256"),
        "d794e14d3715c64e7e9b3364fbf1a29389a4bd327da577e7313ce0de4eafdaa8"
    );
    assert_eq!(
        head_field(v26_summary, "runnerSourceSha256"),
        "afe60083e9b34076c7aab0106216eb5dac7ba689c63ef388112f7b700f842ed0"
    );
    assert_eq!(
        head_field(v26_summary, "emittedBodySha256"),
        "8220b597632c878f90e6ebb8bf4f84ac4beda6a2458c07056663075520ff2f73"
    );
    assert_eq!(
        head_field(v26_summary, "semanticPassSha256"),
        "da5cfb3439d4efec0cbd64299cf037927ab4cea76a20c1c740bdee0780916a49"
    );
    assert_eq!(
        head_field(v26_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v26_summary, "freshRuns"), "2");
    assert_eq!(head_field(v26_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v26_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder26PrelinkedClosure"
    );
    assert_eq!(
        head_field(v26_summary, "javaEvidence"),
        "ReturnedBeforeTwentySeventhHead"
    );

    // Boundary 53 expected-only v27 rows cover the authenticated C-link.
    let v27_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v27.txt"),
    )
    .expect("expected-only order27 C-link fixture");
    assert_eq!(
        sha256_hex(v27_text.as_bytes()),
        "1ba59491992fdd7bd2355e2617b437b84433d3c449cc8f7606cdc0a1e70ac0aa"
    );
    let v27_rows = v27_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v27_rows.len(), 21);
    let v27_body = format!("{}\n", v27_rows[..20].join("\n"));
    assert_eq!(
        sha256_hex(v27_body.as_bytes()),
        "bd006771fb4878072bb24f54cc22efd507dd5114d5e60fccff76479b2cb25c1c"
    );
    let v27_frontier = v27_rows[17];
    assert_eq!(head_field(v27_frontier, "headOrder"), "27");
    assert_eq!(head_field(v27_frontier, "headX"), "33");
    assert_eq!(head_field(v27_frontier, "headSig"), "26");
    assert_eq!(head_field(v27_frontier, "headInterId"), "1337");
    assert_eq!(head_field(v27_frontier, "cAlias"), "h:33:LEFT:BOTTOM");
    assert_eq!(head_field(v27_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v27_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v27_frontier, "relations"), "1");
    assert_eq!(head_field(v27_frontier, "glyphs"), "2");
    assert_eq!(head_field(v27_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v27_frontier, "existingGlyph"), "glyph:314");
    assert_eq!(head_field(v27_frontier, "existingActive"), "true");
    assert_eq!(head_field(v27_frontier, "existingStem"), "-");
    let v27_result = v27_rows[18];
    assert_eq!(head_field(v27_result, "headOrder"), "27");
    assert_eq!(head_field(v27_result, "allocatorBefore"), "2382");
    assert_eq!(head_field(v27_result, "allocatorAfter"), "2383");
    assert_eq!(
        head_field(v27_result, "addedVertices"),
        "[id2383:org.audiveris.omr.sig.inter.StemInter:shape=STEM:grade=0x1.90283def49dbbp-1/3fe90283def49dbb:bounds=1019:388:3:89:removed=false:abnormal=false:manual=false:implicit=false:profile=0:median=0x1.fe56e98b22844p9/408fe56e98b22844:0x1.84p8/4078400000000000:0x1.fe52234da2bdcp9/408fe52234da2bdc:0x1.ddp8/407dd00000000000:width=0x1.4398730e61cc4p1/4004398730e61cc4]"
    );
    assert_eq!(
        head_field(v27_result, "addedEdges"),
        "[system1:sourceId1337:targetId2383:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.f60c652f5e4c7p-1/3fef60c652f5e4c7:impacts=[xInGap:0x1.ec4a4eb7b5219p-1/3feec4a4eb7b5219:w=0x1.0p0/3ff0000000000000,yGap:0x1.0p0/3ff0000000000000:w=0x1.0p0/3ff0000000000000]:dx=-0x1.f891ba6de30c3p-7/bf8f891ba6de30c3:dy=0x0.0p0/0000000000000000:extension=0x1.fe3bd28bd8538p9/408fe3bd28bd8538:0x1.7ap8/4077a00000000000:headSide=LEFT:consistency=0x1.837ba5713280ep0/3ff837ba5713280e]"
    );
    assert_eq!(
        head_field(v27_result, "linkerChanges"),
        "[h:33:LEFT:BOTTOM:false:false->true:false,h:33:LEFT:TOP:false:false->true:false,linker:SLinker:head:33:false:false->true:false]"
    );
    assert_eq!(
        head_field(v27_result, "relationStateHashAfter"),
        "c6dec8e3b73e751d8b552b377dd3fa2289d59644cda1674f4ff594247cbf60dd"
    );
    assert_eq!(
        head_field(v27_result, "linkerStateHashAfter"),
        "778a467db3c8c1cb74e0b70367f7409668d2522c950ad4d12ea3b188a6eb7bb6"
    );
    let v27_java = v27_rows[19];
    assert_eq!(head_field(v27_java, "headOrder"), "27");
    assert_eq!(head_field(v27_java, "headX"), "33");
    assert_eq!(head_field(v27_java, "headSig"), "26");
    assert_eq!(head_field(v27_java, "headInterId"), "1337");
    assert_eq!(
        head_field(v27_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v27_java, "sigVerticesAfter"), "683");
    assert_eq!(head_field(v27_java, "sigEdgesAfter"), "694");
    assert_eq!(head_field(v27_java, "systemStemsAfter"), "44");
    assert_eq!(head_field(v27_java, "nextHeadOrder"), "28");
    assert_eq!(head_field(v27_java, "nextHeadX"), "85");
    assert_eq!(head_field(v27_java, "nextHeadSig"), "87");
    assert_eq!(head_field(v27_java, "nextHeadInterId"), "1463");
    let v27_summary = v27_rows[20];
    assert_eq!(
        head_field(v27_summary, "schema"),
        "stems-head-phase-prefix-v27"
    );
    assert_eq!(head_field(v27_summary, "rows"), "20");
    assert_eq!(
        head_field(v27_summary, "baseV26RunnerSourceSha256"),
        "afe60083e9b34076c7aab0106216eb5dac7ba689c63ef388112f7b700f842ed0"
    );
    assert_eq!(
        head_field(v27_summary, "baseV26FixtureSha256"),
        "a5e6a9cb07b49ecf1753fbe10ba709a63d274dce5393887acddc123e55342c36"
    );
    assert_eq!(
        head_field(v27_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v27_summary, "probeSourceSha256"),
        "5f4c5a69c9fe5e87f23eff31b1524e80459a04a298689609fa80ef142f1cd9c6"
    );
    assert_eq!(
        head_field(v27_summary, "runnerSourceSha256"),
        "f2c1942b3ff6f00a75bb876b6d6d4b53ba2d999bcb5ddaeb88f6dc86850fcdc5"
    );
    assert_eq!(
        head_field(v27_summary, "emittedBodySha256"),
        "bd006771fb4878072bb24f54cc22efd507dd5114d5e60fccff76479b2cb25c1c"
    );
    assert_eq!(
        head_field(v27_summary, "semanticPassSha256"),
        "1033282335cace626465424615847b3e190c718f25acf2fd70e1a6a2d50ec7d7"
    );
    assert_eq!(
        head_field(v27_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v27_summary, "freshRuns"), "2");
    assert_eq!(head_field(v27_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v27_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder27BothOpenCLink"
    );
    assert_eq!(
        head_field(v27_summary, "javaEvidence"),
        "ReturnedBeforeTwentyEighthHead"
    );

    // Boundary 54 expected-only v28 rows cover the four-value closure.
    let v28_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v28.txt"),
    )
    .expect("expected-only order28 continuation fixture");
    assert_eq!(
        sha256_hex(v28_text.as_bytes()),
        "6f30a5cb8706fb0445b5eb84cee2896dfa1b85236f6870a97177714672ef10b7"
    );
    let v28_rows = v28_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v28_rows.len(), 8);
    let v28_body = format!("{}\n", v28_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v28_body.as_bytes()),
        "5a4675dca2831e93c61a028a6d189deed21115e9588e06b1293c37968fd2bef5"
    );
    let v28_java = v28_rows[6];
    assert_eq!(head_field(v28_java, "headOrder"), "28");
    assert_eq!(head_field(v28_java, "headX"), "85");
    assert_eq!(head_field(v28_java, "headSig"), "87");
    assert_eq!(head_field(v28_java, "headInterId"), "1463");
    assert_eq!(
        head_field(v28_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v28_java, "incident"),
        "[stem2366:headSideLEFT:heads[x84:sig86:id1461:sideLEFT,x85:sig87:id1463:sideLEFT,x86:sig85:id1459:sideLEFT]]"
    );
    assert_eq!(
        head_field(v28_java, "closureWrites"),
        "[x84:sig86:LEFT:false->true,x84:sig86:RIGHT:false->true,x86:sig85:LEFT:false->true,x86:sig85:RIGHT:false->true]"
    );
    assert_eq!(head_field(v28_java, "closedValueChanges"), "4");
    assert_eq!(head_field(v28_java, "sigVerticesAfter"), "683");
    assert_eq!(head_field(v28_java, "sigEdgesAfter"), "694");
    assert_eq!(head_field(v28_java, "systemStemsAfter"), "44");
    assert_eq!(
        head_field(v28_java, "relationStateHashAfter"),
        "c6dec8e3b73e751d8b552b377dd3fa2289d59644cda1674f4ff594247cbf60dd"
    );
    assert_eq!(
        head_field(v28_java, "linkerStateHashAfter"),
        "a23c902455e6c34efab501872172bca882a4d0d9e2b87d63c9562a9c7f6a8c77"
    );
    assert_eq!(head_field(v28_java, "nextHeadOrder"), "29");
    assert_eq!(head_field(v28_java, "nextHeadX"), "10");
    assert_eq!(head_field(v28_java, "nextHeadSig"), "9");
    assert_eq!(head_field(v28_java, "nextHeadInterId"), "1303");
    let v28_summary = v28_rows[7];
    assert_eq!(
        head_field(v28_summary, "schema"),
        "stems-head-phase-prefix-v28"
    );
    assert_eq!(head_field(v28_summary, "rows"), "7");
    assert_eq!(
        head_field(v28_summary, "baseV27RunnerSourceSha256"),
        "f2c1942b3ff6f00a75bb876b6d6d4b53ba2d999bcb5ddaeb88f6dc86850fcdc5"
    );
    assert_eq!(
        head_field(v28_summary, "baseV27FixtureSha256"),
        "1ba59491992fdd7bd2355e2617b437b84433d3c449cc8f7606cdc0a1e70ac0aa"
    );
    assert_eq!(
        head_field(v28_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v28_summary, "probeSourceSha256"),
        "d2e07d5dacf3e22ec20a3f53c8e4543763982eec3e88eac1ac8e8e3368422cc2"
    );
    assert_eq!(
        head_field(v28_summary, "runnerSourceSha256"),
        "ec1985d786f0c984f5a09a461008911f12777229b0a08eb71b7e36a39d548d82"
    );
    assert_eq!(
        head_field(v28_summary, "emittedBodySha256"),
        "5a4675dca2831e93c61a028a6d189deed21115e9588e06b1293c37968fd2bef5"
    );
    assert_eq!(
        head_field(v28_summary, "semanticPassSha256"),
        "b4d16e19a892bfb0537f8b7b629e43617687f19794a2cf13332a0e69cdd4e1fd"
    );
    assert_eq!(
        head_field(v28_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v28_summary, "freshRuns"), "2");
    assert_eq!(head_field(v28_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v28_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder28PrelinkedClosure"
    );
    assert_eq!(
        head_field(v28_summary, "javaEvidence"),
        "ReturnedBeforeTwentyNinthHead"
    );

    // Boundary 55 expected-only v29 rows cover the next prelinked closure.
    let v29_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v29.txt"),
    )
    .expect("expected-only order29 continuation fixture");
    assert_eq!(
        sha256_hex(v29_text.as_bytes()),
        "a88b9fd3c27133c3c8bdcc839308365557c0e95c2ac3ea83fe348dc0d1ffa270"
    );
    let v29_rows = v29_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v29_rows.len(), 8);
    let v29_body = format!("{}\n", v29_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v29_body.as_bytes()),
        "410d0c1e04f4c7dfb1b4b83ed0953da53e52605df72e08351231e302027ca84a"
    );
    let v29_java = v29_rows[6];
    assert_eq!(head_field(v29_java, "headOrder"), "29");
    assert_eq!(head_field(v29_java, "headX"), "10");
    assert_eq!(head_field(v29_java, "headSig"), "9");
    assert_eq!(head_field(v29_java, "headInterId"), "1303");
    assert_eq!(
        head_field(v29_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v29_java, "incident"),
        "[stem2355:headSideLEFT:heads[x9:sig8:id1301:sideLEFT,x10:sig9:id1303:sideLEFT]]"
    );
    assert_eq!(
        head_field(v29_java, "closureWrites"),
        "[x9:sig8:LEFT:false->true,x9:sig8:RIGHT:false->true]"
    );
    assert_eq!(head_field(v29_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v29_java, "sigVerticesAfter"), "683");
    assert_eq!(head_field(v29_java, "sigEdgesAfter"), "694");
    assert_eq!(head_field(v29_java, "systemStemsAfter"), "44");
    assert_eq!(
        head_field(v29_java, "relationStateHashAfter"),
        "c6dec8e3b73e751d8b552b377dd3fa2289d59644cda1674f4ff594247cbf60dd"
    );
    assert_eq!(
        head_field(v29_java, "linkerStateHashAfter"),
        "0e03f8ca04d61fd1c7427a20da5237cfabc769628973a4697a61103e66f5f5ce"
    );
    assert_eq!(head_field(v29_java, "nextHeadOrder"), "30");
    assert_eq!(head_field(v29_java, "nextHeadX"), "101");
    assert_eq!(head_field(v29_java, "nextHeadSig"), "43");
    assert_eq!(head_field(v29_java, "nextHeadInterId"), "1371");
    assert_eq!(head_field(v29_java, "terminal"), "ReturnedBeforeNextHead");
    let v29_summary = v29_rows[7];
    assert_eq!(
        head_field(v29_summary, "schema"),
        "stems-head-phase-prefix-v29"
    );
    assert_eq!(head_field(v29_summary, "rows"), "7");
    assert_eq!(
        head_field(v29_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v29_summary, "baseV28RunnerSourceSha256"),
        "ec1985d786f0c984f5a09a461008911f12777229b0a08eb71b7e36a39d548d82"
    );
    assert_eq!(
        head_field(v29_summary, "baseV28FixtureSha256"),
        "6f30a5cb8706fb0445b5eb84cee2896dfa1b85236f6870a97177714672ef10b7"
    );
    assert_eq!(
        head_field(v29_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v29_summary, "probeSourceSha256"),
        "79ddfc2cf532474ff902156eb66c2655ec242ac0a73884fd67bfc74afb6521ca"
    );
    assert_eq!(
        head_field(v29_summary, "runnerSourceSha256"),
        "0ae5afb409d11eef138ed62bb8adbefb04eabfa99c0581cad7a6952ecb5e1d4c"
    );
    assert_eq!(
        head_field(v29_summary, "emittedBodySha256"),
        "410d0c1e04f4c7dfb1b4b83ed0953da53e52605df72e08351231e302027ca84a"
    );
    assert_eq!(
        head_field(v29_summary, "semanticPassSha256"),
        "32c937944c1c015c79ca4993dd299bef9c32ef39e7be71c800ca98d21ccd5cde"
    );
    assert_eq!(
        head_field(v29_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v29_summary, "freshRuns"), "2");
    assert_eq!(head_field(v29_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v29_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder29PrelinkedClosure"
    );
    assert_eq!(
        head_field(v29_summary, "javaEvidence"),
        "ReturnedBeforeThirtiethHead"
    );

    // Boundary 56 expected-only v30 rows cover the next prelinked closure.
    let v30_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v30.txt"),
    )
    .expect("expected-only order30 continuation fixture");
    assert_eq!(
        sha256_hex(v30_text.as_bytes()),
        "c4bde8384b872a03d7f9d7ecd87fdea60dc93a5b418ca831c8dbe5d8c3aa729d"
    );
    let v30_rows = v30_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v30_rows.len(), 8);
    let v30_body = format!("{}\n", v30_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v30_body.as_bytes()),
        "8eebd2a60dfdaf3896a31d7200525fc70667bed42ee8cbcc0076830bae74bd40"
    );
    let v30_java = v30_rows[6];
    assert_eq!(head_field(v30_java, "headOrder"), "30");
    assert_eq!(head_field(v30_java, "headX"), "101");
    assert_eq!(head_field(v30_java, "headSig"), "43");
    assert_eq!(head_field(v30_java, "headInterId"), "1371");
    assert_eq!(
        head_field(v30_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v30_java, "incident"),
        "[stem2343:headSideLEFT:heads[x100:sig42:id1369:sideLEFT,x101:sig43:id1371:sideLEFT]]"
    );
    assert_eq!(
        head_field(v30_java, "closureWrites"),
        "[x100:sig42:LEFT:false->true,x100:sig42:RIGHT:false->true]"
    );
    assert_eq!(head_field(v30_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v30_java, "sigVerticesAfter"), "683");
    assert_eq!(head_field(v30_java, "sigEdgesAfter"), "694");
    assert_eq!(head_field(v30_java, "systemStemsAfter"), "44");
    assert_eq!(
        head_field(v30_java, "relationStateHashAfter"),
        "c6dec8e3b73e751d8b552b377dd3fa2289d59644cda1674f4ff594247cbf60dd"
    );
    assert_eq!(
        head_field(v30_java, "linkerStateHashAfter"),
        "fbe38b9eb6f41dc2658fce5898c99c8ded024b455059a0d95b95ac1a42aa3835"
    );
    assert_eq!(head_field(v30_java, "nextHeadOrder"), "31");
    assert_eq!(head_field(v30_java, "nextHeadX"), "16");
    assert_eq!(head_field(v30_java, "nextHeadSig"), "81");
    assert_eq!(head_field(v30_java, "nextHeadInterId"), "1451");
    assert_eq!(head_field(v30_java, "terminal"), "ReturnedBeforeNextHead");
    let v30_summary = v30_rows[7];
    assert_eq!(
        head_field(v30_summary, "schema"),
        "stems-head-phase-prefix-v30"
    );
    assert_eq!(head_field(v30_summary, "rows"), "7");
    assert_eq!(
        head_field(v30_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v30_summary, "baseV29RunnerSourceSha256"),
        "0ae5afb409d11eef138ed62bb8adbefb04eabfa99c0581cad7a6952ecb5e1d4c"
    );
    assert_eq!(
        head_field(v30_summary, "baseV29FixtureSha256"),
        "a88b9fd3c27133c3c8bdcc839308365557c0e95c2ac3ea83fe348dc0d1ffa270"
    );
    assert_eq!(
        head_field(v30_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v30_summary, "probeSourceSha256"),
        "a8b50543359666567a01d503f46616d113feee03ac60828104d2b52efc558812"
    );
    assert_eq!(
        head_field(v30_summary, "runnerSourceSha256"),
        "d8f55efad82e15eb8b45c52ac8f99031c00ea0dd7143bc30c7c607fc103e71cf"
    );
    assert_eq!(
        head_field(v30_summary, "emittedBodySha256"),
        "8eebd2a60dfdaf3896a31d7200525fc70667bed42ee8cbcc0076830bae74bd40"
    );
    assert_eq!(
        head_field(v30_summary, "semanticPassSha256"),
        "803635259310df2794ac302b43a7b8286c95fb117f6f19177071c1ce25d484a9"
    );
    assert_eq!(
        head_field(v30_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v30_summary, "freshRuns"), "2");
    assert_eq!(head_field(v30_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v30_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder30PrelinkedClosure"
    );
    assert_eq!(
        head_field(v30_summary, "javaEvidence"),
        "ReturnedBeforeThirtyFirstHead"
    );

    // Boundary 57 expected-only v31 rows cover the next prelinked closure.
    let v31_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v31.txt"),
    )
    .expect("expected-only order31 continuation fixture");
    assert_eq!(
        sha256_hex(v31_text.as_bytes()),
        "ab58a7bf9d7a62fe7f9531f8b0f11e845d483d732010e565a1579fb7f4558f85"
    );
    let v31_rows = v31_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v31_rows.len(), 8);
    let v31_body = format!("{}\n", v31_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v31_body.as_bytes()),
        "34baf86107a36d017519d7ac0f0011a0eb8d67f93a5d9b2d95f55ccf0784dcc4"
    );
    let v31_java = v31_rows[6];
    assert_eq!(head_field(v31_java, "headOrder"), "31");
    assert_eq!(head_field(v31_java, "headX"), "16");
    assert_eq!(head_field(v31_java, "headSig"), "81");
    assert_eq!(head_field(v31_java, "headInterId"), "1451");
    assert_eq!(
        head_field(v31_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v31_java, "incident"),
        "[stem2360:headSideLEFT:heads[x15:sig80:id1449:sideLEFT,x16:sig81:id1451:sideLEFT]]"
    );
    assert_eq!(
        head_field(v31_java, "closureWrites"),
        "[x15:sig80:LEFT:false->true,x15:sig80:RIGHT:false->true]"
    );
    assert_eq!(head_field(v31_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v31_java, "sigVerticesAfter"), "683");
    assert_eq!(head_field(v31_java, "sigEdgesAfter"), "694");
    assert_eq!(head_field(v31_java, "systemStemsAfter"), "44");
    assert_eq!(
        head_field(v31_java, "relationStateHashAfter"),
        "c6dec8e3b73e751d8b552b377dd3fa2289d59644cda1674f4ff594247cbf60dd"
    );
    assert_eq!(
        head_field(v31_java, "linkerStateHashAfter"),
        "4713d6c479a46b49da415a3a74fa523427e566caa9a83719dac14f70f3f78beb"
    );
    assert_eq!(head_field(v31_java, "nextHeadOrder"), "32");
    assert_eq!(head_field(v31_java, "nextHeadX"), "34");
    assert_eq!(head_field(v31_java, "nextHeadSig"), "77");
    assert_eq!(head_field(v31_java, "nextHeadInterId"), "1443");
    assert_eq!(head_field(v31_java, "terminal"), "ReturnedBeforeNextHead");
    let v31_summary = v31_rows[7];
    assert_eq!(
        head_field(v31_summary, "schema"),
        "stems-head-phase-prefix-v31"
    );
    assert_eq!(head_field(v31_summary, "rows"), "7");
    assert_eq!(
        head_field(v31_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v31_summary, "baseV30RunnerSourceSha256"),
        "d8f55efad82e15eb8b45c52ac8f99031c00ea0dd7143bc30c7c607fc103e71cf"
    );
    assert_eq!(
        head_field(v31_summary, "baseV30FixtureSha256"),
        "c4bde8384b872a03d7f9d7ecd87fdea60dc93a5b418ca831c8dbe5d8c3aa729d"
    );
    assert_eq!(
        head_field(v31_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v31_summary, "probeSourceSha256"),
        "231028452d789e78ec96e5dc1c2f8ccabe88d85ac59aa9f990e18a0775d44404"
    );
    assert_eq!(
        head_field(v31_summary, "runnerSourceSha256"),
        "e7b8cd3bc87ff55969aee203b6027f7af572428cf91d442f94ea58e8f82d3e42"
    );
    assert_eq!(
        head_field(v31_summary, "emittedBodySha256"),
        "34baf86107a36d017519d7ac0f0011a0eb8d67f93a5d9b2d95f55ccf0784dcc4"
    );
    assert_eq!(
        head_field(v31_summary, "semanticPassSha256"),
        "3d123d0fcd70cdcdc3436a1ffca7b85ecac9e1a350c6a83368f91175e35eb4e4"
    );
    assert_eq!(
        head_field(v31_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v31_summary, "freshRuns"), "2");
    assert_eq!(head_field(v31_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v31_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder31PrelinkedClosure"
    );
    assert_eq!(
        head_field(v31_summary, "javaEvidence"),
        "ReturnedBeforeThirtySecondHead"
    );

    // Boundary 58 expected-only v32 rows cover the prelinked no-op closure.
    let v32_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v32.txt"),
    )
    .expect("expected-only order32 continuation fixture");
    assert_eq!(
        sha256_hex(v32_text.as_bytes()),
        "cceda3e1b00ccf9e4ca5f701c71a0a4da4e764488e192bf056ea645f11ad72c4"
    );
    let v32_rows = v32_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v32_rows.len(), 8);
    let v32_body = format!("{}\n", v32_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v32_body.as_bytes()),
        "77810c34e97279aef05feaa043df82a7ab4ba1566edc5933f38ff80608f10191"
    );
    let v32_java = v32_rows[6];
    assert_eq!(head_field(v32_java, "headOrder"), "32");
    assert_eq!(head_field(v32_java, "headX"), "34");
    assert_eq!(head_field(v32_java, "headSig"), "77");
    assert_eq!(head_field(v32_java, "headInterId"), "1443");
    assert_eq!(
        head_field(v32_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v32_java, "incident"),
        "[stem2368:headSideLEFT:heads[x34:sig77:id1443:sideLEFT]]"
    );
    assert_eq!(head_field(v32_java, "closureWrites"), "-");
    assert_eq!(head_field(v32_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v32_java, "sigVerticesAfter"), "683");
    assert_eq!(head_field(v32_java, "sigEdgesAfter"), "694");
    assert_eq!(head_field(v32_java, "systemStemsAfter"), "44");
    assert_eq!(
        head_field(v32_java, "relationStateHashAfter"),
        "c6dec8e3b73e751d8b552b377dd3fa2289d59644cda1674f4ff594247cbf60dd"
    );
    assert_eq!(
        head_field(v32_java, "linkerStateHashAfter"),
        "4713d6c479a46b49da415a3a74fa523427e566caa9a83719dac14f70f3f78beb"
    );
    assert_eq!(head_field(v32_java, "nextHeadOrder"), "33");
    assert_eq!(head_field(v32_java, "nextHeadX"), "88");
    assert_eq!(head_field(v32_java, "nextHeadSig"), "84");
    assert_eq!(head_field(v32_java, "nextHeadInterId"), "1457");
    assert_eq!(head_field(v32_java, "terminal"), "ReturnedBeforeNextHead");
    let v32_summary = v32_rows[7];
    assert_eq!(
        head_field(v32_summary, "schema"),
        "stems-head-phase-prefix-v32"
    );
    assert_eq!(head_field(v32_summary, "rows"), "7");
    assert_eq!(
        head_field(v32_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v32_summary, "baseV31RunnerSourceSha256"),
        "e7b8cd3bc87ff55969aee203b6027f7af572428cf91d442f94ea58e8f82d3e42"
    );
    assert_eq!(
        head_field(v32_summary, "baseV31FixtureSha256"),
        "ab58a7bf9d7a62fe7f9531f8b0f11e845d483d732010e565a1579fb7f4558f85"
    );
    assert_eq!(
        head_field(v32_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v32_summary, "probeSourceSha256"),
        "d1b3d61c46bfdfe540d33ae751d0006c4518142d8410277d8e4016a4b29b1fe5"
    );
    assert_eq!(
        head_field(v32_summary, "runnerSourceSha256"),
        "fecd661b0c9b9e03f17c9eba3482a86b7f2ae381e49ac93bbbcbfea4756c3cd8"
    );
    assert_eq!(
        head_field(v32_summary, "emittedBodySha256"),
        "77810c34e97279aef05feaa043df82a7ab4ba1566edc5933f38ff80608f10191"
    );
    assert_eq!(
        head_field(v32_summary, "semanticPassSha256"),
        "23a5e25617ef107a7f4b2b85ddb977d8d8b164d0203477d9f995d2d90df55bf5"
    );
    assert_eq!(
        head_field(v32_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v32_summary, "freshRuns"), "2");
    assert_eq!(head_field(v32_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v32_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder32PrelinkedNoOpClosure"
    );
    assert_eq!(
        head_field(v32_summary, "javaEvidence"),
        "ReturnedBeforeThirtyThirdHead"
    );

    // Boundary 59 expected-only v33 rows cover the next prelinked closure.
    let v33_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v33.txt"),
    )
    .expect("expected-only order33 continuation fixture");
    assert_eq!(
        sha256_hex(v33_text.as_bytes()),
        "a058341d3f661be4a677206c7a067f39a0785ae5adeed96be7d7073541fe2982"
    );
    let v33_rows = v33_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v33_rows.len(), 8);
    let v33_body = format!("{}\n", v33_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v33_body.as_bytes()),
        "2c212c41b06dc509b217ced1e3c0bedfd6c538f3684a705eeafc3e60ff33aed4"
    );
    let v33_java = v33_rows[6];
    assert_eq!(head_field(v33_java, "headOrder"), "33");
    assert_eq!(head_field(v33_java, "headX"), "88");
    assert_eq!(head_field(v33_java, "headSig"), "84");
    assert_eq!(head_field(v33_java, "headInterId"), "1457");
    assert_eq!(
        head_field(v33_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v33_java, "incident"),
        "[stem2367:headSideLEFT:heads[x87:sig83:id1455:sideLEFT,x88:sig84:id1457:sideLEFT]]"
    );
    assert_eq!(
        head_field(v33_java, "closureWrites"),
        "[x87:sig83:LEFT:false->true,x87:sig83:RIGHT:false->true]"
    );
    assert_eq!(head_field(v33_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v33_java, "sigVerticesAfter"), "683");
    assert_eq!(head_field(v33_java, "sigEdgesAfter"), "694");
    assert_eq!(head_field(v33_java, "systemStemsAfter"), "44");
    assert_eq!(
        head_field(v33_java, "relationStateHashAfter"),
        "c6dec8e3b73e751d8b552b377dd3fa2289d59644cda1674f4ff594247cbf60dd"
    );
    assert_eq!(
        head_field(v33_java, "linkerStateHashAfter"),
        "f9fad7a380bdd0b2796220aa59cfda4f1ecbf17fe806b0abb8cc2838e9761f61"
    );
    assert_eq!(head_field(v33_java, "nextHeadOrder"), "34");
    assert_eq!(head_field(v33_java, "nextHeadX"), "2");
    assert_eq!(head_field(v33_java, "nextHeadSig"), "36");
    assert_eq!(head_field(v33_java, "nextHeadInterId"), "1357");
    assert_eq!(head_field(v33_java, "terminal"), "ReturnedBeforeNextHead");
    let v33_summary = v33_rows[7];
    assert_eq!(
        head_field(v33_summary, "schema"),
        "stems-head-phase-prefix-v33"
    );
    assert_eq!(head_field(v33_summary, "rows"), "7");
    assert_eq!(
        head_field(v33_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v33_summary, "baseV32RunnerSourceSha256"),
        "fecd661b0c9b9e03f17c9eba3482a86b7f2ae381e49ac93bbbcbfea4756c3cd8"
    );
    assert_eq!(
        head_field(v33_summary, "baseV32FixtureSha256"),
        "cceda3e1b00ccf9e4ca5f701c71a0a4da4e764488e192bf056ea645f11ad72c4"
    );
    assert_eq!(
        head_field(v33_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v33_summary, "probeSourceSha256"),
        "2e4a07c2efbdf0e43bb92f9bd6213cd6faf3e2a0e39eed610f11e13e15e42d72"
    );
    assert_eq!(
        head_field(v33_summary, "runnerSourceSha256"),
        "472e88ea561df7db9280c5ec79a2ea8a5204783d3ffec8894455adcf5b342692"
    );
    assert_eq!(
        head_field(v33_summary, "emittedBodySha256"),
        "2c212c41b06dc509b217ced1e3c0bedfd6c538f3684a705eeafc3e60ff33aed4"
    );
    assert_eq!(
        head_field(v33_summary, "semanticPassSha256"),
        "8aec2db3857f6d2d8dc60bdb381ce3cfb8a16a1e441efd348854489bbcc53b43"
    );
    assert_eq!(
        head_field(v33_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v33_summary, "freshRuns"), "2");
    assert_eq!(head_field(v33_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v33_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder33PrelinkedClosure"
    );
    assert_eq!(
        head_field(v33_summary, "javaEvidence"),
        "ReturnedBeforeThirtyFourthHead"
    );

    // Boundary 60 expected-only v34 rows cover the authenticated C-link.
    let v34_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v34.txt"),
    )
    .expect("expected-only order34 C-link fixture");
    assert_eq!(
        sha256_hex(v34_text.as_bytes()),
        "b67514520fa848fd9758d0bdc740d2be4600c723ac341b57fced42f4657103a8"
    );
    let v34_rows = v34_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v34_rows.len(), 10);
    let v34_body = format!("{}\n", v34_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v34_body.as_bytes()),
        "85d05cca18e6b15414729404191bd84d0729bf657678b0dfaa626ab72b915ae4"
    );
    let v34_frontier = v34_rows[6];
    assert_eq!(head_field(v34_frontier, "headOrder"), "34");
    assert_eq!(head_field(v34_frontier, "headX"), "2");
    assert_eq!(head_field(v34_frontier, "headSig"), "36");
    assert_eq!(head_field(v34_frontier, "headInterId"), "1357");
    assert_eq!(head_field(v34_frontier, "cAlias"), "h:2:LEFT:BOTTOM");
    assert_eq!(head_field(v34_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v34_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v34_frontier, "relations"), "1");
    assert_eq!(head_field(v34_frontier, "glyphs"), "2");
    assert_eq!(head_field(v34_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v34_frontier, "existingGlyph"), "glyph:322");
    assert_eq!(head_field(v34_frontier, "existingActive"), "true");
    assert_eq!(head_field(v34_frontier, "existingStem"), "-");
    let v34_result = v34_rows[7];
    assert_eq!(head_field(v34_result, "headOrder"), "34");
    assert_eq!(head_field(v34_result, "allocatorBefore"), "2383");
    assert_eq!(head_field(v34_result, "allocatorAfter"), "2384");
    assert_eq!(
        head_field(v34_result, "addedVertices"),
        "[id2384:org.audiveris.omr.sig.inter.StemInter:shape=STEM:grade=0x1.9189bb14f51f5p-1/3fe9189bb14f51f5:bounds=393:393:4:86:removed=false:abnormal=false:manual=false:implicit=false:profile=0:median=0x1.8aca8cb6c12c5p8/4078aca8cb6c12c5:0x1.89p8/4078900000000000:0x1.8a79a33aaf5c1p8/4078a79a33aaf5c1:0x1.dfp8/407df00000000000:width=0x1.53594d653594dp1/40053594d653594d]"
    );
    assert_eq!(
        head_field(v34_result, "linkerChanges"),
        "[h:2:LEFT:BOTTOM:false:false->true:false,h:2:LEFT:TOP:false:false->true:false,linker:SLinker:head:2:false:false->true:false]"
    );
    assert_eq!(
        head_field(v34_result, "relationStateHashAfter"),
        "b898177fe61225ee36577bd8a54a0f5e81ebfc1a5f1eb59bbb82572004739a78"
    );
    assert_eq!(
        head_field(v34_result, "linkerStateHashAfter"),
        "0c57b977b4080df4700657c9a1d7d02217e31be66d2d97b5c7346e0d9b387f42"
    );
    let v34_java = v34_rows[8];
    assert_eq!(head_field(v34_java, "headOrder"), "34");
    assert_eq!(head_field(v34_java, "headX"), "2");
    assert_eq!(head_field(v34_java, "headSig"), "36");
    assert_eq!(head_field(v34_java, "headInterId"), "1357");
    assert_eq!(
        head_field(v34_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v34_java, "sigVerticesAfter"), "684");
    assert_eq!(head_field(v34_java, "sigEdgesAfter"), "695");
    assert_eq!(head_field(v34_java, "systemStemsAfter"), "45");
    assert_eq!(head_field(v34_java, "nextHeadOrder"), "35");
    assert_eq!(head_field(v34_java, "nextHeadX"), "50");
    assert_eq!(head_field(v34_java, "nextHeadSig"), "72");
    assert_eq!(head_field(v34_java, "nextHeadInterId"), "1433");
    assert_eq!(head_field(v34_java, "terminal"), "ReturnedBeforeNextHead");
    let v34_summary = v34_rows[9];
    assert_eq!(
        head_field(v34_summary, "schema"),
        "stems-head-phase-prefix-v34"
    );
    assert_eq!(head_field(v34_summary, "rows"), "9");
    assert_eq!(
        head_field(v34_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v34_summary, "baseV33RunnerSourceSha256"),
        "472e88ea561df7db9280c5ec79a2ea8a5204783d3ffec8894455adcf5b342692"
    );
    assert_eq!(
        head_field(v34_summary, "baseV33FixtureSha256"),
        "a058341d3f661be4a677206c7a067f39a0785ae5adeed96be7d7073541fe2982"
    );
    assert_eq!(
        head_field(v34_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v34_summary, "probeSourceSha256"),
        "4cec5bfe6379e31701b7e4ea4f2ad98a8d36680daefa7a8a8d9d4c179d2c6777"
    );
    assert_eq!(
        head_field(v34_summary, "runnerSourceSha256"),
        "60b4cc5a9e0a9fe5c6d4a8bb1b03bfadf065259c07bc124c6587b3d7a9c3a93f"
    );
    assert_eq!(
        head_field(v34_summary, "emittedBodySha256"),
        "85d05cca18e6b15414729404191bd84d0729bf657678b0dfaa626ab72b915ae4"
    );
    assert_eq!(
        head_field(v34_summary, "semanticPassSha256"),
        "486957d6a77dce18fc15bd92761e5624e6b8edef9705d35403f00419b011b4dd"
    );
    assert_eq!(
        head_field(v34_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v34_summary, "freshRuns"), "2");
    assert_eq!(head_field(v34_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v34_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder34CLink"
    );
    assert_eq!(
        head_field(v34_summary, "javaEvidence"),
        "ReturnedBeforeThirtyFifthHead"
    );

    // Boundary 61 expected-only v35 rows cover the post-C-link closure.
    let v35_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v35.txt"),
    )
    .expect("expected-only order35 continuation fixture");
    assert_eq!(
        sha256_hex(v35_text.as_bytes()),
        "2721b843514ce7a695fdacc797addd21597bd604b39168fe63533ecfc01bd55b"
    );
    let v35_rows = v35_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v35_rows.len(), 8);
    let v35_body = format!("{}\n", v35_rows[..7].join("\n"));
    assert_eq!(
        sha256_hex(v35_body.as_bytes()),
        "12d32f9193480ac9772e735735210b3689266458f4dad379a72131bb9024cc84"
    );
    let v35_java = v35_rows[6];
    assert_eq!(head_field(v35_java, "headOrder"), "35");
    assert_eq!(head_field(v35_java, "headX"), "50");
    assert_eq!(head_field(v35_java, "headSig"), "72");
    assert_eq!(head_field(v35_java, "headInterId"), "1433");
    assert_eq!(
        head_field(v35_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v35_java, "incident"),
        "[stem2353:headSideLEFT:heads[x49:sig71:id1431:sideLEFT,x50:sig72:id1433:sideLEFT]]"
    );
    assert_eq!(
        head_field(v35_java, "closureWrites"),
        "[x49:sig71:LEFT:false->true,x49:sig71:RIGHT:false->true]"
    );
    assert_eq!(head_field(v35_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v35_java, "sigVerticesAfter"), "684");
    assert_eq!(head_field(v35_java, "sigEdgesAfter"), "695");
    assert_eq!(head_field(v35_java, "systemStemsAfter"), "45");
    assert_eq!(
        head_field(v35_java, "relationStateHashAfter"),
        "b898177fe61225ee36577bd8a54a0f5e81ebfc1a5f1eb59bbb82572004739a78"
    );
    assert_eq!(
        head_field(v35_java, "linkerStateHashAfter"),
        "1b3dfc3250570293b13eaaee2e6baaa0637dcc3e9933a90fb96bdc3e08e282c5"
    );
    assert_eq!(head_field(v35_java, "nextHeadOrder"), "36");
    assert_eq!(head_field(v35_java, "nextHeadX"), "23");
    assert_eq!(head_field(v35_java, "nextHeadSig"), "14");
    assert_eq!(head_field(v35_java, "nextHeadInterId"), "1313");
    assert_eq!(head_field(v35_java, "terminal"), "ReturnedBeforeNextHead");
    let v35_summary = v35_rows[7];
    assert_eq!(
        head_field(v35_summary, "schema"),
        "stems-head-phase-prefix-v35"
    );
    assert_eq!(head_field(v35_summary, "rows"), "7");
    assert_eq!(
        head_field(v35_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v35_summary, "baseV34RunnerSourceSha256"),
        "60b4cc5a9e0a9fe5c6d4a8bb1b03bfadf065259c07bc124c6587b3d7a9c3a93f"
    );
    assert_eq!(
        head_field(v35_summary, "baseV34FixtureSha256"),
        "b67514520fa848fd9758d0bdc740d2be4600c723ac341b57fced42f4657103a8"
    );
    assert_eq!(
        head_field(v35_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v35_summary, "probeSourceSha256"),
        "611a02c34f4690031db91ce7ccced19ef6a1d7ec3d6da0dd81333f07aa315b42"
    );
    assert_eq!(
        head_field(v35_summary, "runnerSourceSha256"),
        "74aec11451cb5933938b3bc82876ddfdb9e4bdab295e472644698c68d2cbc5ea"
    );
    assert_eq!(
        head_field(v35_summary, "emittedBodySha256"),
        "12d32f9193480ac9772e735735210b3689266458f4dad379a72131bb9024cc84"
    );
    assert_eq!(
        head_field(v35_summary, "semanticPassSha256"),
        "992372127972f17882a8f672653b9b4530497d06c8c6a43f6209ad6c8e22a1dd"
    );
    assert_eq!(
        head_field(v35_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v35_summary, "freshRuns"), "2");
    assert_eq!(head_field(v35_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v35_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder35PrelinkedClosure"
    );
    assert_eq!(
        head_field(v35_summary, "javaEvidence"),
        "ReturnedBeforeThirtySixthHead"
    );

    // Boundary 62 expected-only v36 rows cover the authenticated C-link.
    let v36_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v36.txt"),
    )
    .expect("expected-only order36 C-link fixture");
    assert_eq!(
        sha256_hex(v36_text.as_bytes()),
        "7d7d0d17e51c03a145bdff3a739da3aaaa05fb0c5bba20cd9a46468742eb26e7"
    );
    let v36_rows = v36_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v36_rows.len(), 10);
    let v36_body = format!("{}\n", v36_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v36_body.as_bytes()),
        "7f61d1814c2542ae95f54515aa97a8f35ed3be2905e87336c15a83a0d8c6489b"
    );
    let v36_frontier = v36_rows[6];
    assert_eq!(head_field(v36_frontier, "headOrder"), "36");
    assert_eq!(head_field(v36_frontier, "headX"), "23");
    assert_eq!(head_field(v36_frontier, "headSig"), "14");
    assert_eq!(head_field(v36_frontier, "headInterId"), "1313");
    assert_eq!(head_field(v36_frontier, "cAlias"), "h:23:LEFT:BOTTOM");
    assert_eq!(head_field(v36_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v36_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v36_frontier, "relations"), "1");
    assert_eq!(head_field(v36_frontier, "glyphs"), "1");
    assert_eq!(head_field(v36_frontier, "candidateIdBefore"), "324");
    assert_eq!(head_field(v36_frontier, "existingGlyph"), "glyph:324");
    assert_eq!(head_field(v36_frontier, "existingActive"), "true");
    assert_eq!(head_field(v36_frontier, "existingStem"), "-");
    let v36_result = v36_rows[7];
    assert_eq!(head_field(v36_result, "headOrder"), "36");
    assert_eq!(head_field(v36_result, "allocatorBefore"), "2384");
    assert_eq!(head_field(v36_result, "allocatorAfter"), "2385");
    assert_eq!(
        head_field(v36_result, "addedVertices"),
        "[id2385:org.audiveris.omr.sig.inter.StemInter:shape=STEM:grade=0x1.93520e3096935p-1/3fe93520e3096935:bounds=868:374:4:86:removed=false:abnormal=false:manual=false:implicit=false:profile=0:median=0x1.b3134771ab5bep9/408b3134771ab5be:0x1.76p8/4077600000000000:0x1.b2a547c5d7a5cp9/408b2a547c5d7a5c:0x1.ccp8/407cc00000000000:width=0x1.17d05f417d05fp1/40017d05f417d05f]"
    );
    assert_eq!(
        head_field(v36_result, "linkerChanges"),
        "[h:23:LEFT:BOTTOM:false:false->true:false,h:23:LEFT:TOP:false:false->true:false,linker:SLinker:head:23:false:false->true:false]"
    );
    assert_eq!(
        head_field(v36_result, "relationStateHashAfter"),
        "584ad9360f24f9778575c0f3f92c0572eb55814bfa11aafd461d52380ca09e4b"
    );
    assert_eq!(
        head_field(v36_result, "linkerStateHashAfter"),
        "0e55fd728430544b98cccd41ee30f0bd9dfb097a7e44e054a8e87eadf042773f"
    );
    let v36_java = v36_rows[8];
    assert_eq!(head_field(v36_java, "headOrder"), "36");
    assert_eq!(head_field(v36_java, "headX"), "23");
    assert_eq!(head_field(v36_java, "headSig"), "14");
    assert_eq!(head_field(v36_java, "headInterId"), "1313");
    assert_eq!(
        head_field(v36_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v36_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v36_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v36_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v36_java, "relationStateHashAfter"),
        "584ad9360f24f9778575c0f3f92c0572eb55814bfa11aafd461d52380ca09e4b"
    );
    assert_eq!(
        head_field(v36_java, "linkerStateHashAfter"),
        "0e55fd728430544b98cccd41ee30f0bd9dfb097a7e44e054a8e87eadf042773f"
    );
    assert_eq!(head_field(v36_java, "nextHeadOrder"), "37");
    assert_eq!(head_field(v36_java, "nextHeadX"), "14");
    assert_eq!(head_field(v36_java, "nextHeadSig"), "1");
    assert_eq!(head_field(v36_java, "nextHeadInterId"), "1287");
    assert_eq!(head_field(v36_java, "terminal"), "ReturnedBeforeNextHead");
    let v36_summary = v36_rows[9];
    assert_eq!(
        head_field(v36_summary, "schema"),
        "stems-head-phase-prefix-v36"
    );
    assert_eq!(head_field(v36_summary, "rows"), "9");
    assert_eq!(
        head_field(v36_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v36_summary, "baseV35RunnerSourceSha256"),
        "74aec11451cb5933938b3bc82876ddfdb9e4bdab295e472644698c68d2cbc5ea"
    );
    assert_eq!(
        head_field(v36_summary, "baseV35FixtureSha256"),
        "2721b843514ce7a695fdacc797addd21597bd604b39168fe63533ecfc01bd55b"
    );
    assert_eq!(
        head_field(v36_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v36_summary, "probeSourceSha256"),
        "582922fe7442de97a34732791352550e0026d9cf16cae36d633266eb15273aba"
    );
    assert_eq!(
        head_field(v36_summary, "runnerSourceSha256"),
        "3176407de9bdd88f167e925a2b901f811f230f6b83c5a120ddf031a42ec49fd4"
    );
    assert_eq!(
        head_field(v36_summary, "emittedBodySha256"),
        "7f61d1814c2542ae95f54515aa97a8f35ed3be2905e87336c15a83a0d8c6489b"
    );
    assert_eq!(
        head_field(v36_summary, "semanticPassSha256"),
        "f3e073ba83536e4afc1c0ea13a5933f199cfad57f1b82b6974f5abb9081039bd"
    );
    assert_eq!(
        head_field(v36_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v36_summary, "freshRuns"), "2");
    assert_eq!(head_field(v36_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v36_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder36CLink"
    );
    assert_eq!(
        head_field(v36_summary, "javaEvidence"),
        "ReturnedBeforeThirtySeventhHead"
    );

    // Boundary 63 expected-only v37 rows cover the existing-stem reconciliation.
    let v37_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v37.txt"),
    )
    .expect("expected-only order37 existing-stem fixture");
    assert_eq!(
        sha256_hex(v37_text.as_bytes()),
        "5af8e1928df00217e1780e2e6e0d057c4202b0f1cf46f25d5d889678c5fdf2b8"
    );
    let v37_rows = v37_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v37_rows.len(), 10);
    let v37_body = format!("{}\n", v37_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v37_body.as_bytes()),
        "4c69d4c1740899bf4c71dbc895f022882f87d772233642f638ba3ecdc4db3fb1"
    );
    let v37_frontier = v37_rows[6];
    assert_eq!(head_field(v37_frontier, "headOrder"), "37");
    assert_eq!(head_field(v37_frontier, "headX"), "14");
    assert_eq!(head_field(v37_frontier, "headSig"), "1");
    assert_eq!(head_field(v37_frontier, "headInterId"), "1287");
    assert_eq!(head_field(v37_frontier, "cAlias"), "h:14:LEFT:BOTTOM");
    assert_eq!(head_field(v37_frontier, "lastIndex"), "4");
    assert_eq!(head_field(v37_frontier, "maxIndex"), "4");
    assert_eq!(head_field(v37_frontier, "relations"), "4");
    assert_eq!(head_field(v37_frontier, "glyphs"), "2");
    assert_eq!(head_field(v37_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v37_frontier, "existingGlyph"), "glyph:294");
    assert_eq!(head_field(v37_frontier, "existingActive"), "true");
    assert_eq!(head_field(v37_frontier, "existingStem"), "2340");
    let v37_result = v37_rows[7];
    assert_eq!(head_field(v37_result, "headOrder"), "37");
    assert_eq!(head_field(v37_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v37_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v37_result, "addedVertices"), "-");
    assert_eq!(head_field(v37_result, "addedEdges"), "-");
    assert_eq!(
        head_field(v37_result, "linkerChanges"),
        "[h:13:LEFT:BOTTOM:true:false->true:true,h:13:LEFT:TOP:true:false->true:true,h:13:RIGHT:BOTTOM:false:false->false:true,h:13:RIGHT:TOP:false:false->false:true,linker:SLinker:head:13:false:false->false:true,linker:SLinker:head:13:true:false->true:true]"
    );
    assert_eq!(
        head_field(v37_result, "relationStateHashAfter"),
        "584ad9360f24f9778575c0f3f92c0572eb55814bfa11aafd461d52380ca09e4b"
    );
    assert_eq!(
        head_field(v37_result, "linkerStateHashAfter"),
        "4d02df80c67914270e1f2975016c831848199968a299c68b6f44fdd51acd0e3a"
    );
    let v37_java = v37_rows[8];
    assert_eq!(head_field(v37_java, "headOrder"), "37");
    assert_eq!(head_field(v37_java, "headX"), "14");
    assert_eq!(head_field(v37_java, "headSig"), "1");
    assert_eq!(head_field(v37_java, "headInterId"), "1287");
    assert_eq!(
        head_field(v37_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v37_java, "incident"),
        "[stem2340:headSideLEFT:heads[x13:sig0:id1285:sideLEFT,x14:sig1:id1287:sideLEFT]]"
    );
    assert_eq!(
        head_field(v37_java, "closureWrites"),
        "[x13:sig0:LEFT:false->true,x13:sig0:RIGHT:false->true]"
    );
    assert_eq!(head_field(v37_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v37_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v37_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v37_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v37_java, "relationStateHashAfter"),
        "584ad9360f24f9778575c0f3f92c0572eb55814bfa11aafd461d52380ca09e4b"
    );
    assert_eq!(
        head_field(v37_java, "linkerStateHashAfter"),
        "4d02df80c67914270e1f2975016c831848199968a299c68b6f44fdd51acd0e3a"
    );
    assert_eq!(head_field(v37_java, "nextHeadOrder"), "38");
    assert_eq!(head_field(v37_java, "nextHeadX"), "18");
    assert_eq!(head_field(v37_java, "nextHeadSig"), "4");
    assert_eq!(head_field(v37_java, "nextHeadInterId"), "1293");
    assert_eq!(head_field(v37_java, "terminal"), "ReturnedBeforeNextHead");
    let v37_summary = v37_rows[9];
    assert_eq!(
        head_field(v37_summary, "schema"),
        "stems-head-phase-prefix-v37"
    );
    assert_eq!(head_field(v37_summary, "rows"), "9");
    assert_eq!(
        head_field(v37_summary, "baseProbeSourceSha256"),
        "d5d46115fb4358918648d35e24cd043753b62ce709f767f8958d34ba25c9c4cf"
    );
    assert_eq!(
        head_field(v37_summary, "baseV36RunnerSourceSha256"),
        "3176407de9bdd88f167e925a2b901f811f230f6b83c5a120ddf031a42ec49fd4"
    );
    assert_eq!(
        head_field(v37_summary, "baseV36FixtureSha256"),
        "7d7d0d17e51c03a145bdff3a739da3aaaa05fb0c5bba20cd9a46468742eb26e7"
    );
    assert_eq!(
        head_field(v37_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v37_summary, "probeSourceSha256"),
        "58ed9ebbd2fa05e9e52349b5ad42195a8f9fe534b46e088e6be7dd850d6ab1bb"
    );
    assert_eq!(
        head_field(v37_summary, "runnerSourceSha256"),
        "2fac40e0bf6f49186a994bae499aa371be8bee2152297d325bae067c3f8d5bc1"
    );
    assert_eq!(
        head_field(v37_summary, "emittedBodySha256"),
        "4c69d4c1740899bf4c71dbc895f022882f87d772233642f638ba3ecdc4db3fb1"
    );
    assert_eq!(
        head_field(v37_summary, "semanticPassSha256"),
        "fcb9fec2a764e9ab06d6b91ca856a8832ef236754dcb45ba345ae3f8f7280d90"
    );
    assert_eq!(
        head_field(v37_summary, "completeStumpsFixtureSha256"),
        sha256_hex(complete_stumps_text.as_bytes())
    );
    assert_eq!(head_field(v37_summary, "freshRuns"), "2");
    assert_eq!(head_field(v37_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v37_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder37ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v37_summary, "javaEvidence"),
        "ReturnedBeforeThirtyEighthHead"
    );

    // Boundary 64 expected-only v38 rows cover the next existing-stem reconciliation.
    let v38_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v38.txt"),
    )
    .expect("expected-only order38 existing-stem fixture");
    assert_eq!(
        sha256_hex(v38_text.as_bytes()),
        "98c8d3c19d50df531d756d6fd50ddbc9f07ce7db24bea47849fff731d5271b0f"
    );
    let v38_rows = v38_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v38_rows.len(), 10);
    let v38_body = format!("{}\n", v38_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v38_body.as_bytes()),
        "64a375a90ec14b1e4735027c53a2f650774eb22f8ec6cc4884dacddf008ef859"
    );
    let v38_frontier = v38_rows[6];
    assert_eq!(head_field(v38_frontier, "headOrder"), "38");
    assert_eq!(head_field(v38_frontier, "headX"), "18");
    assert_eq!(head_field(v38_frontier, "headSig"), "4");
    assert_eq!(head_field(v38_frontier, "headInterId"), "1293");
    assert_eq!(head_field(v38_frontier, "cAlias"), "h:18:LEFT:BOTTOM");
    assert_eq!(head_field(v38_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v38_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v38_frontier, "relations"), "2");
    assert_eq!(head_field(v38_frontier, "glyphs"), "1");
    assert_eq!(head_field(v38_frontier, "candidateIdBefore"), "310");
    assert_eq!(head_field(v38_frontier, "existingGlyph"), "glyph:310");
    assert_eq!(head_field(v38_frontier, "existingActive"), "true");
    assert_eq!(head_field(v38_frontier, "existingStem"), "2372");
    let v38_result = v38_rows[7];
    assert_eq!(head_field(v38_result, "headOrder"), "38");
    assert_eq!(head_field(v38_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v38_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v38_result, "addedVertices"), "-");
    assert_eq!(head_field(v38_result, "addedEdges"), "-");
    assert_eq!(head_field(v38_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v38_result, "linkerChanges"),
        "[h:17:LEFT:BOTTOM:true:false->true:true,h:17:LEFT:TOP:true:false->true:true,h:17:RIGHT:BOTTOM:false:false->false:true,h:17:RIGHT:TOP:false:false->false:true,linker:SLinker:head:17:false:false->false:true,linker:SLinker:head:17:true:false->true:true]"
    );
    assert_eq!(
        head_field(v38_result, "linkerStateHashAfter"),
        "b7370ec6f1e8c9b313f64a4d97ddc05df68d90876eb536b78b47a3ee9f6ad9b9"
    );
    let v38_java = v38_rows[8];
    assert_eq!(head_field(v38_java, "headOrder"), "38");
    assert_eq!(head_field(v38_java, "headX"), "18");
    assert_eq!(head_field(v38_java, "headSig"), "4");
    assert_eq!(head_field(v38_java, "headInterId"), "1293");
    assert_eq!(
        head_field(v38_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v38_java, "incident"),
        "[stem2372:headSideLEFT:heads[x17:sig10:id1305:sideLEFT,x18:sig4:id1293:sideLEFT]]"
    );
    assert_eq!(
        head_field(v38_java, "closureWrites"),
        "[x17:sig10:LEFT:false->true,x17:sig10:RIGHT:false->true]"
    );
    assert_eq!(head_field(v38_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v38_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v38_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v38_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v38_java, "linkerStateHashAfter"),
        "b7370ec6f1e8c9b313f64a4d97ddc05df68d90876eb536b78b47a3ee9f6ad9b9"
    );
    assert_eq!(head_field(v38_java, "nextHeadOrder"), "39");
    assert_eq!(head_field(v38_java, "nextHeadX"), "97");
    assert_eq!(head_field(v38_java, "nextHeadSig"), "34");
    assert_eq!(head_field(v38_java, "nextHeadInterId"), "1353");
    assert_eq!(head_field(v38_java, "terminal"), "ReturnedBeforeNextHead");
    let v38_summary = v38_rows[9];
    assert_eq!(
        head_field(v38_summary, "schema"),
        "stems-head-phase-prefix-v38"
    );
    assert_eq!(head_field(v38_summary, "rows"), "9");
    assert_eq!(
        head_field(v38_summary, "baseV37RunnerSourceSha256"),
        "2fac40e0bf6f49186a994bae499aa371be8bee2152297d325bae067c3f8d5bc1"
    );
    assert_eq!(
        head_field(v38_summary, "baseV37FixtureSha256"),
        "5af8e1928df00217e1780e2e6e0d057c4202b0f1cf46f25d5d889678c5fdf2b8"
    );
    assert_eq!(
        head_field(v38_summary, "probeSourceSha256"),
        "8da7b91134b4ae654461eecd7f4f5009e3fe205f140663dad836b0820465a214"
    );
    assert_eq!(
        head_field(v38_summary, "runnerSourceSha256"),
        "ad2edbfdf046db3a27b67d81da23f6f30d254cde9c91eb92063df72da10c7551"
    );
    assert_eq!(
        head_field(v38_summary, "emittedBodySha256"),
        "64a375a90ec14b1e4735027c53a2f650774eb22f8ec6cc4884dacddf008ef859"
    );
    assert_eq!(
        head_field(v38_summary, "semanticPassSha256"),
        "57e46879aca3fc5a02851b590a14347df4535beff0b7c97855d42afe95155422"
    );
    assert_eq!(head_field(v38_summary, "freshRuns"), "2");
    assert_eq!(head_field(v38_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v38_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder38ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v38_summary, "javaEvidence"),
        "ReturnedBeforeThirtyNinthHead"
    );

    // Boundary 65 expected-only v39 rows cover the next existing-stem reconciliation.
    let v39_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v39.txt"),
    )
    .expect("expected-only order39 existing-stem fixture");
    assert_eq!(
        sha256_hex(v39_text.as_bytes()),
        "771b7816918d098e66fa1c599df1a68bfb3e24d1724ea6f701ba3bcc59b031fa"
    );
    let v39_rows = v39_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v39_rows.len(), 10);
    let v39_body = format!("{}\n", v39_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v39_body.as_bytes()),
        "6f3518552f431fd0108d3c64efc6d5c2a99cd57ff841f8cbcc2987ecb80c6090"
    );
    let v39_frontier = v39_rows[6];
    assert_eq!(head_field(v39_frontier, "headOrder"), "39");
    assert_eq!(head_field(v39_frontier, "headX"), "97");
    assert_eq!(head_field(v39_frontier, "headSig"), "34");
    assert_eq!(head_field(v39_frontier, "headInterId"), "1353");
    assert_eq!(head_field(v39_frontier, "cAlias"), "h:97:LEFT:BOTTOM");
    assert_eq!(head_field(v39_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v39_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v39_frontier, "relations"), "2");
    assert_eq!(head_field(v39_frontier, "glyphs"), "1");
    assert_eq!(head_field(v39_frontier, "candidateIdBefore"), "321");
    assert_eq!(head_field(v39_frontier, "existingGlyph"), "glyph:321");
    assert_eq!(head_field(v39_frontier, "existingActive"), "true");
    assert_eq!(head_field(v39_frontier, "existingStem"), "2373");
    let v39_result = v39_rows[7];
    assert_eq!(head_field(v39_result, "headOrder"), "39");
    assert_eq!(head_field(v39_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v39_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v39_result, "addedVertices"), "-");
    assert_eq!(head_field(v39_result, "addedEdges"), "-");
    assert_eq!(head_field(v39_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v39_result, "linkerChanges"),
        "[h:96:LEFT:BOTTOM:true:false->true:true,h:96:LEFT:TOP:true:false->true:true,h:96:RIGHT:BOTTOM:false:false->false:true,h:96:RIGHT:TOP:false:false->false:true,linker:SLinker:head:96:false:false->false:true,linker:SLinker:head:96:true:false->true:true]"
    );
    assert_eq!(
        head_field(v39_result, "linkerStateHashAfter"),
        "11d7b1cc8d591e97f401f227c25fbd757ab36939f0f8286004e9532ab191553f"
    );
    let v39_java = v39_rows[8];
    assert_eq!(head_field(v39_java, "headOrder"), "39");
    assert_eq!(head_field(v39_java, "headX"), "97");
    assert_eq!(head_field(v39_java, "headSig"), "34");
    assert_eq!(head_field(v39_java, "headInterId"), "1353");
    assert_eq!(
        head_field(v39_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v39_java, "incident"),
        "[stem2373:headSideLEFT:heads[x96:sig41:id1367:sideLEFT,x97:sig34:id1353:sideLEFT]]"
    );
    assert_eq!(
        head_field(v39_java, "closureWrites"),
        "[x96:sig41:LEFT:false->true,x96:sig41:RIGHT:false->true]"
    );
    assert_eq!(head_field(v39_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v39_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v39_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v39_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v39_java, "linkerStateHashAfter"),
        "11d7b1cc8d591e97f401f227c25fbd757ab36939f0f8286004e9532ab191553f"
    );
    assert_eq!(head_field(v39_java, "nextHeadOrder"), "40");
    assert_eq!(head_field(v39_java, "nextHeadX"), "6");
    assert_eq!(head_field(v39_java, "nextHeadSig"), "89");
    assert_eq!(head_field(v39_java, "nextHeadInterId"), "1467");
    assert_eq!(head_field(v39_java, "terminal"), "ReturnedBeforeNextHead");
    let v39_summary = v39_rows[9];
    assert_eq!(
        head_field(v39_summary, "schema"),
        "stems-head-phase-prefix-v39"
    );
    assert_eq!(head_field(v39_summary, "rows"), "9");
    assert_eq!(
        head_field(v39_summary, "baseV38RunnerSourceSha256"),
        "ad2edbfdf046db3a27b67d81da23f6f30d254cde9c91eb92063df72da10c7551"
    );
    assert_eq!(
        head_field(v39_summary, "baseV38FixtureSha256"),
        "98c8d3c19d50df531d756d6fd50ddbc9f07ce7db24bea47849fff731d5271b0f"
    );
    assert_eq!(
        head_field(v39_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v39_summary, "probeSourceSha256"),
        "990556c3e12f99826c6ca92596045d44cec482263c76040613b8afc1bfd796d8"
    );
    assert_eq!(
        head_field(v39_summary, "runnerSourceSha256"),
        "bf7855c0d53d59cea3593de72f51f7272168f488e65148267ebd55e9f70110c7"
    );
    assert_eq!(
        head_field(v39_summary, "emittedBodySha256"),
        "6f3518552f431fd0108d3c64efc6d5c2a99cd57ff841f8cbcc2987ecb80c6090"
    );
    assert_eq!(
        head_field(v39_summary, "semanticPassSha256"),
        "2d51ffd86926e5a39870f9e5d1222d359f28121a4f5e9ccda9b072e5fd94b73b"
    );
    assert_eq!(head_field(v39_summary, "freshRuns"), "2");
    assert_eq!(head_field(v39_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v39_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder39ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v39_summary, "javaEvidence"),
        "ReturnedBeforeFortiethHead"
    );

    // Boundary 66 expected-only v40 rows cover the next existing-stem reconciliation.
    let v40_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v40.txt"),
    )
    .expect("expected-only order40 existing-stem fixture");
    assert_eq!(
        sha256_hex(v40_text.as_bytes()),
        "26e4a2ecbd547829c573c4c7737331e4773f6faf64581ecfdf380a6b87283fa9"
    );
    let v40_rows = v40_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v40_rows.len(), 10);
    let v40_body = format!("{}\n", v40_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v40_body.as_bytes()),
        "9be2634c8582ff4f023e17313aa9b91524b542d07c3c69363906b1d1e05acaa6"
    );
    let v40_frontier = v40_rows[6];
    assert_eq!(head_field(v40_frontier, "headOrder"), "40");
    assert_eq!(head_field(v40_frontier, "headX"), "6");
    assert_eq!(head_field(v40_frontier, "headSig"), "89");
    assert_eq!(head_field(v40_frontier, "headInterId"), "1467");
    assert_eq!(head_field(v40_frontier, "cAlias"), "h:6:LEFT:BOTTOM");
    assert_eq!(head_field(v40_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v40_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v40_frontier, "relations"), "3");
    assert_eq!(head_field(v40_frontier, "glyphs"), "1");
    assert_eq!(head_field(v40_frontier, "candidateIdBefore"), "290");
    assert_eq!(head_field(v40_frontier, "existingGlyph"), "glyph:290");
    assert_eq!(head_field(v40_frontier, "existingActive"), "true");
    assert_eq!(head_field(v40_frontier, "existingStem"), "2348");
    let v40_result = v40_rows[7];
    assert_eq!(head_field(v40_result, "headOrder"), "40");
    assert_eq!(head_field(v40_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v40_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v40_result, "addedVertices"), "-");
    assert_eq!(head_field(v40_result, "addedEdges"), "-");
    assert_eq!(head_field(v40_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v40_result, "linkerChanges"),
        "[h:5:LEFT:BOTTOM:true:false->true:true,h:5:LEFT:TOP:true:false->true:true,h:5:RIGHT:BOTTOM:false:false->false:true,h:5:RIGHT:TOP:false:false->false:true,linker:SLinker:head:5:false:false->false:true,linker:SLinker:head:5:true:false->true:true]"
    );
    assert_eq!(
        head_field(v40_result, "linkerStateHashAfter"),
        "9ecc46bf58d4583b1a9eb4cc669b234b9bb8a1ec3105a469e922897eab8ef2d6"
    );
    let v40_java = v40_rows[8];
    assert_eq!(head_field(v40_java, "headOrder"), "40");
    assert_eq!(head_field(v40_java, "headX"), "6");
    assert_eq!(head_field(v40_java, "headSig"), "89");
    assert_eq!(head_field(v40_java, "headInterId"), "1467");
    assert_eq!(
        head_field(v40_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v40_java, "incident"),
        "[stem2348:headSideLEFT:heads[x5:sig88:id1465:sideLEFT,x6:sig89:id1467:sideLEFT]]"
    );
    assert_eq!(
        head_field(v40_java, "closureWrites"),
        "[x5:sig88:LEFT:false->true,x5:sig88:RIGHT:false->true]"
    );
    assert_eq!(head_field(v40_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v40_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v40_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v40_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v40_java, "linkerStateHashAfter"),
        "9ecc46bf58d4583b1a9eb4cc669b234b9bb8a1ec3105a469e922897eab8ef2d6"
    );
    assert_eq!(head_field(v40_java, "nextHeadOrder"), "41");
    assert_eq!(head_field(v40_java, "nextHeadX"), "30");
    assert_eq!(head_field(v40_java, "nextHeadSig"), "67");
    assert_eq!(head_field(v40_java, "nextHeadInterId"), "1423");
    assert_eq!(head_field(v40_java, "terminal"), "ReturnedBeforeNextHead");
    let v40_summary = v40_rows[9];
    assert_eq!(
        head_field(v40_summary, "schema"),
        "stems-head-phase-prefix-v40"
    );
    assert_eq!(head_field(v40_summary, "rows"), "9");
    assert_eq!(
        head_field(v40_summary, "baseV39RunnerSourceSha256"),
        "bf7855c0d53d59cea3593de72f51f7272168f488e65148267ebd55e9f70110c7"
    );
    assert_eq!(
        head_field(v40_summary, "baseV39FixtureSha256"),
        "771b7816918d098e66fa1c599df1a68bfb3e24d1724ea6f701ba3bcc59b031fa"
    );
    assert_eq!(
        head_field(v40_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v40_summary, "probeSourceSha256"),
        "36408206fc9d1f7640b1464ff9a95be6039ce77e21485891f0f889dd0cf52f84"
    );
    assert_eq!(
        head_field(v40_summary, "runnerSourceSha256"),
        "7caaaf046770aafb327359fc587ed54509a83ec867a90a8c53cd254b2de5cb45"
    );
    assert_eq!(
        head_field(v40_summary, "emittedBodySha256"),
        "9be2634c8582ff4f023e17313aa9b91524b542d07c3c69363906b1d1e05acaa6"
    );
    assert_eq!(
        head_field(v40_summary, "semanticPassSha256"),
        "fa014228f89fbba214adaa1525ae8206de28f919ac71b334e2da01587f399db8"
    );
    assert_eq!(head_field(v40_summary, "freshRuns"), "2");
    assert_eq!(head_field(v40_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v40_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder40ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v40_summary, "javaEvidence"),
        "ReturnedBeforeFortyFirstHead"
    );

    // Boundary 67 expected-only order41 existing-stem reconciliation.
    let v41_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v41.txt"),
    )
    .expect("expected-only order41 existing-stem fixture");
    assert_eq!(
        sha256_hex(v41_text.as_bytes()),
        "7bb4ebb479617804363078144c55570d1c76229de551492c7cb14050641f1962"
    );
    let v41_rows = v41_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v41_rows.len(), 10);
    let v41_body = format!("{}\n", v41_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v41_body.as_bytes()),
        "44c4abe29f383cb0dd40f1e5777731d8384a1f97500f5f7d050d205cc48adf28"
    );
    let v41_frontier = v41_rows[6];
    assert_eq!(head_field(v41_frontier, "headOrder"), "41");
    assert_eq!(head_field(v41_frontier, "headX"), "30");
    assert_eq!(head_field(v41_frontier, "headSig"), "67");
    assert_eq!(head_field(v41_frontier, "headInterId"), "1423");
    assert_eq!(head_field(v41_frontier, "cAlias"), "h:30:LEFT:BOTTOM");
    assert_eq!(head_field(v41_frontier, "lastIndex"), "4");
    assert_eq!(head_field(v41_frontier, "maxIndex"), "4");
    assert_eq!(head_field(v41_frontier, "relations"), "4");
    assert_eq!(head_field(v41_frontier, "glyphs"), "2");
    assert_eq!(head_field(v41_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v41_frontier, "existingGlyph"), "glyph:313");
    assert_eq!(head_field(v41_frontier, "existingActive"), "true");
    assert_eq!(head_field(v41_frontier, "existingStem"), "2357");
    let v41_result = v41_rows[7];
    assert_eq!(head_field(v41_result, "headOrder"), "41");
    assert_eq!(head_field(v41_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v41_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v41_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v41_result, "addedVertices"), "-");
    assert_eq!(head_field(v41_result, "addedEdges"), "-");
    assert_eq!(head_field(v41_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v41_result, "linkerChanges"),
        "[h:29:LEFT:BOTTOM:true:false->true:true,h:29:LEFT:TOP:true:false->true:true,h:29:RIGHT:BOTTOM:false:false->false:true,h:29:RIGHT:TOP:false:false->false:true,linker:SLinker:head:29:false:false->false:true,linker:SLinker:head:29:true:false->true:true]"
    );
    assert_eq!(
        head_field(v41_result, "linkerStateHashAfter"),
        "a60ec6a37ddfbe1288fedc069868da539acf3d00a4ec054b7d3f97d9a5e84530"
    );
    let v41_java = v41_rows[8];
    assert_eq!(head_field(v41_java, "headOrder"), "41");
    assert_eq!(head_field(v41_java, "headX"), "30");
    assert_eq!(head_field(v41_java, "headSig"), "67");
    assert_eq!(head_field(v41_java, "headInterId"), "1423");
    assert_eq!(
        head_field(v41_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v41_java, "incident"),
        "[stem2357:headSideLEFT:heads[x29:sig66:id1421:sideLEFT,x30:sig67:id1423:sideLEFT]]"
    );
    assert_eq!(
        head_field(v41_java, "closureWrites"),
        "[x29:sig66:LEFT:false->true,x29:sig66:RIGHT:false->true]"
    );
    assert_eq!(head_field(v41_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v41_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v41_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v41_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v41_java, "linkerStateHashAfter"),
        "a60ec6a37ddfbe1288fedc069868da539acf3d00a4ec054b7d3f97d9a5e84530"
    );
    assert_eq!(head_field(v41_java, "nextHeadOrder"), "42");
    assert_eq!(head_field(v41_java, "nextHeadX"), "43");
    assert_eq!(head_field(v41_java, "nextHeadSig"), "48");
    assert_eq!(head_field(v41_java, "nextHeadInterId"), "1385");
    assert_eq!(head_field(v41_java, "terminal"), "ReturnedBeforeNextHead");
    let v41_summary = v41_rows[9];
    assert_eq!(
        head_field(v41_summary, "schema"),
        "stems-head-phase-prefix-v41"
    );
    assert_eq!(head_field(v41_summary, "rows"), "9");
    assert_eq!(
        head_field(v41_summary, "baseV40RunnerSourceSha256"),
        "7caaaf046770aafb327359fc587ed54509a83ec867a90a8c53cd254b2de5cb45"
    );
    assert_eq!(
        head_field(v41_summary, "baseV40FixtureSha256"),
        "26e4a2ecbd547829c573c4c7737331e4773f6faf64581ecfdf380a6b87283fa9"
    );
    assert_eq!(
        head_field(v41_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v41_summary, "probeSourceSha256"),
        "0af9969baa054555c868a1f98c15010301280c31367d6b57985f7c6ce97a22b1"
    );
    assert_eq!(
        head_field(v41_summary, "runnerSourceSha256"),
        "62be1da6161918739869d9aff57dd324b2145e6bfd6a96eb16fa8c64660c6a12"
    );
    assert_eq!(
        head_field(v41_summary, "emittedBodySha256"),
        "44c4abe29f383cb0dd40f1e5777731d8384a1f97500f5f7d050d205cc48adf28"
    );
    assert_eq!(
        head_field(v41_summary, "semanticPassSha256"),
        "5165580a8c154740a61992c286ecd74fedf663a8cd177b0607c832033fca5827"
    );
    assert_eq!(head_field(v41_summary, "freshRuns"), "2");
    assert_eq!(head_field(v41_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v41_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder41ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v41_summary, "javaEvidence"),
        "ReturnedBeforeFortySecondHead"
    );

    // Boundary 68 expected-only order42 existing-stem reconciliation.
    let v42_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v42.txt"),
    )
    .expect("expected-only order42 existing-stem fixture");
    assert_eq!(
        sha256_hex(v42_text.as_bytes()),
        "64b55e449e38f7af6ed47c1ca026236772a277ac8c5917bc5eaea397125b332c"
    );
    let v42_rows = v42_text
        .lines()
        .filter(|line| !line.starts_with('#'))
        .collect::<Vec<_>>();
    assert_eq!(v42_rows.len(), 10);
    let v42_body = format!("{}\n", v42_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v42_body.as_bytes()),
        "75faf38ec5983a45709de96fc20d02bed6fb03a8e77539aa6fbf067e78c9b612"
    );
    let v42_frontier = v42_rows[6];
    assert_eq!(head_field(v42_frontier, "headOrder"), "42");
    assert_eq!(head_field(v42_frontier, "headX"), "43");
    assert_eq!(head_field(v42_frontier, "headSig"), "48");
    assert_eq!(head_field(v42_frontier, "headInterId"), "1385");
    assert_eq!(head_field(v42_frontier, "cAlias"), "h:43:LEFT:BOTTOM");
    assert_eq!(head_field(v42_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v42_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v42_frontier, "relations"), "2");
    assert_eq!(head_field(v42_frontier, "glyphs"), "2");
    assert_eq!(head_field(v42_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v42_frontier, "existingGlyph"), "glyph:326");
    assert_eq!(head_field(v42_frontier, "existingActive"), "true");
    assert_eq!(head_field(v42_frontier, "existingStem"), "2350");
    let v42_result = v42_rows[7];
    assert_eq!(head_field(v42_result, "headOrder"), "42");
    assert_eq!(head_field(v42_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v42_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v42_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v42_result, "addedVertices"), "-");
    assert_eq!(head_field(v42_result, "addedEdges"), "-");
    assert_eq!(head_field(v42_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v42_result, "linkerChanges"),
        "[h:39:LEFT:BOTTOM:true:false->true:true,h:39:LEFT:TOP:true:false->true:true,h:39:RIGHT:BOTTOM:false:false->false:true,h:39:RIGHT:TOP:false:false->false:true,h:40:LEFT:BOTTOM:true:false->true:true,h:40:LEFT:TOP:true:false->true:true,h:40:RIGHT:BOTTOM:false:false->false:true,h:40:RIGHT:TOP:false:false->false:true,linker:SLinker:head:39:false:false->false:true,linker:SLinker:head:39:true:false->true:true,linker:SLinker:head:40:false:false->false:true,linker:SLinker:head:40:true:false->true:true]"
    );
    assert_eq!(
        head_field(v42_result, "linkerStateHashAfter"),
        "f19d103c1b1cf3f359760c7a949cc78fbbda24c2894293a93d20d60daf45879c"
    );
    let v42_java = v42_rows[8];
    assert_eq!(head_field(v42_java, "headOrder"), "42");
    assert_eq!(head_field(v42_java, "headX"), "43");
    assert_eq!(head_field(v42_java, "headSig"), "48");
    assert_eq!(head_field(v42_java, "headInterId"), "1385");
    assert_eq!(
        head_field(v42_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v42_java, "incident"),
        "[stem2350:headSideLEFT:heads[x39:sig37:id1359:sideLEFT,x40:sig27:id1339:sideLEFT,x43:sig48:id1385:sideLEFT]]"
    );
    assert_eq!(
        head_field(v42_java, "closureWrites"),
        "[x39:sig37:LEFT:false->true,x39:sig37:RIGHT:false->true,x40:sig27:LEFT:false->true,x40:sig27:RIGHT:false->true]"
    );
    assert_eq!(head_field(v42_java, "closedValueChanges"), "4");
    assert_eq!(head_field(v42_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v42_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v42_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v42_java, "linkerStateHashAfter"),
        "f19d103c1b1cf3f359760c7a949cc78fbbda24c2894293a93d20d60daf45879c"
    );
    assert_eq!(head_field(v42_java, "nextHeadOrder"), "43");
    assert_eq!(head_field(v42_java, "nextHeadX"), "25");
    assert_eq!(head_field(v42_java, "nextHeadSig"), "91");
    assert_eq!(head_field(v42_java, "nextHeadInterId"), "1471");
    assert_eq!(head_field(v42_java, "terminal"), "ReturnedBeforeNextHead");
    let v42_summary = v42_rows[9];
    assert_eq!(
        head_field(v42_summary, "schema"),
        "stems-head-phase-prefix-v42"
    );
    assert_eq!(head_field(v42_summary, "rows"), "9");
    assert_eq!(
        head_field(v42_summary, "baseV41RunnerSourceSha256"),
        "62be1da6161918739869d9aff57dd324b2145e6bfd6a96eb16fa8c64660c6a12"
    );
    assert_eq!(
        head_field(v42_summary, "baseV41FixtureSha256"),
        "7bb4ebb479617804363078144c55570d1c76229de551492c7cb14050641f1962"
    );
    assert_eq!(
        head_field(v42_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v42_summary, "probeSourceSha256"),
        "94231862206d686a7a0319ef2bcd6caca1516fbfd6e39a1281f24312d3c9ea04"
    );
    assert_eq!(
        head_field(v42_summary, "runnerSourceSha256"),
        "b3e7d0f4399584faa4a180b87dcc95259114cb4821dd0fcee404739a577c31c0"
    );
    assert_eq!(
        head_field(v42_summary, "emittedBodySha256"),
        "75faf38ec5983a45709de96fc20d02bed6fb03a8e77539aa6fbf067e78c9b612"
    );
    assert_eq!(
        head_field(v42_summary, "semanticPassSha256"),
        "4a8c708ec941174ed73d3fc12e5b8d71107a4b2628fb7755e14677a86bde83ae"
    );
    assert_eq!(head_field(v42_summary, "freshRuns"), "2");
    assert_eq!(head_field(v42_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v42_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder42ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v42_summary, "javaEvidence"),
        "ReturnedBeforeFortyThirdHead"
    );

    // Boundary 69 expected-only order43 existing-stem reconciliation.
    let v43_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v43.txt"),
    )
    .expect("expected-only order43 existing-stem fixture");
    assert_eq!(
        sha256_hex(v43_text.as_bytes()),
        "dc5f7ce12d292a13cc149e7df0249703323df92de9054daf5eff52783b32919d"
    );
    let v43_rows = v43_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v43_rows.len(), 10);
    let v43_body = format!("{}\n", v43_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v43_body.as_bytes()),
        "27ebf1a55921c288191423e18e6be2ed4f22c1a2d610b365a5809faa5606bbb3"
    );
    let v43_frontier = v43_rows[6];
    assert_eq!(head_field(v43_frontier, "headOrder"), "43");
    assert_eq!(head_field(v43_frontier, "headX"), "25");
    assert_eq!(head_field(v43_frontier, "headSig"), "91");
    assert_eq!(head_field(v43_frontier, "headInterId"), "1471");
    assert_eq!(head_field(v43_frontier, "cAlias"), "h:25:LEFT:BOTTOM");
    assert_eq!(head_field(v43_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v43_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v43_frontier, "relations"), "3");
    assert_eq!(head_field(v43_frontier, "glyphs"), "2");
    assert_eq!(head_field(v43_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v43_frontier, "existingGlyph"), "glyph:292");
    assert_eq!(head_field(v43_frontier, "existingActive"), "true");
    assert_eq!(head_field(v43_frontier, "existingStem"), "2356");
    let v43_result = v43_rows[7];
    assert_eq!(head_field(v43_result, "headOrder"), "43");
    assert_eq!(head_field(v43_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v43_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v43_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v43_result, "addedVertices"), "-");
    assert_eq!(head_field(v43_result, "addedEdges"), "-");
    assert_eq!(head_field(v43_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v43_result, "linkerChanges"),
        "[h:24:LEFT:BOTTOM:true:false->true:true,h:24:LEFT:TOP:true:false->true:true,h:24:RIGHT:BOTTOM:false:false->false:true,h:24:RIGHT:TOP:false:false->false:true,linker:SLinker:head:24:false:false->false:true,linker:SLinker:head:24:true:false->true:true]"
    );
    assert_eq!(
        head_field(v43_result, "linkerStateHashAfter"),
        "d2debb35c7af95e9ccf0f9ade011e572015cc3138065617a7eb7ff0871715c44"
    );
    let v43_java = v43_rows[8];
    assert_eq!(head_field(v43_java, "headOrder"), "43");
    assert_eq!(head_field(v43_java, "headX"), "25");
    assert_eq!(head_field(v43_java, "headSig"), "91");
    assert_eq!(head_field(v43_java, "headInterId"), "1471");
    assert_eq!(
        head_field(v43_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v43_java, "incident"),
        "[stem2356:headSideLEFT:heads[x24:sig90:id1469:sideLEFT,x25:sig91:id1471:sideLEFT]]"
    );
    assert_eq!(
        head_field(v43_java, "closureWrites"),
        "[x24:sig90:LEFT:false->true,x24:sig90:RIGHT:false->true]"
    );
    assert_eq!(head_field(v43_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v43_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v43_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v43_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v43_java, "linkerStateHashAfter"),
        "d2debb35c7af95e9ccf0f9ade011e572015cc3138065617a7eb7ff0871715c44"
    );
    assert_eq!(head_field(v43_java, "nextHeadOrder"), "44");
    assert_eq!(head_field(v43_java, "nextHeadX"), "83");
    assert_eq!(head_field(v43_java, "nextHeadSig"), "21");
    assert_eq!(head_field(v43_java, "nextHeadInterId"), "1327");
    assert_eq!(head_field(v43_java, "terminal"), "ReturnedBeforeNextHead");
    let v43_summary = v43_rows[9];
    assert_eq!(
        head_field(v43_summary, "schema"),
        "stems-head-phase-prefix-v43"
    );
    assert_eq!(head_field(v43_summary, "rows"), "9");
    assert_eq!(
        head_field(v43_summary, "baseV42RunnerSourceSha256"),
        "b3e7d0f4399584faa4a180b87dcc95259114cb4821dd0fcee404739a577c31c0"
    );
    assert_eq!(
        head_field(v43_summary, "baseV42FixtureSha256"),
        "64b55e449e38f7af6ed47c1ca026236772a277ac8c5917bc5eaea397125b332c"
    );
    assert_eq!(
        head_field(v43_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v43_summary, "probeSourceSha256"),
        "0f82e9cdee52ef1d8ac25870941d9437e4827c5ac0d6aca0af99934999fba250"
    );
    assert_eq!(
        head_field(v43_summary, "runnerSourceSha256"),
        "421c8cd9b3a9208b509ce511077c6656faeac230212deff7ab797f6ffec73d75"
    );
    assert_eq!(
        head_field(v43_summary, "emittedBodySha256"),
        "27ebf1a55921c288191423e18e6be2ed4f22c1a2d610b365a5809faa5606bbb3"
    );
    assert_eq!(
        head_field(v43_summary, "semanticPassSha256"),
        "45edd5fcc989fdc663f1d95ad379c07b165c058143d6c90c840b04d67dbf5bc3"
    );
    assert_eq!(head_field(v43_summary, "freshRuns"), "2");
    assert_eq!(head_field(v43_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v43_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder43ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v43_summary, "javaEvidence"),
        "ReturnedBeforeFortyFourthHead"
    );

    // Boundary 70 expected-only order44 existing-stem reconciliation.
    let v44_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v44.txt"),
    )
    .expect("expected-only order44 existing-stem fixture");
    assert_eq!(
        sha256_hex(v44_text.as_bytes()),
        "1d5c98477377e64e95a659fa04ed8d8331e02d5e87962811b790ff80f0315515"
    );
    let v44_rows = v44_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v44_rows.len(), 10);
    let v44_body = format!("{}\n", v44_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v44_body.as_bytes()),
        "9b702d865c4e09f400849a7296cf06d0c5750761bc20dc6fdb58a92cd9a3b8aa"
    );
    let v44_frontier = v44_rows[6];
    assert_eq!(head_field(v44_frontier, "headOrder"), "44");
    assert_eq!(head_field(v44_frontier, "headX"), "83");
    assert_eq!(head_field(v44_frontier, "headSig"), "21");
    assert_eq!(head_field(v44_frontier, "headInterId"), "1327");
    assert_eq!(head_field(v44_frontier, "cAlias"), "h:83:LEFT:BOTTOM");
    assert_eq!(head_field(v44_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v44_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v44_frontier, "relations"), "2");
    assert_eq!(head_field(v44_frontier, "glyphs"), "2");
    assert_eq!(head_field(v44_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v44_frontier, "existingGlyph"), "glyph:301");
    assert_eq!(head_field(v44_frontier, "existingActive"), "true");
    assert_eq!(head_field(v44_frontier, "existingStem"), "2358");
    let v44_result = v44_rows[7];
    assert_eq!(head_field(v44_result, "headOrder"), "44");
    assert_eq!(head_field(v44_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v44_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v44_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v44_result, "addedVertices"), "-");
    assert_eq!(head_field(v44_result, "addedEdges"), "-");
    assert_eq!(head_field(v44_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v44_result, "linkerChanges"),
        "[h:82:LEFT:BOTTOM:true:false->true:true,h:82:LEFT:TOP:true:false->true:true,h:82:RIGHT:BOTTOM:false:false->false:true,h:82:RIGHT:TOP:false:false->false:true,linker:SLinker:head:82:false:false->false:true,linker:SLinker:head:82:true:false->true:true]"
    );
    assert_eq!(
        head_field(v44_result, "linkerStateHashAfter"),
        "0f56de838dcde78d69afc1feefdb2953cee19424e672ece8be1c2261e8954608"
    );
    let v44_java = v44_rows[8];
    assert_eq!(head_field(v44_java, "headOrder"), "44");
    assert_eq!(head_field(v44_java, "headX"), "83");
    assert_eq!(head_field(v44_java, "headSig"), "21");
    assert_eq!(head_field(v44_java, "headInterId"), "1327");
    assert_eq!(
        head_field(v44_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v44_java, "incident"),
        "[stem2358:headSideLEFT:heads[x82:sig20:id1325:sideLEFT,x83:sig21:id1327:sideLEFT]]"
    );
    assert_eq!(
        head_field(v44_java, "closureWrites"),
        "[x82:sig20:LEFT:false->true,x82:sig20:RIGHT:false->true]"
    );
    assert_eq!(head_field(v44_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v44_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v44_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v44_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v44_java, "linkerStateHashAfter"),
        "0f56de838dcde78d69afc1feefdb2953cee19424e672ece8be1c2261e8954608"
    );
    assert_eq!(head_field(v44_java, "nextHeadOrder"), "45");
    assert_eq!(head_field(v44_java, "nextHeadX"), "57");
    assert_eq!(head_field(v44_java, "nextHeadSig"), "5");
    assert_eq!(head_field(v44_java, "nextHeadInterId"), "1295");
    assert_eq!(head_field(v44_java, "terminal"), "ReturnedBeforeNextHead");
    let v44_summary = v44_rows[9];
    assert_eq!(
        head_field(v44_summary, "schema"),
        "stems-head-phase-prefix-v44"
    );
    assert_eq!(head_field(v44_summary, "rows"), "9");
    assert_eq!(
        head_field(v44_summary, "baseV43RunnerSourceSha256"),
        "421c8cd9b3a9208b509ce511077c6656faeac230212deff7ab797f6ffec73d75"
    );
    assert_eq!(
        head_field(v44_summary, "baseV43FixtureSha256"),
        "dc5f7ce12d292a13cc149e7df0249703323df92de9054daf5eff52783b32919d"
    );
    assert_eq!(
        head_field(v44_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v44_summary, "probeSourceSha256"),
        "85bf12d76e49fdd036806441f505ac2e7db446d90fc0c3452c5c2c7a78997676"
    );
    assert_eq!(
        head_field(v44_summary, "runnerSourceSha256"),
        "ee0fb2771acf9693f47814e4abe1de1d7e6434a748178fad32ea823d5e3797d7"
    );
    assert_eq!(
        head_field(v44_summary, "emittedBodySha256"),
        "9b702d865c4e09f400849a7296cf06d0c5750761bc20dc6fdb58a92cd9a3b8aa"
    );
    assert_eq!(
        head_field(v44_summary, "semanticPassSha256"),
        "906b8a4f98dafe6e7d937144a251f153a75b37332852108e141b485050dcdf9a"
    );
    assert_eq!(head_field(v44_summary, "freshRuns"), "2");
    assert_eq!(head_field(v44_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v44_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder44ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v44_summary, "javaEvidence"),
        "ReturnedBeforeFortyFifthHead"
    );

    // Boundary 71 expected-only order45 existing-stem reconciliation.
    let v45_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v45.txt"),
    )
    .expect("expected-only order45 existing-stem fixture");
    assert_eq!(
        sha256_hex(v45_text.as_bytes()),
        "f70a5aeee405899ee2e9bf3be6957ffa657c6f0bcd5bc5d84ab0fc0288b19073"
    );
    let v45_rows = v45_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v45_rows.len(), 10);
    let v45_body = format!("{}\n", v45_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v45_body.as_bytes()),
        "3d9f7cd89ed218227e46f04d15a6a525680137f55845ca1c77640db16b4cca93"
    );
    let v45_frontier = v45_rows[6];
    assert_eq!(head_field(v45_frontier, "headOrder"), "45");
    assert_eq!(head_field(v45_frontier, "headX"), "57");
    assert_eq!(head_field(v45_frontier, "headSig"), "5");
    assert_eq!(head_field(v45_frontier, "headInterId"), "1295");
    assert_eq!(head_field(v45_frontier, "cAlias"), "h:57:LEFT:BOTTOM");
    assert_eq!(head_field(v45_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v45_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v45_frontier, "relations"), "2");
    assert_eq!(head_field(v45_frontier, "glyphs"), "2");
    assert_eq!(head_field(v45_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v45_frontier, "existingGlyph"), "glyph:303");
    assert_eq!(head_field(v45_frontier, "existingActive"), "true");
    assert_eq!(head_field(v45_frontier, "existingStem"), "2374");
    let v45_result = v45_rows[7];
    assert_eq!(head_field(v45_result, "headOrder"), "45");
    assert_eq!(head_field(v45_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v45_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v45_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v45_result, "addedVertices"), "-");
    assert_eq!(head_field(v45_result, "addedEdges"), "-");
    assert_eq!(head_field(v45_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v45_result, "linkerChanges"),
        "[h:56:LEFT:BOTTOM:true:false->true:true,h:56:LEFT:TOP:true:false->true:true,h:56:RIGHT:BOTTOM:false:false->false:true,h:56:RIGHT:TOP:false:false->false:true,linker:SLinker:head:56:false:false->false:true,linker:SLinker:head:56:true:false->true:true]"
    );
    assert_eq!(
        head_field(v45_result, "linkerStateHashAfter"),
        "e2fb4e0951a8d20786c180ef190a5bfec58982e5ca33f831063bc9467489e87e"
    );
    let v45_java = v45_rows[8];
    assert_eq!(head_field(v45_java, "headOrder"), "45");
    assert_eq!(head_field(v45_java, "headX"), "57");
    assert_eq!(head_field(v45_java, "headSig"), "5");
    assert_eq!(head_field(v45_java, "headInterId"), "1295");
    assert_eq!(
        head_field(v45_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(
        head_field(v45_java, "incident"),
        "[stem2374:headSideLEFT:heads[x56:sig15:id1315:sideLEFT,x57:sig5:id1295:sideLEFT]]"
    );
    assert_eq!(
        head_field(v45_java, "closureWrites"),
        "[x56:sig15:LEFT:false->true,x56:sig15:RIGHT:false->true]"
    );
    assert_eq!(head_field(v45_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v45_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v45_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v45_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v45_java, "linkerStateHashAfter"),
        "e2fb4e0951a8d20786c180ef190a5bfec58982e5ca33f831063bc9467489e87e"
    );
    assert_eq!(head_field(v45_java, "nextHeadOrder"), "46");
    assert_eq!(head_field(v45_java, "nextHeadX"), "40");
    assert_eq!(head_field(v45_java, "nextHeadSig"), "27");
    assert_eq!(head_field(v45_java, "nextHeadInterId"), "1339");
    assert_eq!(head_field(v45_java, "terminal"), "ReturnedBeforeNextHead");
    let v45_summary = v45_rows[9];
    assert_eq!(
        head_field(v45_summary, "schema"),
        "stems-head-phase-prefix-v45"
    );
    assert_eq!(head_field(v45_summary, "rows"), "9");
    assert_eq!(
        head_field(v45_summary, "baseV44RunnerSourceSha256"),
        "ee0fb2771acf9693f47814e4abe1de1d7e6434a748178fad32ea823d5e3797d7"
    );
    assert_eq!(
        head_field(v45_summary, "baseV44FixtureSha256"),
        "1d5c98477377e64e95a659fa04ed8d8331e02d5e87962811b790ff80f0315515"
    );
    assert_eq!(
        head_field(v45_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v45_summary, "probeSourceSha256"),
        "a00ee470231fa732748b5106eba841530f02502f78c13396caf5034de66326e6"
    );
    assert_eq!(
        head_field(v45_summary, "runnerSourceSha256"),
        "ee0b1141ee872ac784c60e43062c7f8ae98e26730ef596d3fb9c110c520de728"
    );
    assert_eq!(
        head_field(v45_summary, "emittedBodySha256"),
        "3d9f7cd89ed218227e46f04d15a6a525680137f55845ca1c77640db16b4cca93"
    );
    assert_eq!(
        head_field(v45_summary, "semanticPassSha256"),
        "6fb75ce2c4b10b2feaf8b98fd569eb035d7be817a448fdf4c5cf5239cf8eded8"
    );
    assert_eq!(head_field(v45_summary, "freshRuns"), "2");
    assert_eq!(head_field(v45_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v45_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder45ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v45_summary, "javaEvidence"),
        "ReturnedBeforeFortySixthHead"
    );

    // Boundary 72 expected-only order46 existing-stem reconciliation.
    let v46_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v46.txt"),
    )
    .expect("expected-only order46 existing-stem fixture");
    assert_eq!(
        sha256_hex(v46_text.as_bytes()),
        "017cfeddc3faeedda3aca5308c82251135bd0c3308854385f77271cb7fc76f8d"
    );
    let v46_rows = v46_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v46_rows.len(), 10);
    let v46_body = format!("{}\n", v46_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v46_body.as_bytes()),
        "89b8031f853144e07b8789ec9a0ba6d49aa9fd0dce12d2c7298e5036a0ecb4f2"
    );
    let v46_frontier = v46_rows[6];
    assert_eq!(head_field(v46_frontier, "headOrder"), "46");
    assert_eq!(head_field(v46_frontier, "headX"), "40");
    assert_eq!(head_field(v46_frontier, "headSig"), "27");
    assert_eq!(head_field(v46_frontier, "headInterId"), "1339");
    assert_eq!(head_field(v46_frontier, "cAlias"), "h:40:LEFT:BOTTOM");
    assert_eq!(head_field(v46_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v46_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v46_frontier, "relations"), "2");
    assert_eq!(head_field(v46_frontier, "glyphs"), "2");
    assert_eq!(head_field(v46_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v46_frontier, "existingGlyph"), "glyph:326");
    assert_eq!(head_field(v46_frontier, "existingActive"), "true");
    assert_eq!(head_field(v46_frontier, "existingStem"), "2350");
    let v46_result = v46_rows[7];
    assert_eq!(head_field(v46_result, "headOrder"), "46");
    assert_eq!(head_field(v46_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v46_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v46_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v46_result, "addedVertices"), "-");
    assert_eq!(head_field(v46_result, "addedEdges"), "-");
    assert_eq!(head_field(v46_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v46_result, "linkerChanges"),
        "[h:43:LEFT:BOTTOM:true:false->true:true,h:43:LEFT:TOP:true:false->true:true,h:43:RIGHT:BOTTOM:false:false->false:true,h:43:RIGHT:TOP:false:false->false:true,linker:SLinker:head:43:false:false->false:true,linker:SLinker:head:43:true:false->true:true]"
    );
    assert_eq!(
        head_field(v46_result, "linkerStateHashAfter"),
        "e943f52c6f78308f36bb913e332f56e23b4cd6ed2c7947d8c13d8cdf34985ba6"
    );
    let v46_java = v46_rows[8];
    assert_eq!(head_field(v46_java, "headOrder"), "46");
    assert_eq!(head_field(v46_java, "headX"), "40");
    assert_eq!(head_field(v46_java, "headSig"), "27");
    assert_eq!(head_field(v46_java, "headInterId"), "1339");
    assert_eq!(
        head_field(v46_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v46_java, "incident"),
        "[stem2350:headSideLEFT:heads[x39:sig37:id1359:sideLEFT,x40:sig27:id1339:sideLEFT,x43:sig48:id1385:sideLEFT]]"
    );
    assert_eq!(
        head_field(v46_java, "closureWrites"),
        "[x39:sig37:LEFT:true->true,x39:sig37:RIGHT:true->true,x43:sig48:LEFT:false->true,x43:sig48:RIGHT:false->true]"
    );
    assert_eq!(head_field(v46_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v46_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v46_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v46_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v46_java, "linkerStateHashAfter"),
        "e943f52c6f78308f36bb913e332f56e23b4cd6ed2c7947d8c13d8cdf34985ba6"
    );
    assert_eq!(head_field(v46_java, "nextHeadOrder"), "47");
    assert_eq!(head_field(v46_java, "nextHeadX"), "89");
    assert_eq!(head_field(v46_java, "nextHeadSig"), "22");
    assert_eq!(head_field(v46_java, "nextHeadInterId"), "1329");
    assert_eq!(head_field(v46_java, "terminal"), "ReturnedBeforeNextHead");
    let v46_summary = v46_rows[9];
    assert_eq!(
        head_field(v46_summary, "schema"),
        "stems-head-phase-prefix-v46"
    );
    assert_eq!(head_field(v46_summary, "rows"), "9");
    assert_eq!(
        head_field(v46_summary, "baseV45RunnerSourceSha256"),
        "ee0b1141ee872ac784c60e43062c7f8ae98e26730ef596d3fb9c110c520de728"
    );
    assert_eq!(
        head_field(v46_summary, "baseV45FixtureSha256"),
        "f70a5aeee405899ee2e9bf3be6957ffa657c6f0bcd5bc5d84ab0fc0288b19073"
    );
    assert_eq!(
        head_field(v46_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v46_summary, "probeSourceSha256"),
        "c9a1def226782df3853dcd8a9df987b7889006ac38041d1a2f8998e8a2105a69"
    );
    assert_eq!(
        head_field(v46_summary, "runnerSourceSha256"),
        "aaff9b381d5268c42f9688658071bcafd31736c7987c21a79726eb516483fa78"
    );
    assert_eq!(
        head_field(v46_summary, "emittedBodySha256"),
        "89b8031f853144e07b8789ec9a0ba6d49aa9fd0dce12d2c7298e5036a0ecb4f2"
    );
    assert_eq!(
        head_field(v46_summary, "semanticPassSha256"),
        "46281d8c0cedf474303443b88b1823e035ee556074595c3a0c4c506cda181db3"
    );
    assert_eq!(head_field(v46_summary, "freshRuns"), "2");
    assert_eq!(head_field(v46_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v46_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder46ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v46_summary, "javaEvidence"),
        "ReturnedBeforeFortySeventhHead"
    );

    // Boundary 73 expected-only order47 existing-stem reconciliation.
    let v47_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v47.txt"),
    )
    .expect("expected-only order47 existing-stem fixture");
    assert_eq!(
        sha256_hex(v47_text.as_bytes()),
        "5a7989434b78dbd6ea72f113cd9f66078ae8e9c3acabb8980ecdb7577120de39"
    );
    let v47_rows = v47_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v47_rows.len(), 10);
    let v47_body = format!("{}\n", v47_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v47_body.as_bytes()),
        "9b32571ee576c45644622c26be3c0966b8bee1260c6b4e7e27aa2e8d04686d73"
    );
    let v47_frontier = v47_rows[6];
    assert_eq!(head_field(v47_frontier, "headOrder"), "47");
    assert_eq!(head_field(v47_frontier, "headX"), "89");
    assert_eq!(head_field(v47_frontier, "headSig"), "22");
    assert_eq!(head_field(v47_frontier, "headInterId"), "1329");
    assert_eq!(head_field(v47_frontier, "cAlias"), "h:89:LEFT:BOTTOM");
    assert_eq!(head_field(v47_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v47_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v47_frontier, "relations"), "1");
    assert_eq!(head_field(v47_frontier, "glyphs"), "1");
    assert_eq!(head_field(v47_frontier, "candidateIdBefore"), "304");
    assert_eq!(head_field(v47_frontier, "existingGlyph"), "glyph:304");
    assert_eq!(head_field(v47_frontier, "existingActive"), "true");
    assert_eq!(head_field(v47_frontier, "existingStem"), "2359");
    let v47_result = v47_rows[7];
    assert_eq!(head_field(v47_result, "headOrder"), "47");
    assert_eq!(head_field(v47_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v47_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v47_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v47_result, "addedVertices"), "-");
    assert_eq!(head_field(v47_result, "addedEdges"), "-");
    assert_eq!(head_field(v47_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v47_result, "linkerChanges"),
        "[h:90:LEFT:BOTTOM:true:false->true:true,h:90:LEFT:TOP:true:false->true:true,h:90:RIGHT:BOTTOM:false:false->false:true,h:90:RIGHT:TOP:false:false->false:true,linker:SLinker:head:90:false:false->false:true,linker:SLinker:head:90:true:false->true:true]"
    );
    assert_eq!(
        head_field(v47_result, "linkerStateHashAfter"),
        "fa75c79faa8eda320560e4161be89f93b1f562405b68036275bb2b3d1eb23ed5"
    );
    let v47_java = v47_rows[8];
    assert_eq!(head_field(v47_java, "headOrder"), "47");
    assert_eq!(head_field(v47_java, "headX"), "89");
    assert_eq!(head_field(v47_java, "headSig"), "22");
    assert_eq!(head_field(v47_java, "headInterId"), "1329");
    assert_eq!(
        head_field(v47_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v47_java, "incident"),
        "[stem2359:headSideLEFT:heads[x89:sig22:id1329:sideLEFT,x90:sig23:id1331:sideLEFT]]"
    );
    assert_eq!(
        head_field(v47_java, "closureWrites"),
        "[x90:sig23:LEFT:false->true,x90:sig23:RIGHT:false->true]"
    );
    assert_eq!(head_field(v47_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v47_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v47_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v47_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v47_java, "linkerStateHashAfter"),
        "fa75c79faa8eda320560e4161be89f93b1f562405b68036275bb2b3d1eb23ed5"
    );
    assert_eq!(head_field(v47_java, "nextHeadOrder"), "48");
    assert_eq!(head_field(v47_java, "nextHeadX"), "52");
    assert_eq!(head_field(v47_java, "nextHeadSig"), "2");
    assert_eq!(head_field(v47_java, "nextHeadInterId"), "1289");
    assert_eq!(head_field(v47_java, "terminal"), "ReturnedBeforeNextHead");
    let v47_summary = v47_rows[9];
    assert_eq!(
        head_field(v47_summary, "schema"),
        "stems-head-phase-prefix-v47"
    );
    assert_eq!(head_field(v47_summary, "rows"), "9");
    assert_eq!(
        head_field(v47_summary, "baseV46RunnerSourceSha256"),
        "aaff9b381d5268c42f9688658071bcafd31736c7987c21a79726eb516483fa78"
    );
    assert_eq!(
        head_field(v47_summary, "baseV46FixtureSha256"),
        "017cfeddc3faeedda3aca5308c82251135bd0c3308854385f77271cb7fc76f8d"
    );
    assert_eq!(
        head_field(v47_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v47_summary, "probeSourceSha256"),
        "ecd26aba8d5c02fb695cf68ed9006d7f80e002e8287eba941f1dec3655b85a70"
    );
    assert_eq!(
        head_field(v47_summary, "runnerSourceSha256"),
        "7a9605cf09f1d78f899423a816c0c6adc2b121786f56c69c271b41da5527f6ab"
    );
    assert_eq!(
        head_field(v47_summary, "emittedBodySha256"),
        "9b32571ee576c45644622c26be3c0966b8bee1260c6b4e7e27aa2e8d04686d73"
    );
    assert_eq!(
        head_field(v47_summary, "semanticPassSha256"),
        "497efa9c299608de7fbecda7f48531baee96be1fd9cf79435c3ffc289a8aafc9"
    );
    assert_eq!(head_field(v47_summary, "freshRuns"), "2");
    assert_eq!(head_field(v47_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v47_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder47ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v47_summary, "javaEvidence"),
        "ReturnedBeforeFortyEighthHead"
    );

    // Boundary 74 expected-only order48 existing-stem reconciliation.
    let v48_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v48.txt"),
    )
    .expect("expected-only order48 existing-stem fixture");
    assert_eq!(
        sha256_hex(v48_text.as_bytes()),
        "acc3436794b0ea828dbd689adfd072b6844125007131ee4207d9d4402c90cd5d"
    );
    let v48_rows = v48_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v48_rows.len(), 10);
    let v48_body = format!("{}\n", v48_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v48_body.as_bytes()),
        "aa738347bf8581a87c5293e9b549261946b9adfef21c3e07e7d37ebdb21e2907"
    );
    let v48_frontier = v48_rows[6];
    assert_eq!(head_field(v48_frontier, "headOrder"), "48");
    assert_eq!(head_field(v48_frontier, "headX"), "52");
    assert_eq!(head_field(v48_frontier, "headSig"), "2");
    assert_eq!(head_field(v48_frontier, "headInterId"), "1289");
    assert_eq!(head_field(v48_frontier, "cAlias"), "h:52:LEFT:BOTTOM");
    assert_eq!(head_field(v48_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v48_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v48_frontier, "relations"), "4");
    assert_eq!(head_field(v48_frontier, "glyphs"), "1");
    assert_eq!(head_field(v48_frontier, "candidateIdBefore"), "296");
    assert_eq!(head_field(v48_frontier, "existingGlyph"), "glyph:296");
    assert_eq!(head_field(v48_frontier, "existingActive"), "true");
    assert_eq!(head_field(v48_frontier, "existingStem"), "2344");
    let v48_result = v48_rows[7];
    assert_eq!(head_field(v48_result, "headOrder"), "48");
    assert_eq!(head_field(v48_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v48_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v48_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v48_result, "addedVertices"), "-");
    assert_eq!(head_field(v48_result, "addedEdges"), "-");
    assert_eq!(head_field(v48_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v48_result, "linkerChanges"),
        "[h:53:LEFT:BOTTOM:true:false->true:true,h:53:LEFT:TOP:true:false->true:true,h:53:RIGHT:BOTTOM:false:false->false:true,h:53:RIGHT:TOP:false:false->false:true,linker:SLinker:head:53:false:false->false:true,linker:SLinker:head:53:true:false->true:true]"
    );
    assert_eq!(
        head_field(v48_result, "linkerStateHashAfter"),
        "f0521e6e2e043737ae33f4e1e13ea8b2a643a2f23f563c237e91879ef0428e3a"
    );
    let v48_java = v48_rows[8];
    assert_eq!(head_field(v48_java, "headOrder"), "48");
    assert_eq!(head_field(v48_java, "headX"), "52");
    assert_eq!(head_field(v48_java, "headSig"), "2");
    assert_eq!(head_field(v48_java, "headInterId"), "1289");
    assert_eq!(
        head_field(v48_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v48_java, "incident"),
        "[stem2344:headSideLEFT:heads[x52:sig2:id1289:sideLEFT,x53:sig3:id1291:sideLEFT]]"
    );
    assert_eq!(
        head_field(v48_java, "closureWrites"),
        "[x53:sig3:LEFT:false->true,x53:sig3:RIGHT:false->true]"
    );
    assert_eq!(head_field(v48_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v48_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v48_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v48_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v48_java, "linkerStateHashAfter"),
        "f0521e6e2e043737ae33f4e1e13ea8b2a643a2f23f563c237e91879ef0428e3a"
    );
    assert_eq!(head_field(v48_java, "nextHeadOrder"), "49");
    assert_eq!(head_field(v48_java, "nextHeadX"), "35");
    assert_eq!(head_field(v48_java, "nextHeadSig"), "68");
    assert_eq!(head_field(v48_java, "nextHeadInterId"), "1425");
    assert_eq!(head_field(v48_java, "terminal"), "ReturnedBeforeNextHead");
    let v48_summary = v48_rows[9];
    assert_eq!(
        head_field(v48_summary, "schema"),
        "stems-head-phase-prefix-v48"
    );
    assert_eq!(head_field(v48_summary, "rows"), "9");
    assert_eq!(
        head_field(v48_summary, "baseV47RunnerSourceSha256"),
        "7a9605cf09f1d78f899423a816c0c6adc2b121786f56c69c271b41da5527f6ab"
    );
    assert_eq!(
        head_field(v48_summary, "baseV47FixtureSha256"),
        "5a7989434b78dbd6ea72f113cd9f66078ae8e9c3acabb8980ecdb7577120de39"
    );
    assert_eq!(
        head_field(v48_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v48_summary, "probeSourceSha256"),
        "af7f62ae73911530d863cbf8e4f2ee8bb3d019cfb556185e5fca334cad8a318d"
    );
    assert_eq!(
        head_field(v48_summary, "runnerSourceSha256"),
        "925536d8d119102e5a74a3690b2286bde856bd476151243806d68a049aa40fdb"
    );
    assert_eq!(
        head_field(v48_summary, "emittedBodySha256"),
        "aa738347bf8581a87c5293e9b549261946b9adfef21c3e07e7d37ebdb21e2907"
    );
    assert_eq!(
        head_field(v48_summary, "semanticPassSha256"),
        "1183c4dce1c645a0ee070f1bd12d8796b22d9f0bde91c9421c3bc75db833a80f"
    );
    assert_eq!(head_field(v48_summary, "freshRuns"), "2");
    assert_eq!(head_field(v48_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v48_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder48ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v48_summary, "javaEvidence"),
        "ReturnedBeforeFortyNinthHead"
    );

    // Boundary 75 expected-only order49 existing-stem reconciliation.
    let v49_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v49.txt"),
    )
    .expect("expected-only order49 existing-stem fixture");
    assert_eq!(
        sha256_hex(v49_text.as_bytes()),
        "ef4bfca2696caba71b227fb15d36b85adfe34ca519e7d87419d86f2605664147"
    );
    let v49_rows = v49_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v49_rows.len(), 10);
    let v49_body = format!("{}\n", v49_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v49_body.as_bytes()),
        "d79d89bb20f662409759dfb08279d7735dd4050b53926e28f286c2e8319120e9"
    );
    let v49_frontier = v49_rows[6];
    assert_eq!(head_field(v49_frontier, "headOrder"), "49");
    assert_eq!(head_field(v49_frontier, "headX"), "35");
    assert_eq!(head_field(v49_frontier, "headSig"), "68");
    assert_eq!(head_field(v49_frontier, "headInterId"), "1425");
    assert_eq!(head_field(v49_frontier, "cAlias"), "h:35:LEFT:BOTTOM");
    assert_eq!(head_field(v49_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v49_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v49_frontier, "relations"), "1");
    assert_eq!(head_field(v49_frontier, "glyphs"), "1");
    assert_eq!(head_field(v49_frontier, "candidateIdBefore"), "316");
    assert_eq!(head_field(v49_frontier, "existingGlyph"), "glyph:316");
    assert_eq!(head_field(v49_frontier, "existingActive"), "true");
    assert_eq!(head_field(v49_frontier, "existingStem"), "2369");
    let v49_result = v49_rows[7];
    assert_eq!(head_field(v49_result, "headOrder"), "49");
    assert_eq!(head_field(v49_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v49_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v49_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v49_result, "addedVertices"), "-");
    assert_eq!(head_field(v49_result, "addedEdges"), "-");
    assert_eq!(head_field(v49_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v49_result, "linkerChanges"),
        "[h:36:LEFT:BOTTOM:true:false->true:true,h:36:LEFT:TOP:true:false->true:true,h:36:RIGHT:BOTTOM:false:false->false:true,h:36:RIGHT:TOP:false:false->false:true,linker:SLinker:head:36:false:false->false:true,linker:SLinker:head:36:true:false->true:true]"
    );
    assert_eq!(
        head_field(v49_result, "linkerStateHashAfter"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    let v49_java = v49_rows[8];
    assert_eq!(head_field(v49_java, "headOrder"), "49");
    assert_eq!(head_field(v49_java, "headX"), "35");
    assert_eq!(head_field(v49_java, "headSig"), "68");
    assert_eq!(head_field(v49_java, "headInterId"), "1425");
    assert_eq!(
        head_field(v49_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v49_java, "incident"),
        "[stem2369:headSideLEFT:heads[x35:sig68:id1425:sideLEFT,x36:sig69:id1427:sideLEFT]]"
    );
    assert_eq!(
        head_field(v49_java, "closureWrites"),
        "[x36:sig69:LEFT:false->true,x36:sig69:RIGHT:false->true]"
    );
    assert_eq!(head_field(v49_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v49_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v49_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v49_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v49_java, "linkerStateHashAfter"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    assert_eq!(head_field(v49_java, "nextHeadOrder"), "50");
    assert_eq!(head_field(v49_java, "nextHeadX"), "32");
    assert_eq!(head_field(v49_java, "nextHeadSig"), "50");
    assert_eq!(head_field(v49_java, "nextHeadInterId"), "1389");
    assert_eq!(head_field(v49_java, "terminal"), "ReturnedBeforeNextHead");
    let v49_summary = v49_rows[9];
    assert_eq!(
        head_field(v49_summary, "schema"),
        "stems-head-phase-prefix-v49"
    );
    assert_eq!(head_field(v49_summary, "rows"), "9");
    assert_eq!(
        head_field(v49_summary, "baseV48RunnerSourceSha256"),
        "925536d8d119102e5a74a3690b2286bde856bd476151243806d68a049aa40fdb"
    );
    assert_eq!(
        head_field(v49_summary, "baseV48FixtureSha256"),
        "acc3436794b0ea828dbd689adfd072b6844125007131ee4207d9d4402c90cd5d"
    );
    assert_eq!(
        head_field(v49_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v49_summary, "probeSourceSha256"),
        "1848e0a3c06790d58816837b5c60f871be05125037a720e4a228efa42731b5dd"
    );
    assert_eq!(
        head_field(v49_summary, "runnerSourceSha256"),
        "da0ed259bc25e03b3624b96a5078661497fb713f08ca183dd3624bd16e74f406"
    );
    assert_eq!(
        head_field(v49_summary, "emittedBodySha256"),
        "d79d89bb20f662409759dfb08279d7735dd4050b53926e28f286c2e8319120e9"
    );
    assert_eq!(
        head_field(v49_summary, "semanticPassSha256"),
        "734556edff9e5a81b65fb66ad4bc23e6c66f1bbf152eadd130d2a9acaf61f6ad"
    );
    assert_eq!(head_field(v49_summary, "freshRuns"), "2");
    assert_eq!(head_field(v49_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v49_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder49ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v49_summary, "javaEvidence"),
        "ReturnedBeforeFiftiethHead"
    );

    // Boundary 76 expected-only order50 open/undefined frontier.
    let v50_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v50.txt"),
    )
    .expect("expected-only order50 open frontier fixture");
    assert_eq!(
        sha256_hex(v50_text.as_bytes()),
        "00021c6593a39641e87864cb950a06651a797a76899686c23f9faa009e0d275d"
    );
    let v50_rows = v50_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v50_rows.len(), 10);
    let v50_body = format!("{}\n", v50_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v50_body.as_bytes()),
        "776e842e93ea29c4779edc08efa9ab7772741a203a6524fef59307d1021137fe"
    );
    let v50_frontier = v50_rows[6];
    assert_eq!(head_field(v50_frontier, "headOrder"), "50");
    assert_eq!(head_field(v50_frontier, "headX"), "32");
    assert_eq!(head_field(v50_frontier, "headSig"), "50");
    assert_eq!(head_field(v50_frontier, "headInterId"), "1389");
    assert_eq!(head_field(v50_frontier, "cAlias"), "h:32:LEFT:BOTTOM");
    assert_eq!(head_field(v50_frontier, "lastIndex"), "-1");
    assert_eq!(head_field(v50_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v50_frontier, "relations"), "1");
    assert_eq!(head_field(v50_frontier, "glyphs"), "2");
    assert_eq!(head_field(v50_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v50_frontier, "existingGlyph"), "glyph:314");
    assert_eq!(head_field(v50_frontier, "existingActive"), "true");
    assert_eq!(head_field(v50_frontier, "existingStem"), "2383");
    let v50_result = v50_rows[7];
    assert_eq!(head_field(v50_result, "headOrder"), "50");
    assert_eq!(head_field(v50_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v50_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v50_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v50_result, "addedVertices"), "-");
    assert_eq!(head_field(v50_result, "addedEdges"), "-");
    assert_eq!(head_field(v50_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v50_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v50_result, "linkerStateHashBefore"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    assert_eq!(
        head_field(v50_result, "linkerStateHashAfter"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    let v50_java = v50_rows[8];
    assert_eq!(head_field(v50_java, "headOrder"), "50");
    assert_eq!(head_field(v50_java, "headX"), "32");
    assert_eq!(head_field(v50_java, "headSig"), "50");
    assert_eq!(head_field(v50_java, "headInterId"), "1389");
    assert_eq!(
        head_field(v50_java, "decisions"),
        "[LEFT:top=true:bottom=true:branch=Both,RIGHT:top=true:bottom=false:branch=TopOnly]"
    );
    assert_eq!(head_field(v50_java, "incident"), "-");
    assert_eq!(head_field(v50_java, "returned"), "false");
    assert_eq!(head_field(v50_java, "undefs"), "[LEFT]");
    assert_eq!(head_field(v50_java, "closureWrites"), "-");
    assert_eq!(head_field(v50_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v50_java, "sigVerticesBefore"), "685");
    assert_eq!(head_field(v50_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v50_java, "sigEdgesBefore"), "696");
    assert_eq!(head_field(v50_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v50_java, "systemStemsBefore"), "46");
    assert_eq!(head_field(v50_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v50_java, "linkerStateHashBefore"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    assert_eq!(
        head_field(v50_java, "linkerStateHashAfter"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    assert_eq!(head_field(v50_java, "nextHeadOrder"), "51");
    assert_eq!(head_field(v50_java, "nextHeadX"), "19");
    assert_eq!(head_field(v50_java, "nextHeadSig"), "64");
    assert_eq!(head_field(v50_java, "nextHeadInterId"), "1417");
    assert_eq!(head_field(v50_java, "terminal"), "ReturnedBeforeNextHead");
    let v50_summary = v50_rows[9];
    assert_eq!(
        head_field(v50_summary, "schema"),
        "stems-head-phase-prefix-v50"
    );
    assert_eq!(head_field(v50_summary, "rows"), "9");
    assert_eq!(
        head_field(v50_summary, "baseV49RunnerSourceSha256"),
        "da0ed259bc25e03b3624b96a5078661497fb713f08ca183dd3624bd16e74f406"
    );
    assert_eq!(
        head_field(v50_summary, "baseV49FixtureSha256"),
        "ef4bfca2696caba71b227fb15d36b85adfe34ca519e7d87419d86f2605664147"
    );
    assert_eq!(
        head_field(v50_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v50_summary, "probeSourceSha256"),
        "2a97dd755fa5b2b48d76dd914b24ddcf66e1db0597d124630d49cb56edefde99"
    );
    assert_eq!(
        head_field(v50_summary, "runnerSourceSha256"),
        "edff8b0e4715bb198bc70f2cdaac9320d8940f66a417ff9c7cad532cc96a5910"
    );
    assert_eq!(
        head_field(v50_summary, "emittedBodySha256"),
        "776e842e93ea29c4779edc08efa9ab7772741a203a6524fef59307d1021137fe"
    );
    assert_eq!(
        head_field(v50_summary, "semanticPassSha256"),
        "5b134ffb2ec0756647be00a38de6dcb1efabafb9a000872dfeedd8d30f1b7c7c"
    );
    assert_eq!(head_field(v50_summary, "freshRuns"), "2");
    assert_eq!(head_field(v50_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v50_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder50OpenFrontier"
    );
    assert_eq!(
        head_field(v50_summary, "javaEvidence"),
        "ReturnedBeforeFiftyFirstHead"
    );

    // Boundary 77 expected-only order51 existing-stem reconciliation.
    let v51_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v51.txt"),
    )
    .expect("expected-only order51 existing-stem fixture");
    assert_eq!(
        sha256_hex(v51_text.as_bytes()),
        "778c06a6697f96a439146e06d85d9d182d1f7659ef19a15fd9204aef3455546e"
    );
    let v51_rows = v51_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v51_rows.len(), 10);
    let v51_body = format!("{}\n", v51_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v51_body.as_bytes()),
        "ddd236253a96af0b8932e6a873b9d5fe4086c16db1adc86da733ba34ae2dd93a"
    );
    let v51_frontier = v51_rows[6];
    assert_eq!(head_field(v51_frontier, "headOrder"), "51");
    assert_eq!(head_field(v51_frontier, "headX"), "19");
    assert_eq!(head_field(v51_frontier, "headSig"), "64");
    assert_eq!(head_field(v51_frontier, "headInterId"), "1417");
    assert_eq!(head_field(v51_frontier, "cAlias"), "h:19:LEFT:BOTTOM");
    assert_eq!(head_field(v51_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v51_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v51_frontier, "relations"), "1");
    assert_eq!(head_field(v51_frontier, "glyphs"), "2");
    assert_eq!(head_field(v51_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v51_frontier, "existingGlyph"), "glyph:299");
    assert_eq!(head_field(v51_frontier, "existingActive"), "true");
    assert_eq!(head_field(v51_frontier, "existingStem"), "2361");
    let v51_result = v51_rows[7];
    assert_eq!(head_field(v51_result, "headOrder"), "51");
    assert_eq!(head_field(v51_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v51_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v51_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v51_result, "addedVertices"), "-");
    assert_eq!(head_field(v51_result, "addedEdges"), "-");
    assert_eq!(head_field(v51_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v51_result, "linkerChanges"),
        "[h:20:LEFT:BOTTOM:true:false->true:true,h:20:LEFT:TOP:true:false->true:true,h:20:RIGHT:BOTTOM:false:false->false:true,h:20:RIGHT:TOP:false:false->false:true,linker:SLinker:head:20:false:false->false:true,linker:SLinker:head:20:true:false->true:true]"
    );
    assert_eq!(
        head_field(v51_result, "linkerStateHashBefore"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    assert_eq!(
        head_field(v51_result, "linkerStateHashAfter"),
        "506a61f88b08511f46cc8dfca24caa60d4e6b40792f3e0d0942940193e55e4b3"
    );
    let v51_java = v51_rows[8];
    assert_eq!(head_field(v51_java, "headOrder"), "51");
    assert_eq!(head_field(v51_java, "headX"), "19");
    assert_eq!(head_field(v51_java, "headSig"), "64");
    assert_eq!(head_field(v51_java, "headInterId"), "1417");
    assert_eq!(
        head_field(v51_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v51_java, "incident"),
        "[stem2361:headSideLEFT:heads[x19:sig64:id1417:sideLEFT,x20:sig65:id1419:sideLEFT]]"
    );
    assert_eq!(head_field(v51_java, "returned"), "true");
    assert_eq!(head_field(v51_java, "undefs"), "[]");
    assert_eq!(
        head_field(v51_java, "closureWrites"),
        "[x20:sig65:LEFT:false->true,x20:sig65:RIGHT:false->true]"
    );
    assert_eq!(head_field(v51_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v51_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v51_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v51_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v51_java, "linkerStateHashBefore"),
        "3a68887edccfbdb8d58b652e129639ba20b920d647e7d2c4226274ef6e3ba0bf"
    );
    assert_eq!(
        head_field(v51_java, "linkerStateHashAfter"),
        "506a61f88b08511f46cc8dfca24caa60d4e6b40792f3e0d0942940193e55e4b3"
    );
    assert_eq!(head_field(v51_java, "nextHeadOrder"), "52");
    assert_eq!(head_field(v51_java, "nextHeadX"), "15");
    assert_eq!(head_field(v51_java, "nextHeadSig"), "80");
    assert_eq!(head_field(v51_java, "nextHeadInterId"), "1449");
    assert_eq!(head_field(v51_java, "terminal"), "ReturnedBeforeNextHead");
    let v51_summary = v51_rows[9];
    assert_eq!(
        head_field(v51_summary, "schema"),
        "stems-head-phase-prefix-v51"
    );
    assert_eq!(head_field(v51_summary, "rows"), "9");
    assert_eq!(
        head_field(v51_summary, "baseV50RunnerSourceSha256"),
        "edff8b0e4715bb198bc70f2cdaac9320d8940f66a417ff9c7cad532cc96a5910"
    );
    assert_eq!(
        head_field(v51_summary, "baseV50FixtureSha256"),
        "00021c6593a39641e87864cb950a06651a797a76899686c23f9faa009e0d275d"
    );
    assert_eq!(
        head_field(v51_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v51_summary, "probeSourceSha256"),
        "537f15c3d73d79eea77e08b9a72b89fa0a4b54bf2c6c61e951371ea161e42c2e"
    );
    assert_eq!(
        head_field(v51_summary, "runnerSourceSha256"),
        "55f522ebc3bff281f113715e86137e5feac2d74f513907b8361903cdf3b1a828"
    );
    assert_eq!(
        head_field(v51_summary, "emittedBodySha256"),
        "ddd236253a96af0b8932e6a873b9d5fe4086c16db1adc86da733ba34ae2dd93a"
    );
    assert_eq!(
        head_field(v51_summary, "semanticPassSha256"),
        "1a1662346825b66eb64cf50c7ef8ab0699c1ae776321dc0514214c590eec8c4c"
    );
    assert_eq!(head_field(v51_summary, "freshRuns"), "2");
    assert_eq!(head_field(v51_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v51_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder51ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v51_summary, "javaEvidence"),
        "ReturnedBeforeFiftySecondHead"
    );

    // Boundary 78 expected-only order52 existing-stem reconciliation.
    let v52_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v52.txt"),
    )
    .expect("expected-only order52 existing-stem fixture");
    assert_eq!(
        sha256_hex(v52_text.as_bytes()),
        "ff3bbe3fdf9ba0e6140b8105ab46e4c65972d4f4f013d39f90eade2270b64224"
    );
    let v52_rows = v52_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v52_rows.len(), 10);
    let v52_body = format!("{}\n", v52_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v52_body.as_bytes()),
        "bc0b579b830bc4c1ccf057f85efa02b6226f25ade19d62aca28344ff9f0c35da"
    );
    let v52_frontier = v52_rows[6];
    assert_eq!(head_field(v52_frontier, "headOrder"), "52");
    assert_eq!(head_field(v52_frontier, "headX"), "15");
    assert_eq!(head_field(v52_frontier, "headSig"), "80");
    assert_eq!(head_field(v52_frontier, "headInterId"), "1449");
    assert_eq!(head_field(v52_frontier, "cAlias"), "h:15:LEFT:BOTTOM");
    assert_eq!(head_field(v52_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v52_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v52_frontier, "relations"), "2");
    assert_eq!(head_field(v52_frontier, "glyphs"), "2");
    assert_eq!(head_field(v52_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v52_frontier, "existingGlyph"), "glyph:329");
    assert_eq!(head_field(v52_frontier, "existingActive"), "true");
    assert_eq!(head_field(v52_frontier, "existingStem"), "2360");
    let v52_result = v52_rows[7];
    assert_eq!(head_field(v52_result, "headOrder"), "52");
    assert_eq!(head_field(v52_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v52_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v52_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v52_result, "addedVertices"), "-");
    assert_eq!(head_field(v52_result, "addedEdges"), "-");
    assert_eq!(head_field(v52_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v52_result, "linkerChanges"),
        "[h:16:LEFT:BOTTOM:true:false->true:true,h:16:LEFT:TOP:true:false->true:true,h:16:RIGHT:BOTTOM:false:false->false:true,h:16:RIGHT:TOP:false:false->false:true,linker:SLinker:head:16:false:false->false:true,linker:SLinker:head:16:true:false->true:true]"
    );
    assert_eq!(
        head_field(v52_result, "linkerStateHashBefore"),
        "506a61f88b08511f46cc8dfca24caa60d4e6b40792f3e0d0942940193e55e4b3"
    );
    assert_eq!(
        head_field(v52_result, "linkerStateHashAfter"),
        "78bb4058435a4bc7bbb128693e8affda0d1f28133559139bbe41e9d0c850a689"
    );
    let v52_java = v52_rows[8];
    assert_eq!(head_field(v52_java, "headOrder"), "52");
    assert_eq!(head_field(v52_java, "headX"), "15");
    assert_eq!(head_field(v52_java, "headSig"), "80");
    assert_eq!(head_field(v52_java, "headInterId"), "1449");
    assert_eq!(
        head_field(v52_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v52_java, "incident"),
        "[stem2360:headSideLEFT:heads[x15:sig80:id1449:sideLEFT,x16:sig81:id1451:sideLEFT]]"
    );
    assert_eq!(head_field(v52_java, "returned"), "true");
    assert_eq!(head_field(v52_java, "undefs"), "[]");
    assert_eq!(
        head_field(v52_java, "closureWrites"),
        "[x16:sig81:LEFT:false->true,x16:sig81:RIGHT:false->true]"
    );
    assert_eq!(head_field(v52_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v52_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v52_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v52_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v52_java, "linkerStateHashBefore"),
        "506a61f88b08511f46cc8dfca24caa60d4e6b40792f3e0d0942940193e55e4b3"
    );
    assert_eq!(
        head_field(v52_java, "linkerStateHashAfter"),
        "78bb4058435a4bc7bbb128693e8affda0d1f28133559139bbe41e9d0c850a689"
    );
    assert_eq!(head_field(v52_java, "nextHeadOrder"), "53");
    assert_eq!(head_field(v52_java, "nextHeadX"), "84");
    assert_eq!(head_field(v52_java, "nextHeadSig"), "86");
    assert_eq!(head_field(v52_java, "nextHeadInterId"), "1461");
    assert_eq!(head_field(v52_java, "terminal"), "ReturnedBeforeNextHead");
    let v52_summary = v52_rows[9];
    assert_eq!(
        head_field(v52_summary, "schema"),
        "stems-head-phase-prefix-v52"
    );
    assert_eq!(head_field(v52_summary, "rows"), "9");
    assert_eq!(
        head_field(v52_summary, "baseV51RunnerSourceSha256"),
        "55f522ebc3bff281f113715e86137e5feac2d74f513907b8361903cdf3b1a828"
    );
    assert_eq!(
        head_field(v52_summary, "baseV51FixtureSha256"),
        "778c06a6697f96a439146e06d85d9d182d1f7659ef19a15fd9204aef3455546e"
    );
    assert_eq!(
        head_field(v52_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v52_summary, "probeSourceSha256"),
        "053a7ed7993314e846561a404bf093e836211373b51a552ac50c62ca40b0c355"
    );
    assert_eq!(
        head_field(v52_summary, "runnerSourceSha256"),
        "c314a1da865f91ce57128468a77dff85b7dd20719427c9119ca29057331728a6"
    );
    assert_eq!(
        head_field(v52_summary, "emittedBodySha256"),
        "bc0b579b830bc4c1ccf057f85efa02b6226f25ade19d62aca28344ff9f0c35da"
    );
    assert_eq!(
        head_field(v52_summary, "semanticPassSha256"),
        "19d22e7ed4d7319494f3b734e9aed996cb37b9d4b4a27abaa93ccbec3f073e0e"
    );
    assert_eq!(head_field(v52_summary, "freshRuns"), "2");
    assert_eq!(head_field(v52_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v52_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder52ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v52_summary, "javaEvidence"),
        "ReturnedBeforeFiftyThirdHead"
    );

    // Boundary 79 expected-only order53 existing-stem reconciliation.
    let v53_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v53.txt"),
    )
    .expect("expected-only order53 existing-stem fixture");
    assert_eq!(
        sha256_hex(v53_text.as_bytes()),
        "58671e0a19695e626a633a8963683d310083e3bb18ee9d419a69d1db0267be76"
    );
    let v53_rows = v53_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v53_rows.len(), 10);
    let v53_body = format!("{}\n", v53_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v53_body.as_bytes()),
        "575945ccaeb5f2d2288fd8d0cbff7978849bc8b017d39a8876bcef102c09a1a5"
    );
    let v53_frontier = v53_rows[6];
    assert_eq!(head_field(v53_frontier, "headOrder"), "53");
    assert_eq!(head_field(v53_frontier, "headX"), "84");
    assert_eq!(head_field(v53_frontier, "headSig"), "86");
    assert_eq!(head_field(v53_frontier, "headInterId"), "1461");
    assert_eq!(head_field(v53_frontier, "cAlias"), "h:84:LEFT:BOTTOM");
    assert_eq!(head_field(v53_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v53_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v53_frontier, "relations"), "2");
    assert_eq!(head_field(v53_frontier, "glyphs"), "1");
    assert_eq!(head_field(v53_frontier, "candidateIdBefore"), "320");
    assert_eq!(head_field(v53_frontier, "existingGlyph"), "glyph:320");
    assert_eq!(head_field(v53_frontier, "existingActive"), "true");
    assert_eq!(head_field(v53_frontier, "existingStem"), "2366");
    let v53_result = v53_rows[7];
    assert_eq!(head_field(v53_result, "headOrder"), "53");
    assert_eq!(head_field(v53_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v53_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v53_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v53_result, "addedVertices"), "-");
    assert_eq!(head_field(v53_result, "addedEdges"), "-");
    assert_eq!(head_field(v53_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v53_result, "linkerChanges"),
        "[h:85:LEFT:BOTTOM:true:false->true:true,h:85:LEFT:TOP:true:false->true:true,h:85:RIGHT:BOTTOM:false:false->false:true,h:85:RIGHT:TOP:false:false->false:true,linker:SLinker:head:85:false:false->false:true,linker:SLinker:head:85:true:false->true:true]"
    );
    assert_eq!(
        head_field(v53_result, "linkerStateHashBefore"),
        "78bb4058435a4bc7bbb128693e8affda0d1f28133559139bbe41e9d0c850a689"
    );
    assert_eq!(
        head_field(v53_result, "linkerStateHashAfter"),
        "c14baae90be22e2fa2423a64702af53670d8a9663e517b5470ec63f7d21b5ccf"
    );
    let v53_java = v53_rows[8];
    assert_eq!(head_field(v53_java, "headOrder"), "53");
    assert_eq!(head_field(v53_java, "headX"), "84");
    assert_eq!(head_field(v53_java, "headSig"), "86");
    assert_eq!(head_field(v53_java, "headInterId"), "1461");
    assert_eq!(
        head_field(v53_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v53_java, "incident"),
        "[stem2366:headSideLEFT:heads[x84:sig86:id1461:sideLEFT,x85:sig87:id1463:sideLEFT,x86:sig85:id1459:sideLEFT]]"
    );
    assert_eq!(head_field(v53_java, "returned"), "true");
    assert_eq!(head_field(v53_java, "undefs"), "[]");
    assert_eq!(
        head_field(v53_java, "closureWrites"),
        "[x85:sig87:LEFT:false->true,x85:sig87:RIGHT:false->true,x86:sig85:LEFT:true->true,x86:sig85:RIGHT:true->true]"
    );
    assert_eq!(head_field(v53_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v53_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v53_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v53_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v53_java, "linkerStateHashBefore"),
        "78bb4058435a4bc7bbb128693e8affda0d1f28133559139bbe41e9d0c850a689"
    );
    assert_eq!(
        head_field(v53_java, "linkerStateHashAfter"),
        "c14baae90be22e2fa2423a64702af53670d8a9663e517b5470ec63f7d21b5ccf"
    );
    assert_eq!(head_field(v53_java, "nextHeadOrder"), "54");
    assert_eq!(head_field(v53_java, "nextHeadX"), "11");
    assert_eq!(head_field(v53_java, "nextHeadSig"), "62");
    assert_eq!(head_field(v53_java, "nextHeadInterId"), "1413");
    assert_eq!(head_field(v53_java, "terminal"), "ReturnedBeforeNextHead");
    let v53_summary = v53_rows[9];
    assert_eq!(
        head_field(v53_summary, "schema"),
        "stems-head-phase-prefix-v53"
    );
    assert_eq!(head_field(v53_summary, "rows"), "9");
    assert_eq!(
        head_field(v53_summary, "baseV52RunnerSourceSha256"),
        "c314a1da865f91ce57128468a77dff85b7dd20719427c9119ca29057331728a6"
    );
    assert_eq!(
        head_field(v53_summary, "baseV52FixtureSha256"),
        "ff3bbe3fdf9ba0e6140b8105ab46e4c65972d4f4f013d39f90eade2270b64224"
    );
    assert_eq!(
        head_field(v53_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v53_summary, "probeSourceSha256"),
        "c153c5cc6c2dbc02486c179ad04ddeea04cb331eefa345bf1059476ee7d0ba43"
    );
    assert_eq!(
        head_field(v53_summary, "runnerSourceSha256"),
        "cdf67c2e57e5afcbb7e7030d0cf80a2ce50302778032154b5ced066f234a2611"
    );
    assert_eq!(
        head_field(v53_summary, "emittedBodySha256"),
        "575945ccaeb5f2d2288fd8d0cbff7978849bc8b017d39a8876bcef102c09a1a5"
    );
    assert_eq!(
        head_field(v53_summary, "semanticPassSha256"),
        "12ce9cd028a62c77be4f68fab944a998e495b161a24e0f4ac43e6522b23aeb62"
    );
    assert_eq!(head_field(v53_summary, "freshRuns"), "2");
    assert_eq!(head_field(v53_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v53_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder53ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v53_summary, "javaEvidence"),
        "ReturnedBeforeFiftyFourthHead"
    );

    // Boundary 80 expected-only order54 existing-stem reconciliation.
    let v54_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v54.txt"),
    )
    .expect("expected-only order54 existing-stem fixture");
    assert_eq!(
        sha256_hex(v54_text.as_bytes()),
        "c8b6567ddac7269d126846e036fc7e4fbb8a9c430a7b110006ff8e980ad85305"
    );
    let v54_rows = v54_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v54_rows.len(), 10);
    let v54_body = format!("{}\n", v54_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v54_body.as_bytes()),
        "4c8613b927af254cbaa6afdcf4e99dc99a2f149381985fd74b8f15f2b20d0286"
    );
    let v54_frontier = v54_rows[6];
    assert_eq!(head_field(v54_frontier, "headOrder"), "54");
    assert_eq!(head_field(v54_frontier, "headX"), "11");
    assert_eq!(head_field(v54_frontier, "headSig"), "62");
    assert_eq!(head_field(v54_frontier, "headInterId"), "1413");
    assert_eq!(head_field(v54_frontier, "cAlias"), "h:11:LEFT:BOTTOM");
    assert_eq!(head_field(v54_frontier, "lastIndex"), "4");
    assert_eq!(head_field(v54_frontier, "maxIndex"), "4");
    assert_eq!(head_field(v54_frontier, "relations"), "4");
    assert_eq!(head_field(v54_frontier, "glyphs"), "2");
    assert_eq!(head_field(v54_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v54_frontier, "existingGlyph"), "glyph:312");
    assert_eq!(head_field(v54_frontier, "existingActive"), "true");
    assert_eq!(head_field(v54_frontier, "existingStem"), "2349");
    let v54_result = v54_rows[7];
    assert_eq!(head_field(v54_result, "headOrder"), "54");
    assert_eq!(head_field(v54_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v54_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v54_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v54_result, "addedVertices"), "-");
    assert_eq!(head_field(v54_result, "addedEdges"), "-");
    assert_eq!(head_field(v54_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v54_result, "linkerChanges"),
        "[h:12:LEFT:BOTTOM:true:false->true:true,h:12:LEFT:TOP:true:false->true:true,h:12:RIGHT:BOTTOM:false:false->false:true,h:12:RIGHT:TOP:false:false->false:true,linker:SLinker:head:12:false:false->false:true,linker:SLinker:head:12:true:false->true:true]"
    );
    assert_eq!(
        head_field(v54_result, "linkerStateHashBefore"),
        "c14baae90be22e2fa2423a64702af53670d8a9663e517b5470ec63f7d21b5ccf"
    );
    assert_eq!(
        head_field(v54_result, "linkerStateHashAfter"),
        "461cbcd4ab370b14b138081a11aa1b500f5b2669c1fe3fba8a4755e257bb7d07"
    );
    let v54_java = v54_rows[8];
    assert_eq!(head_field(v54_java, "headOrder"), "54");
    assert_eq!(head_field(v54_java, "headX"), "11");
    assert_eq!(head_field(v54_java, "headSig"), "62");
    assert_eq!(head_field(v54_java, "headInterId"), "1413");
    assert_eq!(
        head_field(v54_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v54_java, "incident"),
        "[stem2349:headSideLEFT:heads[x11:sig62:id1413:sideLEFT,x12:sig63:id1415:sideLEFT]]"
    );
    assert_eq!(head_field(v54_java, "returned"), "true");
    assert_eq!(head_field(v54_java, "undefs"), "[]");
    assert_eq!(
        head_field(v54_java, "closureWrites"),
        "[x12:sig63:LEFT:false->true,x12:sig63:RIGHT:false->true]"
    );
    assert_eq!(head_field(v54_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v54_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v54_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v54_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v54_java, "linkerStateHashBefore"),
        "c14baae90be22e2fa2423a64702af53670d8a9663e517b5470ec63f7d21b5ccf"
    );
    assert_eq!(
        head_field(v54_java, "linkerStateHashAfter"),
        "461cbcd4ab370b14b138081a11aa1b500f5b2669c1fe3fba8a4755e257bb7d07"
    );
    assert_eq!(head_field(v54_java, "nextHeadOrder"), "55");
    assert_eq!(head_field(v54_java, "nextHeadX"), "68");
    assert_eq!(head_field(v54_java, "nextHeadSig"), "75");
    assert_eq!(head_field(v54_java, "nextHeadInterId"), "1439");
    assert_eq!(head_field(v54_java, "terminal"), "ReturnedBeforeNextHead");
    let v54_summary = v54_rows[9];
    assert_eq!(
        head_field(v54_summary, "schema"),
        "stems-head-phase-prefix-v54"
    );
    assert_eq!(head_field(v54_summary, "rows"), "9");
    assert_eq!(
        head_field(v54_summary, "baseV53RunnerSourceSha256"),
        "cdf67c2e57e5afcbb7e7030d0cf80a2ce50302778032154b5ced066f234a2611"
    );
    assert_eq!(
        head_field(v54_summary, "baseV53FixtureSha256"),
        "58671e0a19695e626a633a8963683d310083e3bb18ee9d419a69d1db0267be76"
    );
    assert_eq!(
        head_field(v54_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v54_summary, "probeSourceSha256"),
        "e3eb0126cd446a3c67e808c08004775e32aa7e5bd4e78d5d262ad9fafff89abd"
    );
    assert_eq!(
        head_field(v54_summary, "runnerSourceSha256"),
        "1fec8bbf7c5561150d9d69079275fccc1879f5d61756f8c525957c0ef90b16ca"
    );
    assert_eq!(
        head_field(v54_summary, "emittedBodySha256"),
        "4c8613b927af254cbaa6afdcf4e99dc99a2f149381985fd74b8f15f2b20d0286"
    );
    assert_eq!(
        head_field(v54_summary, "semanticPassSha256"),
        "e77cc8596f1540ab17b7894abcbf017cd2e3c79cffc4ae974011910e2081ec96"
    );
    assert_eq!(head_field(v54_summary, "freshRuns"), "2");
    assert_eq!(head_field(v54_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v54_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder54ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v54_summary, "javaEvidence"),
        "ReturnedBeforeFiftyFifthHead"
    );

    // Boundary 81 expected-only order55 existing-stem reconciliation.
    let v55_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v55.txt"),
    )
    .expect("expected-only order55 existing-stem fixture");
    assert_eq!(
        sha256_hex(v55_text.as_bytes()),
        "8ed542577f68000705cd7d166c8d848e48c138ae9f6e8fad0de10a499e46c0ff"
    );
    let v55_rows = v55_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v55_rows.len(), 10);
    let v55_body = format!("{}\n", v55_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v55_body.as_bytes()),
        "fceb2bca866912731d6ab81165fd03254f2c602d3937af196ddfff002d8beb60"
    );
    let v55_frontier = v55_rows[6];
    assert_eq!(head_field(v55_frontier, "headOrder"), "55");
    assert_eq!(head_field(v55_frontier, "headX"), "68");
    assert_eq!(head_field(v55_frontier, "headSig"), "75");
    assert_eq!(head_field(v55_frontier, "headInterId"), "1439");
    assert_eq!(head_field(v55_frontier, "cAlias"), "h:68:LEFT:BOTTOM");
    assert_eq!(head_field(v55_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v55_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v55_frontier, "relations"), "1");
    assert_eq!(head_field(v55_frontier, "glyphs"), "2");
    assert_eq!(head_field(v55_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v55_frontier, "existingGlyph"), "glyph:331");
    assert_eq!(head_field(v55_frontier, "existingActive"), "true");
    assert_eq!(head_field(v55_frontier, "existingStem"), "2347");
    let v55_result = v55_rows[7];
    assert_eq!(head_field(v55_result, "headOrder"), "55");
    assert_eq!(head_field(v55_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v55_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v55_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v55_result, "addedVertices"), "-");
    assert_eq!(head_field(v55_result, "addedEdges"), "-");
    assert_eq!(head_field(v55_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v55_result, "linkerChanges"),
        "[h:69:LEFT:BOTTOM:true:false->true:true,h:69:LEFT:TOP:true:false->true:true,h:69:RIGHT:BOTTOM:false:false->false:true,h:69:RIGHT:TOP:false:false->false:true,linker:SLinker:head:69:false:false->false:true,linker:SLinker:head:69:true:false->true:true]"
    );
    assert_eq!(
        head_field(v55_result, "linkerStateHashBefore"),
        "461cbcd4ab370b14b138081a11aa1b500f5b2669c1fe3fba8a4755e257bb7d07"
    );
    assert_eq!(
        head_field(v55_result, "linkerStateHashAfter"),
        "630f0764ed5766b5bcf6cb02b771656c8bdcca7211b1846918849f76a67c9104"
    );
    let v55_java = v55_rows[8];
    assert_eq!(head_field(v55_java, "headOrder"), "55");
    assert_eq!(head_field(v55_java, "headX"), "68");
    assert_eq!(head_field(v55_java, "headSig"), "75");
    assert_eq!(head_field(v55_java, "headInterId"), "1439");
    assert_eq!(
        head_field(v55_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v55_java, "incident"),
        "[stem2347:headSideLEFT:heads[x68:sig75:id1439:sideLEFT,x69:sig76:id1441:sideLEFT]]"
    );
    assert_eq!(head_field(v55_java, "returned"), "true");
    assert_eq!(head_field(v55_java, "undefs"), "[]");
    assert_eq!(
        head_field(v55_java, "closureWrites"),
        "[x69:sig76:LEFT:false->true,x69:sig76:RIGHT:false->true]"
    );
    assert_eq!(head_field(v55_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v55_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v55_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v55_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v55_java, "linkerStateHashBefore"),
        "461cbcd4ab370b14b138081a11aa1b500f5b2669c1fe3fba8a4755e257bb7d07"
    );
    assert_eq!(
        head_field(v55_java, "linkerStateHashAfter"),
        "630f0764ed5766b5bcf6cb02b771656c8bdcca7211b1846918849f76a67c9104"
    );
    assert_eq!(head_field(v55_java, "nextHeadOrder"), "56");
    assert_eq!(head_field(v55_java, "nextHeadX"), "21");
    assert_eq!(head_field(v55_java, "nextHeadSig"), "11");
    assert_eq!(head_field(v55_java, "nextHeadInterId"), "1307");
    assert_eq!(head_field(v55_java, "terminal"), "ReturnedBeforeNextHead");
    let v55_summary = v55_rows[9];
    assert_eq!(
        head_field(v55_summary, "schema"),
        "stems-head-phase-prefix-v55"
    );
    assert_eq!(head_field(v55_summary, "rows"), "9");
    assert_eq!(
        head_field(v55_summary, "baseV54RunnerSourceSha256"),
        "1fec8bbf7c5561150d9d69079275fccc1879f5d61756f8c525957c0ef90b16ca"
    );
    assert_eq!(
        head_field(v55_summary, "baseV54FixtureSha256"),
        "c8b6567ddac7269d126846e036fc7e4fbb8a9c430a7b110006ff8e980ad85305"
    );
    assert_eq!(
        head_field(v55_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v55_summary, "probeSourceSha256"),
        "6557794a5daf7335e05a6de21f7a9479aa2a601d151163086a224d3b64cfea9d"
    );
    assert_eq!(
        head_field(v55_summary, "runnerSourceSha256"),
        "8dfffd31e6db65348598433cc0e19683d80be55705e9d843bd88e564deb5ca67"
    );
    assert_eq!(
        head_field(v55_summary, "emittedBodySha256"),
        "fceb2bca866912731d6ab81165fd03254f2c602d3937af196ddfff002d8beb60"
    );
    assert_eq!(
        head_field(v55_summary, "semanticPassSha256"),
        "354f56967e0b75c4948321dc0153e041851c47c3cbe1507843abca8de249992a"
    );
    assert_eq!(head_field(v55_summary, "freshRuns"), "2");
    assert_eq!(head_field(v55_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v55_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder55ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v55_summary, "javaEvidence"),
        "ReturnedBeforeFiftySixthHead"
    );

    // Boundary 82 expected-only order56 existing-stem reconciliation.
    let v56_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v56.txt"),
    )
    .expect("expected-only order56 existing-stem fixture");
    assert_eq!(
        sha256_hex(v56_text.as_bytes()),
        "e463b46a707b8c534a1896bf47d7668c580f38c27b2947ccdfb12fb7984e2cc5"
    );
    let v56_rows = v56_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v56_rows.len(), 10);
    let v56_body = format!("{}\n", v56_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v56_body.as_bytes()),
        "6ca99c74758506f6173cc8e9b21d323002c98ebcaa1bb34732091fcd10b4a43a"
    );
    let v56_frontier = v56_rows[6];
    assert_eq!(head_field(v56_frontier, "headOrder"), "56");
    assert_eq!(head_field(v56_frontier, "headX"), "21");
    assert_eq!(head_field(v56_frontier, "headSig"), "11");
    assert_eq!(head_field(v56_frontier, "headInterId"), "1307");
    assert_eq!(head_field(v56_frontier, "cAlias"), "h:21:LEFT:BOTTOM");
    assert_eq!(head_field(v56_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v56_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v56_frontier, "relations"), "4");
    assert_eq!(head_field(v56_frontier, "glyphs"), "1");
    assert_eq!(head_field(v56_frontier, "candidateIdBefore"), "323");
    assert_eq!(head_field(v56_frontier, "existingGlyph"), "glyph:323");
    assert_eq!(head_field(v56_frontier, "existingActive"), "true");
    assert_eq!(head_field(v56_frontier, "existingStem"), "2341");
    let v56_result = v56_rows[7];
    assert_eq!(head_field(v56_result, "headOrder"), "56");
    assert_eq!(head_field(v56_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v56_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v56_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v56_result, "addedVertices"), "-");
    assert_eq!(head_field(v56_result, "addedEdges"), "-");
    assert_eq!(head_field(v56_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v56_result, "linkerChanges"),
        "[h:22:LEFT:BOTTOM:true:false->true:true,h:22:LEFT:TOP:true:false->true:true,h:22:RIGHT:BOTTOM:false:false->false:true,h:22:RIGHT:TOP:false:false->false:true,linker:SLinker:head:22:false:false->false:true,linker:SLinker:head:22:true:false->true:true]"
    );
    assert_eq!(
        head_field(v56_result, "linkerStateHashBefore"),
        "630f0764ed5766b5bcf6cb02b771656c8bdcca7211b1846918849f76a67c9104"
    );
    assert_eq!(
        head_field(v56_result, "linkerStateHashAfter"),
        "5a005ab2218f2c3cd25dbeca2730b845db0859c76e12fa6019c9aadc1b116fca"
    );
    let v56_java = v56_rows[8];
    assert_eq!(head_field(v56_java, "headOrder"), "56");
    assert_eq!(head_field(v56_java, "headX"), "21");
    assert_eq!(head_field(v56_java, "headSig"), "11");
    assert_eq!(head_field(v56_java, "headInterId"), "1307");
    assert_eq!(
        head_field(v56_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v56_java, "incident"),
        "[stem2341:headSideLEFT:heads[x21:sig11:id1307:sideLEFT,x22:sig12:id1309:sideLEFT]]"
    );
    assert_eq!(head_field(v56_java, "returned"), "true");
    assert_eq!(head_field(v56_java, "undefs"), "[]");
    assert_eq!(
        head_field(v56_java, "closureWrites"),
        "[x22:sig12:LEFT:false->true,x22:sig12:RIGHT:false->true]"
    );
    assert_eq!(head_field(v56_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v56_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v56_java, "sigEdgesAfter"), "696");
    assert_eq!(head_field(v56_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v56_java, "linkerStateHashBefore"),
        "630f0764ed5766b5bcf6cb02b771656c8bdcca7211b1846918849f76a67c9104"
    );
    assert_eq!(
        head_field(v56_java, "linkerStateHashAfter"),
        "5a005ab2218f2c3cd25dbeca2730b845db0859c76e12fa6019c9aadc1b116fca"
    );
    assert_eq!(head_field(v56_java, "nextHeadOrder"), "57");
    assert_eq!(head_field(v56_java, "nextHeadX"), "62");
    assert_eq!(head_field(v56_java, "nextHeadSig"), "16");
    assert_eq!(head_field(v56_java, "nextHeadInterId"), "1317");
    assert_eq!(head_field(v56_java, "terminal"), "ReturnedBeforeNextHead");
    let v56_summary = v56_rows[9];
    assert_eq!(
        head_field(v56_summary, "schema"),
        "stems-head-phase-prefix-v56"
    );
    assert_eq!(head_field(v56_summary, "rows"), "9");
    assert_eq!(
        head_field(v56_summary, "baseV55RunnerSourceSha256"),
        "8dfffd31e6db65348598433cc0e19683d80be55705e9d843bd88e564deb5ca67"
    );
    assert_eq!(
        head_field(v56_summary, "baseV55FixtureSha256"),
        "8ed542577f68000705cd7d166c8d848e48c138ae9f6e8fad0de10a499e46c0ff"
    );
    assert_eq!(
        head_field(v56_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v56_summary, "probeSourceSha256"),
        "24e6dd4f7975c419cc473be22f351f1d4687c76b2fcb0100eeb52080b0fb924f"
    );
    assert_eq!(
        head_field(v56_summary, "runnerSourceSha256"),
        "b6a0b6cd9a618052e02da16022a44fe3b218626100852bb6f702ceedc09f3387"
    );
    assert_eq!(
        head_field(v56_summary, "emittedBodySha256"),
        "6ca99c74758506f6173cc8e9b21d323002c98ebcaa1bb34732091fcd10b4a43a"
    );
    assert_eq!(
        head_field(v56_summary, "semanticPassSha256"),
        "3ed440230114439aa352bfe883e6befb404e26b26235b98e060860b6841422e8"
    );
    assert_eq!(head_field(v56_summary, "freshRuns"), "2");
    assert_eq!(head_field(v56_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v56_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder56ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v56_summary, "javaEvidence"),
        "ReturnedBeforeFiftySeventhHead"
    );

    // Boundary 83 expected-only order57 existing-stem C-link reuse.
    let v57_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v57.txt"),
    )
    .expect("expected-only order57 existing-stem C-link fixture");
    assert_eq!(
        sha256_hex(v57_text.as_bytes()),
        "c8b44bab7c0e75e74755e3ca2d29f46b72b90cde0e406524fb395214a6fe25d5"
    );
    let v57_rows = v57_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v57_rows.len(), 10);
    let v57_body = format!("{}\n", v57_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v57_body.as_bytes()),
        "b9e3309cdeea3f2bc59abe296a3d33738e09ab79021fb710259d5b608e8557ac"
    );
    let v57_frontier = v57_rows[6];
    assert_eq!(head_field(v57_frontier, "headOrder"), "57");
    assert_eq!(head_field(v57_frontier, "headX"), "62");
    assert_eq!(head_field(v57_frontier, "headSig"), "16");
    assert_eq!(head_field(v57_frontier, "headInterId"), "1317");
    assert_eq!(head_field(v57_frontier, "cAlias"), "h:62:LEFT:BOTTOM");
    assert_eq!(head_field(v57_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v57_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v57_frontier, "relations"), "1");
    assert_eq!(head_field(v57_frontier, "glyphs"), "2");
    assert_eq!(head_field(v57_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v57_frontier, "existingGlyph"), "glyph:328");
    assert_eq!(head_field(v57_frontier, "existingActive"), "true");
    assert_eq!(head_field(v57_frontier, "existingStem"), "2381");
    assert_eq!(head_field(v57_frontier, "lineChanged"), "false");
    let v57_result = v57_rows[7];
    assert_eq!(head_field(v57_result, "headOrder"), "57");
    assert_eq!(head_field(v57_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v57_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v57_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v57_result, "addedVertices"), "-");
    assert_eq!(head_field(v57_result, "addedSystemStems"), "-");
    assert!(
        head_field(v57_result, "addedEdges").starts_with(
            "[system1:sourceId1317:targetId2381:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false"
        )
    );
    assert_eq!(
        head_field(v57_result, "linkerChanges"),
        "[h:62:LEFT:BOTTOM:false:false->true:false,h:62:LEFT:TOP:false:false->true:false,h:63:LEFT:BOTTOM:true:false->true:true,h:63:LEFT:TOP:true:false->true:true,h:63:RIGHT:BOTTOM:false:false->false:true,h:63:RIGHT:TOP:false:false->false:true,linker:SLinker:head:62:false:false->true:false,linker:SLinker:head:63:false:false->false:true,linker:SLinker:head:63:true:false->true:true]"
    );
    assert_eq!(
        head_field(v57_result, "sigHashBefore"),
        "6517106b0e06335bf50bcc1ba3dface62b503eb94ea3c33cab23ef3a5c6a09f0"
    );
    assert_eq!(
        head_field(v57_result, "sigHashAfter"),
        "c7f7b30b6f28f19df30e5338075d83cd7003e483ee90c96c9347a2a119f2e2c6"
    );
    assert_eq!(
        head_field(v57_result, "linkerStateHashBefore"),
        "5a005ab2218f2c3cd25dbeca2730b845db0859c76e12fa6019c9aadc1b116fca"
    );
    assert_eq!(
        head_field(v57_result, "linkerStateHashAfter"),
        "6af1416efb4ecb928c5a7f4c89221de4508310172f67764b78e7c5fc8f365637"
    );
    let v57_java = v57_rows[8];
    assert_eq!(head_field(v57_java, "headOrder"), "57");
    assert_eq!(head_field(v57_java, "headX"), "62");
    assert_eq!(head_field(v57_java, "headSig"), "16");
    assert_eq!(head_field(v57_java, "headInterId"), "1317");
    assert_eq!(
        head_field(v57_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v57_java, "incident"), "-");
    assert_eq!(head_field(v57_java, "returned"), "true");
    assert_eq!(head_field(v57_java, "undefs"), "[]");
    assert_eq!(head_field(v57_java, "closureWrites"), "-");
    assert_eq!(head_field(v57_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v57_java, "sigVerticesBefore"), "685");
    assert_eq!(head_field(v57_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v57_java, "sigEdgesBefore"), "696");
    assert_eq!(head_field(v57_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v57_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v57_java, "nextHeadOrder"), "58");
    assert_eq!(head_field(v57_java, "nextHeadX"), "92");
    assert_eq!(head_field(v57_java, "nextHeadSig"), "24");
    assert_eq!(head_field(v57_java, "nextHeadInterId"), "1333");
    assert_eq!(head_field(v57_java, "terminal"), "ReturnedBeforeNextHead");
    let v57_summary = v57_rows[9];
    assert_eq!(
        head_field(v57_summary, "schema"),
        "stems-head-phase-prefix-v57"
    );
    assert_eq!(head_field(v57_summary, "rows"), "9");
    assert_eq!(
        head_field(v57_summary, "baseV56RunnerSourceSha256"),
        "b6a0b6cd9a618052e02da16022a44fe3b218626100852bb6f702ceedc09f3387"
    );
    assert_eq!(
        head_field(v57_summary, "baseV56FixtureSha256"),
        "e463b46a707b8c534a1896bf47d7668c580f38c27b2947ccdfb12fb7984e2cc5"
    );
    assert_eq!(
        head_field(v57_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v57_summary, "probeSourceSha256"),
        "ef9f4a596d53db3ab7007d382f16319f44985be90c60f788aa854fb8f7379d5d"
    );
    assert_eq!(
        head_field(v57_summary, "runnerSourceSha256"),
        "a108389f5e9465fb4483ecec852e38dc7985676a1d9f1feb8dcb392b32559fbc"
    );
    assert_eq!(
        head_field(v57_summary, "emittedBodySha256"),
        "b9e3309cdeea3f2bc59abe296a3d33738e09ab79021fb710259d5b608e8557ac"
    );
    assert_eq!(
        head_field(v57_summary, "semanticPassSha256"),
        "790274c39fb5e7b4637d2f7d26a62559c89264c265a19710f36a8351d9f454fe"
    );
    assert_eq!(head_field(v57_summary, "freshRuns"), "2");
    assert_eq!(head_field(v57_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v57_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder57ExistingStemCLink"
    );
    assert_eq!(
        head_field(v57_summary, "javaEvidence"),
        "ReturnedBeforeFiftyEighthHead"
    );

    // Boundary 84 expected-only order58 existing-stem reconciliation.
    let v58_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v58.txt"),
    )
    .expect("expected-only order58 existing-stem fixture");
    assert_eq!(
        sha256_hex(v58_text.as_bytes()),
        "a262c7a657a028a7c2e273283176749bc364717837735a391540cb2783a2ed06"
    );
    let v58_rows = v58_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v58_rows.len(), 10);
    let v58_body = format!("{}\n", v58_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v58_body.as_bytes()),
        "6c0e5202b0e2a891c53a4635ec2729d636e0e7758141174c96462e834602d83b"
    );
    let v58_frontier = v58_rows[6];
    assert_eq!(head_field(v58_frontier, "headOrder"), "58");
    assert_eq!(head_field(v58_frontier, "headX"), "92");
    assert_eq!(head_field(v58_frontier, "headSig"), "24");
    assert_eq!(head_field(v58_frontier, "headInterId"), "1333");
    assert_eq!(head_field(v58_frontier, "cAlias"), "h:92:LEFT:BOTTOM");
    assert_eq!(head_field(v58_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v58_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v58_frontier, "relations"), "3");
    assert_eq!(head_field(v58_frontier, "glyphs"), "2");
    assert_eq!(head_field(v58_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v58_frontier, "existingGlyph"), "glyph:298");
    assert_eq!(head_field(v58_frontier, "existingActive"), "true");
    assert_eq!(head_field(v58_frontier, "existingStem"), "2342");
    let v58_result = v58_rows[7];
    assert_eq!(head_field(v58_result, "headOrder"), "58");
    assert_eq!(head_field(v58_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v58_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v58_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v58_result, "addedVertices"), "-");
    assert_eq!(head_field(v58_result, "addedEdges"), "-");
    assert_eq!(head_field(v58_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v58_result, "linkerChanges"),
        "[h:93:LEFT:BOTTOM:true:false->true:true,h:93:LEFT:TOP:true:false->true:true,h:93:RIGHT:BOTTOM:false:false->false:true,h:93:RIGHT:TOP:false:false->false:true,linker:SLinker:head:93:false:false->false:true,linker:SLinker:head:93:true:false->true:true]"
    );
    assert_eq!(
        head_field(v58_result, "linkerStateHashBefore"),
        "6af1416efb4ecb928c5a7f4c89221de4508310172f67764b78e7c5fc8f365637"
    );
    assert_eq!(
        head_field(v58_result, "linkerStateHashAfter"),
        "7416f9f4a74b1d869bdeb17b8fe4f476985a2571bf58d9176400f1be8798440e"
    );
    let v58_java = v58_rows[8];
    assert_eq!(head_field(v58_java, "headOrder"), "58");
    assert_eq!(head_field(v58_java, "headX"), "92");
    assert_eq!(head_field(v58_java, "headSig"), "24");
    assert_eq!(head_field(v58_java, "headInterId"), "1333");
    assert_eq!(
        head_field(v58_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v58_java, "incident"),
        "[stem2342:headSideLEFT:heads[x92:sig24:id1333:sideLEFT,x93:sig25:id1335:sideLEFT]]"
    );
    assert_eq!(head_field(v58_java, "returned"), "true");
    assert_eq!(head_field(v58_java, "undefs"), "[]");
    assert_eq!(
        head_field(v58_java, "closureWrites"),
        "[x93:sig25:LEFT:false->true,x93:sig25:RIGHT:false->true]"
    );
    assert_eq!(head_field(v58_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v58_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v58_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v58_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v58_java, "linkerStateHashBefore"),
        "6af1416efb4ecb928c5a7f4c89221de4508310172f67764b78e7c5fc8f365637"
    );
    assert_eq!(
        head_field(v58_java, "linkerStateHashAfter"),
        "7416f9f4a74b1d869bdeb17b8fe4f476985a2571bf58d9176400f1be8798440e"
    );
    assert_eq!(head_field(v58_java, "nextHeadOrder"), "59");
    assert_eq!(head_field(v58_java, "nextHeadX"), "100");
    assert_eq!(head_field(v58_java, "nextHeadSig"), "42");
    assert_eq!(head_field(v58_java, "nextHeadInterId"), "1369");
    assert_eq!(head_field(v58_java, "terminal"), "ReturnedBeforeNextHead");
    let v58_summary = v58_rows[9];
    assert_eq!(
        head_field(v58_summary, "schema"),
        "stems-head-phase-prefix-v58"
    );
    assert_eq!(head_field(v58_summary, "rows"), "9");
    assert_eq!(
        head_field(v58_summary, "baseV57RunnerSourceSha256"),
        "a108389f5e9465fb4483ecec852e38dc7985676a1d9f1feb8dcb392b32559fbc"
    );
    assert_eq!(
        head_field(v58_summary, "baseV57FixtureSha256"),
        "c8b44bab7c0e75e74755e3ca2d29f46b72b90cde0e406524fb395214a6fe25d5"
    );
    assert_eq!(
        head_field(v58_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v58_summary, "probeSourceSha256"),
        "23f2d7a80c31898306ce8adcf61be15280aa0457e42988c424a8b2ceee9886d9"
    );
    assert_eq!(
        head_field(v58_summary, "runnerSourceSha256"),
        "9964348b54b3500efda3f1e98b1fcf4e54d9e518de4d416b170a2b1fbe8ea757"
    );
    assert_eq!(
        head_field(v58_summary, "emittedBodySha256"),
        "6c0e5202b0e2a891c53a4635ec2729d636e0e7758141174c96462e834602d83b"
    );
    assert_eq!(
        head_field(v58_summary, "semanticPassSha256"),
        "12d06cc6c5d25d3acf2189827d8bb35fa68d1e6350b7a73777f23d407345a810"
    );
    assert_eq!(head_field(v58_summary, "freshRuns"), "2");
    assert_eq!(head_field(v58_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v58_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder58ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v58_summary, "javaEvidence"),
        "ReturnedBeforeFiftyNinthHead"
    );

    // Boundary 85 expected-only order59 existing-stem reconciliation.
    let v59_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v59.txt"),
    )
    .expect("expected-only order59 existing-stem fixture");
    assert_eq!(
        sha256_hex(v59_text.as_bytes()),
        "53bb43246e40ee07c40ffdbf091c1b8521a114f4ef77f4b16a762ebfc7f5c3be"
    );
    let v59_rows = v59_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v59_rows.len(), 10);
    let v59_body = format!("{}\n", v59_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v59_body.as_bytes()),
        "73ad47a44638b2387f8ccb175f177b5dd4b85b1621622f70f0693c68efcdcc26"
    );
    let v59_frontier = v59_rows[6];
    assert_eq!(head_field(v59_frontier, "headOrder"), "59");
    assert_eq!(head_field(v59_frontier, "headX"), "100");
    assert_eq!(head_field(v59_frontier, "headSig"), "42");
    assert_eq!(head_field(v59_frontier, "headInterId"), "1369");
    assert_eq!(head_field(v59_frontier, "cAlias"), "h:100:LEFT:BOTTOM");
    assert_eq!(head_field(v59_frontier, "lastIndex"), "4");
    assert_eq!(head_field(v59_frontier, "maxIndex"), "4");
    assert_eq!(head_field(v59_frontier, "relations"), "4");
    assert_eq!(head_field(v59_frontier, "glyphs"), "2");
    assert_eq!(head_field(v59_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v59_frontier, "existingGlyph"), "glyph:333");
    assert_eq!(head_field(v59_frontier, "existingActive"), "true");
    assert_eq!(head_field(v59_frontier, "existingStem"), "2343");
    let v59_result = v59_rows[7];
    assert_eq!(head_field(v59_result, "headOrder"), "59");
    assert_eq!(head_field(v59_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v59_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v59_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v59_result, "addedVertices"), "-");
    assert_eq!(head_field(v59_result, "addedEdges"), "-");
    assert_eq!(head_field(v59_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v59_result, "linkerChanges"),
        "[h:101:LEFT:BOTTOM:true:false->true:true,h:101:LEFT:TOP:true:false->true:true,h:101:RIGHT:BOTTOM:false:false->false:true,h:101:RIGHT:TOP:false:false->false:true,linker:SLinker:head:101:false:false->false:true,linker:SLinker:head:101:true:false->true:true]"
    );
    assert_eq!(
        head_field(v59_result, "linkerStateHashBefore"),
        "7416f9f4a74b1d869bdeb17b8fe4f476985a2571bf58d9176400f1be8798440e"
    );
    assert_eq!(
        head_field(v59_result, "linkerStateHashAfter"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    let v59_java = v59_rows[8];
    assert_eq!(head_field(v59_java, "headOrder"), "59");
    assert_eq!(head_field(v59_java, "headX"), "100");
    assert_eq!(head_field(v59_java, "headSig"), "42");
    assert_eq!(head_field(v59_java, "headInterId"), "1369");
    assert_eq!(
        head_field(v59_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v59_java, "incident"),
        "[stem2343:headSideLEFT:heads[x100:sig42:id1369:sideLEFT,x101:sig43:id1371:sideLEFT]]"
    );
    assert_eq!(head_field(v59_java, "returned"), "true");
    assert_eq!(head_field(v59_java, "undefs"), "[]");
    assert_eq!(
        head_field(v59_java, "closureWrites"),
        "[x101:sig43:LEFT:false->true,x101:sig43:RIGHT:false->true]"
    );
    assert_eq!(head_field(v59_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v59_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v59_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v59_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v59_java, "linkerStateHashBefore"),
        "7416f9f4a74b1d869bdeb17b8fe4f476985a2571bf58d9176400f1be8798440e"
    );
    assert_eq!(
        head_field(v59_java, "linkerStateHashAfter"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(head_field(v59_java, "nextHeadOrder"), "60");
    assert_eq!(head_field(v59_java, "nextHeadX"), "71");
    assert_eq!(head_field(v59_java, "nextHeadSig"), "49");
    assert_eq!(head_field(v59_java, "nextHeadInterId"), "1387");
    assert_eq!(head_field(v59_java, "terminal"), "ReturnedBeforeNextHead");
    let v59_summary = v59_rows[9];
    assert_eq!(
        head_field(v59_summary, "schema"),
        "stems-head-phase-prefix-v59"
    );
    assert_eq!(head_field(v59_summary, "rows"), "9");
    assert_eq!(
        head_field(v59_summary, "baseV58RunnerSourceSha256"),
        "9964348b54b3500efda3f1e98b1fcf4e54d9e518de4d416b170a2b1fbe8ea757"
    );
    assert_eq!(
        head_field(v59_summary, "baseV58FixtureSha256"),
        "a262c7a657a028a7c2e273283176749bc364717837735a391540cb2783a2ed06"
    );
    assert_eq!(
        head_field(v59_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v59_summary, "probeSourceSha256"),
        "ccab9109b73c6abda9b7093fa7a5c4df32f41a9e870e1daaae14b93ba2dd32f3"
    );
    assert_eq!(
        head_field(v59_summary, "runnerSourceSha256"),
        "c723a76e61c170f302575b609c5f6a81dbc99ed1b464d8f2558773f846070dcc"
    );
    assert_eq!(
        head_field(v59_summary, "emittedBodySha256"),
        "73ad47a44638b2387f8ccb175f177b5dd4b85b1621622f70f0693c68efcdcc26"
    );
    assert_eq!(
        head_field(v59_summary, "semanticPassSha256"),
        "b1235d8df4ce3bdf79fca3837bc34b99937873e79f6529908d564f881cd38897"
    );
    assert_eq!(head_field(v59_summary, "freshRuns"), "2");
    assert_eq!(head_field(v59_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v59_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder59ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v59_summary, "javaEvidence"),
        "ReturnedBeforeSixtiethHead"
    );

    // Boundary 86 expected-only order60 open/undefined frontier.
    let v60_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v60.txt"),
    )
    .expect("expected-only order60 open frontier fixture");
    assert_eq!(
        sha256_hex(v60_text.as_bytes()),
        "5e031afa32387b6b8a3f097d98b504b32c4216b62b1d67545fe76d3e05b39a28"
    );
    let v60_rows = v60_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v60_rows.len(), 10);
    let v60_body = format!("{}\n", v60_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v60_body.as_bytes()),
        "a0c80c155049fe309e7cbe23f314a6adf247cab176d81360b055317cc1c2f8fb"
    );
    let v60_frontier = v60_rows[6];
    assert_eq!(head_field(v60_frontier, "headOrder"), "60");
    assert_eq!(head_field(v60_frontier, "headX"), "71");
    assert_eq!(head_field(v60_frontier, "headSig"), "49");
    assert_eq!(head_field(v60_frontier, "headInterId"), "1387");
    assert_eq!(head_field(v60_frontier, "cAlias"), "h:71:LEFT:BOTTOM");
    assert_eq!(head_field(v60_frontier, "lastIndex"), "-1");
    assert_eq!(head_field(v60_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v60_frontier, "relations"), "1");
    assert_eq!(head_field(v60_frontier, "glyphs"), "2");
    assert_eq!(head_field(v60_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v60_frontier, "existingGlyph"), "glyph:332");
    assert_eq!(head_field(v60_frontier, "existingActive"), "true");
    assert_eq!(head_field(v60_frontier, "existingStem"), "2382");
    let v60_result = v60_rows[7];
    assert_eq!(head_field(v60_result, "headOrder"), "60");
    assert_eq!(head_field(v60_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v60_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v60_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v60_result, "addedVertices"), "-");
    assert_eq!(head_field(v60_result, "addedEdges"), "-");
    assert_eq!(head_field(v60_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v60_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v60_result, "linkerStateHashBefore"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(
        head_field(v60_result, "linkerStateHashAfter"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    let v60_java = v60_rows[8];
    assert_eq!(head_field(v60_java, "headOrder"), "60");
    assert_eq!(head_field(v60_java, "headX"), "71");
    assert_eq!(head_field(v60_java, "headSig"), "49");
    assert_eq!(head_field(v60_java, "headInterId"), "1387");
    assert_eq!(
        head_field(v60_java, "decisions"),
        "[LEFT:top=true:bottom=true:branch=Both,RIGHT:top=true:bottom=false:branch=TopOnly]"
    );
    assert_eq!(head_field(v60_java, "incident"), "-");
    assert_eq!(head_field(v60_java, "returned"), "false");
    assert_eq!(head_field(v60_java, "undefs"), "[LEFT]");
    assert_eq!(head_field(v60_java, "closureWrites"), "-");
    assert_eq!(head_field(v60_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v60_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v60_java, "sigEdgesBefore"), "697");
    assert_eq!(head_field(v60_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v60_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v60_java, "linkerStateHashBefore"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(
        head_field(v60_java, "linkerStateHashAfter"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(head_field(v60_java, "nextHeadOrder"), "61");
    assert_eq!(head_field(v60_java, "nextHeadX"), "70");
    assert_eq!(head_field(v60_java, "nextHeadSig"), "46");
    assert_eq!(head_field(v60_java, "nextHeadInterId"), "1377");
    assert_eq!(head_field(v60_java, "terminal"), "ReturnedBeforeNextHead");
    let v60_summary = v60_rows[9];
    assert_eq!(
        head_field(v60_summary, "schema"),
        "stems-head-phase-prefix-v60"
    );
    assert_eq!(head_field(v60_summary, "rows"), "9");
    assert_eq!(
        head_field(v60_summary, "baseV59RunnerSourceSha256"),
        "c723a76e61c170f302575b609c5f6a81dbc99ed1b464d8f2558773f846070dcc"
    );
    assert_eq!(
        head_field(v60_summary, "baseV59FixtureSha256"),
        "53bb43246e40ee07c40ffdbf091c1b8521a114f4ef77f4b16a762ebfc7f5c3be"
    );
    assert_eq!(
        head_field(v60_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v60_summary, "probeSourceSha256"),
        "effb5c2ad82f72d354997c647176d7def416acd0a00bda55b75599037327f7a9"
    );
    assert_eq!(
        head_field(v60_summary, "runnerSourceSha256"),
        "89c0d77c4e5829e6faf91179f801812bf97e27df06f0d50d9afcf4aaba63282b"
    );
    assert_eq!(
        head_field(v60_summary, "emittedBodySha256"),
        "a0c80c155049fe309e7cbe23f314a6adf247cab176d81360b055317cc1c2f8fb"
    );
    assert_eq!(
        head_field(v60_summary, "semanticPassSha256"),
        "f3e6e10d179806a4d47574aa519e9f565cd07f9c4d903e42084f842dcbfed7f2"
    );
    assert_eq!(head_field(v60_summary, "freshRuns"), "2");
    assert_eq!(head_field(v60_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v60_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder60OpenFrontier"
    );
    assert_eq!(
        head_field(v60_summary, "javaEvidence"),
        "ReturnedBeforeSixtyFirstHead"
    );

    // Boundary 87 expected-only order61 open/undefined frontier.
    let v61_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v61.txt"),
    )
    .expect("expected-only order61 open frontier fixture");
    assert_eq!(
        sha256_hex(v61_text.as_bytes()),
        "205b8875f6846384245629122645b6ac8cbef4bf18573c2ec539552d7799416d"
    );
    let v61_rows = v61_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v61_rows.len(), 10);
    let v61_body = format!("{}\n", v61_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v61_body.as_bytes()),
        "9bd32daee863bed31b87f7d311d9fd1771cead3cd406b38990bfb3a09dd8d59b"
    );
    let v61_frontier = v61_rows[6];
    assert_eq!(head_field(v61_frontier, "headOrder"), "61");
    assert_eq!(head_field(v61_frontier, "headX"), "70");
    assert_eq!(head_field(v61_frontier, "headSig"), "46");
    assert_eq!(head_field(v61_frontier, "headInterId"), "1377");
    assert_eq!(head_field(v61_frontier, "cAlias"), "h:70:LEFT:BOTTOM");
    assert_eq!(head_field(v61_frontier, "lastIndex"), "-1");
    assert_eq!(head_field(v61_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v61_frontier, "relations"), "1");
    assert_eq!(head_field(v61_frontier, "glyphs"), "2");
    assert_eq!(head_field(v61_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v61_frontier, "existingGlyph"), "glyph:332");
    assert_eq!(head_field(v61_frontier, "existingActive"), "true");
    assert_eq!(head_field(v61_frontier, "existingStem"), "2382");
    let v61_result = v61_rows[7];
    assert_eq!(head_field(v61_result, "headOrder"), "61");
    assert_eq!(head_field(v61_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v61_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v61_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v61_result, "addedVertices"), "-");
    assert_eq!(head_field(v61_result, "addedEdges"), "-");
    assert_eq!(head_field(v61_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v61_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v61_result, "linkerStateHashBefore"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(
        head_field(v61_result, "linkerStateHashAfter"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    let v61_java = v61_rows[8];
    assert_eq!(head_field(v61_java, "headOrder"), "61");
    assert_eq!(head_field(v61_java, "headX"), "70");
    assert_eq!(head_field(v61_java, "headSig"), "46");
    assert_eq!(head_field(v61_java, "headInterId"), "1377");
    assert_eq!(
        head_field(v61_java, "decisions"),
        "[LEFT:top=true:bottom=true:branch=Both,RIGHT:top=true:bottom=false:branch=TopOnly]"
    );
    assert_eq!(head_field(v61_java, "incident"), "-");
    assert_eq!(head_field(v61_java, "returned"), "false");
    assert_eq!(head_field(v61_java, "undefs"), "[LEFT]");
    assert_eq!(head_field(v61_java, "closureWrites"), "-");
    assert_eq!(head_field(v61_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v61_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v61_java, "sigEdgesBefore"), "697");
    assert_eq!(head_field(v61_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v61_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v61_java, "linkerStateHashBefore"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(
        head_field(v61_java, "linkerStateHashAfter"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(head_field(v61_java, "nextHeadOrder"), "62");
    assert_eq!(head_field(v61_java, "nextHeadX"), "9");
    assert_eq!(head_field(v61_java, "nextHeadSig"), "8");
    assert_eq!(head_field(v61_java, "nextHeadInterId"), "1301");
    assert_eq!(head_field(v61_java, "terminal"), "ReturnedBeforeNextHead");
    let v61_summary = v61_rows[9];
    assert_eq!(
        head_field(v61_summary, "schema"),
        "stems-head-phase-prefix-v61"
    );
    assert_eq!(head_field(v61_summary, "rows"), "9");
    assert_eq!(
        head_field(v61_summary, "baseV60RunnerSourceSha256"),
        "89c0d77c4e5829e6faf91179f801812bf97e27df06f0d50d9afcf4aaba63282b"
    );
    assert_eq!(
        head_field(v61_summary, "baseV60FixtureSha256"),
        "5e031afa32387b6b8a3f097d98b504b32c4216b62b1d67545fe76d3e05b39a28"
    );
    assert_eq!(
        head_field(v61_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v61_summary, "probeSourceSha256"),
        "51bfd41f3ece71a1a50ac1425d554e87c956819309c462dcf3444d5b2ecc5f41"
    );
    assert_eq!(
        head_field(v61_summary, "runnerSourceSha256"),
        "092987d133085a52c2782db96521258df39f05caefff69ec1a143c560ab3beed"
    );
    assert_eq!(
        head_field(v61_summary, "emittedBodySha256"),
        "9bd32daee863bed31b87f7d311d9fd1771cead3cd406b38990bfb3a09dd8d59b"
    );
    assert_eq!(
        head_field(v61_summary, "semanticPassSha256"),
        "0e2fe82a1093a61158da4854446070e3cd903f247d7b37b2bf87975db03f864b"
    );
    assert_eq!(head_field(v61_summary, "freshRuns"), "2");
    assert_eq!(head_field(v61_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v61_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder61OpenFrontier"
    );
    assert_eq!(
        head_field(v61_summary, "javaEvidence"),
        "ReturnedBeforeSixtySecondHead"
    );

    // Boundary 88 expected-only order62 existing-stem reconciliation.
    let v62_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v62.txt"),
    )
    .expect("expected-only order62 existing-stem fixture");
    assert_eq!(
        sha256_hex(v62_text.as_bytes()),
        "fa45b09cf2503942f7510482a4489ecaf7b3bb82b6d6efb76eb043c28b87889e"
    );
    let v62_rows = v62_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v62_rows.len(), 10);
    let v62_body = format!("{}\n", v62_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v62_body.as_bytes()),
        "95aab2234fb74d51b4014e5d13387746a13cbf4d32d85d380289b9c053a4958b"
    );
    let v62_frontier = v62_rows[6];
    assert_eq!(head_field(v62_frontier, "headOrder"), "62");
    assert_eq!(head_field(v62_frontier, "headX"), "9");
    assert_eq!(head_field(v62_frontier, "headSig"), "8");
    assert_eq!(head_field(v62_frontier, "headInterId"), "1301");
    assert_eq!(head_field(v62_frontier, "cAlias"), "h:9:LEFT:BOTTOM");
    assert_eq!(head_field(v62_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v62_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v62_frontier, "relations"), "4");
    assert_eq!(head_field(v62_frontier, "glyphs"), "1");
    assert_eq!(head_field(v62_frontier, "candidateIdBefore"), "318");
    assert_eq!(head_field(v62_frontier, "existingGlyph"), "glyph:318");
    assert_eq!(head_field(v62_frontier, "existingActive"), "true");
    assert_eq!(head_field(v62_frontier, "existingStem"), "2355");
    let v62_result = v62_rows[7];
    assert_eq!(head_field(v62_result, "headOrder"), "62");
    assert_eq!(head_field(v62_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v62_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v62_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v62_result, "addedVertices"), "-");
    assert_eq!(head_field(v62_result, "addedEdges"), "-");
    assert_eq!(head_field(v62_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v62_result, "linkerChanges"),
        "[h:10:LEFT:BOTTOM:true:false->true:true,h:10:LEFT:TOP:true:false->true:true,h:10:RIGHT:BOTTOM:false:false->false:true,h:10:RIGHT:TOP:false:false->false:true,linker:SLinker:head:10:false:false->false:true,linker:SLinker:head:10:true:false->true:true]"
    );
    assert_eq!(
        head_field(v62_result, "linkerStateHashBefore"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(
        head_field(v62_result, "linkerStateHashAfter"),
        "974f4eedb16cdeded1612521a81755844d5f6d4c36da8bebae7ccbcd61212fce"
    );
    let v62_java = v62_rows[8];
    assert_eq!(head_field(v62_java, "headOrder"), "62");
    assert_eq!(head_field(v62_java, "headX"), "9");
    assert_eq!(head_field(v62_java, "headSig"), "8");
    assert_eq!(head_field(v62_java, "headInterId"), "1301");
    assert_eq!(
        head_field(v62_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v62_java, "incident"),
        "[stem2355:headSideLEFT:heads[x9:sig8:id1301:sideLEFT,x10:sig9:id1303:sideLEFT]]"
    );
    assert_eq!(head_field(v62_java, "returned"), "true");
    assert_eq!(head_field(v62_java, "undefs"), "[]");
    assert_eq!(
        head_field(v62_java, "closureWrites"),
        "[x10:sig9:LEFT:false->true,x10:sig9:RIGHT:false->true]"
    );
    assert_eq!(head_field(v62_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v62_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v62_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v62_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v62_java, "linkerStateHashBefore"),
        "7e007175d69f6d740228eea4ee806a338aa39079de279a532581d0ff42517f80"
    );
    assert_eq!(
        head_field(v62_java, "linkerStateHashAfter"),
        "974f4eedb16cdeded1612521a81755844d5f6d4c36da8bebae7ccbcd61212fce"
    );
    assert_eq!(head_field(v62_java, "nextHeadOrder"), "63");
    assert_eq!(head_field(v62_java, "nextHeadX"), "41");
    assert_eq!(head_field(v62_java, "nextHeadSig"), "92");
    assert_eq!(head_field(v62_java, "nextHeadInterId"), "1473");
    assert_eq!(head_field(v62_java, "terminal"), "ReturnedBeforeNextHead");
    let v62_summary = v62_rows[9];
    assert_eq!(
        head_field(v62_summary, "schema"),
        "stems-head-phase-prefix-v62"
    );
    assert_eq!(head_field(v62_summary, "rows"), "9");
    assert_eq!(
        head_field(v62_summary, "baseV61RunnerSourceSha256"),
        "092987d133085a52c2782db96521258df39f05caefff69ec1a143c560ab3beed"
    );
    assert_eq!(
        head_field(v62_summary, "baseV61FixtureSha256"),
        "205b8875f6846384245629122645b6ac8cbef4bf18573c2ec539552d7799416d"
    );
    assert_eq!(
        head_field(v62_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v62_summary, "probeSourceSha256"),
        "88dc85c80fd04907171e86d24360e7db17b289c71bd57f9419a3e4d0db5d238c"
    );
    assert_eq!(
        head_field(v62_summary, "runnerSourceSha256"),
        "8a5762e8b8d569095af4d218211be7ad476ba6b9fb0105f757dd022bc0db4ad5"
    );
    assert_eq!(
        head_field(v62_summary, "emittedBodySha256"),
        "95aab2234fb74d51b4014e5d13387746a13cbf4d32d85d380289b9c053a4958b"
    );
    assert_eq!(
        head_field(v62_summary, "semanticPassSha256"),
        "769906a2f379937bc420142d97d003a7c5199aa19cd10ad55a5c468afa607b50"
    );
    assert_eq!(head_field(v62_summary, "freshRuns"), "2");
    assert_eq!(head_field(v62_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v62_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder62ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v62_summary, "javaEvidence"),
        "ReturnedBeforeSixtyThirdHead"
    );

    // Boundary 89 expected-only order63 existing-stem reconciliation.
    let v63_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v63.txt"),
    )
    .expect("expected-only order63 existing-stem fixture");
    assert_eq!(
        sha256_hex(v63_text.as_bytes()),
        "6f57b1db06a5319133c90cbcb88ddd316c6d6741cde0fb23025aab0ac31c7fcf"
    );
    let v63_rows = v63_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v63_rows.len(), 10);
    let v63_body = format!("{}\n", v63_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v63_body.as_bytes()),
        "63f2533e11886b1747e2a1543f35162e84628750b6ae783f045d1bb2e96fb56c"
    );
    let v63_frontier = v63_rows[6];
    assert_eq!(head_field(v63_frontier, "headOrder"), "63");
    assert_eq!(head_field(v63_frontier, "headX"), "41");
    assert_eq!(head_field(v63_frontier, "headSig"), "92");
    assert_eq!(head_field(v63_frontier, "headInterId"), "1473");
    assert_eq!(head_field(v63_frontier, "cAlias"), "h:41:LEFT:BOTTOM");
    assert_eq!(head_field(v63_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v63_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v63_frontier, "relations"), "3");
    assert_eq!(head_field(v63_frontier, "glyphs"), "2");
    assert_eq!(head_field(v63_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v63_frontier, "existingGlyph"), "glyph:293");
    assert_eq!(head_field(v63_frontier, "existingActive"), "true");
    assert_eq!(head_field(v63_frontier, "existingStem"), "2352");
    let v63_result = v63_rows[7];
    assert_eq!(head_field(v63_result, "headOrder"), "63");
    assert_eq!(head_field(v63_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v63_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v63_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v63_result, "addedVertices"), "-");
    assert_eq!(head_field(v63_result, "addedEdges"), "-");
    assert_eq!(head_field(v63_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v63_result, "linkerChanges"),
        "[h:42:LEFT:BOTTOM:true:false->true:true,h:42:LEFT:TOP:true:false->true:true,h:42:RIGHT:BOTTOM:false:false->false:true,h:42:RIGHT:TOP:false:false->false:true,linker:SLinker:head:42:false:false->false:true,linker:SLinker:head:42:true:false->true:true]"
    );
    assert_eq!(
        head_field(v63_result, "linkerStateHashBefore"),
        "974f4eedb16cdeded1612521a81755844d5f6d4c36da8bebae7ccbcd61212fce"
    );
    assert_eq!(
        head_field(v63_result, "linkerStateHashAfter"),
        "8486c51353d815719d6e79147ed298ae8ba130e7465667a89ce9938ed658f411"
    );
    let v63_java = v63_rows[8];
    assert_eq!(head_field(v63_java, "headOrder"), "63");
    assert_eq!(head_field(v63_java, "headX"), "41");
    assert_eq!(head_field(v63_java, "headSig"), "92");
    assert_eq!(head_field(v63_java, "headInterId"), "1473");
    assert_eq!(
        head_field(v63_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v63_java, "incident"),
        "[stem2352:headSideLEFT:heads[x41:sig92:id1473:sideLEFT,x42:sig93:id1475:sideLEFT]]"
    );
    assert_eq!(head_field(v63_java, "returned"), "true");
    assert_eq!(head_field(v63_java, "undefs"), "[]");
    assert_eq!(
        head_field(v63_java, "closureWrites"),
        "[x42:sig93:LEFT:false->true,x42:sig93:RIGHT:false->true]"
    );
    assert_eq!(head_field(v63_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v63_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v63_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v63_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v63_java, "linkerStateHashBefore"),
        "974f4eedb16cdeded1612521a81755844d5f6d4c36da8bebae7ccbcd61212fce"
    );
    assert_eq!(
        head_field(v63_java, "linkerStateHashAfter"),
        "8486c51353d815719d6e79147ed298ae8ba130e7465667a89ce9938ed658f411"
    );
    assert_eq!(head_field(v63_java, "nextHeadOrder"), "64");
    assert_eq!(head_field(v63_java, "nextHeadX"), "3");
    assert_eq!(head_field(v63_java, "nextHeadSig"), "6");
    assert_eq!(head_field(v63_java, "nextHeadInterId"), "1297");
    assert_eq!(head_field(v63_java, "terminal"), "ReturnedBeforeNextHead");
    let v63_summary = v63_rows[9];
    assert_eq!(
        head_field(v63_summary, "schema"),
        "stems-head-phase-prefix-v63"
    );
    assert_eq!(head_field(v63_summary, "rows"), "9");
    assert_eq!(
        head_field(v63_summary, "baseV62RunnerSourceSha256"),
        "8a5762e8b8d569095af4d218211be7ad476ba6b9fb0105f757dd022bc0db4ad5"
    );
    assert_eq!(
        head_field(v63_summary, "baseV62FixtureSha256"),
        "fa45b09cf2503942f7510482a4489ecaf7b3bb82b6d6efb76eb043c28b87889e"
    );
    assert_eq!(
        head_field(v63_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v63_summary, "probeSourceSha256"),
        "7fb7dc32d3be1bcce663116c8150dd436ec9860d831d825ccc8091fd2836c1c6"
    );
    assert_eq!(
        head_field(v63_summary, "runnerSourceSha256"),
        "d535607b178a298755a554bcc878cfd2bff32845960c5d39660a8ddc62992ece"
    );
    assert_eq!(
        head_field(v63_summary, "emittedBodySha256"),
        "63f2533e11886b1747e2a1543f35162e84628750b6ae783f045d1bb2e96fb56c"
    );
    assert_eq!(
        head_field(v63_summary, "semanticPassSha256"),
        "8cbf6614171048e920799629c16bcab2224b5bcf1fc25f8ab698f409c9b66109"
    );
    assert_eq!(head_field(v63_summary, "freshRuns"), "2");
    assert_eq!(head_field(v63_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v63_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder63ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v63_summary, "javaEvidence"),
        "ReturnedBeforeSixtyFourthHead"
    );

    // Boundary 90 expected-only order64 existing-stem reconciliation.
    let v64_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v64.txt"),
    )
    .expect("expected-only order64 existing-stem fixture");
    assert_eq!(
        sha256_hex(v64_text.as_bytes()),
        "2ef4ffd1e739b21998ee8a45652604557b42f41abe185407a9fd254d1e02ed5c"
    );
    let v64_rows = v64_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v64_rows.len(), 10);
    let v64_body = format!("{}\n", v64_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v64_body.as_bytes()),
        "7ce03e841a8f7b15f6af8e24b6db136e987f7ae902ca331a74cb1f5279380815"
    );
    let v64_frontier = v64_rows[6];
    assert_eq!(head_field(v64_frontier, "headOrder"), "64");
    assert_eq!(head_field(v64_frontier, "headX"), "3");
    assert_eq!(head_field(v64_frontier, "headSig"), "6");
    assert_eq!(head_field(v64_frontier, "headInterId"), "1297");
    assert_eq!(head_field(v64_frontier, "cAlias"), "h:3:LEFT:BOTTOM");
    assert_eq!(head_field(v64_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v64_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v64_frontier, "relations"), "2");
    assert_eq!(head_field(v64_frontier, "glyphs"), "2");
    assert_eq!(head_field(v64_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v64_frontier, "existingGlyph"), "glyph:315");
    assert_eq!(head_field(v64_frontier, "existingActive"), "true");
    assert_eq!(head_field(v64_frontier, "existingStem"), "2354");
    let v64_result = v64_rows[7];
    assert_eq!(head_field(v64_result, "headOrder"), "64");
    assert_eq!(head_field(v64_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v64_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v64_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v64_result, "addedVertices"), "-");
    assert_eq!(head_field(v64_result, "addedEdges"), "-");
    assert_eq!(head_field(v64_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v64_result, "linkerChanges"),
        "[h:4:LEFT:BOTTOM:true:false->true:true,h:4:LEFT:TOP:true:false->true:true,h:4:RIGHT:BOTTOM:false:false->false:true,h:4:RIGHT:TOP:false:false->false:true,linker:SLinker:head:4:false:false->false:true,linker:SLinker:head:4:true:false->true:true]"
    );
    assert_eq!(
        head_field(v64_result, "linkerStateHashBefore"),
        "8486c51353d815719d6e79147ed298ae8ba130e7465667a89ce9938ed658f411"
    );
    assert_eq!(
        head_field(v64_result, "linkerStateHashAfter"),
        "0ac9a43e97c1ed619978a0fa611808e7a1b445339b503e2ec8cb5823b0fd970e"
    );
    let v64_java = v64_rows[8];
    assert_eq!(head_field(v64_java, "headOrder"), "64");
    assert_eq!(head_field(v64_java, "headX"), "3");
    assert_eq!(head_field(v64_java, "headSig"), "6");
    assert_eq!(head_field(v64_java, "headInterId"), "1297");
    assert_eq!(
        head_field(v64_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v64_java, "incident"),
        "[stem2354:headSideLEFT:heads[x4:sig7:id1299:sideLEFT,x3:sig6:id1297:sideLEFT]]"
    );
    assert_eq!(head_field(v64_java, "returned"), "true");
    assert_eq!(head_field(v64_java, "undefs"), "[]");
    assert_eq!(
        head_field(v64_java, "closureWrites"),
        "[x4:sig7:LEFT:false->true,x4:sig7:RIGHT:false->true]"
    );
    assert_eq!(head_field(v64_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v64_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v64_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v64_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v64_java, "linkerStateHashBefore"),
        "8486c51353d815719d6e79147ed298ae8ba130e7465667a89ce9938ed658f411"
    );
    assert_eq!(
        head_field(v64_java, "linkerStateHashAfter"),
        "0ac9a43e97c1ed619978a0fa611808e7a1b445339b503e2ec8cb5823b0fd970e"
    );
    assert_eq!(head_field(v64_java, "nextHeadOrder"), "65");
    assert_eq!(head_field(v64_java, "nextHeadX"), "58");
    assert_eq!(head_field(v64_java, "nextHeadSig"), "73");
    assert_eq!(head_field(v64_java, "nextHeadInterId"), "1435");
    assert_eq!(head_field(v64_java, "terminal"), "ReturnedBeforeNextHead");
    let v64_summary = v64_rows[9];
    assert_eq!(
        head_field(v64_summary, "schema"),
        "stems-head-phase-prefix-v64"
    );
    assert_eq!(head_field(v64_summary, "rows"), "9");
    assert_eq!(
        head_field(v64_summary, "baseV63RunnerSourceSha256"),
        "d535607b178a298755a554bcc878cfd2bff32845960c5d39660a8ddc62992ece"
    );
    assert_eq!(
        head_field(v64_summary, "baseV63FixtureSha256"),
        "6f57b1db06a5319133c90cbcb88ddd316c6d6741cde0fb23025aab0ac31c7fcf"
    );
    assert_eq!(
        head_field(v64_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v64_summary, "probeSourceSha256"),
        "b00390eb82d61ed27dc4138e5286c63f2f3db7cf7a9767b729e5037b27070d53"
    );
    assert_eq!(
        head_field(v64_summary, "runnerSourceSha256"),
        "b9f580dbe3fdb53503ee6699a4ab314996bb0e1b176f19487fb08f810f11cd05"
    );
    assert_eq!(
        head_field(v64_summary, "emittedBodySha256"),
        "7ce03e841a8f7b15f6af8e24b6db136e987f7ae902ca331a74cb1f5279380815"
    );
    assert_eq!(
        head_field(v64_summary, "semanticPassSha256"),
        "6530c83d73fd70ad822e709561487eac3d1c8a565bde6300468e6a663396d27b"
    );
    assert_eq!(head_field(v64_summary, "freshRuns"), "2");
    assert_eq!(head_field(v64_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v64_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder64ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v64_summary, "javaEvidence"),
        "ReturnedBeforeSixtyFifthHead"
    );

    // Boundary 91 expected-only order65 existing-stem reconciliation.
    let v65_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v65.txt"),
    )
    .expect("expected-only order65 existing-stem fixture");
    assert_eq!(
        sha256_hex(v65_text.as_bytes()),
        "fff9e008f3caf1bce93f124bd649a33cc08572cc55371324f600691a6f866db5"
    );
    let v65_rows = v65_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v65_rows.len(), 10);
    let v65_body = format!("{}\n", v65_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v65_body.as_bytes()),
        "2d736c86cf6d5bd65a26ff4ec5455a78ada3bf535010fbe69e9b346228b7733a"
    );
    let v65_frontier = v65_rows[6];
    assert_eq!(head_field(v65_frontier, "headOrder"), "65");
    assert_eq!(head_field(v65_frontier, "headX"), "58");
    assert_eq!(head_field(v65_frontier, "headSig"), "73");
    assert_eq!(head_field(v65_frontier, "headInterId"), "1435");
    assert_eq!(head_field(v65_frontier, "cAlias"), "h:58:LEFT:BOTTOM");
    assert_eq!(head_field(v65_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v65_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v65_frontier, "relations"), "1");
    assert_eq!(head_field(v65_frontier, "glyphs"), "1");
    assert_eq!(head_field(v65_frontier, "candidateIdBefore"), "311");
    assert_eq!(head_field(v65_frontier, "existingGlyph"), "glyph:311");
    assert_eq!(head_field(v65_frontier, "existingActive"), "true");
    assert_eq!(head_field(v65_frontier, "existingStem"), "2363");
    let v65_result = v65_rows[7];
    assert_eq!(head_field(v65_result, "headOrder"), "65");
    assert_eq!(head_field(v65_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v65_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v65_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v65_result, "addedVertices"), "-");
    assert_eq!(head_field(v65_result, "addedEdges"), "-");
    assert_eq!(head_field(v65_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v65_result, "linkerChanges"),
        "[h:59:LEFT:BOTTOM:true:false->true:true,h:59:LEFT:TOP:true:false->true:true,h:59:RIGHT:BOTTOM:false:false->false:true,h:59:RIGHT:TOP:false:false->false:true,linker:SLinker:head:59:false:false->false:true,linker:SLinker:head:59:true:false->true:true]"
    );
    assert_eq!(
        head_field(v65_result, "linkerStateHashBefore"),
        "0ac9a43e97c1ed619978a0fa611808e7a1b445339b503e2ec8cb5823b0fd970e"
    );
    assert_eq!(
        head_field(v65_result, "linkerStateHashAfter"),
        "9533685653b5eebaa6c39c2a8adc75622bb614e04eca22a55c4fda1bc149ede5"
    );
    let v65_java = v65_rows[8];
    assert_eq!(head_field(v65_java, "headOrder"), "65");
    assert_eq!(head_field(v65_java, "headX"), "58");
    assert_eq!(head_field(v65_java, "headSig"), "73");
    assert_eq!(head_field(v65_java, "headInterId"), "1435");
    assert_eq!(
        head_field(v65_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v65_java, "incident"),
        "[stem2363:headSideLEFT:heads[x58:sig73:id1435:sideLEFT,x59:sig74:id1437:sideLEFT]]"
    );
    assert_eq!(head_field(v65_java, "returned"), "true");
    assert_eq!(head_field(v65_java, "undefs"), "[]");
    assert_eq!(
        head_field(v65_java, "closureWrites"),
        "[x59:sig74:LEFT:false->true,x59:sig74:RIGHT:false->true]"
    );
    assert_eq!(head_field(v65_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v65_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v65_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v65_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v65_java, "linkerStateHashBefore"),
        "0ac9a43e97c1ed619978a0fa611808e7a1b445339b503e2ec8cb5823b0fd970e"
    );
    assert_eq!(
        head_field(v65_java, "linkerStateHashAfter"),
        "9533685653b5eebaa6c39c2a8adc75622bb614e04eca22a55c4fda1bc149ede5"
    );
    assert_eq!(head_field(v65_java, "nextHeadOrder"), "66");
    assert_eq!(head_field(v65_java, "nextHeadX"), "13");
    assert_eq!(head_field(v65_java, "nextHeadSig"), "0");
    assert_eq!(head_field(v65_java, "nextHeadInterId"), "1285");
    assert_eq!(head_field(v65_java, "terminal"), "ReturnedBeforeNextHead");
    let v65_summary = v65_rows[9];
    assert_eq!(
        head_field(v65_summary, "schema"),
        "stems-head-phase-prefix-v65"
    );
    assert_eq!(head_field(v65_summary, "rows"), "9");
    assert_eq!(
        head_field(v65_summary, "baseV64RunnerSourceSha256"),
        "b9f580dbe3fdb53503ee6699a4ab314996bb0e1b176f19487fb08f810f11cd05"
    );
    assert_eq!(
        head_field(v65_summary, "baseV64FixtureSha256"),
        "2ef4ffd1e739b21998ee8a45652604557b42f41abe185407a9fd254d1e02ed5c"
    );
    assert_eq!(
        head_field(v65_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v65_summary, "probeSourceSha256"),
        "6e24c3b1967f73d453ca4303ff7bc19594a4cccc102776519fd10e37ca728593"
    );
    assert_eq!(
        head_field(v65_summary, "runnerSourceSha256"),
        "427bf9b0703059e8e31df582cf8c11b512c328717a4ef60bd6fcad5775855f3a"
    );
    assert_eq!(
        head_field(v65_summary, "emittedBodySha256"),
        "2d736c86cf6d5bd65a26ff4ec5455a78ada3bf535010fbe69e9b346228b7733a"
    );
    assert_eq!(
        head_field(v65_summary, "semanticPassSha256"),
        "bc76c0ab51b6b65ca0bfaa13a290a67c50b406934ee8d0eed678b74354bfd13b"
    );
    assert_eq!(head_field(v65_summary, "freshRuns"), "2");
    assert_eq!(head_field(v65_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v65_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder65ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v65_summary, "javaEvidence"),
        "ReturnedBeforeSixtySixthHead"
    );

    // Boundary 92 expected-only order66 existing-stem reconciliation.
    let v66_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v66.txt"),
    )
    .expect("expected-only order66 existing-stem fixture");
    assert_eq!(
        sha256_hex(v66_text.as_bytes()),
        "2f1778c60a1beb687eddc90b6d5cc340f9d863ff4610d4664a1f7d6211079eb5"
    );
    let v66_rows = v66_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v66_rows.len(), 10);
    let v66_body = format!("{}\n", v66_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v66_body.as_bytes()),
        "474ea958d9d27bdb9183a2357273137a4562ed3c97d2ab968b196e4728f5701f"
    );
    let v66_frontier = v66_rows[6];
    assert_eq!(head_field(v66_frontier, "headOrder"), "66");
    assert_eq!(head_field(v66_frontier, "headX"), "13");
    assert_eq!(head_field(v66_frontier, "headSig"), "0");
    assert_eq!(head_field(v66_frontier, "headInterId"), "1285");
    assert_eq!(head_field(v66_frontier, "cAlias"), "h:13:LEFT:BOTTOM");
    assert_eq!(head_field(v66_frontier, "lastIndex"), "4");
    assert_eq!(head_field(v66_frontier, "maxIndex"), "4");
    assert_eq!(head_field(v66_frontier, "relations"), "4");
    assert_eq!(head_field(v66_frontier, "glyphs"), "2");
    assert_eq!(head_field(v66_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v66_frontier, "existingGlyph"), "glyph:294");
    assert_eq!(head_field(v66_frontier, "existingActive"), "true");
    assert_eq!(head_field(v66_frontier, "existingStem"), "2340");
    let v66_result = v66_rows[7];
    assert_eq!(head_field(v66_result, "headOrder"), "66");
    assert_eq!(head_field(v66_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v66_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v66_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v66_result, "addedVertices"), "-");
    assert_eq!(head_field(v66_result, "addedEdges"), "-");
    assert_eq!(head_field(v66_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v66_result, "linkerChanges"),
        "[h:14:LEFT:BOTTOM:true:false->true:true,h:14:LEFT:TOP:true:false->true:true,h:14:RIGHT:BOTTOM:false:false->false:true,h:14:RIGHT:TOP:false:false->false:true,linker:SLinker:head:14:false:false->false:true,linker:SLinker:head:14:true:false->true:true]"
    );
    assert_eq!(
        head_field(v66_result, "linkerStateHashBefore"),
        "9533685653b5eebaa6c39c2a8adc75622bb614e04eca22a55c4fda1bc149ede5"
    );
    assert_eq!(
        head_field(v66_result, "linkerStateHashAfter"),
        "cc74b03dd006d9b7402f849f8e7065b2cd05635ca1980b0bc9b732083cecba28"
    );
    let v66_java = v66_rows[8];
    assert_eq!(head_field(v66_java, "headOrder"), "66");
    assert_eq!(head_field(v66_java, "headX"), "13");
    assert_eq!(head_field(v66_java, "headSig"), "0");
    assert_eq!(head_field(v66_java, "headInterId"), "1285");
    assert_eq!(
        head_field(v66_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v66_java, "incident"),
        "[stem2340:headSideLEFT:heads[x13:sig0:id1285:sideLEFT,x14:sig1:id1287:sideLEFT]]"
    );
    assert_eq!(head_field(v66_java, "returned"), "true");
    assert_eq!(head_field(v66_java, "undefs"), "[]");
    assert_eq!(
        head_field(v66_java, "closureWrites"),
        "[x14:sig1:LEFT:false->true,x14:sig1:RIGHT:false->true]"
    );
    assert_eq!(head_field(v66_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v66_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v66_java, "sigEdgesAfter"), "697");
    assert_eq!(head_field(v66_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v66_java, "linkerStateHashBefore"),
        "9533685653b5eebaa6c39c2a8adc75622bb614e04eca22a55c4fda1bc149ede5"
    );
    assert_eq!(
        head_field(v66_java, "linkerStateHashAfter"),
        "cc74b03dd006d9b7402f849f8e7065b2cd05635ca1980b0bc9b732083cecba28"
    );
    assert_eq!(head_field(v66_java, "nextHeadOrder"), "67");
    assert_eq!(head_field(v66_java, "nextHeadX"), "73");
    assert_eq!(head_field(v66_java, "nextHeadSig"), "18");
    assert_eq!(head_field(v66_java, "nextHeadInterId"), "1321");
    assert_eq!(head_field(v66_java, "terminal"), "ReturnedBeforeNextHead");
    let v66_summary = v66_rows[9];
    assert_eq!(
        head_field(v66_summary, "schema"),
        "stems-head-phase-prefix-v66"
    );
    assert_eq!(head_field(v66_summary, "rows"), "9");
    assert_eq!(
        head_field(v66_summary, "baseV65RunnerSourceSha256"),
        "427bf9b0703059e8e31df582cf8c11b512c328717a4ef60bd6fcad5775855f3a"
    );
    assert_eq!(
        head_field(v66_summary, "baseV65FixtureSha256"),
        "fff9e008f3caf1bce93f124bd649a33cc08572cc55371324f600691a6f866db5"
    );
    assert_eq!(
        head_field(v66_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v66_summary, "probeSourceSha256"),
        "afecc8507ba75b9437767c1f24d5d51d1e2db4c777a27cb579f164c5290dd04f"
    );
    assert_eq!(
        head_field(v66_summary, "runnerSourceSha256"),
        "a72d2a1657bdb32f4e179d9bc633a2830e98b2c4e672a7f0faed4562f9955e04"
    );
    assert_eq!(
        head_field(v66_summary, "emittedBodySha256"),
        "474ea958d9d27bdb9183a2357273137a4562ed3c97d2ab968b196e4728f5701f"
    );
    assert_eq!(
        head_field(v66_summary, "semanticPassSha256"),
        "54f1644573ea67bc14ad80ad008e46374570b5e7764f914b46d9d2c1a1cbe66a"
    );
    assert_eq!(head_field(v66_summary, "freshRuns"), "2");
    assert_eq!(head_field(v66_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v66_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder66ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v66_summary, "javaEvidence"),
        "ReturnedBeforeSixtySeventhHead"
    );

    // Boundary 93 expected-only order67 multi-head reuse C-link.
    let v67_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v67.txt"),
    )
    .expect("expected-only order67 multi-head reuse fixture");
    assert_eq!(
        sha256_hex(v67_text.as_bytes()),
        "0f7c0e68fbba2ff4bae8fd5f69218c829e4969892966a8baab03eec4aff03d9f"
    );
    let v67_rows = v67_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v67_rows.len(), 10);
    let v67_body = format!("{}\n", v67_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v67_body.as_bytes()),
        "5ccc17ba83717e8fa53095633924581d51d5862d1489315493c4b560f6ebc12e"
    );
    let v67_frontier = v67_rows[6];
    assert_eq!(head_field(v67_frontier, "headOrder"), "67");
    assert_eq!(head_field(v67_frontier, "headX"), "73");
    assert_eq!(head_field(v67_frontier, "headSig"), "18");
    assert_eq!(head_field(v67_frontier, "headInterId"), "1321");
    assert_eq!(head_field(v67_frontier, "cAlias"), "h:73:LEFT:BOTTOM");
    assert_eq!(head_field(v67_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v67_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v67_frontier, "relations"), "3");
    assert_eq!(head_field(v67_frontier, "glyphs"), "2");
    assert_eq!(head_field(v67_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v67_frontier, "existingGlyph"), "glyph:332");
    assert_eq!(head_field(v67_frontier, "existingActive"), "true");
    assert_eq!(head_field(v67_frontier, "existingStem"), "2382");
    let v67_result = v67_rows[7];
    assert_eq!(head_field(v67_result, "headOrder"), "67");
    assert_eq!(head_field(v67_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v67_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v67_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v67_result, "addedVertices"), "-");
    assert_eq!(head_field(v67_result, "addedSystemStems"), "-");
    let v67_added = head_field(v67_result, "addedEdges");
    assert!(v67_added.starts_with(
        "[system1:sourceId1321:targetId2382:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.a0061ea4a6caep-1/3fea0061ea4a6cae"
    ));
    assert!(v67_added.contains("sourceId1377:targetId2382"));
    assert!(v67_added.contains("sourceId1387:targetId2382"));
    assert!(v67_added.contains("grade=0x1.c8e766ba61044p-1/3fec8e766ba61044"));
    assert_eq!(
        head_field(v67_result, "linkerChanges"),
        "[h:70:LEFT:BOTTOM:false:false->true:true,h:70:LEFT:TOP:false:false->true:true,h:70:RIGHT:BOTTOM:false:false->false:true,h:70:RIGHT:TOP:false:false->false:true,h:71:LEFT:BOTTOM:false:false->true:true,h:71:LEFT:TOP:false:false->true:true,h:71:RIGHT:BOTTOM:false:false->false:true,h:71:RIGHT:TOP:false:false->false:true,h:73:LEFT:BOTTOM:false:false->true:false,h:73:LEFT:TOP:false:false->true:false,h:74:LEFT:BOTTOM:true:false->true:true,h:74:LEFT:TOP:true:false->true:true,h:74:RIGHT:BOTTOM:false:false->false:true,h:74:RIGHT:TOP:false:false->false:true,linker:SLinker:head:70:false:false->false:true,linker:SLinker:head:70:false:false->true:true,linker:SLinker:head:71:false:false->false:true,linker:SLinker:head:71:false:false->true:true,linker:SLinker:head:73:false:false->true:false,linker:SLinker:head:74:false:false->false:true,linker:SLinker:head:74:true:false->true:true]"
    );
    assert_eq!(
        head_field(v67_result, "linkerStateHashBefore"),
        "cc74b03dd006d9b7402f849f8e7065b2cd05635ca1980b0bc9b732083cecba28"
    );
    assert_eq!(
        head_field(v67_result, "linkerStateHashAfter"),
        "6463417c29069adceaa804975dce202bdde97a42df7a9bc63f1007e2a52668ce"
    );
    let v67_java = v67_rows[8];
    assert_eq!(head_field(v67_java, "headOrder"), "67");
    assert_eq!(head_field(v67_java, "headX"), "73");
    assert_eq!(head_field(v67_java, "headSig"), "18");
    assert_eq!(head_field(v67_java, "headInterId"), "1321");
    assert_eq!(
        head_field(v67_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v67_java, "incident"), "-");
    assert_eq!(head_field(v67_java, "returned"), "true");
    assert_eq!(head_field(v67_java, "undefs"), "[]");
    assert_eq!(head_field(v67_java, "closureWrites"), "-");
    assert_eq!(head_field(v67_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v67_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v67_java, "sigEdgesBefore"), "697");
    assert_eq!(head_field(v67_java, "sigEdgesAfter"), "700");
    assert_eq!(head_field(v67_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v67_java, "nextHeadOrder"), "68");
    assert_eq!(head_field(v67_java, "nextHeadX"), "0");
    assert_eq!(head_field(v67_java, "nextHeadSig"), "51");
    assert_eq!(head_field(v67_java, "nextHeadInterId"), "1390");
    assert_eq!(head_field(v67_java, "terminal"), "ReturnedBeforeNextHead");
    let v67_summary = v67_rows[9];
    assert_eq!(
        head_field(v67_summary, "schema"),
        "stems-head-phase-prefix-v67"
    );
    assert_eq!(head_field(v67_summary, "rows"), "9");
    assert_eq!(
        head_field(v67_summary, "baseV66RunnerSourceSha256"),
        "a72d2a1657bdb32f4e179d9bc633a2830e98b2c4e672a7f0faed4562f9955e04"
    );
    assert_eq!(
        head_field(v67_summary, "baseV66FixtureSha256"),
        "2f1778c60a1beb687eddc90b6d5cc340f9d863ff4610d4664a1f7d6211079eb5"
    );
    assert_eq!(
        head_field(v67_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v67_summary, "probeSourceSha256"),
        "7d1ee5b0897fe15e67a4c1c4bfe079eb6b073f687e35e38a005013ed1c07bc2f"
    );
    assert_eq!(
        head_field(v67_summary, "runnerSourceSha256"),
        "0e7656dc064aedfb9cbbab018630198294c83efec005a6451f0d11938e213a5e"
    );
    assert_eq!(
        head_field(v67_summary, "emittedBodySha256"),
        "5ccc17ba83717e8fa53095633924581d51d5862d1489315493c4b560f6ebc12e"
    );
    assert_eq!(
        head_field(v67_summary, "semanticPassSha256"),
        "3be5bc471b46f5d1157fe7c2c81d81f2ad646403a0b554e8d6df179cbc699098"
    );
    assert_eq!(head_field(v67_summary, "freshRuns"), "2");
    assert_eq!(head_field(v67_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v67_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder67MultiHeadReuseCLink"
    );
    assert_eq!(
        head_field(v67_summary, "javaEvidence"),
        "ReturnedBeforeSixtyEighthHead"
    );

    // Boundary 94 expected-only order68 open/undefined frontier.
    let v68_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v68.txt"),
    )
    .expect("expected-only order68 open frontier fixture");
    assert_eq!(
        sha256_hex(v68_text.as_bytes()),
        "b6be7975fc45961e8d5ff869151c6b6fc03ceac63dd13cefb700c05a75d05e48"
    );
    let v68_rows = v68_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v68_rows.len(), 10);
    let v68_body = format!("{}\n", v68_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v68_body.as_bytes()),
        "4f4d96dc7ce313a516606ae08d2d4a7be831e139cb10a7fccdfea75c5e24f89b"
    );
    let v68_frontier = v68_rows[6];
    assert_eq!(head_field(v68_frontier, "headOrder"), "68");
    assert_eq!(head_field(v68_frontier, "headX"), "0");
    assert_eq!(head_field(v68_frontier, "headSig"), "51");
    assert_eq!(head_field(v68_frontier, "headInterId"), "1390");
    assert_eq!(head_field(v68_frontier, "cAlias"), "h:0:LEFT:BOTTOM");
    assert_eq!(head_field(v68_frontier, "lastIndex"), "-1");
    assert_eq!(head_field(v68_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v68_frontier, "relations"), "1");
    assert_eq!(head_field(v68_frontier, "glyphs"), "2");
    assert_eq!(head_field(v68_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v68_frontier, "existingGlyph"), "glyph:322");
    assert_eq!(head_field(v68_frontier, "existingActive"), "true");
    assert_eq!(head_field(v68_frontier, "existingStem"), "2384");
    let v68_result = v68_rows[7];
    assert_eq!(head_field(v68_result, "headOrder"), "68");
    assert_eq!(head_field(v68_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v68_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v68_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v68_result, "addedVertices"), "-");
    assert_eq!(head_field(v68_result, "addedEdges"), "-");
    assert_eq!(head_field(v68_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v68_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v68_result, "linkerStateHashBefore"),
        "6463417c29069adceaa804975dce202bdde97a42df7a9bc63f1007e2a52668ce"
    );
    assert_eq!(
        head_field(v68_result, "linkerStateHashAfter"),
        "6463417c29069adceaa804975dce202bdde97a42df7a9bc63f1007e2a52668ce"
    );
    let v68_java = v68_rows[8];
    assert_eq!(head_field(v68_java, "headOrder"), "68");
    assert_eq!(head_field(v68_java, "headX"), "0");
    assert_eq!(head_field(v68_java, "headSig"), "51");
    assert_eq!(head_field(v68_java, "headInterId"), "1390");
    assert_eq!(
        head_field(v68_java, "decisions"),
        "[LEFT:top=true:bottom=true:branch=Both,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v68_java, "incident"), "-");
    assert_eq!(head_field(v68_java, "returned"), "false");
    assert_eq!(head_field(v68_java, "undefs"), "[LEFT]");
    assert_eq!(head_field(v68_java, "closureWrites"), "-");
    assert_eq!(head_field(v68_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v68_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v68_java, "sigEdgesBefore"), "700");
    assert_eq!(head_field(v68_java, "sigEdgesAfter"), "700");
    assert_eq!(head_field(v68_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v68_java, "nextHeadOrder"), "69");
    assert_eq!(head_field(v68_java, "nextHeadX"), "87");
    assert_eq!(head_field(v68_java, "nextHeadSig"), "83");
    assert_eq!(head_field(v68_java, "nextHeadInterId"), "1455");
    assert_eq!(head_field(v68_java, "terminal"), "ReturnedBeforeNextHead");
    let v68_summary = v68_rows[9];
    assert_eq!(
        head_field(v68_summary, "schema"),
        "stems-head-phase-prefix-v68"
    );
    assert_eq!(head_field(v68_summary, "rows"), "9");
    assert_eq!(
        head_field(v68_summary, "baseV67RunnerSourceSha256"),
        "0e7656dc064aedfb9cbbab018630198294c83efec005a6451f0d11938e213a5e"
    );
    assert_eq!(
        head_field(v68_summary, "baseV67FixtureSha256"),
        "0f7c0e68fbba2ff4bae8fd5f69218c829e4969892966a8baab03eec4aff03d9f"
    );
    assert_eq!(
        head_field(v68_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v68_summary, "probeSourceSha256"),
        "453e6f4fc952f0b0b12e1393ff8c369212b26c964ac2e8b13a95ae72c607e21b"
    );
    assert_eq!(
        head_field(v68_summary, "runnerSourceSha256"),
        "0316c6aa084df10326c34f886443420360b82e81918d935ca0da7187b9f4acbf"
    );
    assert_eq!(
        head_field(v68_summary, "emittedBodySha256"),
        "4f4d96dc7ce313a516606ae08d2d4a7be831e139cb10a7fccdfea75c5e24f89b"
    );
    assert_eq!(
        head_field(v68_summary, "semanticPassSha256"),
        "b74ac75ff823ea362fa18d17553639b9e81ee2ff5aa029bc42dabe7a7e4d376c"
    );
    assert_eq!(head_field(v68_summary, "freshRuns"), "2");
    assert_eq!(head_field(v68_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v68_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder68OpenFrontier"
    );
    assert_eq!(
        head_field(v68_summary, "javaEvidence"),
        "ReturnedBeforeSixtyNinthHead"
    );

    // Boundary 95 expected-only order69 existing-stem reconciliation.
    let v69_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v69.txt"),
    )
    .expect("expected-only order69 existing-stem fixture");
    assert_eq!(
        sha256_hex(v69_text.as_bytes()),
        "fff308360cc99c0039fc973b5c112ba68e1b5f24ff422461214cfa580063dc4b"
    );
    let v69_rows = v69_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v69_rows.len(), 10);
    let v69_body = format!("{}\n", v69_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v69_body.as_bytes()),
        "a6404babf109901251274e03d38c9e7d79c53f9f1df29b2d96442d520e9b985c"
    );
    let v69_frontier = v69_rows[6];
    assert_eq!(head_field(v69_frontier, "headOrder"), "69");
    assert_eq!(head_field(v69_frontier, "headX"), "87");
    assert_eq!(head_field(v69_frontier, "headSig"), "83");
    assert_eq!(head_field(v69_frontier, "headInterId"), "1455");
    assert_eq!(head_field(v69_frontier, "cAlias"), "h:87:LEFT:BOTTOM");
    assert_eq!(head_field(v69_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v69_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v69_frontier, "relations"), "1");
    assert_eq!(head_field(v69_frontier, "glyphs"), "1");
    assert_eq!(head_field(v69_frontier, "candidateIdBefore"), "295");
    assert_eq!(head_field(v69_frontier, "existingGlyph"), "glyph:295");
    assert_eq!(head_field(v69_frontier, "existingActive"), "true");
    assert_eq!(head_field(v69_frontier, "existingStem"), "2367");
    let v69_result = v69_rows[7];
    assert_eq!(head_field(v69_result, "headOrder"), "69");
    assert_eq!(head_field(v69_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v69_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v69_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v69_result, "addedVertices"), "-");
    assert_eq!(head_field(v69_result, "addedEdges"), "-");
    assert_eq!(head_field(v69_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v69_result, "linkerChanges"),
        "[h:88:LEFT:BOTTOM:true:false->true:true,h:88:LEFT:TOP:true:false->true:true,h:88:RIGHT:BOTTOM:false:false->false:true,h:88:RIGHT:TOP:false:false->false:true,linker:SLinker:head:88:false:false->false:true,linker:SLinker:head:88:true:false->true:true]"
    );
    assert_eq!(
        head_field(v69_result, "linkerStateHashBefore"),
        "6463417c29069adceaa804975dce202bdde97a42df7a9bc63f1007e2a52668ce"
    );
    assert_eq!(
        head_field(v69_result, "linkerStateHashAfter"),
        "07d58ecbcdff4104336dcdddb6bb3848425af95f590722ea814c9d3dddd99c2a"
    );
    let v69_java = v69_rows[8];
    assert_eq!(head_field(v69_java, "headOrder"), "69");
    assert_eq!(head_field(v69_java, "headX"), "87");
    assert_eq!(head_field(v69_java, "headSig"), "83");
    assert_eq!(head_field(v69_java, "headInterId"), "1455");
    assert_eq!(
        head_field(v69_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v69_java, "incident"),
        "[stem2367:headSideLEFT:heads[x87:sig83:id1455:sideLEFT,x88:sig84:id1457:sideLEFT]]"
    );
    assert_eq!(head_field(v69_java, "returned"), "true");
    assert_eq!(head_field(v69_java, "undefs"), "[]");
    assert_eq!(
        head_field(v69_java, "closureWrites"),
        "[x88:sig84:LEFT:false->true,x88:sig84:RIGHT:false->true]"
    );
    assert_eq!(head_field(v69_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v69_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v69_java, "sigEdgesAfter"), "700");
    assert_eq!(head_field(v69_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v69_java, "nextHeadOrder"), "70");
    assert_eq!(head_field(v69_java, "nextHeadX"), "1");
    assert_eq!(head_field(v69_java, "nextHeadSig"), "35");
    assert_eq!(head_field(v69_java, "nextHeadInterId"), "1355");
    assert_eq!(head_field(v69_java, "terminal"), "ReturnedBeforeNextHead");
    let v69_summary = v69_rows[9];
    assert_eq!(
        head_field(v69_summary, "schema"),
        "stems-head-phase-prefix-v69"
    );
    assert_eq!(head_field(v69_summary, "rows"), "9");
    assert_eq!(
        head_field(v69_summary, "baseV68RunnerSourceSha256"),
        "0316c6aa084df10326c34f886443420360b82e81918d935ca0da7187b9f4acbf"
    );
    assert_eq!(
        head_field(v69_summary, "baseV68FixtureSha256"),
        "b6be7975fc45961e8d5ff869151c6b6fc03ceac63dd13cefb700c05a75d05e48"
    );
    assert_eq!(
        head_field(v69_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v69_summary, "probeSourceSha256"),
        "3ae74f7a8aeda9808198f2cbbb3f8fec083533e394d33dd652a756bf1466223b"
    );
    assert_eq!(
        head_field(v69_summary, "runnerSourceSha256"),
        "6b1277bfd4169afb25a94034e52a31e938dcac3f02c5b98df5e0e3e383f4a40e"
    );
    assert_eq!(
        head_field(v69_summary, "emittedBodySha256"),
        "a6404babf109901251274e03d38c9e7d79c53f9f1df29b2d96442d520e9b985c"
    );
    assert_eq!(
        head_field(v69_summary, "semanticPassSha256"),
        "0443da7a172439769b2903f52cd479bab031f464ce4009eee0ac7b5c618e401c"
    );
    assert_eq!(head_field(v69_summary, "freshRuns"), "2");
    assert_eq!(head_field(v69_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v69_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder69ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v69_summary, "javaEvidence"),
        "ReturnedBeforeSeventiethHead"
    );

    // Boundary 96 expected-only order70 multi-head reuse C-link.
    let v70_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v70.txt"),
    )
    .expect("expected-only order70 multi-head reuse fixture");
    assert_eq!(
        sha256_hex(v70_text.as_bytes()),
        "cf883421cfd392f124598143d6d48e83019c9ddc6cfd416ccc1a676b9558e142"
    );
    let v70_rows = v70_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v70_rows.len(), 10);
    let v70_body = format!("{}\n", v70_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v70_body.as_bytes()),
        "9664dbd5557ea8b79eec70373fe168d920ef115ca5a41e11cdf7275590dcea05"
    );
    let v70_frontier = v70_rows[6];
    assert_eq!(head_field(v70_frontier, "headOrder"), "70");
    assert_eq!(head_field(v70_frontier, "headX"), "1");
    assert_eq!(head_field(v70_frontier, "headSig"), "35");
    assert_eq!(head_field(v70_frontier, "headInterId"), "1355");
    assert_eq!(head_field(v70_frontier, "cAlias"), "h:1:LEFT:BOTTOM");
    assert_eq!(head_field(v70_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v70_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v70_frontier, "relations"), "2");
    assert_eq!(head_field(v70_frontier, "glyphs"), "2");
    assert_eq!(head_field(v70_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v70_frontier, "existingGlyph"), "glyph:322");
    assert_eq!(head_field(v70_frontier, "existingActive"), "true");
    assert_eq!(head_field(v70_frontier, "existingStem"), "2384");
    let v70_result = v70_rows[7];
    assert_eq!(head_field(v70_result, "headOrder"), "70");
    assert_eq!(head_field(v70_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v70_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v70_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v70_result, "addedVertices"), "-");
    assert_eq!(head_field(v70_result, "addedSystemStems"), "-");
    let v70_added = head_field(v70_result, "addedEdges");
    assert!(v70_added.starts_with(
        "[system1:sourceId1355:targetId2384:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.935961f369da6p-1/3fe935961f369da6"
    ));
    assert!(v70_added.contains("sourceId1390:targetId2384"));
    assert!(v70_added.contains("grade=0x1.bcd262ddce1b1p-1/3febcd262ddce1b1"));
    assert_eq!(
        head_field(v70_result, "linkerChanges"),
        "[h:0:LEFT:BOTTOM:false:false->true:true,h:0:LEFT:TOP:false:false->true:true,h:0:RIGHT:BOTTOM:false:false->false:true,h:0:RIGHT:TOP:false:false->false:true,h:1:LEFT:BOTTOM:false:false->true:false,h:1:LEFT:TOP:false:false->true:false,h:2:LEFT:BOTTOM:true:false->true:true,h:2:LEFT:TOP:true:false->true:true,h:2:RIGHT:BOTTOM:false:false->false:true,h:2:RIGHT:TOP:false:false->false:true,linker:SLinker:head:0:false:false->false:true,linker:SLinker:head:0:false:false->true:true,linker:SLinker:head:1:false:false->true:false,linker:SLinker:head:2:false:false->false:true,linker:SLinker:head:2:true:false->true:true]"
    );
    assert_eq!(
        head_field(v70_result, "linkerStateHashBefore"),
        "07d58ecbcdff4104336dcdddb6bb3848425af95f590722ea814c9d3dddd99c2a"
    );
    assert_eq!(
        head_field(v70_result, "linkerStateHashAfter"),
        "9598d2170824a69b15d1035e1ac51fb3ff60ab3116a26c02019d1174548bbeee"
    );
    let v70_java = v70_rows[8];
    assert_eq!(head_field(v70_java, "headOrder"), "70");
    assert_eq!(head_field(v70_java, "headX"), "1");
    assert_eq!(head_field(v70_java, "headSig"), "35");
    assert_eq!(head_field(v70_java, "headInterId"), "1355");
    assert_eq!(
        head_field(v70_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v70_java, "incident"), "-");
    assert_eq!(head_field(v70_java, "returned"), "true");
    assert_eq!(head_field(v70_java, "undefs"), "[]");
    assert_eq!(head_field(v70_java, "closureWrites"), "-");
    assert_eq!(head_field(v70_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v70_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v70_java, "sigEdgesBefore"), "700");
    assert_eq!(head_field(v70_java, "sigEdgesAfter"), "702");
    assert_eq!(head_field(v70_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v70_java, "nextHeadOrder"), "71");
    assert_eq!(head_field(v70_java, "nextHeadX"), "77");
    assert_eq!(head_field(v70_java, "nextHeadSig"), "38");
    assert_eq!(head_field(v70_java, "nextHeadInterId"), "1361");
    assert_eq!(head_field(v70_java, "terminal"), "ReturnedBeforeNextHead");
    let v70_summary = v70_rows[9];
    assert_eq!(
        head_field(v70_summary, "schema"),
        "stems-head-phase-prefix-v70"
    );
    assert_eq!(head_field(v70_summary, "rows"), "9");
    assert_eq!(
        head_field(v70_summary, "baseV69RunnerSourceSha256"),
        "6b1277bfd4169afb25a94034e52a31e938dcac3f02c5b98df5e0e3e383f4a40e"
    );
    assert_eq!(
        head_field(v70_summary, "baseV69FixtureSha256"),
        "fff308360cc99c0039fc973b5c112ba68e1b5f24ff422461214cfa580063dc4b"
    );
    assert_eq!(
        head_field(v70_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v70_summary, "probeSourceSha256"),
        "60e32fa68785b1e18605107f0ba4503b6b5a437c8a617ad7d97148f76aec1f00"
    );
    assert_eq!(
        head_field(v70_summary, "runnerSourceSha256"),
        "75be33c3f6bf1e2eae337d9808f967172e7633ffad7fe48e7c48441d7e0740be"
    );
    assert_eq!(
        head_field(v70_summary, "emittedBodySha256"),
        "9664dbd5557ea8b79eec70373fe168d920ef115ca5a41e11cdf7275590dcea05"
    );
    assert_eq!(
        head_field(v70_summary, "semanticPassSha256"),
        "43142d66388f8657b64f27c1238babdf75ff7e8ead11590e863144983729e5a6"
    );
    assert_eq!(head_field(v70_summary, "freshRuns"), "2");
    assert_eq!(head_field(v70_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v70_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder70MultiHeadReuseCLink"
    );
    assert_eq!(
        head_field(v70_summary, "javaEvidence"),
        "ReturnedBeforeSeventyFirstHead"
    );

    // Boundary 97 expected-only order71 existing-stem reconciliation.
    let v71_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v71.txt"),
    )
    .expect("expected-only order71 existing-stem fixture");
    assert_eq!(
        sha256_hex(v71_text.as_bytes()),
        "0b6e69a6b50ea4aae7af2da5f4899a9947471e749d60d20b821e60599aac6ea9"
    );
    let v71_rows = v71_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v71_rows.len(), 10);
    let v71_body = format!("{}\n", v71_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v71_body.as_bytes()),
        "f0c5f46faef1d871f20395d869741a14b6174150fc2c1c5b9c0c9144f9ff67ce"
    );
    let v71_frontier = v71_rows[6];
    assert_eq!(head_field(v71_frontier, "headOrder"), "71");
    assert_eq!(head_field(v71_frontier, "headX"), "77");
    assert_eq!(head_field(v71_frontier, "headSig"), "38");
    assert_eq!(head_field(v71_frontier, "headInterId"), "1361");
    assert_eq!(head_field(v71_frontier, "cAlias"), "h:77:LEFT:BOTTOM");
    assert_eq!(head_field(v71_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v71_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v71_frontier, "relations"), "3");
    assert_eq!(head_field(v71_frontier, "glyphs"), "1");
    assert_eq!(head_field(v71_frontier, "candidateIdBefore"), "309");
    assert_eq!(head_field(v71_frontier, "existingGlyph"), "glyph:309");
    assert_eq!(head_field(v71_frontier, "existingActive"), "true");
    assert_eq!(head_field(v71_frontier, "existingStem"), "2370");
    let v71_result = v71_rows[7];
    assert_eq!(head_field(v71_result, "headOrder"), "71");
    assert_eq!(head_field(v71_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v71_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v71_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v71_result, "addedVertices"), "-");
    assert_eq!(head_field(v71_result, "addedEdges"), "-");
    assert_eq!(head_field(v71_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v71_result, "linkerChanges"),
        "[h:78:LEFT:BOTTOM:true:false->true:true,h:78:LEFT:TOP:true:false->true:true,h:78:RIGHT:BOTTOM:false:false->false:true,h:78:RIGHT:TOP:false:false->false:true,linker:SLinker:head:78:false:false->false:true,linker:SLinker:head:78:true:false->true:true]"
    );
    assert_eq!(
        head_field(v71_result, "linkerStateHashBefore"),
        "9598d2170824a69b15d1035e1ac51fb3ff60ab3116a26c02019d1174548bbeee"
    );
    assert_eq!(
        head_field(v71_result, "linkerStateHashAfter"),
        "4031c177499764aeb34b702172271fe946dfe10ff84df6d11ac5a2a09bbbeb84"
    );
    let v71_java = v71_rows[8];
    assert_eq!(head_field(v71_java, "headOrder"), "71");
    assert_eq!(head_field(v71_java, "headX"), "77");
    assert_eq!(head_field(v71_java, "headSig"), "38");
    assert_eq!(head_field(v71_java, "headInterId"), "1361");
    assert_eq!(
        head_field(v71_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v71_java, "incident"),
        "[stem2370:headSideLEFT:heads[x77:sig38:id1361:sideLEFT,x78:sig39:id1363:sideLEFT]]"
    );
    assert_eq!(head_field(v71_java, "returned"), "true");
    assert_eq!(head_field(v71_java, "undefs"), "[]");
    assert_eq!(
        head_field(v71_java, "closureWrites"),
        "[x78:sig39:LEFT:false->true,x78:sig39:RIGHT:false->true]"
    );
    assert_eq!(head_field(v71_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v71_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v71_java, "sigEdgesAfter"), "702");
    assert_eq!(head_field(v71_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v71_java, "linkerStateHashBefore"),
        "9598d2170824a69b15d1035e1ac51fb3ff60ab3116a26c02019d1174548bbeee"
    );
    assert_eq!(
        head_field(v71_java, "linkerStateHashAfter"),
        "4031c177499764aeb34b702172271fe946dfe10ff84df6d11ac5a2a09bbbeb84"
    );
    assert_eq!(head_field(v71_java, "nextHeadOrder"), "72");
    assert_eq!(head_field(v71_java, "nextHeadX"), "26");
    assert_eq!(head_field(v71_java, "nextHeadSig"), "13");
    assert_eq!(head_field(v71_java, "nextHeadInterId"), "1311");
    assert_eq!(head_field(v71_java, "terminal"), "ReturnedBeforeNextHead");
    let v71_summary = v71_rows[9];
    assert_eq!(
        head_field(v71_summary, "schema"),
        "stems-head-phase-prefix-v71"
    );
    assert_eq!(head_field(v71_summary, "rows"), "9");
    assert_eq!(
        head_field(v71_summary, "baseV70RunnerSourceSha256"),
        "75be33c3f6bf1e2eae337d9808f967172e7633ffad7fe48e7c48441d7e0740be"
    );
    assert_eq!(
        head_field(v71_summary, "baseV70FixtureSha256"),
        "cf883421cfd392f124598143d6d48e83019c9ddc6cfd416ccc1a676b9558e142"
    );
    assert_eq!(
        head_field(v71_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v71_summary, "probeSourceSha256"),
        "a266cc40fc91c8cd4c449e3fb12378ddf8afc1eb3399f3d1e96ea5927f0ad461"
    );
    assert_eq!(
        head_field(v71_summary, "runnerSourceSha256"),
        "ed5e460555016bf5c578a398322d6f1d3f256f29cedc3a4e858dc4b0e27a539b"
    );
    assert_eq!(
        head_field(v71_summary, "emittedBodySha256"),
        "f0c5f46faef1d871f20395d869741a14b6174150fc2c1c5b9c0c9144f9ff67ce"
    );
    assert_eq!(
        head_field(v71_summary, "semanticPassSha256"),
        "4656dc9fbbca6cba4a0a68c1d996cad0d2f0a23caac10f47096b9af1ad5fe478"
    );
    assert_eq!(head_field(v71_summary, "freshRuns"), "2");
    assert_eq!(head_field(v71_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v71_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder71ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v71_summary, "javaEvidence"),
        "ReturnedBeforeSeventySecondHead"
    );

    // Boundary 98 expected-only order72 single-head reuse C-link.
    let v72_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v72.txt"),
    )
    .expect("expected-only order72 single-head reuse fixture");
    assert_eq!(
        sha256_hex(v72_text.as_bytes()),
        "e7f02765e22b98dabd98a76f908b344a97fdec3d51b91886e0c795834a4085b6"
    );
    let v72_rows = v72_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v72_rows.len(), 10);
    let v72_body = format!("{}\n", v72_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v72_body.as_bytes()),
        "ad662577ca70f1c50bb7c7bf62d127b8523caf0b574fab701723a55d1548600e"
    );
    let v72_frontier = v72_rows[6];
    assert_eq!(head_field(v72_frontier, "headOrder"), "72");
    assert_eq!(head_field(v72_frontier, "headX"), "26");
    assert_eq!(head_field(v72_frontier, "headSig"), "13");
    assert_eq!(head_field(v72_frontier, "headInterId"), "1311");
    assert_eq!(head_field(v72_frontier, "cAlias"), "h:26:LEFT:BOTTOM");
    assert_eq!(head_field(v72_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v72_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v72_frontier, "relations"), "1");
    assert_eq!(head_field(v72_frontier, "glyphs"), "1");
    assert_eq!(head_field(v72_frontier, "candidateIdBefore"), "324");
    assert_eq!(head_field(v72_frontier, "existingGlyph"), "glyph:324");
    assert_eq!(head_field(v72_frontier, "existingActive"), "true");
    assert_eq!(head_field(v72_frontier, "existingStem"), "2385");
    let v72_result = v72_rows[7];
    assert_eq!(head_field(v72_result, "headOrder"), "72");
    assert_eq!(head_field(v72_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v72_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v72_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v72_result, "addedVertices"), "-");
    assert_eq!(head_field(v72_result, "addedSystemStems"), "-");
    assert!(head_field(v72_result, "addedEdges").starts_with(
        "[system1:sourceId1311:targetId2385:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.4c2ab27b5b4f9p-1/3fe4c2ab27b5b4f9"
    ));
    assert_eq!(
        head_field(v72_result, "linkerChanges"),
        "[h:23:LEFT:BOTTOM:true:false->true:true,h:23:LEFT:TOP:true:false->true:true,h:23:RIGHT:BOTTOM:false:false->false:true,h:23:RIGHT:TOP:false:false->false:true,h:26:LEFT:BOTTOM:false:false->true:false,h:26:LEFT:TOP:false:false->true:false,linker:SLinker:head:23:false:false->false:true,linker:SLinker:head:23:true:false->true:true,linker:SLinker:head:26:false:false->true:false]"
    );
    assert_eq!(
        head_field(v72_result, "linkerStateHashBefore"),
        "4031c177499764aeb34b702172271fe946dfe10ff84df6d11ac5a2a09bbbeb84"
    );
    assert_eq!(
        head_field(v72_result, "linkerStateHashAfter"),
        "8e10ee5b5b94577a0082fd3fd45c2d6d5b3831c83f8c4917b4451a83f02f64a1"
    );
    let v72_java = v72_rows[8];
    assert_eq!(head_field(v72_java, "headOrder"), "72");
    assert_eq!(head_field(v72_java, "headX"), "26");
    assert_eq!(head_field(v72_java, "headSig"), "13");
    assert_eq!(head_field(v72_java, "headInterId"), "1311");
    assert_eq!(
        head_field(v72_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v72_java, "incident"), "-");
    assert_eq!(head_field(v72_java, "returned"), "true");
    assert_eq!(head_field(v72_java, "undefs"), "[]");
    assert_eq!(head_field(v72_java, "closureWrites"), "-");
    assert_eq!(head_field(v72_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v72_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v72_java, "sigEdgesBefore"), "702");
    assert_eq!(head_field(v72_java, "sigEdgesAfter"), "703");
    assert_eq!(head_field(v72_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v72_java, "nextHeadOrder"), "73");
    assert_eq!(head_field(v72_java, "nextHeadX"), "75");
    assert_eq!(head_field(v72_java, "nextHeadSig"), "96");
    assert_eq!(head_field(v72_java, "nextHeadInterId"), "1481");
    assert_eq!(head_field(v72_java, "terminal"), "ReturnedBeforeNextHead");
    let v72_summary = v72_rows[9];
    assert_eq!(
        head_field(v72_summary, "schema"),
        "stems-head-phase-prefix-v72"
    );
    assert_eq!(head_field(v72_summary, "rows"), "9");
    assert_eq!(
        head_field(v72_summary, "baseV71RunnerSourceSha256"),
        "ed5e460555016bf5c578a398322d6f1d3f256f29cedc3a4e858dc4b0e27a539b"
    );
    assert_eq!(
        head_field(v72_summary, "baseV71FixtureSha256"),
        "0b6e69a6b50ea4aae7af2da5f4899a9947471e749d60d20b821e60599aac6ea9"
    );
    assert_eq!(
        head_field(v72_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v72_summary, "probeSourceSha256"),
        "d2193a0bc17aa09dfb9bc04defbbf5560cdcdd3829b22c75f082bbcb60b5e3af"
    );
    assert_eq!(
        head_field(v72_summary, "runnerSourceSha256"),
        "7d38e49da6221953158fde53a699b9436cb01fcf757f0dbce991a1672457eba7"
    );
    assert_eq!(
        head_field(v72_summary, "emittedBodySha256"),
        "ad662577ca70f1c50bb7c7bf62d127b8523caf0b574fab701723a55d1548600e"
    );
    assert_eq!(
        head_field(v72_summary, "semanticPassSha256"),
        "ccc5d4660e3c7f0486201a7185cfdbe73013807dc6c4a0bb1c8466101aeff030"
    );
    assert_eq!(head_field(v72_summary, "freshRuns"), "2");
    assert_eq!(head_field(v72_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v72_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder72SingleHeadReuseCLink"
    );
    assert_eq!(
        head_field(v72_summary, "javaEvidence"),
        "ReturnedBeforeSeventyThirdHead"
    );

    // Boundary 99 expected-only order73 multi-head reuse C-link.
    let v73_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v73.txt"),
    )
    .expect("expected-only order73 multi-head reuse fixture");
    assert_eq!(
        sha256_hex(v73_text.as_bytes()),
        "52e10c12f512c6ab5a3a495a774f717f0354281342a88a1ffcaa079073333e57"
    );
    let v73_rows = v73_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v73_rows.len(), 10);
    let v73_body = format!("{}\n", v73_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v73_body.as_bytes()),
        "77f1b922b959b61f7046abddbadb107697811c167eb83b433a3bc14ac866a022"
    );
    let v73_frontier = v73_rows[6];
    assert_eq!(head_field(v73_frontier, "headOrder"), "73");
    assert_eq!(head_field(v73_frontier, "headX"), "75");
    assert_eq!(head_field(v73_frontier, "headSig"), "96");
    assert_eq!(head_field(v73_frontier, "headInterId"), "1481");
    assert_eq!(head_field(v73_frontier, "cAlias"), "h:75:LEFT:BOTTOM");
    assert_eq!(head_field(v73_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v73_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v73_frontier, "relations"), "2");
    assert_eq!(head_field(v73_frontier, "glyphs"), "1");
    assert_eq!(head_field(v73_frontier, "candidateIdBefore"), "319");
    assert_eq!(head_field(v73_frontier, "existingGlyph"), "glyph:319");
    assert_eq!(head_field(v73_frontier, "existingActive"), "true");
    assert_eq!(head_field(v73_frontier, "existingStem"), "2380");
    let v73_result = v73_rows[7];
    assert_eq!(head_field(v73_result, "headOrder"), "73");
    assert_eq!(head_field(v73_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v73_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v73_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v73_result, "addedVertices"), "-");
    assert_eq!(head_field(v73_result, "addedSystemStems"), "-");
    assert!(head_field(v73_result, "addedEdges").starts_with(
        "[system1:sourceId1481:targetId2380:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.880946d87171ep-1/3fe880946d87171e"
    ));
    assert_eq!(
        head_field(v73_result, "linkerChanges"),
        "[h:72:LEFT:BOTTOM:false:false->true:true,h:72:LEFT:TOP:false:false->true:true,h:72:RIGHT:BOTTOM:false:false->false:true,h:72:RIGHT:TOP:false:false->false:true,h:75:LEFT:BOTTOM:false:false->true:false,h:75:LEFT:TOP:false:false->true:false,h:76:LEFT:BOTTOM:true:false->true:true,h:76:LEFT:TOP:true:false->true:true,h:76:RIGHT:BOTTOM:false:false->false:true,h:76:RIGHT:TOP:false:false->false:true,linker:SLinker:head:72:false:false->false:true,linker:SLinker:head:72:false:false->true:true,linker:SLinker:head:75:false:false->true:false,linker:SLinker:head:76:false:false->false:true,linker:SLinker:head:76:true:false->true:true]"
    );
    assert_eq!(
        head_field(v73_result, "linkerStateHashBefore"),
        "8e10ee5b5b94577a0082fd3fd45c2d6d5b3831c83f8c4917b4451a83f02f64a1"
    );
    assert_eq!(
        head_field(v73_result, "linkerStateHashAfter"),
        "d7ff2e54134e41416c836a996cf0fd107175293eff2a5ad99b3bb84140c76d3d"
    );
    let v73_java = v73_rows[8];
    assert_eq!(head_field(v73_java, "headOrder"), "73");
    assert_eq!(head_field(v73_java, "headX"), "75");
    assert_eq!(head_field(v73_java, "headSig"), "96");
    assert_eq!(head_field(v73_java, "headInterId"), "1481");
    assert_eq!(
        head_field(v73_java, "decisions"),
        "[LEFT:top=false:bottom=true:branch=BottomOnly,RIGHT:top=false:bottom=false:branch=Neither]"
    );
    assert_eq!(head_field(v73_java, "incident"), "-");
    assert_eq!(head_field(v73_java, "returned"), "true");
    assert_eq!(head_field(v73_java, "undefs"), "[]");
    assert_eq!(head_field(v73_java, "closureWrites"), "-");
    assert_eq!(head_field(v73_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v73_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v73_java, "sigEdgesBefore"), "703");
    assert_eq!(head_field(v73_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v73_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v73_java, "nextHeadOrder"), "74");
    assert_eq!(head_field(v73_java, "nextHeadX"), "49");
    assert_eq!(head_field(v73_java, "nextHeadSig"), "71");
    assert_eq!(head_field(v73_java, "nextHeadInterId"), "1431");
    assert_eq!(head_field(v73_java, "terminal"), "ReturnedBeforeNextHead");
    let v73_summary = v73_rows[9];
    assert_eq!(
        head_field(v73_summary, "schema"),
        "stems-head-phase-prefix-v73"
    );
    assert_eq!(head_field(v73_summary, "rows"), "9");
    assert_eq!(
        head_field(v73_summary, "baseV72RunnerSourceSha256"),
        "7d38e49da6221953158fde53a699b9436cb01fcf757f0dbce991a1672457eba7"
    );
    assert_eq!(
        head_field(v73_summary, "baseV72FixtureSha256"),
        "e7f02765e22b98dabd98a76f908b344a97fdec3d51b91886e0c795834a4085b6"
    );
    assert_eq!(
        head_field(v73_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v73_summary, "probeSourceSha256"),
        "97ee6a956b1dc6666a08292766a29da150dff405198b55ce14334239c3c1c3e6"
    );
    assert_eq!(
        head_field(v73_summary, "runnerSourceSha256"),
        "b9ff88d207e2e54e3f011598ed2224d092794c23eac26caff84fa8d4544131f1"
    );
    assert_eq!(
        head_field(v73_summary, "emittedBodySha256"),
        "77f1b922b959b61f7046abddbadb107697811c167eb83b433a3bc14ac866a022"
    );
    assert_eq!(
        head_field(v73_summary, "semanticPassSha256"),
        "ecea9b7439c7b001db4255ad51cbe5d8b085e53da40062135547445e818f82ff"
    );
    assert_eq!(head_field(v73_summary, "freshRuns"), "2");
    assert_eq!(head_field(v73_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v73_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder73MultiHeadReuseCLink"
    );
    assert_eq!(
        head_field(v73_summary, "javaEvidence"),
        "ReturnedBeforeSeventyFourthHead"
    );

    // Boundary 100 expected-only order74 existing-stem reconciliation.
    let v74_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v74.txt"),
    )
    .expect("expected-only order74 existing-stem fixture");
    assert_eq!(
        sha256_hex(v74_text.as_bytes()),
        "fdaf7d51fb50ef62235c2fac95525a1e9d2added8222c9144586e9a7ef313e1c"
    );
    let v74_rows = v74_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v74_rows.len(), 10);
    let v74_body = format!("{}\n", v74_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v74_body.as_bytes()),
        "3e455387d75844d8e2cf6c95cabe761436f2a09756f6d1a893b48f35fcb24878"
    );
    let v74_frontier = v74_rows[6];
    assert_eq!(head_field(v74_frontier, "headOrder"), "74");
    assert_eq!(head_field(v74_frontier, "headX"), "49");
    assert_eq!(head_field(v74_frontier, "headSig"), "71");
    assert_eq!(head_field(v74_frontier, "headInterId"), "1431");
    assert_eq!(head_field(v74_frontier, "cAlias"), "h:49:LEFT:BOTTOM");
    assert_eq!(head_field(v74_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v74_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v74_frontier, "relations"), "1");
    assert_eq!(head_field(v74_frontier, "glyphs"), "2");
    assert_eq!(head_field(v74_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v74_frontier, "existingGlyph"), "glyph:317");
    assert_eq!(head_field(v74_frontier, "existingActive"), "true");
    assert_eq!(head_field(v74_frontier, "existingStem"), "2353");
    let v74_result = v74_rows[7];
    assert_eq!(head_field(v74_result, "headOrder"), "74");
    assert_eq!(head_field(v74_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v74_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v74_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v74_result, "addedVertices"), "-");
    assert_eq!(head_field(v74_result, "addedEdges"), "-");
    assert_eq!(head_field(v74_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v74_result, "linkerChanges"),
        "[h:50:LEFT:BOTTOM:true:false->true:true,h:50:LEFT:TOP:true:false->true:true,h:50:RIGHT:BOTTOM:false:false->false:true,h:50:RIGHT:TOP:false:false->false:true,linker:SLinker:head:50:false:false->false:true,linker:SLinker:head:50:true:false->true:true]"
    );
    assert_eq!(
        head_field(v74_result, "linkerStateHashBefore"),
        "d7ff2e54134e41416c836a996cf0fd107175293eff2a5ad99b3bb84140c76d3d"
    );
    assert_eq!(
        head_field(v74_result, "linkerStateHashAfter"),
        "bf1f2c96d2b5e943ac24911c11df048004a475575a8338d85ac1539a05d1b943"
    );
    let v74_java = v74_rows[8];
    assert_eq!(head_field(v74_java, "headOrder"), "74");
    assert_eq!(head_field(v74_java, "headX"), "49");
    assert_eq!(head_field(v74_java, "headSig"), "71");
    assert_eq!(head_field(v74_java, "headInterId"), "1431");
    assert_eq!(
        head_field(v74_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v74_java, "incident"),
        "[stem2353:headSideLEFT:heads[x49:sig71:id1431:sideLEFT,x50:sig72:id1433:sideLEFT]]"
    );
    assert_eq!(head_field(v74_java, "returned"), "true");
    assert_eq!(head_field(v74_java, "undefs"), "[]");
    assert_eq!(
        head_field(v74_java, "closureWrites"),
        "[x50:sig72:LEFT:false->true,x50:sig72:RIGHT:false->true]"
    );
    assert_eq!(head_field(v74_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v74_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v74_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v74_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v74_java, "linkerStateHashBefore"),
        "d7ff2e54134e41416c836a996cf0fd107175293eff2a5ad99b3bb84140c76d3d"
    );
    assert_eq!(
        head_field(v74_java, "linkerStateHashAfter"),
        "bf1f2c96d2b5e943ac24911c11df048004a475575a8338d85ac1539a05d1b943"
    );
    assert_eq!(head_field(v74_java, "nextHeadOrder"), "75");
    assert_eq!(head_field(v74_java, "nextHeadX"), "31");
    assert_eq!(head_field(v74_java, "nextHeadSig"), "47");
    assert_eq!(head_field(v74_java, "nextHeadInterId"), "1381");
    assert_eq!(head_field(v74_java, "terminal"), "ReturnedBeforeNextHead");
    let v74_summary = v74_rows[9];
    assert_eq!(
        head_field(v74_summary, "schema"),
        "stems-head-phase-prefix-v74"
    );
    assert_eq!(head_field(v74_summary, "rows"), "9");
    assert_eq!(
        head_field(v74_summary, "baseV73RunnerSourceSha256"),
        "b9ff88d207e2e54e3f011598ed2224d092794c23eac26caff84fa8d4544131f1"
    );
    assert_eq!(
        head_field(v74_summary, "baseV73FixtureSha256"),
        "52e10c12f512c6ab5a3a495a774f717f0354281342a88a1ffcaa079073333e57"
    );
    assert_eq!(
        head_field(v74_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v74_summary, "probeSourceSha256"),
        "c0e0d8aa25e63331438c94e1d73b2eac5d1684fdc11ab5d6080a3504d1c4b31c"
    );
    assert_eq!(
        head_field(v74_summary, "runnerSourceSha256"),
        "179a2752d18180ef1c4798223b8a79a3a5398e5b6a45fe7cc711085c5caa178f"
    );
    assert_eq!(
        head_field(v74_summary, "emittedBodySha256"),
        "3e455387d75844d8e2cf6c95cabe761436f2a09756f6d1a893b48f35fcb24878"
    );
    assert_eq!(
        head_field(v74_summary, "semanticPassSha256"),
        "ac3b39fb1c1f79bd245edf3015db20d722fcd024aac5a17ee186e67dce7ce4e1"
    );
    assert_eq!(head_field(v74_summary, "freshRuns"), "2");
    assert_eq!(head_field(v74_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v74_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder74ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v74_summary, "javaEvidence"),
        "ReturnedBeforeSeventyFifthHead"
    );

    // Boundary 101 expected-only order75 open/undefined frontier.
    let v75_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v75.txt"),
    )
    .expect("expected-only order75 open-frontier fixture");
    assert_eq!(
        sha256_hex(v75_text.as_bytes()),
        "890b3f32fadbef6085917d7f6c160e439bd31ac13d2e36ced82eaed5ccaf1c4d"
    );
    let v75_rows = v75_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v75_rows.len(), 10);
    let v75_body = format!("{}\n", v75_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v75_body.as_bytes()),
        "87e22d666e83dcb5b8801961522342ea41a9e39ad02343e5b0d3b621206c4e26"
    );
    let v75_frontier = v75_rows[6];
    assert_eq!(head_field(v75_frontier, "headOrder"), "75");
    assert_eq!(head_field(v75_frontier, "headX"), "31");
    assert_eq!(head_field(v75_frontier, "headSig"), "47");
    assert_eq!(head_field(v75_frontier, "headInterId"), "1381");
    assert_eq!(head_field(v75_frontier, "cAlias"), "h:31:LEFT:BOTTOM");
    assert_eq!(head_field(v75_frontier, "lastIndex"), "-1");
    assert_eq!(head_field(v75_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v75_frontier, "relations"), "1");
    assert_eq!(head_field(v75_frontier, "glyphs"), "2");
    assert_eq!(head_field(v75_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v75_frontier, "existingGlyph"), "glyph:314");
    assert_eq!(head_field(v75_frontier, "existingActive"), "true");
    assert_eq!(head_field(v75_frontier, "existingStem"), "2383");
    let v75_result = v75_rows[7];
    assert_eq!(head_field(v75_result, "headOrder"), "75");
    assert_eq!(head_field(v75_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v75_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v75_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v75_result, "addedVertices"), "-");
    assert_eq!(head_field(v75_result, "addedEdges"), "-");
    assert_eq!(head_field(v75_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v75_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v75_result, "linkerStateHashBefore"),
        "bf1f2c96d2b5e943ac24911c11df048004a475575a8338d85ac1539a05d1b943"
    );
    assert_eq!(
        head_field(v75_result, "linkerStateHashAfter"),
        "bf1f2c96d2b5e943ac24911c11df048004a475575a8338d85ac1539a05d1b943"
    );
    let v75_java = v75_rows[8];
    assert_eq!(head_field(v75_java, "headOrder"), "75");
    assert_eq!(head_field(v75_java, "headX"), "31");
    assert_eq!(head_field(v75_java, "headSig"), "47");
    assert_eq!(head_field(v75_java, "headInterId"), "1381");
    assert_eq!(
        head_field(v75_java, "decisions"),
        "[LEFT:top=true:bottom=true:branch=Both,RIGHT:top=true:bottom=false:branch=TopOnly]"
    );
    assert_eq!(head_field(v75_java, "incident"), "-");
    assert_eq!(head_field(v75_java, "returned"), "false");
    assert_eq!(head_field(v75_java, "undefs"), "[LEFT]");
    assert_eq!(head_field(v75_java, "closureWrites"), "-");
    assert_eq!(head_field(v75_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v75_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v75_java, "sigEdgesBefore"), "705");
    assert_eq!(head_field(v75_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v75_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v75_java, "nextHeadOrder"), "76");
    assert_eq!(head_field(v75_java, "nextHeadX"), "66");
    assert_eq!(head_field(v75_java, "nextHeadSig"), "58");
    assert_eq!(head_field(v75_java, "nextHeadInterId"), "1405");
    assert_eq!(head_field(v75_java, "terminal"), "ReturnedBeforeNextHead");
    let v75_summary = v75_rows[9];
    assert_eq!(
        head_field(v75_summary, "schema"),
        "stems-head-phase-prefix-v75"
    );
    assert_eq!(head_field(v75_summary, "rows"), "9");
    assert_eq!(
        head_field(v75_summary, "baseV74RunnerSourceSha256"),
        "179a2752d18180ef1c4798223b8a79a3a5398e5b6a45fe7cc711085c5caa178f"
    );
    assert_eq!(
        head_field(v75_summary, "baseV74FixtureSha256"),
        "fdaf7d51fb50ef62235c2fac95525a1e9d2added8222c9144586e9a7ef313e1c"
    );
    assert_eq!(
        head_field(v75_summary, "probeSourceSha256"),
        "c86c320452d011fb85f9a600eb7c19c6020cc964998fb70c9e76835fd194a4b7"
    );
    assert_eq!(
        head_field(v75_summary, "runnerSourceSha256"),
        "6535910b7379860aa4766e955fb5d4814d8f44e9841a020546409008195e3bb4"
    );
    assert_eq!(
        head_field(v75_summary, "emittedBodySha256"),
        "87e22d666e83dcb5b8801961522342ea41a9e39ad02343e5b0d3b621206c4e26"
    );
    assert_eq!(
        head_field(v75_summary, "semanticPassSha256"),
        "002dd6876545bd3c6edfb41cc99a8eede78a4158be8552c09955cb8a737db333"
    );
    assert_eq!(head_field(v75_summary, "freshRuns"), "2");
    assert_eq!(head_field(v75_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v75_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder75OpenFrontier"
    );
    assert_eq!(
        head_field(v75_summary, "javaEvidence"),
        "ReturnedBeforeSeventySixthHead"
    );

    // Boundary 102 expected-only order76 existing-stem reconciliation.
    let v76_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v76.txt"),
    )
    .expect("expected-only order76 existing-stem fixture");
    assert_eq!(
        sha256_hex(v76_text.as_bytes()),
        "55f04e5c204f82b3e74d275e93cc93603ffc7b0a6f9e0e78bd7bcb6113544594"
    );
    let v76_rows = v76_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v76_rows.len(), 10);
    let v76_body = format!("{}\n", v76_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v76_body.as_bytes()),
        "72126a76e01200b3c2bc4c1b1dfc2af58ff1d814ae5c61ef5c4d4fbae368d090"
    );
    let v76_frontier = v76_rows[6];
    assert_eq!(head_field(v76_frontier, "headOrder"), "76");
    assert_eq!(head_field(v76_frontier, "headX"), "66");
    assert_eq!(head_field(v76_frontier, "headSig"), "58");
    assert_eq!(head_field(v76_frontier, "headInterId"), "1405");
    assert_eq!(head_field(v76_frontier, "cAlias"), "h:66:LEFT:BOTTOM");
    assert_eq!(head_field(v76_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v76_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v76_frontier, "relations"), "2");
    assert_eq!(head_field(v76_frontier, "glyphs"), "2");
    assert_eq!(head_field(v76_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v76_frontier, "existingGlyph"), "glyph:308");
    assert_eq!(head_field(v76_frontier, "existingActive"), "true");
    assert_eq!(head_field(v76_frontier, "existingStem"), "2375");
    let v76_result = v76_rows[7];
    assert_eq!(head_field(v76_result, "headOrder"), "76");
    assert_eq!(head_field(v76_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v76_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v76_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v76_result, "addedVertices"), "-");
    assert_eq!(head_field(v76_result, "addedEdges"), "-");
    assert_eq!(head_field(v76_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v76_result, "linkerChanges"),
        "[h:67:LEFT:BOTTOM:true:false->true:true,h:67:LEFT:TOP:true:false->true:true,h:67:RIGHT:BOTTOM:false:false->false:true,h:67:RIGHT:TOP:false:false->false:true,linker:SLinker:head:67:false:false->false:true,linker:SLinker:head:67:true:false->true:true]"
    );
    assert_eq!(
        head_field(v76_result, "linkerStateHashBefore"),
        "bf1f2c96d2b5e943ac24911c11df048004a475575a8338d85ac1539a05d1b943"
    );
    assert_eq!(
        head_field(v76_result, "linkerStateHashAfter"),
        "2aa181c6b0767f6471fc8330a2e0644c67553722f480e6ad6cd0b5affe84f205"
    );
    let v76_java = v76_rows[8];
    assert_eq!(head_field(v76_java, "headOrder"), "76");
    assert_eq!(head_field(v76_java, "headX"), "66");
    assert_eq!(head_field(v76_java, "headSig"), "58");
    assert_eq!(head_field(v76_java, "headInterId"), "1405");
    assert_eq!(
        head_field(v76_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v76_java, "incident"),
        "[stem2375:headSideLEFT:heads[x66:sig58:id1405:sideLEFT,x67:sig59:id1407:sideLEFT]]"
    );
    assert_eq!(head_field(v76_java, "returned"), "true");
    assert_eq!(head_field(v76_java, "undefs"), "[]");
    assert_eq!(
        head_field(v76_java, "closureWrites"),
        "[x67:sig59:LEFT:false->true,x67:sig59:RIGHT:false->true]"
    );
    assert_eq!(head_field(v76_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v76_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v76_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v76_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v76_java, "linkerStateHashBefore"),
        "bf1f2c96d2b5e943ac24911c11df048004a475575a8338d85ac1539a05d1b943"
    );
    assert_eq!(
        head_field(v76_java, "linkerStateHashAfter"),
        "2aa181c6b0767f6471fc8330a2e0644c67553722f480e6ad6cd0b5affe84f205"
    );
    assert_eq!(head_field(v76_java, "nextHeadOrder"), "77");
    assert_eq!(head_field(v76_java, "nextHeadX"), "64");
    assert_eq!(head_field(v76_java, "nextHeadSig"), "94");
    assert_eq!(head_field(v76_java, "nextHeadInterId"), "1477");
    assert_eq!(head_field(v76_java, "terminal"), "ReturnedBeforeNextHead");
    let v76_summary = v76_rows[9];
    assert_eq!(
        head_field(v76_summary, "schema"),
        "stems-head-phase-prefix-v76"
    );
    assert_eq!(head_field(v76_summary, "rows"), "9");
    assert_eq!(
        head_field(v76_summary, "baseV75RunnerSourceSha256"),
        "6535910b7379860aa4766e955fb5d4814d8f44e9841a020546409008195e3bb4"
    );
    assert_eq!(
        head_field(v76_summary, "baseV75FixtureSha256"),
        "890b3f32fadbef6085917d7f6c160e439bd31ac13d2e36ced82eaed5ccaf1c4d"
    );
    assert_eq!(
        head_field(v76_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v76_summary, "probeSourceSha256"),
        "6aa7d72f3d0f08afa9c8759d760e821339406100887fe902fec2959fdafae72b"
    );
    assert_eq!(
        head_field(v76_summary, "runnerSourceSha256"),
        "3a899ca2e8cdcfd0b2eba4fb003f42bd46cbfc121eb31c5bd0ab428640f86ee3"
    );
    assert_eq!(
        head_field(v76_summary, "emittedBodySha256"),
        "72126a76e01200b3c2bc4c1b1dfc2af58ff1d814ae5c61ef5c4d4fbae368d090"
    );
    assert_eq!(
        head_field(v76_summary, "semanticPassSha256"),
        "99b1eea132139bb6b088b686680d1723691edd61b779b629fdd2d95cde61d870"
    );
    assert_eq!(head_field(v76_summary, "freshRuns"), "2");
    assert_eq!(head_field(v76_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v76_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder76ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v76_summary, "javaEvidence"),
        "ReturnedBeforeSeventySeventhHead"
    );

    // Boundary 103 expected-only order77 existing-stem reconciliation.
    let v77_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v77.txt"),
    )
    .expect("expected-only order77 existing-stem fixture");
    assert_eq!(
        sha256_hex(v77_text.as_bytes()),
        "e1ab516af406562ef9465b5f63ef584ff3b021611255d6615f31dcc0ceb586e5"
    );
    let v77_rows = v77_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v77_rows.len(), 10);
    let v77_body = format!("{}\n", v77_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v77_body.as_bytes()),
        "31dcc8d4596bd6d432a7db84d2c77c8ffdd87a646b1227139bd27f4ad82ec0e9"
    );
    let v77_frontier = v77_rows[6];
    assert_eq!(head_field(v77_frontier, "headOrder"), "77");
    assert_eq!(head_field(v77_frontier, "headX"), "64");
    assert_eq!(head_field(v77_frontier, "headSig"), "94");
    assert_eq!(head_field(v77_frontier, "headInterId"), "1477");
    assert_eq!(head_field(v77_frontier, "cAlias"), "h:64:LEFT:BOTTOM");
    assert_eq!(head_field(v77_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v77_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v77_frontier, "relations"), "3");
    assert_eq!(head_field(v77_frontier, "glyphs"), "1");
    assert_eq!(head_field(v77_frontier, "candidateIdBefore"), "291");
    assert_eq!(head_field(v77_frontier, "existingGlyph"), "glyph:291");
    assert_eq!(head_field(v77_frontier, "existingActive"), "true");
    assert_eq!(head_field(v77_frontier, "existingStem"), "2346");
    let v77_result = v77_rows[7];
    assert_eq!(head_field(v77_result, "headOrder"), "77");
    assert_eq!(head_field(v77_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v77_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v77_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v77_result, "addedVertices"), "-");
    assert_eq!(head_field(v77_result, "addedEdges"), "-");
    assert_eq!(head_field(v77_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v77_result, "linkerChanges"),
        "[h:65:LEFT:BOTTOM:true:false->true:true,h:65:LEFT:TOP:true:false->true:true,h:65:RIGHT:BOTTOM:false:false->false:true,h:65:RIGHT:TOP:false:false->false:true,linker:SLinker:head:65:false:false->false:true,linker:SLinker:head:65:true:false->true:true]"
    );
    assert_eq!(
        head_field(v77_result, "linkerStateHashBefore"),
        "2aa181c6b0767f6471fc8330a2e0644c67553722f480e6ad6cd0b5affe84f205"
    );
    assert_eq!(
        head_field(v77_result, "linkerStateHashAfter"),
        "8c67beb2484af15ba20586a0f76ee293e9929d69ffcfe55798b7b220a6a3237f"
    );
    let v77_java = v77_rows[8];
    assert_eq!(head_field(v77_java, "headOrder"), "77");
    assert_eq!(head_field(v77_java, "headX"), "64");
    assert_eq!(head_field(v77_java, "headSig"), "94");
    assert_eq!(head_field(v77_java, "headInterId"), "1477");
    assert_eq!(
        head_field(v77_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v77_java, "incident"),
        "[stem2346:headSideLEFT:heads[x64:sig94:id1477:sideLEFT,x65:sig95:id1479:sideLEFT]]"
    );
    assert_eq!(head_field(v77_java, "returned"), "true");
    assert_eq!(head_field(v77_java, "undefs"), "[]");
    assert_eq!(
        head_field(v77_java, "closureWrites"),
        "[x65:sig95:LEFT:false->true,x65:sig95:RIGHT:false->true]"
    );
    assert_eq!(head_field(v77_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v77_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v77_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v77_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v77_java, "linkerStateHashBefore"),
        "2aa181c6b0767f6471fc8330a2e0644c67553722f480e6ad6cd0b5affe84f205"
    );
    assert_eq!(
        head_field(v77_java, "linkerStateHashAfter"),
        "8c67beb2484af15ba20586a0f76ee293e9929d69ffcfe55798b7b220a6a3237f"
    );
    assert_eq!(head_field(v77_java, "nextHeadOrder"), "78");
    assert_eq!(head_field(v77_java, "nextHeadX"), "82");
    assert_eq!(head_field(v77_java, "nextHeadSig"), "20");
    assert_eq!(head_field(v77_java, "nextHeadInterId"), "1325");
    assert_eq!(head_field(v77_java, "terminal"), "ReturnedBeforeNextHead");
    let v77_summary = v77_rows[9];
    assert_eq!(
        head_field(v77_summary, "schema"),
        "stems-head-phase-prefix-v77"
    );
    assert_eq!(head_field(v77_summary, "rows"), "9");
    assert_eq!(
        head_field(v77_summary, "baseV76RunnerSourceSha256"),
        "3a899ca2e8cdcfd0b2eba4fb003f42bd46cbfc121eb31c5bd0ab428640f86ee3"
    );
    assert_eq!(
        head_field(v77_summary, "baseV76FixtureSha256"),
        "55f04e5c204f82b3e74d275e93cc93603ffc7b0a6f9e0e78bd7bcb6113544594"
    );
    assert_eq!(
        head_field(v77_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v77_summary, "probeSourceSha256"),
        "627a176c3b7a3af9ce9c2e2d19abd3890d5f91b0b59b05e2fd1eab15bc298921"
    );
    assert_eq!(
        head_field(v77_summary, "runnerSourceSha256"),
        "5f4d3bed7d720e1025a85cacb6af2b8340cf154ae1f957c214b9cef392362095"
    );
    assert_eq!(
        head_field(v77_summary, "emittedBodySha256"),
        "31dcc8d4596bd6d432a7db84d2c77c8ffdd87a646b1227139bd27f4ad82ec0e9"
    );
    assert_eq!(
        head_field(v77_summary, "semanticPassSha256"),
        "bfb57a604a630af0fab137d936b5569520b70e28e163956feed80f733040950f"
    );
    assert_eq!(head_field(v77_summary, "freshRuns"), "2");
    assert_eq!(head_field(v77_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v77_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder77ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v77_summary, "javaEvidence"),
        "ReturnedBeforeSeventyEighthHead"
    );

    // Boundary 104 expected-only order78 existing-stem reconciliation.
    let v78_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v78.txt"),
    )
    .expect("expected-only order78 existing-stem fixture");
    assert_eq!(
        sha256_hex(v78_text.as_bytes()),
        "74fb2c4b16b28528cf5c7767612ec882346f3079fb09c9c4cabf5985364a5497"
    );
    let v78_rows = v78_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v78_rows.len(), 10);
    let v78_body = format!("{}\n", v78_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v78_body.as_bytes()),
        "161ac87f7e7e77d6c54711069d0b51f2e66327101549d0e2f42d8426b5e1d9ca"
    );
    let v78_frontier = v78_rows[6];
    assert_eq!(head_field(v78_frontier, "headOrder"), "78");
    assert_eq!(head_field(v78_frontier, "headX"), "82");
    assert_eq!(head_field(v78_frontier, "headSig"), "20");
    assert_eq!(head_field(v78_frontier, "headInterId"), "1325");
    assert_eq!(head_field(v78_frontier, "cAlias"), "h:82:LEFT:BOTTOM");
    assert_eq!(head_field(v78_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v78_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v78_frontier, "relations"), "2");
    assert_eq!(head_field(v78_frontier, "glyphs"), "2");
    assert_eq!(head_field(v78_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v78_frontier, "existingGlyph"), "glyph:301");
    assert_eq!(head_field(v78_frontier, "existingActive"), "true");
    assert_eq!(head_field(v78_frontier, "existingStem"), "2358");
    let v78_result = v78_rows[7];
    assert_eq!(head_field(v78_result, "headOrder"), "78");
    assert_eq!(head_field(v78_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v78_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v78_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v78_result, "addedVertices"), "-");
    assert_eq!(head_field(v78_result, "addedEdges"), "-");
    assert_eq!(head_field(v78_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v78_result, "linkerChanges"),
        "[h:83:LEFT:BOTTOM:true:false->true:true,h:83:LEFT:TOP:true:false->true:true,h:83:RIGHT:BOTTOM:false:false->false:true,h:83:RIGHT:TOP:false:false->false:true,linker:SLinker:head:83:false:false->false:true,linker:SLinker:head:83:true:false->true:true]"
    );
    assert_eq!(
        head_field(v78_result, "linkerStateHashBefore"),
        "8c67beb2484af15ba20586a0f76ee293e9929d69ffcfe55798b7b220a6a3237f"
    );
    assert_eq!(
        head_field(v78_result, "linkerStateHashAfter"),
        "1db2621bad8b550ec66f8990eeff747e01d5e36d39f58d617a2379acd6d8d61d"
    );
    let v78_java = v78_rows[8];
    assert_eq!(head_field(v78_java, "headOrder"), "78");
    assert_eq!(head_field(v78_java, "headX"), "82");
    assert_eq!(head_field(v78_java, "headSig"), "20");
    assert_eq!(head_field(v78_java, "headInterId"), "1325");
    assert_eq!(
        head_field(v78_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v78_java, "incident"),
        "[stem2358:headSideLEFT:heads[x82:sig20:id1325:sideLEFT,x83:sig21:id1327:sideLEFT]]"
    );
    assert_eq!(head_field(v78_java, "returned"), "true");
    assert_eq!(head_field(v78_java, "undefs"), "[]");
    assert_eq!(
        head_field(v78_java, "closureWrites"),
        "[x83:sig21:LEFT:false->true,x83:sig21:RIGHT:false->true]"
    );
    assert_eq!(head_field(v78_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v78_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v78_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v78_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v78_java, "linkerStateHashBefore"),
        "8c67beb2484af15ba20586a0f76ee293e9929d69ffcfe55798b7b220a6a3237f"
    );
    assert_eq!(
        head_field(v78_java, "linkerStateHashAfter"),
        "1db2621bad8b550ec66f8990eeff747e01d5e36d39f58d617a2379acd6d8d61d"
    );
    assert_eq!(head_field(v78_java, "nextHeadOrder"), "79");
    assert_eq!(head_field(v78_java, "nextHeadX"), "17");
    assert_eq!(head_field(v78_java, "nextHeadSig"), "10");
    assert_eq!(head_field(v78_java, "nextHeadInterId"), "1305");
    assert_eq!(head_field(v78_java, "terminal"), "ReturnedBeforeNextHead");
    let v78_summary = v78_rows[9];
    assert_eq!(
        head_field(v78_summary, "schema"),
        "stems-head-phase-prefix-v78"
    );
    assert_eq!(head_field(v78_summary, "rows"), "9");
    assert_eq!(
        head_field(v78_summary, "baseV77RunnerSourceSha256"),
        "5f4d3bed7d720e1025a85cacb6af2b8340cf154ae1f957c214b9cef392362095"
    );
    assert_eq!(
        head_field(v78_summary, "baseV77FixtureSha256"),
        "e1ab516af406562ef9465b5f63ef584ff3b021611255d6615f31dcc0ceb586e5"
    );
    assert_eq!(
        head_field(v78_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v78_summary, "probeSourceSha256"),
        "48839e812db5d2d0b5278c26f01b5d44341a775117081f065245f159d1496c42"
    );
    assert_eq!(
        head_field(v78_summary, "runnerSourceSha256"),
        "3e8e0b9e02f164e90d64899657208f4e2d21d3ccddb1ad1aeea1bccf43fcb532"
    );
    assert_eq!(
        head_field(v78_summary, "emittedBodySha256"),
        "161ac87f7e7e77d6c54711069d0b51f2e66327101549d0e2f42d8426b5e1d9ca"
    );
    assert_eq!(
        head_field(v78_summary, "semanticPassSha256"),
        "c00235909398997447a4eaa439a5ee1de0c013514cc18c2dc0f97ee338cafcad"
    );
    assert_eq!(head_field(v78_summary, "freshRuns"), "2");
    assert_eq!(head_field(v78_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v78_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder78ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v78_summary, "javaEvidence"),
        "ReturnedBeforeSeventyNinthHead"
    );

    // Boundary 105 expected-only order79 existing-stem reconciliation.
    let v79_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v79.txt"),
    )
    .expect("expected-only order79 existing-stem fixture");
    assert_eq!(
        sha256_hex(v79_text.as_bytes()),
        "071cfe1bbed8ba76c8d29d3e93bc9dcb1df5eeb223df042ff17d0c4e3a8fbb8a"
    );
    let v79_rows = v79_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v79_rows.len(), 10);
    let v79_body = format!("{}\n", v79_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v79_body.as_bytes()),
        "29a3bb4b543819c3da8c10ab5838d0659d6f381235ea1aca948bb00a11f650e3"
    );
    let v79_frontier = v79_rows[6];
    assert_eq!(head_field(v79_frontier, "headOrder"), "79");
    assert_eq!(head_field(v79_frontier, "headX"), "17");
    assert_eq!(head_field(v79_frontier, "headSig"), "10");
    assert_eq!(head_field(v79_frontier, "headInterId"), "1305");
    assert_eq!(head_field(v79_frontier, "cAlias"), "h:17:LEFT:BOTTOM");
    assert_eq!(head_field(v79_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v79_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v79_frontier, "relations"), "2");
    assert_eq!(head_field(v79_frontier, "glyphs"), "1");
    assert_eq!(head_field(v79_frontier, "candidateIdBefore"), "310");
    assert_eq!(head_field(v79_frontier, "existingGlyph"), "glyph:310");
    assert_eq!(head_field(v79_frontier, "existingActive"), "true");
    assert_eq!(head_field(v79_frontier, "existingStem"), "2372");
    let v79_result = v79_rows[7];
    assert_eq!(head_field(v79_result, "headOrder"), "79");
    assert_eq!(head_field(v79_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v79_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v79_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v79_result, "addedVertices"), "-");
    assert_eq!(head_field(v79_result, "addedEdges"), "-");
    assert_eq!(head_field(v79_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v79_result, "linkerChanges"),
        "[h:18:LEFT:BOTTOM:true:false->true:true,h:18:LEFT:TOP:true:false->true:true,h:18:RIGHT:BOTTOM:false:false->false:true,h:18:RIGHT:TOP:false:false->false:true,linker:SLinker:head:18:false:false->false:true,linker:SLinker:head:18:true:false->true:true]"
    );
    assert_eq!(
        head_field(v79_result, "linkerStateHashBefore"),
        "1db2621bad8b550ec66f8990eeff747e01d5e36d39f58d617a2379acd6d8d61d"
    );
    assert_eq!(
        head_field(v79_result, "linkerStateHashAfter"),
        "f5db2a1fa4f4bd02b06620b5e3b37d76ca435aeb219a62d187d7d3bf7fc9ed1d"
    );
    let v79_java = v79_rows[8];
    assert_eq!(head_field(v79_java, "headOrder"), "79");
    assert_eq!(head_field(v79_java, "headX"), "17");
    assert_eq!(head_field(v79_java, "headSig"), "10");
    assert_eq!(head_field(v79_java, "headInterId"), "1305");
    assert_eq!(
        head_field(v79_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v79_java, "incident"),
        "[stem2372:headSideLEFT:heads[x17:sig10:id1305:sideLEFT,x18:sig4:id1293:sideLEFT]]"
    );
    assert_eq!(head_field(v79_java, "returned"), "true");
    assert_eq!(head_field(v79_java, "undefs"), "[]");
    assert_eq!(
        head_field(v79_java, "closureWrites"),
        "[x18:sig4:LEFT:false->true,x18:sig4:RIGHT:false->true]"
    );
    assert_eq!(head_field(v79_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v79_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v79_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v79_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v79_java, "linkerStateHashBefore"),
        "1db2621bad8b550ec66f8990eeff747e01d5e36d39f58d617a2379acd6d8d61d"
    );
    assert_eq!(
        head_field(v79_java, "linkerStateHashAfter"),
        "f5db2a1fa4f4bd02b06620b5e3b37d76ca435aeb219a62d187d7d3bf7fc9ed1d"
    );
    assert_eq!(head_field(v79_java, "nextHeadOrder"), "80");
    assert_eq!(head_field(v79_java, "nextHeadX"), "29");
    assert_eq!(head_field(v79_java, "nextHeadSig"), "66");
    assert_eq!(head_field(v79_java, "nextHeadInterId"), "1421");
    assert_eq!(head_field(v79_java, "terminal"), "ReturnedBeforeNextHead");
    let v79_summary = v79_rows[9];
    assert_eq!(
        head_field(v79_summary, "schema"),
        "stems-head-phase-prefix-v79"
    );
    assert_eq!(head_field(v79_summary, "rows"), "9");
    assert_eq!(
        head_field(v79_summary, "baseV78RunnerSourceSha256"),
        "3e8e0b9e02f164e90d64899657208f4e2d21d3ccddb1ad1aeea1bccf43fcb532"
    );
    assert_eq!(
        head_field(v79_summary, "baseV78FixtureSha256"),
        "74fb2c4b16b28528cf5c7767612ec882346f3079fb09c9c4cabf5985364a5497"
    );
    assert_eq!(
        head_field(v79_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v79_summary, "probeSourceSha256"),
        "dfb435a85daba3c7007e6db7b49589ba42056e0f2c21689baa2af9a5a7979a77"
    );
    assert_eq!(
        head_field(v79_summary, "runnerSourceSha256"),
        "c76ece1975ddf35e64f0018355190346559e0bcd5329b5ef868bc3ac44a7ad32"
    );
    assert_eq!(
        head_field(v79_summary, "emittedBodySha256"),
        "29a3bb4b543819c3da8c10ab5838d0659d6f381235ea1aca948bb00a11f650e3"
    );
    assert_eq!(
        head_field(v79_summary, "semanticPassSha256"),
        "62436c569f2746ebbf8820758e54cfe6353e2817bf04b1960066dbacdad8bd3f"
    );
    assert_eq!(head_field(v79_summary, "freshRuns"), "2");
    assert_eq!(head_field(v79_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v79_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder79ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v79_summary, "javaEvidence"),
        "ReturnedBeforeEightiethHead"
    );

    // Boundary 106 expected-only order80 existing-stem reconciliation.
    let v80_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v80.txt"),
    )
    .expect("expected-only order80 existing-stem fixture");
    assert_eq!(
        sha256_hex(v80_text.as_bytes()),
        "71e7a89bd72b1f4df666a348f02a9ba9180b8189493a1ec5b6f82aa3ff5158a3"
    );
    let v80_rows = v80_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v80_rows.len(), 10);
    let v80_body = format!("{}\n", v80_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v80_body.as_bytes()),
        "4718edfb6b62f362ecabfadb4fd5f624eaef352d7ec6dd57ee59d346bf1dfdfb"
    );
    let v80_frontier = v80_rows[6];
    assert_eq!(head_field(v80_frontier, "headOrder"), "80");
    assert_eq!(head_field(v80_frontier, "headX"), "29");
    assert_eq!(head_field(v80_frontier, "headSig"), "66");
    assert_eq!(head_field(v80_frontier, "headInterId"), "1421");
    assert_eq!(head_field(v80_frontier, "cAlias"), "h:29:LEFT:BOTTOM");
    assert_eq!(head_field(v80_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v80_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v80_frontier, "relations"), "1");
    assert_eq!(head_field(v80_frontier, "glyphs"), "2");
    assert_eq!(head_field(v80_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v80_frontier, "existingGlyph"), "glyph:313");
    assert_eq!(head_field(v80_frontier, "existingActive"), "true");
    assert_eq!(head_field(v80_frontier, "existingStem"), "2357");
    let v80_result = v80_rows[7];
    assert_eq!(head_field(v80_result, "headOrder"), "80");
    assert_eq!(head_field(v80_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v80_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v80_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v80_result, "addedVertices"), "-");
    assert_eq!(head_field(v80_result, "addedEdges"), "-");
    assert_eq!(head_field(v80_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v80_result, "linkerChanges"),
        "[h:30:LEFT:BOTTOM:true:false->true:true,h:30:LEFT:TOP:true:false->true:true,h:30:RIGHT:BOTTOM:false:false->false:true,h:30:RIGHT:TOP:false:false->false:true,linker:SLinker:head:30:false:false->false:true,linker:SLinker:head:30:true:false->true:true]"
    );
    assert_eq!(
        head_field(v80_result, "linkerStateHashBefore"),
        "f5db2a1fa4f4bd02b06620b5e3b37d76ca435aeb219a62d187d7d3bf7fc9ed1d"
    );
    assert_eq!(
        head_field(v80_result, "linkerStateHashAfter"),
        "ef0893775cb84094c9de4369470e068bab44dd0bb9318792b4f603f89e616cd2"
    );
    let v80_java = v80_rows[8];
    assert_eq!(head_field(v80_java, "headOrder"), "80");
    assert_eq!(head_field(v80_java, "headX"), "29");
    assert_eq!(head_field(v80_java, "headSig"), "66");
    assert_eq!(head_field(v80_java, "headInterId"), "1421");
    assert_eq!(
        head_field(v80_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v80_java, "incident"),
        "[stem2357:headSideLEFT:heads[x29:sig66:id1421:sideLEFT,x30:sig67:id1423:sideLEFT]]"
    );
    assert_eq!(head_field(v80_java, "returned"), "true");
    assert_eq!(head_field(v80_java, "undefs"), "[]");
    assert_eq!(
        head_field(v80_java, "closureWrites"),
        "[x30:sig67:LEFT:false->true,x30:sig67:RIGHT:false->true]"
    );
    assert_eq!(head_field(v80_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v80_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v80_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v80_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v80_java, "linkerStateHashBefore"),
        "f5db2a1fa4f4bd02b06620b5e3b37d76ca435aeb219a62d187d7d3bf7fc9ed1d"
    );
    assert_eq!(
        head_field(v80_java, "linkerStateHashAfter"),
        "ef0893775cb84094c9de4369470e068bab44dd0bb9318792b4f603f89e616cd2"
    );
    assert_eq!(head_field(v80_java, "nextHeadOrder"), "81");
    assert_eq!(head_field(v80_java, "nextHeadX"), "98");
    assert_eq!(head_field(v80_java, "nextHeadSig"), "60");
    assert_eq!(head_field(v80_java, "nextHeadInterId"), "1409");
    assert_eq!(head_field(v80_java, "terminal"), "ReturnedBeforeNextHead");
    let v80_summary = v80_rows[9];
    assert_eq!(
        head_field(v80_summary, "schema"),
        "stems-head-phase-prefix-v80"
    );
    assert_eq!(head_field(v80_summary, "rows"), "9");
    assert_eq!(
        head_field(v80_summary, "baseV79RunnerSourceSha256"),
        "c76ece1975ddf35e64f0018355190346559e0bcd5329b5ef868bc3ac44a7ad32"
    );
    assert_eq!(
        head_field(v80_summary, "baseV79FixtureSha256"),
        "071cfe1bbed8ba76c8d29d3e93bc9dcb1df5eeb223df042ff17d0c4e3a8fbb8a"
    );
    assert_eq!(
        head_field(v80_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v80_summary, "probeSourceSha256"),
        "f9e55b6d72eb01e9b9ebd965ea9e44ef39c725866764b9e83254c43f798309af"
    );
    assert_eq!(
        head_field(v80_summary, "runnerSourceSha256"),
        "f2a107f92bcd9beb6005b2d098832ff1b71fc8b2f342ff069b0e980ed03b97b1"
    );
    assert_eq!(
        head_field(v80_summary, "emittedBodySha256"),
        "4718edfb6b62f362ecabfadb4fd5f624eaef352d7ec6dd57ee59d346bf1dfdfb"
    );
    assert_eq!(
        head_field(v80_summary, "semanticPassSha256"),
        "dfbe81d6e60f7761029afc2ec42b471e528af0c53d3fe7c20e93af8c15386411"
    );
    assert_eq!(head_field(v80_summary, "freshRuns"), "2");
    assert_eq!(head_field(v80_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v80_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder80ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v80_summary, "javaEvidence"),
        "ReturnedBeforeEightyFirstHead"
    );

    // Boundary 107 expected-only order81 existing-stem reconciliation.
    let v81_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v81.txt"),
    )
    .expect("expected-only order81 existing-stem fixture");
    assert_eq!(
        sha256_hex(v81_text.as_bytes()),
        "5b4f89b4cb52fd0075e176a4c6dfe1c78571bc1403b9602cc716c02d63c7488f"
    );
    let v81_rows = v81_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v81_rows.len(), 10);
    let v81_body = format!("{}\n", v81_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v81_body.as_bytes()),
        "9e0555d748b45dd473a30b2156ea0d0bfa4e025ed9273cb1d84b04ebdd898dfd"
    );
    let v81_frontier = v81_rows[6];
    assert_eq!(head_field(v81_frontier, "headOrder"), "81");
    assert_eq!(head_field(v81_frontier, "headX"), "98");
    assert_eq!(head_field(v81_frontier, "headSig"), "60");
    assert_eq!(head_field(v81_frontier, "headInterId"), "1409");
    assert_eq!(head_field(v81_frontier, "cAlias"), "h:98:LEFT:BOTTOM");
    assert_eq!(head_field(v81_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v81_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v81_frontier, "relations"), "1");
    assert_eq!(head_field(v81_frontier, "glyphs"), "1");
    assert_eq!(head_field(v81_frontier, "candidateIdBefore"), "330");
    assert_eq!(head_field(v81_frontier, "existingGlyph"), "glyph:330");
    assert_eq!(head_field(v81_frontier, "existingActive"), "true");
    assert_eq!(head_field(v81_frontier, "existingStem"), "2365");
    let v81_result = v81_rows[7];
    assert_eq!(head_field(v81_result, "headOrder"), "81");
    assert_eq!(head_field(v81_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v81_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v81_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v81_result, "addedVertices"), "-");
    assert_eq!(head_field(v81_result, "addedEdges"), "-");
    assert_eq!(head_field(v81_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v81_result, "linkerChanges"),
        "[h:99:LEFT:BOTTOM:true:false->true:true,h:99:LEFT:TOP:true:false->true:true,h:99:RIGHT:BOTTOM:false:false->false:true,h:99:RIGHT:TOP:false:false->false:true,linker:SLinker:head:99:false:false->false:true,linker:SLinker:head:99:true:false->true:true]"
    );
    assert_eq!(
        head_field(v81_result, "linkerStateHashBefore"),
        "ef0893775cb84094c9de4369470e068bab44dd0bb9318792b4f603f89e616cd2"
    );
    assert_eq!(
        head_field(v81_result, "linkerStateHashAfter"),
        "3261ef26c0298b7daf2337826610aef091b993947790686780b588d1ce4158b6"
    );
    let v81_java = v81_rows[8];
    assert_eq!(head_field(v81_java, "headOrder"), "81");
    assert_eq!(head_field(v81_java, "headX"), "98");
    assert_eq!(head_field(v81_java, "headSig"), "60");
    assert_eq!(head_field(v81_java, "headInterId"), "1409");
    assert_eq!(
        head_field(v81_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v81_java, "incident"),
        "[stem2365:headSideLEFT:heads[x98:sig60:id1409:sideLEFT,x99:sig61:id1411:sideLEFT]]"
    );
    assert_eq!(head_field(v81_java, "returned"), "true");
    assert_eq!(head_field(v81_java, "undefs"), "[]");
    assert_eq!(
        head_field(v81_java, "closureWrites"),
        "[x99:sig61:LEFT:false->true,x99:sig61:RIGHT:false->true]"
    );
    assert_eq!(head_field(v81_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v81_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v81_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v81_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v81_java, "linkerStateHashBefore"),
        "ef0893775cb84094c9de4369470e068bab44dd0bb9318792b4f603f89e616cd2"
    );
    assert_eq!(
        head_field(v81_java, "linkerStateHashAfter"),
        "3261ef26c0298b7daf2337826610aef091b993947790686780b588d1ce4158b6"
    );
    assert_eq!(head_field(v81_java, "nextHeadOrder"), "82");
    assert_eq!(head_field(v81_java, "nextHeadX"), "80");
    assert_eq!(head_field(v81_java, "nextHeadSig"), "32");
    assert_eq!(head_field(v81_java, "nextHeadInterId"), "1349");
    assert_eq!(head_field(v81_java, "terminal"), "ReturnedBeforeNextHead");
    let v81_summary = v81_rows[9];
    assert_eq!(
        head_field(v81_summary, "schema"),
        "stems-head-phase-prefix-v81"
    );
    assert_eq!(head_field(v81_summary, "rows"), "9");
    assert_eq!(
        head_field(v81_summary, "baseV80RunnerSourceSha256"),
        "f2a107f92bcd9beb6005b2d098832ff1b71fc8b2f342ff069b0e980ed03b97b1"
    );
    assert_eq!(
        head_field(v81_summary, "baseV80FixtureSha256"),
        "71e7a89bd72b1f4df666a348f02a9ba9180b8189493a1ec5b6f82aa3ff5158a3"
    );
    assert_eq!(
        head_field(v81_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v81_summary, "probeSourceSha256"),
        "98b3c6ca4f1ab70e90b1db0fcbfa3cb1af6e586a424be35cefffd80e817878ff"
    );
    assert_eq!(
        head_field(v81_summary, "runnerSourceSha256"),
        "6bc0373119f11d790c9010aa66e201e571f95b0a2738c4607669b0e723fb8000"
    );
    assert_eq!(
        head_field(v81_summary, "emittedBodySha256"),
        "9e0555d748b45dd473a30b2156ea0d0bfa4e025ed9273cb1d84b04ebdd898dfd"
    );
    assert_eq!(
        head_field(v81_summary, "semanticPassSha256"),
        "ee8303fdfba8dc5f8d9a4a9060e3467af85077f30ca2733604b9bfc17662ed01"
    );
    assert_eq!(head_field(v81_summary, "freshRuns"), "2");
    assert_eq!(head_field(v81_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v81_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder81ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v81_summary, "javaEvidence"),
        "ReturnedBeforeEightySecondHead"
    );

    // Boundary 108 expected-only order82 existing-stem reconciliation.
    let v82_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v82.txt"),
    )
    .expect("expected-only order82 existing-stem fixture");
    assert_eq!(
        sha256_hex(v82_text.as_bytes()),
        "79497db4a9c58519a8df51aafeed6be1eca9f85773e2a53f049abead8aebd426"
    );
    let v82_rows = v82_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v82_rows.len(), 10);
    let v82_body = format!("{}\n", v82_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v82_body.as_bytes()),
        "f7622b3b339c70fa8f8dc32adbfa97e28c3c393b6db0975a97da07287b87bf22"
    );
    let v82_frontier = v82_rows[6];
    assert_eq!(head_field(v82_frontier, "headOrder"), "82");
    assert_eq!(head_field(v82_frontier, "headX"), "80");
    assert_eq!(head_field(v82_frontier, "headSig"), "32");
    assert_eq!(head_field(v82_frontier, "headInterId"), "1349");
    assert_eq!(head_field(v82_frontier, "cAlias"), "h:80:LEFT:BOTTOM");
    assert_eq!(head_field(v82_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v82_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v82_frontier, "relations"), "1");
    assert_eq!(head_field(v82_frontier, "glyphs"), "1");
    assert_eq!(head_field(v82_frontier, "candidateIdBefore"), "306");
    assert_eq!(head_field(v82_frontier, "existingGlyph"), "glyph:306");
    assert_eq!(head_field(v82_frontier, "existingActive"), "true");
    assert_eq!(head_field(v82_frontier, "existingStem"), "2371");
    let v82_result = v82_rows[7];
    assert_eq!(head_field(v82_result, "headOrder"), "82");
    assert_eq!(head_field(v82_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v82_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v82_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v82_result, "addedVertices"), "-");
    assert_eq!(head_field(v82_result, "addedEdges"), "-");
    assert_eq!(head_field(v82_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v82_result, "linkerChanges"),
        "[h:81:LEFT:BOTTOM:true:false->true:true,h:81:LEFT:TOP:true:false->true:true,h:81:RIGHT:BOTTOM:false:false->false:true,h:81:RIGHT:TOP:false:false->false:true,linker:SLinker:head:81:false:false->false:true,linker:SLinker:head:81:true:false->true:true]"
    );
    assert_eq!(
        head_field(v82_result, "linkerStateHashBefore"),
        "3261ef26c0298b7daf2337826610aef091b993947790686780b588d1ce4158b6"
    );
    assert_eq!(
        head_field(v82_result, "linkerStateHashAfter"),
        "e699544d2f8cf8668ab3f7793d377f23b1a3501ef09a9b64bdb8ac8f42a25b77"
    );
    let v82_java = v82_rows[8];
    assert_eq!(head_field(v82_java, "headOrder"), "82");
    assert_eq!(head_field(v82_java, "headX"), "80");
    assert_eq!(head_field(v82_java, "headSig"), "32");
    assert_eq!(head_field(v82_java, "headInterId"), "1349");
    assert_eq!(
        head_field(v82_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v82_java, "incident"),
        "[stem2371:headSideLEFT:heads[x79:sig40:id1365:sideLEFT,x80:sig32:id1349:sideLEFT,x81:sig33:id1351:sideLEFT]]"
    );
    assert_eq!(head_field(v82_java, "returned"), "true");
    assert_eq!(head_field(v82_java, "undefs"), "[]");
    assert_eq!(
        head_field(v82_java, "closureWrites"),
        "[x79:sig40:LEFT:true->true,x79:sig40:RIGHT:true->true,x81:sig33:LEFT:false->true,x81:sig33:RIGHT:false->true]"
    );
    assert_eq!(head_field(v82_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v82_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v82_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v82_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v82_java, "linkerStateHashBefore"),
        "3261ef26c0298b7daf2337826610aef091b993947790686780b588d1ce4158b6"
    );
    assert_eq!(
        head_field(v82_java, "linkerStateHashAfter"),
        "e699544d2f8cf8668ab3f7793d377f23b1a3501ef09a9b64bdb8ac8f42a25b77"
    );
    assert_eq!(head_field(v82_java, "nextHeadOrder"), "83");
    assert_eq!(head_field(v82_java, "nextHeadX"), "24");
    assert_eq!(head_field(v82_java, "nextHeadSig"), "90");
    assert_eq!(head_field(v82_java, "nextHeadInterId"), "1469");
    assert_eq!(head_field(v82_java, "terminal"), "ReturnedBeforeNextHead");
    let v82_summary = v82_rows[9];
    assert_eq!(
        head_field(v82_summary, "schema"),
        "stems-head-phase-prefix-v82"
    );
    assert_eq!(head_field(v82_summary, "rows"), "9");
    assert_eq!(
        head_field(v82_summary, "baseV81RunnerSourceSha256"),
        "6bc0373119f11d790c9010aa66e201e571f95b0a2738c4607669b0e723fb8000"
    );
    assert_eq!(
        head_field(v82_summary, "baseV81FixtureSha256"),
        "5b4f89b4cb52fd0075e176a4c6dfe1c78571bc1403b9602cc716c02d63c7488f"
    );
    assert_eq!(
        head_field(v82_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v82_summary, "probeSourceSha256"),
        "b3c6dc0ea9cd682cc76d98673e1199937018848cba2699b2a9ed650780b05bcb"
    );
    assert_eq!(
        head_field(v82_summary, "runnerSourceSha256"),
        "28e24f4e4484ecd6967c627a6745be9ef150de32637bf3f3eac8523f10716ba8"
    );
    assert_eq!(
        head_field(v82_summary, "emittedBodySha256"),
        "f7622b3b339c70fa8f8dc32adbfa97e28c3c393b6db0975a97da07287b87bf22"
    );
    assert_eq!(
        head_field(v82_summary, "semanticPassSha256"),
        "252ec7e4dc486816183985eb5cce77849f408211e4d2d7cc4edf4373d820d846"
    );
    assert_eq!(head_field(v82_summary, "freshRuns"), "2");
    assert_eq!(head_field(v82_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v82_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder82ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v82_summary, "javaEvidence"),
        "ReturnedBeforeEightyThirdHead"
    );

    // Boundary 109 expected-only order83 existing-stem reconciliation.
    let v83_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v83.txt"),
    )
    .expect("expected-only order83 existing-stem fixture");
    assert_eq!(
        sha256_hex(v83_text.as_bytes()),
        "c37d7e4015e34d2c0d61cd7c4159ccdd6834b4d575f650d612a5c1d6f94d8cb1"
    );
    let v83_rows = v83_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v83_rows.len(), 10);
    let v83_body = format!("{}\n", v83_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v83_body.as_bytes()),
        "a922ae5d54324f8708ca1c77da1d1e2429fc4927c2816d720bd92fe626176d86"
    );
    let v83_frontier = v83_rows[6];
    assert_eq!(head_field(v83_frontier, "headOrder"), "83");
    assert_eq!(head_field(v83_frontier, "headX"), "24");
    assert_eq!(head_field(v83_frontier, "headSig"), "90");
    assert_eq!(head_field(v83_frontier, "headInterId"), "1469");
    assert_eq!(head_field(v83_frontier, "cAlias"), "h:24:LEFT:BOTTOM");
    assert_eq!(head_field(v83_frontier, "lastIndex"), "3");
    assert_eq!(head_field(v83_frontier, "maxIndex"), "3");
    assert_eq!(head_field(v83_frontier, "relations"), "3");
    assert_eq!(head_field(v83_frontier, "glyphs"), "2");
    assert_eq!(head_field(v83_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v83_frontier, "existingGlyph"), "glyph:292");
    assert_eq!(head_field(v83_frontier, "existingActive"), "true");
    assert_eq!(head_field(v83_frontier, "existingStem"), "2356");
    let v83_result = v83_rows[7];
    assert_eq!(head_field(v83_result, "headOrder"), "83");
    assert_eq!(head_field(v83_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v83_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v83_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v83_result, "addedVertices"), "-");
    assert_eq!(head_field(v83_result, "addedEdges"), "-");
    assert_eq!(head_field(v83_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v83_result, "linkerChanges"),
        "[h:25:LEFT:BOTTOM:true:false->true:true,h:25:LEFT:TOP:true:false->true:true,h:25:RIGHT:BOTTOM:false:false->false:true,h:25:RIGHT:TOP:false:false->false:true,linker:SLinker:head:25:false:false->false:true,linker:SLinker:head:25:true:false->true:true]"
    );
    assert_eq!(
        head_field(v83_result, "linkerStateHashBefore"),
        "e699544d2f8cf8668ab3f7793d377f23b1a3501ef09a9b64bdb8ac8f42a25b77"
    );
    assert_eq!(
        head_field(v83_result, "linkerStateHashAfter"),
        "f3f775b9d586ef8f381d50ce065dd5c0cde605fb5c8e40b0e6543b674acb986a"
    );
    let v83_java = v83_rows[8];
    assert_eq!(head_field(v83_java, "headOrder"), "83");
    assert_eq!(head_field(v83_java, "headX"), "24");
    assert_eq!(head_field(v83_java, "headSig"), "90");
    assert_eq!(head_field(v83_java, "headInterId"), "1469");
    assert_eq!(
        head_field(v83_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v83_java, "incident"),
        "[stem2356:headSideLEFT:heads[x24:sig90:id1469:sideLEFT,x25:sig91:id1471:sideLEFT]]"
    );
    assert_eq!(head_field(v83_java, "returned"), "true");
    assert_eq!(head_field(v83_java, "undefs"), "[]");
    assert_eq!(
        head_field(v83_java, "closureWrites"),
        "[x25:sig91:LEFT:false->true,x25:sig91:RIGHT:false->true]"
    );
    assert_eq!(head_field(v83_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v83_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v83_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v83_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v83_java, "linkerStateHashBefore"),
        "e699544d2f8cf8668ab3f7793d377f23b1a3501ef09a9b64bdb8ac8f42a25b77"
    );
    assert_eq!(
        head_field(v83_java, "linkerStateHashAfter"),
        "f3f775b9d586ef8f381d50ce065dd5c0cde605fb5c8e40b0e6543b674acb986a"
    );
    assert_eq!(head_field(v83_java, "nextHeadOrder"), "84");
    assert_eq!(head_field(v83_java, "nextHeadX"), "94");
    assert_eq!(head_field(v83_java, "nextHeadSig"), "99");
    assert_eq!(head_field(v83_java, "nextHeadInterId"), "1487");
    assert_eq!(head_field(v83_java, "terminal"), "ReturnedBeforeNextHead");
    let v83_summary = v83_rows[9];
    assert_eq!(
        head_field(v83_summary, "schema"),
        "stems-head-phase-prefix-v83"
    );
    assert_eq!(head_field(v83_summary, "rows"), "9");
    assert_eq!(
        head_field(v83_summary, "baseV82RunnerSourceSha256"),
        "28e24f4e4484ecd6967c627a6745be9ef150de32637bf3f3eac8523f10716ba8"
    );
    assert_eq!(
        head_field(v83_summary, "baseV82FixtureSha256"),
        "79497db4a9c58519a8df51aafeed6be1eca9f85773e2a53f049abead8aebd426"
    );
    assert_eq!(
        head_field(v83_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v83_summary, "probeSourceSha256"),
        "d254cb18d4822e60f946f910496e8a1a1bc062e29d0f616135cc9505b03f6b3c"
    );
    assert_eq!(
        head_field(v83_summary, "runnerSourceSha256"),
        "5d1a030d4d98807e022bed40cd4fa44b4057dad9f123b537bdeb17b48fd97a90"
    );
    assert_eq!(
        head_field(v83_summary, "emittedBodySha256"),
        "a922ae5d54324f8708ca1c77da1d1e2429fc4927c2816d720bd92fe626176d86"
    );
    assert_eq!(
        head_field(v83_summary, "semanticPassSha256"),
        "0f284ae561087b6543f434efe9f41b06d714c7e4a2a674a983f1c1d866ab1e0f"
    );
    assert_eq!(head_field(v83_summary, "freshRuns"), "2");
    assert_eq!(head_field(v83_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v83_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder83ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v83_summary, "javaEvidence"),
        "ReturnedBeforeEightyFourthHead"
    );

    // Boundary 110 expected-only order84 existing-stem reconciliation.
    let v84_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v84.txt"),
    )
    .expect("expected-only order84 existing-stem fixture");
    assert_eq!(
        sha256_hex(v84_text.as_bytes()),
        "a5be0414d7c9035cfabd8d023a4b50b8e0ff5d89d14f5f3fbe56d33c5abf18c9"
    );
    let v84_rows = v84_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v84_rows.len(), 10);
    let v84_body = format!("{}\n", v84_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v84_body.as_bytes()),
        "1bd8a1e7a4de7b8c35bf78183c0392fb55ddd79f7ea244839cc8fadc8c47550e"
    );
    let v84_frontier = v84_rows[6];
    assert_eq!(head_field(v84_frontier, "headOrder"), "84");
    assert_eq!(head_field(v84_frontier, "headX"), "94");
    assert_eq!(head_field(v84_frontier, "headSig"), "99");
    assert_eq!(head_field(v84_frontier, "headInterId"), "1487");
    assert_eq!(head_field(v84_frontier, "cAlias"), "h:94:LEFT:BOTTOM");
    assert_eq!(head_field(v84_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v84_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v84_frontier, "relations"), "2");
    assert_eq!(head_field(v84_frontier, "glyphs"), "1");
    assert_eq!(head_field(v84_frontier, "candidateIdBefore"), "297");
    assert_eq!(head_field(v84_frontier, "existingGlyph"), "glyph:297");
    assert_eq!(head_field(v84_frontier, "existingActive"), "true");
    assert_eq!(head_field(v84_frontier, "existingStem"), "2364");
    let v84_result = v84_rows[7];
    assert_eq!(head_field(v84_result, "headOrder"), "84");
    assert_eq!(head_field(v84_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v84_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v84_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v84_result, "addedVertices"), "-");
    assert_eq!(head_field(v84_result, "addedEdges"), "-");
    assert_eq!(head_field(v84_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v84_result, "linkerChanges"),
        "[h:95:LEFT:BOTTOM:true:false->true:true,h:95:LEFT:TOP:true:false->true:true,h:95:RIGHT:BOTTOM:false:false->false:true,h:95:RIGHT:TOP:false:false->false:true,linker:SLinker:head:95:false:false->false:true,linker:SLinker:head:95:true:false->true:true]"
    );
    assert_eq!(
        head_field(v84_result, "linkerStateHashBefore"),
        "f3f775b9d586ef8f381d50ce065dd5c0cde605fb5c8e40b0e6543b674acb986a"
    );
    assert_eq!(
        head_field(v84_result, "linkerStateHashAfter"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    let v84_java = v84_rows[8];
    assert_eq!(head_field(v84_java, "headOrder"), "84");
    assert_eq!(head_field(v84_java, "headX"), "94");
    assert_eq!(head_field(v84_java, "headSig"), "99");
    assert_eq!(head_field(v84_java, "headInterId"), "1487");
    assert_eq!(
        head_field(v84_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v84_java, "incident"),
        "[stem2364:headSideLEFT:heads[x91:sig98:id1485:sideLEFT,x94:sig99:id1487:sideLEFT,x95:sig100:id1489:sideLEFT]]"
    );
    assert_eq!(head_field(v84_java, "returned"), "true");
    assert_eq!(head_field(v84_java, "undefs"), "[]");
    assert_eq!(
        head_field(v84_java, "closureWrites"),
        "[x91:sig98:LEFT:true->true,x91:sig98:RIGHT:true->true,x95:sig100:LEFT:false->true,x95:sig100:RIGHT:false->true]"
    );
    assert_eq!(head_field(v84_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v84_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v84_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v84_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v84_java, "linkerStateHashBefore"),
        "f3f775b9d586ef8f381d50ce065dd5c0cde605fb5c8e40b0e6543b674acb986a"
    );
    assert_eq!(
        head_field(v84_java, "linkerStateHashAfter"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    assert_eq!(head_field(v84_java, "nextHeadOrder"), "85");
    assert_eq!(head_field(v84_java, "nextHeadX"), "79");
    assert_eq!(head_field(v84_java, "nextHeadSig"), "40");
    assert_eq!(head_field(v84_java, "nextHeadInterId"), "1365");
    assert_eq!(head_field(v84_java, "terminal"), "ReturnedBeforeNextHead");
    let v84_summary = v84_rows[9];
    assert_eq!(
        head_field(v84_summary, "schema"),
        "stems-head-phase-prefix-v84"
    );
    assert_eq!(head_field(v84_summary, "rows"), "9");
    assert_eq!(
        head_field(v84_summary, "baseV83RunnerSourceSha256"),
        "5d1a030d4d98807e022bed40cd4fa44b4057dad9f123b537bdeb17b48fd97a90"
    );
    assert_eq!(
        head_field(v84_summary, "baseV83FixtureSha256"),
        "c37d7e4015e34d2c0d61cd7c4159ccdd6834b4d575f650d612a5c1d6f94d8cb1"
    );
    assert_eq!(
        head_field(v84_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v84_summary, "probeSourceSha256"),
        "c23e04e0c13f58708b59971496d4cc6c20ccbcfcae2f10412d1cc3c50081d408"
    );
    assert_eq!(
        head_field(v84_summary, "runnerSourceSha256"),
        "87552c47731d7c9692cfc3f1cbcfd8a5dde655c598d3b83069257b09dd8c286f"
    );
    assert_eq!(
        head_field(v84_summary, "emittedBodySha256"),
        "1bd8a1e7a4de7b8c35bf78183c0392fb55ddd79f7ea244839cc8fadc8c47550e"
    );
    assert_eq!(
        head_field(v84_summary, "semanticPassSha256"),
        "07cfe09f25afec97ffc8cc256ff64c2c282233395ab9071831d0e5284fbcc510"
    );
    assert_eq!(head_field(v84_summary, "freshRuns"), "2");
    assert_eq!(head_field(v84_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v84_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder84ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v84_summary, "javaEvidence"),
        "ReturnedBeforeEightyFifthHead"
    );

    // Boundary 111 expected-only order85 existing-stem reconciliation.
    let v85_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v85.txt"),
    )
    .expect("expected-only order85 existing-stem fixture");
    assert_eq!(
        sha256_hex(v85_text.as_bytes()),
        "ac38c32459c9cc0afeef77572a060e6ef336005671296924424d559e003bcb0f"
    );
    let v85_rows = v85_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v85_rows.len(), 10);
    let v85_body = format!("{}\n", v85_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v85_body.as_bytes()),
        "4f0deda93b3349d9ca85dc36a5708e47b2a4908db14bdab1790f56d9f5e85738"
    );
    let v85_frontier = v85_rows[6];
    assert_eq!(head_field(v85_frontier, "headOrder"), "85");
    assert_eq!(head_field(v85_frontier, "headX"), "79");
    assert_eq!(head_field(v85_frontier, "headSig"), "40");
    assert_eq!(head_field(v85_frontier, "headInterId"), "1365");
    assert_eq!(head_field(v85_frontier, "cAlias"), "h:79:LEFT:BOTTOM");
    assert_eq!(head_field(v85_frontier, "lastIndex"), "2");
    assert_eq!(head_field(v85_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v85_frontier, "relations"), "3");
    assert_eq!(head_field(v85_frontier, "glyphs"), "1");
    assert_eq!(head_field(v85_frontier, "candidateIdBefore"), "306");
    assert_eq!(head_field(v85_frontier, "existingGlyph"), "glyph:306");
    assert_eq!(head_field(v85_frontier, "existingActive"), "true");
    assert_eq!(head_field(v85_frontier, "existingStem"), "2371");
    let v85_result = v85_rows[7];
    assert_eq!(head_field(v85_result, "headOrder"), "85");
    assert_eq!(head_field(v85_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v85_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v85_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v85_result, "addedVertices"), "-");
    assert_eq!(head_field(v85_result, "addedEdges"), "-");
    assert_eq!(head_field(v85_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v85_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v85_result, "linkerStateHashBefore"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    assert_eq!(
        head_field(v85_result, "linkerStateHashAfter"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    let v85_java = v85_rows[8];
    assert_eq!(head_field(v85_java, "headOrder"), "85");
    assert_eq!(head_field(v85_java, "headX"), "79");
    assert_eq!(head_field(v85_java, "headSig"), "40");
    assert_eq!(head_field(v85_java, "headInterId"), "1365");
    assert_eq!(
        head_field(v85_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v85_java, "incident"),
        "[stem2371:headSideLEFT:heads[x79:sig40:id1365:sideLEFT,x80:sig32:id1349:sideLEFT,x81:sig33:id1351:sideLEFT]]"
    );
    assert_eq!(head_field(v85_java, "returned"), "true");
    assert_eq!(head_field(v85_java, "undefs"), "[]");
    assert_eq!(
        head_field(v85_java, "closureWrites"),
        "[x80:sig32:LEFT:true->true,x80:sig32:RIGHT:true->true,x81:sig33:LEFT:true->true,x81:sig33:RIGHT:true->true]"
    );
    assert_eq!(head_field(v85_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v85_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v85_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v85_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v85_java, "linkerStateHashBefore"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    assert_eq!(
        head_field(v85_java, "linkerStateHashAfter"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    assert_eq!(head_field(v85_java, "nextHeadOrder"), "86");
    assert_eq!(head_field(v85_java, "nextHeadX"), "51");
    assert_eq!(head_field(v85_java, "nextHeadSig"), "82");
    assert_eq!(head_field(v85_java, "nextHeadInterId"), "1453");
    assert_eq!(head_field(v85_java, "terminal"), "ReturnedBeforeNextHead");
    let v85_summary = v85_rows[9];
    assert_eq!(
        head_field(v85_summary, "schema"),
        "stems-head-phase-prefix-v85"
    );
    assert_eq!(head_field(v85_summary, "rows"), "9");
    assert_eq!(
        head_field(v85_summary, "baseV84RunnerSourceSha256"),
        "87552c47731d7c9692cfc3f1cbcfd8a5dde655c598d3b83069257b09dd8c286f"
    );
    assert_eq!(
        head_field(v85_summary, "baseV84FixtureSha256"),
        "a5be0414d7c9035cfabd8d023a4b50b8e0ff5d89d14f5f3fbe56d33c5abf18c9"
    );
    assert_eq!(
        head_field(v85_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v85_summary, "probeSourceSha256"),
        "2bb3d131fb888a3031200989770ee53a541a027d7d39f080aacc224dfe648dfe"
    );
    assert_eq!(
        head_field(v85_summary, "runnerSourceSha256"),
        "7b604fa112d346eb560e3086ce31b3d19e929cb45cdda52e172c2178ed8d130a"
    );
    assert_eq!(
        head_field(v85_summary, "emittedBodySha256"),
        "4f0deda93b3349d9ca85dc36a5708e47b2a4908db14bdab1790f56d9f5e85738"
    );
    assert_eq!(
        head_field(v85_summary, "semanticPassSha256"),
        "888fa13db21c733dcf1ad0c1a16674103f9f28ea3175090744b57fbc94ee28a8"
    );
    assert_eq!(head_field(v85_summary, "freshRuns"), "2");
    assert_eq!(head_field(v85_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v85_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder85ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v85_summary, "javaEvidence"),
        "ReturnedBeforeEightySixthHead"
    );

    // Boundary 112 expected-only order86 existing-stem reconciliation.
    let v86_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v86.txt"),
    )
    .expect("expected-only order86 existing-stem fixture");
    assert_eq!(
        sha256_hex(v86_text.as_bytes()),
        "9d2a075961321850ffb365466b5d0d9fed9f447a31bf7dc9f83e3571b0b69097"
    );
    let v86_rows = v86_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v86_rows.len(), 10);
    let v86_body = format!("{}\n", v86_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v86_body.as_bytes()),
        "64ae71ecfb8de0f4aa27872fa5fe872a386eb4f33de4248aedf5b415726c88de"
    );
    let v86_frontier = v86_rows[6];
    assert_eq!(head_field(v86_frontier, "headOrder"), "86");
    assert_eq!(head_field(v86_frontier, "headX"), "51");
    assert_eq!(head_field(v86_frontier, "headSig"), "82");
    assert_eq!(head_field(v86_frontier, "headInterId"), "1453");
    assert_eq!(head_field(v86_frontier, "cAlias"), "h:51:LEFT:BOTTOM");
    assert_eq!(head_field(v86_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v86_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v86_frontier, "relations"), "1");
    assert_eq!(head_field(v86_frontier, "glyphs"), "1");
    assert_eq!(head_field(v86_frontier, "candidateIdBefore"), "334");
    assert_eq!(head_field(v86_frontier, "existingGlyph"), "glyph:334");
    assert_eq!(head_field(v86_frontier, "existingActive"), "true");
    assert_eq!(head_field(v86_frontier, "existingStem"), "2362");
    let v86_result = v86_rows[7];
    assert_eq!(head_field(v86_result, "headOrder"), "86");
    assert_eq!(head_field(v86_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v86_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v86_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v86_result, "addedVertices"), "-");
    assert_eq!(head_field(v86_result, "addedEdges"), "-");
    assert_eq!(head_field(v86_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v86_result, "linkerChanges"),
        "[h:55:LEFT:BOTTOM:true:false->true:true,h:55:LEFT:TOP:true:false->true:true,h:55:RIGHT:BOTTOM:false:false->false:true,h:55:RIGHT:TOP:false:false->false:true,linker:SLinker:head:55:false:false->false:true,linker:SLinker:head:55:true:false->true:true]"
    );
    assert_eq!(
        head_field(v86_result, "linkerStateHashBefore"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    assert_eq!(
        head_field(v86_result, "linkerStateHashAfter"),
        "078a43263e8b27433224cb8f316c0705b6d1cb77620aecaa2e434bdf916874eb"
    );
    let v86_java = v86_rows[8];
    assert_eq!(head_field(v86_java, "headOrder"), "86");
    assert_eq!(head_field(v86_java, "headX"), "51");
    assert_eq!(head_field(v86_java, "headSig"), "82");
    assert_eq!(head_field(v86_java, "headInterId"), "1453");
    assert_eq!(
        head_field(v86_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v86_java, "incident"),
        "[stem2362:headSideLEFT:heads[x51:sig82:id1453:sideLEFT,x54:sig78:id1445:sideLEFT,x55:sig79:id1447:sideLEFT]]"
    );
    assert_eq!(head_field(v86_java, "returned"), "true");
    assert_eq!(head_field(v86_java, "undefs"), "[]");
    assert_eq!(
        head_field(v86_java, "closureWrites"),
        "[x54:sig78:LEFT:true->true,x54:sig78:RIGHT:true->true,x55:sig79:LEFT:false->true,x55:sig79:RIGHT:false->true]"
    );
    assert_eq!(head_field(v86_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v86_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v86_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v86_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v86_java, "linkerStateHashBefore"),
        "7ca029fc2094670ebd9d540e25a9e56b7e4eef2514bf1c1fe019ca0ef619c2db"
    );
    assert_eq!(
        head_field(v86_java, "linkerStateHashAfter"),
        "078a43263e8b27433224cb8f316c0705b6d1cb77620aecaa2e434bdf916874eb"
    );
    assert_eq!(head_field(v86_java, "nextHeadOrder"), "87");
    assert_eq!(head_field(v86_java, "nextHeadX"), "45");
    assert_eq!(head_field(v86_java, "nextHeadSig"), "56");
    assert_eq!(head_field(v86_java, "nextHeadInterId"), "1401");
    assert_eq!(head_field(v86_java, "terminal"), "ReturnedBeforeNextHead");
    let v86_summary = v86_rows[9];
    assert_eq!(
        head_field(v86_summary, "schema"),
        "stems-head-phase-prefix-v86"
    );
    assert_eq!(head_field(v86_summary, "rows"), "9");
    assert_eq!(
        head_field(v86_summary, "baseV85RunnerSourceSha256"),
        "7b604fa112d346eb560e3086ce31b3d19e929cb45cdda52e172c2178ed8d130a"
    );
    assert_eq!(
        head_field(v86_summary, "baseV85FixtureSha256"),
        "ac38c32459c9cc0afeef77572a060e6ef336005671296924424d559e003bcb0f"
    );
    assert_eq!(
        head_field(v86_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v86_summary, "probeSourceSha256"),
        "cb1b92a86c89dea7ae7fe6540878ef287059da7295b611fa69e2d373d587c45d"
    );
    assert_eq!(
        head_field(v86_summary, "runnerSourceSha256"),
        "724e45d8a40b4eb17284004048d9c5c49c9242c15ff70e72c55837dbd05e46fa"
    );
    assert_eq!(
        head_field(v86_summary, "emittedBodySha256"),
        "64ae71ecfb8de0f4aa27872fa5fe872a386eb4f33de4248aedf5b415726c88de"
    );
    assert_eq!(
        head_field(v86_summary, "semanticPassSha256"),
        "9818568015b0a62d5a0e544333f5d1ac374a0b99856b12d8b981bb98a4f5de9c"
    );
    assert_eq!(head_field(v86_summary, "freshRuns"), "2");
    assert_eq!(head_field(v86_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v86_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder86ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v86_summary, "javaEvidence"),
        "ReturnedBeforeEightySeventhHead"
    );

    // Boundary 113 expected-only order87 existing-stem reconciliation.
    let v87_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v87.txt"),
    )
    .expect("expected-only order87 existing-stem fixture");
    assert_eq!(
        sha256_hex(v87_text.as_bytes()),
        "b750df3442489e2da4afe4dafa3a4fd0ac6e2526e3f8e30406313f4e395d766b"
    );
    let v87_rows = v87_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v87_rows.len(), 10);
    let v87_body = format!("{}\n", v87_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v87_body.as_bytes()),
        "ed33959659524afefdf6fbb0dab32feb3ca43fc4b9cc6269cb327c9303356718"
    );
    let v87_frontier = v87_rows[6];
    assert_eq!(head_field(v87_frontier, "headOrder"), "87");
    assert_eq!(head_field(v87_frontier, "headX"), "45");
    assert_eq!(head_field(v87_frontier, "headSig"), "56");
    assert_eq!(head_field(v87_frontier, "headInterId"), "1401");
    assert_eq!(head_field(v87_frontier, "cAlias"), "h:45:LEFT:BOTTOM");
    assert_eq!(head_field(v87_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v87_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v87_frontier, "relations"), "2");
    assert_eq!(head_field(v87_frontier, "glyphs"), "1");
    assert_eq!(head_field(v87_frontier, "candidateIdBefore"), "302");
    assert_eq!(head_field(v87_frontier, "existingGlyph"), "glyph:302");
    assert_eq!(head_field(v87_frontier, "existingActive"), "true");
    assert_eq!(head_field(v87_frontier, "existingStem"), "2377");
    let v87_result = v87_rows[7];
    assert_eq!(head_field(v87_result, "headOrder"), "87");
    assert_eq!(head_field(v87_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v87_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v87_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v87_result, "addedVertices"), "-");
    assert_eq!(head_field(v87_result, "addedEdges"), "-");
    assert_eq!(head_field(v87_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v87_result, "linkerChanges"),
        "[h:46:LEFT:BOTTOM:true:false->true:true,h:46:LEFT:TOP:true:false->true:true,h:46:RIGHT:BOTTOM:false:false->false:true,h:46:RIGHT:TOP:false:false->false:true,linker:SLinker:head:46:false:false->false:true,linker:SLinker:head:46:true:false->true:true]"
    );
    assert_eq!(
        head_field(v87_result, "linkerStateHashBefore"),
        "078a43263e8b27433224cb8f316c0705b6d1cb77620aecaa2e434bdf916874eb"
    );
    assert_eq!(
        head_field(v87_result, "linkerStateHashAfter"),
        "513a6d05065aa2fbd8b05f34635e1309eb999a3d609c66eee27292acff8baab7"
    );
    let v87_java = v87_rows[8];
    assert_eq!(head_field(v87_java, "headOrder"), "87");
    assert_eq!(head_field(v87_java, "headX"), "45");
    assert_eq!(head_field(v87_java, "headSig"), "56");
    assert_eq!(head_field(v87_java, "headInterId"), "1401");
    assert_eq!(
        head_field(v87_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v87_java, "incident"),
        "[stem2377:headSideLEFT:heads[x44:sig70:id1429:sideLEFT,x45:sig56:id1401:sideLEFT,x46:sig57:id1403:sideLEFT]]"
    );
    assert_eq!(head_field(v87_java, "returned"), "true");
    assert_eq!(head_field(v87_java, "undefs"), "[]");
    assert_eq!(
        head_field(v87_java, "closureWrites"),
        "[x44:sig70:LEFT:true->true,x44:sig70:RIGHT:true->true,x46:sig57:LEFT:false->true,x46:sig57:RIGHT:false->true]"
    );
    assert_eq!(head_field(v87_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v87_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v87_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v87_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v87_java, "linkerStateHashBefore"),
        "078a43263e8b27433224cb8f316c0705b6d1cb77620aecaa2e434bdf916874eb"
    );
    assert_eq!(
        head_field(v87_java, "linkerStateHashAfter"),
        "513a6d05065aa2fbd8b05f34635e1309eb999a3d609c66eee27292acff8baab7"
    );
    assert_eq!(head_field(v87_java, "nextHeadOrder"), "88");
    assert_eq!(head_field(v87_java, "nextHeadX"), "72");
    assert_eq!(head_field(v87_java, "nextHeadSig"), "101");
    assert_eq!(head_field(v87_java, "nextHeadInterId"), "1491");
    assert_eq!(head_field(v87_java, "terminal"), "ReturnedBeforeNextHead");
    let v87_summary = v87_rows[9];
    assert_eq!(
        head_field(v87_summary, "schema"),
        "stems-head-phase-prefix-v87"
    );
    assert_eq!(head_field(v87_summary, "rows"), "9");
    assert_eq!(
        head_field(v87_summary, "baseV86RunnerSourceSha256"),
        "724e45d8a40b4eb17284004048d9c5c49c9242c15ff70e72c55837dbd05e46fa"
    );
    assert_eq!(
        head_field(v87_summary, "baseV86FixtureSha256"),
        "9d2a075961321850ffb365466b5d0d9fed9f447a31bf7dc9f83e3571b0b69097"
    );
    assert_eq!(
        head_field(v87_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v87_summary, "probeSourceSha256"),
        "cf63ba17da3e5a5b7fc2ebbcbdfb6c8cdaa137b125e3ee2ed4d2f5b7e3f26eeb"
    );
    assert_eq!(
        head_field(v87_summary, "runnerSourceSha256"),
        "ccd2ec6ebe416f3b6310b703ec8ab284c8f5bbb182c310340c31e149c416dac7"
    );
    assert_eq!(
        head_field(v87_summary, "emittedBodySha256"),
        "ed33959659524afefdf6fbb0dab32feb3ca43fc4b9cc6269cb327c9303356718"
    );
    assert_eq!(
        head_field(v87_summary, "semanticPassSha256"),
        "415def50b007fce1b2b42ad297d81768ed75dcc9ad28fd09efd93cb8939a0a17"
    );
    assert_eq!(head_field(v87_summary, "freshRuns"), "2");
    assert_eq!(head_field(v87_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v87_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder87ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v87_summary, "javaEvidence"),
        "ReturnedBeforeEightyEighthHead"
    );

    // Boundary 114 expected-only order88 existing-stem reconciliation.
    let v88_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v88.txt"),
    )
    .expect("expected-only order88 existing-stem fixture");
    assert_eq!(
        sha256_hex(v88_text.as_bytes()),
        "5abe5a2c309fe817d4921e961d75e39e043eb88862a913ff59cce4e1f541d5bd"
    );
    let v88_rows = v88_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v88_rows.len(), 10);
    let v88_body = format!("{}\n", v88_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v88_body.as_bytes()),
        "96358aa66f2b4cc64189dbd9240fe3921bc6df6adc054156f294c0d3e6cc9702"
    );
    let v88_frontier = v88_rows[6];
    assert_eq!(head_field(v88_frontier, "headOrder"), "88");
    assert_eq!(head_field(v88_frontier, "headX"), "72");
    assert_eq!(head_field(v88_frontier, "headSig"), "101");
    assert_eq!(head_field(v88_frontier, "headInterId"), "1491");
    assert_eq!(head_field(v88_frontier, "cAlias"), "h:72:LEFT:BOTTOM");
    assert_eq!(head_field(v88_frontier, "lastIndex"), "-1");
    assert_eq!(head_field(v88_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v88_frontier, "relations"), "1");
    assert_eq!(head_field(v88_frontier, "glyphs"), "1");
    assert_eq!(head_field(v88_frontier, "candidateIdBefore"), "319");
    assert_eq!(head_field(v88_frontier, "existingGlyph"), "glyph:319");
    assert_eq!(head_field(v88_frontier, "existingActive"), "true");
    assert_eq!(head_field(v88_frontier, "existingStem"), "2380");
    let v88_result = v88_rows[7];
    assert_eq!(head_field(v88_result, "headOrder"), "88");
    assert_eq!(head_field(v88_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v88_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v88_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v88_result, "addedVertices"), "-");
    assert_eq!(head_field(v88_result, "addedEdges"), "-");
    assert_eq!(head_field(v88_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v88_result, "linkerChanges"),
        "[h:75:LEFT:BOTTOM:true:false->true:true,h:75:LEFT:TOP:true:false->true:true,h:75:RIGHT:BOTTOM:false:false->false:true,h:75:RIGHT:TOP:false:false->false:true,linker:SLinker:head:75:false:false->false:true,linker:SLinker:head:75:true:false->true:true]"
    );
    assert_eq!(
        head_field(v88_result, "linkerStateHashBefore"),
        "513a6d05065aa2fbd8b05f34635e1309eb999a3d609c66eee27292acff8baab7"
    );
    assert_eq!(
        head_field(v88_result, "linkerStateHashAfter"),
        "af6786a303ad532d2147000fce06aba36dc100c4349f0c7f30be006519a36834"
    );
    let v88_java = v88_rows[8];
    assert_eq!(head_field(v88_java, "headOrder"), "88");
    assert_eq!(head_field(v88_java, "headX"), "72");
    assert_eq!(head_field(v88_java, "headSig"), "101");
    assert_eq!(head_field(v88_java, "headInterId"), "1491");
    assert_eq!(
        head_field(v88_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v88_java, "incident"),
        "[stem2380:headSideLEFT:heads[x76:sig97:id1483:sideLEFT,x75:sig96:id1481:sideLEFT,x72:sig101:id1491:sideLEFT]]"
    );
    assert_eq!(head_field(v88_java, "returned"), "true");
    assert_eq!(head_field(v88_java, "undefs"), "[]");
    assert_eq!(
        head_field(v88_java, "closureWrites"),
        "[x76:sig97:LEFT:true->true,x76:sig97:RIGHT:true->true,x75:sig96:LEFT:false->true,x75:sig96:RIGHT:false->true]"
    );
    assert_eq!(head_field(v88_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v88_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v88_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v88_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v88_java, "linkerStateHashBefore"),
        "513a6d05065aa2fbd8b05f34635e1309eb999a3d609c66eee27292acff8baab7"
    );
    assert_eq!(
        head_field(v88_java, "linkerStateHashAfter"),
        "af6786a303ad532d2147000fce06aba36dc100c4349f0c7f30be006519a36834"
    );
    assert_eq!(head_field(v88_java, "nextHeadOrder"), "89");
    assert_eq!(head_field(v88_java, "nextHeadX"), "47");
    assert_eq!(head_field(v88_java, "nextHeadSig"), "28");
    assert_eq!(head_field(v88_java, "nextHeadInterId"), "1341");
    assert_eq!(head_field(v88_java, "terminal"), "ReturnedBeforeNextHead");
    let v88_summary = v88_rows[9];
    assert_eq!(
        head_field(v88_summary, "schema"),
        "stems-head-phase-prefix-v88"
    );
    assert_eq!(head_field(v88_summary, "rows"), "9");
    assert_eq!(
        head_field(v88_summary, "baseV87RunnerSourceSha256"),
        "ccd2ec6ebe416f3b6310b703ec8ab284c8f5bbb182c310340c31e149c416dac7"
    );
    assert_eq!(
        head_field(v88_summary, "baseV87FixtureSha256"),
        "b750df3442489e2da4afe4dafa3a4fd0ac6e2526e3f8e30406313f4e395d766b"
    );
    assert_eq!(
        head_field(v88_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v88_summary, "probeSourceSha256"),
        "c01ebab844cffd04899141d5b985909a99640d8c48e7d8152d4e1942f19c9660"
    );
    assert_eq!(
        head_field(v88_summary, "runnerSourceSha256"),
        "830149aa32141461c63ae72e8e8589fd37a7ea4ff7b28baf5a7a730b811670ae"
    );
    assert_eq!(
        head_field(v88_summary, "emittedBodySha256"),
        "96358aa66f2b4cc64189dbd9240fe3921bc6df6adc054156f294c0d3e6cc9702"
    );
    assert_eq!(
        head_field(v88_summary, "semanticPassSha256"),
        "1c55c90e2fd1ed64cffd540d2540bae4cbfca4f6be0a487ede221decfd63625e"
    );
    assert_eq!(head_field(v88_summary, "freshRuns"), "2");
    assert_eq!(head_field(v88_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v88_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder88ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v88_summary, "javaEvidence"),
        "ReturnedBeforeEightyNinthHead"
    );

    // Boundary 115 expected-only order89 existing-stem reconciliation.
    let v89_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v89.txt"),
    )
    .expect("expected-only order89 existing-stem fixture");
    assert_eq!(
        sha256_hex(v89_text.as_bytes()),
        "b4e7cc6e545719f642923a1554e9de4f790e7dd94acec7d9dc5471ffc4d16129"
    );
    let v89_rows = v89_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v89_rows.len(), 10);
    let v89_body = format!("{}\n", v89_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v89_body.as_bytes()),
        "c82ca03ca2753c89750e668fd7ae0aff850b7fca28d8ae276e77a95449d4955a"
    );
    let v89_frontier = v89_rows[6];
    assert_eq!(head_field(v89_frontier, "headOrder"), "89");
    assert_eq!(head_field(v89_frontier, "headX"), "47");
    assert_eq!(head_field(v89_frontier, "headSig"), "28");
    assert_eq!(head_field(v89_frontier, "headInterId"), "1341");
    assert_eq!(head_field(v89_frontier, "cAlias"), "h:47:LEFT:BOTTOM");
    assert_eq!(head_field(v89_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v89_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v89_frontier, "relations"), "1");
    assert_eq!(head_field(v89_frontier, "glyphs"), "1");
    assert_eq!(head_field(v89_frontier, "candidateIdBefore"), "327");
    assert_eq!(head_field(v89_frontier, "existingGlyph"), "glyph:327");
    assert_eq!(head_field(v89_frontier, "existingActive"), "true");
    assert_eq!(head_field(v89_frontier, "existingStem"), "2351");
    let v89_result = v89_rows[7];
    assert_eq!(head_field(v89_result, "headOrder"), "89");
    assert_eq!(head_field(v89_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v89_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v89_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v89_result, "addedVertices"), "-");
    assert_eq!(head_field(v89_result, "addedEdges"), "-");
    assert_eq!(head_field(v89_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v89_result, "linkerChanges"),
        "[h:48:LEFT:BOTTOM:true:false->true:true,h:48:LEFT:TOP:true:false->true:true,h:48:RIGHT:BOTTOM:false:false->false:true,h:48:RIGHT:TOP:false:false->false:true,linker:SLinker:head:48:false:false->false:true,linker:SLinker:head:48:true:false->true:true]"
    );
    assert_eq!(
        head_field(v89_result, "linkerStateHashBefore"),
        "af6786a303ad532d2147000fce06aba36dc100c4349f0c7f30be006519a36834"
    );
    assert_eq!(
        head_field(v89_result, "linkerStateHashAfter"),
        "285211b6ccca5902a2bd82500672c561f223f893a43dddfa6e9b9b7b9c5f0d43"
    );
    let v89_java = v89_rows[8];
    assert_eq!(head_field(v89_java, "headOrder"), "89");
    assert_eq!(head_field(v89_java, "headX"), "47");
    assert_eq!(head_field(v89_java, "headSig"), "28");
    assert_eq!(head_field(v89_java, "headInterId"), "1341");
    assert_eq!(
        head_field(v89_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v89_java, "incident"),
        "[stem2351:headSideLEFT:heads[x47:sig28:id1341:sideLEFT,x48:sig29:id1343:sideLEFT]]"
    );
    assert_eq!(head_field(v89_java, "returned"), "true");
    assert_eq!(head_field(v89_java, "undefs"), "[]");
    assert_eq!(
        head_field(v89_java, "closureWrites"),
        "[x48:sig29:LEFT:false->true,x48:sig29:RIGHT:false->true]"
    );
    assert_eq!(head_field(v89_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v89_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v89_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v89_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v89_java, "linkerStateHashBefore"),
        "af6786a303ad532d2147000fce06aba36dc100c4349f0c7f30be006519a36834"
    );
    assert_eq!(
        head_field(v89_java, "linkerStateHashAfter"),
        "285211b6ccca5902a2bd82500672c561f223f893a43dddfa6e9b9b7b9c5f0d43"
    );
    assert_eq!(head_field(v89_java, "nextHeadOrder"), "90");
    assert_eq!(head_field(v89_java, "nextHeadX"), "27");
    assert_eq!(head_field(v89_java, "nextHeadSig"), "54");
    assert_eq!(head_field(v89_java, "nextHeadInterId"), "1397");
    assert_eq!(head_field(v89_java, "terminal"), "ReturnedBeforeNextHead");
    let v89_summary = v89_rows[9];
    assert_eq!(
        head_field(v89_summary, "schema"),
        "stems-head-phase-prefix-v89"
    );
    assert_eq!(head_field(v89_summary, "rows"), "9");
    assert_eq!(
        head_field(v89_summary, "baseV88RunnerSourceSha256"),
        "830149aa32141461c63ae72e8e8589fd37a7ea4ff7b28baf5a7a730b811670ae"
    );
    assert_eq!(
        head_field(v89_summary, "baseV88FixtureSha256"),
        "5abe5a2c309fe817d4921e961d75e39e043eb88862a913ff59cce4e1f541d5bd"
    );
    assert_eq!(
        head_field(v89_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v89_summary, "probeSourceSha256"),
        "6bddafb2492ece651ad82677f1214ae1fca192c2ce54b4ee9116b8daed0ce861"
    );
    assert_eq!(
        head_field(v89_summary, "runnerSourceSha256"),
        "d35927f89397089ee55fdff06f453132bc4b98a98a92c20123a50ce39f0badb2"
    );
    assert_eq!(
        head_field(v89_summary, "emittedBodySha256"),
        "c82ca03ca2753c89750e668fd7ae0aff850b7fca28d8ae276e77a95449d4955a"
    );
    assert_eq!(
        head_field(v89_summary, "semanticPassSha256"),
        "b111c0dd78045c42b8cb9461c36b5d1c40b78207154f06e8c59b226591452558"
    );
    assert_eq!(head_field(v89_summary, "freshRuns"), "2");
    assert_eq!(head_field(v89_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v89_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder89ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v89_summary, "javaEvidence"),
        "ReturnedBeforeNinetiethHead"
    );

    // Boundary 116 expected-only order90 existing-stem reconciliation.
    let v90_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v90.txt"),
    )
    .expect("expected-only order90 existing-stem fixture");
    assert_eq!(
        sha256_hex(v90_text.as_bytes()),
        "a7ab4b588aaea28c4874c6d2ad2cc39520b83069561b14b49e9ce01644cb6784"
    );
    let v90_rows = v90_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v90_rows.len(), 10);
    let v90_body = format!("{}\n", v90_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v90_body.as_bytes()),
        "4a2989776834570d47899d30c7b0e744560aaab213c3a76718de21ac877f6077"
    );
    let v90_frontier = v90_rows[6];
    assert_eq!(head_field(v90_frontier, "headOrder"), "90");
    assert_eq!(head_field(v90_frontier, "headX"), "27");
    assert_eq!(head_field(v90_frontier, "headSig"), "54");
    assert_eq!(head_field(v90_frontier, "headInterId"), "1397");
    assert_eq!(head_field(v90_frontier, "cAlias"), "h:27:LEFT:BOTTOM");
    assert_eq!(head_field(v90_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v90_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v90_frontier, "relations"), "2");
    assert_eq!(head_field(v90_frontier, "glyphs"), "1");
    assert_eq!(head_field(v90_frontier, "candidateIdBefore"), "300");
    assert_eq!(head_field(v90_frontier, "existingGlyph"), "glyph:300");
    assert_eq!(head_field(v90_frontier, "existingActive"), "true");
    assert_eq!(head_field(v90_frontier, "existingStem"), "2378");
    let v90_result = v90_rows[7];
    assert_eq!(head_field(v90_result, "headOrder"), "90");
    assert_eq!(head_field(v90_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v90_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v90_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v90_result, "addedVertices"), "-");
    assert_eq!(head_field(v90_result, "addedEdges"), "-");
    assert_eq!(head_field(v90_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v90_result, "linkerChanges"),
        "[h:28:LEFT:BOTTOM:true:false->true:true,h:28:LEFT:TOP:true:false->true:true,h:28:RIGHT:BOTTOM:false:false->false:true,h:28:RIGHT:TOP:false:false->false:true,linker:SLinker:head:28:false:false->false:true,linker:SLinker:head:28:true:false->true:true]"
    );
    assert_eq!(
        head_field(v90_result, "linkerStateHashBefore"),
        "285211b6ccca5902a2bd82500672c561f223f893a43dddfa6e9b9b7b9c5f0d43"
    );
    assert_eq!(
        head_field(v90_result, "linkerStateHashAfter"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    let v90_java = v90_rows[8];
    assert_eq!(head_field(v90_java, "headOrder"), "90");
    assert_eq!(head_field(v90_java, "headX"), "27");
    assert_eq!(head_field(v90_java, "headSig"), "54");
    assert_eq!(head_field(v90_java, "headInterId"), "1397");
    assert_eq!(
        head_field(v90_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v90_java, "incident"),
        "[stem2378:headSideLEFT:heads[x27:sig54:id1397:sideLEFT,x28:sig55:id1399:sideLEFT]]"
    );
    assert_eq!(head_field(v90_java, "returned"), "true");
    assert_eq!(head_field(v90_java, "undefs"), "[]");
    assert_eq!(
        head_field(v90_java, "closureWrites"),
        "[x28:sig55:LEFT:false->true,x28:sig55:RIGHT:false->true]"
    );
    assert_eq!(head_field(v90_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v90_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v90_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v90_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v90_java, "linkerStateHashBefore"),
        "285211b6ccca5902a2bd82500672c561f223f893a43dddfa6e9b9b7b9c5f0d43"
    );
    assert_eq!(
        head_field(v90_java, "linkerStateHashAfter"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(head_field(v90_java, "nextHeadOrder"), "91");
    assert_eq!(head_field(v90_java, "nextHeadX"), "91");
    assert_eq!(head_field(v90_java, "nextHeadSig"), "98");
    assert_eq!(head_field(v90_java, "nextHeadInterId"), "1485");
    assert_eq!(head_field(v90_java, "terminal"), "ReturnedBeforeNextHead");
    let v90_summary = v90_rows[9];
    assert_eq!(
        head_field(v90_summary, "schema"),
        "stems-head-phase-prefix-v90"
    );
    assert_eq!(head_field(v90_summary, "rows"), "9");
    assert_eq!(
        head_field(v90_summary, "baseV89RunnerSourceSha256"),
        "d35927f89397089ee55fdff06f453132bc4b98a98a92c20123a50ce39f0badb2"
    );
    assert_eq!(
        head_field(v90_summary, "baseV89FixtureSha256"),
        "b4e7cc6e545719f642923a1554e9de4f790e7dd94acec7d9dc5471ffc4d16129"
    );
    assert_eq!(
        head_field(v90_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v90_summary, "probeSourceSha256"),
        "18e9202fe3ee37f2a9f7e3af0d5a6a2c93dc8d5ec91658a186f48fedfa039380"
    );
    assert_eq!(
        head_field(v90_summary, "runnerSourceSha256"),
        "a1bef0b7346d9b685e3b20bb9a1caa6ed96f9f1f2e90d9f3bd6bbf4c188a015f"
    );
    assert_eq!(
        head_field(v90_summary, "emittedBodySha256"),
        "4a2989776834570d47899d30c7b0e744560aaab213c3a76718de21ac877f6077"
    );
    assert_eq!(
        head_field(v90_summary, "semanticPassSha256"),
        "b43126a8975c3d47f520c779a9a13c1e868f62744772047db1d8a888be816350"
    );
    assert_eq!(head_field(v90_summary, "freshRuns"), "2");
    assert_eq!(head_field(v90_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v90_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder90ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v90_summary, "javaEvidence"),
        "ReturnedBeforeNinetyFirstHead"
    );

    // Boundary 117 expected-only order91 existing-stem reconciliation.
    let v91_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v91.txt"),
    )
    .expect("expected-only order91 existing-stem fixture");
    assert_eq!(
        sha256_hex(v91_text.as_bytes()),
        "f4b8bbe832c33ffd5bf439c34f34eb60b966cbb82a7859368c1e3e789b5ad121"
    );
    let v91_rows = v91_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v91_rows.len(), 10);
    let v91_body = format!("{}\n", v91_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v91_body.as_bytes()),
        "1bf5e2130cc06c9e2a05a8258cc0f3e98e98f9c3eb7e28211c002d2de56b4a4c"
    );
    let v91_frontier = v91_rows[6];
    assert_eq!(head_field(v91_frontier, "headOrder"), "91");
    assert_eq!(head_field(v91_frontier, "headX"), "91");
    assert_eq!(head_field(v91_frontier, "headSig"), "98");
    assert_eq!(head_field(v91_frontier, "headInterId"), "1485");
    assert_eq!(head_field(v91_frontier, "cAlias"), "h:91:LEFT:BOTTOM");
    assert_eq!(head_field(v91_frontier, "lastIndex"), "0");
    assert_eq!(head_field(v91_frontier, "maxIndex"), "0");
    assert_eq!(head_field(v91_frontier, "relations"), "1");
    assert_eq!(head_field(v91_frontier, "glyphs"), "1");
    assert_eq!(head_field(v91_frontier, "candidateIdBefore"), "297");
    assert_eq!(head_field(v91_frontier, "existingGlyph"), "glyph:297");
    assert_eq!(head_field(v91_frontier, "existingActive"), "true");
    assert_eq!(head_field(v91_frontier, "existingStem"), "2364");
    let v91_result = v91_rows[7];
    assert_eq!(head_field(v91_result, "headOrder"), "91");
    assert_eq!(head_field(v91_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v91_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v91_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v91_result, "addedVertices"), "-");
    assert_eq!(head_field(v91_result, "addedEdges"), "-");
    assert_eq!(head_field(v91_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v91_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v91_result, "linkerStateHashBefore"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(
        head_field(v91_result, "linkerStateHashAfter"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    let v91_java = v91_rows[8];
    assert_eq!(head_field(v91_java, "headOrder"), "91");
    assert_eq!(head_field(v91_java, "headX"), "91");
    assert_eq!(head_field(v91_java, "headSig"), "98");
    assert_eq!(head_field(v91_java, "headInterId"), "1485");
    assert_eq!(
        head_field(v91_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v91_java, "incident"),
        "[stem2364:headSideLEFT:heads[x91:sig98:id1485:sideLEFT,x94:sig99:id1487:sideLEFT,x95:sig100:id1489:sideLEFT]]"
    );
    assert_eq!(head_field(v91_java, "returned"), "true");
    assert_eq!(head_field(v91_java, "undefs"), "[]");
    assert_eq!(
        head_field(v91_java, "closureWrites"),
        "[x94:sig99:LEFT:true->true,x94:sig99:RIGHT:true->true,x95:sig100:LEFT:true->true,x95:sig100:RIGHT:true->true]"
    );
    assert_eq!(head_field(v91_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v91_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v91_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v91_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v91_java, "linkerStateHashBefore"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(
        head_field(v91_java, "linkerStateHashAfter"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(head_field(v91_java, "nextHeadOrder"), "92");
    assert_eq!(head_field(v91_java, "nextHeadX"), "54");
    assert_eq!(head_field(v91_java, "nextHeadSig"), "78");
    assert_eq!(head_field(v91_java, "nextHeadInterId"), "1445");
    assert_eq!(head_field(v91_java, "terminal"), "ReturnedBeforeNextHead");
    let v91_summary = v91_rows[9];
    assert_eq!(
        head_field(v91_summary, "schema"),
        "stems-head-phase-prefix-v91"
    );
    assert_eq!(head_field(v91_summary, "rows"), "9");
    assert_eq!(
        head_field(v91_summary, "baseV90RunnerSourceSha256"),
        "a1bef0b7346d9b685e3b20bb9a1caa6ed96f9f1f2e90d9f3bd6bbf4c188a015f"
    );
    assert_eq!(
        head_field(v91_summary, "baseV90FixtureSha256"),
        "a7ab4b588aaea28c4874c6d2ad2cc39520b83069561b14b49e9ce01644cb6784"
    );
    assert_eq!(
        head_field(v91_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v91_summary, "probeSourceSha256"),
        "cf2cf814e6ef85cbc679305cb158af5f4c2a37d5087df8022d5f08e33172825d"
    );
    assert_eq!(
        head_field(v91_summary, "runnerSourceSha256"),
        "cb63feba6a88cdc44daab868b32d6f869cc5da14848dd7ad3a978a547b0fa2c8"
    );
    assert_eq!(
        head_field(v91_summary, "emittedBodySha256"),
        "1bf5e2130cc06c9e2a05a8258cc0f3e98e98f9c3eb7e28211c002d2de56b4a4c"
    );
    assert_eq!(
        head_field(v91_summary, "semanticPassSha256"),
        "678a87f1a99acc0481dba2631aec27b5e204b806f2b147c0dfcccdbc7534906e"
    );
    assert_eq!(head_field(v91_summary, "freshRuns"), "2");
    assert_eq!(head_field(v91_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v91_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder91ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v91_summary, "javaEvidence"),
        "ReturnedBeforeNinetySecondHead"
    );

    // Boundary 118 expected-only order92 existing-stem reconciliation.
    let v92_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v92.txt"),
    )
    .expect("expected-only order92 existing-stem fixture");
    assert_eq!(
        sha256_hex(v92_text.as_bytes()),
        "9ca2993d5ef00cda74cab28dd1258ee85317e6f52279b01a8841fe7d44ed5555"
    );
    let v92_rows = v92_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v92_rows.len(), 10);
    let v92_body = format!("{}\n", v92_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v92_body.as_bytes()),
        "66ca2497cb69b643a40cd45b397c87c54c63caec1cda14db4ade75912495308e"
    );
    let v92_frontier = v92_rows[6];
    assert_eq!(head_field(v92_frontier, "headOrder"), "92");
    assert_eq!(head_field(v92_frontier, "headX"), "54");
    assert_eq!(head_field(v92_frontier, "headSig"), "78");
    assert_eq!(head_field(v92_frontier, "headInterId"), "1445");
    assert_eq!(head_field(v92_frontier, "cAlias"), "h:54:LEFT:BOTTOM");
    assert_eq!(head_field(v92_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v92_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v92_frontier, "relations"), "2");
    assert_eq!(head_field(v92_frontier, "glyphs"), "1");
    assert_eq!(head_field(v92_frontier, "candidateIdBefore"), "334");
    assert_eq!(head_field(v92_frontier, "existingGlyph"), "glyph:334");
    assert_eq!(head_field(v92_frontier, "existingActive"), "true");
    assert_eq!(head_field(v92_frontier, "existingStem"), "2362");
    let v92_result = v92_rows[7];
    assert_eq!(head_field(v92_result, "headOrder"), "92");
    assert_eq!(head_field(v92_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v92_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v92_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v92_result, "addedVertices"), "-");
    assert_eq!(head_field(v92_result, "addedEdges"), "-");
    assert_eq!(head_field(v92_result, "addedSystemStems"), "-");
    assert_eq!(head_field(v92_result, "linkerChanges"), "-");
    assert_eq!(
        head_field(v92_result, "linkerStateHashBefore"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(
        head_field(v92_result, "linkerStateHashAfter"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    let v92_java = v92_rows[8];
    assert_eq!(head_field(v92_java, "headOrder"), "92");
    assert_eq!(head_field(v92_java, "headX"), "54");
    assert_eq!(head_field(v92_java, "headSig"), "78");
    assert_eq!(head_field(v92_java, "headInterId"), "1445");
    assert_eq!(
        head_field(v92_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v92_java, "incident"),
        "[stem2362:headSideLEFT:heads[x51:sig82:id1453:sideLEFT,x54:sig78:id1445:sideLEFT,x55:sig79:id1447:sideLEFT]]"
    );
    assert_eq!(head_field(v92_java, "returned"), "true");
    assert_eq!(head_field(v92_java, "undefs"), "[]");
    assert_eq!(
        head_field(v92_java, "closureWrites"),
        "[x51:sig82:LEFT:true->true,x51:sig82:RIGHT:true->true,x55:sig79:LEFT:true->true,x55:sig79:RIGHT:true->true]"
    );
    assert_eq!(head_field(v92_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v92_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v92_java, "sigEdgesAfter"), "705");
    assert_eq!(head_field(v92_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v92_java, "linkerStateHashBefore"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(
        head_field(v92_java, "linkerStateHashAfter"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(head_field(v92_java, "nextHeadOrder"), "93");
    assert_eq!(head_field(v92_java, "nextHeadX"), "37");
    assert_eq!(head_field(v92_java, "nextHeadSig"), "44");
    assert_eq!(head_field(v92_java, "nextHeadInterId"), "1373");
    assert_eq!(head_field(v92_java, "terminal"), "ReturnedBeforeNextHead");
    let v92_summary = v92_rows[9];
    assert_eq!(
        head_field(v92_summary, "schema"),
        "stems-head-phase-prefix-v92"
    );
    assert_eq!(head_field(v92_summary, "rows"), "9");
    assert_eq!(
        head_field(v92_summary, "baseV91RunnerSourceSha256"),
        "cb63feba6a88cdc44daab868b32d6f869cc5da14848dd7ad3a978a547b0fa2c8"
    );
    assert_eq!(
        head_field(v92_summary, "baseV91FixtureSha256"),
        "f4b8bbe832c33ffd5bf439c34f34eb60b966cbb82a7859368c1e3e789b5ad121"
    );
    assert_eq!(
        head_field(v92_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v92_summary, "probeSourceSha256"),
        "f29980a923e649717e2e02989a1856072bbf4f12d435563b8d6507080f674878"
    );
    assert_eq!(
        head_field(v92_summary, "runnerSourceSha256"),
        "4096c4bad41a694d2c5f23a8405274c2fb83a973b7323a2cecd72790e582960c"
    );
    assert_eq!(
        head_field(v92_summary, "emittedBodySha256"),
        "66ca2497cb69b643a40cd45b397c87c54c63caec1cda14db4ade75912495308e"
    );
    assert_eq!(
        head_field(v92_summary, "semanticPassSha256"),
        "f9063dc41a0b3d6f90f92750337b5722c39889c9cd511a0a6b383473bb08b0b8"
    );
    assert_eq!(head_field(v92_summary, "freshRuns"), "2");
    assert_eq!(head_field(v92_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v92_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder92ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v92_summary, "javaEvidence"),
        "ReturnedBeforeNinetyThirdHead"
    );

    // Boundary 119 expected-only order93 RIGHT-side reuse C-link.
    let v93_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v93.txt"),
    )
    .expect("expected-only order93 right-side reuse fixture");
    assert_eq!(
        sha256_hex(v93_text.as_bytes()),
        "d42d1b84db222735c24771c2479e9f5dbdeca30f3bbbaf3bd33a3b016d2761e4"
    );
    let v93_rows = v93_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v93_rows.len(), 10);
    let v93_body = format!("{}\n", v93_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v93_body.as_bytes()),
        "6145ac721b0b3e352e5404181fc8fd694d7f61cfd16d97d87da67113e15eb501"
    );
    let v93_frontier = v93_rows[6];
    assert_eq!(head_field(v93_frontier, "headOrder"), "93");
    assert_eq!(head_field(v93_frontier, "headX"), "37");
    assert_eq!(head_field(v93_frontier, "headSig"), "44");
    assert_eq!(head_field(v93_frontier, "headInterId"), "1373");
    assert_eq!(head_field(v93_frontier, "cAlias"), "h:37:LEFT:BOTTOM");
    assert_eq!(head_field(v93_frontier, "lastIndex"), "-1");
    assert_eq!(head_field(v93_frontier, "maxIndex"), "2");
    assert_eq!(head_field(v93_frontier, "relations"), "1");
    assert_eq!(head_field(v93_frontier, "glyphs"), "2");
    assert_eq!(head_field(v93_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v93_frontier, "existingGlyph"), "-");
    assert_eq!(head_field(v93_frontier, "existingActive"), "false");
    assert_eq!(head_field(v93_frontier, "existingStem"), "-");
    let v93_result = v93_rows[7];
    assert_eq!(head_field(v93_result, "headOrder"), "93");
    assert_eq!(head_field(v93_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v93_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v93_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v93_result, "addedVertices"), "-");
    assert_eq!(head_field(v93_result, "addedSystemStems"), "-");
    assert!(head_field(v93_result, "addedEdges").starts_with(
        "[system1:sourceId1373:targetId2379:org.audiveris.omr.sig.relation.HeadStemRelation:manual=false:grade=0x1.4fedcec6ab042p-1/3fe4fedcec6ab042"
    ));
    assert!(head_field(v93_result, "addedEdges").contains("headSide=RIGHT"));
    assert_eq!(
        head_field(v93_result, "linkerChanges"),
        "[h:37:RIGHT:BOTTOM:false:false->true:false,h:37:RIGHT:TOP:false:false->true:false,h:38:LEFT:BOTTOM:false:false->false:true,h:38:LEFT:TOP:false:false->false:true,h:38:RIGHT:BOTTOM:true:false->true:true,h:38:RIGHT:TOP:true:false->true:true,linker:SLinker:head:37:false:false->true:false,linker:SLinker:head:38:false:false->false:true,linker:SLinker:head:38:true:false->true:true]"
    );
    assert_eq!(
        head_field(v93_result, "linkerStateHashBefore"),
        "fcb72ffb059ec88be8ce9c7854f69a0e375d6316447020e50873bf9017052e13"
    );
    assert_eq!(
        head_field(v93_result, "linkerStateHashAfter"),
        "3639010163d4542613215c8bd8d96fe83df7e12c33679dc1378ee851ba1fc688"
    );
    let v93_java = v93_rows[8];
    assert_eq!(head_field(v93_java, "headOrder"), "93");
    assert_eq!(head_field(v93_java, "headX"), "37");
    assert_eq!(head_field(v93_java, "headSig"), "44");
    assert_eq!(head_field(v93_java, "headInterId"), "1373");
    assert_eq!(
        head_field(v93_java, "decisions"),
        "[LEFT:top=false:bottom=false:branch=Neither,RIGHT:top=true:bottom=false:branch=TopOnly]"
    );
    assert_eq!(head_field(v93_java, "incident"), "-");
    assert_eq!(head_field(v93_java, "returned"), "true");
    assert_eq!(head_field(v93_java, "undefs"), "[]");
    assert_eq!(head_field(v93_java, "closureWrites"), "-");
    assert_eq!(head_field(v93_java, "closedValueChanges"), "0");
    assert_eq!(head_field(v93_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v93_java, "sigEdgesBefore"), "705");
    assert_eq!(head_field(v93_java, "sigEdgesAfter"), "706");
    assert_eq!(head_field(v93_java, "systemStemsAfter"), "46");
    assert_eq!(head_field(v93_java, "nextHeadOrder"), "94");
    assert_eq!(head_field(v93_java, "nextHeadX"), "96");
    assert_eq!(head_field(v93_java, "nextHeadSig"), "41");
    assert_eq!(head_field(v93_java, "nextHeadInterId"), "1367");
    assert_eq!(head_field(v93_java, "terminal"), "ReturnedBeforeNextHead");
    let v93_summary = v93_rows[9];
    assert_eq!(
        head_field(v93_summary, "schema"),
        "stems-head-phase-prefix-v93"
    );
    assert_eq!(head_field(v93_summary, "rows"), "9");
    assert_eq!(
        head_field(v93_summary, "baseV92RunnerSourceSha256"),
        "4096c4bad41a694d2c5f23a8405274c2fb83a973b7323a2cecd72790e582960c"
    );
    assert_eq!(
        head_field(v93_summary, "baseV92FixtureSha256"),
        "9ca2993d5ef00cda74cab28dd1258ee85317e6f52279b01a8841fe7d44ed5555"
    );
    assert_eq!(
        head_field(v93_summary, "probeSourceSha256"),
        "c575908044c01ccda81d6fa5c5caa1f10d71108cbe948d76cc3ac101918f0fe6"
    );
    assert_eq!(
        head_field(v93_summary, "runnerSourceSha256"),
        "11fc40b137e0adeea392789778c7340698bcca1176275dd6ba0eb430819fccad"
    );
    assert_eq!(
        head_field(v93_summary, "emittedBodySha256"),
        "6145ac721b0b3e352e5404181fc8fd694d7f61cfd16d97d87da67113e15eb501"
    );
    assert_eq!(
        head_field(v93_summary, "semanticPassSha256"),
        "894f0a247c0957b963f20b2e23710995a8b1f8a235acf707c8d11528781d5584"
    );
    assert_eq!(head_field(v93_summary, "freshRuns"), "2");
    assert_eq!(head_field(v93_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v93_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder93RightSideReuseCLink"
    );
    assert_eq!(
        head_field(v93_summary, "javaEvidence"),
        "ReturnedBeforeNinetyFourthHead"
    );

    // Boundary 120 expected-only order94 existing-stem reconciliation.
    let v94_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v94.txt"),
    )
    .expect("expected-only order94 existing-stem fixture");
    assert_eq!(
        sha256_hex(v94_text.as_bytes()),
        "2ea83650480f971d7a2b79e3c1544dc05707e020d2ffa5f0e480550bdcce095c"
    );
    let v94_rows = v94_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v94_rows.len(), 10);
    let v94_body = format!("{}\n", v94_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v94_body.as_bytes()),
        "4be10cdece3949e699b2b8157a339e041c9232828d5654a396f39d0f835bf3bc"
    );
    let v94_frontier = v94_rows[6];
    assert_eq!(head_field(v94_frontier, "headOrder"), "94");
    assert_eq!(head_field(v94_frontier, "headX"), "96");
    assert_eq!(head_field(v94_frontier, "headSig"), "41");
    assert_eq!(head_field(v94_frontier, "headInterId"), "1367");
    assert_eq!(head_field(v94_frontier, "cAlias"), "h:96:LEFT:BOTTOM");
    assert_eq!(head_field(v94_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v94_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v94_frontier, "relations"), "2");
    assert_eq!(head_field(v94_frontier, "glyphs"), "1");
    assert_eq!(head_field(v94_frontier, "candidateIdBefore"), "321");
    assert_eq!(head_field(v94_frontier, "existingGlyph"), "glyph:321");
    assert_eq!(head_field(v94_frontier, "existingActive"), "true");
    assert_eq!(head_field(v94_frontier, "existingStem"), "2373");
    let v94_result = v94_rows[7];
    assert_eq!(head_field(v94_result, "headOrder"), "94");
    assert_eq!(head_field(v94_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v94_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v94_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v94_result, "addedVertices"), "-");
    assert_eq!(head_field(v94_result, "addedEdges"), "-");
    assert_eq!(head_field(v94_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v94_result, "linkerChanges"),
        "[h:97:LEFT:BOTTOM:true:false->true:true,h:97:LEFT:TOP:true:false->true:true,h:97:RIGHT:BOTTOM:false:false->false:true,h:97:RIGHT:TOP:false:false->false:true,linker:SLinker:head:97:false:false->false:true,linker:SLinker:head:97:true:false->true:true]"
    );
    assert_eq!(
        head_field(v94_result, "linkerStateHashBefore"),
        "3639010163d4542613215c8bd8d96fe83df7e12c33679dc1378ee851ba1fc688"
    );
    assert_eq!(
        head_field(v94_result, "linkerStateHashAfter"),
        "c72de770b07b59dcbf6d5b57be7058f92cde1e8f8ff73b2bf8108f8566359933"
    );
    let v94_java = v94_rows[8];
    assert_eq!(head_field(v94_java, "headOrder"), "94");
    assert_eq!(head_field(v94_java, "headX"), "96");
    assert_eq!(head_field(v94_java, "headSig"), "41");
    assert_eq!(head_field(v94_java, "headInterId"), "1367");
    assert_eq!(
        head_field(v94_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v94_java, "incident"),
        "[stem2373:headSideLEFT:heads[x96:sig41:id1367:sideLEFT,x97:sig34:id1353:sideLEFT]]"
    );
    assert_eq!(head_field(v94_java, "returned"), "true");
    assert_eq!(head_field(v94_java, "undefs"), "[]");
    assert_eq!(
        head_field(v94_java, "closureWrites"),
        "[x97:sig34:LEFT:false->true,x97:sig34:RIGHT:false->true]"
    );
    assert_eq!(head_field(v94_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v94_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v94_java, "sigEdgesAfter"), "706");
    assert_eq!(head_field(v94_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v94_java, "linkerStateHashBefore"),
        "3639010163d4542613215c8bd8d96fe83df7e12c33679dc1378ee851ba1fc688"
    );
    assert_eq!(
        head_field(v94_java, "linkerStateHashAfter"),
        "c72de770b07b59dcbf6d5b57be7058f92cde1e8f8ff73b2bf8108f8566359933"
    );
    assert_eq!(head_field(v94_java, "nextHeadOrder"), "95");
    assert_eq!(head_field(v94_java, "nextHeadX"), "7");
    assert_eq!(head_field(v94_java, "nextHeadSig"), "52");
    assert_eq!(head_field(v94_java, "nextHeadInterId"), "1393");
    assert_eq!(head_field(v94_java, "terminal"), "ReturnedBeforeNextHead");
    let v94_summary = v94_rows[9];
    assert_eq!(
        head_field(v94_summary, "schema"),
        "stems-head-phase-prefix-v94"
    );
    assert_eq!(head_field(v94_summary, "rows"), "9");
    assert_eq!(
        head_field(v94_summary, "baseV93RunnerSourceSha256"),
        "11fc40b137e0adeea392789778c7340698bcca1176275dd6ba0eb430819fccad"
    );
    assert_eq!(
        head_field(v94_summary, "baseV93FixtureSha256"),
        "d42d1b84db222735c24771c2479e9f5dbdeca30f3bbbaf3bd33a3b016d2761e4"
    );
    assert_eq!(
        head_field(v94_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v94_summary, "probeSourceSha256"),
        "de43dd8829e117806993f7908e91ef48ae05ca6f41d6629ff36964004222c6c2"
    );
    assert_eq!(
        head_field(v94_summary, "runnerSourceSha256"),
        "d57ea24bda43d732338ad2aa89aeaec9d5f863b733fd9e5eb17853cc76aa4720"
    );
    assert_eq!(
        head_field(v94_summary, "emittedBodySha256"),
        "4be10cdece3949e699b2b8157a339e041c9232828d5654a396f39d0f835bf3bc"
    );
    assert_eq!(
        head_field(v94_summary, "semanticPassSha256"),
        "8ca910725d489970054db6709fc5cdf728aa824e91a4fd0d98dd96de85203ad1"
    );
    assert_eq!(head_field(v94_summary, "freshRuns"), "2");
    assert_eq!(head_field(v94_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v94_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder94ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v94_summary, "javaEvidence"),
        "ReturnedBeforeNinetyFifthHead"
    );

    // Boundary 121 expected-only order95 existing-stem reconciliation.
    let v95_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v95.txt"),
    )
    .expect("expected-only order95 existing-stem fixture");
    assert_eq!(
        sha256_hex(v95_text.as_bytes()),
        "004659ece99b047c1b433226fd9a64cfcfd69e2caf301f3f1c5dc4edf38a50b8"
    );
    let v95_rows = v95_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v95_rows.len(), 10);
    let v95_body = format!("{}\n", v95_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v95_body.as_bytes()),
        "69449676ce408971dd90ff367afe4e07bd519603100c08a06fe36a31cb4c92a1"
    );
    let v95_frontier = v95_rows[6];
    assert_eq!(head_field(v95_frontier, "headOrder"), "95");
    assert_eq!(head_field(v95_frontier, "headX"), "7");
    assert_eq!(head_field(v95_frontier, "headSig"), "52");
    assert_eq!(head_field(v95_frontier, "headInterId"), "1393");
    assert_eq!(head_field(v95_frontier, "cAlias"), "h:7:LEFT:BOTTOM");
    assert_eq!(head_field(v95_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v95_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v95_frontier, "relations"), "2");
    assert_eq!(head_field(v95_frontier, "glyphs"), "1");
    assert_eq!(head_field(v95_frontier, "candidateIdBefore"), "305");
    assert_eq!(head_field(v95_frontier, "existingGlyph"), "glyph:305");
    assert_eq!(head_field(v95_frontier, "existingActive"), "true");
    assert_eq!(head_field(v95_frontier, "existingStem"), "2376");
    let v95_result = v95_rows[7];
    assert_eq!(head_field(v95_result, "headOrder"), "95");
    assert_eq!(head_field(v95_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v95_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v95_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v95_result, "addedVertices"), "-");
    assert_eq!(head_field(v95_result, "addedEdges"), "-");
    assert_eq!(head_field(v95_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v95_result, "linkerChanges"),
        "[h:8:LEFT:BOTTOM:true:false->true:true,h:8:LEFT:TOP:true:false->true:true,h:8:RIGHT:BOTTOM:false:false->false:true,h:8:RIGHT:TOP:false:false->false:true,linker:SLinker:head:8:false:false->false:true,linker:SLinker:head:8:true:false->true:true]"
    );
    assert_eq!(
        head_field(v95_result, "linkerStateHashBefore"),
        "c72de770b07b59dcbf6d5b57be7058f92cde1e8f8ff73b2bf8108f8566359933"
    );
    assert_eq!(
        head_field(v95_result, "linkerStateHashAfter"),
        "0afabc1399251457ef4b60c49038cde18c7c19e4b2eebbe9869eb902e89d55d8"
    );
    let v95_java = v95_rows[8];
    assert_eq!(head_field(v95_java, "headOrder"), "95");
    assert_eq!(head_field(v95_java, "headX"), "7");
    assert_eq!(head_field(v95_java, "headSig"), "52");
    assert_eq!(head_field(v95_java, "headInterId"), "1393");
    assert_eq!(
        head_field(v95_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v95_java, "incident"),
        "[stem2376:headSideLEFT:heads[x8:sig53:id1395:sideLEFT,x7:sig52:id1393:sideLEFT]]"
    );
    assert_eq!(head_field(v95_java, "returned"), "true");
    assert_eq!(head_field(v95_java, "undefs"), "[]");
    assert_eq!(
        head_field(v95_java, "closureWrites"),
        "[x8:sig53:LEFT:false->true,x8:sig53:RIGHT:false->true]"
    );
    assert_eq!(head_field(v95_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v95_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v95_java, "sigEdgesAfter"), "706");
    assert_eq!(head_field(v95_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v95_java, "linkerStateHashBefore"),
        "c72de770b07b59dcbf6d5b57be7058f92cde1e8f8ff73b2bf8108f8566359933"
    );
    assert_eq!(
        head_field(v95_java, "linkerStateHashAfter"),
        "0afabc1399251457ef4b60c49038cde18c7c19e4b2eebbe9869eb902e89d55d8"
    );
    assert_eq!(head_field(v95_java, "nextHeadOrder"), "96");
    assert_eq!(head_field(v95_java, "nextHeadX"), "60");
    assert_eq!(head_field(v95_java, "nextHeadSig"), "30");
    assert_eq!(head_field(v95_java, "nextHeadInterId"), "1345");
    assert_eq!(head_field(v95_java, "terminal"), "ReturnedBeforeNextHead");
    let v95_summary = v95_rows[9];
    assert_eq!(
        head_field(v95_summary, "schema"),
        "stems-head-phase-prefix-v95"
    );
    assert_eq!(head_field(v95_summary, "rows"), "9");
    assert_eq!(
        head_field(v95_summary, "baseV94RunnerSourceSha256"),
        "d57ea24bda43d732338ad2aa89aeaec9d5f863b733fd9e5eb17853cc76aa4720"
    );
    assert_eq!(
        head_field(v95_summary, "baseV94FixtureSha256"),
        "2ea83650480f971d7a2b79e3c1544dc05707e020d2ffa5f0e480550bdcce095c"
    );
    assert_eq!(
        head_field(v95_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v95_summary, "probeSourceSha256"),
        "360e79b8a8934fab2859edd9120f0b19448872dc2de70649f364bf8703c09ef4"
    );
    assert_eq!(
        head_field(v95_summary, "runnerSourceSha256"),
        "25939a63c83a8a31fa981e808c10dbdcd4fb695337aa17aa52b96205a2821cd7"
    );
    assert_eq!(
        head_field(v95_summary, "emittedBodySha256"),
        "69449676ce408971dd90ff367afe4e07bd519603100c08a06fe36a31cb4c92a1"
    );
    assert_eq!(
        head_field(v95_summary, "semanticPassSha256"),
        "ae24e68812beabc46ac557e323bfffb8e9bebae948e1954ea2376032187d8b46"
    );
    assert_eq!(head_field(v95_summary, "freshRuns"), "2");
    assert_eq!(head_field(v95_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v95_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder95ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v95_summary, "javaEvidence"),
        "ReturnedBeforeNinetySixthHead"
    );

    // Boundary 122 expected-only order96 existing-stem reconciliation.
    let v96_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-head-phase-prefix-chula-system1-v96.txt"),
    )
    .expect("expected-only order96 existing-stem fixture");
    assert_eq!(
        sha256_hex(v96_text.as_bytes()),
        "5d3ab89d92445460772989ae421f13a6d2abf68546a97835f9f24242c829dba0"
    );
    let v96_rows = v96_text
        .lines()
        .filter(|line| line.starts_with("stems"))
        .collect::<Vec<_>>();
    assert_eq!(v96_rows.len(), 10);
    let v96_body = format!("{}\n", v96_rows[..9].join("\n"));
    assert_eq!(
        sha256_hex(v96_body.as_bytes()),
        "658a29b4a774742adcb3095ec6147f808a45bdabf216e2256fb21f186c6a69fb"
    );
    let v96_frontier = v96_rows[6];
    assert_eq!(head_field(v96_frontier, "headOrder"), "96");
    assert_eq!(head_field(v96_frontier, "headX"), "60");
    assert_eq!(head_field(v96_frontier, "headSig"), "30");
    assert_eq!(head_field(v96_frontier, "headInterId"), "1345");
    assert_eq!(head_field(v96_frontier, "cAlias"), "h:60:LEFT:BOTTOM");
    assert_eq!(head_field(v96_frontier, "lastIndex"), "1");
    assert_eq!(head_field(v96_frontier, "maxIndex"), "1");
    assert_eq!(head_field(v96_frontier, "relations"), "1");
    assert_eq!(head_field(v96_frontier, "glyphs"), "2");
    assert_eq!(head_field(v96_frontier, "candidateIdBefore"), "0");
    assert_eq!(head_field(v96_frontier, "existingGlyph"), "glyph:335");
    assert_eq!(head_field(v96_frontier, "existingActive"), "true");
    assert_eq!(head_field(v96_frontier, "existingStem"), "2345");
    let v96_result = v96_rows[7];
    assert_eq!(head_field(v96_result, "headOrder"), "96");
    assert_eq!(head_field(v96_result, "allocatorBefore"), "2385");
    assert_eq!(head_field(v96_result, "allocatorAfter"), "2385");
    assert_eq!(head_field(v96_result, "registeredGlyphs"), "-");
    assert_eq!(head_field(v96_result, "addedVertices"), "-");
    assert_eq!(head_field(v96_result, "addedEdges"), "-");
    assert_eq!(head_field(v96_result, "addedSystemStems"), "-");
    assert_eq!(
        head_field(v96_result, "linkerChanges"),
        "[h:61:LEFT:BOTTOM:true:false->true:true,h:61:LEFT:TOP:true:false->true:true,h:61:RIGHT:BOTTOM:false:false->false:true,h:61:RIGHT:TOP:false:false->false:true,linker:SLinker:head:61:false:false->false:true,linker:SLinker:head:61:true:false->true:true]"
    );
    assert_eq!(
        head_field(v96_result, "linkerStateHashBefore"),
        "0afabc1399251457ef4b60c49038cde18c7c19e4b2eebbe9869eb902e89d55d8"
    );
    assert_eq!(
        head_field(v96_result, "linkerStateHashAfter"),
        "e1f06ce299e7f38ec2eb6188379a175200511fcaeaff297b04b4b834a7b34c89"
    );
    let v96_java = v96_rows[8];
    assert_eq!(head_field(v96_java, "headOrder"), "96");
    assert_eq!(head_field(v96_java, "headX"), "60");
    assert_eq!(head_field(v96_java, "headSig"), "30");
    assert_eq!(head_field(v96_java, "headInterId"), "1345");
    assert_eq!(
        head_field(v96_java, "decisions"),
        "[LEFT:SkipAlreadyLinked,RIGHT:SkipClosed]"
    );
    assert_eq!(
        head_field(v96_java, "incident"),
        "[stem2345:headSideLEFT:heads[x60:sig30:id1345:sideLEFT,x61:sig31:id1347:sideLEFT]]"
    );
    assert_eq!(head_field(v96_java, "returned"), "true");
    assert_eq!(head_field(v96_java, "undefs"), "[]");
    assert_eq!(
        head_field(v96_java, "closureWrites"),
        "[x61:sig31:LEFT:false->true,x61:sig31:RIGHT:false->true]"
    );
    assert_eq!(head_field(v96_java, "closedValueChanges"), "2");
    assert_eq!(head_field(v96_java, "sigVerticesAfter"), "685");
    assert_eq!(head_field(v96_java, "sigEdgesAfter"), "706");
    assert_eq!(head_field(v96_java, "systemStemsAfter"), "46");
    assert_eq!(
        head_field(v96_java, "linkerStateHashBefore"),
        "0afabc1399251457ef4b60c49038cde18c7c19e4b2eebbe9869eb902e89d55d8"
    );
    assert_eq!(
        head_field(v96_java, "linkerStateHashAfter"),
        "e1f06ce299e7f38ec2eb6188379a175200511fcaeaff297b04b4b834a7b34c89"
    );
    assert_eq!(head_field(v96_java, "nextHeadOrder"), "97");
    assert_eq!(head_field(v96_java, "nextHeadX"), "44");
    assert_eq!(head_field(v96_java, "nextHeadSig"), "70");
    assert_eq!(head_field(v96_java, "nextHeadInterId"), "1429");
    assert_eq!(head_field(v96_java, "terminal"), "ReturnedBeforeNextHead");
    let v96_summary = v96_rows[9];
    assert_eq!(
        head_field(v96_summary, "schema"),
        "stems-head-phase-prefix-v96"
    );
    assert_eq!(head_field(v96_summary, "rows"), "9");
    assert_eq!(
        head_field(v96_summary, "baseV95RunnerSourceSha256"),
        "25939a63c83a8a31fa981e808c10dbdcd4fb695337aa17aa52b96205a2821cd7"
    );
    assert_eq!(
        head_field(v96_summary, "baseV95FixtureSha256"),
        "004659ece99b047c1b433226fd9a64cfcfd69e2caf301f3f1c5dc4edf38a50b8"
    );
    assert_eq!(
        head_field(v96_summary, "fragmentSourceSha256"),
        "4f27146b667a76b23e38607b8669ae78edeb73af78cad818ce8a95cedf54300c"
    );
    assert_eq!(
        head_field(v96_summary, "probeSourceSha256"),
        "cec232cb82eb06cc4fc2648bb7cacf114384d345e90a07d12150cf92d4b51436"
    );
    assert_eq!(
        head_field(v96_summary, "runnerSourceSha256"),
        "1b8c36025bcf6c3790ae8b1cd2026b290d6d8dcb85080139aa9ecaee6ab4a76e"
    );
    assert_eq!(
        head_field(v96_summary, "emittedBodySha256"),
        "658a29b4a774742adcb3095ec6147f808a45bdabf216e2256fb21f186c6a69fb"
    );
    assert_eq!(
        head_field(v96_summary, "semanticPassSha256"),
        "a6d00bd29f7358bf90848d234da509e68abd6e102ce93b7c4283815e6292e4c2"
    );
    assert_eq!(head_field(v96_summary, "freshRuns"), "2");
    assert_eq!(head_field(v96_summary, "freshRunsByteIdentical"), "true");
    assert_eq!(
        head_field(v96_summary, "nativeScope"),
        "BoundedSnapshotMinimizedOrder96ExistingStemReconciliation"
    );
    assert_eq!(
        head_field(v96_summary, "javaEvidence"),
        "ReturnedBeforeNinetySeventhHead"
    );

    let all_siblings = std::iter::once(&actual)
        .chain(std::iter::once(&second_siblings))
        .chain(repeated.iter().map(|transaction| &transaction.siblings))
        .collect::<Vec<_>>();
    assert_eq!(all_siblings.len(), 32);
    assert_eq!(
        all_siblings
            .iter()
            .map(|transaction| {
                hydrated
                    .stumps
                    .beams_by_abscissa
                    .iter()
                    .find(|beam| beam.source == transaction.base_b_linker.beam)
                    .expect("executed base beam in native stump catalogue")
                    .sig_ordinal
            })
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>(),
        EXECUTED_BASE_BEAM_SIG_ORDINALS
    );
    assert_eq!(
        all_siblings
            .iter()
            .map(|transaction| transaction.assigned_b_linkers.len())
            .sum::<usize>(),
        29
    );
    assert_eq!(
        all_siblings
            .iter()
            .map(|transaction| transaction.b_linker_write_count)
            .sum::<usize>(),
        61
    );
    let all_heads = std::iter::once(&head_actual)
        .chain(std::iter::once(&second_heads))
        .chain(repeated.iter().map(|transaction| &transaction.heads))
        .collect::<Vec<_>>();
    assert_eq!(
        all_heads
            .iter()
            .map(|transaction| transaction.s_linker_write_count)
            .sum::<usize>(),
        68
    );

    // Expected sequencing is deliberately opened only after the native loop
    // has returned and the owned graph/cell terminal has been authenticated.
    let sides_pass_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-sides-pass-chula-system1.txt"),
    )
    .expect("expected-only SIDES pass");
    let expected_steps = sides_pass_text
        .lines()
        .filter(|line| line.starts_with("stemsbeamsidesloopstep ") && line.contains(" step 0 "))
        .map(|line| {
            let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
            let value = |name: &str| {
                tokens
                    .iter()
                    .position(|token| *token == name)
                    .and_then(|index| tokens.get(index + 1))
                    .copied()
                    .expect("SIDES step field")
            };
            (
                value("plan").parse::<usize>().unwrap(),
                value("bAlias").to_owned(),
            )
        })
        .collect::<Vec<_>>();
    let actual_steps = std::iter::once((
        native_base.key.plan.plan_ordinal,
        second_b_alias(actual.base_b_linker),
    ))
    .chain(std::iter::once((
        second_flag.key.plan.plan_ordinal,
        second_b_alias(second_siblings.base_b_linker),
    )))
    .chain(repeated.iter().map(|transaction| {
        (
            transaction.flag.key.plan.plan_ordinal,
            second_b_alias(transaction.siblings.base_b_linker),
        )
    }))
    .collect::<Vec<_>>();
    assert_eq!(actual_steps, expected_steps);

    let expected_siblings = sides_pass_text
        .lines()
        .filter(|line| line.starts_with("stemsbeamsidesloopsibling "))
        .map(|line| {
            let tokens = line.split_ascii_whitespace().collect::<Vec<_>>();
            let value = |name: &str| {
                tokens
                    .iter()
                    .position(|token| *token == name)
                    .and_then(|index| tokens.get(index + 1))
                    .copied()
                    .expect("SIDES sibling field")
            };
            let count = value("siblings").parse::<usize>().unwrap();
            let aliases = if count == 0 {
                Vec::new()
            } else {
                value("aliases").split(',').map(str::to_owned).collect()
            };
            (value("bAlias").to_owned(), aliases)
        })
        .collect::<Vec<_>>();
    let actual_sibling_rows = all_siblings
        .iter()
        .map(|transaction| {
            (
                second_b_alias(transaction.base_b_linker),
                transaction
                    .assigned_b_linkers
                    .iter()
                    .copied()
                    .map(second_b_alias)
                    .collect::<Vec<_>>(),
            )
        })
        .collect::<Vec<_>>();
    assert_eq!(actual_sibling_rows, expected_siblings);
    assert!(sides_pass_text.lines().any(|line| {
        line == "stemsbeamsidesloopcensus chula.png#1 system 1 freshTransactions 31 secondFrontier true sidesExhausted true"
    }));

    // A B16 failure occurs after provisional B12-B15 work, but no part of the
    // caller's complete carrier can escape the shadow.
    let assigned = carried_third
        .siblings
        .assigned_b_linkers
        .first()
        .copied()
        .expect("transaction-3 sibling cell");
    let mut late_failure = carrier_before.clone();
    late_failure
        .b_cells
        .retain(|cell| cell.reference != assigned);
    let late_failure_before = late_failure.clone();
    let bridge_before = bridge.clone();
    advance_native_stems_beam_sides_transaction_from_first_stems_bridge(
        &mut late_failure,
        context,
        &bridge,
    )
    .expect_err("missing sibling B cell must reject the bridge carrier transaction");
    assert_eq!(late_failure, late_failure_before);
    assert_eq!(bridge, bridge_before);

    let third_create = apply_native_stems_beam_vlink_create_stem_transaction(
        third_scheduler,
        &hydrated.builder,
        &hydrated.plans,
        &mut third_transaction_state,
        &b15_hydration::checker_context_for_page(&b15_hydration::native_predecessor_page(
            "chula.png",
        )),
    )
    .expect("native transaction-3 B12");
    let third_live = project_native_stems_beam_vlink_reuse_live_state(
        &second_sig,
        &second_bindings,
        third_scheduler,
        &hydrated.plans,
        &second_s_cells,
        &third_transaction_state.system_stems,
    )
    .expect("native transaction-3 B13 live state");
    let third_reuse = evaluate_native_stems_beam_vlink_reuse_check(
        third_scheduler,
        &hydrated.plans,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &third_create,
        &third_transaction_state,
        &third_live,
        hydrated.relation_parameters,
    )
    .expect("native transaction-3 B13");
    let mut third_sig = second_sig.clone();
    let mut third_bindings = second_bindings.clone();
    let missing_third_beam = beam_bootstrap
        .iter()
        .copied()
        .filter(|entry| entry.source != third.b_linker.beam)
        .collect::<Vec<_>>();
    roll_native_stems_beam_vlink_base_apply_state(
        &second_base.state_after,
        &third_transaction_state,
        &third_reuse,
        &third_sig,
        &third_bindings,
        NativeStemsBeamVLinkBaseRolloverAuthority {
            stump_system: &hydrated.stumps,
            beam_inter_index: &missing_third_beam,
            configured_inter_vip_ids: &[],
        },
    )
    .expect_err("changed beam requires complete page InterIndex authority");
    assert_eq!(third_sig, second_sig);
    assert_eq!(third_bindings, second_bindings);
    let mut third_base_state = roll_native_stems_beam_vlink_base_apply_state(
        &second_base.state_after,
        &third_transaction_state,
        &third_reuse,
        &third_sig,
        &third_bindings,
        NativeStemsBeamVLinkBaseRolloverAuthority {
            stump_system: &hydrated.stumps,
            beam_inter_index: &beam_bootstrap,
            configured_inter_vip_ids: &[],
        },
    )
    .expect("native changed-beam transaction-3 B14 rollover");
    let third_base_state_before = third_base_state.clone();
    assert_eq!(
        (
            third_base_state.inter_index.baseline_entry_count,
            third_base_state.sig.baseline_vertex_count,
            third_base_state.sig.baseline_relation_count,
        ),
        (641, 223, 212)
    );
    let third_base = apply_native_stems_beam_vlink_base_transaction_to_native_sig(
        third_scheduler,
        &hydrated.plans,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &third_create,
        &third_live,
        hydrated.relation_parameters,
        &third_reuse,
        &mut third_base_state,
        &mut third_sig,
        &mut third_bindings,
    )
    .expect("native transaction-3 B14");
    assert_eq!(
        (third_sig.vertices.len(), third_sig.edges.len()),
        (224, 213)
    );
    assert_eq!(third_bindings.stem_vertices[&2].0, 223);
    assert_eq!(third_base.graph_relation_identity, Some(212));
    let third_target_linked = second_cells
        .iter()
        .find(|cell| cell.reference == third.b_linker)
        .expect("transaction-3 target B cell")
        .linked;
    assert!(!third_target_linked);
    let mut third_flag_state = NativeStemsBeamVLinkBLinkerFlagState {
        system_id: 1,
        base_apply_state_before: third_base_state_before,
        target_b_linker: third.b_linker,
        linked: third_target_linked,
        committed: None,
    };
    let third_flag = apply_native_stems_beam_vlink_b_linker_flag_transaction(
        third_scheduler,
        &hydrated.plans,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &third_create,
        &third_live,
        hydrated.relation_parameters,
        &third_reuse,
        &third_base,
        &mut third_flag_state,
    )
    .expect("native transaction-3 B15");
    let mut third_cells = second_cells.clone();
    let third_siblings = apply_native_stems_beam_vlink_sibling_transaction_to_native_sig(
        &mut third_sig,
        &third_bindings,
        third_scheduler,
        &hydrated.stumps,
        &hydrated.vlinkers,
        &hydrated.reachability,
        &hydrated.builder,
        &third_base,
        &third_flag,
        &mut third_cells,
    )
    .expect("native transaction-3 B16");
    assert_eq!(
        third_siblings
            .graph
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![213]
    );
    assert_eq!(
        third_siblings
            .assigned_b_linkers
            .iter()
            .copied()
            .map(second_b_alias)
            .collect::<Vec<_>>(),
        vec!["beam:41:b:0"]
    );
    let mut third_s_cells = second_s_cells.clone();
    let third_heads = apply_native_stems_beam_vlink_head_transaction_to_native_sig(
        &mut third_sig,
        &third_bindings,
        third_scheduler,
        &hydrated.plans,
        &hydrated.builder,
        &hydrated.head_corners,
        &hydrated.reachability,
        &third_flag,
        &third_siblings,
        &third_cells,
        &mut third_s_cells,
    )
    .expect("native transaction-3 B17");
    assert_eq!(
        (third_sig.vertices.len(), third_sig.edges.len()),
        (224, 216)
    );
    assert_eq!(
        third_heads
            .appended_edges
            .iter()
            .map(|edge| edge.0)
            .collect::<Vec<_>>(),
        vec![214, 215]
    );
    assert_eq!(third_heads.s_linker_write_count, 2);
    assert_eq!(third_heads.s_linker_value_change_count, 2);
    let third_outer_resume = apply_native_stems_beam_outer_and_resume_transaction(
        third_scheduler,
        &hydrated.vlinkers,
        &hydrated.builder,
        &hydrated.plans,
        &hydrated.reachability,
        &third_flag,
        &third_siblings,
        &third_heads,
        &mut third_cells,
    )
    .expect("native transaction-3 B18/B19");
    let NativeStemsBeamSchedulerResumeStatus::AwaitingVLinkTransaction(fourth) =
        &third_outer_resume.resume.status
    else {
        panic!("transaction 3 did not reach a fourth frontier");
    };
    assert_eq!(fourth.plan.plan_ordinal, 627);
    assert_eq!(second_b_alias(fourth.b_linker), "beam:22:b:2");
    assert_eq!(fourth.vertical_side, NativeStemVerticalSide::Top);
    assert_eq!(carried_third.create, third_create);
    assert_eq!(carried_third.reuse_live_state, third_live);
    assert_eq!(carried_third.reuse, third_reuse);
    assert_eq!(carried_third.siblings, third_siblings);
    assert_eq!(carried_third.heads, third_heads);
    assert_eq!(carried_third.outer_resume, third_outer_resume);

    let txn2_sibling_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-txn2-sibling-links-chula.txt"),
    )
    .expect("expected-only transaction-2 B16 fixture");
    let txn2_sibling_result = txn2_sibling_text
        .lines()
        .find(|line| {
            line.starts_with("stemsbeamvlinksiblinglinksresult ")
                && line.contains(" system 1 plan 152 scope real case - ")
        })
        .expect("transaction-2 B16 result");
    assert!(txn2_sibling_result.contains(
        "siblings 2 committedEdges 2 committedEdgeAliases [sig-edge:208,sig-edge:209] committedFlags 2 committedBCells [beam:2:b:0,beam:3:b:0] eventCount 6"
    ));
    let txn2_head_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-txn2-head-links-chula.txt"),
    )
    .expect("expected-only transaction-2 B17 fixture");
    let txn2_head_result = txn2_head_text
        .lines()
        .find(|line| {
            line.starts_with("stemsbeamvlinkheadlinksresult ")
                && line.contains(" system 1 plan 152 scope real case - ")
        })
        .expect("transaction-2 B17 result");
    assert!(txn2_head_result.contains(
        "headEntries 2 duplicateEntries 0 relationsInserted 2 sWriteCount 2 sValueChangeCount 2 consistencyWriteCount 2 headAbnormalChangeCount 2 stemAbnormalChangeCount 1 dirtyCascadeCount 3"
    ));
    assert!(sides_pass_text.lines().any(|line| {
        line.starts_with("stemsbeamsidesloopstep ")
            && line.contains(
                " system 1 step 2 beamSig 22 bAlias beam:22:b:0 vSide TOP plan 618 stemProfile 4 linked true",
            )
    }));

    let txn2_base_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-txn2-base-apply-chula.txt"),
    )
    .expect("expected-only transaction-2 B14 fixture");
    let txn2_result = txn2_base_text
        .lines()
        .find(|line| {
            line.starts_with("stemsbeamvlinkbaseapplyresult ")
                && line.contains(" system 1 plan 152 scope real case - ")
        })
        .expect("transaction-2 B14 result");
    assert!(txn2_result.contains(
        "branch NewIdZero retainedDraftIdentity draft:152 retainedDraftGrade 0x1.ee2b2e53080fp-1/3feee2b2e53080f0 applyReturn true graphRelationIdentity sig-edge:207"
    ));
    assert!(txn2_result.contains(
        "finalStemInterId 2341 finalStemSigAttached true finalStemVip false finalStemAbnormal true finalBeamAbnormal false"
    ));

    let mut invalid_scheduler = hydrated.scheduler.clone();
    invalid_scheduler.status =
        audiveris_omr::native_stems_beam_scheduler::NativeStemsBeamSchedulerStatus::Completed {
            retained_for_stumps: Vec::new(),
            final_local_worklist: Vec::new(),
        };
    let before_failed_resume = cells.clone();
    assert!(
        apply_native_stems_beam_outer_and_resume_transaction(
            &invalid_scheduler,
            &hydrated.vlinkers,
            &hydrated.builder,
            &hydrated.plans,
            &hydrated.reachability,
            &hydrated.transaction,
            &actual,
            &head_actual,
            &mut cells,
        )
        .is_err()
    );
    assert_eq!(cells, before_failed_resume);

    // A late second-entry binding failure proves the first provisional S write,
    // edge, and abnormal callbacks cannot escape the clone-and-swap carrier.
    let mut rollback_sig = post_b16_sig.clone();
    let mut rollback_s_cells = pre_b17_s_cells.clone();
    let mut invalid_bindings = bindings.clone();
    let second_head = head_actual.steps[1].corner.head;
    invalid_bindings.head_vertices.remove(&second_head);
    assert!(
        apply_native_stems_beam_vlink_head_transaction_to_native_sig(
            &mut rollback_sig,
            &invalid_bindings,
            &hydrated.scheduler,
            &hydrated.plans,
            &hydrated.builder,
            &hydrated.head_corners,
            &hydrated.reachability,
            &hydrated.transaction,
            &actual,
            &cells,
            &mut rollback_s_cells,
        )
        .is_err()
    );
    assert_eq!(rollback_sig, post_b16_sig);
    assert_eq!(rollback_s_cells, pre_b17_s_cells);

    // Boundary-17 is expected-only from here onward. Compare the frozen
    // native-domain facts without importing Java aliases or persistent IDs.
    let head_text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-vlink-head-links-chula.txt"),
    )
    .expect("frozen B17 fixture");
    let real = |prefix: &str| {
        head_text
            .lines()
            .filter(|line| {
                line.starts_with(prefix) && line.contains(" system 1 plan 143 scope real case - ")
            })
            .collect::<Vec<_>>()
    };
    let entries = real("stemsbeamvlinkheadlinksheadentry ");
    let edges = real("stemsbeamvlinkheadlinksedge ");
    let callbacks = real("stemsbeamvlinkheadlinkscallback ");
    assert_eq!((entries.len(), edges.len(), callbacks.len()), (2, 2, 2));
    for (ordinal, (native, java)) in head_actual.steps.iter().zip(entries).enumerate() {
        assert!(java.contains(&format!(" mapOrdinal {ordinal} ")));
        assert!(java.contains(&format!(" headVertexOrdinal {} ", native.head_vertex.0)));
        assert!(java.contains(" sLinkedBefore false sClosedBefore false "));
        assert!(java.contains(" draftHeadSide LEFT "));
    }
    for (ordinal, (native, java)) in head_actual.steps.iter().zip(edges).enumerate() {
        assert!(java.contains(&format!(" mapOrdinal {ordinal} ")));
        assert!(java.contains(&format!(
            " graphInsertionOrdinal {} ",
            native.appended_edge.expect("linked edge").0
        )));
        assert!(java.contains(&format!(" sourceVertexOrdinal {} ", native.head_vertex.0)));
        assert!(java.contains(" targetVertexOrdinal 221 "));
    }
    assert!(callbacks[0].contains(
        " headAbnormalBefore true headAbnormalAfter false stemAbnormalBefore true stemAbnormalAfter false "
    ));
    assert!(callbacks[1].contains(
        " headAbnormalBefore true headAbnormalAfter false stemAbnormalBefore false stemAbnormalAfter false "
    ));
    let result = real("stemsbeamvlinkheadlinksresult ");
    assert_eq!(result.len(), 1);
    assert!(result[0].contains(
        " headEntries 2 duplicateEntries 0 relationsInserted 2 sWriteCount 2 sValueChangeCount 2 consistencyWriteCount 2 headAbnormalChangeCount 2 stemAbnormalChangeCount 1 dirtyCascadeCount 3 "
    ));

    // From this point onward the frozen B16 fixture is expected-only.
    let text = std::fs::read_to_string(
        repo_root().join("rust/oracle/stems-beam-vlink-sibling-links-chula.txt"),
    )
    .expect("frozen B16 fixture");
    let rows = parse_scaffold_fixture(&text).expect("B16 fixture parses");
    let transactions = validate_core_rows(&rows).expect("B16 core rows");
    let expected = transactions
        .iter()
        .find(|transaction| transaction.key.system == 1)
        .expect("system 1 B16 rows");
    let expected_members = expected
        .rows
        .iter()
        .filter(|row| row.kind == RowKind::GroupMember)
        .collect::<Vec<_>>();
    assert_eq!(expected_members.len(), actual.group_members.len());
    for (native, java) in actual.group_members.iter().zip(expected_members) {
        assert_eq!(native.member_ordinal, java.usize("memberOrdinal").unwrap());
        assert_eq!(
            native.cross,
            parse_point(java.value("verticalCross").unwrap()).unwrap()
        );
        assert_eq!(
            native.left_limit.to_bits(),
            parse_f64(java.value("leftLimit").unwrap())
                .unwrap()
                .to_bits()
        );
        assert_eq!(
            native.right_limit.to_bits(),
            parse_f64(java.value("rightLimit").unwrap())
                .unwrap()
                .to_bits()
        );
        assert_eq!(native.selected, java.bool("selected").unwrap());
        assert_eq!(
            native.sorted_ordinal,
            parse_optional_usize(java.value("sortedOrdinal").unwrap()).unwrap()
        );
        assert_eq!(native.removed_as_base, java.bool("baseIdentity").unwrap());
    }
    for native in &actual.siblings {
        let geometry =
            sibling_transaction_rows(expected, RowKind::Geometry, native.sibling_ordinal)
                .expect("one geometry row");
        let [geometry] = geometry.as_slice() else {
            panic!("expected one geometry row");
        };
        assert_eq!(
            native.geometry.as_ref().expect("linked geometry"),
            &expected_geometry_from_row(geometry).expect("expected geometry")
        );
    }
    assert!(actual.group_state_before[0].abnormal);
    assert!(!actual.group_state_after[0].abnormal);
    assert!(actual.group_state_before[1].abnormal);
    assert!(actual.group_state_after[1].abnormal);

    let sig_ordinal = hydrated
        .stumps
        .beams_by_abscissa
        .iter()
        .map(|beam| (beam.source, beam.sig_ordinal))
        .collect::<BTreeMap<_, _>>();
    let alias = |reference: NativeStemsBeamBLinkerRef| {
        format!(
            "beam:{}:b:{}",
            sig_ordinal[&reference.beam],
            reference.id - 1
        )
    };
    assert_eq!(alias(actual.base_b_linker), "beam:12:b:0");
    assert_eq!(
        actual
            .assigned_b_linkers
            .iter()
            .copied()
            .map(alias)
            .collect::<Vec<_>>(),
        vec!["beam:0:b:0", "beam:1:b:0"]
    );

    // Even malformed persistent-cell authority leaves both carrier products
    // byte-for-byte at the supplied pre-call state.
    let mut rollback_sig = post_b14_sig.clone();
    let mut rollback_cells = pre_b15_cells.clone();
    let missing = actual.assigned_b_linkers[1];
    rollback_cells.retain(|cell| cell.reference != missing);
    let invalid_before = rollback_cells.clone();
    assert!(
        apply_native_stems_beam_vlink_sibling_transaction_to_native_sig(
            &mut rollback_sig,
            &bindings,
            &hydrated.scheduler,
            &hydrated.stumps,
            &hydrated.vlinkers,
            &hydrated.reachability,
            &hydrated.builder,
            &native_base,
            &hydrated.transaction,
            &mut rollback_cells,
        )
        .is_err()
    );
    assert_eq!(rollback_sig, post_b14_sig);
    assert_eq!(rollback_cells, invalid_before);
}
